<?xml version="1.0" encoding="utf-8" standalone="yes"?><rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom" xmlns:content="http://purl.org/rss/1.0/modules/content/"><channel><title>Token Cost Tracking on RockB</title><link>https://baeseokjae.github.io/tags/token-cost-tracking/</link><description>Recent content in Token Cost Tracking on RockB</description><image><title>RockB</title><url>https://baeseokjae.github.io/images/og-default.png</url><link>https://baeseokjae.github.io/images/og-default.png</link></image><generator>Hugo</generator><language>en-us</language><lastBuildDate>Sat, 05 Sep 2026 19:04:09 +0000</lastBuildDate><atom:link href="https://baeseokjae.github.io/tags/token-cost-tracking/index.xml" rel="self" type="application/rss+xml"/><item><title>Frugal Tokens: How to Track Coding Agent Cost and Usage Per Session</title><link>https://baeseokjae.github.io/posts/frugal-tokens-coding-agent-cost-explorer-2026/</link><pubDate>Sat, 05 Sep 2026 19:04:09 +0000</pubDate><guid>https://baeseokjae.github.io/posts/frugal-tokens-coding-agent-cost-explorer-2026/</guid><description>Frugal Tokens is a free, local, read-only dashboard that shows token usage and cost per session across OpenCode, Claude Code, PI, Codex, and Cursor.</description><content:encoded><![CDATA[<p>Frugal Tokens is a free, local, read-only web dashboard that shows exactly how many tokens each of your AI coding agent sessions consumed and what those tokens cost, across OpenCode, Claude Code, PI, Codex, and Cursor. It runs entirely on your machine, never mutates your agent databases, and surfaces hidden costs like prompt-cache misses that most developers never see. This guide walks you through installing it, pointing it at your agents, and reading the per-session explorer to cut your coding agent spend.</p>
<h2 id="what-is-frugal-tokens-and-why-you-need-a-per-session-cost-explorer">What is Frugal Tokens and why you need a per-session cost explorer</h2>
<p>Most developers have no idea how much their AI coding agents actually cost. Your terminal shows you the code, but it rarely shows you the bill. Frugal Tokens is an open-source tool that fills that gap: it reads the local session data that OpenCode, Claude Code, PI (Apple), Codex, and Cursor already store on your machine and renders it into a per-session explorer of token usage and reported cost.</p>
<p>The tool appeared as a Show HN on August 19, 2026, earning 37 points from the Hacker News community. Reviewers called the per-session explorer &ldquo;the best part&rdquo; of the tool, and one commenter noted that independent, cross-harness data is &ldquo;super important for companies measuring ROI on token spend.&rdquo; The core value is simple: before you can reduce coding agent cost, you have to be able to see it.</p>
<h2 id="prerequisites--installing-deno-29-on-macos-or-linux">Prerequisites — installing Deno 2.9+ on macOS or Linux</h2>
<p>Frugal Tokens is written in Deno, so the only real prerequisite is a recent Deno runtime. The project requires <strong>Deno 2.9 or newer</strong>. If you do not have Deno installed, the official README recommends installing it with a single command:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-bash" data-lang="bash"><span style="display:flex;"><span>curl -fsSL https://deno.land/install.sh | sh
</span></span></code></pre></div><p>After the installer finishes, make sure the <code>deno</code> binary is on your PATH. You can verify the version with:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-bash" data-lang="bash"><span style="display:flex;"><span>deno --version
</span></span></code></pre></div><p>If the version prints 2.9.0 or higher, you are ready to move on. Frugal Tokens runs locally and serves a read-only web view at <code>localhost:9000</code>, so no cloud account, API key, or network dependency is required.</p>
<h2 id="first-setup--copy-envexample-build-and-start-the-local-server">First setup — copy .env.example, build, and start the local server</h2>
<p>Once Deno is ready, clone the repository and run the two-command setup. From the project directory:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-bash" data-lang="bash"><span style="display:flex;"><span>cp .env.example .env
</span></span><span style="display:flex;"><span>deno task build <span style="color:#f92672">&amp;&amp;</span> deno task start
</span></span></code></pre></div><p>That is the entire setup. The <code>cp</code> step creates your local configuration file, <code>deno task build</code> compiles the frontend, and <code>deno task start</code> launches the local server. When the server is running, open your browser to <code>http://localhost:9000</code> to see the dashboard.</p>
<p>If you are developing the tool itself rather than just using it, run <code>deno task dev</code> instead. Dev mode serves on port 5273 and computes its own API port, ignoring the <code>PORT</code> variable, so you can run a development instance alongside a production one without a port conflict.</p>
<h2 id="opening-the-dashboard-at-localhost9000-and-the-per-session-explorer">Opening the dashboard at localhost:9000 and the per-session explorer</h2>
<p>The dashboard at <code>localhost:9000</code> is organized around sessions. Each row represents one agent session, and clicking into a session opens the per-session explorer that HN users singled out as the tool&rsquo;s best feature.</p>
<p>Inside the explorer you get a turn-by-turn breakdown: how many tokens were sent in, how many were read from cache, how many were written to cache, how many came back as output, and what the whole exchange cost. This granularity is what separates Frugal Tokens from simpler spend trackers. As one HN commenter put it, tools like Codeburn &ldquo;only tell you spend not messages and everything else&rdquo; — Frugal Tokens gives you the full picture.</p>
<h2 id="pointing-frugal-tokens-at-your-agents--the-env-path-variables">Pointing Frugal Tokens at your agents — the .env path variables</h2>
<p>Frugal Tokens auto-discovers most agent data, but you can override where it looks using environment variables in your <code>.env</code> file. Each harness stores its data in a different place, and knowing these paths is the key to making the tool work with your exact setup.</p>
<table>
  <thead>
      <tr>
          <th>Harness</th>
          <th>Default storage location</th>
          <th>.env variable</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>OpenCode</td>
          <td><code>~/.local/share/opencode/opencode.db</code> (SQLite)</td>
          <td><code>OPENCODE_DB_PATH</code></td>
      </tr>
      <tr>
          <td>Claude Code</td>
          <td><code>~/.claude/projects/&lt;encoded-cwd&gt;</code> (JSONL transcripts)</td>
          <td><code>CLAUDE_CODE_PROJECT_PATH</code></td>
      </tr>
      <tr>
          <td>PI (Apple)</td>
          <td>PI session directory</td>
          <td><code>PI_SESSION_DIR</code></td>
      </tr>
      <tr>
          <td>Codex</td>
          <td>Codex session directory</td>
          <td><code>CODEX_SESSION_DIR</code></td>
      </tr>
      <tr>
          <td>Cursor</td>
          <td><code>~/.cursor/chats/&lt;workspace&gt;/&lt;agent&gt;/store.db</code> (SQLite)</td>
          <td><code>CURSOR_CHATS_PATH</code></td>
      </tr>
  </tbody>
</table>
<p>The OpenCode path points to a SQLite database at <code>~/.local/share/opencode/opencode.db</code>, with the schema observed on July 10, 2026 using OpenCode 1.17.x. Claude Code stores JSONL transcript files under <code>~/.claude/projects/&lt;encoded-cwd&gt;</code>, with the version observed being Claude Code 2.1.202. Cursor keeps per-agent SQLite stores under <code>~/.cursor/chats/&lt;workspace&gt;/&lt;agent&gt;/store.db</code>.</p>
<p>You can also change the API port with the <code>PORT</code> variable. If your agent data lives somewhere non-standard, set the matching variable to the real path and restart the server.</p>
<h2 id="understanding-the-cache-fields--uncachedinput-cacheread-cachewrite-and-derived-metrics">Understanding the cache fields — uncachedInput, cacheRead, cacheWrite, and derived metrics</h2>
<p>To read the dashboard correctly, you need to understand the cache fields. Frugal Tokens maps each harness&rsquo;s usage data into a common vocabulary. For Claude Code, the mapping is:</p>
<ul>
<li><code>input_tokens</code> → <code>uncachedInput</code></li>
<li><code>cache_read_input_tokens</code> → <code>cacheRead</code></li>
<li><code>cache_creation_input_tokens</code> → <code>cacheWrite</code></li>
<li><code>output_tokens</code> → <code>output</code></li>
</ul>
<p>From these raw fields, Frugal Tokens derives two higher-level metrics:</p>
<ul>
<li><strong>freshPrompt (New input)</strong> = input + reported cache write</li>
<li><strong>processed (Total activity)</strong> = input + cache read + cache write + output + reasoning</li>
</ul>
<p>The distinction matters because cached tokens are dramatically cheaper than uncached ones. A session that reads most of its context from cache costs a fraction of one that re-sends everything as fresh input. The <code>cacheRead</code> field is where you see the savings, and the <code>cacheWrite</code> field is where you see the cost of establishing a cache in the first place.</p>
<h2 id="spotting-hidden-prompt-cache-misses-and-idle-gap-ttl-costs-in-your-sessions">Spotting hidden prompt-cache misses and idle-gap TTL costs in your sessions</h2>
<p>The single most expensive hidden cost in AI coding agents is the prompt-cache miss. When you step away from a session for an hour or more, the prompt cache expires, and the next message re-sends the entire context at full uncached prices.</p>
<p>This is not a theoretical concern. On the Frugal Tokens HN thread, one user reported that a single TTL cache miss — responding roughly 1.5 hours later — cost <strong>$6 for one message</strong> in a Fable session. Another commenter admitted they had &ldquo;no idea how many cache misses were happening&rdquo; after stepping away for an hour or more, and used the tool to uncover workflow and build-pipeline slowdowns in heavy sessions.</p>
<p>The per-session explorer makes these misses visible. When you see a cost spike that correlates with an idle gap in the session timeline, that is a TTL cache miss. The fix is behavioral: either keep sessions short, resume them before the cache TTL expires, or redesign your prompts so the expensive context is not re-sent from scratch every time.</p>
<h2 id="comparing-token-usage-and-cost-across-harnesses-cross-harness-view">Comparing token usage and cost across harnesses (cross-harness view)</h2>
<p>One of Frugal Tokens&rsquo; most useful features is the cross-harness comparison. The same task burns different numbers of tokens depending on which agent you use, and the tool lets you see those differences side by side.</p>
<p>The demo surfaces clear tiering themes across models — Opus versus Sonnet versus Haiku, for example — showing that model choice has a direct, measurable effect on token consumption. For teams, this is independent verification that beats vendor claims. As one HN commenter put it, cross-harness token differences are &ldquo;independently-verified data points for ROI measurement.&rdquo; If you are deciding whether to standardize on one agent or let developers pick their own, this view gives you the data to make that call.</p>
<h2 id="optional--periodic-sync-with-frugal_tokens_sync_interval_seconds-and-changing-the-api-port">Optional — periodic sync with FRUGAL_TOKENS_SYNC_INTERVAL_SECONDS and changing the API port</h2>
<p>By default, Frugal Tokens syncs your agent data once at boot. If you want the dashboard to stay fresh while you work, set the <code>FRUGAL_TOKENS_SYNC_INTERVAL_SECONDS</code> variable in your <code>.env</code> file to a positive number of seconds. The server will then re-import your session data on that interval.</p>
<p>If the variable is unset or set to <code>0</code>, the tool performs boot-only sync and does not re-import. For most users, leaving it unset is fine — you can restart the server when you want a fresh view. For teams that keep the dashboard open all day, a sync interval of 60 or 300 seconds keeps the numbers current without much overhead.</p>
<h2 id="frugal-tokens-vs-the-field--agentsview-burnrate-codeburn-and-agentic-metric">Frugal Tokens vs the field — AgentsView, BurnRate, Codeburn, and Agentic Metric</h2>
<p>Frugal Tokens is not the only tool in this space, so it helps to know where it fits. The main alternatives are AgentsView, BurnRate, and Codeburn.</p>
<table>
  <thead>
      <tr>
          <th>Tool</th>
          <th>Focus</th>
          <th>Key difference from Frugal Tokens</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>Frugal Tokens</td>
          <td>Per-session token/cost explorer across 5 harnesses</td>
          <td>Read-only, local, granular cache-miss visibility</td>
      </tr>
      <tr>
          <td>AgentsView</td>
          <td>Browsing/searching/analyzing past sessions</td>
          <td>Auto-discovers dozens of agents; SQLite/Postgres/DuckDB backend</td>
      </tr>
      <tr>
          <td>BurnRate</td>
          <td>Team-wide cost observability</td>
          <td>Tracks subagent behavior; zero config; brew install; paid tiers</td>
      </tr>
      <tr>
          <td>Codeburn</td>
          <td>Spend tracking</td>
          <td>&ldquo;Only tells you spend, not messages&rdquo; — less granular</td>
      </tr>
  </tbody>
</table>
<p>AgentsView is a local-first desktop and web app that auto-discovers session data from dozens of agents including Claude Code, Codex, Gemini, Copilot, Cursor, Qwen, and DeepSeek, with token usage and cost reports plus a cached-versus-uncached breakdown. BurnRate is a free, local-first observability tool for Claude Code, Cursor, and Copilot that tracks subagent behavior and cost, with team-wide management and paid tiers. Codeburn is simpler but, as HN users noted, less granular.</p>
<h2 id="privacy-and-read-only-safety--why-local-first-matters-for-teams">Privacy and read-only safety — why local-first matters for teams</h2>
<p>Frugal Tokens is explicitly read-only. It never mutates your agent databases, and the Cursor capture sidecar is append-only. That design matters for two reasons.</p>
<p>First, it is safe. You can point it at your real session data without worrying that it will corrupt or alter anything. Second, it is private. No code leaves your machine, there is no cloud dependency, and no third party sees your prompts or code. For teams working on sensitive codebases, this local-first, read-only posture is a genuine advantage over hosted analytics tools.</p>
<h2 id="common-pitfalls--stale-paths-missing-session-tags-cursors-missing-cost-data-and-claudes-absent-reportedcost">Common pitfalls — stale paths, missing session tags, Cursor&rsquo;s missing cost data, and Claude&rsquo;s absent reportedCost</h2>
<p>A few gotchas trip up new users. The most common is a stale or wrong path in <code>.env</code> — if the dashboard shows no sessions, double-check that the path variables point at the real locations listed in the table above.</p>
<p>Cursor is a special case: it does not persist historical token usage or dollar cost in its chat stores. That means Frugal Tokens cannot show you cost data for Cursor sessions the way it can for OpenCode or Codex. The Cursor capture sidecar is append-only, so it records what it can without modifying Cursor&rsquo;s own data.</p>
<p>Claude Code is another special case. Its transcripts do not report dollar cost, so <code>reportedCost</code> is absent for Claude sessions. Frugal Tokens compensates by applying versioned model rate cards to emit a <code>computedCost</code> estimate. That estimate is a good approximation, but it is computed, not reported, so treat it accordingly.</p>
<h2 id="verdict--should-you-add-per-session-token-cost-tracking-to-your-workflow">Verdict — should you add per-session token cost tracking to your workflow?</h2>
<p>If you use AI coding agents regularly and care about what they cost, the answer is yes. Frugal Tokens is free, local, read-only, and takes about two minutes to set up. It gives you something most developers simply do not have: a clear, per-session view of token usage and cost across the agents you already use.</p>
<p>The hidden value is the cache-miss visibility. A single TTL cache miss can cost $6 for one message, and most people never see it coming. Once you can see where your money goes, you can act — by shortening sessions, resuming before cache expiry, or redesigning prompts to cut recurring cost. For individual developers and teams alike, that visibility is the first step to actually being frugal with tokens.</p>
<h2 id="faq">FAQ</h2>
<p><strong>Is Frugal Tokens free to use?</strong>
Yes. Frugal Tokens is an open-source tool that runs entirely on your local machine. There is no cloud service, no subscription, and no API key required.</p>
<p><strong>Which coding agents does Frugal Tokens support?</strong>
It supports at least five harnesses: OpenCode, Claude Code, PI (Apple), Codex, and Cursor. Each stores its session data locally, and Frugal Tokens reads that data to build the dashboard.</p>
<p><strong>Does Frugal Tokens modify my agent data?</strong>
No. Frugal Tokens is strictly read-only. It never mutates your agent databases, and the Cursor capture sidecar is append-only, so your data is never altered.</p>
<p><strong>Why is my Cursor session missing cost data?</strong>
Cursor does not persist historical token usage or dollar cost in its chat stores. Frugal Tokens can only show what the harness records, so Cursor sessions may lack the cost breakdown you see for other agents.</p>
<p><strong>What is a TTL cache miss and why is it expensive?</strong>
A TTL cache miss happens when a prompt cache expires — often after you step away from a session for an hour or more. The next message re-sends the entire context at full uncached prices. One HN user reported a single TTL miss costing $6 for one message.</p>
]]></content:encoded></item><item><title>Agent Token Cost Attribution: A Practical Guide for 2026</title><link>https://baeseokjae.github.io/posts/agent-token-cost-attribution-2026/</link><pubDate>Fri, 19 Jun 2026 12:00:00 +0000</pubDate><guid>https://baeseokjae.github.io/posts/agent-token-cost-attribution-2026/</guid><description>Learn how to attribute AI agent token costs per user, feature, and agent run in 2026 — tools, tagging strategies, and the EU AI Act compliance deadline.</description><content:encoded><![CDATA[<p>Agentic coding tasks consume up to <strong>1,000× more tokens</strong> than equivalent chat sessions, input tokens (not output) dominate the bill even with prompt caching enabled, and the same task can vary by <strong>30× in total token consumption</strong> across runs with no correlation to output quality — yet most teams still have zero per-agent, per-feature cost attribution. Fixing that starts with tagging every LLM request at the SDK call site.</p>
<hr>
<h2 id="why-token-cost-attribution-matters-more-in-2026-than-ever">Why Token Cost Attribution Matters More in 2026 Than Ever</h2>
<p>Enterprise GenAI spend hit $37B in 2025, up from $11.5B the year before. 80% of companies exceeded their AI cost forecasts by 25% or more (<a href="/posts/ai-coding-cost-per-developer-2026/">full breakdown</a>). The old approach — a single API key for the whole team, one line item on the AWS bill — no longer works when agents are running unsupervised loops that burn 25,000–35,000 tokens per turn across 50-turn sessions.</p>
<p>Three structural shifts make attribution urgent in 2026:</p>
<ul>
<li><strong>Agentic workloads are 5–30× more expensive than chat</strong> (Gartner, Mar 2026). A single agent run can cost more than a month of casual chatbot usage.</li>
<li><strong>Token consumption is stochastic</strong>. Bai et al. (UMich, Stanford, Google DeepMind, MIT) found the same agentic coding task varied 30× in total tokens between runs. You cannot predict cost from task description alone.</li>
<li><strong>EU AI Act Article 12 takes full effect August 2, 2026</strong>. You will need an audit trail of model calls per user, per system, per time period. Without attribution baked in now, August will be a scramble.</li>
</ul>
<p>I have found that teams that implement cost attribution before they hit $10K/month in API spend consistently keep their FinOps under control. Teams that wait until they see a surprise $50K bill end up reverse-engineering logs for weeks.</p>
<h2 id="what-the-research-actually-says-about-agent-token-consumption">What the Research Actually Says About Agent Token Consumption</h2>
<p>The most comprehensive study of agent token economics to date is Bai et al. (arXiv:2604.22750, Apr 2026), which analyzed token consumption across thousands of agentic coding runs. Their findings should change how you think about cost:</p>
<table>
  <thead>
      <tr>
          <th>Finding</th>
          <th>Implication</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>Input tokens dominate total cost (even with caching)</td>
          <td>Don&rsquo;t obsess over output length — optimize what you send <em>in</em></td>
      </tr>
      <tr>
          <td>Same task → 30× variance in token usage</td>
          <td>Per-task pricing is impossible; you need run-level attribution</td>
      </tr>
      <tr>
          <td>Zero correlation between token consumption and accuracy</td>
          <td>Throwing more context at a problem does not fix bad prompts</td>
      </tr>
      <tr>
          <td>LLMs systematically underestimate their own token usage (max r = 0.39)</td>
          <td>Never trust the model&rsquo;s &ldquo;this will take about N tokens&rdquo; estimate</td>
      </tr>
  </tbody>
</table>
<p>The second paper, Salim et al. &ldquo;Tokenomics&rdquo; (arXiv:2601.14470, Jan 2026), provides a distribution analysis of where tokens go in multi-agent systems. Their key takeaway: coordinator agents (the ones that route tasks to sub-agents) consume 40–60% of total token budget in a multi-agent setup, not the worker agents doing the actual work.</p>
<h3 id="why-accuracy-peaks-at-intermediate-cost">Why Accuracy Peaks at Intermediate Cost</h3>
<p>This is the counter-intuitive finding: using more tokens does not make the agent more correct. In fact, accuracy peaks at intermediate cost levels and drops at both the low end (too little context to understand the task) and the high end (context that drowns the signal). If you see a run consuming 3× the median for your task, do not assume it will be 3× better. Kill it and restart with a tighter scope.</p>
<hr>
<h2 id="the-core-architecture-of-token-cost-attribution">The Core Architecture of Token Cost Attribution</h2>
<p>Attribution is three layers stacked on top of each other. Most teams implement (1) and skip (2) and (3), which is why their &ldquo;cost dashboard&rdquo; shows a big blob of unclassified spend.</p>
<h3 id="layer-1-tag-every-request-at-the-sdk-call-site">Layer 1: Tag Every Request at the SDK Call Site</h3>
<p>Do not tag post-hoc from logs. Logs lose context — you will not be able to reconstruct which user, which feature, or which agent run triggered a given API call three weeks later. Tag at the SDK level:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-python" data-lang="python"><span style="display:flex;"><span><span style="color:#75715e"># Anthropic SDK example</span>
</span></span><span style="display:flex;"><span>client <span style="color:#f92672">=</span> anthropic<span style="color:#f92672">.</span>Anthropic()
</span></span><span style="display:flex;"><span>response <span style="color:#f92672">=</span> client<span style="color:#f92672">.</span>messages<span style="color:#f92672">.</span>create(
</span></span><span style="display:flex;"><span>    model<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;claude-sonnet-4-20260514&#34;</span>,
</span></span><span style="display:flex;"><span>    max_tokens<span style="color:#f92672">=</span><span style="color:#ae81ff">4096</span>,
</span></span><span style="display:flex;"><span>    system<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;You are a code review assistant.&#34;</span>,
</span></span><span style="display:flex;"><span>    messages<span style="color:#f92672">=</span>[{<span style="color:#e6db74">&#34;role&#34;</span>: <span style="color:#e6db74">&#34;user&#34;</span>, <span style="color:#e6db74">&#34;content&#34;</span>: prompt}],
</span></span><span style="display:flex;"><span>    <span style="color:#75715e"># Custom headers for attribution</span>
</span></span><span style="display:flex;"><span>    extra_headers<span style="color:#f92672">=</span>{
</span></span><span style="display:flex;"><span>        <span style="color:#e6db74">&#34;X-Attribution-User-ID&#34;</span>: user_id,
</span></span><span style="display:flex;"><span>        <span style="color:#e6db74">&#34;X-Attribution-Feature&#34;</span>: <span style="color:#e6db74">&#34;pr-review&#34;</span>,
</span></span><span style="display:flex;"><span>        <span style="color:#e6db74">&#34;X-Attribution-Agent-Run-ID&#34;</span>: run_id,
</span></span><span style="display:flex;"><span>        <span style="color:#e6db74">&#34;X-Attribution-Deployment&#34;</span>: <span style="color:#e6db74">&#34;production&#34;</span>,
</span></span><span style="display:flex;"><span>        <span style="color:#e6db74">&#34;X-Attribution-Prompt-Version&#34;</span>: <span style="color:#e6db74">&#34;v2.3&#34;</span>,
</span></span><span style="display:flex;"><span>    }
</span></span><span style="display:flex;"><span>)
</span></span></code></pre></div><p>The fields break into two categories:</p>
<ul>
<li><strong>Low-cardinality</strong> (feature, deployment, prompt version) — use these for alerts and dashboards</li>
<li><strong>High-cardinality</strong> (user_id, agent_run_id) — use these for drill-down investigations</li>
</ul>
<p>Low-cardinality fields change slowly — you can alert when &ldquo;pr-review&rdquo; spikes 2× above baseline. High-cardinality fields are how you answer &ldquo;which user generated that $500 spike yesterday?&rdquo;</p>
<h3 id="layer-2-proxy-level-capture">Layer 2: Proxy-Level Capture</h3>
<p>If you use a gateway (Helicone, Portkey, LiteLLM, FutureAGI), the proxy can inject tags and capture response metadata automatically. This is essential for models that do not support custom headers natively. The gateway should log:</p>
<ul>
<li>Model name and version</li>
<li>Input/output/cached token counts</li>
<li>Latency and tool call counts</li>
<li>Cost at current pricing (update your price table at least monthly — vendors change pricing faster than you expect)</li>
</ul>
<h3 id="layer-3-cost-roll-ups">Layer 3: Cost Roll-Ups</h3>
<p>Once you have tagged and captured, build these five roll-ups in order:</p>
<ol>
<li><strong>Cost per user per day</strong> — the baseline. Spikes here trigger the first investigation.</li>
<li><strong>Cost per feature request</strong> — tells you whether PR reviews cost more than code generation. Use this to prioritize optimization effort.</li>
<li><strong>Cost per agent run (median + p99)</strong> — the variance here is where budget surprises hide. A median of $0.50 with a p99 of $12 means something is wrong with your task scoping.</li>
<li><strong>Cost per successful eval</strong> — for teams running eval-driven development, this is the north star metric.</li>
<li><strong>Cost per customer (B2B multi-tenant)</strong> — for SaaS products reselling AI features, this is your gross margin line item.</li>
</ol>
<hr>
<h2 id="tool-comparison-best-platforms-for-token-cost-attribution-in-2026">Tool Comparison: Best Platforms for Token Cost Attribution in 2026</h2>
<p>Every tool on this list works, but they optimize for different workflows. Pick based on whether you need observability, gateway routing, FinOps, or billing integration.</p>
<table>
  <thead>
      <tr>
          <th>Tool</th>
          <th>Category</th>
          <th>Pricing</th>
          <th>Best For</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td><strong>FutureAGI</strong></td>
          <td>Unified platform</td>
          <td>Free + usage from $5/100K reqs</td>
          <td>Teams wanting cost + eval + gateway in one loop</td>
      </tr>
      <tr>
          <td><strong>Helicone</strong></td>
          <td>Gateway proxy</td>
          <td>Hobby free, Pro $79/mo</td>
          <td>Drop-in per-request cost with zero markup</td>
      </tr>
      <tr>
          <td><strong>Langfuse</strong></td>
          <td>Observability</td>
          <td>Hobby free, Core $29/mo</td>
          <td>Span-level cost with sub-token-type granularity</td>
      </tr>
      <tr>
          <td><strong>Braintrust</strong></td>
          <td>Experiment-first</td>
          <td>Starter free, Pro $249/mo</td>
          <td>CI-gated cost regression checks</td>
      </tr>
      <tr>
          <td><strong>Datadog</strong></td>
          <td>APM observability</td>
          <td>Per span/request volume</td>
          <td>Orgs already on Datadog infra</td>
      </tr>
      <tr>
          <td><strong>Arize Phoenix</strong></td>
          <td>OTel-native</td>
          <td>Free self-host, AX Pro $50/mo</td>
          <td>Portable OpenInference schema</td>
      </tr>
      <tr>
          <td><strong>Portkey</strong></td>
          <td>Gateway + routing</td>
          <td>Free + paid from $49/mo</td>
          <td>Per-key budget caps and failover</td>
      </tr>
      <tr>
          <td><strong>Vantage</strong></td>
          <td>Cloud FinOps</td>
          <td>Usage-based</td>
          <td>Cross-cloud AI cost + anomaly detection</td>
      </tr>
      <tr>
          <td><strong>OpenMeter</strong></td>
          <td>Event-driven metering</td>
          <td>OSS self-host</td>
          <td>Reselling AI features with Stripe billing</td>
      </tr>
      <tr>
          <td><strong>Torii</strong></td>
          <td>SaaS governance</td>
          <td>Enterprise</td>
          <td>Discovering shadow AI accounts per employee</td>
      </tr>
  </tbody>
</table>
<h3 id="how-to-choose">How to Choose</h3>
<p>In practice, I have seen the decision tree work like this:</p>
<ul>
<li><strong>Already on Datadog?</strong> Use Datadog LLM Observability. The infra cost correlation is valuable, and the marginal effort is low.</li>
<li><strong>Self-hosting or OSS-first?</strong> Langfuse (MIT core) or Arize Phoenix (ELv2). Both support the OpenInference semantic convention for portable cost tracking.</li>
<li><strong>Building a product that resells AI?</strong> OpenMeter + Stripe. Its ClickHouse-backed real-time aggregations are purpose-built for usage-based billing.</li>
<li><strong>Need guardrails and routing alongside attribution?</strong> Portkey or FutureAGI. Portkey is stronger on per-key budget caps; FutureAGI is stronger on eval loops.</li>
<li><strong>CI-gated cost regression?</strong> Braintrust. Its experiment-first model ties cost to test runs, not production traffic.</li>
</ul>
<h3 id="important-acquisition-updates">Important Acquisition Updates</h3>
<ul>
<li><strong>Helicone</strong> was acquired by Mintlify (Mar 2026). Maintenance mode with security updates only — do not build new integrations expecting feature development.</li>
<li><strong>Portkey</strong> was acquired by Palo Alto Networks (2025). Security-first roadmap; expect stronger IAM and audit features.</li>
<li><strong>Langfuse</strong> acquired ClickHouse (2026) and is investing heavily in on-premise deployments for regulated industries.</li>
</ul>
<hr>
<h2 id="enforcement-hard-caps-soft-alerts-kill-switches">Enforcement: Hard Caps, Soft Alerts, Kill Switches</h2>
<p>Attribution without enforcement is just a pretty dashboard. You need three tiers of cost control:</p>
<h3 id="hard-cap-per-user">Hard Cap per User</h3>
<p>Set this at the proxy/middleware layer, not the application layer. A per-user daily hard cap prevents a single runaway agent run from burning the monthly budget. Portkey and FutureAGI both support this natively. If you are self-hosted with LiteLLM, add a middleware check:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-python" data-lang="python"><span style="display:flex;"><span><span style="color:#66d9ef">async</span> <span style="color:#66d9ef">def</span> <span style="color:#a6e22e">cost_middleware</span>(request: Request, user_id: str):
</span></span><span style="display:flex;"><span>    daily_cost <span style="color:#f92672">=</span> <span style="color:#66d9ef">await</span> get_daily_cost(user_id)
</span></span><span style="display:flex;"><span>    <span style="color:#66d9ef">if</span> daily_cost <span style="color:#f92672">&gt;</span> USER_DAILY_HARD_CAP:
</span></span><span style="display:flex;"><span>        <span style="color:#66d9ef">raise</span> HTTPException(status_code<span style="color:#f92672">=</span><span style="color:#ae81ff">429</span>, detail<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;Daily budget exhausted&#34;</span>)
</span></span><span style="display:flex;"><span>    <span style="color:#66d9ef">return</span> <span style="color:#66d9ef">await</span> call_next(request)
</span></span></code></pre></div><h3 id="soft-alert-per-feature">Soft Alert per Feature</h3>
<p>Set a rolling baseline for each feature tag. If &ldquo;code-review&rdquo; averages $50/day and suddenly hits $120, fire an alert. Do not page on this — it is an investigation trigger, not an incident. Langfuse and Datadog both support percentile-based alerting.</p>
<h3 id="kill-switch-on-agent-runs">Kill Switch on Agent Runs</h3>
<p>This is the most important control for agentic workloads. Define per-agent-run limits for:</p>
<ul>
<li><strong>Total token count</strong> — hard stop at, say, 500K tokens</li>
<li><strong>Tool call count</strong> — if the agent has tried 50 tools and still has not produced a result, something is wrong</li>
<li><strong>Span depth</strong> — prevent agents from spawning sub-agents that spawn sub-agents</li>
</ul>
<p>Most agent frameworks (LangGraph, CrewAI, OpenAIAgents SDK v2) support these natively now. If your framework does not, wrap each run with a timeout/limit context manager.</p>
<hr>
<h2 id="common-mistakes-that-ruin-cost-attribution">Common Mistakes That Ruin Cost Attribution</h2>
<p>I have seen every one of these in production, and each one costs real money to fix retroactively.</p>
<p><strong>Not tagging every request.</strong> The biggest single mistake. If even 5% of your requests go untagged, your &ldquo;uncategorized&rdquo; bucket grows until it is larger than any tagged bucket, and the dashboard becomes useless. Fix: make tagging mandatory at the SDK wrapper layer, not a developer-side convention.</p>
<p><strong>Stale price tables.</strong> Vendor pricing changes monthly. Claude Opus 4.7 launched at $15/MTok input; Sonnet 4.6 is $3/MTok; GPT-5.5 batch pricing is different from real-time pricing. If your cost dashboard uses prices from three months ago, every cost number is wrong. Automate price table updates from provider APIs or use a gateway that handles this for you.</p>
<p><strong>Ignoring cached vs. uncached input pricing.</strong> Prompt caching cuts cached input costs by 50–90% depending on provider. If you charge every input token at the full rate, you overstate costs for cached-heavy workloads by 2–10×. Track <code>cache_read_input_tokens</code> (Anthropic) and <code>cached_tokens</code> (OpenAI) separately.</p>
<p><strong>Single-dimension slicing only.</strong> A dashboard that shows only cost-per-model tells you nothing about which <em>feature</em> or which <em>customer</em> is expensive. You need at least two dimensions (feature + model, user + feature) to spot the real patterns.</p>
<p><strong>No spike alerts.</strong> Without alerts, your first signal that attribution is broken is the monthly invoice. By then, the expensive run happened three weeks ago and the logs have already rotated. Set per-feature spike alerts at 2× the 7-day rolling average.</p>
<p><strong>Skipping storage retention planning.</strong> Token cost data grows fast — a team of 20 developers running 10 agent sessions per day generates about 200 MB of raw cost records per month. Plan for 12 months of hot storage and 3+ years of cold storage for EU AI Act compliance.</p>
<hr>
<h2 id="eu-ai-act-article-12-the-compliance-deadline">EU AI Act Article 12: The Compliance Deadline</h2>
<p>Article 12 of the EU AI Act requires providers and deployers of high-risk AI systems to maintain automatic logging of:</p>
<ul>
<li>Each operational run (timestamps, input/output)</li>
<li>User identification</li>
<li>System version</li>
<li>Model calls per time period</li>
</ul>
<p>The full enforcement date is <strong>August 2, 2026</strong>. If your AI agents handle any EU user data, your cost attribution system is also your compliance system. The same tags you use for cost roll-ups (user_id, feature, deployment, agent_run_id) satisfy Article 12&rsquo;s audit trail requirements.</p>
<p>The practical implication: you need to keep cost/audit records for at least 12 months (some member states require longer), and those records must be exportable on demand. If your current observability stack purges data after 30 days on the free tier, upgrade now.</p>
<hr>
<h2 id="putting-it-all-together-a-30-day-implementation-plan">Putting It All Together: A 30-Day Implementation Plan</h2>
<p>Day 1–3: <strong>Audit current tagging</strong>. Run a query against your last 30 days of API logs. What percentage of requests carry user_id and feature tags? If it is under 80%, start with the SDK wrapper fix.</p>
<p>Day 4–7: <strong>Choose and deploy a gateway or observability layer</strong>. Do not build this yourself. Pick from the tool table above based on your infra. Deploy in shadow mode (log only, no enforcement) for the first week.</p>
<p>Day 8–14: <strong>Build the five cost roll-ups</strong>. Cost per user per day, cost per feature, cost per agent run (median + p99), cost per eval, cost per customer. Tag these as dashboards in whatever observability platform you chose.</p>
<p>Day 15–21: <strong>Set enforcement layers</strong>. Hard caps per user, soft alerts per feature, kill switches on agent runs. Test the kill switches on a staging agent with a deliberately unbounded task.</p>
<p>Day 22–30: <strong>Validate against real data</strong>. Run the Bai et al. variance test: give three agents the same task and see the token variance. If it is &gt;5×, your task scoping needs work. Set the p99 cost per agent run as your primary budget KPI.</p>
<hr>
<h2 id="faq">FAQ</h2>
<h3 id="what-is-agent-token-cost-attribution-and-why-do-i-need-it">What is agent token cost attribution and why do I need it?</h3>
<p>Agent token cost attribution is the practice of tagging every LLM API request with metadata (user, feature, agent run ID, deployment) so you can answer &ldquo;who spent what, on which feature, in which agent run.&rdquo; Without it, your monthly API invoice is one opaque number. With it, you can spot cost anomalies, enforce budgets per user, and comply with EU AI Act Article 12 logging requirements.</p>
<h3 id="which-tools-are-best-for-token-cost-attribution-in-2026">Which tools are best for token cost attribution in 2026?</h3>
<p>The best tool depends on your infra. For teams already on Datadog, use Datadog LLM Observability. For self-hosted OSS, use Langfuse or Arize Phoenix. For gateway + attribution in one box, use Portkey or FutureAGI. For CI-gated cost tracking, use Braintrust. For building a product that resells AI features, use OpenMeter + Stripe.</p>
<h3 id="how-much-does-agent-token-consumption-vary-between-runs-of-the-same-task">How much does agent token consumption vary between runs of the same task?</h3>
<p>Bai et al. (arXiv:2604.22750) found up to 30× variance in total token consumption for the same agentic coding task across different runs. This is because agents make different tool-calling decisions each time, leading to different context accumulation patterns. Median + p99 cost per agent run is the only reliable metric.</p>
<h3 id="what-is-the-eu-ai-act-article-12-deadline-and-how-does-it-relate-to-cost-attribution">What is the EU AI Act Article 12 deadline and how does it relate to cost attribution?</h3>
<p>Article 12 takes full effect August 2, 2026, requiring automatic logging of model calls per user, per system, per time period for high-risk AI systems. The same tags you implement for cost attribution (user_id, feature, agent_run_id) satisfy Article 12&rsquo;s audit trail requirements. There is no reason to build separate systems.</p>
<h3 id="how-often-should-i-update-my-llm-pricing-tables">How often should I update my LLM pricing tables?</h3>
<p>At least monthly. Vendor pricing changes frequently — new models launch at different rates, batch pricing discounts change, and prompt caching pricing evolves. If your cost dashboard uses prices from more than 30 days ago, every calculated cost number is unreliable. Use a gateway that auto-updates pricing, or write a weekly cron job.</p>
]]></content:encoded></item></channel></rss>