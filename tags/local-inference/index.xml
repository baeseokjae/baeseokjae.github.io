<?xml version="1.0" encoding="utf-8" standalone="yes"?><rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom" xmlns:content="http://purl.org/rss/1.0/modules/content/"><channel><title>Local Inference on RockB</title><link>https://baeseokjae.github.io/tags/local-inference/</link><description>Recent content in Local Inference on RockB</description><image><title>RockB</title><url>https://baeseokjae.github.io/images/og-default.png</url><link>https://baeseokjae.github.io/images/og-default.png</link></image><generator>Hugo</generator><language>en-us</language><lastBuildDate>Fri, 18 Sep 2026 01:01:39 +0000</lastBuildDate><atom:link href="https://baeseokjae.github.io/tags/local-inference/index.xml" rel="self" type="application/rss+xml"/><item><title>Unsloth Dynamic GGUF 3.0: Faster Local Inference Without Sacrificing Accuracy</title><link>https://baeseokjae.github.io/posts/unsloth-dynamic-3-0-ggufs/</link><pubDate>Fri, 18 Sep 2026 01:01:39 +0000</pubDate><guid>https://baeseokjae.github.io/posts/unsloth-dynamic-3-0-ggufs/</guid><description>Unsloth Dynamic 3.0 GGUFs run Qwen3.8-27B-class models on consumer GPUs with &amp;gt;10% better accuracy at the same size. Here&amp;#39;s how to pick the right quant.</description><content:encoded><![CDATA[<p>Unsloth Dynamic GGUF 3.0 is the latest iteration of Unsloth&rsquo;s quantization format for running large models locally. It delivers more than 10% better top-1% accuracy at the same file size as every other GGUF provider, works with llama.cpp and Unsloth Desktop, and makes a 27B-parameter model runnable on as little as 7-8GB of RAM. This review explains what changed, how the quality claims are measured, and exactly which quant to pick for your hardware.</p>
<h2 id="what-is-unsloth-dynamic-30-gguf">What Is Unsloth Dynamic 3.0 GGUF?</h2>
<p>Unsloth Dynamic 3.0 GGUF is a post-training quantization methodology that shrinks large language models into small GGUF files for local inference. It is the successor to Dynamic v2.0 and is built specifically for MoE (mixture-of-experts) and dense models that need to fit on consumer hardware without crashing quality. The flagship release targets Qwen3.8-27B, with support expanded to Meta Muse, Glimmer, DeepSeek-V4-Pro, and NVIDIA Nemotron 3.5.</p>
<p>The core claim is simple to state and hard to beat: Unsloth says its Dynamic v3.0 quants deliver more than 10% better top-1% accuracy at the same size versus every other provider&rsquo;s GGUFs, and that the format is a genuine upgrade over Dynamic v2.0. What makes this notable is that the improvement comes purely from post-training quantization — no quantized-aware training (QAT), no quantized-aware distillation (QAD), and no training on the calibration set. Unsloth calls the 1-bit version &ldquo;UD-IQ1_S,&rdquo; a 6.2GB file that keeps roughly 72% of top-1% accuracy while being 89% smaller than full precision.</p>
<p>Two pieces of context frame the release. First, scale: over 5.1 million Unsloth Qwen3.8 downloads happened in just five days after the models landed. Second, compatibility: the GGUFs run in llama.cpp and in Unsloth Desktop, which is the first local application that can both run and train models. Together these make Dynamic 3.0 the practical default for people who want high-quality local inference on a normal workstation.</p>
<h2 id="dynamic-v30-vs-v20--what-actually-changed">Dynamic v3.0 vs v2.0 — What Actually Changed</h2>
<p>Dynamic v2.0 introduced the core idea: instead of applying one quantization level across an entire model, Unsloth selectively quantizes each layer differently, per model, calibrated on a diverse high-quality dataset of more than 1.5 million tokens. On Qwen3.8, the v2.0 baseline top-1% efficiency scores were IQ2_M at 66.47, Q2_K_XL at 68.70, Q3_K_XL at 70.87, and Q4_K_XL at 71.47.</p>
<p>Dynamic v3.0 keeps the larger quants on the v2.0 methodology while introducing an improved pipeline for the smaller sizes. Three changes drive the gain:</p>
<ul>
<li>Higher-quality imatrix calibration data, refined specifically for agentic coding, chat, and multilingual workloads.</li>
<li>Better layer selection, choosing which layers to compress hardest and which to protect.</li>
<li>More quantization techniques applied to squeeze extra accuracy-per-GB.</li>
</ul>
<p>A practical example of the size benefit: smaller v3.0 quants remove the MTP (multi-token prediction) module to save roughly 500MB. That is meaningful because it lets a borderline machine fit a quant that previously would have spilled into swap.</p>
<p>The headline improvement matters most at the low end. The 1-bit UD-IQ1_S quant keeps roughly 72% top-1% accuracy in a 6.2GB file, and independent analysis reports the 1-bit version holding 77% accuracy on Unsloth&rsquo;s held-out eval while fitting in 7-8GB of RAM. For users with an 8GB machine — which historically could barely run a passable small model — that is the real story of v3.0.</p>
<h2 id="how-unsloth-proves-quality-the-divergence-300-32-metric">How Unsloth Proves Quality: The Divergence-300 @32 Metric</h2>
<p>A common objection to GGUF quality claims is that single-token accuracy measurements miss what actually breaks in real use. Unsloth addresses this with a new metric called Divergence-300 @32.</p>
<p>The metric works by holding out 300 example prompts drawn from Terminal-Bench 2.1, DeepSWE, Harbor, MathArena 2025-26, and non-Latin/long-document prompts. The quantized model is run with greedy argmax decoding over 32 tokens, and its output is compared against the BF16 reference. Because 32 tokens capture far more of the model&rsquo;s actual behavior than a single token, Divergence-300 @32 is a much stronger test of whether a quant has drifted on real agentic and coding tasks.</p>
<table>
  <thead>
      <tr>
          <th>Metric feature</th>
          <th>Single-token top-1%</th>
          <th>Divergence-300 @32</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>Tokens compared per sample</td>
          <td>1</td>
          <td>32</td>
      </tr>
      <tr>
          <td>Task coverage</td>
          <td>Usually general text</td>
          <td>Coding, agentic, math, multilingual, long-doc</td>
      </tr>
      <tr>
          <td>Sensitivity to drift</td>
          <td>Low</td>
          <td>High</td>
      </tr>
      <tr>
          <td>Representative of real use</td>
          <td>Weak</td>
          <td>Strong</td>
      </tr>
  </tbody>
</table>
<p>Two caveats are worth keeping in mind. First, the numbers are self-reported by Unsloth. Second, Divergence-300 @32 scores are not blanket &ldquo;quality grades&rdquo; — a divergence score is about how faithful a quant is to the parent model, not whether the model is good at a task in absolute terms. Still, for comparing quantizations of the same model, it is a materially better signal than top-1% alone.</p>
<p>Unsloth also argues the results do not overfit: the quants are calibrated on one dataset and validated on held-out Wikitext and Code, plus the Divergence-300 set. That split is the right shape for a believable accuracy claim.</p>
<h2 id="qwen38-27b-dynamic-v30-release--the-flagship-quants">Qwen3.8-27B Dynamic v3.0 Release — The Flagship Quants</h2>
<p>The flagship of the Dynamic 3.0 release is the Qwen3.8-27B family, an Apache-2.0 model from Hugging Face with flexible thinking mode and vision-language understanding for both images and video. The GGUF repository on Hugging Face hosts the Dynamic 3.0 quants, and it adds two capabilities directly useful to developers: developer-role support so the model works in agentic tools like Codex, and nested-object parsing improvements for tool calling.</p>
<p>The benchmark picture for the model itself is strong. Independent analysis reports Qwen3.8-27B nearly matching Anthropic&rsquo;s Opus 4.6 Max on several agentic benchmarks: SWE-bench Pro 61.7 versus 53.4, DeepSWE 1.1 at 42.2, and QwenSWEBench 79.0 versus 63.8. Those scores matter because they mean a quantized model in this class is genuinely useful for coding and agent loops, not just chatbot chit-chat.</p>
<p>Two specific quants stand out in the marketing:</p>
<ul>
<li>The 1-bit UD-IQ1_S at 6.2GB retains roughly 72% top-1% accuracy and is 89% smaller than full precision. Independent sources report 77% accuracy on the held-out eval while running in 7-8GB.</li>
<li>The 9.83GB UD-Q2_K_XL is about 8% more accurate on top-1% than the next-best provider&rsquo;s quant at the same size — and in a noted real-world test it built a working HTML program with only one small JavaScript bug.</li>
</ul>
<p>These are the quants that make a 27B-class model a realistic local possibility on a gaming laptop or a compact desktop.</p>
<h2 id="which-quant-should-you-run-by-ramvram">Which Quant Should You Run (by RAM/VRAM)</h2>
<p>The single most important rule when choosing a quant is to pick by your total RAM plus VRAM, not by the highest bit depth you can find. A quant that does not fit into memory will swap, and swapped inference is slower and more error-prone than a smaller quant that stays resident.</p>
<p>For Qwen3.8-27B, Unsloth and independent guides map bit depths to hardware as follows:</p>
<table>
  <thead>
      <tr>
          <th>Quant</th>
          <th>Approx size / RAM needed</th>
          <th>Target hardware</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>UD-IQ1_S (1-bit)</td>
          <td>6.2GB / 7-8GB</td>
          <td>8GB machine, tiny GPU</td>
      </tr>
      <tr>
          <td>2-bit</td>
          <td>9-11GB</td>
          <td>12GB laptop</td>
      </tr>
      <tr>
          <td>3-bit</td>
          <td>12-14GB</td>
          <td>16GB mid-range</td>
      </tr>
      <tr>
          <td>4-bit (sweet spot)</td>
          <td>16-19GB</td>
          <td>16-24GB GPU</td>
      </tr>
      <tr>
          <td>6-bit</td>
          <td>23-26GB</td>
          <td>24-32GB workstation</td>
      </tr>
      <tr>
          <td>8-bit</td>
          <td>31GB+</td>
          <td>High-end</td>
      </tr>
      <tr>
          <td>BF16 full</td>
          <td>56GB</td>
          <td>Server / big VRAM</td>
      </tr>
  </tbody>
</table>
<p>The practical recommendation from independent reviewers: 4-bit (16-19GB) is the sweet spot for 16-24GB machines, delivering near-full quality without the memory pressure of 6-bit or 8-bit. The 1-bit quant is the option for 8GB machines where previously you had no viable high-quality local model at all. Going to 2-bit or 3-bit buys back a little quality on mid-range hardware at the cost of more memory.</p>
<p>If you run a Mac with 24GB of unified memory or an RTX 40/50-series GPU, the 4-bit quant gives the best balance of speed, quality, and fit. If you have an 8GB machine, the 6.2GB 1-bit file is your entry point.</p>
<h2 id="faster-local-inference--agentic-use-llamacpp-unsloth-desktop-tool-calling">Faster Local Inference &amp; Agentic Use: llama.cpp, Unsloth Desktop, Tool-Calling</h2>
<p>Dynamic 3.0 GGUFs are compatible with the two main ways people run local models: llama.cpp and Unsloth Desktop. Because they follow standard GGUF layout, the files drop into llama.cpp without special tooling — you just point llama-server at the Hugging Face repo and choose your quant.</p>
<p>A typical llama.cpp launch for Qwen3.8-27B looks like this:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-bash" data-lang="bash"><span style="display:flex;"><span>llama-server -hf unsloth/Qwen3.8-27B-GGUF:Q4_K_M -ngl <span style="color:#ae81ff">999</span> -fa on -c <span style="color:#ae81ff">65536</span> --port <span style="color:#ae81ff">8080</span>
</span></span></code></pre></div><p>The flags offload all layers to the GPU (<code>-ngl 999</code>), enable flash attention (<code>-fa on</code>), and set a 64K context window (<code>-c 65536</code>). With those settings the 4-bit quant runs comfortably on a 16-24GB GPU.</p>
<p>For agentic use, the developer-role support is the key addition. Qwen3.8-27B now works inside agent frameworks like Codex, and the improved nested-JSON tool-calling parser means the model can emit structured tool calls reliably — the difference between a model that loops on malformed JSON and one that actually drives an agent. On the ecosystem side, Unsloth Desktop is the first local app that can both run and train models, and Unsloth supports NVIDIA Blackwell and RTX 50-series hardware, long 500K-token context training, and faster MoE training.</p>
<p>The result is that &ldquo;faster local inference&rdquo; is not just about tokens-per-second on a chatbot. It is about running a real coding agent offline on a workstation, with tool calling that does not fall apart.</p>
<h2 id="unsloth-vs-other-gguf-providers-qat-imatrix">Unsloth vs Other GGUF Providers (QAT, imatrix)</h2>
<p>Most GGUF providers fall into two camps: standard imatrix quantization and QAT (quantization-aware training). Unsloth Dynamic 3.0 deliberately sits in a third camp — pure post-training quantization with improved calibration material and smarter layer selection — and its differentiators are worth spelling out.</p>
<table>
  <thead>
      <tr>
          <th>Factor</th>
          <th>Standard imatrix GGUFs</th>
          <th>QAT GGUFs</th>
          <th>Unsloth Dynamic 3.0</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>Training-time cost</td>
          <td>None</td>
          <td>High (retrain)</td>
          <td>None</td>
      </tr>
      <tr>
          <td>Accuracy at matched size</td>
          <td>Baseline</td>
          <td>Higher (but costly)</td>
          <td>&gt;10% higher than all others</td>
      </tr>
      <tr>
          <td>Layer handling</td>
          <td>Uniform-ish</td>
          <td>Fixed</td>
          <td>Per-layer, per-model</td>
      </tr>
      <tr>
          <td>Calibration data</td>
          <td>General</td>
          <td>Task-specific</td>
          <td>Agentic, coding, multilingual</td>
      </tr>
      <tr>
          <td>Requires extra training</td>
          <td>No</td>
          <td>Yes</td>
          <td>No</td>
      </tr>
  </tbody>
</table>
<p>QAT can raise accuracy but requires retraining the model with quantization in the loop, which is expensive and slow to release. Standard imatrix quantization adds no training cost but treats the model more uniformly. Unsloth claims to get QAT-level accuracy without the training cost, and its v2.0/v3.0 accuracy-per-GB numbers back that up: the 9.83GB UD-Q2_K_XL is about 8% more accurate than the next-best provider quant at the same size, and the headline claim is &gt;10% better at matched size across the line.</p>
<p>Independent caveats: accuracy claims are self-measured, so treat &ldquo;&gt;10%&rdquo; as Unsloth&rsquo;s number pending third-party replication. What is not in dispute is that the files run everywhere GGUF runs and that the small quants genuinely open up 8GB hardware.</p>
<h2 id="practical-setup--recommended-sampling-settings">Practical Setup &amp; Recommended Sampling Settings</h2>
<p>To get the most out of a Dynamic 3.0 GGUF, follow these steps. First, choose your quant by total RAM plus VRAM using the table above — do not chase the highest bit depth. Second, run it through llama.cpp or Unsloth Desktop. Third, apply the recommended sampling settings, which differ between thinking and non-thinking modes.</p>
<p>Unsloth&rsquo;s recommendation for Qwen3.8-27B sampling:</p>
<ul>
<li>Thinking mode: temperature 1.0, top_p 0.95, top_k 20.</li>
<li>Non-thinking mode: temperature 0.7, top_p 0.80, presence_penalty 1.5.</li>
</ul>
<p>The presence penalty in non-thinking mode discourages repetition when the model is answering directly without a reasoning step, while the higher temperature in thinking mode lets the reasoning chain explore more before committing.</p>
<p>For agentic tool-calling workloads, enable the developer role and rely on the improved nested-JSON parser rather than forcing the model to serialize tool calls into a hand-rolled format. If your machine has less than 16GB of usable memory, start with the 1-bit or 2-bit quant and only step up once you confirm the larger file stays resident without swapping.</p>
<h2 id="caveats--self-reported-benchmarks-and-overfitting-controls">Caveats — Self-Reported Benchmarks and Overfitting Controls</h2>
<p>No review of a quantization release is complete without the caveats. The most important is that the headline accuracy numbers — the &gt;10% improvement, the 72% and 77% 1-bit retention figures — are self-reported by Unsloth on its own Divergence-300 @32 benchmark. The metric is reasonable and better than single-token top-1%, but it has not been independently replicated at scale yet, so treat it as a strong signal rather than an audited number.</p>
<p>The overfitting controls are genuinely sound. Calibration happens on one dataset, and validation runs on held-out Wikitext, Code, and the Divergence-300 examples. That split reduces the risk that the quants are tuned to look good on the training set. But Divergence-300 @32 scores are still relative-to-parent faithfulness scores, not absolute task-quality grades — a high faithfulness to a mediocre parent is not the same as high absolute quality.</p>
<p>Finally, the &ldquo;&gt;10%&rdquo; comparison is against other GGUF providers, not against the unquantized BF16 model. At the 4-bit sweet spot the gap to BF16 is small, but at 1-bit you are trading a measurable chunk of fidelity for the ability to fit in 8GB. Know the trade before you pick the smallest file.</p>
<h2 id="conclusion-is-dynamic-30-worth-it-for-local-inference">Conclusion: Is Dynamic 3.0 Worth It for Local Inference?</h2>
<p>For most people, yes. Unsloth Dynamic 3.0 GGUFs deliver the two things that actually matter for local inference: more accuracy per gigabyte than competing GGUFs, and — through the 1-bit quants — the ability to run a 27B-class model on hardware that previously could not handle it. The 4-bit quant is the reliable sweet spot for 16-24GB machines, offering near-full quality with an easy llama.cpp setup, and the developer-role and tool-calling improvements make the quants genuinely usable for agentic coding.</p>
<p>The honest caveats are that the marquee accuracy figures are self-reported and that smaller quants always cost some fidelity. But the compatibility story is unambiguous: the files run in llama.cpp and Unsloth Desktop today, and with over 5.1 million downloads in five days, Dynamic 3.0 has already become the default way people run Qwen3.8 locally. If you run local models and own any consumer GPU with 8GB or more, Dynamic 3.0 GGUFs are worth downloading right now.</p>
<h2 id="faq">FAQ</h2>
<h3 id="what-is-unsloth-dynamic-gguf">What is Unsloth Dynamic GGUF?</h3>
<p>Unsloth Dynamic GGUF is a post-training quantization format that shrinks LLMs into small GGUF files for local inference. Dynamic 3.0 is the latest iteration, claiming more than 10% better accuracy at the same file size than other providers&rsquo; GGUFs, with pure post-training quantization and no QAT.</p>
<h3 id="how-much-ram-do-i-need-to-run-qwen38-27b-with-dynamic-30-ggufs">How much RAM do I need to run Qwen3.8-27B with Dynamic 3.0 GGUFs?</h3>
<p>It depends on the quant: the 1-bit file runs in 7-8GB, 2-bit needs 9-11GB, 3-bit 12-14GB, 4-bit 16-19GB, 6-bit 23-26GB, 8-bit 31GB, and full BF16 requires 56GB. Pick by your total RAM plus VRAM, not the highest bit depth.</p>
<h3 id="does-a-1-bit-gguf-really-keep-good-accuracy">Does a 1-bit GGUF really keep good accuracy?</h3>
<p>Unsloth reports the 1-bit UD-IQ1_S quant (6.2GB) retains roughly 72% top-1% accuracy and is 89% smaller than full precision; independent analysis reports 77% accuracy on the held-out eval. It is best for 8GB machines, with the 4-bit quant offering much higher fidelity for 16-24GB setups.</p>
<h3 id="which-gguf-is-better-for-local-coding-and-agent-use">Which GGUF is better for local coding and agent use?</h3>
<p>The 4-bit quant is the practical sweet spot for 16-24GB machines. Qwen3.8-27B also adds developer-role support (so it works in agentic tools like Codex) and improved nested-JSON tool-calling parsing, making quantized models more reliable in real agent loops.</p>
<h3 id="do-unsloth-dynamic-30-ggufs-work-with-llamacpp">Do Unsloth Dynamic 3.0 GGUFs work with llama.cpp?</h3>
<p>Yes. They use the standard GGUF format, so they run directly in llama.cpp (for example, with <code>llama-server -hf unsloth/Qwen3.8-27B-GGUF:Q4_K_M -ngl 999 -fa on -c 65536</code>) and in Unsloth Desktop without special tooling.</p>
]]></content:encoded></item><item><title>Multi GPU Local Inference: How to Drift a Model Across Your Home GPUs</title><link>https://baeseokjae.github.io/posts/ai-at-home-multi-gpu-drifting/</link><pubDate>Tue, 01 Sep 2026 04:01:49 +0000</pubDate><guid>https://baeseokjae.github.io/posts/ai-at-home-multi-gpu-drifting/</guid><description>Multi GPU local inference lets you run models that exceed one GPU&amp;#39;s VRAM. Learn pipeline vs tensor parallel, llama.cpp and vLLM configs, and when it&amp;#39;s worth it.</description><content:encoded><![CDATA[<p>Multi GPU local inference means spreading a single large language model across two or more GPUs in your home server so you can run models that exceed the VRAM of any one card, or serve them faster. You do this by &ldquo;drifting&rdquo; the model&rsquo;s layers or tensors across your GPUs using either pipeline (layer) parallelism or tensor parallelism, configured through tools like llama.cpp and vLLM. The right strategy depends on your interconnect speed, your hardware mix, and whether your model architecture even supports the split.</p>
<h2 id="why-run-local-inference-across-multiple-gpus">Why Run Local Inference Across Multiple GPUs</h2>
<p>The single most common reason to run multi GPU local inference is VRAM capacity. A 70B-parameter model in 4-bit quantization needs roughly 40 GB of memory, which exceeds the 24 GB available on a single consumer RTX 4090 or RTX 3090. When your model does not fit in one GPU&rsquo;s VRAM, multi-GPU is the only way to run it locally without offloading to slow system RAM.</p>
<p>The second reason is throughput. Even when a model fits on one card, splitting it across multiple GPUs can increase tokens-per-second for concurrent requests, especially in a home server serving several users or agents at once. The official llama.cpp multi-GPU documentation lists exactly these two triggers: the model does not fit in a single GPU&rsquo;s VRAM, or you want more throughput.</p>
<p>There is also a growing hobbyist pull toward multi-GPU rigs. Projects like nanoslg, an educational multi-GPU LLM server claiming roughly 5x speedup, and LLMKube, which orchestrates local LLMs on Kubernetes with GPU acceleration, show that multi-GPU local inference is an active DIY space. The trend is moving from single-card experiments toward managed, containerized multi-GPU setups.</p>
<h2 id="how-multi-gpu-inference-works-pipeline-vs-tensor-parallelism">How Multi-GPU Inference Works: Pipeline vs Tensor Parallelism</h2>
<p>There are two fundamentally different ways to drift a model across GPUs, and they are not interchangeable.</p>
<p><strong>Pipeline (layer) parallelism</strong> splits the model by layers. GPU 0 runs the first N layers, GPU 1 runs the next N, and so on. Each GPU holds a contiguous slice of the network, and activations flow from one GPU to the next in sequence. In llama.cpp this is the default and most compatible split mode, controlled with <code>--split-mode layer</code>. Because each GPU only needs to hold its own layers, pipeline parallelism is the natural choice when the goal is simply fitting a model that is too big for one card.</p>
<p><strong>Tensor parallelism</strong> splits each individual layer&rsquo;s weight matrices across GPUs. All GPUs work on the same layer simultaneously, each computing a slice of the matrix multiplication, then exchanging partial results. This is faster per token because the work is parallelized within each layer, but it requires far more communication between GPUs and is much more sensitive to interconnect speed. In llama.cpp, tensor parallel is controlled with <code>--split-mode tensor</code> and <code>--tensor-split</code>, and it is explicitly marked experimental.</p>
<p>The practical difference matters for home rigs. Pipeline parallelism is forgiving of slow interconnects because GPUs only exchange activations once per layer boundary. Tensor parallelism exchanges data constantly within every layer, so it demands fast NVLink or at least high-bandwidth PCIe.</p>
<h2 id="choosing-your-parallelism-strategy-based-on-interconnect">Choosing Your Parallelism Strategy Based on Interconnect</h2>
<p>Your interconnect is the single biggest factor in choosing between pipeline and tensor parallelism. The vLLM parallelism scaling documentation is explicit: for GPUs without NVLink interconnect, such as the L40S, prefer pipeline parallelism over tensor parallelism for higher throughput and lower communication overhead.</p>
<p>Here is a practical decision table for a home multi-GPU rig:</p>
<table>
  <thead>
      <tr>
          <th>Interconnect</th>
          <th>Recommended strategy</th>
          <th>Why</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>NVLink (e.g. RTX 3090/4090 pairs, A100/H100)</td>
          <td>Tensor parallel</td>
          <td>Low-latency high-bandwidth links make per-layer data exchange cheap</td>
      </tr>
      <tr>
          <td>PCIe Gen 4/5, no NVLink (e.g. L40S, mixed consumer cards)</td>
          <td>Pipeline (layer) parallel</td>
          <td>Activations exchanged once per layer boundary; far less chatter</td>
      </tr>
      <tr>
          <td>Mixed or unknown interconnect</td>
          <td>Pipeline (layer) parallel</td>
          <td>Default and most compatible; safest fallback</td>
      </tr>
      <tr>
          <td>Multi-node (two separate machines)</td>
          <td>Pipeline parallel + tensor parallel</td>
          <td>Set tensor_parallel_size per node, pipeline_parallel_size to node count</td>
      </tr>
  </tbody>
</table>
<p>The rule of thumb: if your GPUs are not NVLink-connected, tensor parallelism will likely bottleneck on communication and deliver worse throughput than a well-tuned pipeline split. This is why vLLM&rsquo;s guidance for L40S-class hardware points away from tensor parallel.</p>
<h2 id="setting-up-multi-gpu-inference-with-llamacpp">Setting Up Multi-GPU Inference with llama.cpp</h2>
<p>llama.cpp supports four split modes: <code>none</code>, <code>layer</code> (pipeline, the default), <code>row</code> (deprecated), and <code>tensor</code> (experimental). For a home rig, you will almost always use <code>layer</code> or <code>tensor</code>.</p>
<p>The key command-line flags are:</p>
<ul>
<li><code>--split-mode none|layer|row|tensor</code> — how to distribute the model</li>
<li><code>--tensor-split</code> — comma-separated fraction of work per GPU (e.g. <code>1,1</code> for two equal GPUs)</li>
<li><code>--main-gpu</code> — which GPU handles the main compute and output</li>
<li><code>--n-gpu-layers</code> — how many layers to offload to GPU (relevant when mixing GPU and CPU)</li>
<li><code>--device</code> — target a specific device</li>
<li><code>--flash-attn</code> — required for tensor parallel</li>
</ul>
<p>A basic two-GPU pipeline split looks like:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-bash" data-lang="bash"><span style="display:flex;"><span>llama-server -m model.gguf <span style="color:#ae81ff">\
</span></span></span><span style="display:flex;"><span><span style="color:#ae81ff"></span>  --split-mode layer <span style="color:#ae81ff">\
</span></span></span><span style="display:flex;"><span><span style="color:#ae81ff"></span>  --n-gpu-layers <span style="color:#ae81ff">99</span> <span style="color:#ae81ff">\
</span></span></span><span style="display:flex;"><span><span style="color:#ae81ff"></span>  --flash-attn
</span></span></code></pre></div><p>For tensor parallel across two equal GPUs:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-bash" data-lang="bash"><span style="display:flex;"><span>llama-server -m model.gguf <span style="color:#ae81ff">\
</span></span></span><span style="display:flex;"><span><span style="color:#ae81ff"></span>  --split-mode tensor <span style="color:#ae81ff">\
</span></span></span><span style="display:flex;"><span><span style="color:#ae81ff"></span>  --tensor-split 1,1 <span style="color:#ae81ff">\
</span></span></span><span style="display:flex;"><span><span style="color:#ae81ff"></span>  --flash-attn
</span></span></code></pre></div><p>There are important constraints on tensor parallel in llama.cpp. It requires flash attention and a non-quantized KV cache (f32/f16/bf16); a quantized KV cache is not implemented for tensor split. It is also not implemented for MoE or hybrid architectures such as Grok, DeepSeek2, and Mistral4, nor for state-space models like Mamba and Mamba2. If you are running any of those, you must use layer (pipeline) splitting.</p>
<p>NCCL support in llama.cpp is selected at build time with <code>-DGGML_CUDA_NCCL=ON</code> (the default), and NCCL is not automatically distributed with CUDA, so you may need to install it separately for multi-GPU tensor parallel to work.</p>
<h2 id="setting-up-multi-gpu-inference-with-vllm">Setting Up Multi-GPU Inference with vLLM</h2>
<p>vLLM offers a more production-oriented path to multi GPU local inference. Its parallelism model is: single GPU, single-node multi-GPU tensor parallel, or multi-node tensor plus pipeline parallel.</p>
<p>The two settings that matter are <code>tensor_parallel_size</code> and <code>pipeline_parallel_size</code>. vLLM recommends setting <code>tensor_parallel_size</code> to the number of GPUs per node and <code>pipeline_parallel_size</code> to the number of nodes. For a single machine with four GPUs, that means:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-bash" data-lang="bash"><span style="display:flex;"><span>vllm serve meta-llama/Llama-2-70B <span style="color:#ae81ff">\
</span></span></span><span style="display:flex;"><span><span style="color:#ae81ff"></span>  --tensor-parallel-size <span style="color:#ae81ff">4</span> <span style="color:#ae81ff">\
</span></span></span><span style="display:flex;"><span><span style="color:#ae81ff"></span>  --pipeline-parallel-size <span style="color:#ae81ff">1</span>
</span></span></code></pre></div><p>For a two-node setup with four GPUs each:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-bash" data-lang="bash"><span style="display:flex;"><span>vllm serve meta-llama/Llama-2-70B <span style="color:#ae81ff">\
</span></span></span><span style="display:flex;"><span><span style="color:#ae81ff"></span>  --tensor-parallel-size <span style="color:#ae81ff">4</span> <span style="color:#ae81ff">\
</span></span></span><span style="display:flex;"><span><span style="color:#ae81ff"></span>  --pipeline-parallel-size <span style="color:#ae81ff">2</span>
</span></span></code></pre></div><p>vLLM uses Ray for multi-node inference and native Python multiprocessing for single-node inference by default. For a home server, the single-node multiprocessing path is simpler and avoids the overhead of a Ray cluster.</p>
<p>The practical takeaway from vLLM&rsquo;s guidance mirrors llama.cpp: for uneven GPU splits, use pipeline parallelism with <code>tensor_parallel_size=1</code>, and for GPUs without NVLink, prefer pipeline over tensor for throughput.</p>
<h2 id="handling-uneven-gpu-splits-and-mixed-hardware">Handling Uneven GPU Splits and Mixed Hardware</h2>
<p>Home rigs rarely have identical GPUs. You might pair a 24 GB RTX 3090 with a 16 GB RTX 4080, or mix an older card with a newer one. Both llama.cpp and vLLM handle this, but differently.</p>
<p>In llama.cpp, <code>--tensor-split</code> lets you weight the work by GPU capacity. For a 24 GB and 16 GB pair, you might use <code>--tensor-split 3,2</code> to give the larger card more layers. In vLLM, the guidance is to use pipeline parallelism (<code>tensor_parallel_size=1</code>) for uneven splits, because tensor parallelism assumes roughly equal compute across GPUs and will stall on the slowest card.</p>
<p>Mixed hardware also changes your memory planning. The total usable VRAM is the sum of all cards, but the effective throughput is bounded by the slowest GPU in a tensor-parallel configuration. If your goal is purely fitting a large model, pipeline parallelism with weighted splits is the more forgiving path.</p>
<h2 id="common-pitfalls-and-how-to-avoid-them">Common Pitfalls and How to Avoid Them</h2>
<p>Several mistakes trip up first-time multi-GPU builders:</p>
<p><strong>Tensor parallel on unsupported architectures.</strong> MoE models (Grok, DeepSeek2, Mistral4) and state-space models (Mamba, Mamba2) do not support tensor parallel in llama.cpp. Check your model architecture before choosing a split mode, or you will get silent failures or crashes.</p>
<p><strong>Missing flash attention.</strong> Tensor parallel in llama.cpp requires <code>--flash-attn</code> and a non-quantized KV cache. If you are using a quantized KV cache, tensor split will not work.</p>
<p><strong>Slow interconnect with tensor parallel.</strong> On PCIe-only rigs without NVLink, tensor parallelism can be slower than a single GPU because communication overhead dominates. Prefer pipeline parallelism.</p>
<p><strong>NCCL not installed.</strong> llama.cpp builds NCCL support by default, but NCCL itself is not bundled with CUDA. On a fresh install, multi-GPU tensor parallel may fail until you install NCCL.</p>
<p><strong>Uneven GPUs in tensor parallel.</strong> Tensor parallel stalls on the slowest card. Use weighted <code>--tensor-split</code> in llama.cpp or switch to pipeline parallelism in vLLM for mixed hardware.</p>
<h2 id="when-multi-gpu-is-and-isnt-worth-it">When Multi-GPU Is (and Isn&rsquo;t) Worth It</h2>
<p>Multi GPU local inference is not always the right answer. It is worth it when your model does not fit in a single GPU&rsquo;s VRAM and you want to keep inference fully local, or when you need higher throughput for concurrent requests and your interconnect can support it.</p>
<p>It is not worth it when a single GPU already fits the model comfortably, or when your interconnect is too slow to justify the added complexity. A 7B or 13B model on one 24 GB card will often outperform a poorly configured multi-GPU split of the same model. The added power draw, heat, and configuration complexity only pay off when the model genuinely exceeds single-GPU capacity or you have a real concurrency need.</p>
<h2 id="conclusion-building-your-home-multi-gpu-inference-rig">Conclusion: Building Your Home Multi-GPU Inference Rig</h2>
<p>Multi GPU local inference is the practical way to drift large models across your home GPUs. Start with pipeline (layer) parallelism, which is the default and most compatible in llama.cpp and the recommended choice for PCIe-only rigs in vLLM. Move to tensor parallelism only when you have NVLink-connected GPUs, a supported model architecture, flash attention enabled, and a real throughput need.</p>
<p>Check your interconnect first, then your model architecture, then your split mode. For most home servers, a well-tuned pipeline split across two or three GPUs is the sweet spot: it fits models that exceed single-card VRAM without the communication overhead that makes tensor parallel fragile on consumer hardware.</p>
<h2 id="faq">FAQ</h2>
<p><strong>What is multi GPU local inference?</strong>
Multi GPU local inference is running a single large language model across two or more GPUs in your own hardware, using pipeline or tensor parallelism, so you can fit models that exceed one GPU&rsquo;s VRAM or serve them faster.</p>
<p><strong>What is the difference between pipeline and tensor parallelism?</strong>
Pipeline (layer) parallelism splits the model by layers, with each GPU running a contiguous slice. Tensor parallelism splits each layer&rsquo;s weight matrices across GPUs, which is faster per token but requires far more inter-GPU communication.</p>
<p><strong>Does multi GPU inference work with any model?</strong>
No. In llama.cpp, tensor parallel is not implemented for MoE or hybrid architectures like Grok, DeepSeek2, and Mistral4, nor for state-space models like Mamba. Pipeline (layer) parallelism is the most compatible option.</p>
<p><strong>Do I need NVLink for multi GPU inference?</strong>
Not for pipeline parallelism, which works fine over PCIe. Tensor parallelism benefits strongly from NVLink, and vLLM recommends pipeline parallelism for GPUs without NVLink, such as the L40S.</p>
<p><strong>How much VRAM do I need to run a 70B model locally?</strong>
A 70B model in 4-bit quantization needs roughly 40 GB of VRAM, which exceeds a single 24 GB consumer card. Spreading it across two or more GPUs with multi GPU local inference lets you run it fully locally.</p>
]]></content:encoded></item></channel></rss>