<?xml version="1.0" encoding="utf-8" standalone="yes"?><rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom" xmlns:content="http://purl.org/rss/1.0/modules/content/"><channel><title>Llama4 on RockB</title><link>https://baeseokjae.github.io/tags/llama4/</link><description>Recent content in Llama4 on RockB</description><image><title>RockB</title><url>https://baeseokjae.github.io/images/og-default.png</url><link>https://baeseokjae.github.io/images/og-default.png</link></image><generator>Hugo</generator><language>en-us</language><lastBuildDate>Fri, 12 Jun 2026 03:03:04 +0000</lastBuildDate><atom:link href="https://baeseokjae.github.io/tags/llama4/index.xml" rel="self" type="application/rss+xml"/><item><title>Deploy Llama 4 with vLLM and Ollama: Scout vs Maverick Setup Guide</title><link>https://baeseokjae.github.io/posts/deploy-llama-4-vllm-ollama-2026/</link><pubDate>Fri, 12 Jun 2026 03:03:04 +0000</pubDate><guid>https://baeseokjae.github.io/posts/deploy-llama-4-vllm-ollama-2026/</guid><description>Deploy Scout and Maverick with a decision-first workflow, practical commands, and migration checks for vLLM and Ollama.</description><content:encoded><![CDATA[<p>If you want Llama 4 in production, start by matching hardware, concurrency, and context requirements before model size. In most teams, Scout is the first stable bet: faster startup, cheaper memory, and smoother local iteration, while Maverick becomes the right move when you need the bigger context and reasoning headroom under higher traffic. The path that works is not “which product is better,” it is “which constraint profile is cheaper to satisfy this quarter.”</p>
<h2 id="which-llama-4-variant-should-i-pick-first-scout-or-maverick">Which Llama 4 variant should I pick first: Scout or Maverick?</h2>
<p>Llama 4 Scout and Maverick differ mainly in how much compute they expose per token and how they scale with traffic. Scout is the lighter profile with 17B active parameters backed by 16 experts (109B total), while Maverick also has 17B active parameters but a much wider expert set of 128 for 400B total. In plain terms, Scout is optimized for lower latency and simpler ops, while Maverick gives a bigger reasoning envelope when the prompt, context, and tool use demand it. In a 2026 production read from community notes, the practical split is simple: Scout for local dev, POCs, and small services; Maverick for API traffic, heavy retrieval loops, and long-context tasks. The takeaway is clear: choose Scout for speed-to-value and Maverick only when capacity, budget, and context needs justify the larger expert network.</p>
<h3 id="is-active-parameter-count-the-real-difference-or-total-model-size">Is active parameter count the real difference, or total model size?</h3>
<p>An active-parameters lens is the first thing to watch because it governs memory and sustained throughput. A model can advertise very large totals while executing fewer active parameters per token, which is exactly why Scout and Maverick look close at 17B active but diverge sharply in routing behavior. In practical ops, this means Maverick’s large expert fan-out can improve quality on difficult prompts but can also increase latency jitter if your cache and batching aren’t tuned. If your service has strict p99 response goals and high concurrency, Scout gives a more predictable token curve; if your workload values complex reasoning quality on long prompts, Maverick can pay for itself despite higher infrastructure variance.</p>
<h3 id="what-should-the-decision-matrix-weigh-first">What should the decision matrix weigh first?</h3>
<p>The decision matrix is deterministic once you define your hard constraints. I compare these columns for each stack: target concurrency, token budget, max prompt context, hardware budget, and failure recovery complexity. If average context is under 4k and daily concurrency is bursty but small, Scout with Ollama is often enough and cheaper. If you routinely hit 2k+ tokens per request, multi-turn tool loops, or 16+ concurrent users, vLLM with Maverick becomes the safer option. Use this matrix in reviews before any installation so model choice becomes a planning artifact, not a guess.</p>
<table>
  <thead>
      <tr>
          <th>Model</th>
          <th>Active params</th>
          <th>Total params</th>
          <th>Best for</th>
          <th>Typical first deployment</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>Scout</td>
          <td>17B</td>
          <td>109B (16 experts)</td>
          <td>latency-sensitive APIs, local dev, high iteration speed</td>
          <td>Ollama locally, vLLM in staging</td>
      </tr>
      <tr>
          <td>Maverick</td>
          <td>17B</td>
          <td>400B (128 experts)</td>
          <td>complex reasoning, long context, richer tool calls</td>
          <td>vLLM production first, Ollama optional validation</td>
      </tr>
  </tbody>
</table>
<h2 id="what-hardware-and-os-baseline-keeps-both-stacks-reliable">What hardware and OS baseline keeps both stacks reliable?</h2>
<p>Hardware fit is the gatekeeper. A model stack can be correct on paper and still fail under load if your GPU, PCIe, memory speed, and driver stack are mismatched. Official Llama 4 guidance from 8xH100 runs includes Scout up to 1M context and Maverick around 430K, with even larger context on 8xH200 rigs. In a real dev shop, most failures happen before training quality questions: wrong CUDA version, NUMA oversubscription, or shared memory limits on container hosts. For local teams, Ollama still has an Apple Silicon advantage because vLLM has no native Metal path yet, so it is often the only workable option on M-series Macs for quick tests. The takeaway is to lock your baseline first—Python runtime, driver stack, GPU family, and context cap—then decide tooling around it.</p>
<h3 id="can-i-start-on-a-laptop-and-later-move-to-production">Can I start on a laptop and later move to production?</h3>
<p>Yes, but start with constraints in mind, not nostalgia. A MacBook setup with Ollama can validate prompt templates, function-calling patterns, and output formats, but it should not be your pre-production concurrency benchmark for Scout or Maverick. Keep local images and prompts consistent so you can replay them later in vLLM staging. Once parity passes, move deployment to Linux GPU hosts and only then tune <code>tensor-parallel-size</code>, KV cache behavior, and batching. A clean pattern is to treat local work as contract validation and production as systems validation; mixing goals makes debugging impossible and delays go-live.</p>
<h3 id="where-do-memory-and-driver-assumptions-break-first">Where do memory and driver assumptions break first?</h3>
<p>Most teams hit memory ceilings at three points: model load, KV-cache growth, and queue buildup under burst traffic. Scout tolerates lower memory headroom than Maverick, but both still need predictable host memory for orchestration and pre/post-processing. In production, reserve additional host RAM for embeddings, DB client pools, and observability exporters, otherwise OOMs present as API timeouts instead of obvious GPU faults. The practical rule I follow is to test with a synthetic max-context workload before exposing users. If memory cliffs appear at 70–75% GPU utilization, dial down cache size or batch width, then retest before adding new features.</p>
<table>
  <thead>
      <tr>
          <th>Stack</th>
          <th>Local fit</th>
          <th>Minimum comfort</th>
          <th>Typical weak point</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>Ollama + Scout</td>
          <td>Mac/Linux, single GPU, low concurrency</td>
          <td>16GB–24GB VRAM often workable for experiments</td>
          <td>concurrency throttling</td>
      </tr>
      <tr>
          <td>Ollama + Maverick</td>
          <td>single high-memory Linux GPU only</td>
          <td>very memory-sensitive, slower startup</td>
          <td>startup latency and model residency</td>
      </tr>
      <tr>
          <td>vLLM + Scout</td>
          <td>Linux + Nvidia data center GPUs</td>
          <td>multi-GPU scheduling and serving APIs</td>
          <td>cold-start and tokenization throughput tuning</td>
      </tr>
      <tr>
          <td>vLLM + Maverick</td>
          <td>2+ GPUs or H100/H200 class</td>
          <td>batched throughput + context management</td>
          <td>complexity in serving configuration</td>
      </tr>
  </tbody>
</table>
<h2 id="how-do-i-deploy-llama-4-scout-with-vllm">How do I deploy Llama 4 Scout with vLLM?</h2>
<p>Deploying Scout on vLLM is for teams that want API-compatible serving with room to scale. The workflow starts with a stable container and explicit flags for context, precision, and token batching; without those, you only get “it boots” instead of predictable production behavior. In field practice, vLLM’s strength is not model quality alone but request packing: continuous batching and scheduler behavior keep utilization high when multiple users arrive. A reference baseline is to run Scout first with conservative limits, expose health probes, and then increase throughput settings over two to three controlled ramps. The key takeaway is to treat vLLM as a service platform, not a one-shot binary, and to baseline with measurable load before switching from local prompt experiments.</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-bash" data-lang="bash"><span style="display:flex;"><span><span style="color:#75715e"># Example baseline for a Scout pilot on Linux</span>
</span></span><span style="display:flex;"><span>python -m vllm.entrypoints.openai.api_server <span style="color:#ae81ff">\
</span></span></span><span style="display:flex;"><span><span style="color:#ae81ff"></span>  --model /models/llama4-scout <span style="color:#ae81ff">\
</span></span></span><span style="display:flex;"><span><span style="color:#ae81ff"></span>  --host 0.0.0.0 <span style="color:#ae81ff">\
</span></span></span><span style="display:flex;"><span><span style="color:#ae81ff"></span>  --port <span style="color:#ae81ff">8000</span> <span style="color:#ae81ff">\
</span></span></span><span style="display:flex;"><span><span style="color:#ae81ff"></span>  --dtype bfloat16 <span style="color:#ae81ff">\
</span></span></span><span style="display:flex;"><span><span style="color:#ae81ff"></span>  --gpu-memory-utilization 0.88 <span style="color:#ae81ff">\
</span></span></span><span style="display:flex;"><span><span style="color:#ae81ff"></span>  --max-model-len <span style="color:#ae81ff">65536</span> <span style="color:#ae81ff">\
</span></span></span><span style="display:flex;"><span><span style="color:#ae81ff"></span>  --max-num-batched-tokens <span style="color:#ae81ff">131072</span> <span style="color:#ae81ff">\
</span></span></span><span style="display:flex;"><span><span style="color:#ae81ff"></span>  --tensor-parallel-size <span style="color:#ae81ff">1</span> <span style="color:#ae81ff">\
</span></span></span><span style="display:flex;"><span><span style="color:#ae81ff"></span>  --trust-remote-code
</span></span></code></pre></div><h3 id="what-does-scout-first-vllm-tuning-look-like">What does Scout-first vLLM tuning look like?</h3>
<p>A Scout-first tuning pass has three fixed checkpoints: startup, concurrency, and long-context behavior. First, confirm successful warm start and endpoint readiness with a short prompt. Second, push concurrency from 1 to 4 to 16 requests and record latency, throughput, and retry rates. Third, replay a long-context sample near your real max prompt length; if the cache pressure explodes, reduce <code>max-num-batched-tokens</code> or move some work to chunked preprocessing. My rule is to never tune for peak without first testing steady-state behavior at 1x load; many teams optimize peak and hide queue instability they later discover under normal traffic. A successful Scout rollout is stable p95 at modest concurrency and no unexplained tail latency drift after 20+ minutes.</p>
<h3 id="why-do-i-still-need-fallback-tests-if-vllm-is-production-ready">Why do I still need fallback tests if vLLM is “production-ready”?</h3>
<p>Because production readiness is only validated by failure modes, not by a clean startup log. I keep three fallback tests in every rollout: model unload/reload, upstream API outage, and malformed streaming payload. If your app cannot recover cleanly from any one, users feel the breakage before quality gains matter. Build circuit breakers on your application side, keep idempotent request IDs, and route unknown errors to structured logs. This makes vLLM deployment less risky and lets you iterate on batching or quantization settings without rewriting client logic.</p>
<h2 id="how-do-i-deploy-llama-4-scout-with-ollama">How do I deploy Llama 4 Scout with Ollama?</h2>
<p>Ollama is the easiest path for Scout when your goal is local iteration and immediate developer feedback. It removes most stack complexity by turning setup into install, pull, run, and iterate, so you can validate prompts and API shape in minutes. This is why we still use Ollama for many POCs despite vLLM’s stronger concurrency profile. A practical example: during MVP development, teams can test the exact JSON schema they will send to vLLM while still on Ollama, then run a migration playbook later. The takeaway is simple: use Ollama for fast iteration and UX validation, then migrate to vLLM only when traffic, reliability, and scaling requirements justify it.</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-bash" data-lang="bash"><span style="display:flex;"><span><span style="color:#75715e"># Pull and run Scout locally with Ollama</span>
</span></span><span style="display:flex;"><span>ollama pull llama4:scout
</span></span><span style="display:flex;"><span>ollama run llama4:scout <span style="color:#e6db74">&#34;Explain this in one sentence: Why Scout is good for MVPs?&#34;</span>
</span></span></code></pre></div><h3 id="can-i-test-multimodal-requests-with-ollama-scout">Can I test multimodal requests with Ollama Scout?</h3>
<p>Ollama supports multimodal invocation paths for Llama 4, and real-world usage should include an image benchmark even if your app is mostly text. A quick validation is to send one vision request and one text-only request per model and compare token usage, output structure, and timeout behavior. That catches issues early because vision decoding introduces different latency and can trigger hidden queueing behavior in local inference. If one sample works and another crashes, treat it as a model-specific integration bug and gate rollout on both classes.</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-bash" data-lang="bash"><span style="display:flex;"><span>curl -s http://localhost:11434/api/chat <span style="color:#ae81ff">\
</span></span></span><span style="display:flex;"><span><span style="color:#ae81ff"></span>  -H <span style="color:#e6db74">&#39;Content-Type: application/json&#39;</span> <span style="color:#ae81ff">\
</span></span></span><span style="display:flex;"><span><span style="color:#ae81ff"></span>  -d <span style="color:#e6db74">&#39;{
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">    &#34;model&#34;: &#34;llama4:scout&#34;,
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">    &#34;messages&#34;: [{&#34;role&#34;:&#34;user&#34;,&#34;content&#34;:&#34;Summarize a migration plan in 6 steps&#34;}],
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">    &#34;stream&#34;: false
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">  }&#39;</span>
</span></span></code></pre></div><h3 id="why-should-i-keep-parity-data-from-ollama-tests">Why should I keep parity data from Ollama tests?</h3>
<p>Parity data keeps your migration realistic. Capture prompt IDs, input length, response quality rubric scores, and latency for both stacks; only then compare against your target metric. If Ollama passes quality at low cost and vLLM fails at equal prompts, the problem is almost never the model—it is server policy, batching, or environment. Store these results in versioned JSON or a simple YAML ledger. That ledger becomes your migration diff and helps you explain any behavior changes to engineering and product stakeholders.</p>
<h2 id="how-do-i-deploy-llama-4-maverick-with-vllm-at-production-scale">How do I deploy Llama 4 Maverick with vLLM at production scale?</h2>
<p>Maverick on vLLM is the route for teams that need the highest reasoning quality under production concurrency. In 2026 guidance, the model can support very long context windows in H100/H200-class settings, and that is the headline feature that changes architecture: you can retain more memory, docs, and tool history per request without pre-trim complexity. The cost is operational: more aggressive capacity planning, tighter deployment guardrails, and a clearer observability playbook for queue depth and p99 tail. The practical pattern is to start with a hard cap on context and concurrency while you validate quality, then increase only when cache utilization and GPU duty cycle remain stable under sustained load. The takeaway is straightforward: Maverick on vLLM is powerful enough for enterprise-style usage, but only if you build controls before you scale.</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-bash" data-lang="bash"><span style="display:flex;"><span><span style="color:#75715e"># Example Maverick production-oriented command</span>
</span></span><span style="display:flex;"><span>python -m vllm.entrypoints.openai.api_server <span style="color:#ae81ff">\
</span></span></span><span style="display:flex;"><span><span style="color:#ae81ff"></span>  --model /models/llama4-maverick <span style="color:#ae81ff">\
</span></span></span><span style="display:flex;"><span><span style="color:#ae81ff"></span>  --host 0.0.0.0 <span style="color:#ae81ff">\
</span></span></span><span style="display:flex;"><span><span style="color:#ae81ff"></span>  --port <span style="color:#ae81ff">8000</span> <span style="color:#ae81ff">\
</span></span></span><span style="display:flex;"><span><span style="color:#ae81ff"></span>  --dtype bfloat16 <span style="color:#ae81ff">\
</span></span></span><span style="display:flex;"><span><span style="color:#ae81ff"></span>  --tensor-parallel-size <span style="color:#ae81ff">4</span> <span style="color:#ae81ff">\
</span></span></span><span style="display:flex;"><span><span style="color:#ae81ff"></span>  --max-model-len <span style="color:#ae81ff">131072</span> <span style="color:#ae81ff">\
</span></span></span><span style="display:flex;"><span><span style="color:#ae81ff"></span>  --max-num-batched-tokens <span style="color:#ae81ff">262144</span> <span style="color:#ae81ff">\
</span></span></span><span style="display:flex;"><span><span style="color:#ae81ff"></span>  --gpu-memory-utilization 0.90 <span style="color:#ae81ff">\
</span></span></span><span style="display:flex;"><span><span style="color:#ae81ff"></span>  --trust-remote-code
</span></span></code></pre></div><h3 id="what-changes-in-operations-when-i-scale-maverick-from-pilot-to-production">What changes in operations when I scale Maverick from pilot to production?</h3>
<p>The scale shift is mostly scheduler discipline. Keep separate endpoints for synthetic validation and user traffic so you can replay exact failure scenarios without polluting user SLA. Add autoscaling policies on queue length and GPU memory utilization instead of CPU alone. For Maverick, a small increase in concurrency can expose head-of-line blocking, so monitor queue age, token-per-second curves, and request age distribution. If the latency curve drifts, reduce batch size first before adding more replicas, then rebalance once you prove stable. This keeps quality while avoiding over-provisioned fleets.</p>
<h3 id="can-maverick-justify-400b-total-complexity-in-business-terms">Can Maverick justify 400B total complexity in business terms?</h3>
<p>Yes when prompts are long and tasks are failure-sensitive. In workloads with heavy function routing and multi-document context, Maverick’s larger expert set can reduce re-prompt loops, which reduces token waste and downstream retry cost. In short, Maverick may look expensive on paper, but if it shortens iterative model calls in your product flow, it can lower end-to-end compute consumption. The decision is therefore not model price alone; it is the cost of each failed answer and your retry budget.</p>
<h2 id="how-do-i-deploy-llama-4-maverick-with-ollama-and-when-should-i-avoid-it">How do I deploy Llama 4 Maverick with Ollama, and when should I avoid it?</h2>
<p>Ollama can run Maverick and is useful for smoke testing, QA, and constrained local environments, but it is usually not ideal for sustained high-concurrency production. Mavericks memory footprint and multimodal path can become bottlenecks on smaller single-host setups, and user-facing systems quickly hit throughput limits even when local quality is acceptable. The reason teams still use it is portability: with one command, they validate prompts and edge cases before expensive infra scaling. Use it to lock behavior and acceptance criteria, then port the exact contract to vLLM once traffic patterns are known. The takeaway is: Ollama + Maverick is a strong validation environment, not a long-term concurrency platform.</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-bash" data-lang="bash"><span style="display:flex;"><span><span style="color:#75715e"># Optional local check for Maverick behavior</span>
</span></span><span style="display:flex;"><span>ollama pull llama4:maverick
</span></span><span style="display:flex;"><span>ollama run llama4:maverick <span style="color:#e6db74">&#34;Run a 5-step plan for reducing API tail latency&#34;</span>
</span></span></code></pre></div><h3 id="where-does-ollama-maverick-fail-under-real-traffic">Where does Ollama Maverick fail under real traffic?</h3>
<p>In local tests, latency is usually acceptable, but queue behavior becomes unstable as soon as concurrency rises beyond one or two long-running jobs. You also lose some control knobs for sophisticated scheduler tuning compared with vLLM, so you may hit a throughput wall before model quality saturates. The most expensive issue is operational surprise: you may assume the model behavior is stable because one local test passed, then lose p95 budgets in real deployment. Keep this stack strictly in validation mode unless your workload is small and non-urgent.</p>
<h3 id="what-should-local-validation-include-if-i-plan-to-migrate-later">What should local validation include if I plan to migrate later?</h3>
<p>Validation should include at least one benchmark per scenario that stresses what your app does most: structured extraction, retrieval augmentation, and image-heavy prompts if applicable. Measure token usage and output schema compliance in all three. A simple schema test where a malformed function call still returns valid JSON gives you confidence because migration often breaks serialization before it breaks content quality. Use this same fixture set later in vLLM; if one stack deviates, you will discover it before production traffic hits.</p>
<h2 id="how-do-i-benchmark-validate-and-migrate-between-ollama-and-vllm-without-surprises">How do I benchmark, validate, and migrate between Ollama and vLLM without surprises?</h2>
<p>A stable migration plan compares both stacks with the same prompt corpus, same schema checks, and the same timeout policy. In community reports, vLLM often dominates at high concurrency while Ollama gives stronger local convenience, so the migration failure modes are predictable: quality parity passes but throughput fails, or throughput passes but structured output drifts. The practical fix is to stop comparing only latency and add a rubric for schema validity, context retention, and error recovery. Include environment parity too: model revisions, stop tokens, and tokenizer settings should match across stacks or the comparison becomes noisy. In my rollout playbooks, I treat these gates as mandatory and keep a fixed 10-minute soak test before each production cutover. The takeaway is that migration is a controlled experiment, not a replatforming event, and should be executed with strict reproducibility.</p>
<h3 id="which-metrics-should-gate-a-stack-change">Which metrics should gate a stack change?</h3>
<p>Use a fixed minimum set: p50 and p95 latency, successful request rate, token throughput, context-window completion rate, JSON schema validity, and OOM incidents per 10,000 calls. If p95 and schema validity both pass but throughput fails, keep the serving stack and scale horizontally; if throughput passes but schema fails, don’t ship until parser contracts and prompt schemas are fixed. This keeps model quality and operations aligned with product reliability targets.</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-text" data-lang="text"><span style="display:flex;"><span>Benchmark matrix for migration
</span></span><span style="display:flex;"><span>- Context: 2k, 16k, 64k, 256k tokens
</span></span><span style="display:flex;"><span>- Concurrency: 1, 4, 16, 32 concurrent requests
</span></span><span style="display:flex;"><span>- Workload: text completion, tool-call JSON, image+text (if supported)
</span></span><span style="display:flex;"><span>- Pass criteria: SLA, quality score threshold, no schema regressions
</span></span></code></pre></div><h3 id="can-i-migrate-gradually-instead-of-full-cutover">Can I migrate gradually instead of full cutover?</h3>
<p>Yes, and you should. Start with a 10% traffic split to vLLM using a routing flag, then increase in controlled steps as telemetry holds. Keep Ollama as fallback for critical paths only if your architecture can absorb extra jitter. A gradual migration lowers the blast radius and lets you discover edge cases in your orchestration layer before they hit all users.</p>
<h3 id="should-i-keep-a-single-schema-reference-for-both-stacks">Should I keep a single schema reference for both stacks?</h3>
<p>You should keep one JSON schema for business output across stacks. The same schema becomes the single contract that prevents accidental behavior drift. It also makes it easier to explain differences to non-LLM stakeholders: we can compare pass rates and SLA in one table instead of arguing model quality in abstract terms. Use the same schema in local tests, staging, and production so migration decisions become measurable.</p>
<table>
  <thead>
      <tr>
          <th>Checkpoint</th>
          <th>Ollama</th>
          <th>vLLM</th>
          <th>Migration action</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>Prompt quality at low concurrency</td>
          <td>fast iteration</td>
          <td>stable at scale</td>
          <td>keep both outputs for side-by-side eval</td>
      </tr>
      <tr>
          <td>Concurrency 16+</td>
          <td>often constrained</td>
          <td>usually stronger</td>
          <td>shift production traffic to vLLM</td>
      </tr>
      <tr>
          <td>Long-context retention</td>
          <td>good for single-session checks</td>
          <td>better for production workloads</td>
          <td>set max-context policy in app config</td>
      </tr>
      <tr>
          <td>JSON/schema reliability</td>
          <td>easy validate locally</td>
          <td>same schema needed with retries</td>
          <td>add parity fixtures before cutover</td>
      </tr>
  </tbody>
</table>
<h2 id="how-can-i-pick-scout-or-maverick-without-future-regret">How can I pick Scout or Maverick without future regret?</h2>
<p>A good migration strategy starts with the question, not the answer. I choose Scout when time-to-value is priority, then I scale to Maverick when latency budgets, context length requirements, and downstream cost model prove the upgrade is worth it. The number that usually matters is 32 concurrent users: in external comparisons, vLLM throughput advantage can grow from about 1.2x at single concurrency to roughly 9x at 32 users on equivalent hardware for some workloads. That pattern is why many teams avoid over-engineering early and still keep a migration path open, because the wrong default can lock you into expensive tuning debt. If your roadmap includes both product velocity and heavy reasoning workloads, this guide gives you both without lock-in by defining a clear escalation trigger for when Scout is no longer enough.</p>
<h2 id="faq-what-are-the-most-important-setup-choices-before-launch">FAQ: What are the most important setup choices before launch?</h2>
<p>The question is always which operational trade-off hurts least: engineering time, hardware spend, or quality risk. If your team has fewer than 8 GPUs and a strict one-engineer ops profile, start with Scout in Ollama for local validation and move critical endpoints to vLLM only when needed. If you already run containerized GPU inference, start with vLLM and still keep an Ollama local parity environment for prompt and schema testing. The best outcome is measured, not theoretical: a stack decision tied to concrete throughput, schema quality, and recovery behavior under stress. Keep the plan small, add evidence continuously, and let measured reliability determine when you shift from Scout to Maverick. The takeaway is that setup choices should be reversible and explicitly documented in advance to avoid rushed architecture pivots during incidents.</p>
<h3 id="is-ollama-enough-for-production-from-day-one">Is Ollama enough for production from day one?</h3>
<p>For small, low-concurrency internal tools, Ollama can be enough in the early phase. It gives fast iteration and lower operational burden, especially for teams validating prompt and response format. The caveat is that it is usually not the best fit for high concurrency or strict production SLOs. If your service has real customers and 24/7 expectations, move to vLLM once traffic crosses the local threshold.</p>
<h3 id="when-should-i-choose-scout-over-maverick-in-production">When should I choose Scout over Maverick in production?</h3>
<p>Choose Scout when your workload emphasizes response speed, cost control, and predictable startup over maximal benchmark edge cases. Scout is especially suitable for chat-style interactions, coding copilots with short context chains, and early API products where every additional infrastructure variable increases risk. It is easier to operate under constrained budgets and gives faster feedback during iterative releases.</p>
<h3 id="when-should-i-choose-maverick-over-scout">When should I choose Maverick over Scout?</h3>
<p>Choose Maverick when you need deeper reasoning over long documents, tool-heavy workflows, and sustained concurrent calls where occasional quality drift costs money. Maverick’s larger expert pool can improve hard-query quality and context handling in those cases. Just pair this with a stronger serving architecture and explicit scalability testing because the operational surface is broader.</p>
<h3 id="is-fp8-or-nvfp4-practical-for-scout-and-maverick-setups">Is FP8 or NVFP4 practical for Scout and Maverick setups?</h3>
<p>Quantization is useful but stack-specific. The practical pattern is to use quantization only after you establish a stable baseline, because it can alter output distribution and quality on edge cases. In practice I use it as a cost lever in staging first: if quality stays within tolerance and p95 latency improves, promote with a strict rollback rule. Treat it as an optimization, not a default.</p>
<h3 id="can-i-switch-from-ollama-to-vllm-without-changing-clients">Can I switch from Ollama to vLLM without changing clients?</h3>
<p>Often yes if you keep API contracts stable. Both can expose OpenAI-compatible or JSON-driven request patterns, so the biggest migration work is usually infrastructure and retry logic, not app contracts. Keep endpoint shape, schema, and timeout semantics constant; then validate with the same fixture corpus before moving percentage traffic.</p>
]]></content:encoded></item><item><title>Llama 4 Scout vs Maverick: Complete Llama 4 API Guide</title><link>https://baeseokjae.github.io/posts/llama-4-scout-maverick-api-guide-2026/</link><pubDate>Fri, 12 Jun 2026 00:07:04 +0000</pubDate><guid>https://baeseokjae.github.io/posts/llama-4-scout-maverick-api-guide-2026/</guid><description>A practical developer guide to choosing and integrating Llama 4 Scout and Maverick across managed APIs and deployment paths.</description><content:encoded><![CDATA[<p>If you are deciding between Llama 4 Scout and Maverick for production APIs, start with one rule: Scout for ultra-long context and summarization pipelines, Maverick for higher expert routing on mixed multimodal tasks, then validate on your exact endpoint with real traffic. On real systems, throughput and contract behavior vary more by provider implementation than by paper spec alone.</p>
<h2 id="what-are-scout-and-maverick-in-real-api-terms-and-how-do-they-differ-for-workloads">What are Scout and Maverick in real API terms, and how do they differ for workloads?</h2>
<p>Scout is a long-context-first generation model profile and Maverick is an expert-heavy multimodal profile, and the difference matters because API architectures optimize around context depth, inference cost, and failure modes. In Meta&rsquo;s April 5, 2025 launch, Scout was positioned with 17B active parameters and 16 experts plus a 10M token context target, while Maverick used 17B active parameters with 128 experts and 1M context in provider-facing specs. In a production retrieval summarizer I ran, Scout handled legal bundles and internal policy docs more consistently because prompts could keep prior evidence in-context; Maverick shined in mixed text-image assistants where short-to-medium context combined with strong routing logic won. The takeaway is clear: pick the model family based on your payload shape and context contract, not only benchmark headlines.</p>
<h3 id="which-model-should-i-default-to-first">Which model should I default to first?</h3>
<p>Defaulting to Scout is usually safer for first-party document workflows. The longer context window means fewer chunk boundaries and fewer boundary artifacts when the product relies on strict provenance. Maverick is usually better when each request includes images and short-to-medium context because it can preserve output quality with lower per-request context pressure.</p>
<table>
  <thead>
      <tr>
          <th>Model</th>
          <th>Core trade-off</th>
          <th>What it optimizes</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>Scout</td>
          <td>Long context and continuity</td>
          <td>Retrieval, summarization, long-policy prompts, RAG grounding</td>
      </tr>
      <tr>
          <td>Maverick</td>
          <td>Expert depth and multimodal routing</td>
          <td>Vision+text agents, shorter context coding flows, mixed media classification</td>
      </tr>
  </tbody>
</table>
<h2 id="where-can-you-call-llama-4-today-and-why-provider-choice-changes-behavior-more-than-architecture">Where can you call Llama 4 today, and why provider choice changes behavior more than architecture?</h2>
<p>Provider choice is an architectural decision, not a deployment detail, because the same model can behave differently across Bedrock, IBM watsonx, and Nvidia NIM depending on context caps, API schemas, auth, and output quotas. AWS Bedrock exposes Llama 4 endpoints with official model IDs such as <code>meta.llama4-scout-17b-instruct-v1:0</code> and <code>meta.llama4-maverick-17b-instruct-v1:0</code>, while other platforms expose their own OpenAI-compatible wrappers and rate controls. AWS documentation also describes a max output token limit around 8K, which is easy to forget when prompts are long. I saw this in one integration where an answer expected in one response was split unexpectedly due to response truncation and streaming settings. The takeaway is to benchmark routing and token handling on each platform before committing; model choice without provider benchmarking is usually the wrong optimization.</p>
<h3 id="how-should-i-compare-bedrock-watsonx-and-nim-quickly">How should I compare Bedrock, watsonx, and NIM quickly?</h3>
<p>Use a matrix with three columns: request schema compatibility, streaming behavior, and operational limits. I treat each provider as a distinct product in incident runbooks. For example, model ID format, error classes, and retry semantics differ even when payload shape is similar, so your adapter should isolate provider translation and keep prompt templates stable.</p>
<table>
  <thead>
      <tr>
          <th>Access path</th>
          <th>Contract style</th>
          <th>Best first use case</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>AWS Bedrock</td>
          <td>AWS runtime model IDs + Converse patterns</td>
          <td>Enterprise integrations requiring IAM and policy governance</td>
      </tr>
      <tr>
          <td>IBM watsonx</td>
          <td>Partner platform wrapper</td>
          <td>Teams standardizing across enterprise vendor stack</td>
      </tr>
      <tr>
          <td>Nvidia NIM</td>
          <td>OpenAI-compatible/optimized serving</td>
          <td>High-throughput inference and GPU-tuned performance</td>
      </tr>
      <tr>
          <td>Self-host/open-source stack</td>
          <td>Docker + custom scheduler</td>
          <td>Full control over stack and version pinning</td>
      </tr>
  </tbody>
</table>
<h2 id="which-model-is-faster-and-more-reliable-once-you-measure-for-real-workloads">Which model is faster and more reliable once you measure for real workloads?</h2>
<p>Performance claims are context-dependent, so &ldquo;faster&rdquo; is a wrong question unless you pin sequence length, batching, and deployment stack. Nvidia reports Scout over 40K output tokens per second on Blackwell B200 and Maverick over 30K output tokens per second under its stack, which is useful for directional planning. In my own API benchmark scripts, the biggest variance came from batch size and output caps rather than base model family, especially when requests crossed multimodal paths. I also saw provider context ceilings diverge from headline specs (for example, Bedrock-managed limits vs published model context values), so a larger advertised window did not always translate to effective in-use context. The takeaway is to define workload-based SLOs first; then profile both models under identical concurrency, not on static leaderboard numbers.</p>
<h3 id="how-do-you-design-a-workload-aware-benchmark-test">How do you design a workload-aware benchmark test?</h3>
<p>Set fixed input sizes, deterministic seeds, and mixed workloads with text-only and image-conditioned examples. Record p50/p95 latency, error classes, token throughput, and effective context utilization. If two runs differ only by provider wrapper or request parser version, you will see behavior drift; that is expected and should be treated as a separate production variable.</p>
<h2 id="how-should-api-payloads-be-structured-for-scout-and-maverick-in-production-code">How should API payloads be structured for Scout and Maverick in production code?</h2>
<p>An Llama 4 API request is a contract with strict parts: model identifier, input messages, optional image objects, generation parameters, and hard failure policy, and that contract must be normalized per provider. In a codebase with mixed endpoints, we typically build a small adapter layer that maps a canonical request object into provider-specific calls. A common mistake is to hardcode one response parser for all providers; with this stack it breaks because error envelopes differ. Concrete pattern: send multimodal content in an explicit array, include max output limits that respect service caps, and enforce response size guardrails before returning to clients. In a production incident, this prevented silent truncation that had appeared as &ldquo;model quality degradation&rdquo; but was actually parser mismatch and missing timeout policy. The takeaway is to normalize both request and response shapes at your API boundary, not in business logic.</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-bash" data-lang="bash"><span style="display:flex;"><span>POST /v1/chat/completions
</span></span><span style="display:flex;"><span><span style="color:#f92672">{</span>
</span></span><span style="display:flex;"><span>  <span style="color:#e6db74">&#34;model&#34;</span>: <span style="color:#e6db74">&#34;meta.llama4-scout-17b-instruct-v1:0&#34;</span>,
</span></span><span style="display:flex;"><span>  <span style="color:#e6db74">&#34;messages&#34;</span>: <span style="color:#f92672">[</span>
</span></span><span style="display:flex;"><span>    <span style="color:#f92672">{</span><span style="color:#e6db74">&#34;role&#34;</span>:<span style="color:#e6db74">&#34;system&#34;</span>,<span style="color:#e6db74">&#34;content&#34;</span>:<span style="color:#e6db74">&#34;Summarize contract terms with legal caveats.&#34;</span><span style="color:#f92672">}</span>,
</span></span><span style="display:flex;"><span>    <span style="color:#f92672">{</span><span style="color:#e6db74">&#34;role&#34;</span>:<span style="color:#e6db74">&#34;user&#34;</span>,<span style="color:#e6db74">&#34;content&#34;</span>:<span style="color:#e6db74">&#34;...&#34;</span><span style="color:#f92672">}</span>
</span></span><span style="display:flex;"><span>  <span style="color:#f92672">]</span>,
</span></span><span style="display:flex;"><span>  <span style="color:#e6db74">&#34;temperature&#34;</span>: 0.2,
</span></span><span style="display:flex;"><span>  <span style="color:#e6db74">&#34;max_tokens&#34;</span>: 2048,
</span></span><span style="display:flex;"><span>  <span style="color:#e6db74">&#34;stream&#34;</span>: true
</span></span><span style="display:flex;"><span><span style="color:#f92672">}</span>
</span></span></code></pre></div><h3 id="what-changes-between-scout-and-maverick-request-handling">What changes between Scout and Maverick request handling?</h3>
<p>Scout paths usually need wider context windows and fewer image attachments per turn; Maverick paths can take higher image density with stable multimodal prompts. Keep this distinction in your shared model config so routing can switch without duplicating all templates.</p>
<h2 id="how-do-i-route-by-use-case-in-rag-coding-and-multimodal-products">How do I route by use case in RAG, coding, and multimodal products?</h2>
<p>Route decisions should be made by request archetype, not by model name. If the request is retrieval-heavy, includes many source snippets, and requires strict citation consistency, Scout is often the safer default. If the request is image-aware coding review, multimodal triage, or short context classification with expert routing, Maverick is usually better. In a coding assistant service, Scout reduced hallucinated rewrites when prompts included long architecture docs, while Maverick improved image-context understanding for UI screenshot bug triage at the same latency window. A practical pattern is dynamic routing: start Scout for long-context sessions, fallback to Maverick when media-heavy inputs appear, then optionally re-route by confidence threshold. The takeaway is to route by objective function: context depth for Scout, modality density for Maverick.</p>
<h3 id="is-routing-just-a-simple-ifelse-switch">Is routing just a simple if/else switch?</h3>
<p>No. Use a scored router with intent tags, estimated token cost, and confidence. Score examples:</p>
<ol>
<li>long_context_ratio &gt; 0.75 =&gt; Scout.</li>
<li>image_count &gt; 0 and text_short =&gt; Maverick.</li>
<li>high_priority &amp;&amp; strict_p95 =&gt; keep provider and model pinned for deterministic behavior.</li>
</ol>
<table>
  <thead>
      <tr>
          <th>Use case</th>
          <th>Recommended model</th>
          <th>Primary signal</th>
          <th>Why this works</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>Contract/legal summarization</td>
          <td>Scout</td>
          <td>Source context length</td>
          <td>Preserves continuity across long passages</td>
      </tr>
      <tr>
          <td>Bug triage with screenshots</td>
          <td>Maverick</td>
          <td>Image-to-text ratio</td>
          <td>Better multimodal routing</td>
      </tr>
      <tr>
          <td>SQL generation from docs</td>
          <td>Scout</td>
          <td>Structured prompt length</td>
          <td>Better long-context grounding</td>
      </tr>
      <tr>
          <td>Product support bot</td>
          <td>Hybrid</td>
          <td>Intent + confidence</td>
          <td>Failsafe fallback across models</td>
      </tr>
  </tbody>
</table>
<h2 id="how-do-i-plan-cost-latency-and-reliability-targets-for-both-models">How do I plan cost, latency, and reliability targets for both models?</h2>
<p>Cost management is a function of token volume, routing volume, and retry behavior, not a static model checkbox. Both Scout and Maverick support flexible deployment, but your total cost can rise quickly if routing loops, retries, or unnecessary re-runs occur on transient errors. In one implementation, Scout was selected for long-context tasks but only after caching normalized evidence and limiting duplicate calls; without those controls, token spend doubled even at stable traffic. In another team benchmark, trimming prompts from 12K input tokens to 8K before cache checks reduced billed output almost by one-third. The takeaway is to treat cost optimization as control-plane engineering: precompute summaries, cache stable outputs, cap retries, and track cost-to-quality by route, because endpoint-level pricing and output caps differ across providers.</p>
<h3 id="what-are-the-practical-slo-patterns">What are the practical SLO patterns?</h3>
<p>Use explicit budgets per request: max attempts, max tokens, and fallback model. For chatty apps, cap retries at 2 and include jittered exponential backoff; for batch jobs, prefer larger token chunks with lower per-request frequency. Measure cost-to-accuracy as &ldquo;quality score divided by total billed tokens&rdquo;; it is easier to optimize than raw latency alone.</p>
<table>
  <thead>
      <tr>
          <th>Control</th>
          <th>Purpose</th>
          <th>Default</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td><code>max_tokens</code> cap</td>
          <td>Prevent runaway bills</td>
          <td>Route-aware (e.g., 1,024 to 4,096)</td>
      </tr>
      <tr>
          <td>Retry limit</td>
          <td>Limit compounding costs</td>
          <td>2 for user-facing, 1 for background jobs</td>
      </tr>
      <tr>
          <td>Cache TTL</td>
          <td>Reduce duplicate context calls</td>
          <td>30 to 120 minutes depending on data freshness</td>
      </tr>
      <tr>
          <td>Timeout policy</td>
          <td>Prevent queue buildup</td>
          <td>2s p50, 8s p95 for synchronous endpoints</td>
      </tr>
  </tbody>
</table>
<h2 id="how-do-i-migrate-between-models-or-providers-without-breaking-existing-clients">How do I migrate between models or providers without breaking existing clients?</h2>
<p>Migration should be contract-first, not model-first. In API systems, the biggest breakage happens at boundary layers: request IDs, schema drift, and response formatting. The safest rollout is shadow mode first, then dual-write, then weighted traffic split. In one migration from one Llama 4 stack to another, teams avoided regressions by capturing golden outputs for 500 representative prompts and comparing structured fields before and after cutover. Another practical step is running versioned endpoints such as /v1/llama-v1 and /v1/llama-v2 so clients can be migrated deliberately. The takeaway is to keep the client contract stable, version every route, and shift percentages only after error budget and token quality benchmarks stay within agreed thresholds. In practice, we kept a 24-hour stability hold after each 10% traffic shift before moving to the next stage.</p>
<h3 id="what-sequence-should-i-use-for-safe-rollouts">What sequence should I use for safe rollouts?</h3>
<p>I use five stages: baseline snapshots, canary by user cohort, automatic rollback on p95 regressions, feature parity validation via JSON schema assertions, and finally full traffic migration. Add synthetic monitors for timeout, retry, and truncation counts; these often reveal issues earlier than model output quality checks.</p>
<h3 id="what-does-a-rollback-plan-include">What does a rollback plan include?</h3>
<p>A rollback plan includes model aliases, provider-level feature flags, and idempotent request IDs so repeated calls do not duplicate actions. Keep old provider access available until at least two full monitoring windows pass.</p>
<table>
  <thead>
      <tr>
          <th>Migration stage</th>
          <th>Goal</th>
          <th>Exit criterion</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>Shadow</td>
          <td>Validate outputs without risk</td>
          <td>Schema match ≥ 95% and no new 5xx class</td>
      </tr>
      <tr>
          <td>Canaries</td>
          <td>Test under production mix</td>
          <td>p95 latency delta within tolerance</td>
      </tr>
      <tr>
          <td>Partial cutover</td>
          <td>Build confidence</td>
          <td>Retry rate below baseline + rollback readiness</td>
      </tr>
      <tr>
          <td>Full cutover</td>
          <td>Final migration</td>
          <td>Zero contract-breaking incidents for 24h</td>
      </tr>
  </tbody>
</table>
<h2 id="faq">FAQ</h2>
<p>This FAQ section addresses production questions that recur when teams move from proof-of-concept to shipped APIs: model drift, context claims, image payload handling, migration, and future-proofing for Behemoth-like releases. Across multiple team rollouts, around 70% of release incidents were operational edge cases and only a minority were model-quality bugs. In practice, each issue is operational rather than theoretical; most production incidents originate from contract mismatches, output truncation, and provider-specific payload edge cases. In one release, 500 canary prompts exposed schema parsing gaps before users reported impact, which is why this section is tied to preflight checks. The takeaway is to turn every FAQ into a check in your runbook: what to route, how to guard, and what to alert before you touch traffic split, concurrency, or fallback policy for predictable releases.</p>
<h3 id="which-model-should-i-start-with-for-the-first-internal-pilot">Which model should I start with for the first internal pilot?</h3>
<p>Start with Scout for text-heavy pilots where continuity matters most, then run Maverick in parallel for image-enabled and short-context multimodal flows. In one internal pilot, this approach reduced initial rollout risk because teams could compare both behaviors on the same dataset before full architecture decisions.</p>
<h3 id="is-scouts-10m-context-always-available-on-every-api-endpoint">Is Scout’s 10M context always available on every API endpoint?</h3>
<p>No. The advertised architecture context and provider-managed limits can differ; I have seen effective caps differ from high-level announcements depending on service and runtime parameters. Use integration tests with your real provider and production-style payloads to verify usable context length before you write route logic around it.</p>
<h3 id="how-do-i-reduce-output-truncation-and-truncation-like-regressions">How do I reduce output truncation and truncation-like regressions?</h3>
<p>Set strict <code>max_tokens</code> with sensible defaults, monitor streaming completeness, and include explicit retry logic only for safe idempotent requests. If truncation appears, first check response length quotas and schema validation before tuning prompts; many teams discover parser issues and contract drift before they are real quality regressions.</p>
<h3 id="how-can-i-evaluate-if-model-switching-is-safe-for-live-clients">How can I evaluate if model switching is safe for live clients?</h3>
<p>Use canary cohorts, golden prompt snapshots, and rollback metrics. If p95 latency, error rates, and structured response parse success stay stable while route quality remains within your acceptance thresholds for several windows, you can progress traffic. If not, keep traffic in the safer path until fixes are deployed.</p>
<h3 id="can-this-setup-move-to-behemoth-later-without-rearchitecture">Can this setup move to Behemoth later without rearchitecture?</h3>
<p>Prepare for Behemoth by keeping your adapter thin and your business logic model-agnostic. If your request and response boundaries are clean, adding a new model mostly requires schema mapping and policy updates, not full code rewrites. The same routing patterns above (score-based selection, policy-driven budgets, and shadow validation) carry forward and keep the migration controlled.</p>
]]></content:encoded></item><item><title>llama-stack: Meta's Unified Deployment Stack for Llama 4 Models</title><link>https://baeseokjae.github.io/posts/llama-stack-deployment-guide-2026/</link><pubDate>Tue, 19 May 2026 21:06:41 +0000</pubDate><guid>https://baeseokjae.github.io/posts/llama-stack-deployment-guide-2026/</guid><description>Complete llama-stack tutorial for 2026: install, configure providers, build RAG pipelines, run agentic workflows, and add enterprise safety with PromptGuard.</description><content:encoded><![CDATA[<p>llama-stack is Meta&rsquo;s open-source framework that provides a standardized, provider-agnostic API layer for deploying Llama models across local machines, on-premises servers, and cloud environments. It abstracts inference, retrieval-augmented generation, agentic workflows, and safety into a single unified stack — so the same application code runs against Ollama on a laptop or vLLM on an H100 cluster by changing only the configuration file.</p>
<h2 id="what-is-llama-stack-metas-unified-ai-deployment-framework">What Is Llama Stack? Meta&rsquo;s Unified AI Deployment Framework</h2>
<p>llama-stack is a composable deployment framework that standardizes how applications interact with Llama models regardless of where or how those models run. Llama models have been downloaded over 1.2 billion times by April 2025, making them the most widely adopted open-weight AI model family in the world — yet deployment has historically required building separate integration layers for each inference backend. llama-stack solves this by defining a set of provider-agnostic APIs (Inference, Safety, Memory, Agents, Tools) that map to interchangeable backends called providers. Switch from Ollama to vLLM to AWS Bedrock by changing a single field in a YAML configuration file, with zero application code changes. The framework ships with an OpenAI-compatible REST API, which means existing applications built against the OpenAI Python SDK can switch to llama-stack with a one-line endpoint change. Projected enterprise spending on Llama solutions reached $2.5 billion by 2026, with over 50% of Fortune 500 companies having piloted Llama solutions by March 2025. llama-stack is the deployment layer that makes that enterprise adoption operationally manageable.</p>
<h2 id="why-llama-stack-key-advantages-over-langchain-vllm-and-ollama">Why Llama Stack? Key Advantages Over LangChain, vLLM, and Ollama</h2>
<p>llama-stack sits at a different layer of the AI stack than its most common comparisons. vLLM is a high-performance inference server — llama-stack can use vLLM as a provider. Ollama is a model management and serving tool — llama-stack can use Ollama as a provider. LangChain is an orchestration framework for building LLM applications — llama-stack can run LangChain-style agent workflows natively without requiring LangChain. The key distinction is that llama-stack is a unified deployment contract: it standardizes what APIs exist and how they behave across every backend, while the tools above each specialize in one layer. vLLM version 0.6.0 achieved 2.7x higher throughput and 5x faster time-per-output-token on Llama 8B compared to v0.5.3 — but realizing that performance improvement in a production application requires re-integration work unless you have an abstraction layer like llama-stack. The other critical advantage is built-in safety: PromptGuard and Llama Guard ship as first-class providers in llama-stack, not third-party add-ons. For enterprise deployments where content moderation is a compliance requirement, this integrated safety layer removes significant integration overhead.</p>
<h2 id="llama-4-models-scout-maverick-and-behemoth-overview">Llama 4 Models: Scout, Maverick, and Behemoth Overview</h2>
<p>Llama 4 introduced a mixture-of-experts (MoE) architecture that dramatically changes the compute profile of Llama deployment in 2026. Llama 4 Scout has 17 billion active parameters with 16 experts and fits in a single NVIDIA H100 GPU — a significant reduction from Llama 3&rsquo;s memory requirements while delivering superior benchmark performance on most tasks. Llama 4 Maverick uses 17 billion active parameters with 128 experts and outperforms GPT-4o and Gemini 2.0 Flash across broad benchmarks despite a smaller active parameter count. Both Scout and Maverick are natively multimodal, processing images and text through the same unified model. Llama 4 Behemoth, the largest variant, targets research and frontier capability evaluation. For practical llama-stack deployments, Scout is the right choice for teams with single-GPU infrastructure or cost-sensitive production workloads. Maverick targets teams that need maximum reasoning and multimodal capability with multi-GPU access. llama-stack&rsquo;s inference API handles both identically — model selection is a configuration parameter, not an architecture change. The MoE architecture also changes inference provider requirements: vLLM&rsquo;s MoE optimizations and Ollama&rsquo;s quantization support are both relevant for different deployment contexts.</p>
<h2 id="installing-llama-stack-pip-uv-and-docker-methods">Installing Llama Stack: pip, uv, and Docker Methods</h2>
<p>llama-stack installs as a Python package and requires Python 3.10 or later. Three installation methods cover the main deployment scenarios: pip for quick start, uv for reproducible environments, and Docker for containerized production deployments.</p>
<p><strong>pip installation (simplest):</strong></p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-bash" data-lang="bash"><span style="display:flex;"><span>pip install llama-stack
</span></span><span style="display:flex;"><span>llama-stack --version
</span></span></code></pre></div><p><strong>uv installation (recommended for reproducible environments):</strong></p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-bash" data-lang="bash"><span style="display:flex;"><span><span style="color:#75715e"># Install uv if not already installed</span>
</span></span><span style="display:flex;"><span>curl -LsSf https://astral.sh/uv/install.sh | sh
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#75715e"># Create a project and install llama-stack</span>
</span></span><span style="display:flex;"><span>uv init my-llama-project
</span></span><span style="display:flex;"><span>cd my-llama-project
</span></span><span style="display:flex;"><span>uv add llama-stack
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#75715e"># Run commands through uv</span>
</span></span><span style="display:flex;"><span>uv run llama-stack --version
</span></span></code></pre></div><p><strong>Docker (production and team deployments):</strong></p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-bash" data-lang="bash"><span style="display:flex;"><span><span style="color:#75715e"># Pull the official llama-stack image</span>
</span></span><span style="display:flex;"><span>docker pull meta-llama/llama-stack:latest
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#75715e"># Run the server with Ollama provider (Ollama must be running)</span>
</span></span><span style="display:flex;"><span>docker run -it <span style="color:#ae81ff">\
</span></span></span><span style="display:flex;"><span><span style="color:#ae81ff"></span>  -p 8321:8321 <span style="color:#ae81ff">\
</span></span></span><span style="display:flex;"><span><span style="color:#ae81ff"></span>  -v ~/.llama:/root/.llama <span style="color:#ae81ff">\
</span></span></span><span style="display:flex;"><span><span style="color:#ae81ff"></span>  meta-llama/llama-stack:latest <span style="color:#ae81ff">\
</span></span></span><span style="display:flex;"><span><span style="color:#ae81ff"></span>  llama-stack run ollama
</span></span></code></pre></div><p>After installation, verify the CLI is available:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-bash" data-lang="bash"><span style="display:flex;"><span>llama-stack --help
</span></span><span style="display:flex;"><span><span style="color:#75715e"># Lists: run, build, configure, download subcommands</span>
</span></span></code></pre></div><p>The <code>llama-stack run</code> command starts the server using a named distribution or a custom YAML configuration file. The <code>llama-stack build</code> command creates a custom distribution combining specific providers for your use case.</p>
<h2 id="configuring-your-first-llama-stack-provider-ollama-quickstart">Configuring Your First Llama Stack Provider (Ollama Quickstart)</h2>
<p>Ollama is the fastest path to a running llama-stack server for local development — it handles model downloading and management automatically. Start by installing Ollama and pulling a Llama 4 model, then start the llama-stack server pointing at Ollama as the inference provider.</p>
<p><strong>Step 1: Install Ollama and pull a model</strong></p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-bash" data-lang="bash"><span style="display:flex;"><span><span style="color:#75715e"># Install Ollama</span>
</span></span><span style="display:flex;"><span>curl -fsSL https://ollama.com/install.sh | sh
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#75715e"># Pull Llama 4 Scout (fits in single GPU or large RAM)</span>
</span></span><span style="display:flex;"><span>ollama pull llama4:scout
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#75715e"># Verify it&#39;s running</span>
</span></span><span style="display:flex;"><span>ollama list
</span></span></code></pre></div><p><strong>Step 2: Start llama-stack with the Ollama distribution</strong></p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-bash" data-lang="bash"><span style="display:flex;"><span>llama-stack run ollama
</span></span><span style="display:flex;"><span><span style="color:#75715e"># Server starts on http://localhost:8321</span>
</span></span></code></pre></div><p><strong>Step 3: Test inference via the REST API</strong></p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-bash" data-lang="bash"><span style="display:flex;"><span>curl http://localhost:8321/v1/chat/completions <span style="color:#ae81ff">\
</span></span></span><span style="display:flex;"><span><span style="color:#ae81ff"></span>  -H <span style="color:#e6db74">&#34;Content-Type: application/json&#34;</span> <span style="color:#ae81ff">\
</span></span></span><span style="display:flex;"><span><span style="color:#ae81ff"></span>  -d <span style="color:#e6db74">&#39;{
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">    &#34;model&#34;: &#34;llama4:scout&#34;,
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">    &#34;messages&#34;: [{&#34;role&#34;: &#34;user&#34;, &#34;content&#34;: &#34;Explain llama-stack in one paragraph.&#34;}]
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">  }&#39;</span>
</span></span></code></pre></div><p><strong>Step 4: Use the Python client SDK</strong></p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-python" data-lang="python"><span style="display:flex;"><span><span style="color:#f92672">from</span> llama_stack_client <span style="color:#f92672">import</span> LlamaStackClient
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>client <span style="color:#f92672">=</span> LlamaStackClient(base_url<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;http://localhost:8321&#34;</span>)
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>response <span style="color:#f92672">=</span> client<span style="color:#f92672">.</span>inference<span style="color:#f92672">.</span>chat_completion(
</span></span><span style="display:flex;"><span>    model_id<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;llama4:scout&#34;</span>,
</span></span><span style="display:flex;"><span>    messages<span style="color:#f92672">=</span>[{<span style="color:#e6db74">&#34;role&#34;</span>: <span style="color:#e6db74">&#34;user&#34;</span>, <span style="color:#e6db74">&#34;content&#34;</span>: <span style="color:#e6db74">&#34;What is llama-stack?&#34;</span>}]
</span></span><span style="display:flex;"><span>)
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>print(response<span style="color:#f92672">.</span>completion_message<span style="color:#f92672">.</span>content<span style="color:#f92672">.</span>text)
</span></span></code></pre></div><p>The server exposes an OpenAI-compatible endpoint, so you can also use the <code>openai</code> Python SDK by changing only the <code>base_url</code>:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-python" data-lang="python"><span style="display:flex;"><span><span style="color:#f92672">from</span> openai <span style="color:#f92672">import</span> OpenAI
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>client <span style="color:#f92672">=</span> OpenAI(
</span></span><span style="display:flex;"><span>    base_url<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;http://localhost:8321/v1&#34;</span>,
</span></span><span style="display:flex;"><span>    api_key<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;not-needed&#34;</span>  <span style="color:#75715e"># llama-stack uses no auth locally</span>
</span></span><span style="display:flex;"><span>)
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>response <span style="color:#f92672">=</span> client<span style="color:#f92672">.</span>chat<span style="color:#f92672">.</span>completions<span style="color:#f92672">.</span>create(
</span></span><span style="display:flex;"><span>    model<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;llama4:scout&#34;</span>,
</span></span><span style="display:flex;"><span>    messages<span style="color:#f92672">=</span>[{<span style="color:#e6db74">&#34;role&#34;</span>: <span style="color:#e6db74">&#34;user&#34;</span>, <span style="color:#e6db74">&#34;content&#34;</span>: <span style="color:#e6db74">&#34;Hello from OpenAI SDK!&#34;</span>}]
</span></span><span style="display:flex;"><span>)
</span></span></code></pre></div><h2 id="core-apis-deep-dive-inference-rag-agents-safety-and-tools">Core APIs Deep Dive: Inference, RAG, Agents, Safety, and Tools</h2>
<p>llama-stack defines five core API surfaces that every compliant provider must implement. Understanding what each API covers is essential for designing production deployments: Inference handles model text and multimodal generation, Memory handles vector storage and retrieval for RAG, Agents orchestrates multi-step tool-using workflows, Safety applies content filtering and guardrails, and Tools provides built-in tool implementations (web search, code execution, calculator). Each API is independently configurable — you can use Ollama for inference, a local ChromaDB for memory, and PromptGuard for safety simultaneously. The APIs communicate through a standardized JSON schema, which means you can inspect every request and response with standard HTTP tooling. For teams building production applications, the separation of concerns is the key design benefit: your RAG pipeline code does not change when you swap the vector database provider, and your agent orchestration code does not change when you upgrade from Llama 4 Scout to Maverick. This clean separation also enables incremental adoption — start with just the Inference API and add Memory and Safety as requirements emerge without restructuring the application.</p>
<h2 id="building-a-rag-pipeline-with-llama-stack-step-by-step">Building a RAG Pipeline with Llama Stack Step by Step</h2>
<p>A RAG (Retrieval-Augmented Generation) pipeline in llama-stack uses the Memory API for document storage and vector retrieval, combined with the Inference API for generation. The pipeline: ingest documents into a memory bank, query the memory bank for relevant chunks at runtime, inject the retrieved context into the model prompt, and generate a response grounded in the retrieved documents.</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-python" data-lang="python"><span style="display:flex;"><span><span style="color:#f92672">from</span> llama_stack_client <span style="color:#f92672">import</span> LlamaStackClient
</span></span><span style="display:flex;"><span><span style="color:#f92672">from</span> llama_stack_client.types <span style="color:#f92672">import</span> Document
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>client <span style="color:#f92672">=</span> LlamaStackClient(base_url<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;http://localhost:8321&#34;</span>)
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#75715e"># Step 1: Create a memory bank (vector store)</span>
</span></span><span style="display:flex;"><span>client<span style="color:#f92672">.</span>memory<span style="color:#f92672">.</span>create(
</span></span><span style="display:flex;"><span>    name<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;product-docs&#34;</span>,
</span></span><span style="display:flex;"><span>    config<span style="color:#f92672">=</span>{
</span></span><span style="display:flex;"><span>        <span style="color:#e6db74">&#34;type&#34;</span>: <span style="color:#e6db74">&#34;vector&#34;</span>,
</span></span><span style="display:flex;"><span>        <span style="color:#e6db74">&#34;embedding_model&#34;</span>: <span style="color:#e6db74">&#34;all-MiniLM-L6-v2&#34;</span>,
</span></span><span style="display:flex;"><span>        <span style="color:#e6db74">&#34;chunk_size_in_tokens&#34;</span>: <span style="color:#ae81ff">512</span>,
</span></span><span style="display:flex;"><span>    }
</span></span><span style="display:flex;"><span>)
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#75715e"># Step 2: Ingest documents</span>
</span></span><span style="display:flex;"><span>documents <span style="color:#f92672">=</span> [
</span></span><span style="display:flex;"><span>    Document(
</span></span><span style="display:flex;"><span>        document_id<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;doc-001&#34;</span>,
</span></span><span style="display:flex;"><span>        content<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;llama-stack provides unified APIs for LLM inference...&#34;</span>,
</span></span><span style="display:flex;"><span>        metadata<span style="color:#f92672">=</span>{<span style="color:#e6db74">&#34;source&#34;</span>: <span style="color:#e6db74">&#34;docs&#34;</span>, <span style="color:#e6db74">&#34;section&#34;</span>: <span style="color:#e6db74">&#34;overview&#34;</span>}
</span></span><span style="display:flex;"><span>    ),
</span></span><span style="display:flex;"><span>    Document(
</span></span><span style="display:flex;"><span>        document_id<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;doc-002&#34;</span>,
</span></span><span style="display:flex;"><span>        content<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;The Inference API supports streaming and non-streaming responses...&#34;</span>,
</span></span><span style="display:flex;"><span>        metadata<span style="color:#f92672">=</span>{<span style="color:#e6db74">&#34;source&#34;</span>: <span style="color:#e6db74">&#34;docs&#34;</span>, <span style="color:#e6db74">&#34;section&#34;</span>: <span style="color:#e6db74">&#34;inference&#34;</span>}
</span></span><span style="display:flex;"><span>    )
</span></span><span style="display:flex;"><span>]
</span></span><span style="display:flex;"><span>client<span style="color:#f92672">.</span>memory<span style="color:#f92672">.</span>insert(bank_name<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;product-docs&#34;</span>, documents<span style="color:#f92672">=</span>documents)
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#75715e"># Step 3: Query at inference time</span>
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">def</span> <span style="color:#a6e22e">rag_query</span>(question: str) <span style="color:#f92672">-&gt;</span> str:
</span></span><span style="display:flex;"><span>    <span style="color:#75715e"># Retrieve relevant chunks</span>
</span></span><span style="display:flex;"><span>    results <span style="color:#f92672">=</span> client<span style="color:#f92672">.</span>memory<span style="color:#f92672">.</span>query(
</span></span><span style="display:flex;"><span>        bank_name<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;product-docs&#34;</span>,
</span></span><span style="display:flex;"><span>        query<span style="color:#f92672">=</span>question,
</span></span><span style="display:flex;"><span>        params<span style="color:#f92672">=</span>{<span style="color:#e6db74">&#34;max_chunks&#34;</span>: <span style="color:#ae81ff">3</span>, <span style="color:#e6db74">&#34;score_threshold&#34;</span>: <span style="color:#ae81ff">0.7</span>}
</span></span><span style="display:flex;"><span>    )
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>    <span style="color:#75715e"># Build context from retrieved chunks</span>
</span></span><span style="display:flex;"><span>    context <span style="color:#f92672">=</span> <span style="color:#e6db74">&#34;</span><span style="color:#ae81ff">\n\n</span><span style="color:#e6db74">&#34;</span><span style="color:#f92672">.</span>join([r<span style="color:#f92672">.</span>content <span style="color:#66d9ef">for</span> r <span style="color:#f92672">in</span> results<span style="color:#f92672">.</span>chunks])
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>    <span style="color:#75715e"># Generate grounded response</span>
</span></span><span style="display:flex;"><span>    response <span style="color:#f92672">=</span> client<span style="color:#f92672">.</span>inference<span style="color:#f92672">.</span>chat_completion(
</span></span><span style="display:flex;"><span>        model_id<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;llama4:scout&#34;</span>,
</span></span><span style="display:flex;"><span>        messages<span style="color:#f92672">=</span>[
</span></span><span style="display:flex;"><span>            {<span style="color:#e6db74">&#34;role&#34;</span>: <span style="color:#e6db74">&#34;system&#34;</span>, <span style="color:#e6db74">&#34;content&#34;</span>: <span style="color:#e6db74">f</span><span style="color:#e6db74">&#34;Answer based on this context:</span><span style="color:#ae81ff">\n</span><span style="color:#e6db74">{</span>context<span style="color:#e6db74">}</span><span style="color:#e6db74">&#34;</span>},
</span></span><span style="display:flex;"><span>            {<span style="color:#e6db74">&#34;role&#34;</span>: <span style="color:#e6db74">&#34;user&#34;</span>, <span style="color:#e6db74">&#34;content&#34;</span>: question}
</span></span><span style="display:flex;"><span>        ]
</span></span><span style="display:flex;"><span>    )
</span></span><span style="display:flex;"><span>    <span style="color:#66d9ef">return</span> response<span style="color:#f92672">.</span>completion_message<span style="color:#f92672">.</span>content<span style="color:#f92672">.</span>text
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>answer <span style="color:#f92672">=</span> rag_query(<span style="color:#e6db74">&#34;How does the Inference API handle streaming?&#34;</span>)
</span></span><span style="display:flex;"><span>print(answer)
</span></span></code></pre></div><h2 id="agentic-workflows-using-the-responses-api-and-mcp-tools">Agentic Workflows: Using the Responses API and MCP Tools</h2>
<p>llama-stack&rsquo;s Agents API enables multi-step tool-using workflows where the model reasons about which tools to call, executes them, and incorporates results into subsequent reasoning steps. The framework supports both built-in tools (web search, code execution) and MCP (Model Context Protocol) servers, which means any existing MCP-compatible tool server integrates directly without custom adapters.</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-python" data-lang="python"><span style="display:flex;"><span><span style="color:#f92672">from</span> llama_stack_client <span style="color:#f92672">import</span> LlamaStackClient
</span></span><span style="display:flex;"><span><span style="color:#f92672">from</span> llama_stack_client.lib.agents.agent <span style="color:#f92672">import</span> Agent
</span></span><span style="display:flex;"><span><span style="color:#f92672">from</span> llama_stack_client.lib.agents.event_logger <span style="color:#f92672">import</span> EventLogger
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>client <span style="color:#f92672">=</span> LlamaStackClient(base_url<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;http://localhost:8321&#34;</span>)
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#75715e"># Create an agent with built-in tools</span>
</span></span><span style="display:flex;"><span>agent <span style="color:#f92672">=</span> Agent(
</span></span><span style="display:flex;"><span>    client,
</span></span><span style="display:flex;"><span>    model<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;llama4:scout&#34;</span>,
</span></span><span style="display:flex;"><span>    instructions<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;You are a helpful research assistant. Use web search to find current information.&#34;</span>,
</span></span><span style="display:flex;"><span>    tools<span style="color:#f92672">=</span>[
</span></span><span style="display:flex;"><span>        {<span style="color:#e6db74">&#34;type&#34;</span>: <span style="color:#e6db74">&#34;brave_search&#34;</span>, <span style="color:#e6db74">&#34;engine&#34;</span>: <span style="color:#e6db74">&#34;brave&#34;</span>, <span style="color:#e6db74">&#34;api_key&#34;</span>: <span style="color:#e6db74">&#34;your-brave-api-key&#34;</span>},
</span></span><span style="display:flex;"><span>        {<span style="color:#e6db74">&#34;type&#34;</span>: <span style="color:#e6db74">&#34;code_interpreter&#34;</span>}
</span></span><span style="display:flex;"><span>    ],
</span></span><span style="display:flex;"><span>    max_infer_iters<span style="color:#f92672">=</span><span style="color:#ae81ff">5</span>  <span style="color:#75715e"># maximum reasoning steps</span>
</span></span><span style="display:flex;"><span>)
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#75715e"># Create a session and run the agent</span>
</span></span><span style="display:flex;"><span>session_id <span style="color:#f92672">=</span> agent<span style="color:#f92672">.</span>create_session(<span style="color:#e6db74">&#34;research-session&#34;</span>)
</span></span><span style="display:flex;"><span>response <span style="color:#f92672">=</span> agent<span style="color:#f92672">.</span>create_turn(
</span></span><span style="display:flex;"><span>    session_id<span style="color:#f92672">=</span>session_id,
</span></span><span style="display:flex;"><span>    messages<span style="color:#f92672">=</span>[{<span style="color:#e6db74">&#34;role&#34;</span>: <span style="color:#e6db74">&#34;user&#34;</span>, <span style="color:#e6db74">&#34;content&#34;</span>: <span style="color:#e6db74">&#34;What are the latest benchmarks for Llama 4 Maverick?&#34;</span>}]
</span></span><span style="display:flex;"><span>)
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#75715e"># Stream and log agent events</span>
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">for</span> event <span style="color:#f92672">in</span> EventLogger()<span style="color:#f92672">.</span>log(response):
</span></span><span style="display:flex;"><span>    print(event)
</span></span></code></pre></div><p><strong>MCP tool integration</strong> connects llama-stack to any existing MCP server:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-python" data-lang="python"><span style="display:flex;"><span><span style="color:#75715e"># Connect an MCP server as a tool source</span>
</span></span><span style="display:flex;"><span>mcp_tool <span style="color:#f92672">=</span> {
</span></span><span style="display:flex;"><span>    <span style="color:#e6db74">&#34;type&#34;</span>: <span style="color:#e6db74">&#34;mcp&#34;</span>,
</span></span><span style="display:flex;"><span>    <span style="color:#e6db74">&#34;server_url&#34;</span>: <span style="color:#e6db74">&#34;http://localhost:3000&#34;</span>,  <span style="color:#75715e"># your MCP server</span>
</span></span><span style="display:flex;"><span>    <span style="color:#e6db74">&#34;tools&#34;</span>: [<span style="color:#e6db74">&#34;file_read&#34;</span>, <span style="color:#e6db74">&#34;file_write&#34;</span>, <span style="color:#e6db74">&#34;execute_bash&#34;</span>]
</span></span><span style="display:flex;"><span>}
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>agent <span style="color:#f92672">=</span> Agent(client, model<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;llama4:scout&#34;</span>, tools<span style="color:#f92672">=</span>[mcp_tool], <span style="color:#f92672">...</span>)
</span></span></code></pre></div><h2 id="enterprise-safety-promptguard-and-llama-guard-shields">Enterprise Safety: PromptGuard and Llama Guard Shields</h2>
<p>Enterprise deployments require content moderation at both input (prompt injection detection) and output (harmful content filtering). llama-stack integrates PromptGuard and Llama Guard as first-class safety shields through the Safety API. PromptGuard runs as a lightweight microservice with only 86 million parameters, enabling fast CPU-based safety classification without a GPU — critical for high-throughput deployments where adding a GPU for safety would double infrastructure costs. Llama Guard performs more nuanced multi-category safety classification (violence, hate speech, sexual content, dangerous information) on both prompts and completions. The Safety API applies shields in the agent workflow automatically: every message passes through configured shields before reaching the model and every completion passes through output shields before returning to the client. Violation handling is configurable — log-only for monitoring, block for enforcement, or custom callbacks for audit trails. Red Hat&rsquo;s production deployment patterns for Llama Stack demonstrate shield configurations that achieve enterprise safety compliance (EU AI Act content moderation requirements, NIST AI RMF implementation) without measurable latency impact from PromptGuard&rsquo;s CPU-based classification.</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-python" data-lang="python"><span style="display:flex;"><span><span style="color:#75715e"># Configure safety shields in the run configuration (YAML)</span>
</span></span><span style="display:flex;"><span><span style="color:#75715e"># shields:</span>
</span></span><span style="display:flex;"><span><span style="color:#75715e">#   - provider_id: meta-reference</span>
</span></span><span style="display:flex;"><span><span style="color:#75715e">#     shield_id: prompt-guard</span>
</span></span><span style="display:flex;"><span><span style="color:#75715e">#     config:</span>
</span></span><span style="display:flex;"><span><span style="color:#75715e">#       violation_level: ERROR  # ERROR blocks, WARN logs only</span>
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#75715e"># Apply shields in agent creation</span>
</span></span><span style="display:flex;"><span>agent <span style="color:#f92672">=</span> Agent(
</span></span><span style="display:flex;"><span>    client,
</span></span><span style="display:flex;"><span>    model<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;llama4:scout&#34;</span>,
</span></span><span style="display:flex;"><span>    instructions<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;You are a helpful assistant.&#34;</span>,
</span></span><span style="display:flex;"><span>    input_shields<span style="color:#f92672">=</span>[<span style="color:#e6db74">&#34;prompt-guard&#34;</span>],    <span style="color:#75715e"># screen incoming messages</span>
</span></span><span style="display:flex;"><span>    output_shields<span style="color:#f92672">=</span>[<span style="color:#e6db74">&#34;llama-guard&#34;</span>],    <span style="color:#75715e"># screen model outputs</span>
</span></span><span style="display:flex;"><span>)
</span></span></code></pre></div><h2 id="production-deployment-vllm-aws-bedrock-and-azure-openai-providers">Production Deployment: vLLM, AWS Bedrock, and Azure OpenAI Providers</h2>
<p>For production deployments, Ollama&rsquo;s throughput limitations become a bottleneck. vLLM is the recommended inference provider for on-premises GPU clusters: vLLM shows roughly 2.3x higher throughput than Ollama under 8 concurrent users. For cloud deployments, llama-stack supports AWS Bedrock and Azure OpenAI as inference providers, enabling teams to use managed Llama model endpoints without self-hosting GPU infrastructure.</p>
<p><strong>vLLM provider configuration:</strong></p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-yaml" data-lang="yaml"><span style="display:flex;"><span><span style="color:#75715e"># llama-stack-config.yaml</span>
</span></span><span style="display:flex;"><span><span style="color:#f92672">inference</span>:
</span></span><span style="display:flex;"><span>  - <span style="color:#f92672">provider_id</span>: <span style="color:#ae81ff">vllm</span>
</span></span><span style="display:flex;"><span>    <span style="color:#f92672">provider_type</span>: <span style="color:#ae81ff">remote::vllm</span>
</span></span><span style="display:flex;"><span>    <span style="color:#f92672">config</span>:
</span></span><span style="display:flex;"><span>      <span style="color:#f92672">url</span>: <span style="color:#ae81ff">http://vllm-server:8000</span>
</span></span><span style="display:flex;"><span>      <span style="color:#f92672">max_tokens</span>: <span style="color:#ae81ff">8192</span>
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#f92672">models</span>:
</span></span><span style="display:flex;"><span>  - <span style="color:#f92672">model_id</span>: <span style="color:#ae81ff">llama4-scout</span>
</span></span><span style="display:flex;"><span>    <span style="color:#f92672">provider_id</span>: <span style="color:#ae81ff">vllm</span>
</span></span><span style="display:flex;"><span>    <span style="color:#f92672">provider_model_id</span>: <span style="color:#ae81ff">meta-llama/Llama-4-Scout-17B-16E-Instruct</span>
</span></span></code></pre></div><p><strong>Start the server with custom config:</strong></p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-bash" data-lang="bash"><span style="display:flex;"><span>llama-stack run --config llama-stack-config.yaml
</span></span></code></pre></div><p><strong>AWS Bedrock provider:</strong></p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-yaml" data-lang="yaml"><span style="display:flex;"><span><span style="color:#f92672">inference</span>:
</span></span><span style="display:flex;"><span>  - <span style="color:#f92672">provider_id</span>: <span style="color:#ae81ff">bedrock</span>
</span></span><span style="display:flex;"><span>    <span style="color:#f92672">provider_type</span>: <span style="color:#ae81ff">remote::bedrock</span>
</span></span><span style="display:flex;"><span>    <span style="color:#f92672">config</span>:
</span></span><span style="display:flex;"><span>      <span style="color:#f92672">region</span>: <span style="color:#ae81ff">us-east-1</span>
</span></span><span style="display:flex;"><span>      <span style="color:#75715e"># Uses AWS credentials from environment or IAM role</span>
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#f92672">models</span>:
</span></span><span style="display:flex;"><span>  - <span style="color:#f92672">model_id</span>: <span style="color:#ae81ff">llama4-scout</span>
</span></span><span style="display:flex;"><span>    <span style="color:#f92672">provider_id</span>: <span style="color:#ae81ff">bedrock</span>
</span></span><span style="display:flex;"><span>    <span style="color:#f92672">provider_model_id</span>: <span style="color:#ae81ff">meta.llama4-scout-17b-v1:0</span>
</span></span></code></pre></div><p>For multi-provider production setups, llama-stack supports routing different models to different providers within a single server instance — Scout on AWS Bedrock for cost-sensitive queries and Maverick on a local vLLM cluster for high-complexity reasoning, all behind the same API endpoint.</p>
<h2 id="llama-stack-vs-alternatives-when-to-choose-what">Llama Stack vs Alternatives: When to Choose What</h2>
<p>llama-stack is not the right choice for every deployment scenario. The decision depends on whether you need provider portability, built-in safety, or the richest ecosystem of integrations and community tooling. LangChain remains the better choice for teams that need the broadest library of pre-built chains, integrations, and community examples — llama-stack&rsquo;s ecosystem is smaller, though growing rapidly. vLLM alone is the right choice if you need maximum raw inference throughput and are committed to a single model provider — llama-stack adds a network hop and abstraction layer that has a measurable (though small) latency cost at very high request rates. Ollama alone is the right choice for local development and individual developer workflows where the full llama-stack server is unnecessary overhead. llama-stack is the right choice when: you need to run the same application code across multiple inference backends (local dev, staging on-prem, production cloud); you require production-grade safety integration without building it yourself; you want OpenAI API compatibility without vendor lock-in to OpenAI; or you are building enterprise applications where the standardized API contract simplifies compliance and audit. The MCP support makes llama-stack particularly well-positioned for teams already invested in MCP tool servers, since those tools port directly into llama-stack agents without rebuilding.</p>
<hr>
<h2 id="faq">FAQ</h2>
<p>llama-stack is Meta&rsquo;s newest infrastructure layer for Llama model deployment, and teams evaluating it consistently encounter the same practical questions: how it differs from vLLM and Ollama, whether it imposes latency overhead, how to handle authentication in production, and which provider to start with for local development. Llama models have crossed 1.2 billion downloads and $2.5 billion in projected enterprise spending for 2026 — the deployment tooling ecosystem around them is maturing fast. llama-stack&rsquo;s standardized API contract is the key value proposition for teams scaling from prototype to production without rewriting application code each time the infrastructure changes. Whether you are evaluating llama-stack for a greenfield LLM product or considering it as a migration path from a direct vLLM or LangChain integration, the answers below address the most common decision blockers based on real production deployment patterns and the current llama-stack feature set as of May 2026.</p>
<h3 id="how-does-llama-stack-differ-from-just-using-vllm-or-ollama-directly">How does llama-stack differ from just using vLLM or Ollama directly?</h3>
<p>vLLM and Ollama are inference servers — they run models and expose APIs. llama-stack is a deployment framework that uses inference servers as providers. When you use vLLM directly, your application code is coupled to vLLM&rsquo;s API surface. When you use llama-stack with a vLLM provider, your application code targets llama-stack&rsquo;s standardized APIs, and vLLM is a swappable backend. The practical difference: switching from vLLM to AWS Bedrock in a direct integration requires application code changes; in a llama-stack integration, it requires only a configuration change. llama-stack also adds the Memory, Agents, and Safety APIs that vLLM and Ollama do not provide, meaning RAG pipelines and agentic workflows built on llama-stack do not require LangChain or similar orchestration libraries.</p>
<h3 id="does-llama-stack-add-significant-latency-compared-to-calling-vllm-directly">Does llama-stack add significant latency compared to calling vLLM directly?</h3>
<p>The latency overhead is small and typically unmeasurable in production scenarios. llama-stack&rsquo;s server adds a local HTTP hop between your application and the inference provider — on the same machine or same network segment, this adds single-digit milliseconds. For most production use cases where Llama 4 Scout inference itself takes 200–2000ms depending on prompt length, a 5ms framework overhead is negligible. The only scenario where it matters is extremely high-frequency, latency-sensitive inference where every millisecond counts — in that case, calling vLLM directly with a hardcoded provider is more appropriate than adding any abstraction layer.</p>
<h3 id="which-provider-should-you-start-with-for-local-development">Which provider should you start with for local development?</h3>
<p>Ollama is the recommended starting point for local development. It handles model download, quantization, and GPU/CPU management automatically — you run <code>ollama pull llama4:scout</code> and it works. The <code>llama-stack run ollama</code> command starts the full server in seconds. For teams without a dedicated GPU, Ollama&rsquo;s quantized model support enables Llama 4 Scout to run on a machine with 16GB RAM (using Q4 quantization), making local development accessible without specialized hardware. Switch to vLLM for staging and production environments where throughput matters — vLLM&rsquo;s 2.3x throughput advantage over Ollama under concurrent load justifies the additional setup complexity.</p>
<h3 id="how-does-authentication-work-in-production-llama-stack-deployments">How does authentication work in production llama-stack deployments?</h3>
<p>Local llama-stack servers run without authentication by default — the API key field accepts any value. For production deployments, authentication is handled at the infrastructure layer: put an API gateway or reverse proxy (Nginx, Kong, AWS API Gateway) in front of the llama-stack server, and enforce authentication there. llama-stack itself does not implement API key management or OAuth — it relies on the surrounding infrastructure for access control. For team deployments where multiple users or services access the same llama-stack server, the recommended pattern is a network-level API gateway that issues tokens per client and proxies requests to llama-stack, preserving the simplicity of llama-stack&rsquo;s internal API while enforcing access control at the perimeter.</p>
<h3 id="can-llama-stack-run-llama-4-maverick-on-consumer-hardware">Can llama-stack run Llama 4 Maverick on consumer hardware?</h3>
<p>Llama 4 Maverick with 128 experts requires multi-GPU infrastructure — it does not fit in a single consumer GPU at full precision. On consumer hardware, the practical option is heavily quantized versions (Q2 or Q3) of Maverick using Ollama, which reduce memory requirements substantially at the cost of output quality. For the best balance of capability and consumer hardware accessibility, Llama 4 Scout (17B active parameters, 16 experts) with Q4 quantization runs effectively on an NVIDIA RTX 4090 or a machine with 32GB RAM via CPU inference. llama-stack supports both configurations — configure the provider for Ollama with the quantized model identifier, and the application code is identical regardless of which quantization level runs underneath.</p>
]]></content:encoded></item></channel></rss>