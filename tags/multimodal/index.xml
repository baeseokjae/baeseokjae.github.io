<?xml version="1.0" encoding="utf-8" standalone="yes"?><rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom" xmlns:content="http://purl.org/rss/1.0/modules/content/"><channel><title>Multimodal on RockB</title><link>https://baeseokjae.github.io/tags/multimodal/</link><description>Recent content in Multimodal on RockB</description><image><title>RockB</title><url>https://baeseokjae.github.io/images/og-default.png</url><link>https://baeseokjae.github.io/images/og-default.png</link></image><generator>Hugo</generator><language>en-us</language><lastBuildDate>Thu, 10 Sep 2026 06:34:16 +0000</lastBuildDate><atom:link href="https://baeseokjae.github.io/tags/multimodal/index.xml" rel="self" type="application/rss+xml"/><item><title>dsh-vision-router Visual Memory: Eyes for Text-Only DeepSeek Harness Agents</title><link>https://baeseokjae.github.io/posts/dsh-vision-router-visual-memory/</link><pubDate>Thu, 10 Sep 2026 06:34:16 +0000</pubDate><guid>https://baeseokjae.github.io/posts/dsh-vision-router-visual-memory/</guid><description>dsh-vision-router gives text-only DeepSeek Harness agents eyes: pixel-faithful vision routing, 14 vision tools, cached visual memory, and a free keyless fallback.</description><content:encoded><![CDATA[<p>dsh-vision-router is a DeepSeek Harness (DSH) plugin that gives text-only agents eyes by routing image turns to a separate vision model while DeepSeek stays the reasoning brain. It installs with one command, needs no Python and no API key, and ships 14 deep vision tools plus cached visual memory so a text-only agent genuinely remembers earlier images without re-spending vision calls.</p>
<h2 id="what-is-dsh-vision-router-and-why-text-only-deepseek-agents-need-eyes">What is dsh-vision-router and why text-only DeepSeek agents need eyes</h2>
<p>DeepSeek Harness is a local agent framework built around DeepSeek models. Many of its most popular models, including deepseek-v4-flash, are text-only: they declare <code>inputModalities: [&quot;text&quot;]</code> and simply cannot accept image attachments. That is a real limitation for agent work, because a growing share of tasks — reading a screenshot, checking a rendered UI, verifying a chart, OCRing a receipt — are fundamentally visual.</p>
<p>dsh-vision-router solves this by keeping the architecture clean: DeepSeek remains the brain, and a separate vision model becomes the eyes. When an image arrives, the plugin hands that turn to a vision model, gets a description or a structured result, and feeds it back to DeepSeek as ordinary text. Text turns are untouched in model, cost, and context. The plugin is free by default, runs with no Python, and installs in a single command.</p>
<p>The project has drawn real attention: it carries roughly 1,093 GitHub stars on the DSH Hub catalog (updated 2026-09-09) and about 1,021 stars with 44 forks on the ysr666 repository. That level of adoption signals that &ldquo;text-only brain plus separate vision model&rdquo; is not a niche hack — it is becoming the standard architecture for local multimodal agent work.</p>
<h2 id="the-problem-dsh-rejects-images-for-text-only-models-model_does_not_support_images">The problem: DSH rejects images for text-only models (MODEL_DOES_NOT_SUPPORT_IMAGES)</h2>
<p>The root problem is documented in the DeepSeek Harness discussions. When the active model is text-only, DSH rejects image attachments at the GUI/host admission layer with the error <code>MODEL_DOES_NOT_SUPPORT_IMAGES</code>. The model&rsquo;s <code>inputModalities</code> array simply does not include <code>&quot;image&quot;</code>, so the harness refuses the attachment before the agent ever sees it.</p>
<p>A common private-deployment setup tries to work around this by pairing a text-only main model with a separate multimodal &ldquo;vision helper&rdquo; — for example Qwen3.6-27B served through llama.cpp as an MCP server exposing a <code>describe_image(path)</code> function. That MCP workaround works when you can hand it a file path, but the GUI attachment path never yields a reference the agent can actually see. The image is dropped at admission, and the agent is left blind.</p>
<p>This is exactly the gap dsh-vision-router fills. Instead of fighting the admission layer, it patches the harness composition so image turns are routed to a vision-capable model before DeepSeek has to process them. The result is that pasting an image into a text-only DeepSeek session just works, like an ordinary tool-calling turn.</p>
<h2 id="routing-bridge-vs-description-bridge-how-dsh-vision-router-keeps-pixels-faithful">Routing bridge vs description bridge: how dsh-vision-router keeps pixels faithful</h2>
<p>Most DSH vision plugins take a &ldquo;description bridge&rdquo; approach: they convert an image into a text description up front, then feed that text to the model. This is lossy, one-shot, and blind to pixels. If the description misses a detail, the model never gets a second chance to look.</p>
<p>dsh-vision-router is a &ldquo;routing bridge&rdquo; instead. It hands the image turn straight to a vision model, pixel-faithful, and lets that model do the actual seeing. The vision model can crop, ground, compare pixels, run OCR, trace contours, and take screenshots — operations that a text description can never capture. DeepSeek then reasons over the structured result.</p>
<p>The plugin is capability-aware: it auto-routes based on measured evidence, never inferred from model names. It classifies errors (region, ToS, quota, rate-limit, context, network) and applies Retry-After-aware cooldowns, so a failing vision backend does not silently break the turn.</p>
<h2 id="one-command-install-and-the--vision-composer-control">One-command install and the &ldquo;👁 Vision&rdquo; composer control</h2>
<p>Installation is a single command:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-bash" data-lang="bash"><span style="display:flex;"><span>npx @deepseek-ai/dsh plugin --profile web add dsh-vision-router
</span></span></code></pre></div><p>Then restart <code>dsh web</code>. The plugin ships its own composition patch (<code>dsh.bundle.patch</code>), so there are no manual file edits to make. After restart, a &ldquo;👁 Vision&rdquo; composer control appears in the web UI, and pasting an image into the chat works like any ordinary tool-calling turn.</p>
<p>The plugin is verified against DSH 0.1.5-alpha.1 (v2.1.5), with stable Host support through 0.1.2-rc.1. It requires Node.js 22 or newer.</p>
<h2 id="the-14-deep-vision-tools-and-what-each-one-does">The 14 deep vision tools and what each one does</h2>
<p>The plugin exposes 14 deep vision tools (15 with the privacy-gated <code>vision_screenshot</code> opt-in). Here is what each one does:</p>
<table>
  <thead>
      <tr>
          <th>Tool</th>
          <th>Purpose</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td><code>vision_describe</code></td>
          <td>Free-form visual Q&amp;A about an image</td>
      </tr>
      <tr>
          <td><code>vision_ground</code></td>
          <td>Ground a phrase to a bounding region in the image</td>
      </tr>
      <tr>
          <td><code>vision_detect</code></td>
          <td>Detect objects or regions of interest</td>
      </tr>
      <tr>
          <td><code>vision_crop</code></td>
          <td>Crop a region for closer inspection</td>
      </tr>
      <tr>
          <td><code>vision_present</code></td>
          <td>Present an image or region back to the model</td>
      </tr>
      <tr>
          <td><code>vision_pixel_diff</code></td>
          <td>Compare two images pixel-by-pixel and report the diff</td>
      </tr>
      <tr>
          <td><code>vision_colors</code></td>
          <td>Extract the dominant color palette</td>
      </tr>
      <tr>
          <td><code>vision_ocr</code></td>
          <td>Extract text from the image</td>
      </tr>
      <tr>
          <td><code>vision_trace</code></td>
          <td>Trace contours into SVG paths</td>
      </tr>
      <tr>
          <td><code>vision_extract_foreground</code></td>
          <td>Cut out the foreground subject</td>
      </tr>
      <tr>
          <td><code>vision_html_screenshot</code></td>
          <td>Render HTML to a screenshot</td>
      </tr>
      <tr>
          <td><code>vision_screenshot</code></td>
          <td>Take a screenshot (opt-in, privacy-gated)</td>
      </tr>
      <tr>
          <td><code>vision_long_screenshot_ocr</code></td>
          <td>OCR a long/scrolling screenshot</td>
      </tr>
      <tr>
          <td><code>vision_bootstrap</code></td>
          <td>Initialize the vision pipeline</td>
      </tr>
  </tbody>
</table>
<p>The pipeline runs on <code>sharp</code> (downscale, crop, pixel diff), <code>potrace</code> (SVG trace), <code>tesseract</code> (OCR), and system Chrome (HTML screenshots) — no Python required.</p>
<h2 id="visual-memory-how-image-answers-are-cached-and-reused">Visual memory: how image answers are cached and reused</h2>
<p>The &ldquo;visual memory&rdquo; in the keyword is the plugin&rsquo;s caching layer. Vision answers are cached by attachment content hash, so when the same image appears again in a later text turn, the plugin substitutes the recorded description — marked as untrusted evidence — without re-spending a vision call.</p>
<p>By default the cache keeps answers for <code>cacheTtlSeconds</code> of 3600 seconds (one hour) and up to <code>cacheMaxEntries</code> of 200 entries. This matters for two reasons. First, it saves cost and latency: the same screenshot referenced three times costs one vision call, not three. Second, it gives the text-only agent genuine continuity: it can &ldquo;remember&rdquo; an image from earlier in the conversation even though DeepSeek itself never saw the pixels.</p>
<h2 id="the-free-vision-chain-ovhcloud-fallback-and-free-key-channels">The free vision chain: OVHcloud fallback and free key channels</h2>
<p>The plugin is free out of the box. It ships a built-in keyless OVHcloud anonymous fallback chain with 5 model buckets. The anonymous tier is capped at about 2 requests per minute per IP per model, which across the independent buckets works out to roughly 10 RPM in theory.</p>
<p>User-provided vision models always run first. If you add a free OVHcloud AI Endpoints access key, the same Qwen2.5-VL-72B-Instruct endpoint jumps to 400 requests per minute per project per model — a 200x improvement over the anonymous cap.</p>
<p>Free-tier vision policies rotate, so the plugin&rsquo;s &ldquo;free vision key channels&rdquo; note tracks the current landscape: Cerebras retired its free tier in July 2026, SambaNova dropped to 20 requests per day, and Hugging Face is now $0.10 per month. The keyless fallback means you are never blocked when a free key disappears.</p>
<h2 id="local-first-privacy-ollama-and-lm-studio-backends">Local-first privacy: Ollama and LM Studio backends</h2>
<p>For private or offline work, the plugin supports optional Ollama and LM Studio keyless vision backends. This is the local-first privacy path: recognition happens entirely on your machine, with no external endpoint and no data leaving the host. It is the right choice when the images are sensitive, when you are offline, or when you simply want zero external dependencies.</p>
<h2 id="the-verifiable-pixel-loop-for-ui-restoration">The verifiable pixel loop for UI restoration</h2>
<p>One of the plugin&rsquo;s most distinctive workflows is the verifiable pixel loop. Instead of eyeballing whether a UI rebuild matches a reference, you measure it:</p>
<ol>
<li>Feed the reference image to the agent.</li>
<li>Use <code>vision_html_screenshot</code> to render the current implementation.</li>
<li>Use <code>vision_pixel_diff</code> to compare the two pixel-by-pixel.</li>
<li>Fix the mismatch, re-render, and repeat until the diff converges.</li>
</ol>
<p>The project&rsquo;s demo verified a UI rebuild at a 2.54% final diff — 32,939 differing pixels out of 1,296,000, at a threshold of 16 per channel. That turns UI restoration from a subjective &ldquo;looks close&rdquo; into a measurable, converging number.</p>
<h2 id="configuration-essentials-routingmode-stealth-autowrapproviders-cache">Configuration essentials (routingMode, stealth, autoWrapProviders, cache)</h2>
<p>The plugin&rsquo;s key configuration options:</p>
<ul>
<li><strong>routingMode</strong> — how image turns are routed to vision backends (capability-aware auto-routing by default).</li>
<li><strong>stealth</strong> — optionally take over the official <code>deepseek-official</code> route so the model picker looks exactly like stock while each entry is an auto-vision wrapper.</li>
<li><strong>autoWrapProviders</strong> — automatically wrap provider entries so every model in the picker gets vision capability.</li>
<li><strong>cacheTtlSeconds / cacheMaxEntries</strong> — control the visual-memory cache lifetime and size (defaults 3600s and 200 entries).</li>
</ul>
<h2 id="troubleshooting-common-issues-bom-sharp-conflict-dsh-web-ui-rewrite">Troubleshooting common issues (BOM, sharp conflict, dsh-web-ui rewrite)</h2>
<p>Three issues come up most often. First, a byte-order mark (BOM) in a config or patch file can break parsing — strip it if the plugin fails to load. Second, <code>sharp</code> can conflict with an existing native dependency in the project; resolve the version mismatch before the pipeline runs. Third, if you have customized <code>dsh-web-ui</code>, the plugin&rsquo;s composition patch may need to be re-applied after a rewrite, because the patch targets the stock UI structure.</p>
<h2 id="alternatives-compared">Alternatives compared</h2>
<p>dsh-vision-router is not the only option. Here is how it stacks up against the main alternatives:</p>
<table>
  <thead>
      <tr>
          <th>Approach</th>
          <th>How it works</th>
          <th>Best for</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>dsh-vision-router</td>
          <td>Routing bridge, pixel-faithful, 14 tools, free fallback</td>
          <td>Text-only DeepSeek agents that need real vision</td>
      </tr>
      <tr>
          <td>dsh-vision-sidecar / proxy</td>
          <td>External vision service</td>
          <td>Teams that already run a vision service</td>
      </tr>
      <tr>
          <td>dsh-vision-toolkit</td>
          <td>Tool collection, no routing</td>
          <td>Simple, one-off vision calls</td>
      </tr>
      <tr>
          <td>Pilco-mmbridge</td>
          <td>Turns a text-only model multimodal without fine-tuning</td>
          <td>Running multimodal benchmarks on text-only LLMs</td>
      </tr>
      <tr>
          <td>MCP <code>describe_image</code></td>
          <td>File-path-based description helper</td>
          <td>When you can pass file paths, not GUI attachments</td>
      </tr>
  </tbody>
</table>
<p>The plugin is a poor fit where the harness already provides the required vision route, or where external anonymous endpoints are unsuitable — the fallback is rate-limited to 2 req/min per IP per model. But for a text-only DeepSeek agent that needs real, pixel-faithful eyes with free fallback and cached visual memory, dsh-vision-router is the most complete option.</p>
<h2 id="faq">FAQ</h2>
<p><strong>Does dsh-vision-router require an API key?</strong>
No. It works free out of the box with a built-in keyless OVHcloud anonymous fallback chain. You can optionally add your own vision model or a free OVHcloud key to lift the rate limit from ~2 to 400 requests per minute.</p>
<p><strong>Does it need Python?</strong>
No. The entire pipeline runs on sharp, potrace, tesseract, and system Chrome. It requires Node.js 22 or newer.</p>
<p><strong>How do I install it?</strong>
Run <code>npx @deepseek-ai/dsh plugin --profile web add dsh-vision-router</code>, restart <code>dsh web</code>, and a &ldquo;👁 Vision&rdquo; composer control appears. No manual file edits are needed.</p>
<p><strong>What is visual memory in dsh-vision-router?</strong>
Vision answers are cached by attachment content hash (default 1-hour TTL, 200 entries). When the same image reappears, the plugin substitutes the recorded description without re-spending a vision call, so the text-only agent remembers earlier images.</p>
<p><strong>Which DeepSeek models does it support?</strong>
It is designed for text-only models like deepseek-v4-flash that reject image attachments with <code>MODEL_DOES_NOT_SUPPORT_IMAGES</code>. It is verified against DSH 0.1.5-alpha.1 with stable Host support through 0.1.2-rc.1.</p>
]]></content:encoded></item><item><title>GPT-5 Turbo Review 2026: Native Image+Audio, Better JSON, April 7 Release</title><link>https://baeseokjae.github.io/posts/gpt-5-turbo-review-2026/</link><pubDate>Fri, 15 May 2026 09:07:05 +0000</pubDate><guid>https://baeseokjae.github.io/posts/gpt-5-turbo-review-2026/</guid><description>GPT-5 Turbo (GPT-5.4 mini) delivers native image+audio, strict JSON outputs, and 2x faster inference at $0.15/M tokens — here&amp;#39;s the developer verdict fo...</description><content:encoded><![CDATA[<p>GPT-5 Turbo — OpenAI&rsquo;s fast, efficient variant marketed as GPT-5 mini and later GPT-5.4 mini — delivers native multimodal input (images and audio in a single API call), strict JSON structured outputs, and 400K-token context at roughly $0.15 per million input tokens. It is the practical choice for production applications where cost and latency matter more than raw intelligence ceiling.</p>
<h2 id="what-is-gpt-5-turbo-openais-fast-multimodal-model-explained">What Is GPT-5 Turbo? OpenAI&rsquo;s Fast, Multimodal Model Explained</h2>
<p>GPT-5 Turbo refers to the fast, cost-optimized tier of OpenAI&rsquo;s GPT-5 family — officially shipped as <strong>GPT-5 mini</strong> (August 7, 2025) and its successor <strong>GPT-5.4 mini</strong> (March 17, 2026). Just as GPT-4 Turbo was the speed-and-price-optimized version of GPT-4, GPT-5 Turbo is the developer-friendly workhorse of the fifth generation. GPT-5.4 mini runs more than 2x faster than the original GPT-5 mini while approaching flagship GPT-5.4 performance on reasoning and coding benchmarks. The model supports text, images, and audio natively — no add-on vision API, no separate speech-to-text pipeline. Context window reaches 400K tokens, more than 3x the 128K cap on GPT-4o mini. Pricing sits at approximately $0.15 per million input tokens and $0.60 per million output tokens. For developers building RAG pipelines, voice assistants, or document-parsing agents, GPT-5.4 mini hits the sweet spot between the budget Gemini Flash tier and the premium GPT-5.5 flagship. The result is a model that most real-world production apps can actually afford to run at scale.</p>
<h3 id="how-does-gpt-5-turbo-differ-from-gpt-5-flagship">How Does GPT-5 Turbo Differ from GPT-5 Flagship?</h3>
<p>GPT-5 Turbo (GPT-5.4 mini) trades some reasoning depth for dramatically lower cost and latency. The flagship GPT-5 family achieves 94.6% on AIME 2025 math and 74.9% on SWE-bench Verified; the mini tier falls below those peaks but still outperforms GPT-4o on most standard tasks. For workloads that don&rsquo;t require multi-step theorem proving or advanced agent planning — customer support, document classification, voice Q&amp;A — GPT-5 Turbo is the rational pick.</p>
<h2 id="key-features-native-image--audio-input-without-add-ons">Key Features: Native Image &amp; Audio Input Without Add-Ons</h2>
<p>GPT-5 Turbo&rsquo;s unified multimodal architecture means a single API endpoint accepts text, base64-encoded images, and audio chunks simultaneously — eliminating the multi-service orchestration that GPT-4o vision required. Before this release, building a voice-enabled receipt scanner meant stitching together Whisper for transcription, GPT-4o vision for OCR, and a text model for response generation, with three separate API calls, three latency hops, and three billing meters. GPT-5 Turbo collapses that into one call. In practice, developers report 40–60% reduction in end-to-end response time for hybrid voice+vision tasks. The audio support handles multi-speaker transcription and speaker diarization natively, making it viable for meeting-note agents. Images are processed at up to 2048×2048 resolution with automatic tiling for high-detail documents. This architectural unification also simplifies error handling: there is one retry boundary, one rate limit to monitor, and one model version to pin in production deployments.</p>
<h3 id="what-audio-formats-does-gpt-5-turbo-support">What Audio Formats Does GPT-5 Turbo Support?</h3>
<p>GPT-5 Turbo accepts MP3, WAV, FLAC, and M4A audio inputs directly in the chat completions endpoint. Audio is billed at a per-second rate (approximately $0.006/second), and the model returns transcription + reasoning in the same response object, enabling sub-second round trips for short voice queries. Speaker identification metadata is returned as structured JSON when requested.</p>
<h3 id="what-image-inputs-work-with-gpt-5-turbo">What Image Inputs Work with GPT-5 Turbo?</h3>
<p>Images can be passed as URLs or base64 blobs via the <code>image_url</code> content block. The model handles JPEG, PNG, GIF (first frame), and WebP. For multi-page documents, each page is sent as a separate image in the same messages array — the 400K token context window comfortably handles 20–30 pages per request. OCR accuracy on printed text exceeds 98% in internal benchmarks; handwriting recognition hovers around 91%.</p>
<h2 id="better-json--structured-outputs--zero-schema-violations-in-production">Better JSON &amp; Structured Outputs — Zero Schema Violations in Production</h2>
<p>GPT-5&rsquo;s structured outputs enforce strict JSON schema compliance at the token generation level — meaning the model is physically unable to produce a response that violates the declared schema. This is a fundamental architectural departure from GPT-4o&rsquo;s JSON mode, which requested JSON-formatted responses but offered no hard guarantee. In production systems, GPT-4o&rsquo;s JSON mode failed silently roughly 2–5% of the time: truncated objects, missing required fields, or escaped characters that broke downstream parsers. GPT-5 Turbo&rsquo;s Strict Mode eliminates this failure class entirely by running a context-free grammar (CFG) engine that constrains the token sampling distribution to only valid schema continuations. Developers define the schema using Pydantic (Python), Zod (TypeScript), or raw JSON Schema objects, and the API returns a <code>parsed</code> field alongside the raw <code>content</code> — pre-validated and deserialized. For any pipeline where downstream code consumes model output as structured data — tool calls, database inserts, webhook payloads — enabling <code>strict: true</code> in the response format is a one-line change that eliminates an entire class of runtime exceptions. OpenAI documented zero schema violations across 10,000 test calls in their Strict Mode evaluation suite.</p>
<h3 id="how-to-enable-strict-json-mode-in-gpt-5-turbo">How to Enable Strict JSON Mode in GPT-5 Turbo</h3>
<p>Set <code>response_format</code> to <code>{ &quot;type&quot;: &quot;json_schema&quot;, &quot;json_schema&quot;: { &quot;strict&quot;: true, &quot;schema&quot;: &lt;your_schema&gt; } }</code> in your API call. When using the Python SDK, pass a Pydantic model class directly to <code>response_format</code> and the SDK handles schema serialization automatically. The <code>strict: true</code> flag tells the CFG engine to enforce the schema — without it you get the legacy best-effort JSON mode.</p>
<h3 id="json-mode-legacy-vs-strict-mode-which-should-you-use">JSON Mode Legacy vs. Strict Mode: Which Should You Use?</h3>
<p>Use <strong>Strict Mode</strong> for all new production code. Legacy JSON Mode exists only for backward compatibility with prompts that rely on natural language schema descriptions rather than formal JSON Schema objects. Any application writing model output to a database, calling a downstream API, or triggering a workflow should use Strict Mode — the operational risk of silent schema violations in legacy mode is not worth the marginal prompt flexibility.</p>
<h2 id="performance-benchmarks-gpt-5-turbo-vs-gpt-4o-claude-and-gemini">Performance Benchmarks: GPT-5 Turbo vs. GPT-4o, Claude, and Gemini</h2>
<p>GPT-5 Turbo (GPT-5.4 mini) significantly outperforms GPT-4o mini on every standard benchmark while keeping costs in the same range, making the upgrade decision straightforward for most teams. On the flagship end, GPT-5.5 (April 23, 2026) achieves 88.7% on SWE-Bench Verified and 92.4% on MMLU; GPT-5 Turbo mini trails those numbers but more than doubles GPT-4o mini&rsquo;s SWE-bench score. The flagship GPT-5 itself hit 74.9% on SWE-bench Verified and 94.6% on AIME 2025 math — a 26-point improvement over GPT-4o&rsquo;s 71%. For computer use (autonomous desktop/browser control), GPT-5.4 achieves 75% on the OSWorld benchmark, surpassing the human baseline of 72.4%. These numbers represent the broader GPT-5 family; the mini tier scores 10–20 percentage points lower on complex reasoning tasks but is on par with GPT-4o flagship for most NLP, classification, and extraction workloads. Hallucination rate improvements are meaningful: GPT-5.5 reduced hallucinations by 60% vs. GPT-5.4, and the mini tier inherits much of this improvement from the same pre-training and RLHF recipe. MRCR v2 (long-context recall) at 1M tokens improved from 36.6% to 74.0% in the 5.5 generation.</p>
<table>
  <thead>
      <tr>
          <th>Model</th>
          <th>SWE-bench Verified</th>
          <th>MMLU</th>
          <th>Context</th>
          <th>Input $/M</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>GPT-4o mini</td>
          <td>~30%</td>
          <td>~82%</td>
          <td>128K</td>
          <td>$0.15</td>
      </tr>
      <tr>
          <td>GPT-5 Turbo (5.4 mini)</td>
          <td>~55%</td>
          <td>~88%</td>
          <td>400K</td>
          <td>$0.15</td>
      </tr>
      <tr>
          <td>GPT-5.4 flagship</td>
          <td>80%</td>
          <td>~90%</td>
          <td>400K</td>
          <td>$2.50</td>
      </tr>
      <tr>
          <td>GPT-5.5</td>
          <td>88.7%</td>
          <td>92.4%</td>
          <td>1M</td>
          <td>$5.00</td>
      </tr>
      <tr>
          <td>Claude Haiku 4.5</td>
          <td>~50%</td>
          <td>~86%</td>
          <td>200K</td>
          <td>$0.25</td>
      </tr>
      <tr>
          <td>Gemini 3.1 Flash</td>
          <td>~48%</td>
          <td>~85%</td>
          <td>1M</td>
          <td>$0.075</td>
      </tr>
  </tbody>
</table>
<h2 id="pricing--cost-analysis--when-gpt-5-turbo-beats-the-competition">Pricing &amp; Cost Analysis — When GPT-5 Turbo Beats the Competition</h2>
<p>GPT-5 Turbo (GPT-5.4 mini) costs approximately $0.15 per million input tokens and $0.60 per million output tokens — the same price bracket as GPT-4o mini, but with dramatically better capability. This pricing positions it as the dominant choice in the &ldquo;budget but capable&rdquo; tier for OpenAI workloads. However, context matters: Gemini 3.1 Flash costs roughly $0.075 per million input tokens (50% cheaper), and for tasks that don&rsquo;t require strict JSON enforcement or OpenAI-specific features, it delivers competitive results. Gemini 3.1 Pro at $2/$12 vs. GPT-5 flagship at $5/$30 delivers approximately 60% cost savings on standard reasoning tasks, according to LM Council benchmarks. For token-intensive pipelines, the GPT-5.5 model&rsquo;s 40% reduction in output tokens on Codex tasks partially offsets its 2x higher price per million tokens — teams running code-generation agents at volume may find GPT-5.5 cheaper per completed task despite the higher rate. The decision framework: for interactive user-facing apps where latency dominates, GPT-5 Turbo mini is the default; for batch offline workloads where cost dominates, benchmark Gemini Flash against your specific task; for maximum capability in agentic pipelines, GPT-5.5 is worth the premium.</p>
<table>
  <thead>
      <tr>
          <th>Model</th>
          <th>Input $/M</th>
          <th>Output $/M</th>
          <th>Best For</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>GPT-5 Turbo (5.4 mini)</td>
          <td>$0.15</td>
          <td>$0.60</td>
          <td>Interactive apps, RAG, default choice</td>
      </tr>
      <tr>
          <td>Gemini 3.1 Flash</td>
          <td>$0.075</td>
          <td>$0.30</td>
          <td>Batch jobs, cost-first workloads</td>
      </tr>
      <tr>
          <td>Claude Haiku 4.5</td>
          <td>$0.25</td>
          <td>$1.25</td>
          <td>Anthropic ecosystem, tool use</td>
      </tr>
      <tr>
          <td>GPT-5.4 flagship</td>
          <td>$2.50</td>
          <td>$15.00</td>
          <td>Complex agents, OSWorld tasks</td>
      </tr>
      <tr>
          <td>GPT-5.5</td>
          <td>$5.00</td>
          <td>$30.00</td>
          <td>Max capability, long-context recall</td>
      </tr>
  </tbody>
</table>
<h2 id="real-developer-use-cases-rag-agents-vision-apis-voice-apps">Real Developer Use Cases: RAG, Agents, Vision APIs, Voice Apps</h2>
<p>GPT-5 Turbo&rsquo;s combination of 400K context, native multimodal input, and strict JSON outputs directly addresses four high-value production patterns that were previously difficult or expensive to implement with GPT-4o. For <strong>RAG (Retrieval-Augmented Generation)</strong>, the 400K context window allows passing 200–300 retrieved chunks in a single call without truncation, eliminating the multi-hop retrieval cascades that GPT-4o&rsquo;s 128K limit forced. For <strong>agentic tool use</strong>, strict JSON means tool-call responses are guaranteed schema-valid — no more try/except wrappers around model outputs, no retry loops for malformed JSON. For <strong>vision document parsing</strong>, sending a 20-page PDF as 20 image frames in one request is feasible at the 400K limit; the model returns structured extraction results as a Pydantic-validated object in a single round trip. For <strong>voice applications</strong>, native audio input eliminates the Whisper pre-processing step: the user&rsquo;s speech arrives, gets transcribed and interpreted in one model call, and structured data (intent, entities, confidence) comes back in the same response. Teams that have migrated from GPT-4o to GPT-5 Turbo for these patterns report 35–50% reduction in infrastructure complexity and 20–40% reduction in median response latency.</p>
<h3 id="building-a-voice--vision-agent-with-gpt-5-turbo">Building a Voice + Vision Agent with GPT-5 Turbo</h3>
<p>A minimal voice-image agent needs three components: a microphone capture library (e.g., PyAudio), a base64 encoder for audio chunks and image frames, and a single <code>client.chat.completions.create()</code> call with <code>response_format</code> set to your Pydantic schema. The model handles transcription, visual grounding, and structured extraction in one hop. Latency for a 5-second voice clip + single image averages 800–1200ms in AWS us-east-1 — suitable for real-time assistant UX.</p>
<h2 id="gpt-5-turbo-api-quick-start-imageaudiojson-in-one-call">GPT-5 Turbo API Quick Start: Image+Audio+JSON in One Call</h2>
<p>The GPT-5 Turbo API follows the same OpenAI chat completions interface as GPT-4o but extends the content array to accept audio blobs alongside image URLs. The key change from GPT-4o is the <code>response_format</code> field, which now supports <code>json_schema</code> with <code>strict: true</code> for guaranteed schema compliance. Below is a complete Python example that sends an image and audio clip simultaneously and returns a typed Pydantic object — replacing what previously required three separate API calls (Whisper + Vision + Chat) with one. The model identifier to use is <code>gpt-5-4-mini-2026-03-17</code> (or the alias <code>gpt-5-mini</code> for the original August 2025 release). Pin the full dated version string in production — model aliases can update silently. Error handling is straightforward: <code>strict: true</code> eliminates JSON parsing errors; audio format mismatches raise a 400 with a clear error code; the main runtime error class to handle is rate limiting (<code>RateLimitError</code>), which should trigger exponential backoff starting at 1 second.</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-python" data-lang="python"><span style="display:flex;"><span><span style="color:#f92672">from</span> openai <span style="color:#f92672">import</span> OpenAI
</span></span><span style="display:flex;"><span><span style="color:#f92672">from</span> pydantic <span style="color:#f92672">import</span> BaseModel
</span></span><span style="display:flex;"><span><span style="color:#f92672">import</span> base64
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>client <span style="color:#f92672">=</span> OpenAI()
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">class</span> <span style="color:#a6e22e">ExtractedData</span>(BaseModel):
</span></span><span style="display:flex;"><span>    transcript: str
</span></span><span style="display:flex;"><span>    intent: str
</span></span><span style="display:flex;"><span>    entities: list[str]
</span></span><span style="display:flex;"><span>    confidence: float
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">with</span> open(<span style="color:#e6db74">&#34;user_audio.mp3&#34;</span>, <span style="color:#e6db74">&#34;rb&#34;</span>) <span style="color:#66d9ef">as</span> f:
</span></span><span style="display:flex;"><span>    audio_b64 <span style="color:#f92672">=</span> base64<span style="color:#f92672">.</span>b64encode(f<span style="color:#f92672">.</span>read())<span style="color:#f92672">.</span>decode()
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>response <span style="color:#f92672">=</span> client<span style="color:#f92672">.</span>beta<span style="color:#f92672">.</span>chat<span style="color:#f92672">.</span>completions<span style="color:#f92672">.</span>parse(
</span></span><span style="display:flex;"><span>    model<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;gpt-5-4-mini-2026-03-17&#34;</span>,
</span></span><span style="display:flex;"><span>    messages<span style="color:#f92672">=</span>[{
</span></span><span style="display:flex;"><span>        <span style="color:#e6db74">&#34;role&#34;</span>: <span style="color:#e6db74">&#34;user&#34;</span>,
</span></span><span style="display:flex;"><span>        <span style="color:#e6db74">&#34;content&#34;</span>: [
</span></span><span style="display:flex;"><span>            {<span style="color:#e6db74">&#34;type&#34;</span>: <span style="color:#e6db74">&#34;text&#34;</span>, <span style="color:#e6db74">&#34;text&#34;</span>: <span style="color:#e6db74">&#34;Extract intent and entities from this audio and image.&#34;</span>},
</span></span><span style="display:flex;"><span>            {<span style="color:#e6db74">&#34;type&#34;</span>: <span style="color:#e6db74">&#34;image_url&#34;</span>, <span style="color:#e6db74">&#34;image_url&#34;</span>: {<span style="color:#e6db74">&#34;url&#34;</span>: <span style="color:#e6db74">&#34;https://example.com/receipt.jpg&#34;</span>}},
</span></span><span style="display:flex;"><span>            {<span style="color:#e6db74">&#34;type&#34;</span>: <span style="color:#e6db74">&#34;input_audio&#34;</span>, <span style="color:#e6db74">&#34;input_audio&#34;</span>: {<span style="color:#e6db74">&#34;data&#34;</span>: audio_b64, <span style="color:#e6db74">&#34;format&#34;</span>: <span style="color:#e6db74">&#34;mp3&#34;</span>}}
</span></span><span style="display:flex;"><span>        ]
</span></span><span style="display:flex;"><span>    }],
</span></span><span style="display:flex;"><span>    response_format<span style="color:#f92672">=</span>ExtractedData,
</span></span><span style="display:flex;"><span>)
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>result: ExtractedData <span style="color:#f92672">=</span> response<span style="color:#f92672">.</span>choices[<span style="color:#ae81ff">0</span>]<span style="color:#f92672">.</span>message<span style="color:#f92672">.</span>parsed
</span></span><span style="display:flex;"><span>print(result<span style="color:#f92672">.</span>intent, result<span style="color:#f92672">.</span>entities)
</span></span></code></pre></div><h2 id="limitations--when-to-upgrade-to-gpt-55">Limitations &amp; When to Upgrade to GPT-5.5</h2>
<p>GPT-5 Turbo (GPT-5.4 mini) has three clear limitations that should push teams toward GPT-5.5 for specific workloads. First, <strong>long-context recall</strong>: at 400K tokens, it handles large documents well, but GPT-5.5&rsquo;s 1M context with 74% MRCR v2 recall is necessary for full-codebase analysis, legal document review spanning hundreds of filings, or scientific literature synthesis across entire research corpora. Second, <strong>complex multi-step reasoning</strong>: GPT-5 Turbo&rsquo;s mini-tier architecture trades depth for speed — it will produce incorrect results on multi-hop logical deductions, advanced math proofs, and intricate planning tasks where GPT-5.5&rsquo;s 88.7% SWE-bench score represents a real capability gap. Third, <strong>agentic computer use</strong>: GPT-5.4 flagship&rsquo;s 75% OSWorld score (surpassing human baseline) is not available in the mini tier — autonomous browser and desktop control requires the full flagship model. For everything else — classification, extraction, summarization, RAG, voice Q&amp;A, customer support, and most API integrations — GPT-5 Turbo is the correct model, and upgrading to GPT-5.5 is unnecessary cost.</p>
<h3 id="is-gpt-5-turbo-good-enough-for-production-code-generation">Is GPT-5 Turbo Good Enough for Production Code Generation?</h3>
<p>For standard code generation tasks — writing functions, fixing bugs, generating tests, explaining code — GPT-5 Turbo is production-ready. It outperforms GPT-4o on all coding benchmarks. The gap to GPT-5.5 becomes significant only for end-to-end software engineering tasks (full PR generation from a GitHub issue, multi-file refactors) where the SWE-bench 55% vs. 88.7% difference is observable. For those tasks, route to GPT-5.5 or use a code-specialized agent framework.</p>
<h2 id="verdict-is-gpt-5-turbo-the-best-fast-model-in-2026">Verdict: Is GPT-5 Turbo the Best Fast Model in 2026?</h2>
<p>GPT-5 Turbo (GPT-5.4 mini) is the best fast OpenAI model available in 2026, and for most production applications it is also the right default model — not just the fast tier. The combination of native multimodal input (images + audio in one call), strict JSON schema enforcement at token level, 400K context window, and GPT-4o mini pricing creates a package that eliminates entire infrastructure layers that GPT-4o required. The only credible competition comes from Gemini 3.1 Flash on pure cost (50% cheaper input) and Claude Haiku 4.5 for Anthropic-ecosystem tool-use patterns. For developers already on OpenAI infrastructure, the upgrade path from GPT-4o mini to GPT-5.4 mini is a model name change — same API contract, same pricing tier, dramatically better results. The case for staying on GPT-4o mini is essentially zero: GPT-5.4 mini matches it on cost, doubles it on capability, and triples its context window. For new projects, GPT-5 Turbo should be the starting point, with GPT-5.5 reserved for the tasks where the benchmark gap (long-context recall, complex agents, computer use) actually manifests in your specific workload.</p>
<hr>
<h2 id="faq">FAQ</h2>
<p>The most common questions developers ask before adopting GPT-5 Turbo in production center on three themes: what exactly the model is (since OpenAI never officially branded it &ldquo;Turbo&rdquo;), how much it costs compared to the GPT-4o mini it replaces, and whether the native multimodal and JSON improvements are real or marketing. The short answers: GPT-5 Turbo = GPT-5.4 mini (released March 17, 2026), it costs the same as GPT-4o mini ($0.15/M input tokens) while delivering 2x the speed and 3x the context window, and the strict JSON enforcement is a genuine architectural change — not a prompt-level improvement. Below are the five questions that come up most frequently in developer forums and OpenAI community threads, answered directly based on documented API behavior, published benchmark data, and real migration experiences from teams that have already moved their workloads to GPT-5.4 mini in 2026.</p>
<h3 id="what-is-gpt-5-turbo-and-when-was-it-released">What is GPT-5 Turbo and when was it released?</h3>
<p>GPT-5 Turbo is the informal name for OpenAI&rsquo;s fast, cost-efficient GPT-5 tier — officially called GPT-5 mini (released August 7, 2025) and updated as GPT-5.4 mini (released March 17, 2026). It is the GPT-5 family&rsquo;s equivalent of GPT-4 Turbo: optimized for speed and cost rather than maximum reasoning depth. The name &ldquo;GPT-5 Turbo&rdquo; is used colloquially by developers to distinguish the mini tier from the flagship GPT-5.4 and GPT-5.5 models.</p>
<h3 id="how-much-does-gpt-5-turbo-cost-per-million-tokens">How much does GPT-5 Turbo cost per million tokens?</h3>
<p>GPT-5.4 mini costs approximately $0.15 per million input tokens and $0.60 per million output tokens — identical in price to GPT-4o mini. This makes it the clear upgrade path: same cost, dramatically higher capability and larger context window (400K vs. 128K). For batch workloads where cost is the primary driver, Gemini 3.1 Flash at $0.075 input is cheaper but lacks strict JSON enforcement and native audio support.</p>
<h3 id="does-gpt-5-turbo-support-image-and-audio-inputs">Does GPT-5 Turbo support image and audio inputs?</h3>
<p>Yes. GPT-5.4 mini accepts images and audio in the same API call via the standard chat completions endpoint — no separate vision or speech-to-text API required. Supported audio formats include MP3, WAV, FLAC, and M4A. Images can be passed as URLs or base64 blobs (JPEG, PNG, WebP, GIF). Both modalities are processed natively within the model&rsquo;s unified architecture, enabling sub-second latency for hybrid voice+vision tasks.</p>
<h3 id="what-is-the-difference-between-gpt-5-turbo-json-mode-and-strict-mode">What is the difference between GPT-5 Turbo JSON mode and Strict Mode?</h3>
<p>Legacy JSON Mode (available since GPT-4o) requests JSON-formatted output but does not guarantee schema compliance — approximately 2–5% of responses had formatting errors in production. GPT-5&rsquo;s Strict Mode enforces the declared JSON schema at token-generation level using a context-free grammar engine, making it physically impossible for the model to produce an invalid response. For any production application writing model output to a database or downstream API, Strict Mode is the correct choice. Enable it by setting <code>response_format</code> to <code>{ &quot;type&quot;: &quot;json_schema&quot;, &quot;json_schema&quot;: { &quot;strict&quot;: true, &quot;schema&quot;: &lt;schema&gt; } }</code>.</p>
<h3 id="should-i-use-gpt-5-turbo-or-gpt-55-for-my-application">Should I use GPT-5 Turbo or GPT-5.5 for my application?</h3>
<p>Use GPT-5 Turbo (GPT-5.4 mini) for interactive user-facing apps, RAG pipelines, voice assistants, document extraction, classification, and standard code generation — it handles 80%+ of production workloads at $0.15/M input. Upgrade to GPT-5.5 when you specifically need: (1) context beyond 400K tokens (GPT-5.5 supports 1M), (2) long-context recall above 74% on MRCR v2, (3) autonomous computer use (OSWorld), or (4) end-to-end software engineering on complex multi-file tasks where the 88.7% SWE-bench score makes a measurable difference. GPT-5.5 also generates 40% fewer output tokens on Codex tasks, which can offset its 2x higher per-token cost for code-heavy workloads.</p>
]]></content:encoded></item></channel></rss>