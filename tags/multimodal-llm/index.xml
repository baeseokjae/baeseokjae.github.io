<?xml version="1.0" encoding="utf-8" standalone="yes"?><rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom" xmlns:content="http://purl.org/rss/1.0/modules/content/"><channel><title>Multimodal LLM on RockB</title><link>https://baeseokjae.github.io/tags/multimodal-llm/</link><description>Recent content in Multimodal LLM on RockB</description><image><title>RockB</title><url>https://baeseokjae.github.io/images/og-default.png</url><link>https://baeseokjae.github.io/images/og-default.png</link></image><generator>Hugo</generator><language>en-us</language><lastBuildDate>Sat, 12 Sep 2026 10:01:11 +0000</lastBuildDate><atom:link href="https://baeseokjae.github.io/tags/multimodal-llm/index.xml" rel="self" type="application/rss+xml"/><item><title>Cove Sensory MCP: Give Text-Only LLMs Eyes and Ears Locally</title><link>https://baeseokjae.github.io/posts/cove-sensory-mcp-llm/</link><pubDate>Sat, 12 Sep 2026 10:01:11 +0000</pubDate><guid>https://baeseokjae.github.io/posts/cove-sensory-mcp-llm/</guid><description>Cove Sensory MCP adds local vision and audio to text-only LLMs. A step-by-step guide to setup, providers, and privacy-first design.</description><content:encoded><![CDATA[<p>Cove Sensory MCP is a local stdio MCP server that hands text-only LLMs like Claude Code and Codex their missing senses: image, video, audio, and music understanding. It routes media files through configurable multimodal providers—Gemini, MiniMax-M3, or OpenAI-compatible endpoints—while keeping everything on-device and privacy-first. Rather than a full assistant, it is a composable sensory layer you bolt onto your existing agent.</p>
<h2 id="h2-what-is-cove-sensory-mcp-and-why-a-sensory-layer">H2: What Is Cove Sensory MCP and Why a &ldquo;Sensory Layer&rdquo;?</h2>
<p>Cove Sensory MCP is a Model Context Protocol server that deliberately limits itself to a single job: giving text-only language models the ability to perceive media files. The concept comes from Anthropic&rsquo;s Model Context Protocol, a universal open standard that lets AI applications connect to external tools and context. Instead of trying to build chat, memory, personality, playback, monitoring, or call policies, Cove&rsquo;s creators carved out one focused capability—sensing—and made it reusable across agents.</p>
<p>The philosophy is important. Most multimodal integrations try to be everything at once. Cove does the opposite: it is a sensory layer only. That means it plugs cleanly into whatever agent you already use, exposes exactly four sensing tools, and leaves reasoning, memory, and orchestration to the host. This composability is what makes it attractive for developers who want vision and audio without replacing their existing stack.</p>
<p>The repository, hosted by moonlin1213, sits under Apache License 2.0 and is written primarily in Python, with supporting PowerShell and Shell scripts. It is young but active: it reached 58 GitHub stars and 14 forks within its first month after creation on 2026-08-08. The single top contributor accounts for 71 contributions, and the project tracks zero open issues—signs of a tightly scoped, well-maintained codebase.</p>
<h2 id="h2-the-problem-text-only-llms-with-no-eyes-or-ears">H2: The Problem: Text-Only LLMs with No Eyes or Ears</h2>
<p>Text-only language models are extraordinarily capable at reasoning over words, but they are effectively blind and deaf to raw media. Give one a screenshot, a diagnostic video, a meeting recording, or a music file and it can only guess what is inside. This is a real limitation for workflows that depend on visual or auditory evidence: debugging from error screenshots, reviewing product mockups, transcribing meetings, moderating video content, or identifying songs.</p>
<p>The conventional workaround is to upload files to a web-based multimodal model and paste the description back. That approach has costs: the media leaves your machine, you juggle multiple tools and accounts, and your text agent loses the thread of context. Developers who run local coding agents especially want to keep media on-device while still getting the interpretive power of a strong multimodal model.</p>
<p>Cove Sensory MCP closes that gap by acting as a bridge. The text agent reads a file path or a direct HTTPS URL, hands it to Cove&rsquo;s sensing tools, and those tools forward the media to a provider you authorize. The provider returns a natural-language description, and that description flows back into your agent&rsquo;s context as if it were ordinary text. Your agent gains eyes and ears without ever changing how it reasons.</p>
<h2 id="h2-meet-the-four-sense-tools-sense_image-sense_video-sense_audio-sense_music">H2: Meet the Four Sense Tools (sense_image, sense_video, sense_audio, sense_music)</h2>
<p>Cove exposes four sensing tools plus three helpers. The primary tools map one-to-one onto media types:</p>
<ul>
<li><strong>sense_image</strong> — Analyzes a still image and returns a structured description. This covers screenshots, diagrams, photos, and UI mockups.</li>
<li><strong>sense_video</strong> — Understands video content, useful for reviewing clips, monitoring footage, or extracting narrative from short recordings.</li>
<li><strong>sense_audio</strong> — Transcribes and interprets audio, covering recordings, voice notes, and spoken-word meetings.</li>
<li><strong>sense_music</strong> — Analyzes music files, suited to genre identification, mood analysis, or metadata extraction.</li>
</ul>
<p>Alongside these sit three support tools:</p>
<ul>
<li><strong>sensory_setup_guide</strong> — Walks you through verifying that your MCP stack is correctly configured for sensing.</li>
<li><strong>sensory_status</strong> — Reports the current health of the sensory layer, including provider readiness and required dependencies.</li>
<li><strong>sensory_self_test</strong> — Runs an end-to-end check to confirm that a provider can actually digest a sample media file.</li>
</ul>
<p>Each sensing tool is deliberately thin: it receives media, selects the authorized provider, and returns the provider&rsquo;s interpretation. Because the tools are separate, you can grant granular permissions—say, image sensing only—instead of opening the whole multimodal surface to your agent.</p>
<h2 id="h2-choosing-your-eyes-and-ears-gemini-minimax-m3-and-openai-compatible-audio">H2: Choosing Your Eyes and Ears: Gemini, MiniMax-M3, and OpenAI-Compatible Audio</h2>
<p>Cove does not lock you into one provider. Instead, it supports a provider picker so you can pair the best model to each sense. The project documents three representative configurations:</p>
<ul>
<li><strong>Gemini as eye and ear</strong> — A single Gemini provider can handle image, video, and audio, making it the simplest all-rounder for &ldquo;eyes and ears&rdquo; in one place.</li>
<li><strong>MiniMax-M3 as an eye</strong> — Configured as an image and video-only provider, this is a good choice when you want strong visual analysis from a dedicated vision model.</li>
<li><strong>OpenAI-compatible audio_url_data_uri as an ear</strong> — For audio understanding, you can point Cove at any OpenAI-compatible endpoint that accepts audio URLs or data URIs, such as Qwen3-Omni served through SiliconFlow.</li>
</ul>
<p>This flexibility means you can optimize for cost, privacy, or capability per media type rather than accepting a single vendor&rsquo;s trade-offs. The configuration happens in the credential store, not in the chat interface: you set provider credentials using <code>env:VAR</code> references or an OS-level credential store, so keys never sit in your conversation history or in plaintext config files.</p>
<p>A cross-provider fallback model adds stability: if one provider fails or returns a weak result, Cove can retry with a secondary provider—but only for ones you have explicitly authorized. The design never infers a fallback you have not configured.</p>
<h2 id="h2-privacy-first-by-design-path-whitelists-credential-stores-and-https-only">H2: Privacy-First by Design: Path Whitelists, Credential Stores, and HTTPS-Only</h2>
<p>Security is the backbone of Cove&rsquo;s architecture, and it is worth understanding before you wire it in. Media can be sensitive—screenshots of proprietary code, recordings of private meetings—so the project enforces layered controls:</p>
<ul>
<li><strong>Path whitelists</strong> — When your agent references a local file, its absolute path must fall within the roots you have configured. Anything outside is refused.</li>
<li><strong>OS credential stores</strong> — Provider API keys live in the operating system&rsquo;s credential store or in environment-variable references, not in plaintext files or chat history.</li>
<li><strong>HTTPS-only URLs</strong> — Remote media must use direct HTTPS links. Cove blocks redirects, credentials embedded in URLs, private networks, localhost, and metadata endpoints.</li>
</ul>
<p>These rules align comfortably with privacy frameworks like the GDPR and CCPA: the media stays on your device until it is sent only to the authorized provider, and you retain control over which third party ever sees a single file. Only the selected, authorized provider receives a given file—never a silently chosen default.</p>
<p>For a privacy-aware operator, this is the core selling point. You get the interpretive power of hosted multimodal models without broadcasting every file to every vendor.</p>
<h2 id="h2-step-by-step-local-setup-with-uv-doctor-and-print-config">H2: Step-by-Step Local Setup with uv, doctor, and print-config</h2>
<p>Getting Cove running locally is a straightforward sequence. The recommended path uses <code>uv</code>, the fast Python package manager:</p>
<ol>
<li><strong>Clone the repository</strong> — <code>git clone</code> the Cove Sensory MCP repo into your working directory.</li>
<li><strong>Sync dependencies with <code>uv sync --locked</code></strong> — This installs pinned dependencies exactly as the project expects, avoiding drift.</li>
<li><strong>Run the doctor</strong> — Cove ships a diagnostic command that verifies your environment, flags missing dependencies, and confirms the server can start.</li>
<li><strong>Print your config</strong> — Run <code>print-config</code> for your specific renderer. Configured renderers include generic, codex, claude-desktop, and claude-code, each of which needs slightly different MCP registration JSON.</li>
<li><strong>Configure the credential store</strong> — Set your provider keys via <code>env:VAR</code> references or the OS credential store before first use.</li>
</ol>
<p>The <code>print-config</code> step is where people often stumble. Each renderer expects the MCP server registered in a particular shape, and Cove generates the exact snippet for the renderer you picked, so you can paste it into the right config file without guessing. After registration, restart your agent and run <code>sensory_self_test</code> to verify the whole pipeline end to end before trusting it with real media.</p>
<h2 id="h2-wiring-it-into-codex-claude-code-and-claude-desktop">H2: Wiring It Into Codex, Claude Code, and Claude Desktop</h2>
<p>Cove is designed to snap into the most popular local agents. Because it runs as a stdio MCP server, each host configures it the same conceptual way—register an MCP server pointing at the Cove command—but the specifics differ per renderer.</p>
<ul>
<li><strong>Claude Code</strong> — Register Cove in Claude Code&rsquo;s MCP configuration, then invoke the sensing tools directly in your coding session. A developer can ask &ldquo;what does this error screenshot show?&rdquo; and get a grounded visual description without leaving the terminal.</li>
<li><strong>Codex</strong> — Configure the codex renderer the same way. Codex can then call the multimodal tools for image and audio analysis in the middle of a coding task.</li>
<li><strong>Claude Desktop</strong> — For a desktop companion, register the server and invoke it with the standard tool-call pattern. The desktop renderer gets the same sensing capability in a GUI context.</li>
</ul>
<p>In every case the workflow is identical for your agent: it sees a media path or URL, invokes <code>sense_image</code> (or its siblings), and receives a text description back. Once registered, the sensing tools behave like any other MCP tool to your host, so your existing permission and approval rules still apply.</p>
<h2 id="h2-when-ffmpeg-is-required-and-how-to-handle-videoaudio-prep">H2: When FFmpeg Is Required and How to Handle Video/Audio Prep</h2>
<p>FFmpeg is optional for images but becomes a hard dependency for video and audio preparation. Cove relies on FFmpeg to normalize media into a format the provider can actually ingest: container conversion, codec compatibility, duration trimming, and sometimes downscaling. If you plan to sense video or audio, you must have a working FFmpeg binary on the machine running the MCP server.</p>
<p>Common pitfalls during FFmpeg setup include:</p>
<ul>
<li><strong>Missing or old builds</strong> — Ensure FFmpeg is on your system&rsquo;s PATH and recent enough to handle modern codecs like H.264/H.265 and AAC.</li>
<li><strong>Codec gaps</strong> — Some providers reject unusual containers; let FFmpeg transcode to a broadly supported profile first.</li>
<li><strong>Large files</strong> — For long recordings, decide on duration limits up front, because processing cost and latency scale with file size.</li>
</ul>
<p>A useful habit is to run <code>sensory_self_test</code> with a short sample after installing FFmpeg. That confirms the prep pipeline works before you hand Cove a long meeting recording. The cross-provider fallback also helps here: if one provider chokes on a particular codec, a secondary authorized provider may succeed instead.</p>
<h2 id="h2-cross-provider-fallback-self-tests-and-security-limits">H2: Cross-Provider Fallback, Self-Tests, and Security Limits</h2>
<p>Beyond the basics, Cove gives you operational controls that matter in production:</p>
<ul>
<li><strong>Cross-provider fallback</strong> — Failures on one provider can retry against another, but only providers you explicitly authorized. This improves reliability without expanding your trust surface.</li>
<li><strong>sensory_self_test</strong> — A quick end-to-end check that validates provider connectivity and media digestion before you commit to real files.</li>
<li><strong>sensory_status</strong> — Live health reporting so you can see whether the sensory layer, its providers, and FFmpeg are all ready.</li>
<li><strong>Security limits</strong> — Local paths must be inside configured roots; URLs must be direct HTTPS; and Cove blocks redirects, embedded credentials, private networks, localhost, and metadata endpoints.</li>
</ul>
<p>These controls make the server predictable in adversarial or messy environments. The whitelist and HTTPS restrictions in particular matter if your agent might be handed arbitrary file paths or URLs, because they sharply limit what Cove is willing to touch on your behalf.</p>
<h2 id="h2-is-cove-sensory-mcp-right-for-your-agent">H2: Is Cove Sensory MCP Right for Your Agent?</h2>
<p>Cove Sensory MCP is an excellent fit when you already run a capable text agent and need to add reliable, privacy-respecting media understanding without switching your whole stack. It shines for developers using Codex or Claude Code who want to interpret screenshots and recordings in-context, and for teams that value on-device control and GDPR/CCPA-style data discipline.</p>
<p>It is less ideal if you are looking for an all-in-one assistant with memory, personality, playback, or monitoring—Cove deliberately does not include those, and you would end up assembling them yourself. Similarly, if you need only image understanding for one specific provider, the provider-picker flexibility may be more than you require, though it does not hurt.</p>
<p>On balance, if your workflow bumps up against the blindness of a text-only LLM and you want a composable, secure, and local answer, Cove Sensory MCP is a compelling, low-friction choice—backed by a young but active, cleanly licensed, and well-documented codebase.</p>
<h2 id="faq">FAQ</h2>
<h3 id="q1-do-text-only-llms-like-claude-code-really-need-an-mcp-sensory-server">Q1: Do text-only LLMs like Claude Code really need an MCP sensory server?</h3>
<p>Yes, if they work with media. Without a sensory layer, a text-only LLM can only guess the contents of an image, video, or audio file. A server like Cove lets the agent receive a grounded text description, so it can reason about media instead of guessing.</p>
<h3 id="q2-which-providers-does-cove-sensory-mcp-support">Q2: Which providers does Cove Sensory MCP support?</h3>
<p>Cove uses a configurable provider picker. Documented configurations include Gemini for image, video, and audio; MiniMax-M3 for image and video; and OpenAI-compatible endpoints (for example, Qwen3-Omni via SiliconFlow) for audio. Provider credentials are set in the credential store, not in chat.</p>
<h3 id="q3-is-my-media-ever-uploaded-and-to-whom">Q3: Is my media ever uploaded, and to whom?</h3>
<p>Media stays on your machine until a sensing tool sends it to the provider you explicitly authorized. Path whitelists, OS credential stores, and HTTPS-only URL rules control exactly what leaves the device and where it goes, aligning with privacy frameworks like GDPR and CCPA.</p>
<h3 id="q4-why-is-ffmpeg-required-for-video-and-audio">Q4: Why is FFmpeg required for video and audio?</h3>
<p>FFmpeg normalizes media into a format the provider can ingest—handling container conversion, codec compatibility, and duration trimming. It is optional for images but required for video and audio sensing.</p>
<h3 id="q5-how-do-i-verify-the-setup-before-trusting-it-with-real-files">Q5: How do I verify the setup before trusting it with real files?</h3>
<p>Run <code>sensory_self_test</code> after configuration and FFmpeg installation. It sends a sample file through the provider pipeline end to end and confirms the sensing layer works before you point it at sensitive or large media.</p>
]]></content:encoded></item><item><title>JARVIS-1: Open-World Multi-Task Agents with Memory-Augmented Multimodal LLMs — Full Review</title><link>https://baeseokjae.github.io/posts/jarvis-1-memory-augmented-multimodal-2026/</link><pubDate>Mon, 20 Jul 2026 19:02:40 +0000</pubDate><guid>https://baeseokjae.github.io/posts/jarvis-1-memory-augmented-multimodal-2026/</guid><description>JARVIS-1 is a memory-augmented multimodal LLM agent that completes 200+ Minecraft tasks with 5x reliability over prior SOTA through self-improving multimodal memory.</description><content:encoded><![CDATA[<h2 id="what-is-jarvis-1-and-why-does-it-matter-for-open-world-ai-agents">What Is JARVIS-1 and Why Does It Matter for Open-World AI Agents?</h2>
<p>JARVIS-1 is an open-world multi-task agent that combines multimodal large language models with a growing memory system to complete over 200 different tasks in Minecraft using human-like control and observation spaces. Developed by Team CraftJarvis at Peking University and BIGAI, JARVIS-1 achieves a 5x reliability improvement over previous state-of-the-art agents on the challenging ObtainDiamondPickaxe task and near-perfect performance on short-horizon tasks like chopping trees. Its key innovation is a multimodal memory that blends pre-trained knowledge from the LLM with actual game survival experiences, enabling self-improvement without retraining.</p>
<h2 id="how-does-jarvis-1s-architecture-work">How Does JARVIS-1&rsquo;s Architecture Work?</h2>
<p>JARVIS-1 follows a three-stage pipeline: multimodal perception, plan generation, and goal-conditioned control. Each stage is designed to handle the unique challenges of open-world environments where the agent must interpret visual observations, reason about long-term goals, and execute precise actions.</p>
<h3 id="how-does-multimodal-input-processing-work">How Does Multimodal Input Processing Work?</h3>
<p>The agent receives visual observations from the Minecraft environment as RGB frames and textual instructions describing the task. A pre-trained multimodal language model processes both modalities jointly, mapping visual observations and text instructions into a shared representation space. This allows JARVIS-1 to understand what it sees (a tree, a crafting table, a furnace) in the context of what it needs to do (chop wood, craft tools, smelt ore).</p>
<p>Unlike pure text-based approaches like GITM that rely on structured action spaces and text-only knowledge, JARVIS-1 operates in a human-like observation space — it sees pixel frames just as a human player would. This makes the approach more transferable to real-world robotics scenarios where raw visual input is the norm.</p>
<h3 id="how-does-plan-generation-work-with-multimodal-llms">How Does Plan Generation Work with Multimodal LLMs?</h3>
<p>Once the visual and textual inputs are processed, the multimodal LLM generates high-level plans. These plans decompose the task into sub-goals that the agent can execute sequentially. For example, to obtain a diamond pickaxe, the agent must plan: gather wood → craft a wooden pickaxe → mine stone → craft a stone pickaxe → mine iron ore → smelt iron → craft an iron pickaxe → mine diamonds → craft a diamond pickaxe.</p>
<p>The LLM&rsquo;s pre-trained knowledge provides commonsense reasoning about these dependencies — it knows that you need a furnace before you can smelt iron, and that you need a pickaxe before you can mine stone. This commonsense reasoning is what separates JARVIS-1 from pure reinforcement learning approaches that must discover these dependencies through trial and error.</p>
<h3 id="how-do-goal-conditioned-controllers-execute-plans">How Do Goal-Conditioned Controllers Execute Plans?</h3>
<p>After the plan is generated, goal-conditioned controllers translate each sub-goal into low-level actions. These controllers are trained to execute specific behaviors — moving to a location, interacting with a block, crafting an item — given a goal state. The controllers bridge the gap between high-level symbolic planning and low-level embodied control, a critical challenge for any open-world agent.</p>
<h2 id="what-makes-multimodal-memory-the-key-innovation">What Makes Multimodal Memory the Key Innovation?</h2>
<p>The defining feature of JARVIS-1 is its multimodal memory system, which stores both visual and textual experiences from past episodes. This memory grows over time and directly influences future planning and execution.</p>
<h3 id="pre-trained-knowledge-vs-game-experience">Pre-Trained Knowledge vs. Game Experience</h3>
<p>JARVIS-1&rsquo;s memory has two sources. First, the pre-trained multimodal LLM brings vast knowledge about the world — how tools work, what materials are needed for crafting, the structure of the Minecraft tech tree. Second, the agent accumulates actual game experiences: what happened when it tried to smelt iron without fuel, where it found diamonds, which sequence of actions led to success.</p>
<p>The combination is powerful. Pre-trained knowledge gives the agent a strong starting point, while game experience allows it to adapt to the specific environment and recover from mistakes. This mirrors how humans learn — we start with general knowledge and refine it through practice.</p>
<h3 id="self-improvement-through-growing-memory-the-epoch-13-analysis">Self-Improvement Through Growing Memory: The Epoch 1→3 Analysis</h3>
<p>The JARVIS-1 project page demonstrates self-improvement across three training epochs on the ObtainDiamondPickaxe task:</p>
<table>
  <thead>
      <tr>
          <th>Epoch</th>
          <th>Outcome</th>
          <th>Key Failure / Improvement</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>Epoch 1</td>
          <td>Fails</td>
          <td>Agent attempts to smelt iron but has no furnace</td>
      </tr>
      <tr>
          <td>Epoch 2</td>
          <td>Fails</td>
          <td>Agent builds a furnace but lacks fuel (coal/wood)</td>
      </tr>
      <tr>
          <td>Epoch 3</td>
          <td>Succeeds efficiently</td>
          <td>Agent mines extra logs for fuel before smelting, completing the full chain</td>
      </tr>
  </tbody>
</table>
<p>In Epoch 1, the agent&rsquo;s pre-trained knowledge told it to smelt iron, but it didn&rsquo;t know a furnace was needed. After failing, the memory recorded this gap. In Epoch 2, the agent built a furnace but didn&rsquo;t gather fuel — another failure recorded. By Epoch 3, the accumulated multimodal memory contained both failures, and the agent planned ahead: mine extra wood for fuel, build the furnace, then smelt. The task succeeded efficiently.</p>
<p>This three-epoch trajectory is remarkable because no model weights were updated. The agent improved purely through its growing memory, demonstrating that multimodal memory can substitute for expensive retraining in many scenarios.</p>
<h2 id="what-performance-benchmarks-does-jarvis-1-achieve">What Performance Benchmarks Does JARVIS-1 Achieve?</h2>
<p>JARVIS-1 was evaluated across a comprehensive suite of Minecraft tasks, from simple atomic actions to complex multi-step crafting chains spanning the entire tech tree.</p>
<h3 id="short-horizon-tasks-near-perfect-performance">Short-Horizon Tasks: Near-Perfect Performance</h3>
<p>On short-horizon tasks — actions that require only a few steps, such as chopping a tree, mining dirt, or crafting a wooden plank — JARVIS-1 achieves near-perfect performance. These tasks benefit from the agent&rsquo;s strong pre-trained knowledge and the fact that fewer steps mean fewer opportunities for error accumulation.</p>
<h3 id="long-horizon-tasks-5x-improvement-on-obtaindiamondpickaxe">Long-Horizon Tasks: 5x Improvement on ObtainDiamondPickaxe</h3>
<p>The ObtainDiamondPickaxe task is the standard benchmark for Minecraft agents. It requires the agent to navigate the full tech tree: collect wood, craft tools, mine progressively better materials, and finally craft a diamond pickaxe. JARVIS-1 achieves a 5x reliability improvement over previous state-of-the-art agents on this task.</p>
<table>
  <thead>
      <tr>
          <th>Metric</th>
          <th>JARVIS-1</th>
          <th>Prior SOTA</th>
          <th>Improvement</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>ObtainDiamondPickaxe success rate</td>
          <td>5x more reliable</td>
          <td>Baseline</td>
          <td>5x improvement</td>
      </tr>
      <tr>
          <td>Short-horizon task accuracy</td>
          <td>Near-perfect</td>
          <td>Variable</td>
          <td>Significant</td>
      </tr>
      <tr>
          <td>Total tasks supported</td>
          <td>200+</td>
          <td>~70 (DEPS)</td>
          <td>2.85x more tasks</td>
      </tr>
      <tr>
          <td>Self-improvement method</td>
          <td>Growing memory (no retrain)</td>
          <td>Skill library (Voyager)</td>
          <td>No GPU needed</td>
      </tr>
  </tbody>
</table>
<h3 id="200-tasks-across-the-minecraft-tech-tree">200+ Tasks Across the Minecraft Tech Tree</h3>
<p>JARVIS-1 can complete over 200 different tasks spanning the entire Minecraft technology tree. This includes atomic tasks (chop a tree, mine stone), programmatic tasks (craft a pickaxe, build a furnace), and open-ended tasks (explore a cave, survive a night). The breadth of tasks demonstrates that memory-augmented multimodal LLMs can generalize across diverse objectives without task-specific fine-tuning.</p>
<h2 id="how-does-jarvis-1-compare-to-other-minecraft-agents">How Does JARVIS-1 Compare to Other Minecraft Agents?</h2>
<p>Several notable approaches have tackled the Minecraft agent problem. Here is how JARVIS-1 stacks up against its predecessors and competitors.</p>
<h3 id="voyager-the-llm-powered-skill-library">Voyager: The LLM-Powered Skill Library</h3>
<p>Voyager, published in May 2023, was the first LLM-powered embodied lifelong learning agent in Minecraft. It uses GPT-4 via blackbox queries and features three components: an automatic curriculum, a skill library of executable code, and iterative prompting. Voyager obtained 3.3x more unique items, traveled 2.3x longer distances, and achieved 15.3x faster tech tree milestones than prior SOTA.</p>
<p>However, Voyager relies entirely on text-based prompting of GPT-4 and does not use multimodal input. It cannot see the game world — it only receives text descriptions. JARVIS-1 improves on this by processing visual observations directly, making it more suitable for real-world applications where text descriptions are unavailable.</p>
<h3 id="gitm-text-based-llm-knowledge-and-memory">GITM: Text-Based LLM Knowledge and Memory</h3>
<p>Ghost in the Minecraft (GITM) uses LLMs with text-based knowledge and memory, achieving a +47.5% improvement in success rate on the ObtainDiamond task. It was the first agent to procure all items in the Minecraft Overworld technology tree. Notably, GITM requires no GPU for training — only 32 CPU cores.</p>
<p>GITM&rsquo;s structured action space is both a strength and a limitation. It enables efficient planning but restricts the agent to predefined actions. JARVIS-1&rsquo;s human-like observation space is more flexible and transferable.</p>
<h3 id="deps-interactive-planning-with-goal-selection">DEPS: Interactive Planning with Goal Selection</h3>
<p>DEPS (Describe, Explain, Plan and Select) introduced an interactive planning approach for zero-shot multi-task agents handling 70+ Minecraft tasks. Its goal selector module ranks parallel candidate sub-goals, achieving nearly double overall performance versus baselines. DEPS also demonstrated effectiveness in ALFWorld and tabletop manipulation environments.</p>
<h3 id="plan4mc-skill-rl-and-graph-based-planning">Plan4MC: Skill RL and Graph-Based Planning</h3>
<p>Plan4MC combines skill reinforcement learning with planning for open-world long-horizon tasks. It introduces three types of fine-grained basic skills with intrinsic rewards, including a novel Finding-skill for exploration. Its LLM-based skill graph enables structured planning across 40 diverse Minecraft tasks, making it the most sample-efficient demonstration-free RL method at the time.</p>
<h3 id="omnijarvis-unified-vla-tokenization-successor">OmniJARVIS: Unified VLA Tokenization (Successor)</h3>
<p>OmniJARVIS, published in July 2024 by the same research group, represents the next evolution. It introduces unified Vision-Language-Action tokenization with a self-supervised behavior encoder that produces discretized tokens. OmniJARVIS creates unified token sequences for instructions, memories, thoughts, observations, and actions, enabling a single model to reason, plan, answer questions, and act. It achieves excellent results on atomic, programmatic, and open-ended tasks.</p>
<h3 id="comparison-table">Comparison Table</h3>
<table>
  <thead>
      <tr>
          <th>Feature</th>
          <th>JARVIS-1</th>
          <th>Voyager</th>
          <th>GITM</th>
          <th>DEPS</th>
          <th>Plan4MC</th>
          <th>OmniJARVIS</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>Multimodal input</td>
          <td>Yes (vision + text)</td>
          <td>No (text only)</td>
          <td>No (text only)</td>
          <td>No (text only)</td>
          <td>No (text only)</td>
          <td>Yes (unified tokens)</td>
      </tr>
      <tr>
          <td>Memory type</td>
          <td>Multimodal (growing)</td>
          <td>Skill library (code)</td>
          <td>Text-based memory</td>
          <td>None</td>
          <td>Skill graph</td>
          <td>Unified token memory</td>
      </tr>
      <tr>
          <td>Tasks supported</td>
          <td>200+</td>
          <td>Unlimited (curriculum)</td>
          <td>Full tech tree</td>
          <td>70+</td>
          <td>40</td>
          <td>200+</td>
      </tr>
      <tr>
          <td>Self-improvement</td>
          <td>Growing memory</td>
          <td>Iterative prompting</td>
          <td>N/A</td>
          <td>N/A</td>
          <td>RL training</td>
          <td>Unified tokens</td>
      </tr>
      <tr>
          <td>GPU needed</td>
          <td>Yes (multimodal LLM)</td>
          <td>No (GPT-4 API)</td>
          <td>No (32 CPU cores)</td>
          <td>No (API)</td>
          <td>Yes (RL)</td>
          <td>Yes</td>
      </tr>
      <tr>
          <td>Publication</td>
          <td>Nov 2023</td>
          <td>May 2023</td>
          <td>May 2023</td>
          <td>Feb 2023</td>
          <td>Mar 2023</td>
          <td>Jul 2024</td>
      </tr>
  </tbody>
</table>
<h2 id="what-are-the-critical-limitations-of-jarvis-1">What Are the Critical Limitations of JARVIS-1?</h2>
<p>Despite its impressive results, JARVIS-1 has several limitations worth examining.</p>
<p><strong>Computational requirements.</strong> JARVIS-1 requires a multimodal LLM for inference, which demands GPU resources. This contrasts with GITM, which runs on 32 CPU cores, and Voyager, which uses the GPT-4 API. For researchers without GPU access, JARVIS-1&rsquo;s approach may be less accessible.</p>
<p><strong>Minecraft-specific evaluation.</strong> All benchmarks are conducted within Minecraft. While Minecraft is a rich open-world environment, it is still a game with simplified physics and clear reward structures. Transfer to real-world robotics — with noisy sensors, continuous action spaces, and physical constraints — remains unproven.</p>
<p><strong>Memory growth management.</strong> The multimodal memory grows over time as the agent accumulates experiences. Without a forgetting or summarization mechanism, the memory could become unwieldy after thousands of episodes. The paper does not fully address how memory scales with long-term deployment.</p>
<p><strong>Dependency on pre-trained models.</strong> JARVIS-1&rsquo;s performance is bounded by the capabilities of its underlying multimodal LLM. If the LLM lacks knowledge about a particular domain or makes reasoning errors, the agent inherits those limitations. The self-improvement mechanism can compensate for some gaps but cannot overcome fundamental model weaknesses.</p>
<p><strong>Evaluation methodology.</strong> The 5x reliability improvement figure is impressive, but the paper does not provide extensive statistical analysis across multiple seeds or environments. Reproducibility and variance across runs would strengthen the claims.</p>
<h2 id="what-are-the-future-directions-for-memory-augmented-multimodal-agents">What Are the Future Directions for Memory-Augmented Multimodal Agents?</h2>
<p>The trajectory from Voyager to JARVIS-1 to OmniJARVIS suggests several clear research directions.</p>
<p><strong>Unified tokenization.</strong> OmniJARVIS&rsquo;s approach of representing instructions, memories, thoughts, observations, and actions as a unified token sequence points toward a future where a single model handles all aspects of agent behavior without separate planning and control modules.</p>
<p><strong>Scalable memory architectures.</strong> As agents accumulate more experiences, efficient memory retrieval and summarization become critical. Future work will likely explore hierarchical memory, attention-based retrieval, and learned forgetting mechanisms.</p>
<p><strong>Transfer to robotics.</strong> The ultimate test for open-world agents is deployment on physical robots. JARVIS-1&rsquo;s human-like observation space makes it a strong candidate for transfer, but the gap between Minecraft and the real world remains substantial.</p>
<p><strong>Multi-agent collaboration.</strong> Minecraft supports multiplayer environments. Extending JARVIS-1 to multi-agent settings where agents share memories and coordinate tasks is a natural next step.</p>
<p><strong>Sample efficiency.</strong> While JARVIS-1 improves through memory rather than retraining, it still requires multiple episodes to accumulate useful experiences. Combining memory augmentation with more sample-efficient exploration strategies could accelerate learning.</p>
<h2 id="conclusion">Conclusion</h2>
<p>JARVIS-1 represents a significant step forward in open-world multi-task agents by demonstrating that memory-augmented multimodal LLMs can achieve reliable, self-improving performance across 200+ diverse tasks. Its 5x reliability improvement on the ObtainDiamondPickaxe benchmark and near-perfect short-horizon task performance validate the approach of combining pre-trained multimodal knowledge with growing experiential memory.</p>
<p>The evolution from Voyager&rsquo;s skill library approach to JARVIS-1&rsquo;s multimodal memory to OmniJARVIS&rsquo;s unified tokenization shows a clear trajectory toward more integrated, capable agents. While limitations remain — particularly around computational requirements, Minecraft-specific evaluation, and memory scaling — the direction is promising for anyone building generalist embodied AI systems.</p>
<p>For researchers and practitioners, JARVIS-1 offers a compelling architecture that balances pre-trained knowledge with experiential learning, achieving self-improvement without the cost of model retraining. As the field moves toward unified vision-language-action models, the lessons from JARVIS-1&rsquo;s multimodal memory design will likely influence the next generation of open-world agents.</p>
<h2 id="frequently-asked-questions">Frequently Asked Questions</h2>
<h3 id="what-is-jarvis-1">What is JARVIS-1?</h3>
<p>JARVIS-1 is an open-world multi-task agent that uses a memory-augmented multimodal large language model to complete over 200 different tasks in Minecraft. It processes visual observations and text instructions to generate plans, executes them through goal-conditioned controllers, and improves over time by accumulating multimodal memories of its experiences.</p>
<h3 id="how-is-jarvis-1-different-from-voyager">How is JARVIS-1 different from Voyager?</h3>
<p>Voyager uses GPT-4 with a skill library of executable code and iterative prompting but operates on text descriptions only. JARVIS-1 processes visual observations directly through a multimodal LLM and uses a growing multimodal memory system rather than a code-based skill library. JARVIS-1 also achieves a 5x reliability improvement on the ObtainDiamondPickaxe task.</p>
<h3 id="does-jarvis-1-require-gpu-training">Does JARVIS-1 require GPU training?</h3>
<p>JARVIS-1 requires a GPU to run its multimodal LLM for inference, but it does not require model retraining or fine-tuning. The agent improves through its growing memory system, not through weight updates. This is different from approaches like Plan4MC that require reinforcement learning training.</p>
<h3 id="what-tasks-can-jarvis-1-complete">What tasks can JARVIS-1 complete?</h3>
<p>JARVIS-1 can complete over 200 different Minecraft tasks spanning atomic actions (chopping trees, mining stone), programmatic tasks (crafting tools, building furnaces), and open-ended tasks (exploring caves, surviving nights). It covers the full Minecraft technology tree from basic wood collection to diamond tool crafting.</p>
<h3 id="what-is-omnijarvis-and-how-does-it-relate-to-jarvis-1">What is OmniJARVIS and how does it relate to JARVIS-1?</h3>
<p>OmniJARVIS is the successor to JARVIS-1, published in July 2024 by the same research group. It introduces unified Vision-Language-Action tokenization where instructions, memories, thoughts, observations, and actions are all represented as a single token sequence. This allows a single model to reason, plan, answer questions, and act without separate modules.</p>
]]></content:encoded></item></channel></rss>