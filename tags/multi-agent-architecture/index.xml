<?xml version="1.0" encoding="utf-8" standalone="yes"?><rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom" xmlns:content="http://purl.org/rss/1.0/modules/content/"><channel><title>Multi-Agent Architecture on RockB</title><link>https://baeseokjae.github.io/tags/multi-agent-architecture/</link><description>Recent content in Multi-Agent Architecture on RockB</description><image><title>RockB</title><url>https://baeseokjae.github.io/images/og-default.png</url><link>https://baeseokjae.github.io/images/og-default.png</link></image><generator>Hugo</generator><language>en-us</language><lastBuildDate>Thu, 24 Sep 2026 19:01:02 +0000</lastBuildDate><atom:link href="https://baeseokjae.github.io/tags/multi-agent-architecture/index.xml" rel="self" type="application/rss+xml"/><item><title>Agentic Workflow Collaboration: How to Let Agents Do Agent Work</title><link>https://baeseokjae.github.io/posts/raymond-agentic-workflow-agents-work/</link><pubDate>Thu, 24 Sep 2026 19:01:02 +0000</pubDate><guid>https://baeseokjae.github.io/posts/raymond-agentic-workflow-agents-work/</guid><description>Letting agents do agent work means orchestrating a team of specialized AI workers — not prompting one model. Here&amp;#39;s how to design multi-agent workflows.</description><content:encoded><![CDATA[<p>Letting agents do agent work means moving from prompting a single model to orchestrating a team of specialized AI workers that collaborate toward one outcome. The real bottleneck in 2026 is not model capability — it is coordination. In this guide, you will learn how to design agent teams, choose the right collaboration patterns, structure handoffs, and govern human-on-the-loop workflows that actually ship.</p>
<h2 id="what-does-it-mean-to-let-agents-do-agent-work">What Does It Mean to Let Agents Do Agent Work?</h2>
<p>For years, &ldquo;automation with AI&rdquo; meant one model, one prompt, one answer. Andrew Ng&rsquo;s research reframed this: a relatively weak GPT-3.5 model driven by an iterative agentic workflow scored <strong>95.1% on HumanEval</strong>, while GPT-4 zero-shot scored only 67%. The lesson is not that bigger models are obsolete — it is that <em>how you structure the work</em> often matters more than raw model size.</p>
<p>Letting agents do agent work is the practical consequence of that insight. Instead of asking one agent to hold an entire task, you decompose the task into discrete outcomes and hand each outcome to a specialist. An orchestrator plans and delegates; workers execute; a reviewer checks the result. Each agent owns one responsibility. None of them holds the whole picture, and that division is exactly what makes the system reliable, debuggable, and safe enough for production.</p>
<p>Adoption has moved from novelty to default. Anthropic&rsquo;s 2026 State of AI Agents Report found that <strong>57% of organizations now deploy agents for multi-stage workflows</strong>, and 16% have advanced to cross-functional processes spanning multiple teams. Halkwinds Research reports that <strong>45% of enterprise AI teams have at least one autonomous agent in production</strong> — up from under 3% in 2024. This is not a research trend; it is how modern software is being built.</p>
<h2 id="the-coordination-problem-why-multi-agent-collaboration-is-hard">The Coordination Problem: Why Multi-Agent Collaboration Is Hard</h2>
<p>The hardest part of building an agent team is not the agents themselves — it is getting them to work together. Practitioners across CrewAI, LangGraph, AutoGen, OpenAI Agents SDK, Google ADK, and Claude Code all converge on the same diagnosis: the coordination problem is the biggest bottleneck in production multi-agent systems.</p>
<h3 id="what-makes-coordination-fail">What makes coordination fail</h3>
<ul>
<li><strong>Shared context decay</strong>: each agent&rsquo;s context window is finite. When five agents each hold fragments of a task, no single agent has the whole truth.</li>
<li><strong>Ambiguous handoffs</strong>: passing an unstructured string between agents loses the data, the confidence level, and the intent behind the work.</li>
<li><strong>Competing outputs</strong>: parallel workers can produce conflicting results with no built-in resolution mechanism.</li>
<li><strong>Runaway autonomy</strong>: without explicit ownership, agents drift, duplicate work, or silently fail in ways no one notices.</li>
<li><strong>Latency and cost creep</strong>: coordination overhead can multiply runtime cost by 2–3x when the handoff format is wasteful.</li>
</ul>
<p>The design response is a principle worth writing down: <strong>each agent owns one outcome, and agents communicate through structured data, not freeform conversation.</strong> When an agent&rsquo;s output is a typed object — not prose — the next agent can act on it deterministically, and a human reviewer can audit the whole chain.</p>
<h2 id="five-core-collaboration-patterns">Five Core Collaboration Patterns</h2>
<p>Anthropic&rsquo;s widely cited December 2024 taxonomy gave the field its core vocabulary: <strong>chain, router, parallel, orchestrator-workers, and evaluator-optimizer</strong>. Modern practice extends this with a handful of additional arrangements. Here is how they map to real use cases.</p>
<table>
  <thead>
      <tr>
          <th>Pattern</th>
          <th>How it works</th>
          <th>Best for</th>
          <th>Realistic example</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>Chain</td>
          <td>Agent output feeds the next agent in sequence</td>
          <td>Linear transforms</td>
          <td>Summarize -&gt; translate -&gt; proofread</td>
      </tr>
      <tr>
          <td>Router</td>
          <td>A model classifies the input and routes to the right specialist</td>
          <td>Clear category boundaries</td>
          <td>Ticket -&gt; billing vs support vs refund</td>
      </tr>
      <tr>
          <td>Parallel</td>
          <td>Independent workers execute simultaneously on the same input</td>
          <td>Fanning heavy work out</td>
          <td>Research -&gt; drafting several sections</td>
      </tr>
      <tr>
          <td>Orchestrator-Worker</td>
          <td>Thin orchestrator plans and delegates to specialized workers</td>
          <td>Complex, decomposable tasks</td>
          <td>Content pipeline: research -&gt; write -&gt; SEO -&gt; publish</td>
      </tr>
      <tr>
          <td>Evaluator-Optimizer</td>
          <td>A generator and a critic iterate until the critic passes</td>
          <td>Work that needs self-correction</td>
          <td>Code generation -&gt; test harness -&gt; fix loop</td>
      </tr>
      <tr>
          <td>Blackboard</td>
          <td>Agents share a central store and react to posted updates</td>
          <td>Dynamic, loosely coupled work</td>
          <td>Design sprints, iterative R&amp;D</td>
      </tr>
      <tr>
          <td>Handoff</td>
          <td>An agent actively transfers control and context to the next agent</td>
          <td>Conversational or sequential ownership</td>
          <td>Human handoff in support flows</td>
      </tr>
  </tbody>
</table>
<p>The orchestrator-worker pattern is by far the most used in production. The orchestrator&rsquo;s only &ldquo;tools&rdquo; are the other agents. It does not do the work; it decides what needs doing, hands each piece to the right specialist, and synthesizes the results.</p>
<h2 id="designing-agent-teams-roles-ownership-and-handoffs">Designing Agent Teams: Roles, Ownership, and Handoffs</h2>
<p>The single most useful rule for designing an agent team is: <strong>start with one agent, then split only when it struggles.</strong> Bytemind AI&rsquo;s 2026 study puts it plainly — split when an agent loses context, reaches for the wrong tools, or is too slow. Do not design a five-agent system on day one because it is elegant; design it because the evidence says one agent cannot do the job.</p>
<h3 id="natural-fault-lines-for-splitting">Natural fault lines for splitting</h3>
<ul>
<li><strong>By context</strong>: when one agent&rsquo;s context window can no longer hold the full task, split along the boundaries of what each agent needs to know.</li>
<li><strong>By tools</strong>: when different stages need disjoint tools and credentials, each specialist gets its own, minimizing blast radius.</li>
<li><strong>By domain</strong>: separate concerns such as research, writing, and compliance so each agent develops deep, predictable competence.</li>
</ul>
<p>The Architect-Executor-Reviewer loop is the most common concrete pattern in coding workflows: an architect plans, an executor implements, and a reviewer validates before anything is committed. Ralph Loop extends this into a stateless-but-iterative <code>Pick -&gt; Implement -&gt; Validate -&gt; Commit -&gt; Reset</code> cycle that keeps parallel workers from stepping on each other. In practice, <strong>worktree isolation</strong> prevents merge conflicts when multiple agents work on the same repository simultaneously.</p>
<h2 id="orchestrator-worker-vs-handoff-choosing-the-right-pattern">Orchestrator-Worker vs Handoff: Choosing the Right Pattern</h2>
<p>Orchestrator-worker and handoff are frequently confused because both involve multiple agents. The distinction is who holds control.</p>
<table>
  <thead>
      <tr>
          <th>Dimension</th>
          <th>Orchestrator-Worker</th>
          <th>Handoff</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>Control model</td>
          <td>Central orchestrator plans and delegates</td>
          <td>Control moves from agent to agent</td>
      </tr>
      <tr>
          <td>Who holds state</td>
          <td>The orchestrator holds the plan</td>
          <td>The active agent holds the conversation</td>
      </tr>
      <tr>
          <td>Failure recovery</td>
          <td>Orchestrator can re-route to another worker</td>
          <td>Harder — the failing agent may be mid-flow</td>
      </tr>
      <tr>
          <td>Best for</td>
          <td>Batch processing, pipeline work, clear subtasks</td>
          <td>Conversational flows, sequential ownership</td>
      </tr>
      <tr>
          <td>Latency</td>
          <td>Parallelization possible</td>
          <td>Strictly sequential</td>
      </tr>
  </tbody>
</table>
<p>Choose orchestrator-worker when the task decomposes into parallelizable subtasks with a known plan. Choose handoff when the work is a continuum where the next stage genuinely depends on the previous agent&rsquo;s live state — for example, a support flow that escales from a triage bot to a human. For most content, research, and coding pipelines, orchestrator-worker wins because it parallelizes and lets you scale workers horizontally.</p>
<h2 id="communication-as-data-not-conversation">Communication as Data, Not Conversation</h2>
<p>The biggest single mistake teams make is letting agents talk to each other like people. Freeform conversational passing between agents is slow, hard to audit, and burns tokens fast. The production-grade alternative is a <strong>structured handoff object</strong> that travels with the work.</p>
<p>A well-formed handoff carries at least four fields:</p>
<ul>
<li><strong>Summary</strong> — a compact description of what was accomplished.</li>
<li><strong>Data</strong> — the typed results the next agent needs.</li>
<li><strong>Confidence</strong> — how certain the sender is in the output.</li>
<li><strong>Needs review</strong> — a flag marking whether a human gate is required.</li>
</ul>
<p>This is the difference between handing a colleague a paragraph and handing them a schema-validated JSON contract. Structured data lets downstream agents act deterministically, lets you log and replay the full pipeline, and lets a reviewer audit each transition instead of reading a sprawling conversation.</p>
<h2 id="governance-and-human-on-the-loop-trusting-agent-teams">Governance and Human-on-the-Loop: Trusting Agent Teams</h2>
<p>Full autonomy is a demo artifact, not a production default. Halkwinds Research found that <strong>67% of production agent deployments include a mandatory human review gate</strong>. Human-on-the-loop — a human overseeing the loop rather than sitting inside it — is the enterprise standard, and you should treat it as such regardless of how confident your agents appear.</p>
<p>Human review belongs at three positions:</p>
<ul>
<li><strong>Pre-execution</strong>: approve the plan before any agent touches real data.</li>
<li><strong>Post-execution blocking</strong>: approve the output before it ships.</li>
<li><strong>By-exception</strong>: only intervene when a confidence threshold drops or a flag fires.</li>
</ul>
<p>Security is the other half of governance. Halkwinds reports that <strong>41% of enterprise agent deployments experienced at least one prompt injection attempt in 2025</strong>. Every tool an agent can call is a potential attack surface. Isolate credentials per agent, validate handoffs against schemas, and treat agent output as untrusted input until a review gate clears it.</p>
<h2 id="how-to-start-from-one-agent-to-a-coordinated-workflow">How to Start: From One Agent to a Coordinated Workflow</h2>
<p>The fastest path to a production multi-agent workflow is not to architect it up front — it is to grow it.</p>
<h3 id="a-practical-progression">A practical progression</h3>
<ol>
<li><strong>Run one agent on the full task.</strong> Measure accuracy, cost, latency, and context pressure.</li>
<li><strong>Identify the failure point.</strong> Does the agent lose context? Reach for the wrong tools? Time out?</li>
<li><strong>Split exactly there.</strong> Introduce one new agent at the failure boundary, with a structured handoff between them.</li>
<li><strong>Add review.</strong> Insert the cheapest human gate that catches real errors.</li>
<li><strong>Scale workers.</strong> Once the pattern works, parallelize — add workers behind an orchestrator, not by redesigning the flow.</li>
</ol>
<p>This incremental path keeps you honest. It also matches the evidence: Databricks reports <strong>327% growth in multi-agent workflows in four months</strong>, and notes that today <strong>80% of databases are now built by AI agents</strong> — growth that came from teams shipping incrementally, not from grand designs.</p>
<h2 id="real-world-results-and-statistics">Real-World Results and Statistics</h2>
<p>The numbers behind agentic workflow collaboration make the case without rhetoric:</p>
<ul>
<li><strong>95.1%</strong> HumanEval for GPT-3.5 with an iterative agentic workflow vs <strong>67%</strong> for GPT-4 zero-shot (Andrew Ng, Agentic Design Patterns).</li>
<li><strong>6x</strong> more tasks per day for multi-agent architectures vs comparable single-agent systems (Halkwinds Research 2026).</li>
<li><strong>57%</strong> of organizations deploy agents for multi-stage workflows (Anthropic 2026 State of AI Agents).</li>
<li><strong>45%</strong> of enterprise teams have at least one autonomous agent in production, up from under 3% in 2024 (Halkwinds Research 2026).</li>
<li><strong>327%</strong> growth in multi-agent workflows in four months; <strong>80%</strong> of databases now built by AI agents (Databricks 2026).</li>
<li><strong>33%</strong> of enterprise software apps forecast to include agentic AI by 2028, up from under 1% in 2024 (Gartner).</li>
<li><strong>100%</strong> of surveyed enterprises plan to expand agentic AI in 2026; <strong>74%</strong> call production deployment critical (CrewAI 2026 survey of 500 C-level executives).</li>
<li><strong>31%</strong> average of workflows automated with agentic AI, with ~33% expansion expected in 2026 (CrewAI).</li>
<li><strong>67%</strong> of production deployments include a mandatory human review gate (Halkwinds 2026).</li>
<li><strong>41%</strong> of deployments reported at least one prompt injection attempt in 2025 (Halkwinds 2026).</li>
</ul>
<p>One case study is worth repeating: accuracy on a complex coordination task rose from 70% to 90% when a single agent was replaced by a coordinated team — at 2–3x the cost. That tradeoff is the real decision most teams face, and it is why the workflow choice — not model choice — dominates cost, latency, and reliability.</p>
<h2 id="key-takeaways">Key Takeaways</h2>
<ul>
<li><strong>Coordination, not model capability, is the bottleneck</strong> in production multi-agent systems.</li>
<li><strong>Each agent owns one outcome</strong>; orchestrators plan and delegate but never do the work themselves.</li>
<li><strong>Communication is data, not conversation</strong>: structured handoffs carrying summary, data, confidence, and a needs-review flag beat freeform text.</li>
<li><strong>Start with one agent and split only when it struggles</strong> — along context, tools, or domain — not by designing multi-agent up front.</li>
<li><strong>Human-on-the-loop is the production default</strong>: 67% of deployments gate agents behind a mandatory review.</li>
<li><strong>The workflow choice drives cost, latency, and reliability</strong> as much as the model choice — often more.</li>
</ul>
<p>Letting agents do agent work is not about removing humans. It is about getting the division of labor right: specialists do what they do best, an orchestrator keeps them aligned, and a human stays on the loop where it counts.</p>
<h2 id="faq">FAQ</h2>
<p><strong>What is the difference between an agentic workflow and a multi-agent workflow?</strong>
An agentic workflow is any workflow where an AI model dynamically decides its steps. A multi-agent workflow is a specific kind of agentic workflow where multiple specialized agents collaborate, typically coordinated by an orchestrator, handoffs, or a shared store.</p>
<p><strong>When should I use orchestrator-workers vs the handoff pattern?</strong>
Use orchestrator-workers when a task decomposes into parallelizable subtasks against a known plan — it parallelizes and scales horizontally. Use handoff when control must move sequentially and the next stage depends on the active agent&rsquo;s live state, such as escalating from a triage bot to a human.</p>
<p><strong>How do agents communicate in production multi-agent systems?</strong>
Through structured data, not freeform conversation. A structured handoff object carries a summary, the typed results, a confidence score, and a needs-review flag, which lets downstream agents act deterministically and lets humans audit each transition.</p>
<p><strong>Is full AI agent autonomy ready for production?</strong>
Not as a default. 67% of production deployments include a mandatory human review gate. Human-on-the-loop governance — review at pre-execution, post-execution, or by exception — is the enterprise standard, and it also mitigates prompt injection attacks, which 41% of deployments experienced in 2025.</p>
<p><strong>I&rsquo;m new to this. How do I start building an agent team?</strong>
Run a single agent on the full task first. Find where it fails — context loss, wrong tools, or latency — then split exactly at that boundary with a structured handoff. Add the cheapest human review gate, then scale workers behind an orchestrator only after the pattern works.</p>
]]></content:encoded></item></channel></rss>