<?xml version="1.0" encoding="utf-8" standalone="yes"?><rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom" xmlns:content="http://purl.org/rss/1.0/modules/content/"><channel><title>Windows Automation on RockB</title><link>https://baeseokjae.github.io/tags/windows-automation/</link><description>Recent content in Windows Automation on RockB</description><image><title>RockB</title><url>https://baeseokjae.github.io/images/og-default.png</url><link>https://baeseokjae.github.io/images/og-default.png</link></image><generator>Hugo</generator><language>en-us</language><lastBuildDate>Fri, 17 Jul 2026 10:02:55 +0000</lastBuildDate><atom:link href="https://baeseokjae.github.io/tags/windows-automation/index.xml" rel="self" type="application/rss+xml"/><item><title>Nova Deterministic AI Agent 2026: The Windows Desktop Agent That Doesn't Hallucinate</title><link>https://baeseokjae.github.io/posts/nova-deterministic-ai-agent-2026/</link><pubDate>Fri, 17 Jul 2026 10:02:55 +0000</pubDate><guid>https://baeseokjae.github.io/posts/nova-deterministic-ai-agent-2026/</guid><description>Nova is an open-source Windows AI agent that eliminates hallucinations through native UI Automation, sandboxed code execution, and human-in-the-loop confirmation.</description><content:encoded><![CDATA[<h2 id="what-is-nova--overview-of-the-deterministic-windows-ai-agent">What Is Nova? — Overview of the Deterministic Windows AI Agent</h2>
<p>Nova is an open-source Windows desktop AI agent launched on July 2, 2026, that solves the most persistent problem in AI automation: hallucinated actions. Unlike traditional AI agents that guess at screen coordinates and frequently click the wrong targets, Nova uses native Windows UI Automation (UIA) to interact with applications through their actual interface elements — buttons, menus, text fields, and dialog boxes — rather than blind pixel coordinates. This architectural choice, combined with a Python code sandbox, human-in-the-loop (HITL) confirmation for dangerous operations, and automatic file rollback from backups, makes Nova one of the most reliable Windows automation agents available in 2026.</p>
<h2 id="key-features--voice-control-ui-automation-file-management-browser-agent-memory-and-more">Key Features — Voice Control, UI Automation, File Management, Browser Agent, Memory, and More</h2>
<p>Nova packs a comprehensive feature set that rivals established desktop assistants with thousands of stars. Here is what it offers out of the box:</p>
<h3 id="voice-control-and-speech-recognition">Voice Control and Speech Recognition</h3>
<p>Nova supports both cloud-based and local speech-to-text through Groq API and Whisper.cpp, giving users flexibility depending on their privacy requirements and internet connectivity. Text-to-speech is handled through the Windows native TTS engine or cloud alternatives.</p>
<h3 id="native-windows-ui-automation">Native Windows UI Automation</h3>
<p>Instead of sending blind mouse clicks to fixed coordinates — the approach that causes most desktop agent failures — Nova uses Microsoft&rsquo;s UI Automation (UIA) framework to identify and interact with actual UI elements. This means it can find the &ldquo;Save&rdquo; button by its accessibility name, not by guessing pixel (1024, 768) might be the right spot.</p>
<h3 id="file-management-with-rollback">File Management with Rollback</h3>
<p>Every file operation Nova performs is backed up automatically. If the agent makes an unwanted change, users can roll back to the previous state — a safety net absent from virtually every competing Windows agent.</p>
<h3 id="browser-agent">Browser Agent</h3>
<p>Nova includes a browser automation module that can navigate web pages, fill forms, extract data, and perform research tasks. This works alongside the desktop UI automation for seamless cross-application workflows.</p>
<h3 id="persistent-memory-and-plan-management">Persistent Memory and Plan Management</h3>
<p>Nova uses SQLite for persistent storage of plans, conversation history, and application state. If the agent crashes or the system restarts, Nova can recover its in-progress tasks from the crash recovery database rather than starting from scratch.</p>
<h3 id="background-plan-execution">Background Plan Execution</h3>
<p>Users can queue multiple plans and let Nova execute them in the background while they continue working. The agent manages its own task queue and reports results as each plan completes.</p>
<h2 id="how-nova-achieves-deterministic-execution--native-uia-vs-blind-clicks-sandbox-hitl">How Nova Achieves Deterministic Execution — Native UIA vs Blind Clicks, Sandbox, HITL</h2>
<p>The term &ldquo;deterministic AI agent&rdquo; might sound like an oxymoron to anyone familiar with the probabilistic nature of large language models. Nova does not eliminate LLM randomness — it eliminates the <em>action</em> randomness that plagues coordinate-based agents.</p>
<h3 id="the-blind-click-problem">The Blind Click Problem</h3>
<p>Most desktop AI agents work by taking screenshots, asking an LLM to identify where to click, and then sending mouse events to those pixel coordinates. This approach fails when:</p>
<ul>
<li>The screen resolution changes</li>
<li>The window is resized or moved</li>
<li>A different theme or DPI scaling is applied</li>
<li>The application updates its layout</li>
<li>Multiple monitors are in use</li>
</ul>
<p>Each of these scenarios causes the agent to click the wrong location, producing a cascade of errors that the LLM cannot recover from without human intervention.</p>
<h3 id="novas-uia-solution">Nova&rsquo;s UIA Solution</h3>
<p>Nova bypasses the entire coordinate-guessing problem by using Windows UI Automation, the same framework that screen readers and accessibility tools use. UIA provides a tree of all interactive elements on screen with their roles, names, and states. Nova&rsquo;s executor queries this tree to find the exact element it needs — a button labeled &ldquo;Submit,&rdquo; a text field with the name &ldquo;Search,&rdquo; a checkbox with the state &ldquo;unchecked&rdquo; — and sends input directly to that element.</p>
<p>This approach is fundamentally more reliable because:</p>
<ul>
<li><strong>Element identity is stable</strong> — a button&rsquo;s accessibility name does not change when the window moves</li>
<li><strong>State is observable</strong> — Nova can check whether a button is enabled or disabled before clicking</li>
<li><strong>Feedback is structural</strong> — after an action, Nova can verify the UI state changed as expected</li>
</ul>
<h3 id="three-layer-safety-architecture">Three-Layer Safety Architecture</h3>
<p>Nova&rsquo;s deterministic guarantees rest on three layers:</p>
<table>
	<thead>
			<tr>
					<th>Layer</th>
					<th>Mechanism</th>
					<th>What It Prevents</th>
			</tr>
	</thead>
	<tbody>
			<tr>
					<td><strong>UI Automation</strong></td>
					<td>Native UIA element targeting</td>
					<td>Wrong-click hallucinations from coordinate guessing</td>
			</tr>
			<tr>
					<td><strong>Code Sandbox</strong></td>
					<td>Restricted Python execution environment</td>
					<td>Arbitrary system commands and file system damage</td>
			</tr>
			<tr>
					<td><strong>HITL Confirmation</strong></td>
					<td>User approval for dangerous operations</td>
					<td>Unauthorized file deletion, registry changes, or installs</td>
			</tr>
	</tbody>
</table>
<h2 id="architecture-deep-dive--agent-planner-executor-recovery-engine-security-sandbox-storage-layer">Architecture Deep Dive — Agent Planner, Executor, Recovery Engine, Security Sandbox, Storage Layer</h2>
<p>Nova&rsquo;s architecture is modular and well-documented, with over 220 automated tests covering each component.</p>
<h3 id="agent-planner">Agent Planner</h3>
<p>The Planner module receives a user&rsquo;s natural language request and breaks it down into a sequence of discrete steps. Each step is typed — UI action, file operation, code execution, or browser navigation — and validated before execution begins. The planner uses the LLM (via Groq or OpenRouter) for task decomposition but constrains the output to a structured plan format that the Executor can follow deterministically.</p>
<h3 id="executor">Executor</h3>
<p>The Executor is the heart of Nova&rsquo;s deterministic guarantee. It takes each planned step and executes it through the appropriate channel:</p>
<ul>
<li><strong>UI steps</strong> → Windows UIA API</li>
<li><strong>File steps</strong> → Sandboxed file system with automatic backup</li>
<li><strong>Code steps</strong> → Python sandbox with restricted imports and no network access</li>
<li><strong>Browser steps</strong> → Playwright-based browser automation</li>
</ul>
<p>Each step reports success or failure with structured feedback. If a step fails, the Executor can retry with adjusted parameters or escalate to the user for guidance.</p>
<h3 id="recovery-engine">Recovery Engine</h3>
<p>Nova&rsquo;s crash recovery system, documented in CRASH_RECOVERY.md, uses SQLite to persist the state of all active plans. On restart, Nova reads the recovery database and offers to resume interrupted plans. This is particularly valuable for long-running automation tasks that might take hours to complete.</p>
<h3 id="security-sandbox">Security Sandbox</h3>
<p>The sandbox module (<code>modules/security/sandbox.py</code>) restricts Python code execution to a safe subset of the language. Dangerous operations — file system writes outside allowed directories, network access, subprocess calls — are blocked by default. The sandbox also enforces system directory write protection, preventing Nova from modifying critical Windows system files.</p>
<h3 id="storage-layer">Storage Layer</h3>
<p>SQLite serves as Nova&rsquo;s persistent storage for conversation history, plan state, user preferences, and backup metadata. This lightweight approach keeps the agent self-contained with no external database dependencies.</p>
<h2 id="nova-vs-competitors--comparison-with-pygpt-skales-windows-computer-use-mcp-clawbridge-nemo-assistant">Nova vs Competitors — Comparison with PyGPT, Skales, Windows Computer Use MCP, Clawbridge, Nemo Assistant</h2>
<p>Nova enters a competitive landscape of desktop AI agents. Here is how it stacks up against the alternatives:</p>
<table>
	<thead>
			<tr>
					<th>Feature</th>
					<th>Nova</th>
					<th>PyGPT (1,851★)</th>
					<th>Skales (1,183★)</th>
					<th>Win Computer Use MCP (23★)</th>
					<th>Clawbridge (21★)</th>
					<th>Nemo Assistant (23★)</th>
			</tr>
	</thead>
	<tbody>
			<tr>
					<td><strong>Windows UIA</strong></td>
					<td>✅ Native</td>
					<td>❌ No</td>
					<td>❌ No</td>
					<td>✅ MCP-based</td>
					<td>❌ No</td>
					<td>❌ No</td>
			</tr>
			<tr>
					<td><strong>Deterministic Execution</strong></td>
					<td>✅ Yes</td>
					<td>❌ No</td>
					<td>❌ No</td>
					<td>❌ No</td>
					<td>❌ No</td>
					<td>❌ No</td>
			</tr>
			<tr>
					<td><strong>HITL Security</strong></td>
					<td>✅ Yes</td>
					<td>❌ No</td>
					<td>❌ No</td>
					<td>❌ No</td>
					<td>❌ No</td>
					<td>❌ No</td>
			</tr>
			<tr>
					<td><strong>File Rollback</strong></td>
					<td>✅ Yes</td>
					<td>❌ No</td>
					<td>❌ No</td>
					<td>❌ No</td>
					<td>❌ No</td>
					<td>❌ No</td>
			</tr>
			<tr>
					<td><strong>Code Sandbox</strong></td>
					<td>✅ Yes</td>
					<td>❌ No</td>
					<td>❌ No</td>
					<td>❌ No</td>
					<td>❌ No</td>
					<td>❌ No</td>
			</tr>
			<tr>
					<td><strong>Crash Recovery</strong></td>
					<td>✅ Yes</td>
					<td>❌ No</td>
					<td>❌ No</td>
					<td>❌ No</td>
					<td>❌ No</td>
					<td>❌ No</td>
			</tr>
			<tr>
					<td><strong>Voice Control</strong></td>
					<td>✅ Yes</td>
					<td>✅ Yes</td>
					<td>❌ No</td>
					<td>❌ No</td>
					<td>❌ No</td>
					<td>❌ No</td>
			</tr>
			<tr>
					<td><strong>Cross-Platform</strong></td>
					<td>❌ Windows only</td>
					<td>✅ All platforms</td>
					<td>✅ All platforms</td>
					<td>✅ Windows</td>
					<td>✅ Windows</td>
					<td>✅ Windows</td>
			</tr>
			<tr>
					<td><strong>GitHub Stars</strong></td>
					<td>7★</td>
					<td>1,851★</td>
					<td>1,183★</td>
					<td>23★</td>
					<td>21★</td>
					<td>23★</td>
			</tr>
			<tr>
					<td><strong>Release Date</strong></td>
					<td>July 2026</td>
					<td>Mature</td>
					<td>Mature</td>
					<td>2025</td>
					<td>2025</td>
					<td>2025</td>
			</tr>
	</tbody>
</table>
<h3 id="key-takeaways-from-the-comparison">Key Takeaways from the Comparison</h3>
<p><strong>PyGPT</strong> is the most feature-rich desktop AI assistant with support for GPT-5, GPT-4, o1, o3, Gemini, Claude, Ollama, DeepSeek, Perplexity, and Grok. It offers chat, vision, voice, RAG, and image generation. However, it lacks Windows-native UI automation entirely and has no deterministic execution guarantees. PyGPT is a conversation tool, not an automation agent.</p>
<p><strong>Skales</strong> is a cross-platform personal AI agent with goal-oriented autonomous task execution and a unique team mode for pairing two desktops. It supports Windows, macOS, Linux, Android, and iOS. But like PyGPT, it is not specifically deterministic or Windows-focused.</p>
<p><strong>Windows Computer Use MCP</strong> provides 22 MCP tools for click, type, screenshot, OCR, and UI inspection on Windows. It works with any MCP-compatible agent. However, it has no built-in deterministic guarantees or HITL confirmation — it is a tool library, not a complete agent.</p>
<p><strong>Clawbridge</strong> offers plain English commands, workflow recording, and bring-your-own-key (BYOK) for browser and desktop automation. It has no telemetry and no cloud server dependency. But it lacks deterministic execution and rollback support.</p>
<p><strong>Nemo Assistant</strong> is a local-first floating desktop AI assistant for Windows with selection actions, OCR, notes, and tool-calling. It is Windows-native but limited in scope with no deterministic execution or rollback features.</p>
<h2 id="security-model--code-sandbox-system-directory-protection-permission-confirmation-file-rollback">Security Model — Code Sandbox, System Directory Protection, Permission Confirmation, File Rollback</h2>
<p>Nova&rsquo;s security architecture is arguably its strongest differentiator. While most desktop agents operate with full system privileges and no oversight, Nova implements a defense-in-depth approach.</p>
<h3 id="python-code-sandbox">Python Code Sandbox</h3>
<p>When Nova executes Python code — whether from a user request or an automated plan — it runs inside a restricted sandbox that:</p>
<ul>
<li>Blocks imports of dangerous modules (<code>os</code>, <code>subprocess</code>, <code>shutil</code>, <code>ctypes</code>)</li>
<li>Disables network access</li>
<li>Limits file writes to designated directories</li>
<li>Enforces a maximum execution time</li>
<li>Logs all operations for audit</li>
</ul>
<h3 id="system-directory-write-protection">System Directory Write Protection</h3>
<p>Nova maintains a blocklist of protected system directories including <code>C:\Windows</code>, <code>C:\Program Files</code>, and registry paths. Any attempt to write to these locations is intercepted and either blocked or escalated for HITL approval.</p>
<h3 id="human-in-the-loop-confirmation">Human-in-the-Loop Confirmation</h3>
<p>For operations classified as dangerous — file deletion, software installation, registry modification, or execution of downloaded files — Nova pauses and presents the user with a clear description of what it is about to do. The user can approve, deny, or modify the operation. This confirmation is not a nuisance prompt for every action; Nova&rsquo;s risk classifier only triggers HITL for operations that could cause irreversible damage.</p>
<h3 id="automatic-file-rollback">Automatic File Rollback</h3>
<p>Before modifying any file, Nova creates a backup in its SQLite-backed storage. If the user decides the change was incorrect, they can instruct Nova to roll back to the pre-operation state. This is a safety net that no competing Windows agent offers.</p>
<h2 id="installation-and-setup--python-314-api-keys-dependencies-desktop-ui">Installation and Setup — Python 3.14, API Keys, Dependencies, Desktop UI</h2>
<p>Getting started with Nova requires Python 3.14 and a few API keys. The setup process is straightforward:</p>
<ol>
<li><strong>Clone the repository</strong>: <code>git clone https://github.com/KremlevLev/nova</code></li>
<li><strong>Install dependencies</strong>: <code>pip install -r requirements.txt</code></li>
<li><strong>Configure API keys</strong>: Edit <code>.env</code> with your Groq and/or OpenRouter API keys</li>
<li><strong>Launch the desktop UI</strong>: <code>python main.py</code></li>
</ol>
<p>Nova supports both cloud inference (Groq, OpenRouter) and local inference (Whisper.cpp for speech-to-text, llama.cpp for LLM). The local mode is particularly valuable for users who need offline operation or have strict data privacy requirements.</p>
<p>The desktop UI provides a floating interface similar to Nemo Assistant, with a chat input area, plan queue display, and status indicators for active tasks.</p>
<h2 id="real-world-use-cases--development-workflow-desktop-automation-voice-controlled-operations">Real-World Use Cases — Development Workflow, Desktop Automation, Voice-Controlled Operations</h2>
<h3 id="development-workflow-automation">Development Workflow Automation</h3>
<p>A developer can ask Nova to &ldquo;set up a new React project in the workspace folder, install dependencies, and open it in VS Code.&rdquo; Nova will create the directory, run <code>npx create-react-app</code>, install packages, and launch VS Code — all while backing up any files it modifies and asking for confirmation before touching existing code.</p>
<h3 id="desktop-automation">Desktop Automation</h3>
<p>For power users, Nova can automate repetitive Windows tasks: renaming batches of files, organizing downloads folders, filling out forms across multiple applications, extracting data from legacy software that has no API, and generating reports from multiple sources.</p>
<h3 id="voice-controlled-operations">Voice-Controlled Operations</h3>
<p>With the voice module enabled, users can control their Windows desktop entirely by voice. &ldquo;Nova, open Chrome, go to Gmail, and read me my latest emails&rdquo; becomes a single voice command that the agent executes through UIA-driven browser interaction.</p>
<h3 id="research-and-data-extraction">Research and Data Extraction</h3>
<p>Nova&rsquo;s browser agent can navigate to multiple websites, extract structured data, and compile it into a spreadsheet or document — all without the user touching the keyboard.</p>
<h2 id="limitations-and-early-stage-considerations--small-community-russian-first-documentation-windows-only">Limitations and Early-Stage Considerations — Small Community, Russian-First Documentation, Windows-Only</h2>
<p>Nova is a very early-stage project with significant limitations that potential users should consider:</p>
<h3 id="small-community-and-limited-track-record">Small Community and Limited Track Record</h3>
<p>With only 7 GitHub stars as of mid-July 2026, Nova has virtually no community support. There are no third-party plugins, no community-contributed workflows, and limited troubleshooting resources. The project was created on July 2, 2026 — it is less than three weeks old at the time of this review.</p>
<h3 id="russian-first-documentation">Russian-First Documentation</h3>
<p>The README and most documentation are written in Russian, which may be a barrier for English-speaking users. While the code comments and variable names use English, the primary user-facing documentation requires translation or Russian language proficiency.</p>
<h3 id="windows-only-limitation">Windows-Only Limitation</h3>
<p>Nova is exclusively a Windows agent. Users on macOS or Linux cannot use it. This is by design — Nova&rsquo;s core differentiator is Windows UIA integration — but it limits the addressable audience significantly.</p>
<h3 id="llm-dependency">LLM Dependency</h3>
<p>Despite its deterministic execution layer, Nova still depends on an LLM for task planning and natural language understanding. If the LLM misinterprets a request, the plan itself may be wrong, even if the execution is correct. The HITL layer catches dangerous operations, but subtle logical errors in plans may go undetected.</p>
<h3 id="no-published-roadmap">No Published Roadmap</h3>
<p>The repository does not include a public roadmap or contribution guidelines, making it difficult for potential contributors to understand the project&rsquo;s direction or how to get involved.</p>
<h2 id="verdict--who-should-use-nova-in-2026">Verdict — Who Should Use Nova in 2026?</h2>
<p>Nova is a promising but very early-stage project that addresses a genuine gap in the Windows AI agent ecosystem: reliable, deterministic desktop automation with enterprise-grade safety features.</p>
<p><strong>Use Nova if:</strong></p>
<ul>
<li>You need a Windows automation agent that does not click the wrong targets</li>
<li>You value safety features like HITL confirmation, code sandboxing, and file rollback</li>
<li>You are comfortable with Russian-language documentation</li>
<li>You want to contribute to an early-stage open-source project with a well-architected codebase</li>
<li>You need offline-capable AI desktop automation</li>
</ul>
<p><strong>Wait or look elsewhere if:</strong></p>
<ul>
<li>You need cross-platform support</li>
<li>You require a mature, battle-tested solution with community support</li>
<li>You do not read Russian and need English documentation</li>
<li>You need a conversational AI assistant rather than an automation agent (PyGPT is better suited)</li>
</ul>
<p>Nova&rsquo;s 220+ automated tests, modular architecture, and thoughtful security design suggest a project built with care, even if it is early. For Windows power users and developers who value deterministic execution and safety, Nova is worth watching — and for the adventurous, worth trying today.</p>
<h2 id="frequently-asked-questions">Frequently Asked Questions</h2>
<h3 id="what-makes-nova-different-from-other-windows-ai-agents">What makes Nova different from other Windows AI agents?</h3>
<p>Nova uses native Windows UI Automation (UIA) to interact with applications through their actual interface elements rather than guessing screen coordinates. This eliminates the most common source of desktop agent hallucinations. It also includes a Python code sandbox, HITL confirmation for dangerous operations, and automatic file rollback — features no competing Windows agent offers together.</p>
<h3 id="does-nova-require-an-internet-connection">Does Nova require an internet connection?</h3>
<p>No. Nova supports both cloud inference (Groq, OpenRouter) and local inference (Whisper.cpp for speech-to-text, llama.cpp for LLM). Users can run Nova entirely offline with local models, though cloud inference provides access to more capable LLMs.</p>
<h3 id="is-nova-safe-to-use-on-a-production-windows-machine">Is Nova safe to use on a production Windows machine?</h3>
<p>Nova includes multiple safety layers: a Python code sandbox that blocks dangerous operations, system directory write protection, HITL confirmation for destructive actions, and automatic file rollback from backups. These features make it safer than most desktop agents, but as an early-stage project with a small community, users should exercise caution and test in a non-production environment first.</p>
<h3 id="how-do-i-install-nova-on-windows">How do I install Nova on Windows?</h3>
<p>Clone the repository from GitHub, install Python 3.14 and the required dependencies, configure your API keys in the <code>.env</code> file, and run <code>python main.py</code>. The project requires Windows 10 or later and works with both cloud and local LLM providers.</p>
<h3 id="can-nova-automate-any-windows-application">Can Nova automate any Windows application?</h3>
<p>Nova can automate any application that exposes UI elements through Windows UI Automation. Most modern Windows applications (including Microsoft Office, Chrome, VS Code, and system dialogs) support UIA. Legacy applications that render custom-drawn interfaces without accessibility support may not work reliably with Nova&rsquo;s UIA-based approach.</p>
]]></content:encoded></item></channel></rss>