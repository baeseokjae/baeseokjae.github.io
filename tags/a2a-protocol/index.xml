<?xml version="1.0" encoding="utf-8" standalone="yes"?><rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom" xmlns:content="http://purl.org/rss/1.0/modules/content/"><channel><title>A2A Protocol on RockB</title><link>https://baeseokjae.github.io/tags/a2a-protocol/</link><description>Recent content in A2A Protocol on RockB</description><image><title>RockB</title><url>https://baeseokjae.github.io/images/og-default.png</url><link>https://baeseokjae.github.io/images/og-default.png</link></image><generator>Hugo</generator><language>en-us</language><lastBuildDate>Thu, 20 Aug 2026 19:01:29 +0000</lastBuildDate><atom:link href="https://baeseokjae.github.io/tags/a2a-protocol/index.xml" rel="self" type="application/rss+xml"/><item><title>OpenAgent Compat Lab: Deterministic Compatibility for Agent Endpoints</title><link>https://baeseokjae.github.io/posts/open-agent-compat-lab-agent-endpoint-compatibility/</link><pubDate>Thu, 20 Aug 2026 19:01:29 +0000</pubDate><guid>https://baeseokjae.github.io/posts/open-agent-compat-lab-agent-endpoint-compatibility/</guid><description>OpenAgent Compat Lab tests agent endpoints deterministically — schema validation, JSON-RPC conformance, and auth checks that prove agents actually interoperate.</description><content:encoded><![CDATA[<p>An OpenAgent Compat Lab is a deterministic testing environment that verifies an agent endpoint actually conforms to a protocol contract — running real JSON-RPC calls, validating the response against a published schema, and checking authentication before an agent is ever trusted to interoperate. It answers the question &ldquo;can these two agents talk&rdquo; with evidence rather than a handshake. As platforms like A2Apex score agents on a 0–100 trust scale using live endpoint tests, and as the A2A protocol gathers more than 50 launch partners, deterministic compatibility testing has become the missing trust layer for agent interoperability.</p>
<h2 id="why-agent-interoperability-is-still-the-blocker">Why agent interoperability is still the blocker</h2>
<p>Agents are proliferating across the enterprise, but they do not reliably work together. The problem is not that agents cannot connect — it is that nobody can prove they will work correctly before integration. This is the difference between &ldquo;they can talk&rdquo; and &ldquo;they can be proven to talk.&rdquo;</p>
<p>Interoperability promises are routinely made on paper and broken in practice. An agent advertises a capability, and the consuming application discovers mid-integration that the endpoint returns a malformed response, drops an optional field the client depends on, or rejects a valid authentication scheme. These failures are expensive precisely because they surface late, after code has been written against an assumed contract.</p>
<p>The industry response has been a rush of specifications — MCP for tools, A2A for communication, ACP for editors, and OpenAgent for definition. But a spec is only a document. Deterministic compatibility testing is what turns a spec from a set of suggestions into a measurable, verifiable guarantee.</p>
<h2 id="the-compatibility-stack-mcp-a2a-acp-and-openspec-defined">The compatibility stack: MCP, A2A, ACP, and OpenSpec defined</h2>
<p>To understand where a Compat Lab fits, it helps to separate the four layers that define modern agent interoperability. Each answers a different question.</p>
<table>
  <thead>
      <tr>
          <th>Layer</th>
          <th>Protocol</th>
          <th>Question it answers</th>
          <th>Primary audience</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>Tools &amp; context</td>
          <td>Model Context Protocol (MCP)</td>
          <td>What tools and context can the agent access?</td>
          <td>Agent developers</td>
      </tr>
      <tr>
          <td>Agent communication</td>
          <td>Agent2Agent (A2A)</td>
          <td>How do agents talk to each other?</td>
          <td>Enterprise integrators</td>
      </tr>
      <tr>
          <td>Editor integration</td>
          <td>Agent Client Protocol (ACP)</td>
          <td>How does an agent work inside an IDE?</td>
          <td>End-user developers</td>
      </tr>
      <tr>
          <td>Agent definition</td>
          <td>OpenAgent</td>
          <td>What is the agent, in a portable form?</td>
          <td>Platform builders</td>
      </tr>
  </tbody>
</table>
<p>MCP, launched by Anthropic, gives agents access to tools and context. A2A, launched by Google in April 2025 with more than 50 technology partners including Atlassian, Box, Cohere, Intuit, LangChain, MongoDB, PayPal, Salesforce, SAP, ServiceNow, UKG, and Workday, defines how agents communicate. The two are complementary: MCP feeds an agent, A2A lets agents transact.</p>
<p>ACP, adopted jointly by JetBrains and Zed, is a client protocol that lets AI coding agents work inside any editor regardless of vendor, with the IDE mediating access to files, terminals, and tools. The OpenAgent specification takes a different angle entirely, treating agents like typed functions with a deterministic, verifiable view of behavior and interfaces rather than opaque prompt chains.</p>
<p>None of these protocols, on their own, guarantees compatibility. That is the job of the compat lab.</p>
<h2 id="what-is-a-deterministic-first-compat-lab-for-agent-endpoints">What is a deterministic-first Compat Lab for agent endpoints?</h2>
<p>A Compat Lab is a controlled environment where an agent&rsquo;s endpoint is exercised against a formal conformance target. &ldquo;Deterministic&rdquo; is the operative word: the test harness runs the same well-defined request sequence and checks the response against a fixed schema, so the outcome is reproducible rather than anecdotal.</p>
<p>Deterministic testing stands in contrast to &ldquo;it worked in a demo.&rdquo; A demo proves that a specific conversation happened once. A deterministic test proves that any compliant client can expect the same structured behavior. That distinction matters for trust: enterprises cannot scale integration on anecdotes.</p>
<p>The lab model typically works in four phases:</p>
<ol>
<li>Load the agent card or endpoint definition.</li>
<li>Validate the card&rsquo;s schema compliance before deployment.</li>
<li>Run live JSON-RPC test calls against the real endpoint.</li>
<li>Score the results and issue a conformance badge.</li>
</ol>
<p>This converts compatibility from an assertion to a measurement. The agent either passes the endpoint test or it does not.</p>
<h2 id="endpoint-conformance-testing-schema-json-rpc-validation-and-auth-checks">Endpoint conformance testing: schema, JSON-RPC validation, and auth checks</h2>
<p>Conformance testing runs an endpoint through a battery of deterministic checks. The three most important are schema validation, request-response validation, and authentication checks.</p>
<p>Schema validation confirms the agent card conforms to the published specification. In the agent definition (A2A, OpenAgent), fields such as identity, capabilities, tools, knowledge sources, behavior, interaction model, and performance criteria must be present and correctly typed. A platform such as A2Apex performs full schema compliance checking against the spec before deployment, so an agent cannot even reach the directory if its card is malformed.</p>
<p>JSON-RPC validation is the live test. The harness sends real protocol calls to the endpoint and validates the structure of each response. It catches malformed responses, wrong JSON-RPC version handling, missing error objects, and payloads that do not match the negotiated method. This is where deterministic endpoint testing earns its keep: every call is reproducible, and every failure is attributable.</p>
<p>Authentication checks validate that the endpoint correctly implements its declared security scheme. Whether the agent uses API keys, OAuth, or JWT, the test harness confirms the scheme is actually enforced — and that a correctly credentialed request is accepted while an uncredentialed one is rejected. The auth scheme is part of the contract, not an afterthought.</p>
<h2 id="certification-and-trust-scores-from-directory-to-verified-directory">Certification and trust scores: from directory to verified directory</h2>
<p>A directory that merely lists agents tells you nothing about quality. A verified directory tells you which agents have passed a reproducible endpoint test. This shift — from an agent directory to a verified agent directory — is one of the clearest trends in the space.</p>
<p>Trust scoring is the operationalization of this idea. Platforms such as A2Apex assign a 0–100 trust score and tier it into Gold, Silver, and Bronze badges. The score aggregates live endpoint test results, schema compliance, and security checks into a single number a purchasing or integration team can act on.</p>
<table>
  <thead>
      <tr>
          <th>Badge</th>
          <th>Implied confidence</th>
          <th>What it means in practice</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>Gold</td>
          <td>Highest trust</td>
          <td>Passed all endpoint tests, full schema + security conformance</td>
      </tr>
      <tr>
          <td>Silver</td>
          <td>Strong trust</td>
          <td>Passed core tests with minor gaps</td>
      </tr>
      <tr>
          <td>Bronze</td>
          <td>Basic trust</td>
          <td>Endpoint responds and conforms to the card schema</td>
      </tr>
  </tbody>
</table>
<p>These badges are embeddable, turning a verified agent directory into a discoverable, trustworthy surface. For buyers, the score compresses a complex compatibility evaluation into one decision datum.</p>
<h2 id="enterprise-benefits-lower-integration-cost-no-vendor-lock-in">Enterprise benefits: lower integration cost, no vendor lock-in</h2>
<p>The economic case for deterministic compatibility testing is straightforward. Interoperability lowers integration cost because a verified endpoint can be onboarded without re-doing discovery and contract verification every time. Compliance goes down, reuse goes up.</p>
<p>Second, deterministic testing reduces vendor lock-in. When an agent conforms to a published contract and has passed an endpoint test, the consuming side is no longer coupled to a single vendor&rsquo;s implementation. It can swap implementations that meet the same contract. This compounds the reuse across an enterprise estate: once an agent is certified, it can be reused by any other agent that consumes the same contract.</p>
<h2 id="limitations-and-open-questions">Limitations and open questions</h2>
<p>Deterministic compatibility testing does not solve everything. There are open questions:</p>
<ul>
<li>A passing endpoint test proves the happy path and the tested edge cases, not the full behavior of a live agent at scale.</li>
<li>Trust scores are only as good as the test coverage behind them; a sparse test suite can produce a high score that overstates reality.</li>
<li>The specification layer is still fragmented across A2A, MCP, ACP, and OpenAgent, and a compat lab that tests one protocol does not automatically certify against another.</li>
<li>Model-driven behavior is inherently probabilistic in responses, so deterministic schema conformance does not guarantee deterministic semantic output.</li>
<li>Certification creates a new dependency on the certifier, who must be trusted to update tests as the protocol evolves.</li>
</ul>
<h2 id="conclusion-the-path-to-a-trustworthy-interoperable-agent-ecosystem">Conclusion: the path to a trustworthy, interoperable agent ecosystem</h2>
<p>Agent interoperability will not scale on good intentions. The specification stack — MCP for tools, A2A for communication, ACP for editors, OpenAgent for definition — provides the contracts, but contracts only matter when they can be enforced. An OpenAgent Compat Lab enforces them: it loads the agent card, validates the schema, runs real JSON-RPC endpoint calls, checks authentication, and produces a reproducible, trust-scored result.</p>
<p>Moving from &ldquo;agents can talk&rdquo; to &ldquo;agents can be proven to talk&rdquo; is the trust layer that enterprises need before they invest in multi-agent systems at scale. With more than 50 partners behind A2A, deterministic compatibility testing from platforms that certify verified agents is the practical on-ramp. The agent economy will be built on verified endpoints, not promises — and the compat lab is where that verification happens.</p>
<h2 id="faq">FAQ</h2>
<h3 id="what-is-an-openagent-compat-lab">What is an OpenAgent Compat Lab?</h3>
<p>It is a deterministic testing environment that verifies an agent endpoint conforms to a protocol contract by running live JSON-RPC calls, validating the response against a schema, and checking authentication before an agent is trusted or certified.</p>
<h3 id="how-does-deterministic-agent-testing-work">How does deterministic agent testing work?</h3>
<p>A harness loads the agent&rsquo;s definition, validates its schema, sends the same reproducible JSON-RPC request sequence against the real endpoint, and scores the response against the protocol specification. The same input produces the same verifiable outcome.</p>
<h3 id="what-is-the-difference-between-mcp-a2a-and-acp">What is the difference between MCP, A2A, and ACP?</h3>
<p>MCP supplies tools and context to a single agent, A2A defines how separate agents communicate, and ACP lets AI coding agents run inside an editor independent of vendor. They are complementary layers of the interoperability stack.</p>
<h3 id="what-is-an-agent-trust-score">What is an agent trust score?</h3>
<p>It is a 0–100 score computed from deterministic endpoint tests, schema compliance, and security checks, usually tiered into Gold, Silver, and Bronze badges to signal which agents have been verified as interoperable.</p>
<h3 id="why-is-schema-validation-important-for-agent-endpoints">Why is schema validation important for agent endpoints?</h3>
<p>A malformed or non-compliant agent card breaks integration even before runtime. Validating the card against the published schema before deployment catches contract violations early and makes endpoint testing more reliable.</p>
]]></content:encoded></item><item><title>Your Agents Should Be Multiplayer: Collaborative AI Workflows (2026)</title><link>https://baeseokjae.github.io/posts/multiplayer-agents-2026/</link><pubDate>Tue, 14 Jul 2026 10:00:00 +0000</pubDate><guid>https://baeseokjae.github.io/posts/multiplayer-agents-2026/</guid><description>Single-agent AI is hitting a wall. Here&amp;#39;s why the next frontier is agent teams — and the protocols, tools, and patterns that make multiplayer AI workflows work in production.</description><content:encoded><![CDATA[<p>I&rsquo;ve been running production AI agent systems for over a year now, and the single biggest shift I&rsquo;ve seen in 2026 is this: the best agents don&rsquo;t work alone. The teams getting real leverage out of AI aren&rsquo;t the ones with one super-agent — they&rsquo;re the ones running five, ten, or twenty specialized agents that talk to each other.</p>
<p>This isn&rsquo;t a prediction. It&rsquo;s already happening. Meta&rsquo;s HyperAgents paper (arXiv:2603.19461) proved that multi-agent systems can solve problems no single agent can touch. A production field study from Calx showed six agents building 82,000 lines of code in 20 days for $250. And the infrastructure to make this work — protocols, SDKs, open-source orchestrators — is already here, just not widely adopted yet.</p>
<p>Let me walk through what &ldquo;multiplayer AI&rdquo; actually means in practice, the stack that makes it work, and the hard lessons I&rsquo;ve learned about where it breaks.</p>
<h2 id="the-multiplayer-paradigm-shift">The Multiplayer Paradigm Shift</h2>
<p>Think about how you actually build software. You don&rsquo;t have one developer do everything. You have a frontend person, a backend person, someone on DevOps, a reviewer. They talk in Slack, leave PR comments, sync on architecture decisions. The whole is smarter than any individual.</p>
<p>AI agents work the same way — or at least, they should. A single agent has a context window, a tool set, and a personality (its system prompt). Ask it to do everything and you get a jack-of-all-trades that&rsquo;s mediocre at each task. Specialize your agents — one for research, one for code generation, one for review, one for deployment — and suddenly each one can be excellent at its job.</p>
<p>The gaming analogy is apt. We went from single-player (one agent, one session) to local multiplayer (multiple agents on the same machine) to online multiplayer (agents across machines, communicating over protocols). Most teams are still in the single-player era. The ones who&rsquo;ve moved to multiplayer are seeing 3-4x throughput improvements.</p>
<h2 id="the-emerging-protocol-stack-mcp--a2a">The Emerging Protocol Stack: MCP + A2A</h2>
<p>The biggest question when I started building multi-agent systems was: how do they talk to each other? The answer in 2026 is a two-protocol stack that&rsquo;s rapidly becoming standard.</p>
<p><strong>MCP (Model Context Protocol)</strong> — Anthropic&rsquo;s protocol for agents to access tools. Your agent talks to a database, a file system, an API. MCP is the &ldquo;how do I interact with the world&rdquo; layer.</p>
<p><strong>A2A (Agent-to-Agent Protocol)</strong> — Google&rsquo;s protocol, donated to the Linux Foundation in June 2025, for agents to talk to other agents. A2A is the &ldquo;how do I coordinate with my peers&rdquo; layer.</p>
<p>I&rsquo;ve written a <a href="/posts/mcp-vs-a2a-protocol-2026/">detailed comparison of MCP vs A2A</a> if you want the full technical breakdown, but the short version is: you need both. MCP without A2A means your agent can use tools but can&rsquo;t delegate work. A2A without MCP means your agents can talk but can&rsquo;t do anything useful.</p>
<p>In production, I run agents that expose both interfaces. A research agent exposes an A2A endpoint that other agents call with &ldquo;research this topic and return a brief.&rdquo; Internally, that agent uses MCP to query web search, read documentation, and write files. The calling agent never needs to know about those tools — it just sends an A2A message and gets a result back.</p>
<h2 id="what-hyperagents-and-the-calx-study-actually-tell-us">What HyperAgents and the Calx Study Actually Tell Us</h2>
<p>Meta&rsquo;s HyperAgents paper dropped in March 2026 and it&rsquo;s one of those papers that changes how you think about agent architecture. The headline finding: on math transfer tasks, HyperAgents achieved imp@50 = 0.630 where traditional transfer methods scored 0.0. That&rsquo;s not an incremental improvement — it&rsquo;s a phase change.</p>
<p>The key insight is that <strong>documentation is not behavior</strong>. You can write the most detailed system prompt in the world, but an agent won&rsquo;t actually follow it the way you expect. Meta found that executable mechanisms — actual code, rules enforced at runtime, behavioral guardrails — transfer agent improvement across domains. Written rules don&rsquo;t.</p>
<p>The Calx field study drives this home with real numbers. A team ran six AI agents that built 82,000 lines of code in 20 days for $250. Impressive, right? But when they transferred 237 correction rules to a new agent, it still made 44 new mistakes — 13 of which were in categories the rules explicitly covered. The rules were documented. The agent just didn&rsquo;t follow them.</p>
<p>This is the single most important lesson for multi-agent systems: <strong>don&rsquo;t trust prompts to enforce behavior</strong>. If you want an agent to follow a rule, make it executable. Use runtime validation, output guards, and tool-level constraints. A prompt is a suggestion. A code check is a guarantee.</p>
<h2 id="claude-codes-hidden-multi-agent-infrastructure">Claude Code&rsquo;s Hidden Multi-Agent Infrastructure</h2>
<p>Here&rsquo;s something that surprised me: Claude Code&rsquo;s agent SDK already ships with full multi-agent coordination built in. I&rsquo;m talking about in-process agent runners, mailbox messaging between agents, team-based architecture with team leads, and shutdown coordination. It&rsquo;s all there in <code>@anthropic-ai/claude-agent-sdk</code>.</p>
<p>Most people use Claude Code as a single-agent REPL. You type a prompt, it does a thing. But the SDK supports spawning child agents, routing messages between them, and coordinating work across a team of agents. The infrastructure for multiplayer agents is already in your node_modules — most people just haven&rsquo;t wired it up.</p>
<p>I&rsquo;ve been running a setup where a &ldquo;team lead&rdquo; agent receives a task, breaks it into subtasks, and dispatches each one to a specialized child agent. The research agent gathers context, the coding agent writes code, the review agent checks for issues, and the documentation agent writes the docs. They communicate through the SDK&rsquo;s mailbox system. The team lead handles conflicts and merges results.</p>
<p>If you&rsquo;re already using Claude Code, you&rsquo;re one config file away from a multi-agent setup. The SDK documentation covers the <code>AgentTeam</code>, <code>AgentRunner</code>, and <code>Mailbox</code> classes. Start there.</p>
<h2 id="the-open-source-ecosystem">The Open-Source Ecosystem</h2>
<p>The protocol stack and commercial SDKs are one path. But the open-source ecosystem is where the real experimentation is happening, and it&rsquo;s moving fast.</p>
<p><strong>OpenSwarm</strong> is an open-source multi-agent Claude CLI orchestrator that coordinates multiple Claude Code agents as a team with task distribution. It hit 34 points on Hacker News — strong community interest in this space. It&rsquo;s designed for Linear/GitHub workflows, which makes sense: those are the most common multi-agent use cases right now.</p>
<p><strong>Egregore</strong> tackles the isolation problem head-on. Every Claude Code session starts from scratch — no memory of what happened before. Egregore provides shared memory and coordination for multiplayer Claude Code sessions. If you&rsquo;ve ever had an agent redo work because it didn&rsquo;t remember what another agent already did, you know exactly why this matters.</p>
<p><strong>Muster</strong> takes a different approach entirely. It&rsquo;s a multi-agent product team built on markdown files. The coordination layer is literally <code>.md</code> files — agents write plans, specs, and status updates to markdown, and other agents read them. It&rsquo;s lightweight, zero-infrastructure, and surprisingly effective. I&rsquo;ve used this pattern myself for quick experiments before investing in a full protocol stack.</p>
<p>I covered the major frameworks in my <a href="/posts/multi-agent-framework-comparison-2026/">multi-agent framework comparison</a>, but the open-source tools above are worth watching because they&rsquo;re solving real pain points that the big frameworks don&rsquo;t address.</p>
<h2 id="real-world-use-cases">Real-World Use Cases</h2>
<p>Let me give you three patterns I&rsquo;ve seen work in production.</p>
<p><strong>Code review pipelines.</strong> One agent writes code, another reviews it. The reviewer agent has a different system prompt, different tools (security scanner, linter, style checker), and a different temperature setting. It catches things the writer agent misses because it&rsquo;s looking at the code from a different angle. I&rsquo;ve seen this catch subtle race conditions and security issues that a single agent would have missed.</p>
<p><strong>Document collaboration.</strong> Platforms like odocs are pioneering multiplayer doc editing where humans and AI agents edit together in real-time. Partial document updates preserve version history without full rewrites — a critical feature when you have multiple agents touching the same document. Agents can share context directly to other agents through the platform, which solves the &ldquo;I need to know what agent B already figured out&rdquo; problem.</p>
<p><strong>Research-to-code pipelines.</strong> A research agent gathers context, a planning agent creates a spec, a coding agent implements it, and a testing agent validates it. Each agent passes artifacts to the next through A2A messages or shared files. I run this exact pipeline for my blog&rsquo;s content production — a strategist agent identifies topics, a researcher gathers data, a writer drafts the post, and a publisher deploys it. Each agent is specialized and excellent at its one job.</p>
<h2 id="the-trust-layer-problem">The Trust Layer Problem</h2>
<p>Here&rsquo;s the hard truth: multi-agent systems introduce trust problems that single-agent systems don&rsquo;t have.</p>
<p>When you have one agent, you review its output. When you have ten agents, you can&rsquo;t review everything. You need automated trust mechanisms.</p>
<p>The Calx approach — tracking corrections and compiling them into enforced rules — is one solution. Every time a human corrects an agent&rsquo;s output, that correction becomes a rule that all agents in the system must follow. It&rsquo;s executable, not documented. It&rsquo;s enforced at the tool level, not the prompt level.</p>
<p>Another approach is the behavioral guardrail pattern. Each agent has a &ldquo;guard&rdquo; that validates its output before passing it to the next agent. The guard checks for policy violations, security issues, and format compliance. If the output fails, the agent retries with the guard&rsquo;s feedback. This is the pattern I use in production, and it catches about 95% of issues before they propagate.</p>
<p>The remaining 5% require human review. The trick is knowing which 5% — and that&rsquo;s where correction tracking becomes essential. Log every agent output, every human correction, and use that data to improve your guards.</p>
<h2 id="the-road-ahead">The Road Ahead</h2>
<p>Looking at 2027, I see three trends that will define multiplayer AI workflows.</p>
<p>First, <strong>protocol consolidation</strong>. MCP and A2A are winning, but there&rsquo;s still fragmentation in how agents discover each other, authenticate, and negotiate capabilities. Expect a registry layer — something like a DNS for agents — to emerge.</p>
<p>Second, <strong>behavioral guardrails become standard</strong>. The HyperAgents insight — documentation is not behavior — will drive a new category of tools that enforce agent behavior at runtime. Every production multi-agent system will have a guard layer.</p>
<p>Third, <strong>the cost curve flips</strong>. Right now, running multiple agents costs more than running one. But as models get cheaper and specialized small models outperform general large models on specific tasks, running a team of specialized agents will be cheaper than one generalist agent. The Calx study already hints at this: $250 for 82,000 lines of code across six agents. That math gets better every quarter.</p>
<p>If you&rsquo;re still running single-agent workflows, start experimenting with multiplayer patterns today. Wire up two agents that talk to each other. Add a reviewer agent to your code generation pipeline. Try the Claude Code SDK&rsquo;s team features. The infrastructure is ready. The protocols are stable. The only thing missing is your architecture.</p>
<p>For more context on how to set up parallel agent workflows, check out my <a href="/posts/multi-agent-coding-workflow-2026/">multi-agent coding workflow guide</a> — it covers the practical setup patterns I use daily.</p>
]]></content:encoded></item><item><title>Google ADK A2A Protocol Guide for Cross-Framework Agent Interoperability</title><link>https://baeseokjae.github.io/posts/google-adk-a2a-protocol-guide-2026/</link><pubDate>Fri, 12 Jun 2026 07:51:58 +0000</pubDate><guid>https://baeseokjae.github.io/posts/google-adk-a2a-protocol-guide-2026/</guid><description>A practical Google ADK A2A protocol guide for building interoperable agents across frameworks, clouds, and teams.</description><content:encoded><![CDATA[<p>The google adk a2a protocol pairing gives developers a practical way to build agents in Google ADK while exposing them through the open Agent2Agent protocol. Use ADK for agent logic, workflows, tools, and state; use A2A when those agents need to collaborate across frameworks, clouds, services, or organizational boundaries.</p>
<h2 id="what-do-google-adk-and-a2a-solve-together">What Do Google ADK and A2A Solve Together?</h2>
<p>Google ADK and A2A solve different parts of the same multi-agent system: ADK builds and runs the agent, while A2A lets that agent communicate with other agents through a shared protocol. Google announced ADK Python v1.0.0 as production-ready at Google I/O 2025, and ADK Python v2.2.0 was the latest release in the research brief dated June 12, 2026. A2A moved from a Google-led protocol into an open standard hosted by the Linux Foundation, with more than 150 supporting organizations announced on April 9, 2026. The practical result is a cleaner boundary: teams can use ADK for prompts, tools, graph workflows, memory, and orchestration, then publish selected capabilities through A2A Agent Cards, tasks, messages, and artifacts. The takeaway is simple: ADK is your implementation framework, and A2A is your interoperability contract.</p>
<p>I use this split when an agent needs to leave the comfort of one codebase. A customer-support triage agent can stay as an internal ADK sub-agent if every collaborator lives in the same application. The moment it needs a contract-analysis agent maintained by legal, a refund agent owned by finance, or a third-party research agent hosted on another cloud, A2A becomes valuable.</p>
<p>The decision is less about vendor preference and more about coupling. ADK gives you a productive local runtime. A2A keeps the network boundary explicit enough that another team can replace its implementation without breaking your caller.</p>
<h2 id="what-is-the-2026-agent-stack-mcp-for-tools-adk-for-agent-logic-and-a2a-for-interoperability">What Is the 2026 Agent Stack: MCP for Tools, ADK for Agent Logic, and A2A for Interoperability?</h2>
<p>The 2026 agent stack is a three-layer architecture where MCP connects agents to tools and context, Google ADK defines agent behavior and workflows, and A2A coordinates communication between autonomous agents. The official A2A documentation frames this stack directly: build agents with ADK or another framework, equip them with MCP or another tool layer, then communicate with A2A across remote agents, local agents, and humans. This distinction matters because teams often confuse tool calling with agent interoperability. MCP answers, &ldquo;How does this agent call a database, file system, or SaaS API?&rdquo; ADK answers, &ldquo;How does this agent reason, route, retry, and maintain state?&rdquo; A2A answers, &ldquo;How does this agent discover and collaborate with another agent it does not own?&rdquo; The takeaway is that MCP, ADK, and A2A are complementary layers, not competing standards.</p>
<p>Here is the mental model I recommend in architecture reviews:</p>
<table>
  <thead>
      <tr>
          <th>Layer</th>
          <th>Primary job</th>
          <th>Typical owner</th>
          <th>Example</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>MCP</td>
          <td>Tool and context access</td>
          <td>Platform or app team</td>
          <td>A code agent reads a repository, calls GitHub, or queries a vector index</td>
      </tr>
      <tr>
          <td>ADK</td>
          <td>Agent logic and workflow</td>
          <td>Agent team</td>
          <td>A planner routes requests through graph nodes, retries failed steps, and stores state</td>
      </tr>
      <tr>
          <td>A2A</td>
          <td>Agent-to-agent collaboration</td>
          <td>Cross-team platform</td>
          <td>A finance agent delegates invoice validation to a vendor-risk agent</td>
      </tr>
  </tbody>
</table>
<p>This separation also makes failures easier to debug. If the agent cannot read a file, look at MCP or the tool adapter. If the agent chooses the wrong branch, look at ADK workflow logic. If a remote collaborator cannot be discovered, authenticated, or resumed, look at the A2A boundary.</p>
<h2 id="which-a2a-core-concepts-do-developers-need-agent-cards-skills-tasks-messages-artifacts-and-streaming">Which A2A Core Concepts Do Developers Need: Agent Cards, Skills, Tasks, Messages, Artifacts, and Streaming?</h2>
<p>A2A core concepts define how agents advertise capabilities, accept work, exchange state, and return results over a protocol that other frameworks can understand. Agent Cards describe an agent&rsquo;s public identity, endpoint, authentication requirements, and skills; the well-known discovery pattern commonly uses metadata such as <code>/.well-known/agent.json</code>. Skills describe what the agent can do. Tasks represent units of delegated work with lifecycle state. Messages carry user, agent, or system communication. Artifacts carry durable outputs such as files, reports, JSON payloads, or generated documents. Streaming, often implemented over Server-Sent Events in A2A examples, lets long-running agents report progress without forcing clients to poll blindly. A2A also uses familiar web mechanics such as HTTP/HTTPS and JSON-RPC 2.0 in common implementations. The takeaway is that A2A is not just remote prompt calling; it is a task and capability contract.</p>
<p>The most important design habit is to make the Agent Card boring and precise. Do not advertise &ldquo;does analytics&rdquo; when the skill is actually &ldquo;summarizes daily revenue variance from a warehouse-backed report.&rdquo; Remote orchestrators use this metadata to choose a collaborator, and vague skills create expensive runtime mistakes.</p>
<p>For artifacts, prefer structured outputs over prose when another agent will consume the result. A fraud-screening agent should return a risk score, reasons, evidence references, and confidence, not a paragraph that another model must parse again.</p>
<h2 id="how-do-you-expose-a-google-adk-agent-as-an-a2a-server">How Do You Expose a Google ADK Agent as an A2A Server?</h2>
<p>Exposing a Google ADK agent as an A2A server means wrapping the ADK runner behind an A2A-compatible application that publishes an Agent Card, accepts A2A tasks, invokes the ADK agent, and returns messages or artifacts through the task lifecycle. Google&rsquo;s own conversion guidance uses an AgentSkill and AgentCard as the public discovery identity, then bridges inbound A2A requests into ADK execution through an AgentTaskManager or AgentExecutor-style adapter. Community examples also call out ADK&rsquo;s <code>to_a2a()</code> helper as a fast path for turning an ADK agent into an A2A app. A practical deployment often runs the service on Cloud Run, exposes the metadata endpoint, protects the task endpoint with OAuth or service identity, and tests it with an A2A client. The takeaway is that the wrapper should translate protocols, not bury business logic.</p>
<p>A minimal conversion plan looks like this:</p>
<ol>
<li>Start with a working ADK agent and a narrow responsibility.</li>
<li>Define one or more A2A skills that match real capabilities.</li>
<li>Publish an Agent Card with endpoint, auth scheme, version, and skill metadata.</li>
<li>Implement the A2A task handler as a thin adapter around ADK execution.</li>
<li>Return structured artifacts for machine consumers and readable messages for humans.</li>
<li>Add contract tests that call the A2A endpoint without importing ADK internals.</li>
</ol>
<p>The adapter layer should be intentionally small. If it starts containing business rules, routing policy, or custom memory handling, you are building a second framework beside ADK. Keep the A2A server focused on discovery, authentication, task translation, streaming updates, and result packaging.</p>
<h2 id="how-do-you-build-a-cross-framework-orchestrator-with-adk-and-a2a">How Do You Build a Cross-Framework Orchestrator with ADK and A2A?</h2>
<p>A cross-framework orchestrator with ADK and A2A is an ADK agent that discovers remote A2A Agent Cards, evaluates their skills, delegates tasks to selected agents, and combines their artifacts into a final result. This pattern is useful when teams want Google ADK for orchestration but need to collaborate with agents built in Microsoft Agent Framework, LangGraph, BeeAI, custom Python services, or vendor-hosted platforms. A concrete example is an enterprise procurement workflow: an ADK orchestrator receives a supplier onboarding request, calls a risk agent maintained by compliance, calls a contract agent maintained by legal, and calls an ERP agent maintained by finance. Each remote agent keeps its own framework and deployment model, but exposes a shared A2A surface. The takeaway is that the orchestrator should reason over capabilities and results, not remote implementation details.</p>
<p>In practice, I keep an internal registry of trusted Agent Cards rather than letting production orchestrators crawl arbitrary endpoints. Discovery still uses the A2A contract, but platform governance decides which cards are trusted, what scopes they receive, and which versions are allowed.</p>
<p>The orchestrator prompt should not say, &ldquo;Call the legal agent whenever legal is relevant.&rdquo; Encode that routing as workflow policy where possible. ADK 2.0 graph workflows are useful here because fan-out, fan-in, loops, retries, dynamic nodes, human-in-the-loop steps, and nested workflows can be represented explicitly instead of hidden inside one large prompt.</p>
<h2 id="what-production-architecture-issues-matter-for-authentication-identity-observability-retries-and-deployment">What Production Architecture Issues Matter for Authentication, Identity, Observability, Retries, and Deployment?</h2>
<p>Production A2A architecture depends on treating remote agents like critical services: authenticate callers, verify agent identity, trace every task, retry safely, and deploy behind controlled endpoints. A2A v0.2 introduced standardized authentication schemes based on an OpenAPI-like authentication schema, which matters because real deployments cannot rely on unsigned Agent Cards or shared demo tokens. In a production design, each agent should have a stable service identity, scoped authorization, versioned Agent Cards, task IDs that flow through logs and traces, and timeout rules that match the business process. Long-running tasks need streaming progress or resumable state, while non-idempotent tasks need idempotency keys before retries are enabled. Cloud Run, Kubernetes, or managed agent platforms can all work if the protocol boundary stays stable. The takeaway is that interoperability is only useful when the operating model is as explicit as the API.</p>
<p>The failure mode I see most often is optimistic delegation. A caller sends a task to a remote agent and assumes the result will arrive quickly, in the expected shape, with no partial failures. That assumption fails the first time a human approval is needed, a model provider throttles, or a downstream SaaS API times out.</p>
<p>Build the control plane before broad rollout. You need dashboards for task volume, latency, failure reason, artifact size, auth denials, protocol versions, and remote-agent health. If you cannot answer &ldquo;Which remote agent produced this artifact and under which policy?&rdquo; you are not ready for regulated workflows.</p>
<h2 id="how-do-adk-a2a-and-mcp-compare-and-when-should-you-use-each-layer">How Do ADK, A2A, and MCP Compare, and When Should You Use Each Layer?</h2>
<p>ADK, A2A, and MCP compare best by boundary: ADK is for building and orchestrating agent behavior, MCP is for connecting an agent to tools and context, and A2A is for connecting agents to other agents. Google ADK Python 2.0 adds graph-based workflows for routing, fan-out/fan-in, loops, retry, state management, dynamic nodes, human-in-the-loop, and nested workflows, which makes it a strong fit for internal agent execution. MCP is a better fit when the same agent needs controlled access to GitHub, a database, a browser, or an enterprise API. A2A is the right layer when the collaborator is itself an autonomous agent with its own skills, identity, lifecycle, and artifacts. The takeaway is to choose the layer based on the boundary you are crossing.</p>
<table>
  <thead>
      <tr>
          <th>Question</th>
          <th style="text-align: right">Use ADK</th>
          <th style="text-align: right">Use MCP</th>
          <th style="text-align: right">Use A2A</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>Are you defining agent reasoning, state, and workflow?</td>
          <td style="text-align: right">Yes</td>
          <td style="text-align: right">No</td>
          <td style="text-align: right">No</td>
      </tr>
      <tr>
          <td>Are you connecting one agent to tools or data sources?</td>
          <td style="text-align: right">Sometimes</td>
          <td style="text-align: right">Yes</td>
          <td style="text-align: right">No</td>
      </tr>
      <tr>
          <td>Are you delegating work to another autonomous agent?</td>
          <td style="text-align: right">Sometimes</td>
          <td style="text-align: right">No</td>
          <td style="text-align: right">Yes</td>
      </tr>
      <tr>
          <td>Do you need cross-framework collaboration?</td>
          <td style="text-align: right">No by itself</td>
          <td style="text-align: right">No</td>
          <td style="text-align: right">Yes</td>
      </tr>
      <tr>
          <td>Do you need task lifecycle and artifacts from a remote agent?</td>
          <td style="text-align: right">No by itself</td>
          <td style="text-align: right">No</td>
          <td style="text-align: right">Yes</td>
      </tr>
  </tbody>
</table>
<p>Do not expose every ADK sub-agent as an A2A service. Internal delegation inside one ADK application is cheaper, faster, and easier to refactor. Use A2A when the collaboration boundary is real: another team owns the agent, another framework runs it, another cloud hosts it, or another organization governs it.</p>
<h2 id="what-is-the-current-ecosystem-status-for-linux-foundation-governance-cloud-support-and-sdk-maturity">What Is the Current Ecosystem Status for Linux Foundation Governance, Cloud Support, and SDK Maturity?</h2>
<p>The A2A ecosystem in 2026 has moved beyond a Google-only proposal into a Linux Foundation-hosted open standard with broad vendor participation, but teams should still validate maturity through proof-of-concept work. A2A launched in April 2025 with support from more than 50 technology partners and service providers, including Atlassian, Box, Cohere, Intuit, LangChain, MongoDB, PayPal, Salesforce, SAP, ServiceNow, Workday, Accenture, Deloitte, McKinsey, PwC, TCS, and Wipro. On April 9, 2026, the Linux Foundation announced A2A had surpassed 150 supporting organizations, with integrations across Google, Microsoft, and AWS platforms and production deployments across multiple industries. The research brief also recorded A2A v1.0.1 as the latest GitHub release on May 28, 2026. The takeaway is that A2A has real momentum, but production confidence still comes from testing your own workflow.</p>
<p>ADK is also moving quickly. The brief recorded Google ADK Python v2.2.0 as the latest release on June 4, 2026, and noted that ADK documentation shows language tabs for Python, JavaScript, Go, Java, and Kotlin. That breadth helps teams standardize patterns without forcing every service into one runtime.</p>
<p>My read is pragmatic: the governance story is strong enough to justify architecture experiments, and the SDK story is strong enough to build pilots. For critical workflows, I would still pin protocol and SDK versions, keep compatibility tests in CI, and avoid relying on undocumented behavior from any one implementation.</p>
<h2 id="what-implementation-pitfalls-should-teams-avoid">What Implementation Pitfalls Should Teams Avoid?</h2>
<p>Common A2A and ADK implementation pitfalls come from exposing too much too early, trusting metadata too blindly, and treating remote agents like local function calls. The A2A GitHub repository had 24,247 stars and 2,459 forks when checked on June 12, 2026, which signals developer interest, but popularity does not remove distributed-systems risk. The first pitfall is a vague Agent Card that makes orchestration decisions unreliable. The second is missing authentication or unsigned metadata that lets callers confuse identity with availability. The third is returning prose where downstream agents need structured artifacts. The fourth is skipping task lifecycle observability, which makes long-running workflows impossible to debug. The fifth is wrapping tiny internal ADK sub-agents as network services before there is a real ownership boundary. The takeaway is that A2A should make agent collaboration explicit, not turn every helper into infrastructure.</p>
<p>I also avoid building a universal &ldquo;agent broker&rdquo; too early. It sounds attractive, but most teams learn more from two or three well-instrumented agent-to-agent integrations than from a generic router with weak policy. Start with known collaborators, explicit contracts, and boring operational metrics.</p>
<p>Versioning deserves special care. Agent Cards should expose enough version information for clients to make safe choices, and clients should tolerate additive changes. If a remote agent changes artifact structure without a compatibility window, your orchestrator will fail in a place that looks like a reasoning problem but is really a contract problem.</p>
<h2 id="what-starter-blueprint-should-you-use-for-a-2026-interoperable-agent-system">What Starter Blueprint Should You Use for a 2026 Interoperable Agent System?</h2>
<p>A good 2026 starter blueprint is a small ADK-orchestrated system with two or three well-scoped remote A2A agents, one shared identity model, contract tests, and structured artifacts from day one. For example, a developer-platform team could build an ADK orchestrator that receives a release-readiness request, delegates dependency scanning to a security agent, changelog drafting to a documentation agent, and deployment-risk analysis to an operations agent. Each agent exposes an Agent Card and one or two skills, runs behind authenticated HTTPS, and returns JSON artifacts plus human-readable summaries. ADK graph workflows handle routing, fan-out/fan-in, retries, and human approval. MCP can still connect individual agents to GitHub, ticketing systems, or internal databases. The takeaway is to begin with a narrow production-shaped slice rather than a broad demo mesh.</p>
<p>My default checklist for the first pilot is short:</p>
<table>
  <thead>
      <tr>
          <th>Area</th>
          <th>Starter decision</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>Agent scope</td>
          <td>One business capability per remote agent</td>
      </tr>
      <tr>
          <td>Discovery</td>
          <td>Trusted registry of Agent Cards</td>
      </tr>
      <tr>
          <td>Authentication</td>
          <td>Service identity plus scoped authorization</td>
      </tr>
      <tr>
          <td>Results</td>
          <td>JSON artifacts with schema tests</td>
      </tr>
      <tr>
          <td>Workflow</td>
          <td>ADK graph with explicit retry and approval nodes</td>
      </tr>
      <tr>
          <td>Operations</td>
          <td>Trace IDs across every task, message, and artifact</td>
      </tr>
  </tbody>
</table>
<p>Once that works, expand horizontally. Add another remote agent, another artifact type, or another framework. Do not expand by making the orchestrator smarter in vague ways. The value of the google adk a2a protocol approach is that each boundary stays visible enough to test, secure, and replace.</p>
<h2 id="what-questions-do-developers-ask-about-google-adk-and-a2a">What Questions Do Developers Ask About Google ADK and A2A?</h2>
<p>Google ADK and A2A questions usually center on whether the technologies overlap, how they relate to MCP, and when a team should introduce a network protocol between agents. The most practical answer is that ADK is a framework for building agents, A2A is a protocol for agent interoperability, and MCP is a tool/context access layer. As of the June 12, 2026 research brief, ADK Python was at v2.2.0, A2A&rsquo;s latest GitHub release was v1.0.1, and A2A was hosted by the Linux Foundation. Those facts make the stack credible enough for pilots and carefully scoped production work, but not a reason to skip design discipline. The right adoption path is a narrow workflow with clear ownership boundaries, authenticated endpoints, structured artifacts, and contract tests. The takeaway is that ADK plus A2A is powerful when the collaboration boundary is real.</p>
<h3 id="is-google-adk-the-same-thing-as-the-a2a-protocol">Is Google ADK the same thing as the A2A protocol?</h3>
<p>Google ADK is not the same thing as the A2A protocol; ADK is an agent development framework, while A2A is an interoperability protocol. Use ADK to define agent behavior, tools, state, workflows, and orchestration. Use A2A when that agent needs to advertise skills, accept remote tasks, stream progress, or return artifacts to another agent or orchestrator.</p>
<h3 id="do-i-need-mcp-if-i-already-use-a2a">Do I need MCP if I already use A2A?</h3>
<p>MCP is still useful with A2A because the two layers solve different problems. MCP connects an agent to tools, services, files, APIs, and context sources. A2A connects one autonomous agent to another autonomous agent. A common pattern is an ADK agent that uses MCP to read GitHub and uses A2A to delegate security review to a remote agent.</p>
<h3 id="when-should-i-not-expose-an-adk-sub-agent-through-a2a">When should I not expose an ADK sub-agent through A2A?</h3>
<p>You should not expose an ADK sub-agent through A2A when it is only an internal helper inside one application and has no independent ownership, deployment, or framework boundary. Local ADK composition is simpler and faster. Add A2A when another team, service, cloud, framework, or organization needs to call that capability through a stable contract.</p>
<h3 id="what-should-an-agent-card-include-in-a-production-system">What should an Agent Card include in a production system?</h3>
<p>An Agent Card should include the agent&rsquo;s identity, endpoint, supported protocol version, authentication requirements, skill descriptions, input expectations, output or artifact formats, and operational ownership. In production, treat the card like an API contract. Keep skills narrow, version changes clearly, and store trusted cards in a governed registry instead of accepting arbitrary metadata.</p>
<h3 id="can-a2a-work-across-non-google-frameworks">Can A2A work across non-Google frameworks?</h3>
<p>A2A can work across non-Google frameworks because its purpose is cross-framework agent interoperability. An ADK orchestrator can call agents implemented in Microsoft Agent Framework, LangGraph, BeeAI, custom services, or vendor platforms if those agents expose compatible A2A endpoints and trusted Agent Cards. The hard parts are authentication, versioning, observability, and artifact contracts, not the framework names.</p>
]]></content:encoded></item><item><title>CrewAI A2A Protocol Tutorial: Build Interoperable Agents with Agent2Agent Support</title><link>https://baeseokjae.github.io/posts/crewai-a2a-protocol-tutorial-2026/</link><pubDate>Thu, 23 Apr 2026 01:23:58 +0000</pubDate><guid>https://baeseokjae.github.io/posts/crewai-a2a-protocol-tutorial-2026/</guid><description>Step-by-step tutorial on using the A2A protocol with CrewAI to build cross-framework multi-agent systems in 2026.</description><content:encoded><![CDATA[<p>The A2A (Agent2Agent) protocol lets you connect a CrewAI agent to a LangGraph agent — or any other compliant framework — over a standard HTTP interface, with no custom glue code. Setup takes about 15 minutes once your CrewAI environment is running.</p>
<h2 id="what-is-the-a2a-protocol">What Is the A2A Protocol?</h2>
<p>The A2A (Agent2Agent) protocol is an open HTTP-based standard that defines how AI agents from different frameworks discover each other, exchange tasks, and stream results — without requiring framework-specific integration code. Originally developed by Google and donated to the Linux Foundation in early 2026, A2A is now a vendor-neutral specification backed by Anthropic, Microsoft, Salesforce, and over 50 other organizations. Think of it as the HTTP of multi-agent systems: just as HTTP lets any browser talk to any web server regardless of their underlying technology, A2A lets any compliant agent talk to any other. The protocol uses JSON-RPC 2.0 over HTTPS, supports server-sent events for streaming, and mandates an <code>/.well-known/agent.json</code> discovery endpoint so agents can advertise their capabilities. CrewAI adopted A2A as a first-class feature in version 0.80, making it possible to delegate tasks from a CrewAI crew to a LangGraph graph, a Semantic Kernel agent, or a custom Python service — all with a single configuration block. For teams building composite AI systems in 2026, A2A removes the biggest integration pain point: the need to write and maintain bespoke adapter layers every time you add a new agent framework.</p>
<h2 id="why-crewai-adopted-a2a-as-a-first-class-primitive">Why CrewAI Adopted A2A as a First-Class Primitive</h2>
<p>CrewAI treats A2A as a delegation primitive, not an afterthought bolt-on. Rather than wrapping A2A in a special tool class or plugin, CrewAI exposes it directly as a configuration option on the <code>Agent</code> class — the same place you define an agent&rsquo;s role, backstory, and LLM. This design decision reflects a key insight: 85% of developers in 2026 regularly use AI tools across multiple frameworks, which means cross-framework agent communication is a mainstream problem, not an edge case. Before A2A, connecting a CrewAI research agent to a LangGraph execution agent meant writing custom REST adapters, managing serialization formats, and handling auth manually for each pair of frameworks. With A2A, CrewAI agents can delegate tasks to any compliant server agent by specifying a URL and auth config — the protocol handles capability negotiation, task lifecycle, and streaming. CrewAI also acts as an A2A server itself: you can expose any CrewAI crew as an A2A-compliant endpoint, making it accessible to agents built in other frameworks. This bidirectional support is what makes A2A genuinely useful in production: you can participate in a multi-framework ecosystem as both a consumer and a provider without rewriting your core crew logic.</p>
<h2 id="a2a-vs-direct-api-integration-key-differences">A2A vs Direct API Integration: Key Differences</h2>
<p>A2A protocol and direct API calls solve different problems, and picking the wrong one creates unnecessary complexity. A direct API call works best when you control both ends of the integration, both agents use the same framework version, and you only need request-response (no streaming). A2A is better when agents are built on different frameworks, owned by different teams, or need to stream results back incrementally.</p>
<table>
  <thead>
      <tr>
          <th>Dimension</th>
          <th>Direct API Call</th>
          <th>A2A Protocol</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>Discovery</td>
          <td>Manual, hardcoded</td>
          <td>Automatic via <code>/.well-known/agent.json</code></td>
      </tr>
      <tr>
          <td>Auth</td>
          <td>Custom per integration</td>
          <td>Standardized: Bearer, OAuth2, API keys</td>
      </tr>
      <tr>
          <td>Streaming</td>
          <td>Manual SSE/WebSocket</td>
          <td>Built into the spec</td>
      </tr>
      <tr>
          <td>Framework coupling</td>
          <td>Tight</td>
          <td>None</td>
      </tr>
      <tr>
          <td>Schema validation</td>
          <td>Ad hoc</td>
          <td>JSON-RPC 2.0 enforced</td>
      </tr>
      <tr>
          <td>Task lifecycle</td>
          <td>Custom</td>
          <td>Standardized (<code>submitted</code>, <code>working</code>, <code>completed</code>)</td>
      </tr>
      <tr>
          <td>Interoperability</td>
          <td>Framework-specific</td>
          <td>Any A2A-compliant agent</td>
      </tr>
  </tbody>
</table>
<p>A direct API call has less overhead and fewer moving parts when you control both sides. But A2A wins in any scenario where you&rsquo;re connecting agents from different teams or frameworks — the standardization eliminates the negotiation cost of &ldquo;how do we send data between these two things?&rdquo; every time you add a new agent to the system.</p>
<p>A2A also defines a formal task lifecycle (<code>submitted</code> → <code>working</code> → <code>completed</code> / <code>failed</code>) that makes it easy to poll for status or stream progress without reinventing that machinery for each integration. This is particularly valuable for long-running tasks like research synthesis or code generation, where you want intermediate updates rather than a single blocking response.</p>
<h2 id="setting-up-crewai-with-a2a-support">Setting Up CrewAI with A2A Support</h2>
<p>Installing A2A support in CrewAI requires the <code>crewai[a2a]</code> extra, which pulls in the <code>a2a-sdk</code> package alongside the base framework. Here is the full setup sequence:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-bash" data-lang="bash"><span style="display:flex;"><span><span style="color:#75715e"># Create and activate a virtual environment</span>
</span></span><span style="display:flex;"><span>python -m venv .venv
</span></span><span style="display:flex;"><span>source .venv/bin/activate
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#75715e"># Install CrewAI with A2A support</span>
</span></span><span style="display:flex;"><span>pip install <span style="color:#e6db74">&#34;crewai[a2a]&gt;=0.80.0&#34;</span>
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#75715e"># Verify the installation</span>
</span></span><span style="display:flex;"><span>python -c <span style="color:#e6db74">&#34;import crewai; print(crewai.__version__)&#34;</span>
</span></span></code></pre></div><p>You also need an LLM API key. CrewAI defaults to OpenAI but works with any LiteLLM-compatible provider:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-bash" data-lang="bash"><span style="display:flex;"><span>export OPENAI_API_KEY<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;sk-...&#34;</span>
</span></span><span style="display:flex;"><span><span style="color:#75715e"># Or use a different provider</span>
</span></span><span style="display:flex;"><span>export ANTHROPIC_API_KEY<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;sk-ant-...&#34;</span>
</span></span></code></pre></div><p>If you are connecting to an existing A2A server agent (a LangGraph endpoint, for example), you need the server&rsquo;s base URL and its auth credentials. Collect these before writing any code — you will pass them in the <code>A2AClientConfig</code> object shown in the next section.</p>
<p>Your project structure for a minimal A2A-enabled crew looks like this:</p>



<div class="goat svg-container ">
  
    <svg
      xmlns="http://www.w3.org/2000/svg"
      font-family="Menlo,Lucida Console,monospace"
      
        viewBox="0 0 112 89"
      >
      <g transform='translate(8,16)'>
<path d='M 84,8 L 52,88' fill='none' stroke='currentColor'></path>
<text text-anchor='middle' x='0' y='4' fill='currentColor' style='font-size:1em'>m</text>
<text text-anchor='middle' x='0' y='20' fill='currentColor' style='font-size:1em'>├</text>
<text text-anchor='middle' x='0' y='36' fill='currentColor' style='font-size:1em'>├</text>
<text text-anchor='middle' x='0' y='52' fill='currentColor' style='font-size:1em'>├</text>
<text text-anchor='middle' x='0' y='68' fill='currentColor' style='font-size:1em'>└</text>
<text text-anchor='middle' x='8' y='4' fill='currentColor' style='font-size:1em'>y</text>
<text text-anchor='middle' x='8' y='20' fill='currentColor' style='font-size:1em'>─</text>
<text text-anchor='middle' x='8' y='36' fill='currentColor' style='font-size:1em'>─</text>
<text text-anchor='middle' x='8' y='52' fill='currentColor' style='font-size:1em'>─</text>
<text text-anchor='middle' x='8' y='68' fill='currentColor' style='font-size:1em'>─</text>
<text text-anchor='middle' x='16' y='4' fill='currentColor' style='font-size:1em'>_</text>
<text text-anchor='middle' x='16' y='20' fill='currentColor' style='font-size:1em'>─</text>
<text text-anchor='middle' x='16' y='36' fill='currentColor' style='font-size:1em'>─</text>
<text text-anchor='middle' x='16' y='52' fill='currentColor' style='font-size:1em'>─</text>
<text text-anchor='middle' x='16' y='68' fill='currentColor' style='font-size:1em'>─</text>
<text text-anchor='middle' x='24' y='4' fill='currentColor' style='font-size:1em'>a</text>
<text text-anchor='middle' x='32' y='4' fill='currentColor' style='font-size:1em'>2</text>
<text text-anchor='middle' x='32' y='20' fill='currentColor' style='font-size:1em'>a</text>
<text text-anchor='middle' x='32' y='36' fill='currentColor' style='font-size:1em'>t</text>
<text text-anchor='middle' x='32' y='52' fill='currentColor' style='font-size:1em'>c</text>
<text text-anchor='middle' x='32' y='68' fill='currentColor' style='font-size:1em'>.</text>
<text text-anchor='middle' x='40' y='4' fill='currentColor' style='font-size:1em'>a</text>
<text text-anchor='middle' x='40' y='20' fill='currentColor' style='font-size:1em'>g</text>
<text text-anchor='middle' x='40' y='36' fill='currentColor' style='font-size:1em'>a</text>
<text text-anchor='middle' x='40' y='52' fill='currentColor' style='font-size:1em'>r</text>
<text text-anchor='middle' x='40' y='68' fill='currentColor' style='font-size:1em'>e</text>
<text text-anchor='middle' x='48' y='4' fill='currentColor' style='font-size:1em'>_</text>
<text text-anchor='middle' x='48' y='20' fill='currentColor' style='font-size:1em'>e</text>
<text text-anchor='middle' x='48' y='36' fill='currentColor' style='font-size:1em'>s</text>
<text text-anchor='middle' x='48' y='52' fill='currentColor' style='font-size:1em'>e</text>
<text text-anchor='middle' x='48' y='68' fill='currentColor' style='font-size:1em'>n</text>
<text text-anchor='middle' x='56' y='4' fill='currentColor' style='font-size:1em'>c</text>
<text text-anchor='middle' x='56' y='20' fill='currentColor' style='font-size:1em'>n</text>
<text text-anchor='middle' x='56' y='36' fill='currentColor' style='font-size:1em'>k</text>
<text text-anchor='middle' x='56' y='52' fill='currentColor' style='font-size:1em'>w</text>
<text text-anchor='middle' x='56' y='68' fill='currentColor' style='font-size:1em'>v</text>
<text text-anchor='middle' x='64' y='4' fill='currentColor' style='font-size:1em'>r</text>
<text text-anchor='middle' x='64' y='20' fill='currentColor' style='font-size:1em'>t</text>
<text text-anchor='middle' x='64' y='36' fill='currentColor' style='font-size:1em'>s</text>
<text text-anchor='middle' x='64' y='52' fill='currentColor' style='font-size:1em'>.</text>
<text text-anchor='middle' x='72' y='4' fill='currentColor' style='font-size:1em'>e</text>
<text text-anchor='middle' x='72' y='20' fill='currentColor' style='font-size:1em'>s</text>
<text text-anchor='middle' x='72' y='36' fill='currentColor' style='font-size:1em'>.</text>
<text text-anchor='middle' x='72' y='52' fill='currentColor' style='font-size:1em'>p</text>
<text text-anchor='middle' x='80' y='4' fill='currentColor' style='font-size:1em'>w</text>
<text text-anchor='middle' x='80' y='20' fill='currentColor' style='font-size:1em'>.</text>
<text text-anchor='middle' x='80' y='36' fill='currentColor' style='font-size:1em'>p</text>
<text text-anchor='middle' x='80' y='52' fill='currentColor' style='font-size:1em'>y</text>
<text text-anchor='middle' x='88' y='20' fill='currentColor' style='font-size:1em'>p</text>
<text text-anchor='middle' x='88' y='36' fill='currentColor' style='font-size:1em'>y</text>
<text text-anchor='middle' x='96' y='20' fill='currentColor' style='font-size:1em'>y</text>
</g>

    </svg>
  
</div>
<p>Keep credentials in <code>.env</code> and load them with <code>python-dotenv</code>. Never hardcode tokens directly in agent configuration files.</p>
<h2 id="building-your-first-a2a-agent-in-crewai">Building Your First A2A Agent in CrewAI</h2>
<p>A2A agents in CrewAI work by wrapping a remote A2A server in an <code>A2AClientConfig</code> and attaching it to a standard <code>Agent</code> definition. The local agent delegates tasks to the remote server automatically when assigned tasks that require it. Here is a complete working example:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-python" data-lang="python"><span style="display:flex;"><span><span style="color:#f92672">from</span> crewai <span style="color:#f92672">import</span> Agent, Task, Crew
</span></span><span style="display:flex;"><span><span style="color:#f92672">from</span> crewai.a2a <span style="color:#f92672">import</span> A2AClientConfig, A2AAuthConfig
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#75715e"># Configure the A2A client — points to a remote compliant agent</span>
</span></span><span style="display:flex;"><span>a2a_config <span style="color:#f92672">=</span> A2AClientConfig(
</span></span><span style="display:flex;"><span>    server_url<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;https://my-langgraph-agent.example.com&#34;</span>,
</span></span><span style="display:flex;"><span>    auth<span style="color:#f92672">=</span>A2AAuthConfig(
</span></span><span style="display:flex;"><span>        type<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;bearer&#34;</span>,
</span></span><span style="display:flex;"><span>        token<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;your-bearer-token-here&#34;</span>
</span></span><span style="display:flex;"><span>    )
</span></span><span style="display:flex;"><span>)
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#75715e"># Define a CrewAI agent that delegates to the A2A server</span>
</span></span><span style="display:flex;"><span>researcher <span style="color:#f92672">=</span> Agent(
</span></span><span style="display:flex;"><span>    role<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;Research Specialist&#34;</span>,
</span></span><span style="display:flex;"><span>    goal<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;Gather and summarize technical information on any topic&#34;</span>,
</span></span><span style="display:flex;"><span>    backstory<span style="color:#f92672">=</span>(
</span></span><span style="display:flex;"><span>        <span style="color:#e6db74">&#34;You are a senior researcher with access to specialized &#34;</span>
</span></span><span style="display:flex;"><span>        <span style="color:#e6db74">&#34;analysis tools hosted on external agent servers.&#34;</span>
</span></span><span style="display:flex;"><span>    ),
</span></span><span style="display:flex;"><span>    a2a_client<span style="color:#f92672">=</span>a2a_config,
</span></span><span style="display:flex;"><span>    verbose<span style="color:#f92672">=</span><span style="color:#66d9ef">True</span>
</span></span><span style="display:flex;"><span>)
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#75715e"># Task will be delegated through A2A to the remote agent</span>
</span></span><span style="display:flex;"><span>research_task <span style="color:#f92672">=</span> Task(
</span></span><span style="display:flex;"><span>    description<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;Research the latest advances in transformer architecture pruning&#34;</span>,
</span></span><span style="display:flex;"><span>    expected_output<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;A 500-word technical summary with three key findings&#34;</span>,
</span></span><span style="display:flex;"><span>    agent<span style="color:#f92672">=</span>researcher
</span></span><span style="display:flex;"><span>)
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>crew <span style="color:#f92672">=</span> Crew(agents<span style="color:#f92672">=</span>[researcher], tasks<span style="color:#f92672">=</span>[research_task])
</span></span><span style="display:flex;"><span>result <span style="color:#f92672">=</span> crew<span style="color:#f92672">.</span>kickoff()
</span></span><span style="display:flex;"><span>print(result)
</span></span></code></pre></div><p>When <code>crew.kickoff()</code> runs, CrewAI sends the task payload to the remote A2A server via JSON-RPC, polls for status updates, and streams back the result. From the LLM&rsquo;s perspective, it simply receives the completed output — the A2A transport layer is invisible.</p>
<p>The <code>A2AClientConfig</code> supports four auth types out of the box: <code>bearer</code> (token in Authorization header), <code>oauth2</code> (client credentials flow), <code>api_key</code> (custom header), and <code>http_basic</code>. Match the type to what the server agent expects.</p>
<h2 id="cross-framework-communication-crewai--langgraph">Cross-Framework Communication: CrewAI + LangGraph</h2>
<p>The most practical A2A use case in 2026 is connecting a CrewAI crew to a LangGraph workflow — the two most popular agent frameworks with overlapping but non-identical strengths. CrewAI excels at role-based multi-agent orchestration; LangGraph excels at complex stateful workflows with explicit graph logic. Combining them via A2A lets you use each where it fits best. Google&rsquo;s purchasing concierge Codelab demonstrates exactly this pattern: a CrewAI &ldquo;Burger agent&rdquo; and a LangGraph &ldquo;Pizza agent&rdquo; work together via A2A to fulfill a composite food order, with each framework handling the domain it knows best.</p>
<p>First, expose your LangGraph graph as an A2A server:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-python" data-lang="python"><span style="display:flex;"><span><span style="color:#75715e"># langgraph_server.py</span>
</span></span><span style="display:flex;"><span><span style="color:#f92672">from</span> langgraph.a2a <span style="color:#f92672">import</span> A2AServer
</span></span><span style="display:flex;"><span><span style="color:#f92672">from</span> my_graph <span style="color:#f92672">import</span> compiled_graph
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>app <span style="color:#f92672">=</span> A2AServer(
</span></span><span style="display:flex;"><span>    graph<span style="color:#f92672">=</span>compiled_graph,
</span></span><span style="display:flex;"><span>    agent_card<span style="color:#f92672">=</span>{
</span></span><span style="display:flex;"><span>        <span style="color:#e6db74">&#34;name&#34;</span>: <span style="color:#e6db74">&#34;LangGraph Execution Agent&#34;</span>,
</span></span><span style="display:flex;"><span>        <span style="color:#e6db74">&#34;description&#34;</span>: <span style="color:#e6db74">&#34;Executes structured workflows with state management&#34;</span>,
</span></span><span style="display:flex;"><span>        <span style="color:#e6db74">&#34;version&#34;</span>: <span style="color:#e6db74">&#34;1.0.0&#34;</span>,
</span></span><span style="display:flex;"><span>        <span style="color:#e6db74">&#34;capabilities&#34;</span>: [<span style="color:#e6db74">&#34;streaming&#34;</span>, <span style="color:#e6db74">&#34;stateful-execution&#34;</span>]
</span></span><span style="display:flex;"><span>    }
</span></span><span style="display:flex;"><span>)
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#75715e"># Run with: uvicorn langgraph_server:app --port 8001</span>
</span></span></code></pre></div><p>Then point your CrewAI agent at that server:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-python" data-lang="python"><span style="display:flex;"><span><span style="color:#f92672">from</span> crewai.a2a <span style="color:#f92672">import</span> A2AClientConfig, A2AAuthConfig
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>langgraph_config <span style="color:#f92672">=</span> A2AClientConfig(
</span></span><span style="display:flex;"><span>    server_url<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;http://localhost:8001&#34;</span>,
</span></span><span style="display:flex;"><span>    auth<span style="color:#f92672">=</span>A2AAuthConfig(type<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;bearer&#34;</span>, token<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;dev-token&#34;</span>)
</span></span><span style="display:flex;"><span>)
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>executor_agent <span style="color:#f92672">=</span> Agent(
</span></span><span style="display:flex;"><span>    role<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;Workflow Executor&#34;</span>,
</span></span><span style="display:flex;"><span>    goal<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;Execute structured multi-step workflows via LangGraph&#34;</span>,
</span></span><span style="display:flex;"><span>    backstory<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;Specialist in stateful task execution with rollback support&#34;</span>,
</span></span><span style="display:flex;"><span>    a2a_client<span style="color:#f92672">=</span>langgraph_config
</span></span><span style="display:flex;"><span>)
</span></span></code></pre></div><p>The two agents now communicate over A2A: CrewAI sends tasks as JSON-RPC messages, LangGraph processes them through its state graph, and results stream back to the CrewAI crew. Neither framework needs to know anything about the other&rsquo;s internals.</p>
<h2 id="authentication-and-security-for-a2a-agents">Authentication and Security for A2A Agents</h2>
<p>A2A&rsquo;s authentication model mirrors enterprise API security patterns, which makes it straightforward to integrate with existing identity infrastructure. CrewAI&rsquo;s <code>A2AAuthConfig</code> supports four auth strategies, each suited to different deployment scenarios. The bearer token approach is simplest and works for internal services; OAuth2 client credentials is the right choice for production B2B agent communication where you need short-lived, auditable tokens.</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-python" data-lang="python"><span style="display:flex;"><span><span style="color:#f92672">from</span> crewai.a2a <span style="color:#f92672">import</span> A2AAuthConfig
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#75715e"># Bearer token (simple, good for internal/dev)</span>
</span></span><span style="display:flex;"><span>bearer_auth <span style="color:#f92672">=</span> A2AAuthConfig(
</span></span><span style="display:flex;"><span>    type<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;bearer&#34;</span>,
</span></span><span style="display:flex;"><span>    token<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;eyJhbGciOiJSUzI1NiJ9...&#34;</span>
</span></span><span style="display:flex;"><span>)
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#75715e"># OAuth2 client credentials (production cross-org communication)</span>
</span></span><span style="display:flex;"><span>oauth2_auth <span style="color:#f92672">=</span> A2AAuthConfig(
</span></span><span style="display:flex;"><span>    type<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;oauth2&#34;</span>,
</span></span><span style="display:flex;"><span>    client_id<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;crewai-crew-prod&#34;</span>,
</span></span><span style="display:flex;"><span>    client_secret<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;your-secret&#34;</span>,
</span></span><span style="display:flex;"><span>    token_url<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;https://auth.example.com/oauth/token&#34;</span>,
</span></span><span style="display:flex;"><span>    scopes<span style="color:#f92672">=</span>[<span style="color:#e6db74">&#34;agent:read&#34;</span>, <span style="color:#e6db74">&#34;agent:execute&#34;</span>]
</span></span><span style="display:flex;"><span>)
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#75715e"># API key via custom header (common for SaaS agent platforms)</span>
</span></span><span style="display:flex;"><span>api_key_auth <span style="color:#f92672">=</span> A2AAuthConfig(
</span></span><span style="display:flex;"><span>    type<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;api_key&#34;</span>,
</span></span><span style="display:flex;"><span>    header_name<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;X-Agent-API-Key&#34;</span>,
</span></span><span style="display:flex;"><span>    api_key<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;ak_live_...&#34;</span>
</span></span><span style="display:flex;"><span>)
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#75715e"># HTTP Basic (legacy systems or internal proxies)</span>
</span></span><span style="display:flex;"><span>basic_auth <span style="color:#f92672">=</span> A2AAuthConfig(
</span></span><span style="display:flex;"><span>    type<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;http_basic&#34;</span>,
</span></span><span style="display:flex;"><span>    username<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;crew-service&#34;</span>,
</span></span><span style="display:flex;"><span>    password<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;internal-password&#34;</span>
</span></span><span style="display:flex;"><span>)
</span></span></code></pre></div><p>Beyond auth, enforce these security practices for production A2A deployments:</p>
<ul>
<li><strong>TLS everywhere</strong>: Only connect to <code>https://</code> endpoints in production. A2A over plain HTTP leaks task payloads.</li>
<li><strong>Rotate bearer tokens</strong>: Set a short TTL (1–24 hours) and automate rotation via your secrets manager.</li>
<li><strong>Scope OAuth2 tokens tightly</strong>: Request only the scopes an agent needs (<code>agent:execute</code> not <code>agent:admin</code>).</li>
<li><strong>Validate the agent card</strong>: Before delegating tasks to a discovered agent, verify its <code>/.well-known/agent.json</code> matches your expected capabilities and version.</li>
<li><strong>Rate-limit outbound requests</strong>: Wrap your <code>A2AClientConfig</code> in a circuit breaker to prevent a misbehaving remote agent from cascading failures into your crew.</li>
</ul>
<h2 id="debugging-with-a2a-inspector">Debugging with A2A Inspector</h2>
<p>The A2A Inspector is a browser-based tool for observing the JSON-RPC messages that flow between A2A agents in real time. It works by proxying traffic between your client and server, displaying each request and response in a structured UI. This is the fastest way to diagnose why a delegation is failing or why results are arriving malformed.</p>
<p>To use it during local development:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-bash" data-lang="bash"><span style="display:flex;"><span><span style="color:#75715e"># Install the inspector CLI</span>
</span></span><span style="display:flex;"><span>pip install a2a-inspector
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#75715e"># Start the inspector proxy (listens on 8080, forwards to your server on 8001)</span>
</span></span><span style="display:flex;"><span>a2a-inspector proxy --target http://localhost:8001 --port <span style="color:#ae81ff">8080</span>
</span></span></code></pre></div><p>Then point your <code>A2AClientConfig</code> at the inspector proxy instead of directly at the server:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-python" data-lang="python"><span style="display:flex;"><span>a2a_config <span style="color:#f92672">=</span> A2AClientConfig(
</span></span><span style="display:flex;"><span>    server_url<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;http://localhost:8080&#34;</span>,  <span style="color:#75715e"># Inspector proxy</span>
</span></span><span style="display:flex;"><span>    auth<span style="color:#f92672">=</span>A2AAuthConfig(type<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;bearer&#34;</span>, token<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;dev-token&#34;</span>)
</span></span><span style="display:flex;"><span>)
</span></span></code></pre></div><p>Open <code>http://localhost:8080/inspector</code> in your browser. You will see each JSON-RPC call as it happens, including the task payload sent to the server, status update messages streamed back, and the final result envelope.</p>
<p>Common issues the Inspector surfaces immediately:</p>
<table>
  <thead>
      <tr>
          <th>Symptom</th>
          <th>Root cause</th>
          <th>Fix</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>401 on every request</td>
          <td>Wrong auth type or expired token</td>
          <td>Check <code>A2AAuthConfig.type</code> matches server expectation</td>
      </tr>
      <tr>
          <td>Task stuck in <code>working</code></td>
          <td>Remote agent timeout or exception</td>
          <td>Check server logs; add timeout to <code>A2AClientConfig</code></td>
      </tr>
      <tr>
          <td>Empty result payload</td>
          <td>Schema mismatch in response</td>
          <td>Inspect raw JSON; check <code>expected_output</code> format</td>
      </tr>
      <tr>
          <td>404 on agent card</td>
          <td>Wrong <code>server_url</code> or server not started</td>
          <td>Curl <code>{server_url}/.well-known/agent.json</code> manually</td>
      </tr>
  </tbody>
</table>
<p>The Inspector also records a session log you can export as JSON — useful for sharing a debugging trace with a teammate or filing a bug report against a third-party agent service.</p>
<h2 id="deploying-a2a-agents-to-production">Deploying A2A Agents to Production</h2>
<p>Production A2A deployments require three infrastructure decisions: where to host your A2A server agents, how to handle discovery, and how to manage agent card versioning. Google Cloud Run is the most common choice for A2A server agents in 2026 because it scales to zero between requests, supports streaming (required for A2A SSE), and has native integration with Google&rsquo;s Agent Engine for multi-agent orchestration at scale.</p>
<p>Here is a minimal <code>Dockerfile</code> for a CrewAI A2A server agent:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-dockerfile" data-lang="dockerfile"><span style="display:flex;"><span><span style="color:#66d9ef">FROM</span><span style="color:#e6db74"> python:3.12-slim</span><span style="color:#960050;background-color:#1e0010">
</span></span></span><span style="display:flex;"><span><span style="color:#960050;background-color:#1e0010">
</span></span></span><span style="display:flex;"><span><span style="color:#960050;background-color:#1e0010"></span><span style="color:#66d9ef">WORKDIR</span><span style="color:#e6db74"> /app</span><span style="color:#960050;background-color:#1e0010">
</span></span></span><span style="display:flex;"><span><span style="color:#960050;background-color:#1e0010"></span><span style="color:#66d9ef">COPY</span> requirements.txt .<span style="color:#960050;background-color:#1e0010">
</span></span></span><span style="display:flex;"><span><span style="color:#960050;background-color:#1e0010"></span><span style="color:#66d9ef">RUN</span> pip install --no-cache-dir -r requirements.txt<span style="color:#960050;background-color:#1e0010">
</span></span></span><span style="display:flex;"><span><span style="color:#960050;background-color:#1e0010">
</span></span></span><span style="display:flex;"><span><span style="color:#960050;background-color:#1e0010"></span><span style="color:#66d9ef">COPY</span> . .<span style="color:#960050;background-color:#1e0010">
</span></span></span><span style="display:flex;"><span><span style="color:#960050;background-color:#1e0010">
</span></span></span><span style="display:flex;"><span><span style="color:#960050;background-color:#1e0010"></span><span style="color:#66d9ef">ENV</span> PORT<span style="color:#f92672">=</span><span style="color:#ae81ff">8080</span>
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">EXPOSE</span><span style="color:#e6db74"> 8080</span><span style="color:#960050;background-color:#1e0010">
</span></span></span><span style="display:flex;"><span><span style="color:#960050;background-color:#1e0010">
</span></span></span><span style="display:flex;"><span><span style="color:#960050;background-color:#1e0010"></span><span style="color:#66d9ef">CMD</span> [<span style="color:#e6db74">&#34;uvicorn&#34;</span>, <span style="color:#e6db74">&#34;main:app&#34;</span>, <span style="color:#e6db74">&#34;--host&#34;</span>, <span style="color:#e6db74">&#34;0.0.0.0&#34;</span>, <span style="color:#e6db74">&#34;--port&#34;</span>, <span style="color:#e6db74">&#34;8080&#34;</span>]<span style="color:#960050;background-color:#1e0010">
</span></span></span></code></pre></div><p>Deploy to Cloud Run:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-bash" data-lang="bash"><span style="display:flex;"><span>gcloud run deploy crewai-a2a-agent <span style="color:#ae81ff">\
</span></span></span><span style="display:flex;"><span><span style="color:#ae81ff"></span>  --source . <span style="color:#ae81ff">\
</span></span></span><span style="display:flex;"><span><span style="color:#ae81ff"></span>  --region us-central1 <span style="color:#ae81ff">\
</span></span></span><span style="display:flex;"><span><span style="color:#ae81ff"></span>  --allow-unauthenticated <span style="color:#ae81ff">\
</span></span></span><span style="display:flex;"><span><span style="color:#ae81ff"></span>  --set-env-vars OPENAI_API_KEY<span style="color:#f92672">=</span>$OPENAI_API_KEY
</span></span></code></pre></div><p>For the discovery endpoint, host your <code>agent.json</code> at the service URL&rsquo;s <code>/.well-known/agent.json</code> path. Cloud Run handles HTTPS automatically, satisfying A2A&rsquo;s TLS requirement.</p>
<p>For multi-region deployments, put a load balancer in front of your Cloud Run services and ensure the agent card&rsquo;s <code>url</code> field points to the load balancer endpoint, not individual regional endpoints. This keeps the A2A discovery address stable across deployments.</p>
<p>Agent card versioning matters when you update an agent&rsquo;s capabilities. Increment the <code>version</code> field in your agent card and maintain backward compatibility for at least one version cycle — client agents may cache the card and not pick up new capabilities immediately.</p>
<h2 id="real-world-use-cases-for-interoperable-agents">Real-World Use Cases for Interoperable Agents</h2>
<p>A2A interoperability unlocks composite AI architectures that weren&rsquo;t practical before a common standard existed. The most valuable real-world patterns in 2026 fall into three categories: cross-team agent composition, cross-vendor delegation, and capability specialization. In enterprise settings, different teams often own different AI systems — the data team has LangGraph pipelines, the product team has CrewAI crews, and a vendor provides a specialized analysis agent. A2A lets all three participate in a single workflow without requiring any team to rewrite their agent stack. A cross-team research pipeline, for example, might have a CrewAI orchestrator crew dispatching subtasks to a LangGraph data retrieval graph and a third-party A2A-compliant summarization service, then assembling the results into a final report. Each component is independently deployed, versioned, and scaled. Cross-vendor delegation covers scenarios like routing complex financial analysis to a specialized vendor agent while keeping sensitive data processing on your own infrastructure — A2A&rsquo;s standardized auth makes this auditable and controllable. Capability specialization is the most granular pattern: one agent per specialized skill (web search, code execution, database query), all exposed as A2A servers, composed by a lightweight orchestrator that routes tasks to the right specialist. This &ldquo;skill bus&rdquo; architecture scales well because you add new capabilities by deploying a new A2A server — no changes to the orchestrator.</p>
<p>Concrete examples already running in production:</p>
<ul>
<li><strong>E-commerce fulfillment</strong>: CrewAI order management crew + LangGraph inventory agent + vendor shipping A2A service</li>
<li><strong>Legal document review</strong>: CrewAI case coordinator + specialized contract parsing A2A agent + compliance checker</li>
<li><strong>Software delivery</strong>: CrewAI project manager + GitHub Copilot Workspace A2A endpoint + test runner agent</li>
</ul>
<h2 id="faq">FAQ</h2>
<p><strong>What Python version does CrewAI A2A support?</strong>
CrewAI with A2A support requires Python 3.10 or higher. Python 3.12 is recommended for production deployments because it includes performance improvements that reduce latency for SSE streaming in high-throughput agent pipelines.</p>
<p><strong>Can I use CrewAI as an A2A server, not just a client?</strong>
Yes. CrewAI 0.80+ includes an <code>A2AServer</code> class that wraps your crew and exposes it as an A2A-compliant HTTP endpoint, including the <code>/.well-known/agent.json</code> discovery file. Any A2A-compliant client — LangGraph, Semantic Kernel, or a custom agent — can then delegate tasks to your crew.</p>
<p><strong>Is A2A the same as MCP (Model Context Protocol)?</strong>
No. MCP (developed by Anthropic) standardizes how agents access tools and resources. A2A standardizes how agents communicate with other agents. They are complementary: an agent might use MCP to access a database tool and A2A to delegate a subtask to another agent. Many frameworks, including CrewAI, support both.</p>
<p><strong>How does A2A handle long-running tasks that take minutes to complete?</strong>
A2A defines a task lifecycle (<code>submitted</code> → <code>working</code> → <code>completed</code>) with optional server-sent events for streaming intermediate updates. For tasks that take more than a few seconds, configure your <code>A2AClientConfig</code> with a <code>streaming=True</code> flag and handle the event stream in your crew&rsquo;s task callback. This prevents HTTP timeouts and gives users incremental progress feedback.</p>
<p><strong>What happens when an A2A server agent is unavailable?</strong>
CrewAI surfaces an <code>A2AConnectionError</code> or <code>A2ATaskError</code> depending on whether the failure is at the connection or task execution layer. Wrap your crew&rsquo;s <code>kickoff()</code> in a try/except block and implement retry logic with exponential backoff. For production systems, add a circuit breaker — if the remote agent fails more than N times in a window, stop sending requests and alert the on-call engineer.</p>
]]></content:encoded></item></channel></rss>