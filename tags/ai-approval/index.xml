<?xml version="1.0" encoding="utf-8" standalone="yes"?><rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom" xmlns:content="http://purl.org/rss/1.0/modules/content/"><channel><title>AI Approval on RockB</title><link>https://baeseokjae.github.io/tags/ai-approval/</link><description>Recent content in AI Approval on RockB</description><image><title>RockB</title><url>https://baeseokjae.github.io/images/og-default.png</url><link>https://baeseokjae.github.io/images/og-default.png</link></image><generator>Hugo</generator><language>en-us</language><lastBuildDate>Sat, 05 Sep 2026 07:01:36 +0000</lastBuildDate><atom:link href="https://baeseokjae.github.io/tags/ai-approval/index.xml" rel="self" type="application/rss+xml"/><item><title>dsh-auto-review: Second-Model AI Review for DeepSeek Harness Approval Requests</title><link>https://baeseokjae.github.io/posts/dsh-auto-review-second-model-approval-2026/</link><pubDate>Sat, 05 Sep 2026 07:01:36 +0000</pubDate><guid>https://baeseokjae.github.io/posts/dsh-auto-review-second-model-approval-2026/</guid><description>dsh-auto-review puts a read-only second model on DeepSeek Harness approval requests, returning a structured allow/deny verdict with reasons and failing closed by default.</description><content:encoded><![CDATA[<p>dsh-auto-review is a DeepSeek Harness plugin that puts a read-only second model on the approval chain, deciding allow or deny for every tool request and returning a structured verdict with a reason. It fails closed by default, so any crash, timeout, or schema mismatch resolves to rejected rather than a silent grant, and it feeds deny reasons back to the calling model so the agent learns instead of retrying blindly.</p>
<h2 id="what-is-dsh-auto-review-and-why-a-second-model">What is dsh-auto-review and why a second model?</h2>
<p>dsh-auto-review is one of 33 DeepSeek Harness plugins maintained by PerryLink, and it plugs directly into the approval/request waterfall of DeepSeek Harness, the host platform that has grown to 212,530+ GitHub stars. Instead of relying on pattern-based rules alone, it introduces a second, independent model as a read-only reviewer subagent that examines actual workspace evidence before deciding whether a tool call should proceed.</p>
<p>The core idea is that a second model adds a layer of judgment that static rules cannot. A regex rule can catch a known-bad pattern, but it cannot reason about context, intent, or whether a command is safe given the current state of the repository. The reviewer reads real files, globs the workspace, and greps for evidence, then returns a structured verdict with a reason. That reason is what makes the difference: the calling model learns why a request was denied and can adjust, rather than blindly retrying the same action.</p>
<h2 id="how-the-approval-chain-works-answerer-waterfall-aihumannever-policies">How the approval chain works (answerer waterfall, ai/human/never policies)</h2>
<p>DeepSeek Harness routes every tool request through an answerer chain, and dsh-auto-review inserts itself as a second-model reviewer on that chain. The plugin is session- and tool-policy-scoped for the interactive harness, and it never infers durable grants. Every tool call is evaluated against a per-tool policy that can be one of three values:</p>
<ul>
<li><strong>ai</strong> — the second-model reviewer decides allow or deny autonomously.</li>
<li><strong>human</strong> — the request is delegated to the human approval chain.</li>
<li><strong>never</strong> — the tool is hard-disabled and can never be approved.</li>
</ul>
<p>Out of the box, the shipped patch AI-reviews <code>bash</code> and <code>write</code>; every other tool, including <code>edit</code>, delegates to the human chain. This is a deliberately conservative default: only the two highest-risk tools are handed to the autonomous reviewer, and everything else keeps a human in the loop.</p>
<p>The plugin delegates everything it does not own via <code>next()</code>, so the rest of the answerer waterfall continues to work normally. This is a key design point — dsh-auto-review does not replace the approval chain, it sits on it and makes a decision for the tools it is configured to review.</p>
<h2 id="installing-dsh-auto-review">Installing dsh-auto-review</h2>
<p>dsh-auto-review requires Node <code>^22.19 || &gt;=24.0.0</code> and DeepSeek Harness <code>0.1.2-rc.1</code> (peers <code>&gt;=0.1.0-rc.8 &lt;0.2.0</code>). There are several installation channels:</p>
<table>
  <thead>
      <tr>
          <th>Channel</th>
          <th>Command</th>
          <th>Notes</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>Git</td>
          <td><code>git clone https://github.com/PerryLink/dsh-auto-review</code></td>
          <td>Full source, best for development</td>
      </tr>
      <tr>
          <td>npm</td>
          <td><code>npm install dsh-auto-review</code></td>
          <td>Standard package install</td>
      </tr>
      <tr>
          <td>1024 store</td>
          <td><code>dsh plugin install dsh-auto-review</code></td>
          <td>DeepSeek Harness plugin store</td>
      </tr>
      <tr>
          <td>Tarball</td>
          <td><code>npm pack</code> / direct tarball</td>
          <td>For offline or pinned installs</td>
      </tr>
  </tbody>
</table>
<p>After installing, you enable the plugin in your DeepSeek Harness configuration and point it at your <code>cordis.yml</code> file, where all routing and safety settings live. The plugin ships with a patch that applies the default <code>ai</code> policy to <code>bash</code> and <code>write</code>, so you can start reviewing immediately and then tune the policy to your needs.</p>
<h2 id="configuring-toolspolicy-riskrules-and-reviewer-settings-in-cordisyml">Configuring toolsPolicy, riskRules, and reviewer settings in cordis.yml</h2>
<p>All routing and safety configuration lives in <code>cordis.yml</code>, which makes the whole system config-driven and changeable without touching code. The three main sections are:</p>
<p><strong>toolsPolicy</strong> — maps each tool to <code>ai</code>, <code>human</code>, or <code>never</code>. This is where you decide which tools the second model reviews autonomously and which stay on the human chain.</p>
<p><strong>riskRules</strong> — regex-based rules that flag risky arguments or patterns. These run before the model review and can force a denial or escalate a request to human review based on what the tool is about to do.</p>
<p><strong>reviewer settings</strong> — configuration for the reviewer subagent itself, including the model to use, the timeout, and the fallback policy.</p>
<p>A typical configuration looks like this:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-yaml" data-lang="yaml"><span style="display:flex;"><span><span style="color:#f92672">toolsPolicy</span>:
</span></span><span style="display:flex;"><span>  <span style="color:#f92672">bash</span>: <span style="color:#ae81ff">ai</span>
</span></span><span style="display:flex;"><span>  <span style="color:#f92672">write</span>: <span style="color:#ae81ff">ai</span>
</span></span><span style="display:flex;"><span>  <span style="color:#f92672">edit</span>: <span style="color:#ae81ff">human</span>
</span></span><span style="display:flex;"><span>  <span style="color:#f92672">rm</span>: <span style="color:#ae81ff">never</span>
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#f92672">riskRules</span>:
</span></span><span style="display:flex;"><span>  - <span style="color:#f92672">pattern</span>: <span style="color:#e6db74">&#34;rm -rf /&#34;</span>
</span></span><span style="display:flex;"><span>    <span style="color:#f92672">action</span>: <span style="color:#ae81ff">deny</span>
</span></span><span style="display:flex;"><span>  - <span style="color:#f92672">pattern</span>: <span style="color:#e6db74">&#34;curl.*\\|.*sh&#34;</span>
</span></span><span style="display:flex;"><span>    <span style="color:#f92672">action</span>: <span style="color:#ae81ff">human</span>
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#f92672">reviewer</span>:
</span></span><span style="display:flex;"><span>  <span style="color:#f92672">model</span>: <span style="color:#ae81ff">deepseek-reviewer</span>
</span></span><span style="display:flex;"><span>  <span style="color:#f92672">timeoutMs</span>: <span style="color:#ae81ff">60000</span>
</span></span><span style="display:flex;"><span>  <span style="color:#f92672">fallbackPolicy</span>: <span style="color:#ae81ff">rejected</span>
</span></span></code></pre></div><p>The default reviewer timeout is 60000ms, and the default <code>fallbackPolicy</code> is <code>rejected</code>, meaning the system fails closed. You can change any of these values from <code>cordis.yml</code> without rebuilding or redeploying the plugin.</p>
<h2 id="the-read-only-reviewer-subagent-verdicts-redaction-and-context-isolation">The read-only reviewer subagent: verdicts, redaction, and context isolation</h2>
<p>The reviewer is a read-only subagent with a strict tool allow-list: <code>read</code>, <code>glob</code>, and <code>grep</code>. It cannot write, edit, run bash, fetch the network, or delegate. This is a hard security boundary — the decision-maker has no way to mutate the system it is judging.</p>
<p>When a tool request arrives, the reviewer examines the workspace, gathers evidence, and returns a structured verdict with three fields:</p>
<ul>
<li><strong>decision</strong> — <code>allow</code> or <code>deny</code></li>
<li><strong>reason</strong> — a human-readable explanation of the decision</li>
<li><strong>riskLevel</strong> — a severity rating for the request</li>
</ul>
<p>Two additional protections make the reviewer trustworthy. First, sensitive arguments are redacted before they enter the reviewer prompt: tokens, passwords, <code>api_key</code>, <code>Authorization</code> headers, credentials, and private keys are stripped so secrets never reach the decision-maker. Second, workspace instruction files such as <code>AGENTS.md</code> and <code>CLAUDE.md</code> are filtered out, so repository-controlled text cannot influence the reviewer. This context isolation means the second model judges the request on its own evidence, not on instructions that a repository owner could inject.</p>
<h2 id="fail-closed-behavior-and-fallbackpolicy">Fail-closed behavior and fallbackPolicy</h2>
<p>The most important safety property of dsh-auto-review is that it fails closed. Every abnormal path resolves to <code>rejected</code> by default:</p>
<ul>
<li><strong>Reviewer crash</strong> — if the reviewer process dies, the request is denied.</li>
<li><strong>Timeout</strong> — if the reviewer exceeds the 60000ms default timeout, the request is denied.</li>
<li><strong>Schema mismatch</strong> — if the reviewer returns a verdict that does not match the expected structure, the request is denied.</li>
</ul>
<p>In all three cases, the request resolves through <code>fallbackPolicy</code>, which defaults to <code>rejected</code>. There is no silent grant path. If the second model cannot make a decision, the safe answer is no. This is the opposite of fail-open systems, where an error might let a dangerous command through because the guard failed.</p>
<h2 id="safety-knobs-circuit-breaker-risk-policy-auto-review-approve-override">Safety knobs: circuit breaker, risk policy, /auto-review approve override</h2>
<p>Beyond the core allow/deny decision, dsh-auto-review ships several safety knobs that let you tune how aggressive or conservative the review is:</p>
<p><strong>Rejection circuit breaker</strong> — the default is 3 consecutive denials, or 6 of the last 10 verdicts, per turn. When the breaker trips, the reviewer stops accepting requests for that turn, preventing a runaway loop of denied actions from consuming resources or masking a deeper problem.</p>
<p><strong>Risk-level policy</strong> — you can set a maximum acceptable risk level. Requests above that level are denied or escalated regardless of the reviewer&rsquo;s verdict, giving you a hard ceiling on what the second model is allowed to approve.</p>
<p><strong>One-shot <code>/auto-review approve</code> override</strong> — a single explicit command that approves a specific request, bypassing the reviewer for that one call. This is a deliberate escape hatch for cases where a human has verified the action and wants to proceed without waiting for the model.</p>
<p><strong>Never-policy hard disable</strong> — for tools you never want used, the <code>never</code> policy is a hard disable. No override, no escalation, no approval path. The tool simply cannot run.</p>
<h2 id="audit-trail-and-session-log-reconstruction">Audit trail and session-log reconstruction</h2>
<p>Every decision is recorded in the session log, and the full audit trail can be reconstructed from three event types:</p>
<ul>
<li><strong>approval/asked</strong> — the tool request entered the approval chain.</li>
<li><strong>autoReview/verdict</strong> — the reviewer returned its structured verdict.</li>
<li><strong>approval/decided</strong> — the final allow/deny decision was made.</li>
</ul>
<p>By correlating these events, you can reconstruct exactly what was requested, what the second model decided, and what the final outcome was. This is critical for compliance, debugging, and understanding why an agent behaved a certain way. The deny reasons are also fed back to the calling model, so the audit trail captures not just the decision but the reasoning behind it.</p>
<h2 id="the-standalone-mcp-server-for-external-clients">The standalone MCP server for external clients</h2>
<p>dsh-auto-review also ships a standalone MCP server that exposes the same deterministic rules and cache to external clients such as Claude or Codex. This server runs the rule-based layer — <code>toolsPolicy</code>, <code>riskRules</code>, and the cache — without invoking a model review. It is useful when you want the deterministic guardrails of dsh-auto-review outside the DeepSeek Harness environment, or when you want a lightweight, model-free approval layer for other tools.</p>
<p>The MCP server is a good fit for teams that want the policy and risk-rule enforcement without the cost or latency of a second-model call on every request.</p>
<h2 id="dsh-auto-review-vs-dsh-auto-approval-which-to-choose">dsh-auto-review vs dsh-auto-approval: which to choose</h2>
<p>There are two similar plugins in the ecosystem, and they differ in important ways. The table below summarizes the key differences:</p>
<table>
  <thead>
      <tr>
          <th>Aspect</th>
          <th>dsh-auto-review</th>
          <th>dsh-auto-approval</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>Position</td>
          <td>Official answerer chain</td>
          <td>Tools/pre-execute waterfall</td>
      </tr>
      <tr>
          <td>Decision model</td>
          <td>Read-only second model, structured verdict</td>
          <td>Two-state allow/deny classifier</td>
      </tr>
      <tr>
          <td>Delegation</td>
          <td>Always delegates what it does not own via <code>next()</code></td>
          <td>Monorepo of host + client UI halves</td>
      </tr>
      <tr>
          <td>Output</td>
          <td><code>{decision, reason, riskLevel}</code></td>
          <td>Allow/deny only</td>
      </tr>
      <tr>
          <td>Deny reasons</td>
          <td>Fed back to calling model</td>
          <td>Not fed back</td>
      </tr>
      <tr>
          <td>Audit</td>
          <td>Session-log events</td>
          <td>File-log audit</td>
      </tr>
      <tr>
          <td>Scope</td>
          <td>Session- and tool-policy-scoped</td>
          <td>Classifies every tool call</td>
      </tr>
  </tbody>
</table>
<p>If you want a second model that reasons about evidence, explains its decisions, and feeds reasons back to the agent, dsh-auto-review is the stronger choice. If you want a lightweight two-state classifier that runs on the pre-execute waterfall with a file-log audit, dsh-auto-approval may be simpler. For most teams that already use DeepSeek Harness, dsh-auto-review&rsquo;s official answerer-chain integration and structured verdicts make it the more capable option.</p>
<h2 id="known-limitations-and-security-boundaries">Known limitations and security boundaries</h2>
<p>dsh-auto-review is powerful, but it has clear boundaries you should understand before relying on it:</p>
<ul>
<li><strong>It is not a human replacement.</strong> The <code>ai</code> policy is for tools you trust the second model to judge. High-stakes or irreversible actions should stay on the <code>human</code> chain.</li>
<li><strong>It never infers durable grants.</strong> Every decision is scoped to the current session and tool policy. There is no persistent &ldquo;trusted&rdquo; state that carries across sessions.</li>
<li><strong>The reviewer is read-only by design.</strong> It can only read, glob, and grep. It cannot verify a command by running it, so its judgment is based on static evidence.</li>
<li><strong>Fail-closed means false denials are possible.</strong> A timeout or crash denies a request even if it was safe. This is the intended trade-off for safety.</li>
<li><strong>The MCP server has no model review.</strong> If you use the standalone server, you get deterministic rules only, not the second-model judgment.</li>
</ul>
<p>The security boundary is deliberately conservative: the reviewer cannot mutate anything, secrets are redacted, and repository-controlled instruction files are filtered out. This makes dsh-auto-review a strong guardrail, but you should still pair it with human oversight for the most sensitive operations.</p>
<h2 id="faq">FAQ</h2>
<p><strong>What is dsh-auto-review?</strong>
dsh-auto-review is a DeepSeek Harness plugin that adds a read-only second-model reviewer to the approval chain. It decides allow or deny for tool requests and returns a structured verdict with a reason, failing closed by default.</p>
<p><strong>How does the second model review work?</strong>
The reviewer subagent uses a read-only tool allow-list (read, glob, grep) to examine workspace evidence, then returns a structured verdict with decision, reason, and riskLevel. Sensitive arguments are redacted and repository instruction files are filtered out before the request reaches the model.</p>
<p><strong>What does fail closed mean in dsh-auto-review?</strong>
Fail closed means any abnormal path — reviewer crash, timeout, or schema mismatch — resolves to rejected through the fallbackPolicy, which defaults to rejected. There is no silent grant path; if the reviewer cannot decide, the safe answer is no.</p>
<p><strong>Which tools does dsh-auto-review review by default?</strong>
Out of the box, the shipped patch AI-reviews <code>bash</code> and <code>write</code>. Every other tool, including <code>edit</code>, delegates to the human approval chain. You can change this in <code>cordis.yml</code> with per-tool <code>ai</code>, <code>human</code>, or <code>never</code> policies.</p>
<p><strong>How do I install dsh-auto-review?</strong>
You can install it via git clone, npm, the 1024 plugin store, or a tarball. It requires Node <code>^22.19 || &gt;=24.0.0</code> and DeepSeek Harness <code>0.1.2-rc.1</code> (peers <code>&gt;=0.1.0-rc.8 &lt;0.2.0</code>).</p>
]]></content:encoded></item></channel></rss>