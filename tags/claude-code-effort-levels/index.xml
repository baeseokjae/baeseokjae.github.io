<?xml version="1.0" encoding="utf-8" standalone="yes"?><rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom" xmlns:content="http://purl.org/rss/1.0/modules/content/"><channel><title>Claude Code Effort Levels on RockB</title><link>https://baeseokjae.github.io/tags/claude-code-effort-levels/</link><description>Recent content in Claude Code Effort Levels on RockB</description><image><title>RockB</title><url>https://baeseokjae.github.io/images/og-default.png</url><link>https://baeseokjae.github.io/images/og-default.png</link></image><generator>Hugo</generator><language>en-us</language><lastBuildDate>Tue, 25 Aug 2026 22:01:56 +0000</lastBuildDate><atom:link href="https://baeseokjae.github.io/tags/claude-code-effort-levels/index.xml" rel="self" type="application/rss+xml"/><item><title>Claude Code Effort Levels A/B Test: What Reduced Effort Means for Coding Agents</title><link>https://baeseokjae.github.io/posts/claude-code-effort-ab-test/</link><pubDate>Tue, 25 Aug 2026 22:01:56 +0000</pubDate><guid>https://baeseokjae.github.io/posts/claude-code-effort-ab-test/</guid><description>Claude Code&amp;#39;s reduced-effort A/B test makes coding agents less proactive, not less smart. Learn what effort levels control, when low effort helps or hurts, and how to override.</description><content:encoded><![CDATA[<p>Anthropic is quietly A/B testing reduced default effort levels in Claude Code, and the change does not make the coding agent dumber — it makes it less proactive. Effort controls how much autonomous work Claude performs per turn (reading files, running tests, double-checking its own output) before responding or asking for context. When the default drops, you get faster, cheaper turns that skip deep investigation, which is fine for scoped tasks but can silently degrade complex multi-file refactors.</p>
<h2 id="what-are-claude-code-effort-levels">What Are Claude Code Effort Levels?</h2>
<p>Effort is the single most misunderstood knob in Claude Code because people confuse it with model quality. The clearest mental model comes straight from Anthropic&rsquo;s own documentation: <strong>model = how capable, effort = how thorough</strong>. Switching from a high-effort setting to a low-effort setting does not swap in a weaker model; it changes how much work Claude decides to do on each turn before it answers.</p>
<p>At higher effort, Claude takes more actions before responding. It reads files, runs tests, re-checks its reasoning, and double-checks edge cases. At lower effort, it prefers to ask you for more context rather than spend tokens investigating on its own. The official Claude Code blog frames it as a trade-off between autonomy and token spend: &ldquo;at lower effort, it asks for more context rather than spending tokens.&rdquo;</p>
<p>This distinction matters because it changes how you diagnose problems. If Claude skips a file you expected it to read, or skips running tests, that is usually an effort problem, not a capability problem. The model understands the task; it is just choosing not to be thorough about it.</p>
<h2 id="the-ab-test-what-reduced-effort-actually-changes">The A/B Test: What &ldquo;Reduced Effort&rdquo; Actually Changes</h2>
<p>Anthropic has been running A/B tests on reduced — meaning lower — default effort levels inside Claude Code. A 215-point Hacker News story documented exactly this: &ldquo;Anthropic appears to be A/B testing reduced effort levels in Claude Code.&rdquo; What this means in practice is that some users, some sessions, or some model versions get a lower default than the traditional &ldquo;high,&rdquo; and many of them never notice until output quality changes.</p>
<p>The concrete behavioral change is straightforward. Under reduced effort, Claude does <strong>less autonomous work per turn</strong>:</p>
<ul>
<li>Fewer file reads before proposing a change</li>
<li>Fewer test runs before declaring a change complete</li>
<li>Fewer double-checks of its own output</li>
<li>A stronger tendency to ask you clarifying questions instead of figuring things out</li>
</ul>
<p>The A/B test is experimental, which is why you may see inconsistent behavior across sessions on the same project. One session might default to high and thoroughly inspect everything; the next defaults lower and &ldquo;bails&rdquo; early. This inconsistency is the tell-tale sign you have been put into the reduced-effort bucket.</p>
<h2 id="effort-levels-by-model-and-how-fallback-works">Effort Levels by Model and How Fallback Works</h2>
<p>Effort is not a single scale — supported levels depend on which model you are running. According to the official model configuration documentation, the supported sets are:</p>
<table>
  <thead>
      <tr>
          <th>Model</th>
          <th>Supported effort levels</th>
          <th>Default</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>Fable 5 / Opus 5 / Sonnet 5</td>
          <td>low, medium, high, xhigh, max</td>
          <td>high</td>
      </tr>
      <tr>
          <td>Opus 4.7</td>
          <td>low, medium, high, xhigh, max</td>
          <td><strong>xhigh</strong></td>
      </tr>
      <tr>
          <td>Opus 4.6 / Sonnet 4.6</td>
          <td>low, medium, high, max</td>
          <td>high</td>
      </tr>
  </tbody>
</table>
<p>A few things stand out. Opus 4.7 is the only model that defaults to xhigh rather than high. And the fallback behavior is important: if you set an effort level your current model does not support, Claude Code falls back to the <strong>highest supported level at or below</strong> what you asked for. So if you request &ldquo;max&rdquo; on a model that only goes up to &ldquo;high,&rdquo; you get high — not an error.</p>
<p>This fallback is a common source of confusion in A/B-style workflows. Users report setting xhigh or max and assuming they got it, when the model silently downgraded the request. Always confirm the level your model actually supports before relying on the setting.</p>
<h2 id="how-effort-affects-tokens-cost-and-latency">How Effort Affects Tokens, Cost, and Latency</h2>
<p>The headline number in Anthropic&rsquo;s documentation is stark: the high-effort path generates <strong>roughly 7x more tokens</strong> than low effort to reach a higher-confidence answer. That 7x multiplier is why effort tuning is so tempting as a cost lever — and why the naive &ldquo;always lower the effort&rdquo; approach can backfire.</p>
<p>Independent benchmarking backs this up. A 200-run headless Claude Code benchmark (5 prompt variants x 2 effort anchors x 2 model sides x 10 prompts, comparing Opus 4.6 vs 4.7) found that <strong>cost was the clearest signal — and the cheapest path was not &ldquo;always lower effort.&rdquo;</strong> High effort with fewer or no tool calls often cost less than medium effort with aggressive tool use.</p>
<p>Why? Two mechanisms dominate cost:</p>
<ol>
<li><strong>Cache writes.</strong> Reading files and processing tool results balloons the prompt cache on every turn. Each tool result gets re-sent, and cache writes are expensive.</li>
<li><strong>Tool use frequency.</strong> Sessions that call tools constantly accumulate mid-session context that has to be re-processed.</li>
</ol>
<p>The benchmark&rsquo;s most counterintuitive finding was that a &ldquo;concise&rdquo; prompt <em>increased</em> tool calls at medium effort, while a &ldquo;no-tools&rdquo; prompt eliminated mid-session context accumulation. Output tokens did not explain cost either — cheaper runs did not always produce shorter answers.</p>
<table>
  <thead>
      <tr>
          <th>Strategy</th>
          <th>Observed cost impact</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>Always lowest effort</td>
          <td>Not reliably cheapest; tool use can dominate</td>
      </tr>
      <tr>
          <td>High effort + few tool calls</td>
          <td>Often cheaper than medium + heavy tool use</td>
      </tr>
      <tr>
          <td>Prompt suppression (no-tools)</td>
          <td>Eliminated mid-session context growth</td>
      </tr>
      <tr>
          <td>ultrathink</td>
          <td>Slow everywhere, did not reduce cost</td>
      </tr>
  </tbody>
</table>
<p>The practical takeaway: effort is one input to cost, but <strong>tool-use behavior and prompt cache are often the real levers</strong>. If you want to cut spend, first reduce unnecessary tool calls before you hammer effort down.</p>
<h2 id="when-reduced-effort-is-fine-vs-when-it-breaks">When Reduced Effort Is Fine vs When It Breaks</h2>
<p>Reduced effort is not uniformly good or bad — it is a trade-off that depends on the task shape. The important insight is that quality degrades predictably in some situations and stays fine in others.</p>
<p><strong>Reduced effort is usually fine for:</strong></p>
<ul>
<li>Single-file edits with clear scope</li>
<li>Simple Q&amp;A about your codebase</li>
<li>Boilerplate generation</li>
<li>Repetitive, well-specified transformations</li>
</ul>
<p><strong>Reduced effort tends to break for:</strong></p>
<ul>
<li>Multi-file refactors where the agent must trace dependencies</li>
<li>Test-heavy workflows where skipping tests hides regressions</li>
<li>Tasks with implicit requirements the agent must discover by reading</li>
<li>Anything where &ldquo;bailing&rdquo; early produces a plausible-but-wrong answer</li>
</ul>
<p>The 200-run benchmark found both high-effort Opus models passed every testable prompt (0 failures across high-effort runs), while medium-effort runs had instruction-following (IFEval) constraint failures. That is a clean signal: lower effort increases the chance of subtle, spec-violating output that looks correct at a glance.</p>
<h2 id="how-to-detect-your-session-is-on-reduced-effort">How to Detect Your Session Is on Reduced Effort</h2>
<p>Because the A/B test is experimental, you cannot always trust that your session is on the default you expect. Here are the concrete behavioral signals that your session is running reduced effort:</p>
<ul>
<li>Claude proposes changes <strong>without reading</strong> the relevant files first</li>
<li>Claude declares a change complete <strong>without running tests</strong></li>
<li>Claude asks you clarifying questions that it should be able to answer from the repo</li>
<li>The same prompt that previously produced a thorough multi-step plan now returns a single, shallow answer</li>
<li>Behavior is inconsistent session-to-session on the same project</li>
</ul>
<p>If you see a pattern of skipped files or skipped tests, that is the reduced-effort fingerprint. It is the exact scenario Anthropic&rsquo;s guidance warns about: &ldquo;if Claude skipped a file or tests, raise effort.&rdquo;</p>
<h2 id="how-to-override-effort">How to Override Effort</h2>
<p>You are not stuck with whatever default the A/B test assigned you. Effort can be controlled three ways, per the official documentation:</p>
<ol>
<li><strong><code>/effort</code></strong> — the interactive slash command inside Claude Code</li>
<li><strong><code>--effort</code></strong> — the CLI flag when launching a session</li>
<li><strong><code>effortLevel</code></strong> — the setting in <code>settings.json</code> for persistent configuration</li>
</ol>
<p>Organizations can also <strong>cap available levels</strong> via Organization effort limits. If you work under an org policy, the cap shows up in the supported set for your session, and requests above the cap silently fall back to the highest allowed level.</p>
<p>If you are in an A/B bucket with a reduced default and want the thorough behavior back, set <code>effortLevel: &quot;high&quot;</code> (or xhigh/max on models that support them) explicitly rather than relying on the default. Because effort is a per-request value sent to the model alongside the prompt, overriding it re-establishes the behavior you want for that session.</p>
<h2 id="a-practical-ab-test-playbook-for-your-own-coding-workflows">A Practical A/B Test Playbook for Your Own Coding Workflows</h2>
<p>Instead of fighting the global A/B test, run your own. Treat effort as a manual override and measure the actual effect on your real workload:</p>
<ol>
<li><strong>Start at the default.</strong> Do not touch effort on routine, well-scoped tasks.</li>
<li><strong>Raise effort when Claude &ldquo;bails.&rdquo;</strong> If it skips files or tests, bump to high/xhigh before you correct it manually.</li>
<li><strong>Measure cost, not just effort.</strong> Log token usage per session. The cheapest configuration is often high effort with disciplined tool use, not the lowest effort.</li>
<li><strong>Use prompt suppression for no-tool tasks.</strong> When a task does not need tool calls, suppress them rather than dropping effort — this removes the biggest cost driver.</li>
<li><strong>Compare apples to apples.</strong> Run the same prompt at low and high effort on a fixed task, and grade both the output quality and the token bill, not just the wall-clock latency.</li>
</ol>
<p>This playbook mirrors what the independent benchmark found: effort tuning is only effective when you control tool use and cache behavior in the same experiment. Isolate the variable before you draw conclusions.</p>
<h2 id="common-pitfalls-and-community-findings">Common Pitfalls and Community Findings</h2>
<p>The community has surfaced several gotchas worth knowing before you tune:</p>
<ul>
<li><strong><code>/effort</code> is global, not per-session.</strong> A GitHub issue documented that <code>/effort</code> applies across concurrent sessions instead of being scoped to one session. This breaks A/B-style parallel workflows where you want different effort levels on different tasks at once. Be aware that setting effort in one terminal can leak into another.</li>
<li><strong>&ldquo;Concise&rdquo; prompts can backfire.</strong> In the benchmark, a &ldquo;concise&rdquo; instruction increased tool calls at medium effort, the opposite of the intended effect.</li>
<li><strong>ultrathink is not a cost reducer.</strong> It was slow everywhere and did not help cost, so do not reach for it as a savings lever.</li>
<li><strong>Effort fallback is silent.</strong> Unsupported levels downgrade without error; verify your model&rsquo;s supported set.</li>
<li><strong>Reduced effort and quality.</strong> The HN discussion centered on whether lower defaults degrade real coding output. The benchmark evidence says yes for constraint-heavy tasks, so treat reduced defaults with skepticism on complex work.</li>
</ul>
<p>There is even emerging tooling — an &ldquo;Effort Router&rdquo; that selects effort per Claude turn — a sign that developers want finer-grained control than a single global setting.</p>
<h2 id="conclusion--tune-effort-like-a-manual-override-not-a-global-switch">Conclusion — Tune Effort Like a Manual Override, Not a Global Switch</h2>
<p>Anthropic&rsquo;s reduced-effort A/B test is a reminder that effort is a behavioral dial, not a quality dial. Reduced effort does not make Claude less capable; it makes it less proactive — fewer file reads, fewer test runs, more questions back at you. That is ideal for scoped, routine tasks and quietly dangerous for complex refactors and test-heavy work.</p>
<p>The most reliable strategy is to stop relying on the default entirely. Start at whatever effort the session gives you, watch for the &ldquo;bail&rdquo; signals, and raise effort explicitly when Claude skips files or tests. Manage cost through tool-use discipline and prompt cache behavior rather than reflexively dropping effort. Model decides how capable the answer can be; effort decides how hard the agent works to get it right. Tune the override with that distinction in mind, and you get the thoroughness you need only when you actually need it.</p>
<h2 id="faq">FAQ</h2>
<p><strong>What do Claude Code effort levels actually control?</strong>
Effort controls how much autonomous work Claude does per turn before responding — reading files, running tests, and double-checking its output. Model decides capability; effort decides thoroughness.</p>
<p><strong>What is the Claude Code effort A/B test?</strong>
Anthropic has been A/B testing reduced (lower) default effort levels in Claude Code. Some sessions default to a lower level than traditional &ldquo;high,&rdquo; making Claude less proactive without changing the underlying model.</p>
<p><strong>Which effort levels does each Claude model support?</strong>
Fable 5, Opus 5, and Sonnet 5 support low, medium, high, xhigh, and max. Opus 4.7 also supports all five and defaults to xhigh. Opus 4.6 and Sonnet 4.6 support low, medium, high, and max, defaulting to high.</p>
<p><strong>How much more expensive is high effort?</strong>
Anthropic reports the high-effort path generates roughly 7x more tokens than low effort. However, independent benchmarks found cost is dominated by tool use and prompt cache behavior, so &ldquo;always low effort&rdquo; is not reliably the cheapest path.</p>
<p><strong>How do I change the effort level in Claude Code?</strong>
Use the <code>/effort</code> slash command, the <code>--effort</code> CLI flag, or the <code>effortLevel</code> setting in <code>settings.json</code>. Organizations can cap available levels, and unsupported levels silently fall back to the highest supported level at or below your request.</p>
]]></content:encoded></item></channel></rss>