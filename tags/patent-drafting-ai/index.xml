<?xml version="1.0" encoding="utf-8" standalone="yes"?><rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom" xmlns:content="http://purl.org/rss/1.0/modules/content/"><channel><title>Patent Drafting AI on RockB</title><link>https://baeseokjae.github.io/tags/patent-drafting-ai/</link><description>Recent content in Patent Drafting AI on RockB</description><image><title>RockB</title><url>https://baeseokjae.github.io/images/og-default.png</url><link>https://baeseokjae.github.io/images/og-default.png</link></image><generator>Hugo</generator><language>en-us</language><lastBuildDate>Mon, 07 Sep 2026 16:03:19 +0000</lastBuildDate><atom:link href="https://baeseokjae.github.io/tags/patent-drafting-ai/index.xml" rel="self" type="application/rss+xml"/><item><title>Legal Skills Markdown Agents: Replacing Lawyers with Markdown Files</title><link>https://baeseokjae.github.io/posts/legal-skills-replacing-lawyers-markdown/</link><pubDate>Mon, 07 Sep 2026 16:03:19 +0000</pubDate><guid>https://baeseokjae.github.io/posts/legal-skills-replacing-lawyers-markdown/</guid><description>Legal skills markdown agents turn legal work into executable SKILL.md playbooks and versioned law files, displacing billable hours with a repo checkout.</description><content:encoded><![CDATA[<p>Legal skills markdown agents are turning legal work into executable playbooks: a public repo called <code>legal-skills</code> packages U.S. utility-patent work as SKILL.md files, checklists, and deterministic Python scripts that run a 316-item pre-filing audit without opening a matter. The claim is blunt — displace legal fees, not assist counsel — and it sits on a broader movement where legislation itself becomes versioned Markdown in git. This guide explains what the pattern is, why Markdown is the right format, where agents still fail, and what stays reserved to human lawyers.</p>
<h2 id="what-replacing-lawyers-with-markdown-files-actually-means">What &ldquo;Replacing Lawyers with Markdown Files&rdquo; Actually Means</h2>
<p>The phrase sounds like a joke, but it describes a concrete engineering pattern. Instead of paying a firm to run a process, you encode the process itself as files: a <code>SKILL.md</code> that tells an AI agent what to do, checklists that enumerate every step, intake questionnaires that gather facts, and deterministic scripts that enforce the mechanical parts. The <code>legal-skills</code> repository by Greg Fodor is the clearest example. It frames patent work as a document pipeline that firms wrap in scarcity, letterhead, and a rate card — and then unwraps it.</p>
<p>The goal is not to make lawyers faster. It is to make the lawyer&rsquo;s process runnable by a model that costs less than lunch. Drafting and filing a U.S. utility patent application can cost roughly $18,000 at a firm; the repo offers a checkout-and-run alternative. A patent boutique associate is billed at about $400/hour for reading a draft once. When the process lives in versioned files, that reading is done by an agent that never bills an hour.</p>
<p>This is a different claim from &ldquo;AI assists lawyers.&rdquo; It is the claim that most of what clients pay for is process, and process belongs in files — not firms.</p>
<h2 id="why-markdown-is-the-right-format-for-legal-work">Why Markdown Is the Right Format for Legal Work</h2>
<p>Markdown is plain text with lightweight structure: headings, lists, tables, and links. That makes it the ideal substrate for legal work for three reasons.</p>
<p>First, it is diffable. When a contract or a statute is a Markdown file, every change is a visible diff. You can see exactly what a revision altered, who made it, and when. This is the core value proposition of the &ldquo;legislation as code&rdquo; family: <code>legalize-es</code> (Spanish legislation in Markdown, versioned as git) has 1,879 GitHub stars, and <code>legalize-kr</code> (Korean) has 1,518. Each law is a file, each reform a commit, each promulgation date a real git timestamp.</p>
<p>Second, it is auditable. Git history is an immutable ledger of who changed what. For legal documents, where provenance and version matter, that is not a nice-to-have — it is the whole point.</p>
<p>Third, it separates content from typesetting. The &ldquo;Markdown for Lawyers&rdquo; idea dates to at least 2016, and its insight was that lawyerly content should not be welded to a word processor&rsquo;s layout. Write the substance in Markdown, render it to PDF or HTML when needed. The <code>lexicon.esq</code> project and <code>legal-markdown-js</code> both build on this: Markdown as the authoring format for contracts, with structured output and git-friendly tooling.</p>
<table>
  <thead>
      <tr>
          <th>Format</th>
          <th>Diffable</th>
          <th>Auditable</th>
          <th>Versioned</th>
          <th>Renders to PDF</th>
          <th>Agent-runnable</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>Word .docx</td>
          <td>No</td>
          <td>No</td>
          <td>No</td>
          <td>Yes</td>
          <td>Poor</td>
      </tr>
      <tr>
          <td>PDF</td>
          <td>No</td>
          <td>No</td>
          <td>No</td>
          <td>Native</td>
          <td>Poor</td>
      </tr>
      <tr>
          <td>Markdown</td>
          <td>Yes</td>
          <td>Yes</td>
          <td>Yes</td>
          <td>Via tooling</td>
          <td>Yes</td>
      </tr>
  </tbody>
</table>
<h2 id="the-agent-skills-model-skillmd-as-an-executable-legal-playbook">The Agent-Skills Model: SKILL.md as an Executable Legal Playbook</h2>
<p>The <code>legal-skills</code> repo runs three skills in order. The first, <code>patent-audit</code>, executes a 316-item pre-filing checklist. The second, <code>patent-examine</code>, runs a recurrent simulated USPTO examination loop. The third, <code>patent-workaround</code>, performs adversarial design-around testing. Together they simulate the full prosecution cycle without opening a matter.</p>
<p>The key design decision is that the model follows a contract, not a vibe. A <code>SKILL.md</code> file is a structured instruction set that tells the agent exactly what to do at each step. The model does not improvise a legal strategy; it executes a documented procedure. This is what makes the playbook &ldquo;executable&rdquo; — the intelligence is partly in the file, not only in the model.</p>
<p>The 316-item audit breaks down into four categories:</p>
<ul>
<li><strong>105 blocking items</strong> — things that must be resolved before filing.</li>
<li><strong>28 mechanical items</strong> — handled by code, because models cannot reliably count or do date arithmetic.</li>
<li><strong>169 assisted items</strong> — the model works with evidence the user provides.</li>
<li><strong>119 judgment items</strong> — where the model reasons, but a human should review.</li>
</ul>
<p>This distribution is the honest version of the claim. A large share of the work is mechanical or assisted, and that share is what gets displaced. The judgment items are where a human stays in the loop.</p>
<h2 id="legislation-as-code-versioned-law-in-git">Legislation as Code: Versioned Law in Git</h2>
<p>The same pattern scales from a single patent to an entire legal corpus. The <code>legalize</code> family treats legislation as a Git repository: each law is a Markdown file, each revision is a commit with the real promulgation date. <code>legalize-es</code> (Spanish) is the largest at 1,879 stars; <code>legalize-kr</code> (Korean) follows at 1,518; the pattern also extends to Greek and the core <code>legalize-dev/legalize</code> project.</p>
<p>Why does this matter? Because law is versioned by nature. A statute is amended, repealed, and re-enacted. When that history lives in git, you get three properties for free:</p>
<ul>
<li><strong>Diffability</strong> — see exactly what a reform changed.</li>
<li><strong>Auditability</strong> — trace every change to a commit and a date.</li>
<li><strong>Reproducibility</strong> — rebuild any historical version of the law on demand.</li>
</ul>
<p>This is the same value proposition as the patent playbook, applied to the law itself. If process belongs in files, so does the law the process operates on.</p>
<h2 id="where-ai-agents-still-fail-and-why-deterministic-checks-matter">Where AI Agents Still Fail (and Why Deterministic Checks Matter)</h2>
<p>The <code>legal-skills</code> README is explicit about a hard limit: models cannot count, do date arithmetic, or exhaustively cross-reference. That is why the repo ships deterministic Python scripts. The mechanical parts of the audit — counting items, checking dates, cross-referencing requirements — are enforced by code, not by the model&rsquo;s best guess.</p>
<p>This is the crucial engineering insight. An LLM is a probabilistic text generator. It is excellent at drafting, summarizing, and reasoning about open-ended questions. It is unreliable at anything that requires exact enumeration or arithmetic. A 316-item checklist is exactly the kind of task where a model will confidently skip item 214 or miscompute a deadline.</p>
<p>The solution is to split the work by nature:</p>
<ul>
<li><strong>Mechanical</strong> → deterministic code. No model involved.</li>
<li><strong>Assisted</strong> → model + evidence the user supplies.</li>
<li><strong>Judgment</strong> → model reasons, human reviews.</li>
</ul>
<p>This division is what makes the whole system trustworthy enough to run without opening a matter. The model does what models are good at; the code does what code is good at; the human does what only a human can do.</p>
<h2 id="what-stays-reserved-to-human-lawyers">What Stays Reserved to Human Lawyers</h2>
<p>The <code>legal-skills</code> repo carries a heavy &ldquo;not legal advice&rdquo; disclaimer, and it is not boilerplate. The skills do not file, sign, or appear before any tribunal. Some acts stay reserved to a registered practitioner by law — signing a filing, appearing in court, and making duty-of-candor calls to a patent office.</p>
<p>This is the honest boundary of the &ldquo;replace lawyers with markdown&rdquo; thesis. What gets displaced is process: the reading, the drafting, the checklist-running, the cross-referencing. What does not get displaced is the set of acts that the legal system reserves to licensed humans. A Markdown file cannot sign a declaration under penalty of perjury. An agent cannot appear before the USPTO. And the judgment calls about candor and disclosure are exactly the kind of thing a court will hold a human responsible for.</p>
<p>So the accurate framing is not &ldquo;no lawyers.&rdquo; It is &ldquo;far fewer billable hours for process, and a sharper focus on the acts that only a licensed human can perform.&rdquo;</p>
<h2 id="how-to-start-a-practical-guide">How to Start: A Practical Guide</h2>
<p>If you want to apply this pattern, you do not need to start with a 316-item patent audit. Start small.</p>
<ol>
<li><strong>Pick a repeatable legal process</strong> you already do — a contract review, a compliance checklist, a filing checklist.</li>
<li><strong>Write it as a SKILL.md</strong> — a structured instruction set that tells an agent what to do at each step.</li>
<li><strong>Separate the mechanical from the judgment.</strong> Anything that requires counting, dates, or exhaustive cross-referencing goes into a deterministic script. Everything else can be model-assisted.</li>
<li><strong>Version everything in git.</strong> Your process, your checklists, and your drafts should all be diffable and auditable.</li>
<li><strong>Keep a human on the judgment items.</strong> The goal is to shrink the billable surface, not to remove accountability.</li>
</ol>
<p>The <code>legal-skills</code> repo is the reference implementation for the patent domain. The <code>legalize</code> family shows the pattern at the scale of an entire legal corpus. <code>lexicon.esq</code> and <code>legal-markdown-js</code> show the authoring and tooling side. Together they form a coherent stack: Markdown for content, git for versioning, SKILL.md for agent instructions, and deterministic code for the mechanical parts.</p>
<h2 id="the-future-of-legal-fees">The Future of Legal Fees</h2>
<p>The economic backdrop is a repricing of the billable hour. Legal&rsquo;s AI repricing is estimated at a $900 billion market — the &ldquo;Death of the Billable Hour&rdquo; thesis. When a process that a firm bills at $400/hour can be run by a model that costs less than lunch, the rate card itself is under pressure.</p>
<p>The direction is clear. Process moves into versioned files. Agents execute documented playbooks. Deterministic code enforces the mechanical parts. And the billable hour — the unit that made process expensive — gets repriced by the same technology that made it cheap.</p>
<h2 id="faq">FAQ</h2>
<p><strong>What are legal skills markdown agents?</strong>
They are executable legal playbooks written as SKILL.md files that tell an AI agent how to run a legal process — like a 316-item patent pre-filing audit — with deterministic scripts enforcing the mechanical parts.</p>
<p><strong>Can Markdown really replace a lawyer?</strong>
Not entirely. It displaces the process work — reading, drafting, checklist-running, cross-referencing — but signing, filing, appearing before a tribunal, and duty-of-candor calls stay reserved to licensed practitioners.</p>
<p><strong>Why is Markdown better than Word or PDF for legal documents?</strong>
Markdown is plain text, so it is diffable, auditable, and versionable in git. You can see exactly what a revision changed, trace it to a commit, and rebuild any historical version on demand.</p>
<p><strong>What is &ldquo;legislation as code&rdquo;?</strong>
It is the pattern of managing law as a Git repository — each law a Markdown file, each reform a commit with the real promulgation date. The <code>legalize</code> family applies it to Spanish, Korean, and Greek legislation.</p>
<p><strong>Where do AI agents still fail in legal work?</strong>
They cannot reliably count, do date arithmetic, or exhaustively cross-reference. That is why the mechanical parts of a legal playbook are enforced by deterministic Python scripts rather than by the model.</p>
]]></content:encoded></item></channel></rss>