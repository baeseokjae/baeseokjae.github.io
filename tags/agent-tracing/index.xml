<?xml version="1.0" encoding="utf-8" standalone="yes"?><rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom" xmlns:content="http://purl.org/rss/1.0/modules/content/"><channel><title>Agent Tracing on RockB</title><link>https://baeseokjae.github.io/tags/agent-tracing/</link><description>Recent content in Agent Tracing on RockB</description><image><title>RockB</title><url>https://baeseokjae.github.io/images/og-default.png</url><link>https://baeseokjae.github.io/images/og-default.png</link></image><generator>Hugo</generator><language>en-us</language><lastBuildDate>Sun, 16 Aug 2026 16:01:16 +0000</lastBuildDate><atom:link href="https://baeseokjae.github.io/tags/agent-tracing/index.xml" rel="self" type="application/rss+xml"/><item><title>Langfuse for DeepSeek Harness: OpenTelemetry-Based Agent Observability</title><link>https://baeseokjae.github.io/posts/langfuse-for-deepseek-harness-opentelemetry-based-agent-observability/</link><pubDate>Sun, 16 Aug 2026 16:01:16 +0000</pubDate><guid>https://baeseokjae.github.io/posts/langfuse-for-deepseek-harness-opentelemetry-based-agent-observability/</guid><description>Learn how to add OpenTelemetry-based observability to a DeepSeek harness with Langfuse, covering OTLP export, the OpenAI SDK integration, and self-hosting.</description><content:encoded><![CDATA[<p>DeepSeek harnesses — the scripts, agents, and pipelines that call DeepSeek models — become production systems the moment they leave your laptop, and production systems need observability. The fastest way to get it is to instrument your harness with OpenTelemetry and export the traces to Langfuse, an open-source AI engineering platform that turns raw OTLP spans into searchable, debuggable agent traces. This guide walks you through the exact setup, from the OTLP endpoint and authentication to the OpenAI SDK integration and self-hosting, so you can trace every DeepSeek call end to end.</p>
<h2 id="why-deepseek-harnesses-need-observability">Why DeepSeek Harnesses Need Observability</h2>
<p>A DeepSeek harness is rarely a single API call. In practice it is a chain: a prompt template, a retrieval step, a model call, a tool invocation, a second model call to parse the result, and a final formatting pass. When that chain returns a wrong answer, you need to know which step failed and why. Without tracing, you are debugging blind — guessing whether the problem was a bad prompt, a malformed tool response, or a model that simply drifted.</p>
<p>Observability answers three questions that every harness operator asks daily: What did the model actually receive as input? What did it return? And how long did each step take, and at what cost? Langfuse captures all of this as structured traces, so a single failed run becomes a clickable timeline instead of a wall of log lines. For teams running DeepSeek in production, this is the difference between a five-minute fix and a five-hour investigation.</p>
<p>The stakes are measurable. Langfuse is a YC W23-backed open-source project with roughly 33,186 GitHub stars and about 3,574 forks, and it has become the de facto trace backend for LLM applications precisely because it makes this debugging workflow concrete. When you instrument a DeepSeek harness, you are not adding a nice-to-have — you are adding the tool that tells you whether your agent is actually working.</p>
<h2 id="what-is-opentelemetry-and-how-it-fits-llm-tracing">What Is OpenTelemetry and How It Fits LLM Tracing</h2>
<p>OpenTelemetry (OTel) is a CNCF project that provides a standard, vendor-neutral way to collect distributed traces, metrics, and logs from applications. Instead of baking a proprietary SDK into your code, you emit spans and traces using a common specification, then export them to any backend that speaks the OpenTelemetry Protocol (OTLP). This is the key architectural decision: your instrumentation is portable, and your trace backend is swappable.</p>
<p>For LLM workloads, OTel defines semantic conventions for spans that represent model calls, retrievals, and tool executions. A single agent run becomes a trace — a tree of spans — where the root span is the overall request and child spans represent each model call, embedding, or tool use. Each span carries attributes like the model name, input tokens, output tokens, latency, and cost, which is exactly the data you need to monitor a DeepSeek harness.</p>
<p>The practical benefit is that you can instrument once with OTel and export to Langfuse, Grafana, Jaeger, or any other OTLP-compatible backend. If you later decide Langfuse is not the right fit, your instrumentation does not change. This vendor neutrality is the strongest argument for the OTel route over a proprietary SDK, and it is why Langfuse explicitly supports receiving OTLP traces on its <code>/api/public/otel</code> endpoint.</p>
<h2 id="langfuse-as-the-otel-trace-backend">Langfuse as the OTel Trace Backend</h2>
<p>Langfuse receives OpenTelemetry traces on the <code>/api/public/otel</code> endpoint, which is the OTLP ingestion path. You point your exporter at this endpoint, authenticate with your Langfuse keys, and spans start flowing. Langfuse offers cloud regions in the EU, US, and Japan, plus a HIPAA-compliant option, so you can choose where your DeepSeek trace data lives.</p>
<p>The endpoint and authentication details are straightforward:</p>
<ul>
<li><strong>OTLP endpoint:</strong> For Langfuse Cloud, use the region-specific URL (for example, <code>https://cloud.langfuse.com/api/public/otel</code>). For self-hosted Langfuse version 3.22.0 or later, use <code>http://localhost:3000/api/public/otel</code>.</li>
<li><strong>Authentication:</strong> Use Basic Auth with a base64-encoded <code>pk-lf-...:sk-lf-...</code> pair. The public key identifies your project; the secret key authorizes writes.</li>
<li><strong>Ingestion version:</strong> For Langfuse v4, include the <code>x-langfuse-ingestion-version: 4</code> header to enable real-time ingestion.</li>
</ul>
<p>You set the exporter endpoint and credentials as environment variables, most commonly <code>OTEL_EXPORTER_OTLP_ENDPOINT</code>, <code>LANGFUSE_PUBLIC_KEY</code>, and <code>LANGFUSE_SECRET_KEY</code>. Once configured, any OTel-compatible exporter in your DeepSeek harness can ship traces to Langfuse without custom code.</p>
<h2 id="option-a--instrument-a-deepseek-harness-with-the-langfuse-openai-sdk">Option A — Instrument a DeepSeek Harness with the Langfuse OpenAI SDK</h2>
<p>DeepSeek&rsquo;s API is OpenAI-compatible, which means you can use the OpenAI SDK to call DeepSeek models. This is the single most important fact for observability, because it lets you reuse the entire OpenAI SDK integration ecosystem — including Langfuse&rsquo;s drop-in tracing wrapper.</p>
<p>The Langfuse Python SDK provides a <code>Langfuse</code> client that wraps the OpenAI SDK. Instead of calling <code>OpenAI()</code> directly, you create a <code>Langfuse</code> client and use its <code>.openai</code> attribute to get a traced client. Every request you make through that client is automatically wrapped in a span, capturing the model, prompt, completion, token counts, latency, and cost.</p>
<p>Here is the minimal setup for a DeepSeek harness:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-python" data-lang="python"><span style="display:flex;"><span><span style="color:#f92672">import</span> os
</span></span><span style="display:flex;"><span><span style="color:#f92672">from</span> langfuse.openai <span style="color:#f92672">import</span> OpenAI
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#75715e"># Set these in your environment</span>
</span></span><span style="display:flex;"><span>os<span style="color:#f92672">.</span>environ[<span style="color:#e6db74">&#34;LANGFUSE_PUBLIC_KEY&#34;</span>] <span style="color:#f92672">=</span> <span style="color:#e6db74">&#34;pk-lf-...&#34;</span>
</span></span><span style="display:flex;"><span>os<span style="color:#f92672">.</span>environ[<span style="color:#e6db74">&#34;LANGFUSE_SECRET_KEY&#34;</span>] <span style="color:#f92672">=</span> <span style="color:#e6db74">&#34;sk-lf-...&#34;</span>
</span></span><span style="display:flex;"><span>os<span style="color:#f92672">.</span>environ[<span style="color:#e6db74">&#34;LANGFUSE_BASE_URL&#34;</span>] <span style="color:#f92672">=</span> <span style="color:#e6db74">&#34;https://cloud.langfuse.com&#34;</span>
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>client <span style="color:#f92672">=</span> OpenAI(
</span></span><span style="display:flex;"><span>    base_url<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;https://api.deepseek.com&#34;</span>,  <span style="color:#75715e"># DeepSeek&#39;s OpenAI-compatible endpoint</span>
</span></span><span style="display:flex;"><span>    api_key<span style="color:#f92672">=</span>os<span style="color:#f92672">.</span>environ[<span style="color:#e6db74">&#34;DEEPSEEK_API_KEY&#34;</span>],
</span></span><span style="display:flex;"><span>)
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>response <span style="color:#f92672">=</span> client<span style="color:#f92672">.</span>chat<span style="color:#f92672">.</span>completions<span style="color:#f92672">.</span>create(
</span></span><span style="display:flex;"><span>    model<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;deepseek-chat&#34;</span>,
</span></span><span style="display:flex;"><span>    messages<span style="color:#f92672">=</span>[{<span style="color:#e6db74">&#34;role&#34;</span>: <span style="color:#e6db74">&#34;user&#34;</span>, <span style="color:#e6db74">&#34;content&#34;</span>: <span style="color:#e6db74">&#34;Explain observability in one sentence.&#34;</span>}],
</span></span><span style="display:flex;"><span>)
</span></span><span style="display:flex;"><span>print(response<span style="color:#f92672">.</span>choices[<span style="color:#ae81ff">0</span>]<span style="color:#f92672">.</span>message<span style="color:#f92672">.</span>content)
</span></span></code></pre></div><p>Because the client is the Langfuse-wrapped OpenAI client, that single <code>create</code> call produces a full trace in Langfuse with zero manual span management. For a harness that makes many calls — a retrieval step, a reasoning step, a tool call — each one becomes a child span under the parent trace, giving you a complete timeline of the agent run.</p>
<p>Langfuse also integrates natively with LangChain, LlamaIndex, and LiteLLM, so if your DeepSeek harness is built on one of those frameworks, you get tracing with a one-line callback or wrapper rather than manual instrumentation. This is the lowest-friction path and the right choice for most teams.</p>
<h2 id="option-b--raw-opentelemetry-export-to-langfuse-for-custom-harnesses">Option B — Raw OpenTelemetry Export to Langfuse for Custom Harnesses</h2>
<p>If your DeepSeek harness is written in a language without a Langfuse SDK, or you want to keep your instrumentation fully vendor-neutral, you can use the native OpenTelemetry API and export spans directly to Langfuse. This is the &ldquo;raw OTel&rdquo; path, and it is the right choice when you need maximum control or are standardizing on OTel across multiple backends.</p>
<p>The pattern is to create a tracer, start spans around each logical step, and let the OTLP exporter ship them to Langfuse. Here is a Python example using the <code>opentelemetry-sdk</code> and <code>opentelemetry-exporter-otlp-proto-http</code> packages:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-python" data-lang="python"><span style="display:flex;"><span><span style="color:#f92672">from</span> opentelemetry <span style="color:#f92672">import</span> trace
</span></span><span style="display:flex;"><span><span style="color:#f92672">from</span> opentelemetry.sdk.trace <span style="color:#f92672">import</span> TracerProvider
</span></span><span style="display:flex;"><span><span style="color:#f92672">from</span> opentelemetry.sdk.trace.export <span style="color:#f92672">import</span> BatchSpanProcessor
</span></span><span style="display:flex;"><span><span style="color:#f92672">from</span> opentelemetry.exporter.otlp.proto.http.trace_exporter <span style="color:#f92672">import</span> OTLPSpanExporter
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>provider <span style="color:#f92672">=</span> TracerProvider()
</span></span><span style="display:flex;"><span>provider<span style="color:#f92672">.</span>add_span_processor(
</span></span><span style="display:flex;"><span>    BatchSpanProcessor(
</span></span><span style="display:flex;"><span>        OTLPSpanExporter(
</span></span><span style="display:flex;"><span>            endpoint<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;http://localhost:3000/api/public/otel&#34;</span>,
</span></span><span style="display:flex;"><span>            headers<span style="color:#f92672">=</span>{<span style="color:#e6db74">&#34;Authorization&#34;</span>: <span style="color:#e6db74">&#34;Basic &lt;base64(pk:sk)&gt;&#34;</span>},
</span></span><span style="display:flex;"><span>        )
</span></span><span style="display:flex;"><span>    )
</span></span><span style="display:flex;"><span>)
</span></span><span style="display:flex;"><span>trace<span style="color:#f92672">.</span>set_tracer_provider(provider)
</span></span><span style="display:flex;"><span>tracer <span style="color:#f92672">=</span> trace<span style="color:#f92672">.</span>get_tracer(<span style="color:#e6db74">&#34;deepseek-harness&#34;</span>)
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">with</span> tracer<span style="color:#f92672">.</span>start_as_current_span(<span style="color:#e6db74">&#34;agent.run&#34;</span>) <span style="color:#66d9ef">as</span> root:
</span></span><span style="display:flex;"><span>    <span style="color:#66d9ef">with</span> tracer<span style="color:#f92672">.</span>start_as_current_span(<span style="color:#e6db74">&#34;deepseek.call&#34;</span>) <span style="color:#66d9ef">as</span> span:
</span></span><span style="display:flex;"><span>        span<span style="color:#f92672">.</span>set_attribute(<span style="color:#e6db74">&#34;gen_ai.request.model&#34;</span>, <span style="color:#e6db74">&#34;deepseek-chat&#34;</span>)
</span></span><span style="display:flex;"><span>        <span style="color:#75715e"># ... call DeepSeek here ...</span>
</span></span><span style="display:flex;"><span>        span<span style="color:#f92672">.</span>set_attribute(<span style="color:#e6db74">&#34;gen_ai.usage.input_tokens&#34;</span>, <span style="color:#ae81ff">120</span>)
</span></span><span style="display:flex;"><span>        span<span style="color:#f92672">.</span>set_attribute(<span style="color:#e6db74">&#34;gen_ai.usage.output_tokens&#34;</span>, <span style="color:#ae81ff">45</span>)
</span></span></code></pre></div><p>The trade-off is clear: raw OTel gives you portability and control, but you must manually set the semantic-convention attributes that Langfuse uses to render cost, tokens, and model names. The Langfuse SDK does this for you automatically. For a custom harness, weigh whether the extra control is worth the manual attribute management.</p>
<h2 id="self-hosting-langfuse-for-private-deepseek-trace-data">Self-Hosting Langfuse for Private DeepSeek Trace Data</h2>
<p>If your DeepSeek harness processes sensitive data, you may not want traces leaving your infrastructure. Langfuse is fully self-hostable, and self-hosting gives you complete control over where trace data lives. The self-hosted instance exposes the same OTLP endpoint on <code>localhost:3000</code> (for version 3.22.0 and later), so the exporter configuration is identical to the cloud path — you just change the endpoint.</p>
<p>Self-hosting is a good fit when you need data residency guarantees, want to avoid per-token or per-trace cloud costs at high volume, or need to keep DeepSeek prompts and completions inside your own network. The setup is a standard Docker deployment, and because Langfuse is open source, you can inspect and extend it.</p>
<p>The main cost is operational: you are now responsible for running, upgrading, and backing up the Langfuse instance. For a small team this is a reasonable trade for data privacy; for a large one, the cloud regions (EU, US, Japan, HIPAA) may already satisfy your residency requirements without the operational burden.</p>
<h2 id="enabling-real-time-ingestion-in-langfuse-v4">Enabling Real-Time Ingestion in Langfuse v4</h2>
<p>One of the biggest changes in Langfuse v4 is real-time ingestion. Langfuse claims up to 165x faster real-time ingestion compared to prior versions, which matters for live agent debugging. In older versions, traces could be batched and delayed; in v4, you can see spans appear as they happen, which is essential when you are watching a DeepSeek agent run in production and need to catch a failure the moment it occurs.</p>
<p>To opt into real-time ingestion, include the <code>x-langfuse-ingestion-version: 4</code> header on your OTLP export requests. If you are using the Langfuse SDK, this is handled for you. If you are using raw OTel, add the header to your exporter&rsquo;s headers dictionary alongside the Basic Auth credentials.</p>
<p>Real-time ingestion changes your debugging workflow. Instead of running a batch, waiting, and then inspecting traces, you can watch a live agent run unfold in the Langfuse UI, spot a bad tool call mid-run, and intervene. For a DeepSeek harness that makes many sequential calls, this live view is the difference between catching a problem in seconds and discovering it after the fact.</p>
<h2 id="comparing-langfuse-sdk-vs-raw-otel-for-your-harness">Comparing Langfuse SDK vs Raw OTel for Your Harness</h2>
<p>The choice between the Langfuse SDK and raw OpenTelemetry is the central decision in this guide. Both end with traces in Langfuse, but they differ in effort, portability, and control.</p>
<table>
  <thead>
      <tr>
          <th>Consideration</th>
          <th>Langfuse SDK</th>
          <th>Raw OpenTelemetry</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>Setup effort</td>
          <td>Minimal — wrap the OpenAI client</td>
          <td>Moderate — configure tracer and exporter</td>
      </tr>
      <tr>
          <td>Span attributes</td>
          <td>Automatic (tokens, cost, model)</td>
          <td>Manual (you set semantic conventions)</td>
      </tr>
      <tr>
          <td>Vendor neutrality</td>
          <td>Tied to Langfuse</td>
          <td>Fully portable across OTel backends</td>
      </tr>
      <tr>
          <td>Language support</td>
          <td>Python, JS, plus framework integrations</td>
          <td>Any language with an OTel SDK</td>
      </tr>
      <tr>
          <td>Real-time ingestion</td>
          <td>Automatic in v4</td>
          <td>Requires the <code>x-langfuse-ingestion-version: 4</code> header</td>
      </tr>
      <tr>
          <td>Best for</td>
          <td>Most teams, especially OpenAI-SDK-based harnesses</td>
          <td>Custom harnesses, multi-backend standardization</td>
      </tr>
  </tbody>
</table>
<p>For most DeepSeek harnesses, the Langfuse SDK is the right call. It is less code, produces richer traces automatically, and handles real-time ingestion for you. Choose raw OTel when you need to export to multiple backends, are working in a language without a Langfuse SDK, or want to keep your instrumentation independent of any single vendor.</p>
<h2 id="common-pitfalls-and-debugging-tips">Common Pitfalls and Debugging Tips</h2>
<p>Even with a clean setup, a few issues trip up most teams. Here are the ones to watch for.</p>
<p><strong>Wrong OTLP endpoint.</strong> The most common mistake is pointing the exporter at the Langfuse base URL instead of the <code>/api/public/otel</code> path. The OTLP endpoint must include that suffix. If you see no traces, check that your exporter is hitting <code>.../api/public/otel</code>, not <code>.../api</code>.</p>
<p><strong>Authentication failures.</strong> Langfuse expects Basic Auth with a base64-encoded <code>pk-lf-...:sk-lf-...</code> pair. If you pass the keys as separate headers or forget the base64 encoding, ingestion fails silently. Verify the header format before debugging anything else.</p>
<p><strong>Missing ingestion header.</strong> On Langfuse v4, omitting <code>x-langfuse-ingestion-version: 4</code> can cause traces to be delayed or dropped. Add the header explicitly when using raw OTel.</p>
<p><strong>DeepSeek base URL confusion.</strong> Remember that DeepSeek is OpenAI-compatible but has its own base URL (<code>https://api.deepseek.com</code>). If you point the OpenAI SDK at the default OpenAI endpoint, your DeepSeek calls fail. Set <code>base_url</code> explicitly.</p>
<p><strong>No spans for framework calls.</strong> If you are using LangChain or LlamaIndex and see no traces, you likely forgot to register the Langfuse callback or handler. The SDK integration is not automatic — you must wire it in.</p>
<p><strong>Token and cost attributes missing.</strong> With raw OTel, Langfuse cannot compute cost unless you set the <code>gen_ai.usage.input_tokens</code> and <code>gen_ai.usage.output_tokens</code> attributes. If your traces render without cost, this is the cause.</p>
<h2 id="conclusion-and-next-steps">Conclusion and Next Steps</h2>
<p>Adding OpenTelemetry-based observability to a DeepSeek harness with Langfuse is a high-leverage change. You get a complete, searchable timeline of every model call, tool invocation, and retrieval step, with token counts and cost — the data you need to debug failures and optimize spend. The two paths, the Langfuse OpenAI SDK and raw OTel export, both end at the same trace backend, so you can start with the SDK and migrate to raw OTel later if your needs change.</p>
<p>Start by instrumenting a single DeepSeek harness with the Langfuse OpenAI SDK, confirm traces appear in the Langfuse UI, then add real-time ingestion and expand to the rest of your pipeline. If data privacy is a concern, self-host Langfuse and keep every trace inside your network. The result is a DeepSeek harness you can actually see, debug, and trust in production.</p>
<h2 id="faq">FAQ</h2>
<p><strong>What is a DeepSeek harness?</strong>
A DeepSeek harness is any script, agent, or pipeline that calls DeepSeek models — typically a chain of prompt, retrieval, model call, and tool steps. It is the code that turns the DeepSeek API into a working application.</p>
<p><strong>How do I trace DeepSeek calls with Langfuse?</strong>
Because DeepSeek&rsquo;s API is OpenAI-compatible, you can use the Langfuse OpenAI SDK wrapper. Create a <code>Langfuse</code> client, use its <code>.openai</code> attribute, and set <code>base_url</code> to <code>https://api.deepseek.com</code>. Every call is traced automatically.</p>
<p><strong>Does Langfuse support OpenTelemetry?</strong>
Yes. Langfuse receives OTLP traces on its <code>/api/public/otel</code> endpoint, so any OpenTelemetry-compatible exporter can ship spans to Langfuse. This is the vendor-neutral path for custom harnesses.</p>
<p><strong>Can I self-host Langfuse for DeepSeek trace data?</strong>
Yes. Langfuse is open source and self-hostable. For version 3.22.0 and later, the self-hosted instance exposes the OTLP endpoint on <code>localhost:3000</code>, keeping all trace data inside your infrastructure.</p>
<p><strong>What is the <code>x-langfuse-ingestion-version: 4</code> header for?</strong>
It opts into Langfuse v4&rsquo;s real-time ingestion, which Langfuse claims is up to 165x faster than prior versions. It makes traces appear live, which is essential for debugging DeepSeek agents in production.</p>
]]></content:encoded></item><item><title>Helios: Open-Source AI Agent Observability with Tracing and Cost Monitoring</title><link>https://baeseokjae.github.io/posts/helios-open-source-agent-observability-2026/</link><pubDate>Sun, 26 Jul 2026 07:01:57 +0000</pubDate><guid>https://baeseokjae.github.io/posts/helios-open-source-agent-observability-2026/</guid><description>Helios is an open-source AI agent observability platform combining execution tracing, cost analytics, and security monitoring in a single self-hosted stack.</description><content:encoded><![CDATA[<p>Helios is a brand-new open-source observability platform purpose-built for AI agents that combines execution tracing, cost analytics, and security monitoring — including PII detection and prompt injection detection — in a single self-hosted stack. Launched on July 22, 2026 under the MIT license, Helios addresses the critical infrastructure gap behind the statistic that 88% of AI agents fail to reach production, offering teams a unified dashboard to understand exactly what their agents are doing, how much they cost, and whether they are safe.</p>
<h2 id="what-is-helios">What Is Helios?</h2>
<p>Helios is an open-source observability platform designed specifically for AI agent workloads. Unlike general-purpose LLM tracing tools that focus narrowly on prompt-response logging, Helios provides three integrated capabilities in one platform: agent execution tracing with full span-level visibility, real-time cost analytics across models and providers, and security monitoring that detects PII leaks and prompt injection attacks. The project is MIT-licensed, self-hostable via a single Docker Compose command, and comes with a Python SDK that requires fewer than 10 lines of code to integrate.</p>
<p>The project was created on July 22, 2026 and is in its earliest stage — currently at 1 GitHub star. This makes it a project to watch closely as it evolves from foundation to production-ready, especially for teams that want to influence its roadmap from day one.</p>
<h2 id="why-agent-observability-matters-in-2026">Why Agent Observability Matters in 2026</h2>
<p>The AI agent market is exploding. According to Digital Applied&rsquo;s Agentic AI Statistics 2026, the agentic AI market is projected to grow from $7.6 billion to $236 billion by 2034, representing a compound annual growth rate of over 40%. Yet the same research reveals a sobering reality: 88% of AI agents fail to reach production. For the 79% of enterprises that have adopted AI agents in some form, only 11% are running them in production.</p>
<p>This massive failure rate is not random. The primary reason agents stall is a lack of observability — teams simply cannot see what their agents are doing when things go wrong. Traditional application monitoring tools are designed for deterministic, request-response systems. AI agents are non-deterministic, multi-step, tool-calling systems that branch unpredictably. When an agent hallucinates, calls the wrong tool, enters an infinite loop, or leaks sensitive data, standard APM tools provide no useful signal.</p>
<p>The agents that do reach production deliver an average 171% ROI (192% in the US), according to the same report. This means the incentive to get observability right is enormous. The teams that can debug, optimize, and secure their agents effectively will capture disproportionate value from the AI agent revolution.</p>
<h2 id="helios-architecture--tracing-cost-and-security-in-one-stack">Helios Architecture — Tracing, Cost, and Security in One Stack</h2>
<p>Helios is built on an asynchronous architecture designed for production agent workloads. The system has two layers that work together:</p>
<p><strong>Fast synchronous ingestion layer.</strong> When an agent makes a call, the Helios SDK sends trace data to the ingestion endpoint synchronously with minimal overhead. This ensures the agent&rsquo;s own latency is not impacted by observability processing.</p>
<p><strong>Async analysis pipeline.</strong> Once ingested, traces flow into an asynchronous pipeline that handles three parallel workloads: cost calculation (computing token usage and pricing across models), security scanning (running PII detection and prompt injection classifiers), and risk scoring (combining multiple detector signals into a per-run risk score).</p>
<p>This architecture means the SDK call is fast — the heavy analysis happens in the background. Teams get real-time visibility into agent execution without sacrificing agent performance.</p>
<p>The entire stack runs on a single Docker Compose command, making self-hosting straightforward. All data stays on your infrastructure, eliminating the vendor lock-in and data sovereignty concerns that come with cloud-only observability platforms.</p>
<h2 id="key-features-deep-dive">Key Features Deep Dive</h2>
<h3 id="agent-execution-tracing">Agent Execution Tracing</h3>
<p>Helios provides full span-level tracing for agent runs. Each agent execution is captured as a trace containing multiple spans — one for each LLM call, tool invocation, or sub-agent delegation. The trace view shows the complete execution tree, including timing, input/output payloads, and error states.</p>
<p>This is critical for debugging agent failures. When an agent produces a wrong answer, the trace reveals exactly which step went wrong: did the LLM hallucinate? Did a tool return unexpected data? Did the agent enter a loop? Without tracing, debugging an agent is essentially guessing.</p>
<p>Helios supports both automatic instrumentation (wrapping popular frameworks) and manual instrumentation via decorators for custom agent logic. The SDK is designed for drop-in integration with existing agents.</p>
<h3 id="cost-analytics-dashboard">Cost Analytics Dashboard</h3>
<p>Cost monitoring is a first-class feature in Helios, not an afterthought. The cost analytics dashboard breaks down spending by model, by provider, by agent, and by time period. Teams can see exactly how much each agent run costs, which models are driving the most expense, and how costs trend over time.</p>
<p>With organizations running dozens of agents across multiple models (GPT-4o, Claude Sonnet, Gemini, open-source models), cost visibility is essential for budgeting and optimization. Helios surfaces the data needed to make informed decisions about model selection, caching strategies, and agent design.</p>
<p>The async cost calculation pipeline computes costs based on actual token usage against provider pricing, so teams get accurate, real-time cost data without manual estimation.</p>
<h3 id="security-monitoring-pii-prompt-injection-risk-scoring">Security Monitoring (PII, Prompt Injection, Risk Scoring)</h3>
<p>This is Helios&rsquo;s strongest differentiator. Most agent observability tools focus exclusively on tracing and cost. Helios adds a built-in security monitoring layer that scans every agent run for:</p>
<p><strong>PII detection.</strong> The security pipeline automatically scans LLM inputs and outputs for personally identifiable information — email addresses, phone numbers, credit card numbers, social security numbers, and other sensitive data patterns. If an agent accidentally leaks PII in its output or receives PII in a prompt, Helios flags it immediately.</p>
<p><strong>Prompt injection detection.</strong> Helios analyzes prompts for known injection patterns, including jailbreak attempts, prompt leaking, and indirect injection via tool outputs. This is increasingly important as agents gain access to external data sources and tools that could be vectors for injection attacks.</p>
<p><strong>Risk scoring engine.</strong> The most innovative feature is Helios&rsquo;s built-in risk scoring engine, which combines signals from multiple detectors into a single per-run risk score. A run that triggers PII detection AND shows prompt injection patterns gets a higher risk score than one with only a single signal. This composite score lets teams triage security incidents by severity rather than drowning in individual alerts.</p>
<h2 id="helios-vs-the-competition">Helios vs. The Competition</h2>
<p>The open-source LLM observability space has several established players. Here is how Helios compares:</p>
<table>
  <thead>
      <tr>
          <th>Feature</th>
          <th>Helios</th>
          <th>Langfuse</th>
          <th>Arize Phoenix</th>
          <th>OpenLLMetry</th>
          <th>Helicone</th>
          <th>AgentOps</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>GitHub Stars</td>
          <td>1 (new)</td>
          <td>31,857</td>
          <td>10,733</td>
          <td>7,328</td>
          <td>5,995</td>
          <td>5,729</td>
      </tr>
      <tr>
          <td>License</td>
          <td>MIT</td>
          <td>MIT</td>
          <td>Elastic</td>
          <td>Apache 2.0</td>
          <td>Apache 2.0</td>
          <td>MIT</td>
      </tr>
      <tr>
          <td>Agent Tracing</td>
          <td>Yes</td>
          <td>Yes</td>
          <td>Yes</td>
          <td>Yes</td>
          <td>Yes</td>
          <td>Yes</td>
      </tr>
      <tr>
          <td>Cost Analytics</td>
          <td>Built-in</td>
          <td>Built-in</td>
          <td>Via Arize AX</td>
          <td>Limited</td>
          <td>Built-in</td>
          <td>Built-in</td>
      </tr>
      <tr>
          <td>Security Monitoring</td>
          <td>Built-in (PII, injection, risk scoring)</td>
          <td>No</td>
          <td>No</td>
          <td>No</td>
          <td>No</td>
          <td>Limited</td>
      </tr>
      <tr>
          <td>Self-Hosted</td>
          <td>Yes (Docker Compose)</td>
          <td>Yes</td>
          <td>Yes</td>
          <td>Yes</td>
          <td>Yes</td>
          <td>Yes</td>
      </tr>
      <tr>
          <td>OTel Compatible</td>
          <td>No (native SDK)</td>
          <td>No</td>
          <td>Yes</td>
          <td>Yes</td>
          <td>No</td>
          <td>No</td>
      </tr>
      <tr>
          <td>Agent-Specific SDK</td>
          <td>Yes</td>
          <td>No (LLM-focused)</td>
          <td>No (LLM-focused)</td>
          <td>No (LLM-focused)</td>
          <td>No (proxy-based)</td>
          <td>Yes</td>
      </tr>
      <tr>
          <td>Integration Complexity</td>
          <td>&lt;10 lines</td>
          <td>SDK</td>
          <td>SDK</td>
          <td>OTel config</td>
          <td>Proxy setup</td>
          <td>SDK</td>
      </tr>
  </tbody>
</table>
<h3 id="langfuse--the-llm-trace-store-leader">Langfuse — The LLM Trace Store Leader</h3>
<p>Langfuse is the dominant open-source LLM observability platform with 31,857 GitHub stars. It offers a comprehensive platform including tracing, evaluations, prompt management, a playground, and datasets. Langfuse is excellent for general LLM application observability and has a mature feature set built over years of development.</p>
<p>However, Langfuse is primarily an LLM tracing platform, not an agent observability platform. Teams using Langfuse for agent workloads often need to build their own evaluation and security layers on top. Langfuse does not include built-in PII detection, prompt injection scanning, or risk scoring. For teams that need those capabilities, Helios offers them out of the box.</p>
<h3 id="arize-phoenix--the-otel-compatible-veteran">Arize Phoenix — The OTel-Compatible Veteran</h3>
<p>Arize Phoenix (10,733 stars) is a leading AI observability platform with strong OpenTelemetry compatibility. It is an excellent choice for teams already invested in the OpenTelemetry ecosystem. The open-source Phoenix project provides core tracing, and Arize AX adds hosted dashboards, longer retention, and evaluation workflows.</p>
<p>Phoenix&rsquo;s strength is its OTel-native approach, which makes it easy to integrate into existing observability stacks. Its weakness for agent workloads is the same as Langfuse&rsquo;s — no built-in security monitoring. Teams need to add separate security tooling. Helios&rsquo;s all-in-one approach (tracing + cost + security) means fewer integrations to manage.</p>
<h3 id="openllmetry--the-otel-standard-approach">OpenLLMetry — The OTel Standard Approach</h3>
<p>OpenLLMetry by Traceloop (7,328 stars) takes a pure OpenTelemetry approach to LLM observability. It provides standard OTel instrumentation for GenAI applications, making it lightweight to integrate into existing OTel infrastructure. For teams that already run the OTel collector stack, OpenLLMetry is a natural fit.</p>
<p>The trade-off is that OpenLLMetry is intentionally minimal — it provides instrumentation and trace export, but the visualization, cost analytics, and security features depend on whatever backend you connect it to. Helios provides a complete, self-contained experience.</p>
<h3 id="helicone--the-proxy-based-alternative">Helicone — The Proxy-Based Alternative</h3>
<p>Helicone (5,995 stars, YC W23) takes a unique approach: it operates as a proxy layer between your application and LLM providers. This proxy-based architecture means integration can be as simple as changing the API endpoint URL. Helicone also includes a caching layer that can improve response times and reduce costs.</p>
<p>Helicone&rsquo;s proxy approach is elegant for simple use cases but becomes more complex for agent workloads that involve multiple tool calls, sub-agent delegation, and non-LLM operations. Helios&rsquo;s SDK-based approach gives finer-grained control over what gets traced in complex agent execution flows.</p>
<h3 id="agentops--the-agent-specific-sdk">AgentOps — The Agent-Specific SDK</h3>
<p>AgentOps (5,729 stars) is the closest competitor to Helios in terms of focus. It provides a Python SDK specifically for AI agent monitoring, with LLM cost tracking and benchmarking. It integrates with CrewAI, Agno, OpenAI Agents SDK, LangChain, AutoGen, AG2, and CamelAI.</p>
<p>AgentOps and Helios share the agent-specific focus, but Helios differentiates with its built-in security monitoring layer (PII detection, prompt injection detection, risk scoring). AgentOps has a more mature integration ecosystem today, but Helios&rsquo;s security features address a growing pain point that AgentOps does not cover.</p>
<h3 id="where-helios-stands-out">Where Helios Stands Out</h3>
<p>Helios&rsquo;s key differentiators are clear:</p>
<ol>
<li>
<p><strong>Security monitoring built in.</strong> No other open-source agent observability tool includes PII detection, prompt injection detection, and risk scoring as first-class features. This is Helios&rsquo;s strongest moat.</p>
</li>
<li>
<p><strong>All-in-one platform.</strong> Tracing, cost analytics, and security in a single self-hosted stack. Teams do not need to stitch together multiple tools.</p>
</li>
<li>
<p><strong>Async architecture.</strong> Fast ingestion with background analysis means observability does not slow down agents.</p>
</li>
<li>
<p><strong>Self-hosted with full data ownership.</strong> No vendor lock-in, no data leaving your infrastructure.</p>
</li>
<li>
<p><strong>Early stage = influence.</strong> At 1 star and 4 days old, early adopters can shape the project&rsquo;s direction.</p>
</li>
</ol>
<h2 id="getting-started-with-helios">Getting Started with Helios</h2>
<p>Getting started with Helios is straightforward. The project is available on GitHub and the self-hosted backend runs on a single Docker Compose command:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-bash" data-lang="bash"><span style="display:flex;"><span>git clone https://github.com/helios-observability/helios
</span></span><span style="display:flex;"><span>cd helios
</span></span><span style="display:flex;"><span>docker compose up
</span></span></code></pre></div><p>The Python SDK can be installed via pip and integrated in fewer than 10 lines of code:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-python" data-lang="python"><span style="display:flex;"><span><span style="color:#f92672">from</span> helios <span style="color:#f92672">import</span> Helios
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>helios <span style="color:#f92672">=</span> Helios(api_key<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;your-api-key&#34;</span>)
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#a6e22e">@helios.trace_agent</span>()
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">def</span> <span style="color:#a6e22e">my_agent</span>(query: str):
</span></span><span style="display:flex;"><span>    <span style="color:#75715e"># Your agent logic here</span>
</span></span><span style="display:flex;"><span>    response <span style="color:#f92672">=</span> llm_call(query)
</span></span><span style="display:flex;"><span>    <span style="color:#66d9ef">return</span> response
</span></span></code></pre></div><p>The SDK supports automatic instrumentation for popular agent frameworks and manual decorators for custom logic. Once integrated, traces appear in the Helios dashboard in real time, with cost calculations and security scans running in the background.</p>
<p>For teams already running agents in production, the SDK is designed for gradual adoption — you can instrument a single agent first, verify the integration, then roll out across your entire agent fleet.</p>
<h2 id="roadmap-and-future-direction">Roadmap and Future Direction</h2>
<p>Helios is a very early-stage project (created July 22, 2026, 1 GitHub star, MIT licensed). The current release provides the core architecture: trace ingestion, cost calculation, security scanning, and the dashboard UI. Based on the project&rsquo;s stated direction, the following capabilities are likely on the roadmap:</p>
<p><strong>Framework integrations.</strong> Deeper integrations with popular agent frameworks like CrewAI, LangChain, AutoGen, and OpenAI Agents SDK for automatic instrumentation without manual decorators.</p>
<p><strong>Alerting and notifications.</strong> Configurable alerts for cost thresholds, security incidents, and agent failure rates, delivered via Slack, email, or webhook.</p>
<p><strong>Extended security detectors.</strong> Additional security scanning capabilities including toxicity detection, bias detection, and content policy enforcement.</p>
<p><strong>Multi-team support.</strong> Organization-level dashboards with team-based access control for enterprises running many agents across multiple teams.</p>
<p><strong>Export and API.</strong> Programmatic access to trace data via API for custom analytics, compliance reporting, and integration with existing observability stacks.</p>
<p><strong>OpenTelemetry bridge.</strong> An OTel exporter that allows Helios traces to be forwarded to existing OTel-compatible backends, bridging the gap for teams with established observability infrastructure.</p>
<p>The project&rsquo;s early stage means the roadmap is fluid and community input will shape priorities. For teams that want a say in how an agent observability platform evolves, now is the time to get involved.</p>
<h2 id="verdict--who-should-use-helios">Verdict — Who Should Use Helios?</h2>
<p>Helios is not yet a production-ready platform — it is a 4-day-old project with 1 GitHub star. But it addresses a real and growing need that no other open-source tool fully covers.</p>
<p><strong>Use Helios if:</strong> You are building AI agents and want an all-in-one observability platform that includes security monitoring from day one. You prefer self-hosted solutions with full data ownership. You want to influence an open-source project&rsquo;s direction from its earliest stage.</p>
<p><strong>Wait if:</strong> You need a battle-tested, production-proven platform today. Langfuse or Arize Phoenix are more mature choices for general LLM observability. You need deep integrations with specific agent frameworks that Helios does not yet support.</p>
<p><strong>Watch if:</strong> You are evaluating observability options for a future agent deployment. Helios&rsquo;s combination of tracing, cost analytics, and security monitoring is unique. If the project executes on its roadmap, it could become the leading open-source agent observability platform.</p>
<p>The 88% agent failure rate is not inevitable. The teams that invest in observability infrastructure — tracing to debug failures, cost analytics to optimize spending, and security monitoring to prevent data leaks — will be the ones that capture the 171% ROI that production agents deliver. Helios offers a promising foundation for that infrastructure, built specifically for the unique challenges of AI agent workloads.</p>
<h2 id="frequently-asked-questions">Frequently Asked Questions</h2>
<p><strong>What is Helios and how does it differ from Langfuse?</strong>
Helios is an open-source AI agent observability platform that combines execution tracing, cost analytics, and security monitoring (PII detection, prompt injection detection, risk scoring) in a single stack. Langfuse focuses primarily on LLM tracing and evaluations but does not include built-in security monitoring. Helios is purpose-built for agent workloads rather than general LLM applications.</p>
<p><strong>Is Helios free to use?</strong>
Yes, Helios is completely free and open source under the MIT license. You can self-host it on your own infrastructure with no licensing fees, usage limits, or vendor lock-in. There is no paid tier or cloud offering at this stage.</p>
<p><strong>How do I integrate Helios with my existing AI agents?</strong>
Helios provides a Python SDK that requires fewer than 10 lines of code to integrate. You install the SDK via pip, initialize it with your API key, and add decorators to your agent functions. The SDK supports both automatic instrumentation for popular frameworks and manual instrumentation for custom agent logic.</p>
<p><strong>Does Helios support OpenTelemetry?</strong>
Not natively at this stage. Helios uses its own SDK-based instrumentation rather than OpenTelemetry. However, an OTel bridge is a likely future addition to the roadmap, which would allow Helios traces to be forwarded to existing OTel-compatible backends.</p>
<p><strong>Can Helios detect prompt injection attacks in real time?</strong>
Helios&rsquo;s security scanning runs in the async analysis pipeline, which means detection happens shortly after ingestion rather than blocking the agent call in real time. The risk scoring engine combines PII detection, prompt injection signals, and other detector outputs into a per-run risk score for triage. For real-time blocking, teams would need to combine Helios with a guardrail tool that runs inline before the LLM call.</p>
]]></content:encoded></item></channel></rss>