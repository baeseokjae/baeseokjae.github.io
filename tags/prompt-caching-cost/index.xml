<?xml version="1.0" encoding="utf-8" standalone="yes"?><rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom" xmlns:content="http://purl.org/rss/1.0/modules/content/"><channel><title>Prompt Caching Cost on RockB</title><link>https://baeseokjae.github.io/tags/prompt-caching-cost/</link><description>Recent content in Prompt Caching Cost on RockB</description><image><title>RockB</title><url>https://baeseokjae.github.io/images/og-default.png</url><link>https://baeseokjae.github.io/images/og-default.png</link></image><generator>Hugo</generator><language>en-us</language><lastBuildDate>Tue, 25 Aug 2026 10:02:22 +0000</lastBuildDate><atom:link href="https://baeseokjae.github.io/tags/prompt-caching-cost/index.xml" rel="self" type="application/rss+xml"/><item><title>Frugal Tokens: Track Cost and Usage Across All Your Coding Agents</title><link>https://baeseokjae.github.io/posts/frugal-tokens-explore-cost-and-usage-across-all-your-coding-agents/</link><pubDate>Tue, 25 Aug 2026 10:02:22 +0000</pubDate><guid>https://baeseokjae.github.io/posts/frugal-tokens-explore-cost-and-usage-across-all-your-coding-agents/</guid><description>Frugal Tokens is a local, read-only tool that tracks token usage and reported cost across OpenCode, Claude Code, PI, Codex, and Cursor. Here&amp;#39;s how it works and whether you need it.</description><content:encoded><![CDATA[<p>Frugal Tokens is a free, local-first, read-only tool that lets you see exactly how many tokens each of your AI coding agents consumed and what that cost you, across OpenCode, Claude Code, PI, Codex, and Cursor. It runs locally in Deno, never touches your agent databases, and surfaces per-session token usage plus the hidden cost of broken prompt-cache reuse. It is the sharpest per-session cost diagnostic we tested for teams that want to measure ROI on AI coding spend.</p>
<h2 id="what-is-frugal-tokens-a-local-read-only-token-and-cost-viewer-across-your-coding-agents">What is Frugal Tokens? A local, read-only token-and-cost viewer across your coding agents</h2>
<p>Frugal Tokens is an open-source utility that answers one question that most developers cannot answer today: <em>how much did my AI coding agents actually cost, and where did the tokens go?</em> It is built by dpclark4 and appeared on Hacker News as a Show HN (37 points), where it drew a crowd of developers who admitted they had &ldquo;no idea how many cache misses were happening&rdquo; during their sessions.</p>
<p>The tool runs entirely on your own machine using Deno (version 2.9+). It reads the local session and usage databases that your coding agents already write, normalizes them, and serves a read-only web view at <code>localhost:9000</code>. Because it is read-only, it never mutates or corrupts agent state — a meaningful safety property when you are poking around the internals of tools you rely on daily.</p>
<p>At the time of writing, Frugal Tokens supports at least five harnesses: OpenCode, Claude Code, PI (Apple&rsquo;s coding agent), Codex, and Cursor. That cross-harness coverage is what makes it unusual. Most developers work in one primary agent, but many now mix two or three. Frugal Tokens lets you compare them side by side.</p>
<h2 id="the-real-pain-point-hidden-prompt-cache-misses-and-idle-gap-ttl-costs">The real pain point: hidden prompt-cache misses and idle-gap TTL costs</h2>
<p>To understand why a token tracker matters, you have to understand prompt caching — because that is where most silent AI cost lives. When you send repeated turns to a model like Claude or GPT, the provider can cache the shared prefix of your prompt. A cache <em>read</em> is dramatically cheaper than fresh <em>uncached</em> input. In Anthropic&rsquo;s pricing, cached input is typically about 90% cheaper than uncached input. If you keep your session alive and your context stable, subsequent turns bill at the cheap cache-read rate.</p>
<p>The problem is that caches expire. Anthropic&rsquo;s prompt cache, for example, has TTL windows (commonly 5 minutes and 1 hour) after which a cached entry is dropped. When you step away from your machine for a coffee break and come back, your next turn may no longer be cached — and it bills as full-priced uncached input.</p>
<p>The research brief captures a striking real-world example: a single TTL cache miss, in a Fable session where the user responded roughly 1.5 hours later, cost $6 for one message (<a href="https://news.ycombinator.com/item?id=49364223">HN thread</a>). That is the hidden tax of idle gaps. Frugal Tokens specifically surfaces these failures through its cache-miss pricing analysis, which examines <code>uncachedInput</code>, <code>cacheRead</code>, and <code>cacheWrite</code> fields across the 5m/1h TTL windows to estimate the extra dollars you spent because a cache was not reused (<a href="https://github.com/dpclark4/frugal-tokens/blob/main/src/server/cacheMissPricing.ts">source code</a>).</p>
<h2 id="how-frugal-tokens-measures-token-usage-across-opencode-claude-code-pi-codex-and-cursor">How Frugal Tokens measures token usage across OpenCode, Claude Code, PI, Codex, and Cursor</h2>
<p>Frugal Tokens does not intercept your traffic or wrap your CLI. Instead, it reads the telemetry your agents already persist locally:</p>
<ul>
<li><strong>Claude Code</strong> stores session and usage data under its local project and config directories, including per-turn token counts and cache fields.</li>
<li><strong>Codex</strong> keeps JSONL session logs with usage records.</li>
<li><strong>OpenCode</strong> writes message and cost metadata to its storage.</li>
<li><strong>Cursor</strong> and <strong>PI</strong> expose session/usage structures that the tool parses and normalizes.</li>
</ul>
<p>For each harness, Frugal Tokens extracts token usage and the <em>reported cost</em> — the dollar figure the vendor itself associates with those tokens. It deliberately relies on these reported values rather than computing its own pricing model. That is both a strength and a caveat: it gives you the vendor&rsquo;s own accounting, but if a vendor under-reports, so will the tool.</p>
<p>The cross-harness comparison is the feature developers actually wanted. The same logical task burns different token totals depending on the harness, the underlying model (Opus vs Sonnet vs Haiku tiering being a recurring theme), and how aggressively each tool caches. Independent, cross-harness data is, in the words of one HN commenter, &ldquo;super important for companies measuring ROI on token spend as they recover from letting people go wild on token usage in the past year&rdquo; (<a href="https://news.ycombinator.com/item?id=49364223">source</a>).</p>
<h2 id="the-per-session-explorer-and-cache-miss-pricing--the-standout-features">The per-session explorer and cache-miss pricing — the standout features</h2>
<p>Two features distinguish Frugal Tokens from a plain &ldquo;spend dashboard&rdquo;:</p>
<p><strong>The per-session explorer.</strong> Rather than just rolling up a total, Frugal Tokens lets you drill into an individual session and see the token and cost breakdown turn by turn. You can spot the exact moment a session&rsquo;s cost spiked, correlate it with a long idle gap, and see whether that spike was a cache miss.</p>
<p><strong>Cache-miss pricing.</strong> This is the feature that turns observability into action. Frugal Tokens estimates what a session <em>would have</em> cost had cache reuse worked, and compares it to what it <em>did</em> cost. When that gap is large, you have a concrete incentive to redesign your workflow — shorter sessions, faster turnarounds, or a tool with better caching — rather than blindly paying the difference.</p>
<p>Below is a representative breakdown of the cache states Frugal Tokens tracks and why each affects your bill:</p>
<table>
  <thead>
      <tr>
          <th>Cache state</th>
          <th>What it means</th>
          <th>Cost impact</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td><code>cacheWrite</code> (5m/1h)</td>
          <td>Prompt prefix written to cache</td>
          <td>Small write cost, enables future savings</td>
      </tr>
      <tr>
          <td><code>cacheRead</code></td>
          <td>Cached prefix reused from a live TTL window</td>
          <td>~90% cheaper than uncached input</td>
      </tr>
      <tr>
          <td><code>uncachedInput</code></td>
          <td>No cache hit; full prefix billed fresh</td>
          <td>Full price — the expensive path</td>
      </tr>
      <tr>
          <td>TTL miss</td>
          <td>Cache expired because session sat idle too long</td>
          <td>Billed as uncached; can cost dollars per turn</td>
      </tr>
  </tbody>
</table>
<h2 id="frugal-tokens-vs-the-field-agentsview-codeburn-burnrate-and-agentic-metric">Frugal Tokens vs the field: AgentsView, Codeburn, BurnRate, and Agentic Metric</h2>
<p>Frugal Tokens is not alone in the &ldquo;agentic metrics&rdquo; category, but it occupies a specific niche. Here is how it compares to the closest alternatives:</p>
<table>
  <thead>
      <tr>
          <th>Tool</th>
          <th>Primary focus</th>
          <th>Local-first</th>
          <th>Cache-miss diagnostics</th>
          <th>Session-level drill-down</th>
          <th>Best for</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td><strong>Frugal Tokens</strong></td>
          <td>Token + reported cost across 5 harnesses</td>
          <td>Yes (read-only)</td>
          <td>Yes — TTL/cache pricing</td>
          <td>Yes</td>
          <td>Developers who want the hidden cache cost surfaced</td>
      </tr>
      <tr>
          <td><strong>AgentsView</strong></td>
          <td>Browse/search/analyze past sessions from dozens of agents</td>
          <td>Yes (desktop/web + SQLite/Postgres/DuckDB backend)</td>
          <td>Yes — cached vs uncached breakdown</td>
          <td>Yes</td>
          <td>Power users with many agents and export needs</td>
      </tr>
      <tr>
          <td><strong>Codeburn</strong></td>
          <td>Dollar spend only</td>
          <td>Cloud</td>
          <td>No — spend only</td>
          <td>No</td>
          <td>Teams that just want the bottom line</td>
      </tr>
      <tr>
          <td><strong>BurnRate</strong></td>
          <td>Team-wide spend + agent config + cost calculator</td>
          <td>Yes (local-first, free tier)</td>
          <td>Partial</td>
          <td>Partial</td>
          <td>Teams managing budgets across providers</td>
      </tr>
      <tr>
          <td><strong>Agentic Metric</strong></td>
          <td>Lightweight token/cost metrics</td>
          <td>Yes</td>
          <td>Limited</td>
          <td>Limited</td>
          <td>Minimal setups that want a lightweight counter</td>
      </tr>
  </tbody>
</table>
<p>The clearest distinction is <strong>granularity vs. simplicity</strong>. Codeburn explicitly tells you spend and &ldquo;not messages and everything else,&rdquo; which an HN user flagged as a limitation. BurnRate goes further into team-wide cost management and even offers a &ldquo;cut AI costs 40%&rdquo; course. AgentsView reads from the widest set of agents (Claude Code, Codex, Gemini, Copilot, Cursor, Qwen, DeepSeek, OpenCode, and more) and is arguably the most full-featured suite.</p>
<p>Frugal Tokens sits on the diagnostic end: it is not trying to be your entire spend-management platform. It is trying to be the sharpest per-session and per-cache diagnostic available, and its single-session cache-miss pricing is the most precise we found.</p>
<h2 id="local-first-read-only-and-private-why-that-matters-for-teams">Local-first, read-only, and private: why that matters for teams</h2>
<p>A large part of Frugal Tokens&rsquo; appeal is architectural. Everything runs locally in Deno with no cloud dependency. Your agent data — which often contains source code context, file paths, and internal reasoning — never leaves your machine. For teams with sensitive codebases that are understandably wary of sending session telemetry to a third-party SaaS, this is a decisive advantage.</p>
<p>The read-only guarantee reinforces that trust. Frugal Tokens only opens agent databases to read them; it makes no writes. You are not handing your session history to another vendor, and you are not risking corruption of the very tools you depend on. If your security review requires &ldquo;no external exfiltration and no writes,&rdquo; Frugal Tokens passes both checks out of the box.</p>
<p>That local-first posture is also a practical resilience win: no account to create, no API keys to leak, no dependency on a startup&rsquo;s uptime. It is a <code>deno run</code> away from a working dashboard.</p>
<h2 id="who-is-frugal-tokens-for-and-the-trade-offs-vs-spend-only-trackers">Who is Frugal Tokens for, and the trade-offs vs spend-only trackers</h2>
<p>Frugal Tokens is for developers and small teams who want to <em>understand</em> their AI spend, not just see a number. It is the right fit if you:</p>
<ul>
<li>Work across two or more coding agents (Claude Code plus Codex, Cursor plus OpenCode) and want a fair comparison.</li>
<li>Suspect prompt-cache misses are inflating your bill and want proof, per session.</li>
<li>Operate under privacy constraints and refuse to send session data to a cloud dashboard.</li>
<li>Are responsible for justifying AI tooling spend and need concrete, per-session evidence.</li>
</ul>
<p>The trade-offs are real. First, because it relies on each vendor&rsquo;s <em>reported</em> cost, it is only as accurate as the vendors&rsquo; own accounting. Second, it is not a team-wide budget management platform — if you need role-based access, team dashboards, and invoice-level cost management, BurnRate or a full observability suite is a better fit. Third, its harness coverage (five) is narrower than AgentsView&rsquo;s (dozens). Fourth, it is a developer-facing tool: there is no hosted version, so non-technical stakeholders cannot log in and check numbers.</p>
<p>If you only need a single bottom-line spend figure across the team, a spend-only tracker like Codeburn is simpler. If you need per-session cache diagnostics to <em>reduce</em> spend, Frugal Tokens is the sharper instrument.</p>
<h2 id="verdict--should-you-add-per-session-token-cost-tracking-to-your-workflow">Verdict — should you add per-session token cost tracking to your workflow?</h2>
<p>Yes — for most developers actively using AI coding agents, the answer is yes. The single most compelling argument is the $6 single-message TTL miss and the widespread admission that developers have &ldquo;no idea&rdquo; how many cache misses occur. If you cannot see cache misses, you cannot fix them, and you are silently paying a premium on every idle-gap resumption.</p>
<p>Frugal Tokens earns a strong recommendation for the developer who wants a privacy-preserving, read-only, per-session view across the five most common harnesses. It is free, local, and its cache-miss pricing turns vague anxiety about AI cost into a concrete, actionable number. Pair it with a spend-only or team-level tool if you also need the company-wide bottom line, but make Frugal Tokens (or its equivalent diagnostic depth) part of your workflow if you care about token cost tracking for coding agents at all.</p>
<p>The bottom line: AI coding cost is no longer opaque. Tools like Frugal Tokens put the per-session and per-cache truth back in your hands — and for teams measuring ROI on agent spend, that visibility is no longer a nicety. It is a governance requirement.</p>
<h2 id="frequently-asked-questions">Frequently Asked Questions</h2>
<h3 id="is-frugal-tokens-free-to-use">Is Frugal Tokens free to use?</h3>
<p>Yes. Frugal Tokens is open source and runs locally in Deno (2.9+). There is no hosted version and no subscription — you run it yourself on your own machine.</p>
<h3 id="which-coding-agents-does-frugal-tokens-support">Which coding agents does Frugal Tokens support?</h3>
<p>It tracks token usage and reported cost for at least five harnesses: OpenCode, Claude Code, PI (Apple), Codex, and Cursor. This coverage is narrower than some rivals like AgentsView, which reads from dozens of agents.</p>
<h3 id="why-does-my-ai-agent-cost-more-after-i-step-away-from-my-keyboard">Why does my AI agent cost more after I step away from my keyboard?</h3>
<p>Prompt caches expire on a TTL (commonly 5 minutes and 1 hour). If your session sits idle longer than the TTL, the next turn bills as fresh uncached input instead of a cheap cache read. Frugal Tokens surfaces these TTL misses and their dollar cost.</p>
<h3 id="does-frugal-tokens-send-my-data-anywhere">Does Frugal Tokens send my data anywhere?</h3>
<p>No. It is local-first and read-only. It reads your agents&rsquo; local session databases, normalizes the data, and serves a local dashboard at localhost:9000. Nothing leaves your machine, and it makes no writes to your agent databases.</p>
<h3 id="how-accurate-is-the-cost-frugal-tokens-reports">How accurate is the cost Frugal Tokens reports?</h3>
<p>It uses the reported cost figures that each vendor associates with the tokens consumed. That means it is accurate to the extent the vendors&rsquo; own accounting is accurate. It does not compute its own pricing model, so a vendor that under-reports will show lower costs.</p>
]]></content:encoded></item></channel></rss>