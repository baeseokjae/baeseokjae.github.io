<?xml version="1.0" encoding="utf-8" standalone="yes"?><rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom" xmlns:content="http://purl.org/rss/1.0/modules/content/"><channel><title>Zero-Dependency Memory on RockB</title><link>https://baeseokjae.github.io/tags/zero-dependency-memory/</link><description>Recent content in Zero-Dependency Memory on RockB</description><image><title>RockB</title><url>https://baeseokjae.github.io/images/og-default.png</url><link>https://baeseokjae.github.io/images/og-default.png</link></image><generator>Hugo</generator><language>en-us</language><lastBuildDate>Mon, 27 Jul 2026 10:02:34 +0000</lastBuildDate><atom:link href="https://baeseokjae.github.io/tags/zero-dependency-memory/index.xml" rel="self" type="application/rss+xml"/><item><title>Engram MCP: Zero-Dependency Persistent Memory for AI Coding Agents</title><link>https://baeseokjae.github.io/posts/engram-mcp-persistent-ai-agent-memory/</link><pubDate>Mon, 27 Jul 2026 10:02:34 +0000</pubDate><guid>https://baeseokjae.github.io/posts/engram-mcp-persistent-ai-agent-memory/</guid><description>Engram MCP is a zero-dependency Go binary that gives AI coding agents persistent memory via SQLite + FTS5, supporting 12+ agents with 20 MCP tools.</description><content:encoded><![CDATA[<h2 id="what-is-engram-mcp">What is Engram MCP?</h2>
<p>Engram MCP is an open-source, zero-dependency persistent memory system for AI coding agents built as a Model Context Protocol (MCP) server. Written in Go and distributed as a single binary, Engram gives AI agents the ability to remember information across sessions, conversations, and even different machines — without requiring Node.js, Python, Docker, or a vector database. Created by Gentleman-Programming in February 2026, it has rapidly become the most popular AI agent memory solution on GitHub with over 5,700 stars and 605 forks in just five months.</p>
<h2 id="why-ai-agents-need-persistent-memory">Why AI Agents Need Persistent Memory</h2>
<p>Every developer who has worked with AI coding agents has experienced the frustration: you tell the agent your project structure, coding conventions, and architecture decisions in one session, only to have it forget everything when you start a new one. This is the session amnesia problem — the fundamental limitation of stateless AI interactions.</p>
<p>Without persistent memory, AI agents operate in a vacuum. Each session starts from scratch, forcing developers to repeatedly explain project context, preferred patterns, and past decisions. This wastes time, introduces inconsistency, and limits the agent&rsquo;s ability to act as a true long-term coding partner.</p>
<p>Persistent memory solves this by giving agents a durable storage layer they can read from and write to across sessions. When an agent can remember that you prefer async/await over raw promises, that your API follows a specific error-handling pattern, or that a particular module was refactored last week, it transitions from a stateless autocomplete tool to a genuine collaborative partner.</p>
<h2 id="architecture-deep-dive">Architecture Deep Dive</h2>
<p>Engram&rsquo;s architecture is deliberately minimal. The entire system is a single Go binary that communicates with AI agents through MCP&rsquo;s stdio transport protocol. Here is how it works under the hood:</p>
<p><strong>Go Binary with Pure Go SQLite.</strong> Engram is written in Go and uses <code>modernc.org/sqlite</code> — a pure Go SQLite implementation that requires no CGO, no GCC, and no system-level SQLite libraries. This is what makes the zero-dependency promise real: you download one file, make it executable, and it runs on any Linux, macOS, or Windows system.</p>
<p><strong>SQLite + FTS5 Backend.</strong> Instead of spinning up a vector database like ChromaDB, Pinecone, or Qdrant, Engram uses SQLite with its built-in FTS5 full-text search extension. FTS5 provides fast, ranked full-text search over stored memories without any external dependencies. The design decision is deliberate: for the vast majority of agent memory use cases — recalling past decisions, project conventions, code patterns, and configuration preferences — keyword search with FTS5 covers 95% of needs without the complexity, cost, and latency of embedding-based vector search.</p>
<p><strong>MCP stdio Transport.</strong> Engram runs as a subprocess of the AI agent, communicating over standard input/output using the Model Context Protocol. This means no network ports to configure, no servers to manage, and no authentication tokens to set up. The agent launches Engram, and the two processes exchange structured messages through a pipe.</p>
<p><strong>20 MCP Tools.</strong> Engram exposes 20 distinct MCP tools organized into several categories:</p>
<table>
	<thead>
			<tr>
					<th>Category</th>
					<th>Tools</th>
					<th>Purpose</th>
			</tr>
	</thead>
	<tbody>
			<tr>
					<td>Save</td>
					<td><code>mem_save</code>, <code>mem_save_batch</code></td>
					<td>Store new memories with optional tags and priority</td>
			</tr>
			<tr>
					<td>Search</td>
					<td><code>mem_search</code>, <code>mem_search_similar</code></td>
					<td>Retrieve memories by keyword or semantic similarity</td>
			</tr>
			<tr>
					<td>Context</td>
					<td><code>mem_context</code>, <code>mem_context_clear</code></td>
					<td>Manage working context for the current session</td>
			</tr>
			<tr>
					<td>Session</td>
					<td><code>mem_session_start</code>, <code>mem_session_end</code>, <code>mem_session_list</code></td>
					<td>Lifecycle management for memory sessions</td>
			</tr>
			<tr>
					<td>Conflict</td>
					<td><code>mem_judge</code>, <code>mem_resolve</code>, <code>mem_diff</code></td>
					<td>Surface and resolve conflicting memories</td>
			</tr>
			<tr>
					<td>Utility</td>
					<td><code>mem_stats</code>, <code>mem_export</code>, <code>mem_import</code>, <code>mem_clean</code>, <code>mem_backup</code>, <code>mem_restore</code>, <code>mem_config</code>, <code>mem_status</code></td>
					<td>Maintenance, migration, and introspection</td>
			</tr>
	</tbody>
</table>
<p>This comprehensive tool set means agents can not only store and retrieve memories but also manage session boundaries, detect conflicts between old and new information, and perform maintenance operations — all through the same MCP interface.</p>
<h2 id="20-mcp-tools-full-breakdown">20 MCP Tools: Full Breakdown</h2>
<p>The 20 MCP tools in Engram cover the full lifecycle of agent memory management:</p>
<p><strong>Save Tools.</strong> <code>mem_save</code> stores a single memory with optional tags, priority level, and source attribution. <code>mem_save_batch</code> allows bulk storage of multiple memories in one call, useful when an agent processes a large codebase and wants to record many observations at once.</p>
<p><strong>Search Tools.</strong> <code>mem_search</code> performs FTS5 full-text search across all stored memories, returning ranked results with relevance scores. <code>mem_search_similar</code> uses the FTS5 ranking to find semantically related memories based on content overlap — a lightweight alternative to vector embeddings.</p>
<p><strong>Context Tools.</strong> <code>mem_context</code> retrieves the most relevant memories for the current working context, automatically filtering by recency and relevance. <code>mem_context_clear</code> resets the working context when switching tasks or projects.</p>
<p><strong>Session Tools.</strong> <code>mem_session_start</code> begins a new memory session, creating a logical boundary for related memories. <code>mem_session_end</code> closes the current session. <code>mem_session_list</code> enumerates all sessions, allowing agents to navigate between different work contexts.</p>
<p><strong>Conflict Tools.</strong> <code>mem_judge</code> surfaces memories that contradict each other — for example, if an agent was told to use tabs in one session and spaces in another. <code>mem_resolve</code> lets the agent or user pick the correct version. <code>mem_diff</code> shows the difference between conflicting memories.</p>
<p><strong>Utility Tools.</strong> <code>mem_stats</code> provides memory usage statistics. <code>mem_export</code> and <code>mem_import</code> enable memory migration between Engram instances. <code>mem_clean</code> removes stale or low-priority memories. <code>mem_backup</code> and <code>mem_restore</code> provide snapshot-based backup and recovery. <code>mem_config</code> reads or modifies Engram&rsquo;s runtime configuration. <code>mem_status</code> returns the health and state of the memory store.</p>
<h2 id="agent-compatibility">Agent Compatibility</h2>
<p>Engram is designed to be agent-agnostic — it works with any MCP-compatible AI coding agent. The supported agents include:</p>
<table>
	<thead>
			<tr>
					<th>Agent</th>
					<th>Setup Method</th>
					<th>Notes</th>
			</tr>
	</thead>
	<tbody>
			<tr>
					<td>Claude Code</td>
					<td>MCP config file</td>
					<td>Add Engram as an MCP server in claude_desktop_config.json</td>
			</tr>
			<tr>
					<td>Codex</td>
					<td>MCP config file</td>
					<td>Works with Codex CLI via stdio transport</td>
			</tr>
			<tr>
					<td>Gemini CLI</td>
					<td>MCP config file</td>
					<td>Google&rsquo;s agent supports MCP stdio servers</td>
			</tr>
			<tr>
					<td>Cursor</td>
					<td>MCP config file</td>
					<td>Add in Cursor&rsquo;s MCP server settings</td>
			</tr>
			<tr>
					<td>Windsurf</td>
					<td>MCP config file</td>
					<td>Configure in Windsurf&rsquo;s MCP settings panel</td>
			</tr>
			<tr>
					<td>VS Code Copilot</td>
					<td>MCP config file</td>
					<td>Via VS Code MCP extension support</td>
			</tr>
			<tr>
					<td>OpenCode</td>
					<td>MCP config file</td>
					<td>Open-source agent with MCP support</td>
			</tr>
			<tr>
					<td>Antigravity</td>
					<td>MCP config file</td>
					<td>Experimental agent with MCP compatibility</td>
			</tr>
			<tr>
					<td>Qwen Code</td>
					<td>MCP config file</td>
					<td>Alibaba&rsquo;s coding agent</td>
			</tr>
			<tr>
					<td>Kiro</td>
					<td>MCP config file</td>
					<td>Lightweight coding agent</td>
			</tr>
			<tr>
					<td>Kilo Code</td>
					<td>MCP config file</td>
					<td>VS Code extension with MCP support</td>
			</tr>
			<tr>
					<td>Pi</td>
					<td>MCP config file</td>
					<td>General-purpose AI agent</td>
			</tr>
	</tbody>
</table>
<p>The setup is identical across all agents: download the Engram binary, point the agent&rsquo;s MCP configuration to the binary path, and the agent gains persistent memory capabilities immediately.</p>
<h2 id="competitor-comparison">Competitor Comparison</h2>
<p>The MCP memory ecosystem has grown rapidly, with over 10 competing projects on GitHub. Here is how Engram stacks up against the main alternatives:</p>
<table>
	<thead>
			<tr>
					<th>Feature</th>
					<th>Engram</th>
					<th>Nocturne Memory</th>
					<th>Stash</th>
					<th>MegaMemory</th>
					<th>Phantom</th>
			</tr>
	</thead>
	<tbody>
			<tr>
					<td><strong>Stars</strong></td>
					<td>5,712</td>
					<td>1,278</td>
					<td>759</td>
					<td>305</td>
					<td>1,454</td>
			</tr>
			<tr>
					<td><strong>Language</strong></td>
					<td>Go</td>
					<td>Python</td>
					<td>Go</td>
					<td>TypeScript</td>
					<td>Python</td>
			</tr>
			<tr>
					<td><strong>Dependencies</strong></td>
					<td>None</td>
					<td>Python runtime</td>
					<td>Postgres</td>
					<td>Node.js</td>
					<td>Python runtime</td>
			</tr>
			<tr>
					<td><strong>Backend</strong></td>
					<td>SQLite + FTS5</td>
					<td>Graph-like</td>
					<td>Postgres</td>
					<td>In-process embeddings</td>
					<td>Custom</td>
			</tr>
			<tr>
					<td><strong>Search</strong></td>
					<td>FTS5 keyword</td>
					<td>Graph traversal</td>
					<td>SQL</td>
					<td>Semantic</td>
					<td>Custom</td>
			</tr>
			<tr>
					<td><strong>License</strong></td>
					<td>MIT</td>
					<td>MIT</td>
					<td>Apache-2.0</td>
					<td>MIT</td>
					<td>Custom</td>
			</tr>
			<tr>
					<td><strong>Binary Size</strong></td>
					<td>~10 MB</td>
					<td>N/A</td>
					<td>~10 MB</td>
					<td>N/A</td>
					<td>N/A</td>
			</tr>
			<tr>
					<td><strong>Cloud Sync</strong></td>
					<td>Optional</td>
					<td>No</td>
					<td>No</td>
					<td>No</td>
					<td>Yes</td>
			</tr>
			<tr>
					<td><strong>Git Sync</strong></td>
					<td>Yes</td>
					<td>No</td>
					<td>No</td>
					<td>No</td>
					<td>No</td>
			</tr>
			<tr>
					<td><strong>Agent Count</strong></td>
					<td>12+</td>
					<td>Any MCP</td>
					<td>Any MCP</td>
					<td>Any MCP</td>
					<td>Single agent</td>
			</tr>
	</tbody>
</table>
<p>Engram leads in adoption (5,712 stars), zero-dependency architecture, and unique features like Git sync and two-layer privacy stripping. Nocturne Memory offers a graph-based alternative for structured memory relationships. Stash provides a Postgres-backed solution for teams already running Postgres. MegaMemory brings semantic search with in-process embeddings. Phantom is more of a full AI assistant than a pure memory layer.</p>
<h2 id="zero-dependency-advantage">Zero-Dependency Advantage</h2>
<p>The single-binary approach is Engram&rsquo;s most distinctive technical advantage. Here is why it matters:</p>
<p><strong>No Runtime Requirements.</strong> Competing solutions require Node.js (MegaMemory, AgentRecall-X), Python (Nocturne, Phantom), Docker (EngramMemory/engram-memory), or Postgres (Stash). Engram requires nothing — not even a Go runtime, since it is compiled to a static binary.</p>
<p><strong>Instant Setup.</strong> Download the binary, <code>chmod +x</code>, configure the MCP path, and you are done. There is no <code>npm install</code>, <code>pip install</code>, <code>docker pull</code>, or <code>apt-get</code> step. This makes Engram ideal for CI/CD pipelines, ephemeral development environments, and air-gapped systems.</p>
<p><strong>Cross-Platform Consistency.</strong> Because Engram bundles its own SQLite (via <code>modernc.org/sqlite</code>), the behavior is identical on every platform. There is no risk of version mismatches between system SQLite libraries or missing FTS5 support.</p>
<p><strong>Small Footprint.</strong> The Engram binary is approximately 10 MB — tiny compared to a Node.js runtime (~50 MB), Python interpreter (~30 MB), or Docker image (~200 MB+). This matters for resource-constrained environments like edge devices, containers, and cloud functions.</p>
<p><strong>No Port Conflicts.</strong> Since Engram uses MCP stdio transport, there are no network ports to manage. Multiple Engram instances can run simultaneously without port conflicts, each attached to a different agent session.</p>
<h2 id="sqlite--fts5-vs-vector-databases">SQLite + FTS5 vs Vector Databases</h2>
<p>One of Engram&rsquo;s most debated design decisions is using SQLite FTS5 instead of vector embeddings. Here is the trade-off:</p>
<p><strong>When FTS5 Wins.</strong> For the majority of agent memory use cases, keyword search is sufficient. When an agent needs to recall &ldquo;the error handling pattern we agreed on&rdquo; or &ldquo;the database schema for the user model,&rdquo; FTS5 full-text search finds the relevant memory quickly and accurately. FTS5 supports ranked results, prefix queries, and phrase matching — all without generating a single embedding vector.</p>
<p><strong>When Vector Search Wins.</strong> For fuzzy semantic matching — &ldquo;find memories similar to this concept&rdquo; without knowing the exact keywords — vector embeddings provide better recall. If an agent needs to find &ldquo;all memories related to authentication&rdquo; but the stored memories use terms like &ldquo;login,&rdquo; &ldquo;OAuth,&rdquo; &ldquo;JWT,&rdquo; and &ldquo;SSO,&rdquo; vector search would surface them all while FTS5 might miss some.</p>
<p><strong>The Pragmatic Verdict.</strong> Engram&rsquo;s approach covers 95% of real-world agent memory use cases with zero embedding costs, zero API calls, and zero infrastructure. For the remaining 5% where semantic search is genuinely needed, Engram&rsquo;s <code>mem_search_similar</code> tool uses FTS5&rsquo;s built-in ranking to approximate semantic matching. Projects that require true vector search can look at MegaMemory (in-process embeddings) or EngramMemory/engram-memory (Qdrant + FastEmbed).</p>
<h2 id="privacy-and-data-control">Privacy and Data Control</h2>
<p>Engram takes a privacy-first approach to agent memory with a two-layer stripping architecture:</p>
<p><strong>Plugin-Level Stripping.</strong> Before a memory reaches the storage layer, the MCP plugin strips sensitive information — API keys, tokens, file paths, and personally identifiable information — from the memory content. This happens at the agent integration level, before data is transmitted to Engram.</p>
<p><strong>Store-Level Stripping.</strong> The storage layer applies a second round of sanitization, catching anything the plugin layer might have missed. This defense-in-depth approach ensures that sensitive data never persists in the memory store.</p>
<p><strong>Local-First by Default.</strong> All memories are stored in a local SQLite database on the user&rsquo;s machine. No data leaves the local system unless the user explicitly opts into cloud sync. This contrasts with cloud-dependent memory services like Mem0 and Zep, which require sending memory data to external servers.</p>
<p><strong>Full Data Portability.</strong> Because memories are stored in a standard SQLite database, users have complete access to their data. The <code>mem_export</code> and <code>mem_import</code> tools allow migration between Engram instances, and the raw SQLite file can be queried with any SQLite client.</p>
<h2 id="git-sync-and-cross-machine-memory">Git Sync and Cross-Machine Memory</h2>
<p>One of Engram&rsquo;s most innovative features is Git-based memory synchronization. Here is how it works:</p>
<p><strong>Compressed Chunk Storage.</strong> Memories are stored as compressed chunks rather than individual files. This keeps the storage efficient and avoids the problem of thousands of tiny files that plagues file-based memory systems.</p>
<p><strong>No Merge Conflicts.</strong> Because Engram uses a chunk-based approach with append-only semantics, there are no merge conflicts when syncing between machines. Each machine&rsquo;s memories are additive — new chunks are added, and conflicts are resolved at the application level rather than the file level.</p>
<p><strong>Cross-Machine Workflow.</strong> A developer can work on their desktop in the morning, commit memory changes to a Git repository, and pull those memories on their laptop in the afternoon. The agent on the laptop has full context of the morning&rsquo;s work without any manual transfer.</p>
<p><strong>Team Collaboration.</strong> Multiple developers working on the same project can share memory through a shared Git repository. When one developer&rsquo;s agent learns a project convention or discovers a bug pattern, that knowledge propagates to the entire team&rsquo;s agents through Git sync.</p>
<h2 id="cloud-integration">Cloud Integration</h2>
<p>For users who want cloud-based memory synchronization without managing Git repositories, Engram offers opt-in cloud replication:</p>
<p><strong>Project-Scoped Sync.</strong> Cloud sync is organized by project, not by user. This means memories are scoped to the project context, preventing cross-project contamination.</p>
<p><strong>Local SQLite Remains Authoritative.</strong> Even with cloud sync enabled, the local SQLite database remains the primary data store. The cloud is a replication target, not a source of truth. This ensures that the system works offline and that the user always has full control over their data.</p>
<p><strong>Encrypted Transport.</strong> Cloud sync uses encrypted channels to protect memory data in transit. The two-layer stripping ensures that even if the transport were compromised, sensitive data would not be exposed.</p>
<h2 id="getting-started">Getting Started</h2>
<p>Getting started with Engram takes less than five minutes:</p>
<p><strong>Step 1: Download the Binary.</strong> Download the latest Engram release from the GitHub releases page. Choose the binary for your operating system and architecture.</p>
<p><strong>Step 2: Make It Executable.</strong> Run <code>chmod +x engram</code> on Linux or macOS. On Windows, the binary runs directly.</p>
<p><strong>Step 3: Configure Your Agent.</strong> Add Engram as an MCP server in your agent&rsquo;s configuration. For Claude Code, this means adding an entry to <code>claude_desktop_config.json</code>:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-json" data-lang="json"><span style="display:flex;"><span>{
</span></span><span style="display:flex;"><span>  <span style="color:#f92672">&#34;mcpServers&#34;</span>: {
</span></span><span style="display:flex;"><span>    <span style="color:#f92672">&#34;engram&#34;</span>: {
</span></span><span style="display:flex;"><span>      <span style="color:#f92672">&#34;command&#34;</span>: <span style="color:#e6db74">&#34;/path/to/engram&#34;</span>,
</span></span><span style="display:flex;"><span>      <span style="color:#f92672">&#34;args&#34;</span>: [<span style="color:#e6db74">&#34;mcp&#34;</span>]
</span></span><span style="display:flex;"><span>    }
</span></span><span style="display:flex;"><span>  }
</span></span><span style="display:flex;"><span>}
</span></span></code></pre></div><p><strong>Step 4: Save Your First Memory.</strong> Ask your agent to remember something: &ldquo;Save that I prefer async/await over .then() chains.&rdquo; The agent calls <code>mem_save</code> through MCP, and the memory is stored in the local SQLite database.</p>
<p><strong>Step 5: Verify Persistence.</strong> Start a new session and ask: &ldquo;What do I prefer for async code?&rdquo; The agent calls <code>mem_search</code>, finds the stored memory, and applies it — no re-explanation needed.</p>
<h2 id="verdict">Verdict</h2>
<p>Engram MCP is the best choice for developers and teams who want persistent AI agent memory without infrastructure overhead. Its zero-dependency design, SQLite + FTS5 backend, and agent-agnostic compatibility make it the most accessible memory solution in the MCP ecosystem.</p>
<p><strong>Choose Engram if:</strong> You want instant setup, no runtime dependencies, local-first privacy, and compatibility with any MCP-compatible agent. It is ideal for individual developers, small teams, and anyone who values simplicity and data control.</p>
<p><strong>Consider alternatives if:</strong> You need true semantic vector search (MegaMemory or EngramMemory/engram-memory), you are already running Postgres and want a SQL-backed solution (Stash), or you prefer graph-based structured memory (Nocturne Memory).</p>
<p>With 5,712 GitHub stars, 20 MCP tools, support for 12+ agents, and a thriving open-source community, Engram has established itself as the de facto standard for AI agent persistent memory in 2026.</p>
<h2 id="faq">FAQ</h2>
<p><strong>Q: Does Engram require an internet connection?</strong>
A: No. Engram is fully local-first. All memories are stored in a local SQLite database on your machine. Cloud sync is optional and opt-in only.</p>
<p><strong>Q: Can Engram work with multiple agents simultaneously?</strong>
A: Yes. Engram is agent-agnostic and can be configured with any MCP-compatible agent. Each agent instance gets its own Engram process via stdio transport.</p>
<p><strong>Q: How does Engram handle conflicting memories?</strong>
A: Engram provides dedicated conflict tools — <code>mem_judge</code> surfaces contradictions, <code>mem_diff</code> shows the differences, and <code>mem_resolve</code> lets the agent or user pick the correct version.</p>
<p><strong>Q: Is Engram free to use?</strong>
A: Yes. Engram is MIT-licensed open-source software. There are no paid tiers, no API costs, and no usage limits. Cloud sync is also free and optional.</p>
<p><strong>Q: What happens to my memories if I uninstall Engram?</strong>
A: Your memories are stored in a standard SQLite database file. You can back it up with <code>mem_backup</code>, export it with <code>mem_export</code>, or simply copy the SQLite file before uninstalling. The data is fully portable.</p>
]]></content:encoded></item></channel></rss>