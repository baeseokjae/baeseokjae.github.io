<?xml version="1.0" encoding="utf-8" standalone="yes"?><rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom" xmlns:content="http://purl.org/rss/1.0/modules/content/"><channel><title>AI Engineering on RockB</title><link>https://baeseokjae.github.io/tags/ai-engineering/</link><description>Recent content in AI Engineering on RockB</description><image><title>RockB</title><url>https://baeseokjae.github.io/images/og-default.png</url><link>https://baeseokjae.github.io/images/og-default.png</link></image><generator>Hugo</generator><language>en-us</language><lastBuildDate>Mon, 15 Jun 2026 15:12:58 +0000</lastBuildDate><atom:link href="https://baeseokjae.github.io/tags/ai-engineering/index.xml" rel="self" type="application/rss+xml"/><item><title>AI Harness Engineering: Structured Workflows for Deterministic AI-Assisted Development</title><link>https://baeseokjae.github.io/posts/ai-harness-engineering-guide-2026/</link><pubDate>Mon, 15 Jun 2026 15:12:58 +0000</pubDate><guid>https://baeseokjae.github.io/posts/ai-harness-engineering-guide-2026/</guid><description>A practical guide to AI harness engineering for deterministic AI-assisted development workflows.</description><content:encoded><![CDATA[<p>AI harness engineering is the practice of wrapping AI coding agents in structured workflows, constraints, state, and verification so their output becomes repeatable enough for production software delivery. The useful shift is not better prompting. It is turning AI assistance into an engineered system with typed inputs, tool limits, tests, and review gates.</p>
<h2 id="what-is-ai-harness-engineering">What Is AI Harness Engineering?</h2>
<p>AI harness engineering is the design of the system around an AI coding model: the intake format, repository context, tool permissions, execution state, verification checks, and escalation rules that determine how work moves from request to merged code. OpenAI described an internal agent-first beta product in 2026 that produced roughly one million lines and about 1,500 merged pull requests over five months, which shows the scale this pattern targets. A harness does not make a model deterministic in the mathematical sense. It makes the surrounding workflow deterministic enough that the same class of request follows the same route, gathers the same evidence, hits the same checks, and leaves the same artifacts. In practice, the harness becomes the operating system for AI-assisted development. The takeaway: reliable AI coding comes from engineered boundaries, not from trusting a chat transcript.</p>
<h3 id="what-does-a-harness-include">What does a harness include?</h3>
<p>An AI harness includes the repeatable parts a senior engineer would otherwise hold in their head: repo maps, ownership rules, coding standards, sandbox permissions, test commands, PR templates, review criteria, and failure policies. The model still writes and edits code, but the harness defines where it can work, what it must prove, and when it must stop.</p>
<h2 id="why-prompting-alone-does-not-make-ai-development-deterministic">Why Prompting Alone Does Not Make AI Development Deterministic?</h2>
<p>Prompting alone does not make AI development deterministic because a prompt is advisory text, while a harness is an enforceable workflow with state, constraints, and checks. Stack Overflow&rsquo;s 2025 Developer Survey found that 84% of respondents use or plan to use AI tools in development, yet 66% cite &ldquo;almost right, but not quite&rdquo; answers as a frustration. That gap is the prompt-only failure mode: the model can sound precise while choosing the wrong module, inventing an API, missing a migration, or skipping a security constraint. A strong prompt may improve one session, but it rarely guarantees that the next session will inspect the same files, run the same tests, or preserve the same architecture boundary. Deterministic AI-assisted development requires repeatable inputs and observable outputs. The takeaway: prompts can guide behavior, but harnesses make behavior inspectable and enforceable.</p>
<h3 id="where-do-prompts-still-help">Where do prompts still help?</h3>
<p>Prompts still help when they are treated as one feedforward control inside a larger system. A good repo instruction file can define naming conventions, preferred libraries, and review expectations. The difference is that the harness backs those words with file scopes, command allowlists, automated checks, and human approval points.</p>
<h2 id="what-is-the-harness-formula-model-context-tools-state-and-verification">What Is the Harness Formula: Model, Context, Tools, State, and Verification?</h2>
<p>The harness formula is model plus context plus tools plus state plus verification, arranged as a workflow instead of a conversation. Google Cloud&rsquo;s 2025 DORA report announcement said 90% of respondents use AI at work and more than 80% believe it increased productivity, but 30% report little or no trust in AI-generated code. That trust gap appears when one element of the formula is weak. A capable model without repository context guesses. Context without tools cannot make changes. Tools without state lose continuity across sessions. State without verification preserves mistakes. Verification without routing creates review bottlenecks. A useful harness explicitly names each part: which model handles planning, which files define context, which commands are allowed, where progress is recorded, and which checks decide completion. The takeaway: AI development becomes dependable when every agent action has context, capability, memory, and proof.</p>
<h3 id="how-should-teams-split-planning-and-coding">How should teams split planning and coding?</h3>
<p>Teams should split planning and coding when the change has meaningful blast radius. A planning pass can produce a repository impact map with touched files, symbols, risks, and tests. A coding pass then receives a narrower contract. That separation catches wrong assumptions before the agent starts editing production code.</p>
<h2 id="how-does-harness-engineering-differ-from-prompt-engineering-and-context-engineering">How Does Harness Engineering Differ From Prompt Engineering and Context Engineering?</h2>
<p>Harness engineering differs from prompt engineering and context engineering by controlling the whole delivery path, not just the model&rsquo;s input text. Prompt engineering tunes instructions; context engineering selects the evidence the model sees; AI harness engineering defines the workflow that turns an intent into reviewed, tested, mergeable work. The distinction matters in 2026 because AI coding is no longer a single developer asking for a helper function. It is agents modifying multi-repository systems, generating tests, calling CLIs, opening pull requests, and continuing work across context windows. Prompt engineering can say &ldquo;follow the architecture.&rdquo; Context engineering can provide the architecture document. Harness engineering can block edits outside owned modules, require an impact map, run architecture fitness tests, and route exceptions to a human. The takeaway: prompts and context are components; the harness is the production system.</p>
<table>
  <thead>
      <tr>
          <th>Discipline</th>
          <th>Primary object</th>
          <th>Failure it reduces</th>
          <th>Enforcement level</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>Prompt engineering</td>
          <td>Instructions</td>
          <td>Vague model behavior</td>
          <td>Low</td>
      </tr>
      <tr>
          <td>Context engineering</td>
          <td>Retrieved evidence</td>
          <td>Missing or stale knowledge</td>
          <td>Medium</td>
      </tr>
      <tr>
          <td>Harness engineering</td>
          <td>Workflow and controls</td>
          <td>Unreviewed, unstable delivery</td>
          <td>High</td>
      </tr>
  </tbody>
</table>
<h2 id="what-is-the-structured-workflow-pattern-for-ai-assisted-development">What Is the Structured Workflow Pattern for AI-Assisted Development?</h2>
<p>The structured workflow pattern for AI-assisted development is intake, impact map, task contract, execution, verification, and review. Red Hat&rsquo;s 2026 discussion of harness engineering emphasizes moving from free-form tickets to explicit files, symbols, acceptance criteria, and test expectations before implementation begins. That sequence mirrors how strong human teams work: define the problem, identify the affected surface area, narrow the implementation scope, make the change, prove the behavior, and then review the evidence. The AI-specific benefit is that every handoff becomes machine-readable enough for an agent to resume or for a reviewer to audit. A vague request like &ldquo;fix billing retries&rdquo; becomes a contract naming the retry scheduler, idempotency key path, database migration risk, expected tests, and rollback concern. The takeaway: structure in produces structure out.</p>
<h3 id="what-should-an-impact-map-contain">What should an impact map contain?</h3>
<p>An impact map should list likely files, symbols, routes, database tables, feature flags, owners, risks, and required checks. It is not a design essay. It is a routing artifact that tells the coding agent where to look and tells the reviewer whether the agent edited the correct part of the system.</p>
<h2 id="how-do-you-build-a-deterministic-ai-assisted-development-harness">How Do You Build a Deterministic AI-Assisted Development Harness?</h2>
<p>You build a deterministic AI-assisted development harness by converting implicit engineering judgment into explicit workflow rules that agents must follow before, during, and after code changes. A practical first version can be built in two weeks for one repository: define an intake schema, require an impact map, constrain file access by task, run a known test matrix, emit a review summary, and block completion when checks fail. The important decision is to start with one high-volume workflow, such as bug fixes or dependency updates, instead of trying to harness every engineering activity at once. Determinism comes from narrowing degrees of freedom: the same request type uses the same planner, the same artifacts, the same sandbox, and the same exit criteria. The takeaway: start with the smallest workflow where repeatability matters and make every step observable.</p>
<h3 id="what-is-a-good-first-workflow-to-harness">What is a good first workflow to harness?</h3>
<p>A good first workflow is a repetitive change with known verification, such as small bug fixes, test repairs, API client updates, or dependency bumps. Avoid starting with ambiguous architecture work. The harness should prove value on tasks where success can be checked with tests, diff review, and clear acceptance criteria.</p>
<h2 id="what-guides-and-feedforward-controls-should-a-harness-use">What Guides and Feedforward Controls Should a Harness Use?</h2>
<p>Guides and feedforward controls are the up-front artifacts that steer an AI agent before it makes a change, including repo rules, architecture maps, AGENTS.md files, ownership boundaries, and tool permissions. Martin Fowler&rsquo;s harness framing separates these feedforward guides from feedback sensors because they solve different problems: guides reduce wrong first moves, while sensors catch bad outcomes. In a real repository, feedforward controls should answer basic questions without requiring the model to infer them from thousands of files. Which package owns authentication? Which migration pattern is allowed? Which test runner covers frontend components? Which directories are off-limits without approval? The strongest guides are short, local, and enforceable. A 300-line architecture manifesto is less useful than a directory-level rule backed by CI. The takeaway: give agents the same guardrails you expect new senior engineers to learn quickly.</p>
<h3 id="how-should-agentsmd-be-written">How should AGENTS.md be written?</h3>
<p>AGENTS.md should be specific, local, and operational. Include build commands, test commands, forbidden patterns, naming rules, dependency policy, and review expectations. Keep strategic context elsewhere. The file should help an agent choose the next command or reject a risky edit, not teach the full company history.</p>
<h2 id="what-sensors-and-feedback-loops-make-ai-coding-reliable">What Sensors and Feedback Loops Make AI Coding Reliable?</h2>
<p>Sensors and feedback loops make AI coding reliable by turning generated code into measured evidence through tests, linters, type checks, evals, logs, review agents, and CI gates. Apiiro reported that by June 2025, AI-generated code introduced more than 10,000 new security findings per month across studied repositories, even while trivial syntax errors dropped sharply. That pattern is exactly why syntax-level checks are not enough. A harness needs layered sensors: fast local checks for immediate correction, deeper CI checks for integration confidence, security scans for abuse paths, and review summaries that highlight risky assumptions. Feedback loops should be automatic where possible and explicit where judgment is needed. If a test fails, the agent can retry inside a limit. If a permission boundary changes, a human should approve. The takeaway: reliable AI delivery depends on sensors that detect the failures models are least likely to notice.</p>
<h3 id="what-checks-belong-in-the-fast-loop">What checks belong in the fast loop?</h3>
<p>The fast loop should include formatting, type checks, focused unit tests, generated artifact checks, and static analysis that runs in minutes. Slow end-to-end suites can run later, but the agent needs quick evidence while the change is still fresh enough to repair without guessing.</p>
<h2 id="how-do-you-manage-long-running-agents-across-context-windows">How Do You Manage Long-Running Agents Across Context Windows?</h2>
<p>Managing long-running agents across context windows means treating continuity as a first-class harness concern, not as a hope that the next session will remember the previous one. Anthropic&rsquo;s engineering guidance on long-running agents highlights the problem directly: each new coding session may start without full prior working memory, so durable artifacts must carry the work forward. In practice, that means every agent run should leave a compact state file or issue comment with the objective, completed steps, changed files, commands run, failing checks, unresolved decisions, and exact next action. The harness should distinguish durable state from conversational noise. A future agent should not need to reread a 200-message transcript to know whether a migration was applied. The takeaway: if the workflow can span context windows, the handoff artifact is part of the product.</p>
<h3 id="what-should-a-continuation-note-include">What should a continuation note include?</h3>
<p>A continuation note should include the goal, current status, files touched, commands run, test results, blockers, decisions made, and the next concrete command or edit. Avoid vague notes like &ldquo;continue debugging.&rdquo; The next session needs a restart point, not a memory test.</p>
<h2 id="what-security-and-governance-controls-does-ai-generated-code-need">What Security and Governance Controls Does AI-Generated Code Need?</h2>
<p>AI-generated code needs security and governance controls that focus on authorization, data access, dependency changes, secrets, compliance boundaries, and architectural drift. Apiiro reported that privilege escalation paths increased 322% and architectural design flaws rose 153% in studied AI-written code, even as syntax errors and many logic bugs decreased. That mix should change how teams review AI output. The easy defects may be caught by compilers and tests, while the dangerous defects hide in permission checks, trust boundaries, and cross-service assumptions. A harness should require threat-aware review for auth changes, block secret exposure, flag new dependencies, require owners for sensitive directories, and record why an agent needed elevated tool access. Governance is not bureaucracy when agents can change many files quickly. The takeaway: AI code review must prioritize systemic risk, not just whether the diff runs.</p>
<h3 id="where-should-human-approval-be-mandatory">Where should human approval be mandatory?</h3>
<p>Human approval should be mandatory for authentication, authorization, payments, migrations, public APIs, cryptography, production infrastructure, compliance logic, and dependency additions. These areas encode business and risk decisions that tests rarely cover completely. The harness can prepare evidence, but it should not silently approve these changes.</p>
<h2 id="what-metrics-should-engineering-teams-track-for-ai-harnesses">What Metrics Should Engineering Teams Track for AI Harnesses?</h2>
<p>Engineering teams should track AI harness metrics that show whether agent-assisted work is becoming faster, safer, and easier to review, including first-pass success, review toil, pull request size, rework rate, escaped defects, delivery stability, and time to green CI. The 2025 DORA findings say AI adoption has a positive relationship with throughput and product performance, but a negative relationship with software delivery stability unless teams have strong testing, version control, feedback loops, and platform practices. That is the measurement warning. Counting generated lines or accepted suggestions is shallow because it rewards volume. A better dashboard compares agent work to human baselines: how often the first patch passes checks, how many reviewer comments are about basic mistakes, how often changes are reverted, and whether incidents rise. The takeaway: measure the whole delivery system, not the model&rsquo;s apparent speed.</p>
<table>
  <thead>
      <tr>
          <th>Metric</th>
          <th>What it reveals</th>
          <th>Bad signal</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>First-pass success</td>
          <td>Harness quality and context fit</td>
          <td>Frequent basic failures</td>
      </tr>
      <tr>
          <td>Review toil</td>
          <td>Human load</td>
          <td>Repeated comments on same issue</td>
      </tr>
      <tr>
          <td>PR size</td>
          <td>Scope control</td>
          <td>Large mixed-purpose diffs</td>
      </tr>
      <tr>
          <td>Rework rate</td>
          <td>Planning accuracy</td>
          <td>Multiple rewrites after review</td>
      </tr>
      <tr>
          <td>Escaped defects</td>
          <td>Production safety</td>
          <td>Faster merges with more incidents</td>
      </tr>
  </tbody>
</table>
<h2 id="what-are-common-failure-modes-and-the-harness-fix-for-each">What Are Common Failure Modes and the Harness Fix for Each?</h2>
<p>Common AI coding failure modes include vague context, wrong-file edits, invented APIs, architecture drift, long-running context loss, security regressions, review overload, and merge instability. Stack Overflow&rsquo;s 2025 survey found that 45.2% of developers cite time-consuming debugging of AI-generated code, which is often the cost of discovering these failures late. A harness fixes them by moving discovery earlier and making correction mechanical. Vague context gets an intake schema. Wrong files get an impact map and ownership rules. Invented APIs get compile checks and repository search requirements. Architecture drift gets fitness tests and directory policies. Context loss gets durable handoff notes. Security regressions get targeted scanners and human gates. Review overload gets smaller task contracts and generated evidence summaries. Merge instability gets branch discipline and CI queues. The takeaway: every repeated AI failure should become a harness rule, not a reviewer reminder.</p>
<table>
  <thead>
      <tr>
          <th>Failure mode</th>
          <th>Harness fix</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>Vague request</td>
          <td>Required intake fields and acceptance criteria</td>
      </tr>
      <tr>
          <td>Wrong module</td>
          <td>Repository impact map before edits</td>
      </tr>
      <tr>
          <td>Invented API</td>
          <td>Search-before-use rule and type checks</td>
      </tr>
      <tr>
          <td>Context loss</td>
          <td>Durable continuation artifact</td>
      </tr>
      <tr>
          <td>Security regression</td>
          <td>Sensitive-path gates and security scans</td>
      </tr>
      <tr>
          <td>Review overload</td>
          <td>PR size limits and evidence summary</td>
      </tr>
  </tbody>
</table>
<h2 id="what-is-a-reference-ai-harness-blueprint-for-engineering-teams">What Is a Reference AI Harness Blueprint for Engineering Teams?</h2>
<p>A reference AI harness blueprint is a small set of workflow components that turn AI coding from ad hoc assistance into a controlled delivery lane. A solid 2026 blueprint has nine parts: intake schema, repository impact map, task contract, context pack, tool permission policy, execution sandbox, automated verification, review checklist, and retry or escalation policy. Each part should be versioned near the code so it evolves with the system. For example, a bug-fix lane might accept a ticket, generate a scoped impact map, ask a human to approve the plan, grant edit access only to named directories, run focused tests, produce a PR summary with risk notes, and stop after two failed repair attempts. The blueprint is intentionally boring because boring workflows are inspectable. The takeaway: the best harness is the one your team can audit under pressure.</p>
<h3 id="what-should-be-versioned">What should be versioned?</h3>
<p>Version the intake schema, prompt templates, repo rules, tool policies, test matrices, review checklists, and escalation rules. If a harness decision affects production code, it should be reviewable like production code. Hidden dashboard settings and private prompts create behavior that the team cannot debug.</p>
<h2 id="what-is-the-2026-outlook-for-engineers-as-harness-designers">What Is the 2026 Outlook for Engineers as Harness Designers?</h2>
<p>The 2026 outlook is that senior engineers will spend more time designing harnesses, reviewing system behavior, and improving feedback loops while agents perform more routine implementation work. OpenAI estimated its agent-first internal experiment took about one tenth the time it would have taken to write the code by hand, but that result depended on humans steering architecture, process, and review rather than disappearing from the loop. This is the realistic future: engineers become responsible for shaping the environment where AI work happens. The valuable skill is not typing every line or writing clever one-off prompts. It is encoding architectural intent, narrowing ambiguity, defining quality gates, and turning repeated review comments into automated controls. Teams that build this capability will get compounding benefits because every harness improvement helps future work. The takeaway: AI does not remove engineering judgment; it moves judgment into the system.</p>
<h3 id="how-should-teams-change-roles">How should teams change roles?</h3>
<p>Teams should make ownership explicit. Staff engineers can own architecture guides and risk gates. Platform teams can own execution sandboxes and CI feedback. Product engineers can own acceptance criteria and domain examples. Security can own sensitive-path policies. The harness works when every control has a maintainer.</p>
<h2 id="faq">FAQ</h2>
<p>AI harness engineering FAQs usually center on scope, determinism, and adoption because teams are moving from individual AI coding experiments to production workflows. Stack Overflow&rsquo;s 2025 survey found 51% of professional developers use AI tools daily, but the same teams still face debugging burden, trust gaps, and review risk. The practical questions are not whether AI can generate code, but how a team should constrain that generation, prove the result, and decide when humans must intervene. Treat these answers as operating guidance for engineering leaders and senior developers building a repeatable AI-assisted development lane. A useful harness starts small, measures delivery outcomes, and turns recurring review pain into explicit workflow controls. The takeaway: the FAQ for AI harness engineering is really about making AI work auditable enough for normal software ownership.</p>
<h3 id="what-is-ai-harness-engineering-in-simple-terms">What is AI harness engineering in simple terms?</h3>
<p>AI harness engineering is the practice of putting structured workflow, context, tool limits, tests, and review gates around AI coding agents. Instead of asking a model to &ldquo;be careful,&rdquo; the harness defines what it may edit, what evidence it must gather, and what checks must pass.</p>
<h3 id="is-ai-harness-engineering-the-same-as-prompt-engineering">Is AI harness engineering the same as prompt engineering?</h3>
<p>AI harness engineering is broader than prompt engineering. Prompt engineering improves instructions given to the model, while harness engineering controls the full path from request intake to verified code. A harness may use prompts, but it also includes permissions, state, tests, CI, and escalation policies.</p>
<h3 id="can-ai-assisted-development-be-truly-deterministic">Can AI-assisted development be truly deterministic?</h3>
<p>AI-assisted development is not deterministic at the model-output level, but it can be deterministic at the workflow level. The same request type can follow the same intake, planning, execution, verification, and review sequence. That makes outcomes more repeatable and failures easier to diagnose.</p>
<h3 id="what-is-the-best-first-ai-harness-to-build">What is the best first AI harness to build?</h3>
<p>The best first AI harness is a narrow, high-volume workflow with clear tests, such as bug fixes, test repairs, dependency updates, or API client changes. Start where verification is strong. Avoid ambiguous redesign work until the team has reliable planning and review loops.</p>
<h3 id="how-do-you-know-an-ai-harness-is-working">How do you know an AI harness is working?</h3>
<p>An AI harness is working when first-pass success rises, review comments become less repetitive, PRs stay smaller, rework falls, CI failures become easier to diagnose, and production defects do not increase. The goal is not more generated code. The goal is faster delivery with stable quality.</p>
]]></content:encoded></item><item><title>Claude Opus 4.8 to Fable 5 Migration Guide: What Changes in 2026</title><link>https://baeseokjae.github.io/posts/claude-opus-4-8-to-fable-5-migration-guide-2026/</link><pubDate>Mon, 15 Jun 2026 03:11:29 +0000</pubDate><guid>https://baeseokjae.github.io/posts/claude-opus-4-8-to-fable-5-migration-guide-2026/</guid><description>Practical Claude Opus 4.8 to Fable 5 migration guide covering API changes, routing, cost, refusals, and compliance.</description><content:encoded><![CDATA[<p>Claude Opus 4.8 to Fable 5 migration is not just a model ID swap. Update <code>claude-opus-4-8</code> to <code>claude-fable-5</code>, then retest thinking budgets, refusal handling, fallback paths, data retention, and cost per completed task before sending production traffic.</p>
<h2 id="should-you-migrate-from-opus-48-to-fable-5">Should You Migrate from Opus 4.8 to Fable 5?</h2>
<p>Claude Opus 4.8 to Fable 5 migration is best treated as a targeted production upgrade, not a universal replacement, because Fable 5 changes capability, pricing, context, retention, and response semantics at the same time. Claude Fable 5 became available on June 9, 2026, with the API model ID <code>claude-fable-5</code>, a 1M token context window, and up to 128k output tokens per request. That is a meaningful jump for long-context coding, agentic workflows, audits, and multi-step repair loops. It also brings $10 per million input tokens and $50 per million output tokens, commonly compared with Opus 4.8 at roughly half that price. The practical answer: migrate your hardest, most failure-prone workloads first, keep Opus 4.8 for routine high-volume traffic, and make routing decisions from evaluation data rather than vendor positioning. The takeaway is simple: Fable 5 is a premium path, not a default path.</p>
<h3 id="what-is-the-safest-first-move">What is the safest first move?</h3>
<p>The safest first move is to create a compatibility branch that changes only the model ID, observability, and refusal handling. Do not rewrite prompts and tools at the same time. Run existing evals against both models, capture completion rate and total cost, then decide which traffic should move.</p>
<h2 id="what-actually-changes-in-the-api">What Actually Changes in the API?</h2>
<p>The Claude Fable 5 API change starts with the model ID <code>claude-fable-5</code>, but the important differences are output budgeting, adaptive thinking, refusal signaling, context size, and data retention. Anthropic&rsquo;s migration guidance says requests without a thinking field run with adaptive thinking on Fable 5, and <code>max_tokens</code> remains a hard cap across both internal thinking and visible response text. That means an old Opus 4.8 request with a tight output budget may now truncate useful text because part of the allowance is consumed by thinking. Fable 5 refusals also return as successful HTTP 200 responses with <code>stop_reason: refusal</code>, so a status-code-only client can misclassify a declined request as a completed task. Add the 1M context window, 128k output ceiling, and 30-day retention policy, and this becomes an application contract review. The takeaway: update the client contract before trusting benchmark gains.</p>
<table>
  <thead>
      <tr>
          <th>Area</th>
          <th style="text-align: right">Opus 4.8 assumption</th>
          <th>Fable 5 migration action</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>Model ID</td>
          <td style="text-align: right"><code>claude-opus-4-8</code></td>
          <td>Use <code>claude-fable-5</code></td>
      </tr>
      <tr>
          <td>Thinking</td>
          <td style="text-align: right">Explicit settings may dominate</td>
          <td>Expect adaptive thinking by default</td>
      </tr>
      <tr>
          <td>Output budget</td>
          <td style="text-align: right">Mostly visible answer budget</td>
          <td>Budget thinking plus visible response</td>
      </tr>
      <tr>
          <td>Refusals</td>
          <td style="text-align: right">Often handled like errors or text</td>
          <td>Check <code>stop_reason: refusal</code></td>
      </tr>
      <tr>
          <td>Retention</td>
          <td style="text-align: right">May fit stricter enterprise modes</td>
          <td>Confirm 30-day retention eligibility</td>
      </tr>
  </tbody>
</table>
<h3 id="which-client-code-should-change-first">Which client code should change first?</h3>
<p>The client code should first change in the response normalization layer. Add <code>stop_reason</code>, thinking settings, token usage, retry count, fallback model, and final task outcome to the same structured log record. Without that record, every later migration decision becomes guesswork.</p>
<h2 id="what-is-the-migration-checklist-for-claude-opus-4-8-to-claude-fable-5">What Is the Migration Checklist for claude-opus-4-8 to claude-fable-5?</h2>
<p>A Claude Fable 5 migration checklist is a short sequence of code, policy, and evaluation changes that prevent a model upgrade from silently changing production behavior. The minimum checklist has 8 items: replace the model ID, increase or retune <code>max_tokens</code>, verify adaptive thinking behavior, set effort defaults, handle <code>stop_reason: refusal</code>, add a fallback path, confirm 30-day retention is allowed, and rerun task-level benchmarks. In a real service, I would also add dashboard panels for output tokens, refusal rate, average tool calls, latency, and cost per successful task. The mistake is to treat the migration like a dependency bump where passing unit tests is enough. Fable 5 may be better at hard jobs and still worse for your margin, compliance boundary, or latency budget. The takeaway: the migration is complete only when behavior, cost, and policy have all been verified.</p>
<ol>
<li>Replace <code>claude-opus-4-8</code> with <code>claude-fable-5</code> behind a feature flag.</li>
<li>Recalculate <code>max_tokens</code> for thinking plus final answer.</li>
<li>Default most workloads to high effort before trying xhigh.</li>
<li>Treat <code>stop_reason: refusal</code> as a distinct terminal state.</li>
<li>Route refusal, latency, and budget failures to a controlled fallback.</li>
<li>Confirm whether 30-day retention is acceptable for the data class.</li>
<li>Run old and new models against the same task corpus.</li>
<li>Launch by traffic slice, not by global switch.</li>
</ol>
<h3 id="how-should-feature-flags-be-structured">How should feature flags be structured?</h3>
<p>Feature flags should separate model choice from effort level and fallback policy. A single <code>use_fable_5=true</code> flag hides the reason a task changed behavior. Use independent controls such as <code>primary_model</code>, <code>effort</code>, <code>fallback_model</code>, and <code>retention_allowed</code>.</p>
<h2 id="how-do-adaptive-thinking-max_tokens-and-effort-settings-change">How Do Adaptive Thinking, max_tokens, and Effort Settings Change?</h2>
<p>Adaptive thinking in Fable 5 means the model can spend part of the response budget on reasoning behavior before producing the visible answer, and that makes <code>max_tokens</code> a migration risk. Anthropic states that requests without a thinking field run with adaptive thinking on <code>claude-fable-5</code>; it also recommends high effort as the default for most Fable 5 tasks and xhigh only for the most capability-sensitive workloads. If an Opus 4.8 coding agent used <code>max_tokens: 4096</code> and expected most of that to become patch text, Fable 5 may need a higher ceiling for the same job because thinking and answer share the cap. I usually retune by task class: short extraction, code review, patch generation, long-context audit, and autonomous repair loop. The takeaway: effort and token budget are now product settings, not harmless model parameters.</p>
<table>
  <thead>
      <tr>
          <th>Workload</th>
          <th>Starting effort</th>
          <th>Token budget guidance</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>Short classification</td>
          <td>high only if accuracy matters</td>
          <td>Keep tight and measure refusals</td>
      </tr>
      <tr>
          <td>Code review</td>
          <td>high</td>
          <td>Increase budget for evidence and fixes</td>
      </tr>
      <tr>
          <td>Multi-file patching</td>
          <td>high, test xhigh</td>
          <td>Reserve room for diff explanation</td>
      </tr>
      <tr>
          <td>Long-context audit</td>
          <td>high</td>
          <td>Expect large context and larger output</td>
      </tr>
      <tr>
          <td>Agent repair loop</td>
          <td>high first</td>
          <td>Use xhigh only after eval evidence</td>
      </tr>
  </tbody>
</table>
<h3 id="when-is-xhigh-justified">When is xhigh justified?</h3>
<p>xhigh is justified when failed reasoning is more expensive than extra tokens. Examples include multi-repo migrations, security-sensitive code repair, and long-running agent workflows where one wrong plan creates several failed tool calls. For routine summarization, xhigh is usually waste.</p>
<h2 id="why-are-refusals-and-fallbacks-different-in-fable-5">Why Are Refusals and Fallbacks Different in Fable 5?</h2>
<p>Fable 5 refusal handling is different because a declined request can arrive as an HTTP 200 response with <code>stop_reason: refusal</code>, which means transport success no longer implies task success. Anthropic&rsquo;s Fable 5 documentation calls out this response pattern directly, and Anthropic also says new safeguards are intentionally cautious and may produce benign false positives while the system is refined. This matters in production because many integrations only branch on exceptions, HTTP status, or whether <code>content[0].text</code> exists. A coding agent that receives a refusal during dependency analysis should not mark the job complete; it should record the refusal, decide whether the request can be reframed safely, route to a fallback such as Opus 4.8 where allowed, or escalate to a human workflow. The takeaway: refusal is a first-class outcome, not an error string to bury in logs.</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-ts" data-lang="ts"><span style="display:flex;"><span><span style="color:#66d9ef">if</span> (<span style="color:#a6e22e">response</span>.<span style="color:#a6e22e">stop_reason</span> <span style="color:#f92672">===</span> <span style="color:#e6db74">&#34;refusal&#34;</span>) {
</span></span><span style="display:flex;"><span>  <span style="color:#a6e22e">metrics</span>.<span style="color:#a6e22e">increment</span>(<span style="color:#e6db74">&#34;llm.refusal&#34;</span>, { <span style="color:#a6e22e">model</span><span style="color:#f92672">:</span> <span style="color:#e6db74">&#34;claude-fable-5&#34;</span> });
</span></span><span style="display:flex;"><span>  <span style="color:#66d9ef">return</span> <span style="color:#a6e22e">runFallbackOrEscalate</span>({
</span></span><span style="display:flex;"><span>    <span style="color:#a6e22e">reason</span><span style="color:#f92672">:</span> <span style="color:#e6db74">&#34;model_refusal&#34;</span>,
</span></span><span style="display:flex;"><span>    <span style="color:#a6e22e">originalTaskId</span>,
</span></span><span style="display:flex;"><span>    <span style="color:#a6e22e">fallbackModel</span><span style="color:#f92672">:</span> <span style="color:#e6db74">&#34;claude-opus-4-8&#34;</span>
</span></span><span style="display:flex;"><span>  });
</span></span><span style="display:flex;"><span>}
</span></span></code></pre></div><h3 id="what-should-fallback-preserve">What should fallback preserve?</h3>
<p>Fallback should preserve the original task ID, prompt version, input classification, model response metadata, and final user-visible outcome. If fallback hides the refusal, your dashboards will show a healthy completion rate while users experience slower or inconsistent behavior.</p>
<h2 id="how-should-you-think-about-pricing-and-cost-per-completed-task">How Should You Think About Pricing and Cost per Completed Task?</h2>
<p>Fable 5 pricing should be evaluated as cost per completed task, not cost per token, because the model is more expensive per token but may need fewer turns on difficult work. The published Fable 5 API price is $10 per million input tokens and $50 per million output tokens, while Opus 4.8 comparisons commonly list $5 and $25 respectively. A naive migration therefore doubles unit token cost. That does not automatically double workload cost if Fable 5 solves a long-horizon code task in 3 turns that took Opus 4.8 7 turns plus human cleanup. It is still a bad trade for simple extraction, templated support replies, routing decisions, and high-volume background summarization. I measure input tokens, output tokens, tool calls, retries, elapsed time, and whether the task passed acceptance tests. The takeaway: pay for Fable 5 where it reduces total failure cost.</p>
<table>
  <thead>
      <tr>
          <th>Metric</th>
          <th>Why it matters</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>Cost per successful task</td>
          <td>Captures retries and failures</td>
      </tr>
      <tr>
          <td>Human edit minutes</td>
          <td>Shows whether better output saves labor</td>
      </tr>
      <tr>
          <td>Tool calls per task</td>
          <td>Reveals agent loop efficiency</td>
      </tr>
      <tr>
          <td>Refusal rate</td>
          <td>Exposes safeguard friction</td>
      </tr>
      <tr>
          <td>P95 latency</td>
          <td>Prevents premium routing from hurting UX</td>
      </tr>
  </tbody>
</table>
<h3 id="should-prompt-caching-change-the-decision">Should prompt caching change the decision?</h3>
<p>Prompt caching should be part of the decision when your workload sends large repeated context, such as repo maps, policy packs, or tool descriptions. Caching can reduce repeated input cost, but it does not fix expensive output, bad routing, or unnecessary xhigh effort.</p>
<h2 id="what-data-retention-and-compliance-review-is-required">What Data Retention and Compliance Review Is Required?</h2>
<p>Claude Fable 5 data retention review is required because Fable 5 and Mythos 5 are covered models with 30-day data retention and are not available under zero data retention. For regulated teams, that single fact can override every benchmark improvement. If your Opus 4.8 deployment currently handles customer secrets, unreleased financial data, medical text, legal discovery, or proprietary source code under a zero-retention agreement, you cannot assume Fable 5 is an allowed destination. The compliance work is concrete: classify each route by data type, confirm contractual eligibility, document retention controls, and block Fable 5 for disallowed classes at runtime. Do not rely on developers remembering which endpoint is safe. Put the decision in code, policy, and tests. The takeaway: retention eligibility is a release gate, not an afterthought.</p>
<h3 id="how-do-you-enforce-retention-rules-in-code">How do you enforce retention rules in code?</h3>
<p>Enforce retention rules before model selection. Attach a data classification to each request, then allow <code>claude-fable-5</code> only when the classification permits 30-day retention. The router should fail closed, log the reason, and choose an approved model rather than silently downgrading policy.</p>
<h2 id="when-should-you-keep-opus-48">When Should You Keep Opus 4.8?</h2>
<p>Opus 4.8 should stay in production when the workload is stable, high-volume, latency-sensitive, cost-sensitive, or blocked by Fable 5 retention rules. The benchmark gap can be real, but not every task benefits from frontier reasoning. Coursiv reports Anthropic-published deltas of 80.3% versus 69.2% on SWE-bench Pro and 29.3% versus 13.4% on FrontierCode Diamond for Fable 5 versus Opus 4.8, which points to stronger hard-code performance. Those numbers do not prove Fable 5 is better for invoice extraction, small prompt rewrites, conventional support macros, or batch summarization. Tessl&rsquo;s evaluation angle also found that supplied skills and context moved both models by about 17 points, a reminder that harness quality can matter as much as model choice. The takeaway: keep Opus 4.8 where reliability, compliance, and unit economics already work.</p>
<h3 id="which-workloads-usually-remain-on-opus-48">Which workloads usually remain on Opus 4.8?</h3>
<p>Workloads that usually remain on Opus 4.8 include deterministic transformations, short summaries, structured extraction with tight schemas, low-risk customer support drafting, and internal jobs with strict budget ceilings. These jobs need consistency and throughput more than maximal reasoning depth.</p>
<h2 id="when-is-fable-5-worth-the-premium">When Is Fable 5 Worth the Premium?</h2>
<p>Fable 5 is worth the premium when failures compound across many steps, context windows are large, and a better first plan saves more money than the extra tokens cost. The model&rsquo;s 1M token context window and 128k output ceiling make it a strong candidate for repository-wide audits, long legal or technical document analysis, spreadsheet reasoning, multi-file coding changes, and autonomous agent repair loops. Anthropic positions Fable 5 as a widely available model for demanding reasoning and long-horizon agentic work, and that matches where I would test it first. The key is not whether the prompt looks impressive; it is whether the final task passes acceptance criteria with fewer retries, fewer tool calls, and less human editing. The takeaway: use Fable 5 where task complexity makes Opus 4.8 failures expensive.</p>
<table>
  <thead>
      <tr>
          <th>Use Fable 5 for</th>
          <th>Keep Opus 4.8 for</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>Multi-repo migrations</td>
          <td>Simple classification</td>
      </tr>
      <tr>
          <td>Deep code review</td>
          <td>Short summaries</td>
      </tr>
      <tr>
          <td>Long-context audits</td>
          <td>Commodity extraction</td>
      </tr>
      <tr>
          <td>Complex agent planning</td>
          <td>High-volume low-margin traffic</td>
      </tr>
      <tr>
          <td>Hard debugging loops</td>
          <td>ZDR-bound workloads</td>
      </tr>
  </tbody>
</table>
<h3 id="what-is-a-concrete-developer-example">What is a concrete developer example?</h3>
<p>A concrete developer example is a monorepo framework migration where the model must read existing patterns, update code, revise tests, and recover from failing CI. Fable 5 may be cheaper overall if it avoids repeated wrong edits and shortens the human review loop.</p>
<h2 id="how-do-you-build-a-routing-strategy-instead-of-a-blanket-switch">How Do You Build a Routing Strategy Instead of a Blanket Switch?</h2>
<p>A Claude Fable 5 routing strategy assigns each request to the cheapest model that meets the task&rsquo;s capability, latency, retention, and reliability requirements. In practice, that means Opus 4.8 remains the default for routine work, while Fable 5 receives tasks with large context, high ambiguity, repeated Opus failures, or high downstream failure cost. A simple router can start with 5 signals: data classification, task type, context size, historical failure rate, and user tier. For example, a codebase audit with 600k tokens of context and prior Opus 4.8 failures should route to Fable 5 high effort; a 600-token rewrite should not. Routing also makes rollback safer because you can reduce Fable traffic by class instead of reverting the whole migration. The takeaway: routing turns Fable 5 into a precision tool instead of an expensive default.</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-ts" data-lang="ts"><span style="display:flex;"><span><span style="color:#66d9ef">function</span> <span style="color:#a6e22e">chooseClaudeModel</span>(<span style="color:#a6e22e">task</span>: <span style="color:#66d9ef">Task</span>)<span style="color:#f92672">:</span> <span style="color:#a6e22e">ClaudeRoute</span> {
</span></span><span style="display:flex;"><span>  <span style="color:#66d9ef">if</span> (<span style="color:#f92672">!</span><span style="color:#a6e22e">task</span>.<span style="color:#a6e22e">retentionAllows30Days</span>) <span style="color:#66d9ef">return</span> { <span style="color:#a6e22e">model</span><span style="color:#f92672">:</span> <span style="color:#e6db74">&#34;claude-opus-4-8&#34;</span> };
</span></span><span style="display:flex;"><span>  <span style="color:#66d9ef">if</span> (<span style="color:#a6e22e">task</span>.<span style="color:#a6e22e">contextTokens</span> <span style="color:#f92672">&gt;</span> <span style="color:#ae81ff">200</span><span style="color:#a6e22e">_000</span>) <span style="color:#66d9ef">return</span> { <span style="color:#a6e22e">model</span><span style="color:#f92672">:</span> <span style="color:#e6db74">&#34;claude-fable-5&#34;</span>, <span style="color:#a6e22e">effort</span><span style="color:#f92672">:</span> <span style="color:#e6db74">&#34;high&#34;</span> };
</span></span><span style="display:flex;"><span>  <span style="color:#66d9ef">if</span> (<span style="color:#a6e22e">task</span>.<span style="color:#a6e22e">previousFailures</span> <span style="color:#f92672">&gt;=</span> <span style="color:#ae81ff">2</span>) <span style="color:#66d9ef">return</span> { <span style="color:#a6e22e">model</span><span style="color:#f92672">:</span> <span style="color:#e6db74">&#34;claude-fable-5&#34;</span>, <span style="color:#a6e22e">effort</span><span style="color:#f92672">:</span> <span style="color:#e6db74">&#34;high&#34;</span> };
</span></span><span style="display:flex;"><span>  <span style="color:#66d9ef">if</span> (<span style="color:#a6e22e">task</span>.<span style="color:#66d9ef">type</span> <span style="color:#f92672">===</span> <span style="color:#e6db74">&#34;long_horizon_agent&#34;</span>) <span style="color:#66d9ef">return</span> { <span style="color:#a6e22e">model</span><span style="color:#f92672">:</span> <span style="color:#e6db74">&#34;claude-fable-5&#34;</span>, <span style="color:#a6e22e">effort</span><span style="color:#f92672">:</span> <span style="color:#e6db74">&#34;high&#34;</span> };
</span></span><span style="display:flex;"><span>  <span style="color:#66d9ef">return</span> { <span style="color:#a6e22e">model</span><span style="color:#f92672">:</span> <span style="color:#e6db74">&#34;claude-opus-4-8&#34;</span> };
</span></span><span style="display:flex;"><span>}
</span></span></code></pre></div><h3 id="what-should-the-router-log">What should the router log?</h3>
<p>The router should log the selected model, rejected models, routing reason, data classification, effort level, fallback policy, estimated context tokens, and final task outcome. These fields let you prove whether routing is improving success rate or just increasing spend.</p>
<h2 id="how-should-you-benchmark-your-own-workloads">How Should You Benchmark Your Own Workloads?</h2>
<p>A Fable 5 migration benchmark should compare task outcomes, not just model responses, across the same representative workload corpus. Use at least 100 real or realistic tasks per major class if you have the volume, and include cases that previously failed under Opus 4.8. Track pass rate, human edit time, tool calls, retries, latency, refusal rate, token cost, and cost per accepted result. Do not let a generic benchmark decide production routing for you. SWE-bench Pro and FrontierCode Diamond are useful signals for hard coding capability, but your prompts, tools, schemas, data retention constraints, and acceptance tests define your real deployment. I prefer an A/B harness that replays frozen inputs into both models and grades against deterministic checks where possible. The takeaway: benchmark the workflow you actually sell or operate.</p>
<h3 id="what-should-the-eval-dataset-include">What should the eval dataset include?</h3>
<p>The eval dataset should include easy baseline tasks, normal production tasks, historical failures, adversarial edge cases, and compliance-sensitive examples that must not route to Fable 5. A dataset with only impressive hard prompts will overstate the value of migration.</p>
<h2 id="what-are-the-common-migration-mistakes">What Are the Common Migration Mistakes?</h2>
<p>Common Claude Opus 4.8 to Fable 5 migration mistakes are treating the change as a model-name replacement, underbudgeting <code>max_tokens</code>, ignoring <code>stop_reason: refusal</code>, skipping retention review, and moving all traffic at once. The most damaging version I have seen in model migrations is silent success: the API returns 200, the application stores a response, and nobody notices that the underlying task was refused, truncated, or routed through an unapproved retention path. Another common mistake is testing only happy-path prompts written by the migration team. Production users bring stale context, malformed files, ambiguous instructions, and budget pressure. Fable 5 may handle many of those better than Opus 4.8, but the migration still needs guardrails. The takeaway: most failures come from integration assumptions, not from the model being weak.</p>
<h3 id="which-mistake-is-easiest-to-miss">Which mistake is easiest to miss?</h3>
<p>The easiest mistake to miss is <code>max_tokens</code> truncation caused by adaptive thinking. The response may look reasonable in short manual testing, then fail on production tasks where the model spends more budget reasoning and has too little room left for the final answer.</p>
<h2 id="what-is-the-final-recommendation">What Is the Final Recommendation?</h2>
<p>The final recommendation for Claude Opus 4.8 to Fable 5 migration is to adopt Fable 5 through controlled routing, not blanket replacement. Start with workloads where Fable 5&rsquo;s 1M context window, 128k output limit, stronger hard-code benchmark profile, and long-horizon reasoning can reduce expensive failures. Keep Opus 4.8 for stable high-volume tasks, low-margin jobs, strict latency paths, and any route where 30-day retention is not allowed. Before launch, update response handling for <code>stop_reason: refusal</code>, retune <code>max_tokens</code>, set high effort as the baseline, measure xhigh separately, and compare cost per completed task. A successful migration should show better acceptance rates or lower human intervention on selected task classes, not merely a newer model name in logs. The takeaway: migrate selectively, measure honestly, and keep the cheaper model where it still wins.</p>
<h3 id="what-does-a-good-rollout-look-like">What does a good rollout look like?</h3>
<p>A good rollout starts with internal traffic, then 5% of eligible production tasks, then task-class expansion after metrics hold. Roll back by route when refusal rate, cost, latency, or acceptance rate crosses a predefined threshold. Keep both models observable until routing decisions stabilize.</p>
<h2 id="faq">FAQ</h2>
<p>This Claude Fable 5 migration FAQ answers the operational questions developers usually ask after the first implementation pass: whether the model ID change is enough, how to handle refusals, what happens to zero data retention, when to use xhigh effort, and how to compare cost. The short version is that Fable 5 is a stronger and more expensive model with different production contracts. It supports a 1M token context window and up to 128k output tokens, but it also uses adaptive thinking by default, returns refusals through <code>stop_reason: refusal</code>, and is covered by 30-day retention. Those details affect application code, compliance review, dashboards, and rollout strategy. If a migration plan does not mention routing, evaluation, and fallback behavior, it is incomplete. The takeaway: the FAQ items below should become acceptance criteria for the migration ticket.</p>
<h3 id="is-changing-the-model-id-enough-to-migrate">Is changing the model ID enough to migrate?</h3>
<p>Changing the model ID is enough only for a smoke test. For production, you also need to retune <code>max_tokens</code>, verify effort settings, handle <code>stop_reason: refusal</code>, confirm retention eligibility, and benchmark task outcomes against Opus 4.8.</p>
<h3 id="does-fable-5-replace-opus-48-for-every-workload">Does Fable 5 replace Opus 4.8 for every workload?</h3>
<p>Fable 5 should not replace Opus 4.8 for every workload. It is best for hard reasoning, large context, and long-horizon agentic work. Opus 4.8 can remain better for routine, high-volume, latency-sensitive, or zero-retention-bound traffic.</p>
<h3 id="how-should-i-handle-fable-5-refusals">How should I handle Fable 5 refusals?</h3>
<p>Handle Fable 5 refusals by checking <code>stop_reason</code> on every response. If it equals <code>refusal</code>, mark the task as not completed, log the event, and either use an approved fallback, safely reframe the request, or escalate for review.</p>
<h3 id="what-is-the-biggest-compliance-issue">What is the biggest compliance issue?</h3>
<p>The biggest compliance issue is data retention. Fable 5 and Mythos 5 are covered models with 30-day data retention and are not available under zero data retention, so regulated or confidential routes need explicit approval before migration.</p>
<h3 id="how-do-i-know-whether-fable-5-is-worth-the-cost">How do I know whether Fable 5 is worth the cost?</h3>
<p>You know Fable 5 is worth the cost when selected workloads show higher accepted completion rates, fewer retries, lower human edit time, or fewer tool calls after accounting for the $10 input and $50 output per million token pricing.</p>
]]></content:encoded></item></channel></rss>