<?xml version="1.0" encoding="utf-8" standalone="yes"?><rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom" xmlns:content="http://purl.org/rss/1.0/modules/content/"><channel><title>Penetration-Testing on RockB</title><link>https://baeseokjae.github.io/tags/penetration-testing/</link><description>Recent content in Penetration-Testing on RockB</description><image><title>RockB</title><url>https://baeseokjae.github.io/images/og-default.png</url><link>https://baeseokjae.github.io/images/og-default.png</link></image><generator>Hugo</generator><language>en-us</language><lastBuildDate>Fri, 28 Aug 2026 16:01:54 +0000</lastBuildDate><atom:link href="https://baeseokjae.github.io/tags/penetration-testing/index.xml" rel="self" type="application/rss+xml"/><item><title>ZhiShi Review 2026: A Rust Agent Harness for Security Research</title><link>https://baeseokjae.github.io/posts/zhishi-security-research-agent-harness-2026/</link><pubDate>Fri, 28 Aug 2026 16:01:54 +0000</pubDate><guid>https://baeseokjae.github.io/posts/zhishi-security-research-agent-harness-2026/</guid><description>ZhiShi is an AGPL-3.0 agent harness for security research that combines a Tauri Rust shell with a TypeScript loop engine, driving CTF, fuzzing, and real CVE reproduction.</description><content:encoded><![CDATA[<p>ZhiShi is an open-source agent harness purpose-built for security research — a human-in-the-loop workbench for binary exploitation, penetration testing, whitebox code audit, and AI security — that layers a domain-specific research loop on top of the MIT-licensed <code>@earendil-works/pi-agent-core</code> engine. It uses a Tauri (Rust) desktop shell wrapping a TypeScript/Node core, and publishes unusual evidence-first proof of capability: 100% Pwnable.kr CTF completion, 98% Hacker101, and a fully reproduced exploit chain for CVE-2026-34621 (Adobe Acrobat Reader, CVSS 8.6).</p>
<h2 id="what-is-zhishi-positioning-an-agent-harness-for-security-research">What is ZhiShi? Positioning an agent harness for security research</h2>
<p>The &ldquo;agent harness for security research&rdquo; framing matters because it is deliberately narrower — and more honest — than the generic &ldquo;AI hacking tool&rdquo; marketing common in 2026. ZhiShi does not claim to be a fully autonomous pentester. Instead it describes itself as a control plane for the researcher: the agent owns the execution environment, compiles and iterates on exploit code inside it, maintains an evidence-backed research archive, and pauses for human approval at defined safety gates.</p>
<p>Positioning itself as a harness rather than an agent framework lets ZhiShi solve a concrete operational problem: reproducibility. A research harness gives the agent durable state — the environment snapshot it created, the hypothesis it is testing, the evidence it has collected — so a multi-hour binary-exploitation session does not evaporate between LLM turns. This is the same architectural idea behind agent harnesses in general software engineering, applied specifically to offensive security.</p>
<p>The project is young and moving fast. The repository was created 2026-08-18 and sat at roughly 69 stars and 4 forks as of 2026-08-28 (source: GitHub API). That is a rapidly growing new project, which cuts both ways: it signals real community interest but also means the codebase and claims should be treated as early-stage and subject to change.</p>
<h2 id="the-rust-question--tauri-shell-vs-typescriptnode-core">The &lsquo;Rust&rsquo; question — Tauri shell vs TypeScript/Node core</h2>
<p>The title&rsquo;s &ldquo;Rust agent harness&rdquo; requires a precise reality check. The Rust component is the desktop application shell, built with Tauri (<code>src-tauri</code>). The harness core — the loop engine, tool orchestration, environment management, and research memory — is TypeScript/Node, requiring Node.js &gt;= 22.</p>
<p>This split is worth understanding before you invest time, because it shapes both deployment and extension. The Tauri shell provides the cross-platform GUI (React + zustand + xterm, with SSE for live streams) and a small Rust footprint for desktop integration. The actual security logic lives in the Node sidecar that exposes the admin API the GUI talks to.</p>
<table>
  <thead>
      <tr>
          <th>Layer</th>
          <th>Technology</th>
          <th>Role</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>Desktop shell</td>
          <td>Tauri (Rust) + React/zustand/xterm</td>
          <td>GUI, terminal, SSE live streams</td>
      </tr>
      <tr>
          <td>Sidecar / admin API</td>
          <td>TypeScript / Node.js (&gt;= 22)</td>
          <td>Bridges GUI to loop engine</td>
      </tr>
      <tr>
          <td>Loop engine</td>
          <td><code>@earendil-works/pi-agent-core</code> (MIT) + custom</td>
          <td>Orchestrates agent turns, tools, state</td>
      </tr>
      <tr>
          <td>LLM vendors</td>
          <td>kimi, deepseek, openai, moonshot, Tongyi, Zhipu, SiliconFlow</td>
          <td>Model inference with key management</td>
      </tr>
      <tr>
          <td>Environments</td>
          <td>Docker, VMware, Hyper-V, VirtualBox, SSH</td>
          <td>Agent-owned exec sandboxes</td>
      </tr>
  </tbody>
</table>
<p>The practical consequence: if you want to extend the harness, you are mostly writing TypeScript, not Rust. The Rust is a shell, not the brains. Users coming for a Rust-native security tooling experience should set expectations accordingly — the Rust surface is real but comparatively thin.</p>
<h2 id="core-architecture-gui-sidecar-loop-engine-and-llm-providers">Core architecture: GUI, sidecar, loop engine, and LLM providers</h2>
<p>The end-to-end data flow is: GUI (React + zustand + xterm, SSE) → sidecar (admin API) → self-built loop engine → LLM vendors → environment (Docker/VM/SSH) with a <code>research_events</code> memory store.</p>
<p>A notable architectural decision is building the loop engine on top of <code>@earendil-works/pi-agent-core</code> (MIT) rather than writing the transport, state management, and attachment handling from scratch. The project explicitly credits pi for these capabilities. This is a build-versus-buy decision worth evaluating: instead of reinventing a generic agent loop, ZhiShi reuses a battle-tested generic core and concentrates its effort on the security-specific layer — environment fusion, native code iteration, evidence tracking, and safety gates.</p>
<p>The core engine tools are deliberately minimal and general: <code>env_exec</code>, <code>env_bg</code> (for long-running background tasks like fuzzing), <code>delegate_task</code> (spawning sub-agents such as a crash-triager), <code>research_log</code>, <code>research_archive</code>, <code>request_decision</code>, and <code>declare_completion</code>. These map cleanly onto a security-research workflow: spawn a fuzzer in the background, delegate crash triage to a sub-agent, log hypotheses, archive evidence, and request a human decision when the agent leaves its approved scope.</p>
<p>LLM provider support is broad — eight built-in endpoints covering kimi, deepseek, openai, moonshot, Tongyi, Zhipu, and SiliconFlow — with key management and model switching. This matters for real use because different models are good at different parts of the research loop: a fast cheap model for routine env commands, a stronger model for exploit synthesis.</p>
<h2 id="the-three-pillars-environment-fusion-native-tools-native-code">The three pillars: environment fusion, native tools, native code</h2>
<p>ZhiShi organizes its capability around three pillars, and the second and third are what distinguish it from a plain agent-plus-shell script.</p>
<p>Environment fusion means the agent owns the environment: it can create, run, snapshot, and roll back execution environments over Docker, VMware, Hyper-V, VirtualBox, and SSH targets. There are 7+ built-in environment recipes (dev, pwn, fuzz, rev, code-audit, pentest, ai-security, plus VM variants). The snapshot/rollback capability is critical for exploitation work — you can deliberately crash a target, inspect the crash, then restore a clean state and iterate.</p>
<p>Native tools means the harness discovers and uses tools already present in the target environment rather than bundling them. This keeps the attack surface aligned with a real target: the agent uses the gdb, pwntools, or fuzzer that a real researcher would actually reach for.</p>
<p>Native code means exploit, shellcode, and fuzz harnesses are compiled and iterated in-environment, not pre-written by the harness. The agent writes code, compiles it against the target&rsquo;s real toolchain, runs it, reads the result, and iterates. This is where the reproducibility value shows up: each iteration leaves state in the environment the agent controls.</p>
<h2 id="the-research-archive-dashboard-and-evidence-first-memory-model">The research-archive dashboard and evidence-first memory model</h2>
<p>A genuinely differentiated feature is the research-archive dashboard, laid out as a left stream / right archive split (roughly 6:4). It is structured memory built around the scientific method: hypotheses, evidence, conclusions, and open questions grow live as the session runs.</p>
<p>The evidence-first model is enforced with citations. Findings must cite V# evidence records, and when the agent falsifies or corrects a hypothesis it leaves an audit trail. This is an answer to one of the biggest problems in AI-assisted security research: the model confidently asserts a conclusion with no retrievable chain of evidence. ZhiShi&rsquo;s archive forces every conclusion to point back to concrete artifacts in the <code>research_events</code> store, making results auditable and, in principle, reproducible.</p>
<p>For security research specifically this is not a nicety — it is the difference between a claim you can defend and a claim you have to retract. A researcher taking a finding to a vendor (for a CVE filing) or to a client (for a pentest report) needs the evidence chain. The archive is designed to produce exactly that.</p>
<h2 id="auto-loop-research-budget-pause-points-and-human-acceptance-gates">Auto-loop research: budget, pause points, and human acceptance gates</h2>
<p>ZhiShi&rsquo;s auto-loop agent lets a researcher launch a goal-directed research campaign with three locks set at the start: the goal, a budget, and an acceptance criteria. The budget bounds rounds, tokens, or time — a practical guardrail against the runaway-token problem that plagues long autonomous agent runs.</p>
<p>The loop runs with defined pause points. When the agent reaches a decision that falls outside its approved scope, or when it has assembled an acceptance package against the locked criteria, control returns to the human for a final sign-off. The system then produces an auto-generated report of what it did.</p>
<p>This human-in-the-loop design is the right call for offensive security and is worth highlighting as a model for other &ldquo;autonomous&rdquo; research tools. The acceptance package pattern — the agent gathers evidence that it has met the stated goal, and a human signs off — keeps the loop genuinely useful for exploratory research while preventing unbounded autonomous action. Budget locks plus human acceptance gates is a strong governance structure.</p>
<h2 id="proven-results-ctf-clears-cve-reproductions-and-whitebox-findings">Proven results: CTF clears, CVE reproductions, and whitebox findings</h2>
<p>The project&rsquo;s credibility rests on published results, which is rarer than it should be in the &ldquo;AI hacking&rdquo; space. The README&rsquo;s 实战成果 (practical results) table reports:</p>
<table>
  <thead>
      <tr>
          <th>Target</th>
          <th>Completion</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>Pwnable.kr</td>
          <td>100%</td>
      </tr>
      <tr>
          <td>Hacker101 CTF</td>
          <td>98%</td>
      </tr>
      <tr>
          <td>HackThisSite</td>
          <td>96%</td>
      </tr>
      <tr>
          <td>ROP Emporium</td>
          <td>91%</td>
      </tr>
      <tr>
          <td>VulnHub</td>
          <td>90%</td>
      </tr>
  </tbody>
</table>
<p>Beyond CTF practice, ZhiShi claims real-world vulnerability research. The flagship is CVE-2026-34621 — Adobe Acrobat Reader prototype pollution, CVSS 8.6 — reproduced as a full end-to-end exploit chain from trigger to weaponization (source: CVE record + README). It also reports CVE-2026-0961 (Wireshark BLF file-parse crash / DoS, CVSS 5.5), the CVE-2025-1162/1163/1164/1170/1171 series, and a CVE-2026-66319 (Microsoft, pending). The broader pipeline claims 7 vulnerabilities in a Microsoft review queue and 200+ vulnerabilities from whitebox audits, with some filed to CVE, plus multiple Chrome and Firefox vulnerability analyses.</p>
<p>The Wireshark BLF case is a useful generalization test: it demonstrates the fuzz/crash-triage workflow, where the harness drives <code>env_bg</code> background fuzzing and a crash-triager sub-agent to find and reproduce parser crashes. This shows the harness moving beyond pwn/CTF into real-world bug hunting.</p>
<h2 id="safety-and-dual-use-human-in-the-loop-gates-and-legal-authorization">Safety and dual-use: human-in-the-loop gates and legal authorization</h2>
<p>Offensive tooling carries obvious dual-use tension, and ZhiShi does not ignore it. The architecture bakes in human-in-the-loop safety: rule hard-gates, output sanitization, and an out-of-scope human-approval modal that fires before the agent takes actions outside its declared scope. Combined with the acceptance-package sign-off, this means the harness is designed to keep a human accountable for consequential actions.</p>
<p>The licensing is a deliberate governance choice: AGPL-3.0. That is a strong copyleft license that discourages the kind of closed-source commercialization of offensive tooling that would be harder to audit. The README also carries a legal-authorization caveat, reminding users they are responsible for ensuring they have permission to test any target.</p>
<p>Responsible disclosure matters: the project reports findings to vendors and files to CVE rather than dumping 200+ vulnerabilities publicly without coordination. That is the professionally responsible pattern for vulnerability research, and it is worth noting as a positive signal about the project&rsquo;s intent.</p>
<h2 id="how-zhishi-compares-to-generic-agent-cores-pi-and-other-security-agents">How ZhiShi compares to generic agent cores (pi) and other security agents</h2>
<p>The cleanest way to think about ZhiShi is as a specialization: it takes a generic agent core (<code>@earendil-works/pi-agent-core</code>) and builds a domain-specific harness on top. A generic agent framework gives you transport, state, and tool calling; it does not give you environment snapshots, exploit compilation loops, or an evidence-cited research archive. ZhiShi&rsquo;s contribution is precisely that security-specific layer.</p>
<p>The tradeoff versus building your own loop from scratch is the familiar one: reusing pi gets you battle-tested transport and state management faster, but it also means ZhiShi&rsquo;s loop is constrained by pi&rsquo;s abstractions. For most users this is a good trade — you inherit stability and focus your effort on the domain logic that differentiates you.</p>
<p>Compared to other &ldquo;AI pentest&rdquo; or &ldquo;AI hacking&rdquo; tools of 2026, ZhiShi differentiates on evidence discipline and environment ownership rather than on raw autonomous capability. Many competitors demo an impressive single exploit; ZhiShi publishes completion percentages, reproduced CVEs, and an audit-trail archive. That is a meaningful credibility difference, even accounting for the fact that these are self-reported.</p>
<h2 id="verification-status-and-limits-of-self-reported-claims">Verification status and limits of self-reported claims</h2>
<p>ZhiShi reports a strong internal verification story: 2370+ unit tests all green, with <code>tsc --noEmit</code>, <code>eslint</code>, and <code>depcruise</code> architecture checks at zero errors. It also mentions live dogfooding (ret2win, a three-domain 1.1.8 challenge, a cJSON four-round 1.4.6 run) and GUI end-to-end walkthroughs across versions 1.3.0–1.3.8.</p>
<p>These are meaningful signals of engineering rigor, but they are self-reported. An independent reviewer cannot fully verify the CTF percentages, the 200+ whitebox findings, or the Microsoft review queue from the public repository alone. The CVEs are verifiable — CVE-2026-34621 and CVE-2026-0961 are real published records — but the volume claims and completion percentages rest on the project&rsquo;s own accounting.</p>
<p>That is not a reason to dismiss the project; it is a reason to treat the headline numbers as claims to validate through your own runs rather than as independent benchmarks. The fact that some flagship results (the Adobe exploit) are externally verifiable via CVE records raises the confidence you can reasonably place in the rest, but it does not make the rest independently proven.</p>
<h2 id="verdict-who-should-use-zhishi-and-what-to-watch">Verdict: who should use ZhiShi and what to watch</h2>
<p>ZhiShi is best suited to security researchers who want a reproducible, evidence-first harness for exploitation, pentest, code audit, or AI-security work — especially those who value the audit-trail archive and the human-in-the-loop safety gates over raw autonomous speed. The AGPL-3.0 license, Node-based extensibility, and broad LLM support make it approachable for researchers who already live in a TypeScript toolchain.</p>
<p>What to watch: the project is very new (created 2026-08-18), so the community, roadmap, and long-term maintenance are unproven. The &ldquo;Rust&rdquo; branding overstates the Rust surface — it is a Tauri shell over a Node core. And the most impressive claims are self-reported, so budget time to reproduce a few results before committing a serious research pipeline to it. On balance, ZhiShi is one of the more credible and better-governed offensive-security harnesses to appear in 2026, and it is worth a close look.</p>
<h2 id="faq">FAQ</h2>
<h3 id="is-zhishi-actually-written-in-rust">Is ZhiShi actually written in Rust?</h3>
<p>The desktop shell is Rust, via Tauri, but the harness core — loop engine, tools, environment management, and research memory — is TypeScript/Node (Node.js &gt;= 22). Extending the harness means writing TypeScript, not Rust.</p>
<h3 id="what-license-is-zhishi-released-under">What license is ZhiShi released under?</h3>
<p>ZhiShi is AGPL-3.0 licensed, a strong copyleft license that allows use and modification while requiring derivative works to share the same license. It also carries a legal-authorization caveat about responsible use.</p>
<h3 id="what-real-vulnerabilities-has-zhishi-reproduced">What real vulnerabilities has ZhiShi reproduced?</h3>
<p>Its flagship is CVE-2026-34621 (Adobe Acrobat Reader prototype pollution, CVSS 8.6), reproduced as an end-to-end exploit chain. It also reports CVE-2026-0961 (Wireshark BLF DoS) and a series of 2025-era CVEs, plus 200+ whitebox findings.</p>
<h3 id="what-is-the-research-archive-dashboard">What is the research-archive dashboard?</h3>
<p>It is a structured-memory panel (left stream / right archive) where hypotheses, evidence, conclusions, and open questions grow live, with mandatory V# evidence citations. Falsified or corrected hypotheses leave an audit trail for reproducibility.</p>
<h3 id="is-zhishi-a-fully-autonomous-hacking-tool">Is ZhiShi a fully autonomous hacking tool?</h3>
<p>No. ZhiShi frames itself as a human-in-the-loop harness. It enforces budget locks, pause points, out-of-scope human-approval modals, and a final human sign-off on auto-loop acceptance packages, keeping a human accountable for consequential actions.</p>
]]></content:encoded></item><item><title>Strix AI Pentesting Framework Guide 2026: Autonomous Penetration Testing with AI Agents</title><link>https://baeseokjae.github.io/posts/strix-ai-pentesting-framework-2026/</link><pubDate>Fri, 17 Jul 2026 04:04:55 +0000</pubDate><guid>https://baeseokjae.github.io/posts/strix-ai-pentesting-framework-2026/</guid><description>Strix is an open-source AI pentesting framework that uses multi-agent orchestration to autonomously discover, exploit, and validate vulnerabilities with working PoCs.</description><content:encoded><![CDATA[<h2 id="what-is-strix-the-open-source-ai-pentesting-framework">What is Strix? The Open-Source AI Pentesting Framework</h2>
<p>Strix is an open-source, AI-powered penetration testing framework that uses a multi-agent orchestration system called Graph of Agents to autonomously discover, exploit, and validate security vulnerabilities. Built in Python and licensed under Apache 2.0, Strix has amassed over 42,000 GitHub stars and 4,300 forks since its launch in August 2025, making it the fastest-growing security testing tool in the open-source ecosystem. Unlike traditional vulnerability scanners that flood teams with false positives, Strix generates working proof-of-concept exploits for every finding it reports, giving developers actionable evidence they can fix immediately.</p>
<h2 id="why-strix-matters-in-2026-the-shift-to-autonomous-security-testing">Why Strix Matters in 2026: The Shift to Autonomous Security Testing</h2>
<p>The cybersecurity landscape in 2026 is defined by a fundamental imbalance: attack surfaces are expanding faster than the pool of skilled security testers can keep up. Cloud-native architectures, microservices, API-first designs, and AI-generated code have multiplied the number of potential entry points in the average application by an order of magnitude. Traditional manual penetration testing — booking a pentest firm, waiting two weeks for a report, then sifting through hundreds of potential findings — is no longer viable for organizations shipping code multiple times per day.</p>
<p>Strix represents a paradigm shift. By embedding AI agents directly into the security testing workflow, it enables continuous, automated penetration testing that runs alongside development. The tool&rsquo;s explosive growth — 42,000+ GitHub stars in under a year — signals that the security community has recognized this shift. According to industry analysis from RedFox Security, AI pentesting tools are rapidly replacing manual workflows because they can operate at machine speed, scale horizontally across targets, and improve over time as the underlying AI models advance.</p>
<p>For startups and SMBs that cannot afford enterprise pentesting engagements costing $20,000–$100,000 per assessment, Strix democratizes security testing. It puts enterprise-grade autonomous penetration testing capabilities into the hands of any development team with a GitHub account and an API key.</p>
<h2 id="key-features-and-capabilities">Key Features and Capabilities</h2>
<h3 id="multi-agent-orchestration-graph-of-agents">Multi-Agent Orchestration (Graph of Agents)</h3>
<p>The core innovation of Strix is its Graph of Agents architecture. Instead of a single monolithic AI making all decisions, Strix deploys a team of specialized AI agents that collaborate like a human red team. Each agent has a distinct role:</p>
<ul>
<li><strong>Reconnaissance Agents</strong> — Probe the target surface, enumerate endpoints, discover subdomains, and map the attack surface.</li>
<li><strong>Vulnerability Analysis Agents</strong> — Analyze discovered services and endpoints for known vulnerability patterns and logic flaws.</li>
<li><strong>Exploitation Agents</strong> — Attempt to exploit identified vulnerabilities, generating working proof-of-concept code.</li>
<li><strong>Post-Exploitation Agents</strong> — Assess the blast radius of successful exploits, checking for lateral movement opportunities and privilege escalation paths.</li>
</ul>
<p>These agents communicate through a directed graph structure, passing findings and context between each other. If the Recon agent discovers a new endpoint, it dynamically routes that information to the Vulnerability Analysis agent, which may trigger the Exploitation agent. This collaborative approach produces more thorough results than any single-agent system could achieve.</p>
<h3 id="full-pentesting-toolkit-recon-exploitation-validation">Full Pentesting Toolkit (Recon, Exploitation, Validation)</h3>
<p>Strix ships with a comprehensive toolkit that covers the entire penetration testing lifecycle:</p>
<table>
  <thead>
      <tr>
          <th>Phase</th>
          <th>Capabilities</th>
          <th>AI Agent Role</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>Reconnaissance</td>
          <td>Subdomain enumeration, port scanning, technology fingerprinting, API endpoint discovery</td>
          <td>Recon Agent maps the attack surface</td>
      </tr>
      <tr>
          <td>Vulnerability Analysis</td>
          <td>OWASP Top 10 coverage, business logic analysis, configuration review</td>
          <td>Analysis Agent identifies potential weaknesses</td>
      </tr>
      <tr>
          <td>Exploitation</td>
          <td>SQL injection, XSS, SSRF, RCE, XXE, broken access control, authentication bypass</td>
          <td>Exploitation Agent generates working PoCs</td>
      </tr>
      <tr>
          <td>Post-Exploitation</td>
          <td>Lateral movement assessment, privilege escalation checks, data exfiltration simulation</td>
          <td>Post-Exploitation Agent evaluates impact</td>
      </tr>
      <tr>
          <td>Reporting</td>
          <td>Structured findings with PoC code, remediation guidance, severity scoring</td>
          <td>Reporting Agent compiles actionable output</td>
      </tr>
  </tbody>
</table>
<p>Strix covers the full OWASP Top 10, including broken access control, injection attacks, SSRF, XXE, RCE, XSS, and business logic flaws. This breadth makes it suitable for testing modern web applications, APIs, and microservice architectures.</p>
<h3 id="real-exploit-validation-with-working-pocs">Real Exploit Validation with Working PoCs</h3>
<p>This is arguably Strix&rsquo;s most important differentiator. Traditional SAST and DAST tools are notorious for generating high volumes of false positives — alerts that require manual triage by a security engineer to determine whether they represent real risk. A 2025 study by the SANS Institute found that security teams waste an average of 30% of their time triaging false positives from legacy scanners.</p>
<p>Strix eliminates this problem at the source. When Strix reports a vulnerability, it includes a working proof-of-concept exploit that actually demonstrates the vulnerability. This means:</p>
<ul>
<li><strong>Zero false positives</strong> — If Strix reports it, the exploit works.</li>
<li><strong>Immediate reproducibility</strong> — Developers can run the PoC to see the vulnerability in action.</li>
<li><strong>Faster remediation</strong> — The PoC serves as both evidence and a test case for the fix.</li>
</ul>
<p>This approach transforms the security testing workflow from &ldquo;investigate and triage&rdquo; to &ldquo;read the PoC and fix the code.&rdquo;</p>
<h3 id="cicd-and-devsecops-integration">CI/CD and DevSecOps Integration</h3>
<p>Strix is designed from the ground up as a developer-first tool that integrates natively into CI/CD pipelines. It supports:</p>
<ul>
<li><strong>GitHub Actions</strong> — Automated scanning on every pull request with blocking gates</li>
<li><strong>GitLab CI</strong> — Pipeline integration with merge request annotations</li>
<li><strong>Bitbucket Pipelines</strong> — Automated security checks in Bitbucket workflows</li>
<li><strong>Slack</strong> — Real-time notifications for critical findings</li>
<li><strong>Jira and Linear</strong> — Automatic issue creation from discovered vulnerabilities</li>
</ul>
<p>This CI/CD-native approach means Strix can catch vulnerabilities before they ever reach production, shifting security left without adding friction to the development workflow.</p>
<h2 id="how-strix-works-under-the-hood">How Strix Works Under the Hood</h2>
<h3 id="architecture-overview">Architecture Overview</h3>
<p>Strix&rsquo;s architecture is built around three core components:</p>
<ol>
<li>
<p><strong>The Orchestrator</strong> — The central coordinator that manages the Graph of Agents, routes findings between agents, and controls the scan lifecycle. It determines which agents to deploy based on the target profile and scan mode.</p>
</li>
<li>
<p><strong>Agent Runtime</strong> — Each AI agent runs in an isolated execution environment with access to a curated set of security tools (Nmap, SQLMap, custom exploit modules, etc.). Agents can write and execute Python code dynamically to craft custom exploits.</p>
</li>
<li>
<p><strong>The Sandbox</strong> — All agent operations execute within a sandboxed environment that prevents any impact on the host system or network. This is critical for safe automated exploitation testing.</p>
</li>
</ol>
<p>The system uses a publish-subscribe message bus for inter-agent communication. When an agent discovers a finding, it publishes the result to the bus. Other agents subscribed to that finding type receive the data and can act on it, creating a dynamic, responsive testing workflow.</p>
<h3 id="supported-ai-providers-and-models">Supported AI Providers and Models</h3>
<p>Strix supports a wide range of AI providers, giving teams flexibility in choosing their preferred model:</p>
<table>
  <thead>
      <tr>
          <th>Provider</th>
          <th>Supported Models</th>
          <th>Use Case</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>OpenAI</td>
          <td>GPT-5.4, GPT-4o</td>
          <td>General-purpose pentesting with strong reasoning</td>
      </tr>
      <tr>
          <td>Anthropic</td>
          <td>Claude Sonnet 4.6</td>
          <td>Long-context analysis, complex exploit chains</td>
      </tr>
      <tr>
          <td>Google</td>
          <td>Gemini 3 Pro</td>
          <td>Multi-modal analysis, large codebase review</td>
      </tr>
      <tr>
          <td>Ollama</td>
          <td>Local open-source models</td>
          <td>Air-gapped environments, offline testing</td>
      </tr>
      <tr>
          <td>LMStudio</td>
          <td>Local models</td>
          <td>Privacy-sensitive testing, no data leaving the network</td>
      </tr>
      <tr>
          <td>Custom OpenAI-compatible</td>
          <td>Any OpenAI API-compatible endpoint</td>
          <td>Enterprise proxy setups, custom model deployments</td>
      </tr>
  </tbody>
</table>
<p>This provider flexibility is a major advantage. Teams working with sensitive data can run Strix entirely offline using local models via Ollama or LMStudio, while teams needing maximum capability can connect to frontier models from OpenAI, Anthropic, or Google.</p>
<h3 id="the-sandboxed-execution-environment">The Sandboxed Execution Environment</h3>
<p>Safety is a first-class concern in Strix. The sandboxed execution environment ensures that:</p>
<ul>
<li>Exploit code runs in an isolated container with no network access to production systems</li>
<li>File system access is restricted to a temporary working directory</li>
<li>Resource limits prevent denial-of-service conditions</li>
<li>All agent actions are logged for audit and review</li>
<li>The sandbox is destroyed after each scan, leaving no residual artifacts</li>
</ul>
<p>This sandbox design makes Strix safe to run in CI/CD pipelines alongside production code, a requirement that many traditional pentesting tools cannot meet.</p>
<h2 id="getting-started-with-strix">Getting Started with Strix</h2>
<h3 id="prerequisites-and-installation">Prerequisites and Installation</h3>
<p>Before installing Strix, ensure your environment meets these requirements:</p>
<ul>
<li><strong>Python 3.10+</strong> — Strix is written in Python and requires a recent version</li>
<li><strong>Docker</strong> — Required for the sandboxed execution environment</li>
<li><strong>An AI provider API key</strong> — OpenAI, Anthropic, Google, or a local model setup</li>
<li><strong>Git</strong> — For CI/CD integration features</li>
</ul>
<p>Installation is straightforward via pip:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-bash" data-lang="bash"><span style="display:flex;"><span>pip install strix-ai
</span></span></code></pre></div><p>Or using Docker for a fully containerized setup:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-bash" data-lang="bash"><span style="display:flex;"><span>docker pull usestrix/strix:latest
</span></span></code></pre></div><p>Strix can also be installed from source directly from the GitHub repository:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-bash" data-lang="bash"><span style="display:flex;"><span>git clone https://github.com/usestrix/strix.git
</span></span><span style="display:flex;"><span>cd strix
</span></span><span style="display:flex;"><span>pip install -e .
</span></span></code></pre></div><h3 id="configuration-and-first-scan">Configuration and First Scan</h3>
<p>After installation, configure Strix with your AI provider credentials:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-bash" data-lang="bash"><span style="display:flex;"><span>strix config set --provider openai --api-key YOUR_API_KEY
</span></span></code></pre></div><p>Then run your first scan against a target:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-bash" data-lang="bash"><span style="display:flex;"><span>strix scan https://your-target-app.com
</span></span></code></pre></div><p>Strix will automatically deploy its reconnaissance agents to map the attack surface, followed by vulnerability analysis and exploitation agents. The first scan on a typical web application takes 10–30 minutes depending on the complexity of the target and the scan mode selected.</p>
<h3 id="understanding-scan-modes-quick-standard-deep">Understanding Scan Modes (Quick, Standard, Deep)</h3>
<p>Strix offers three scan modes that balance speed against thoroughness:</p>
<table>
  <thead>
      <tr>
          <th>Mode</th>
          <th>Duration</th>
          <th>Coverage</th>
          <th>Best For</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>Quick</td>
          <td>5–10 minutes</td>
          <td>Surface-level recon, common vulnerabilities</td>
          <td>CI/CD pre-merge checks, rapid feedback</td>
      </tr>
      <tr>
          <td>Standard</td>
          <td>10–30 minutes</td>
          <td>Full OWASP Top 10 coverage, moderate exploitation</td>
          <td>Regular security testing, sprint reviews</td>
      </tr>
      <tr>
          <td>Deep</td>
          <td>30–90 minutes</td>
          <td>Comprehensive testing, complex exploit chains, business logic analysis</td>
          <td>Pre-release audits, compliance requirements</td>
      </tr>
  </tbody>
</table>
<p>The Quick mode is designed for CI/CD pipelines where scan time directly impacts developer velocity. Deep mode is reserved for thorough assessments before major releases or compliance audits.</p>
<h2 id="advanced-usage-and-techniques">Advanced Usage and Techniques</h2>
<h3 id="headless-mode-for-automation">Headless Mode for Automation</h3>
<p>For automated and CI/CD environments, Strix provides a headless mode that runs without interactive output:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-bash" data-lang="bash"><span style="display:flex;"><span>strix scan https://your-target-app.com --headless --output results.json
</span></span></code></pre></div><p>Headless mode outputs structured JSON results that can be parsed by other tools, posted to Slack, or used to create Jira issues automatically. This is the foundation of Strix&rsquo;s DevSecOps integration.</p>
<h3 id="custom-instructions-and-targeting">Custom Instructions and Targeting</h3>
<p>Advanced users can provide custom instructions to guide Strix&rsquo;s agents toward specific testing priorities:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-bash" data-lang="bash"><span style="display:flex;"><span>strix scan https://your-target-app.com <span style="color:#ae81ff">\
</span></span></span><span style="display:flex;"><span><span style="color:#ae81ff"></span>  --instructions <span style="color:#e6db74">&#34;Focus on authentication bypass and SSRF vulnerabilities. Skip XSS testing.&#34;</span>
</span></span></code></pre></div><p>This capability allows teams to tailor scans to their specific threat model, reducing scan time by focusing on the vulnerabilities most relevant to their application architecture.</p>
<h3 id="multi-target-scanning">Multi-Target Scanning</h3>
<p>Strix supports scanning multiple targets in a single run, useful for organizations managing multiple applications or microservices:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-bash" data-lang="bash"><span style="display:flex;"><span>strix scan https://app1.example.com https://api.example.com https://admin.example.com
</span></span></code></pre></div><p>Each target is tested independently, and results are aggregated into a single report with per-target findings.</p>
<h2 id="integrating-strix-into-cicd-pipelines">Integrating Strix into CI/CD Pipelines</h2>
<h3 id="github-actions-setup">GitHub Actions Setup</h3>
<p>Integrating Strix with GitHub Actions is the most common deployment pattern. Here is a minimal workflow configuration:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-yaml" data-lang="yaml"><span style="display:flex;"><span><span style="color:#f92672">name</span>: <span style="color:#ae81ff">Strix Security Scan</span>
</span></span><span style="display:flex;"><span><span style="color:#f92672">on</span>:
</span></span><span style="display:flex;"><span>  <span style="color:#f92672">pull_request</span>:
</span></span><span style="display:flex;"><span>    <span style="color:#f92672">branches</span>: [<span style="color:#ae81ff">main]</span>
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#f92672">jobs</span>:
</span></span><span style="display:flex;"><span>  <span style="color:#f92672">security-scan</span>:
</span></span><span style="display:flex;"><span>    <span style="color:#f92672">runs-on</span>: <span style="color:#ae81ff">ubuntu-latest</span>
</span></span><span style="display:flex;"><span>    <span style="color:#f92672">steps</span>:
</span></span><span style="display:flex;"><span>      - <span style="color:#f92672">uses</span>: <span style="color:#ae81ff">actions/checkout@v4</span>
</span></span><span style="display:flex;"><span>      - <span style="color:#f92672">name</span>: <span style="color:#ae81ff">Run Strix Scan</span>
</span></span><span style="display:flex;"><span>        <span style="color:#f92672">uses</span>: <span style="color:#ae81ff">usestrix/strix-action@v1</span>
</span></span><span style="display:flex;"><span>        <span style="color:#f92672">with</span>:
</span></span><span style="display:flex;"><span>          <span style="color:#f92672">target</span>: <span style="color:#ae81ff">${{ github.event.deployment.payload.web_url || &#39;http://localhost:3000&#39; }}</span>
</span></span><span style="display:flex;"><span>          <span style="color:#f92672">api-key</span>: <span style="color:#ae81ff">${{ secrets.STRIX_API_KEY }}</span>
</span></span><span style="display:flex;"><span>          <span style="color:#f92672">mode</span>: <span style="color:#ae81ff">quick</span>
</span></span><span style="display:flex;"><span>          <span style="color:#f92672">fail-on</span>: <span style="color:#ae81ff">critical</span>
</span></span></code></pre></div><p>This configuration runs a Quick scan on every pull request targeting the main branch. If Strix discovers a critical vulnerability, the workflow fails, preventing the PR from being merged until the issue is addressed.</p>
<h3 id="gitlab-and-bitbucket-integration">GitLab and Bitbucket Integration</h3>
<p>Strix provides equivalent integrations for GitLab CI and Bitbucket Pipelines. The GitLab configuration follows a similar pattern:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-yaml" data-lang="yaml"><span style="display:flex;"><span><span style="color:#f92672">strix-scan</span>:
</span></span><span style="display:flex;"><span>  <span style="color:#f92672">image</span>: <span style="color:#ae81ff">usestrix/strix:latest</span>
</span></span><span style="display:flex;"><span>  <span style="color:#f92672">script</span>:
</span></span><span style="display:flex;"><span>    - <span style="color:#ae81ff">strix scan $CI_ENVIRONMENT_URL --headless --output gl-results.json</span>
</span></span><span style="display:flex;"><span>  <span style="color:#f92672">artifacts</span>:
</span></span><span style="display:flex;"><span>    <span style="color:#f92672">paths</span>: [<span style="color:#ae81ff">gl-results.json]</span>
</span></span></code></pre></div><p>Bitbucket users can add Strix as a pipe in their <code>bitbucket-pipelines.yml</code>:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-yaml" data-lang="yaml"><span style="display:flex;"><span>- <span style="color:#f92672">pipe</span>: <span style="color:#ae81ff">docker://usestrix/strix-bitbucket-pipe:latest</span>
</span></span><span style="display:flex;"><span>  <span style="color:#f92672">variables</span>:
</span></span><span style="display:flex;"><span>    <span style="color:#f92672">TARGET_URL</span>: <span style="color:#e6db74">&#39;https://staging.example.com&#39;</span>
</span></span><span style="display:flex;"><span>    <span style="color:#f92672">STRIX_API_KEY</span>: <span style="color:#ae81ff">$STRIX_API_KEY</span>
</span></span></code></pre></div><h3 id="pr-based-security-gates">PR-Based Security Gates</h3>
<p>The most powerful pattern is using Strix as a PR-based security gate. When configured this way:</p>
<ol>
<li>A developer opens a pull request with code changes</li>
<li>CI automatically deploys the branch to an ephemeral environment</li>
<li>Strix scans the ephemeral environment</li>
<li>If critical or high-severity vulnerabilities are found, the PR is blocked</li>
<li>The developer receives a detailed finding with a working PoC</li>
<li>After fixing the vulnerability, the developer re-runs the scan</li>
<li>The PR is unblocked and can be merged</li>
</ol>
<p>This workflow catches vulnerabilities at the earliest possible stage — before code review, before staging, and certainly before production. Organizations using this pattern report catching 60–80% of security issues during development rather than in production.</p>
<h2 id="strix-vs-other-ai-pentesting-tools-2026-comparison">Strix vs Other AI Pentesting Tools (2026 Comparison)</h2>
<h3 id="strix-vs-xbow">Strix vs XBOW</h3>
<p>XBOW (by XBOW AI) is a commercial AI pentesting tool focused on API security testing. Here is how they compare:</p>
<table>
  <thead>
      <tr>
          <th>Feature</th>
          <th>Strix</th>
          <th>XBOW</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>License</td>
          <td>Open Source (Apache 2.0)</td>
          <td>Commercial</td>
      </tr>
      <tr>
          <td>Price</td>
          <td>Free</td>
          <td>Subscription-based</td>
      </tr>
      <tr>
          <td>Architecture</td>
          <td>Multi-agent (Graph of Agents)</td>
          <td>Single-agent</td>
      </tr>
      <tr>
          <td>CI/CD Integration</td>
          <td>GitHub Actions, GitLab, Bitbucket, Slack, Jira, Linear</td>
          <td>GitHub Actions, Jira</td>
      </tr>
      <tr>
          <td>AI Providers</td>
          <td>OpenAI, Anthropic, Google, Ollama, LMStudio</td>
          <td>Proprietary model</td>
      </tr>
      <tr>
          <td>Exploit Validation</td>
          <td>Working PoCs for every finding</td>
          <td>PoCs for select findings</td>
      </tr>
      <tr>
          <td>Local/Air-Gapped</td>
          <td>Yes (via Ollama/LMStudio)</td>
          <td>No</td>
      </tr>
      <tr>
          <td>Community</td>
          <td>42,000+ GitHub stars</td>
          <td>Smaller community</td>
      </tr>
  </tbody>
</table>
<p>Strix&rsquo;s open-source nature and multi-provider support give it a significant advantage for teams that want flexibility and community-driven development.</p>
<h3 id="strix-vs-nodezero">Strix vs NodeZero</h3>
<p>NodeZero (by Horizon3.ai) is an enterprise autonomous pentesting platform. The comparison reveals different target audiences:</p>
<table>
  <thead>
      <tr>
          <th>Feature</th>
          <th>Strix</th>
          <th>NodeZero</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>Target Audience</td>
          <td>Developers, SMBs, DevOps teams</td>
          <td>Enterprise security teams</td>
      </tr>
      <tr>
          <td>Deployment</td>
          <td>CLI, Docker, CI/CD</td>
          <td>SaaS platform, on-prem appliance</td>
      </tr>
      <tr>
          <td>Setup Time</td>
          <td>Minutes</td>
          <td>Hours to days</td>
      </tr>
      <tr>
          <td>AI Architecture</td>
          <td>Graph of Agents (multi-agent)</td>
          <td>Proprietary AI engine</td>
      </tr>
      <tr>
          <td>Reporting</td>
          <td>JSON, CLI output, CI annotations</td>
          <td>Full compliance reports, executive summaries</td>
      </tr>
      <tr>
          <td>Price</td>
          <td>Free (open source)</td>
          <td>Enterprise pricing ($50K+/year)</td>
      </tr>
      <tr>
          <td>External Pentesting</td>
          <td>Yes</td>
          <td>Yes (with SaaS node)</td>
      </tr>
      <tr>
          <td>Internal Network Testing</td>
          <td>Limited</td>
          <td>Full internal network assessment</td>
      </tr>
  </tbody>
</table>
<p>Strix is the better choice for development teams that want fast, automated security testing integrated into their existing workflows. NodeZero is更适合 for enterprise teams needing comprehensive external and internal network penetration testing with compliance-ready reporting.</p>
<h3 id="strix-vs-traditional-sastdast">Strix vs Traditional SAST/DAST</h3>
<p>Traditional static and dynamic analysis tools have been the standard for application security testing for over a decade. Strix represents a fundamentally different approach:</p>
<table>
  <thead>
      <tr>
          <th>Aspect</th>
          <th>Traditional SAST/DAST</th>
          <th>Strix</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>False Positive Rate</td>
          <td>30–50% typical</td>
          <td>Near zero (validated exploits)</td>
      </tr>
      <tr>
          <td>Setup Complexity</td>
          <td>Moderate to high</td>
          <td>Low (pip install)</td>
      </tr>
      <tr>
          <td>Scan Speed</td>
          <td>Fast (pattern matching)</td>
          <td>Moderate (AI reasoning)</td>
      </tr>
      <tr>
          <td>Depth</td>
          <td>Surface-level patterns</td>
          <td>Deep business logic analysis</td>
      </tr>
      <tr>
          <td>Exploit Validation</td>
          <td>None</td>
          <td>Working PoCs</td>
      </tr>
      <tr>
          <td>AI Integration</td>
          <td>Limited or none</td>
          <td>Native multi-agent AI</td>
      </tr>
      <tr>
          <td>Cost</td>
          <td>$10K–$100K/year</td>
          <td>Free (open source)</td>
      </tr>
      <tr>
          <td>Learning Curve</td>
          <td>Moderate</td>
          <td>Low (CLI-native)</td>
      </tr>
  </tbody>
</table>
<p>Traditional SAST/DAST tools still have a role in catching simple, well-known vulnerability patterns quickly. But for deep, contextual security testing that understands application logic and generates verified exploits, Strix is the superior choice.</p>
<h2 id="real-world-use-cases">Real-World Use Cases</h2>
<h3 id="application-security-testing">Application Security Testing</h3>
<p>The primary use case for Strix is automated security testing of web applications and APIs. Development teams integrate Strix into their CI/CD pipeline to catch vulnerabilities during development. A typical SaaS company running 50+ microservices might configure Strix to scan each service on every PR, catching hundreds of vulnerabilities per quarter that would otherwise reach production.</p>
<h3 id="bug-bounty-automation">Bug Bounty Automation</h3>
<p>Bug bounty hunters and security researchers use Strix to automate the initial reconnaissance and vulnerability discovery phases of their workflow. Strix can quickly map an application&rsquo;s attack surface, identify low-hanging fruit, and generate PoCs for common vulnerability classes. This frees the researcher to focus on complex, business-logic-level vulnerabilities that require human creativity.</p>
<h3 id="continuous-security-monitoring">Continuous Security Monitoring</h3>
<p>Organizations with compliance requirements (SOC 2, ISO 27001, PCI DSS) use Strix for continuous security monitoring. By scheduling regular scans — daily for critical applications, weekly for internal tools — teams maintain an ongoing security posture assessment without manual effort. Strix&rsquo;s enterprise features, including SSO (SAML/OIDC), compliance-ready reports, and VPC/self-hosted deployment, make it suitable for regulated environments.</p>
<h2 id="limitations-and-considerations">Limitations and Considerations</h2>
<p>While Strix is a powerful tool, it is important to understand its limitations:</p>
<ul>
<li>
<p><strong>Not a replacement for human testers</strong> — Strix excels at automated vulnerability discovery, but complex business logic flaws, multi-step attack chains, and context-dependent vulnerabilities still benefit from human expertise. The best approach is Strix handling the 80% of routine testing while human testers focus on the remaining 20% that requires creativity and domain knowledge.</p>
</li>
<li>
<p><strong>AI provider costs</strong> — While Strix itself is free, running scans requires API calls to AI providers. A Deep scan on a complex application might consume $5–$20 in API credits. Teams should budget accordingly, especially for high-volume CI/CD usage.</p>
</li>
<li>
<p><strong>Scan duration</strong> — Deep scans can take 30–90 minutes, which may be too slow for some CI/CD workflows. The Quick mode addresses this for pre-merge checks, but teams needing sub-minute scan times should supplement Strix with traditional SAST tools for rapid feedback.</p>
</li>
<li>
<p><strong>Network access requirements</strong> — Strix needs network access to the target application. For internal services or staging environments behind VPNs, additional configuration is required.</p>
</li>
<li>
<p><strong>Learning curve for custom instructions</strong> — While basic usage is straightforward, getting the most out of Strix&rsquo;s custom instructions and targeting features requires understanding how to phrase prompts effectively for the AI agents.</p>
</li>
</ul>
<h2 id="enterprise-features-and-pricing">Enterprise Features and Pricing</h2>
<p>Strix offers a tiered feature set:</p>
<table>
  <thead>
      <tr>
          <th>Feature</th>
          <th>Community (Free)</th>
          <th>Enterprise</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>Core scanning engine</td>
          <td>✓</td>
          <td>✓</td>
      </tr>
      <tr>
          <td>Graph of Agents</td>
          <td>✓</td>
          <td>✓</td>
      </tr>
      <tr>
          <td>CI/CD integrations</td>
          <td>GitHub Actions only</td>
          <td>All integrations</td>
      </tr>
      <tr>
          <td>AI providers</td>
          <td>All supported</td>
          <td>All supported + custom</td>
      </tr>
      <tr>
          <td>SSO (SAML/OIDC)</td>
          <td>—</td>
          <td>✓</td>
      </tr>
      <tr>
          <td>Compliance reports</td>
          <td>—</td>
          <td>SOC 2, ISO 27001, PCI DSS</td>
      </tr>
      <tr>
          <td>BYOK (Bring Your Own Key)</td>
          <td>—</td>
          <td>✓</td>
      </tr>
      <tr>
          <td>VPC/Self-hosted deployment</td>
          <td>—</td>
          <td>✓</td>
      </tr>
      <tr>
          <td>Priority support</td>
          <td>—</td>
          <td>✓</td>
      </tr>
      <tr>
          <td>Price</td>
          <td>Free</td>
          <td>Custom pricing</td>
      </tr>
  </tbody>
</table>
<p>Enterprise pricing is available through the Strix website and varies based on deployment scale, support level, and additional compliance requirements.</p>
<h2 id="conclusion-is-strix-right-for-your-security-workflow">Conclusion: Is Strix Right for Your Security Workflow?</h2>
<p>Strix represents a genuine leap forward in automated security testing. Its multi-agent architecture, real exploit validation, and CI/CD-native design address the fundamental problems that have plagued traditional vulnerability scanners for years: false positives, slow feedback loops, and poor developer experience.</p>
<p>For development teams that ship code frequently and want to catch vulnerabilities before they reach production, Strix is an essential addition to the security toolkit. Its open-source nature, zero cost for the core product, and broad AI provider support make it accessible to teams of any size.</p>
<p>For enterprise security teams, Strix offers a complementary capability to existing testing programs. It excels at continuous, automated testing that runs alongside development, freeing human testers to focus on the complex, creative work that AI cannot yet handle.</p>
<p>The 42,000+ GitHub stars and rapid community growth are not just vanity metrics — they reflect a genuine need that Strix fills. As attack surfaces continue to expand and the security talent gap persists, autonomous AI-powered penetration testing tools like Strix will become a standard part of every organization&rsquo;s security program.</p>
<h2 id="frequently-asked-questions">Frequently Asked Questions</h2>
<h3 id="is-strix-completely-free-to-use">Is Strix completely free to use?</h3>
<p>Yes, Strix is open source under the Apache 2.0 license and completely free to use. You only pay for the AI provider API calls made during scans, which typically cost $1–$20 per scan depending on the mode and target complexity. Enterprise features like SSO, compliance reports, and VPC deployment require a paid subscription.</p>
<h3 id="does-strix-replace-human-penetration-testers">Does Strix replace human penetration testers?</h3>
<p>No, Strix is designed to augment human testers, not replace them. It automates the 80% of routine vulnerability discovery and validation work, allowing human security experts to focus on complex business logic flaws, multi-step attack chains, and creative exploitation scenarios that require human judgment and domain expertise.</p>
<h3 id="can-strix-run-completely-offline">Can Strix run completely offline?</h3>
<p>Yes, Strix can run fully offline using local AI models via Ollama or LMStudio. This makes it suitable for air-gapped environments, classified networks, and organizations with strict data residency requirements. The trade-off is that local models may produce less sophisticated results than frontier models from OpenAI or Anthropic.</p>
<h3 id="what-types-of-vulnerabilities-can-strix-detect">What types of vulnerabilities can Strix detect?</h3>
<p>Strix covers the full OWASP Top 10, including broken access control, SQL injection, cross-site scripting (XSS), server-side request forgery (SSRF), XML external entity injection (XXE), remote code execution (RCE), authentication bypass, and business logic flaws. It also detects configuration issues, exposed secrets, and insecure API endpoints.</p>
<h3 id="how-long-does-a-typical-strix-scan-take">How long does a typical Strix scan take?</h3>
<p>Scan duration depends on the mode selected. Quick mode completes in 5–10 minutes and is designed for CI/CD pre-merge checks. Standard mode takes 10–30 minutes and provides full OWASP Top 10 coverage. Deep mode runs for 30–90 minutes and includes comprehensive testing with complex exploit chains and business logic analysis.</p>
]]></content:encoded></item><item><title>CAI Open-Source Security Agent Framework: Build and Deploy Offensive AI Security Agents</title><link>https://baeseokjae.github.io/posts/cai-open-source-security-agent-guide-2026/</link><pubDate>Sat, 25 Apr 2026 15:04:16 +0000</pubDate><guid>https://baeseokjae.github.io/posts/cai-open-source-security-agent-guide-2026/</guid><description>Complete guide to CAI, the open-source cybersecurity AI framework that won 41/45 CTF flags and delivers 91% solve rates for automated pentesting.</description><content:encoded><![CDATA[<p>CAI (Cybersecurity AI) is an open-source framework from Alias Robotics that lets security engineers build, orchestrate, and deploy autonomous AI agents for offensive security tasks — from reconnaissance to exploitation, bug bounty automation to CTF solving. Install it with <code>pip install cai-framework</code>, point it at a target, and it handles the full pentest loop without step-by-step human direction.</p>
<h2 id="what-is-cai-the-open-source-cybersecurity-ai-framework-explained">What Is CAI? The Open-Source Cybersecurity AI Framework Explained</h2>
<p>CAI is an open-source cybersecurity AI framework developed by Alias Robotics that provides a structured, modular foundation for building autonomous security agents capable of performing offensive tasks — reconnaissance, vulnerability scanning, exploitation, and privilege escalation — with minimal human intervention. Unlike running an LLM against a system prompt and hoping for the best, CAI wraps the AI loop in a production-ready architecture: structured agent definitions, reusable tool libraries, handoff protocols between agents, input/output guardrails, and human-in-the-loop (HITL) checkpoints. The framework supports over 300 AI models including OpenAI GPT-4o, Anthropic Claude, DeepSeek, and local deployments via Ollama — meaning you can run fully air-gapped without a cloud dependency.</p>
<p>In 2025, CAI proved it was not a research toy. It won the Neurogrid CTF, capturing 41 of 45 available flags and the $50,000 top prize. In the Cyber Apocalypse CTF 2025, competing against 8,129 teams, it outperformed 99.7% of participants at peak — while only running for 3 of the 72 available hours. Across all major CTF events in 2025, CAI achieved a 99.04% mean percentile ranking. Against real-world targets like vulnbank.org, it identified SQL injection vulnerabilities allowing full account takeover. The framework delivers 91% solve rates and 98% cost reduction versus traditional security testing approaches.</p>
<h2 id="installing-cai-from-zero-to-running-your-first-security-agent">Installing CAI: From Zero to Running Your First Security Agent</h2>
<p>CAI installation follows standard Python tooling and takes under five minutes on any system with Python 3.10+. The framework is distributed via PyPI, with optional extras for specific integrations.</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-bash" data-lang="bash"><span style="display:flex;"><span><span style="color:#75715e"># Basic installation</span>
</span></span><span style="display:flex;"><span>pip install cai-framework
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#75715e"># With all optional dependencies (recommended for security work)</span>
</span></span><span style="display:flex;"><span>pip install cai-framework<span style="color:#f92672">[</span>all<span style="color:#f92672">]</span>
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#75715e"># Verify installation</span>
</span></span><span style="display:flex;"><span>cai --version
</span></span></code></pre></div><p>Before running your first agent, set your model provider credentials. CAI reads these from environment variables:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-bash" data-lang="bash"><span style="display:flex;"><span><span style="color:#75715e"># For Anthropic Claude (recommended for complex reasoning)</span>
</span></span><span style="display:flex;"><span>export ANTHROPIC_API_KEY<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;sk-ant-...&#34;</span>
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#75715e"># For OpenAI</span>
</span></span><span style="display:flex;"><span>export OPENAI_API_KEY<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;sk-...&#34;</span>
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#75715e"># For local Ollama (air-gapped environments)</span>
</span></span><span style="display:flex;"><span><span style="color:#75715e"># No API key needed — just ensure Ollama is running</span>
</span></span></code></pre></div><p>The fastest way to validate the install is to run CAI&rsquo;s built-in interactive mode against a test target. The Alias Robotics team maintains a deliberately vulnerable practice environment:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-bash" data-lang="bash"><span style="display:flex;"><span><span style="color:#75715e"># Launch CAI interactive mode</span>
</span></span><span style="display:flex;"><span>cai
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#75715e"># Inside the CAI shell, run against DVWA or a local test box</span>
</span></span><span style="display:flex;"><span>&gt; target 10.10.10.10
</span></span><span style="display:flex;"><span>&gt; agent recon
</span></span><span style="display:flex;"><span>&gt; run
</span></span></code></pre></div><p>CAI&rsquo;s interactive mode gives you a REPL where you compose agent pipelines before executing them. For production use, you&rsquo;ll define agents programmatically in Python — which is where the real power surfaces.</p>
<h2 id="cais-8-pillar-architecture-a-deep-dive">CAI&rsquo;s 8-Pillar Architecture: A Deep Dive</h2>
<p>CAI is built around eight architectural pillars that together create a reliable, auditable security automation pipeline. Understanding these pillars is essential before building custom agents, because each one maps to a specific failure mode in naive LLM-based security automation.</p>
<p><strong>Agents</strong> are the core execution unit. Each agent is a structured object with a defined role, a set of allowed tools, a system prompt scoped to its function, and optional handoff targets. You don&rsquo;t instantiate a generic &ldquo;security AI&rdquo; — you define a recon agent, an exploitation agent, a reporting agent, each with a narrow charter.</p>
<p><strong>Tools</strong> are Python functions exposed to agents as callable actions. CAI ships with pre-built tools for port scanning (nmap integration), web crawling, SQL injection probing, SSH connection management, and CVE lookup. You can extend this with your own tools decorated with <code>@cai_tool</code>.</p>
<p><strong>Handoffs</strong> are the protocol by which one agent transfers control to another, passing context and artifacts. A recon agent that finds an open SSH port can hand off to an SSH brute-force agent with the discovered credentials list — without you writing the orchestration glue.</p>
<p><strong>Patterns</strong> are reusable agent workflow templates. CAI ships patterns for vulnerability assessment, privilege escalation, lateral movement, and report generation.</p>
<p><strong>Turns</strong> are the unit of agent execution. Each turn, the agent receives context, reasons, picks a tool or handoff, executes, and returns an observation. CAI tracks turn history for debugging and audit logs.</p>
<p><strong>Tracing</strong> provides structured logs of every agent action, tool call, model inference, and handoff. Traces are exportable to JSON and integrate with observability platforms.</p>
<p><strong>Guardrails</strong> are input and output filters that block unsafe commands, detect prompt injection attempts, and enforce scope constraints (e.g., &ldquo;only target this IP range&rdquo;).</p>
<p><strong>HITL (Human-in-the-Loop)</strong> checkpoints pause execution before high-risk actions — destructive exploits, data exfiltration, privilege escalation — and wait for human confirmation. You configure which action categories require approval.</p>
<h2 id="building-your-first-offensive-security-agent-with-cai">Building Your First Offensive Security Agent with CAI</h2>
<p>A complete, functional CAI agent requires three components: a role definition, a tool set, and execution logic. Here is a minimal but real reconnaissance agent you can adapt for authorized testing:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-python" data-lang="python"><span style="display:flex;"><span><span style="color:#f92672">from</span> cai <span style="color:#f92672">import</span> Agent, run
</span></span><span style="display:flex;"><span><span style="color:#f92672">from</span> cai.tools <span style="color:#f92672">import</span> nmap_scan, whois_lookup, dns_enumerate
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>recon_agent <span style="color:#f92672">=</span> Agent(
</span></span><span style="display:flex;"><span>    name<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;ReconAgent&#34;</span>,
</span></span><span style="display:flex;"><span>    model<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;claude-sonnet-4-6&#34;</span>,
</span></span><span style="display:flex;"><span>    instructions<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;&#34;&#34;
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">    You are a reconnaissance specialist. Your goal is to map the attack surface
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">    of the target system. Use nmap for port/service discovery, DNS enumeration
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">    for subdomain mapping, and WHOIS for ownership context.
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">    
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">    Always start with passive reconnaissance before active scanning.
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">    Report open ports, running services with version info, and any
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">    identified subdomains in a structured JSON format.
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">    
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">    Scope: </span><span style="color:#e6db74">{target}</span><span style="color:#e6db74">. Do not scan outside this scope.
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">    &#34;&#34;&#34;</span>,
</span></span><span style="display:flex;"><span>    tools<span style="color:#f92672">=</span>[nmap_scan, whois_lookup, dns_enumerate],
</span></span><span style="display:flex;"><span>)
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>result <span style="color:#f92672">=</span> run(recon_agent, target<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;10.10.10.10&#34;</span>)
</span></span><span style="display:flex;"><span>print(result<span style="color:#f92672">.</span>report)
</span></span></code></pre></div><p>The <code>run()</code> call handles the agentic loop — the agent reasons about which tool to call next, executes it, observes the result, and continues until it determines reconnaissance is complete or hits a turn limit.</p>
<p>For a more realistic workflow, chain a recon agent into a vulnerability assessment agent:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-python" data-lang="python"><span style="display:flex;"><span><span style="color:#f92672">from</span> cai <span style="color:#f92672">import</span> Agent, run
</span></span><span style="display:flex;"><span><span style="color:#f92672">from</span> cai.tools <span style="color:#f92672">import</span> nmap_scan, nikto_scan, sqlmap_probe
</span></span><span style="display:flex;"><span><span style="color:#f92672">from</span> cai.handoffs <span style="color:#f92672">import</span> create_handoff
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>vuln_agent <span style="color:#f92672">=</span> Agent(
</span></span><span style="display:flex;"><span>    name<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;VulnAssessAgent&#34;</span>,
</span></span><span style="display:flex;"><span>    model<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;claude-sonnet-4-6&#34;</span>,
</span></span><span style="display:flex;"><span>    instructions<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;&#34;&#34;
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">    You receive a recon report. Your job is to identify exploitable vulnerabilities
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">    in the discovered services. Probe web services with nikto, test for SQL injection
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">    on identified endpoints with sqlmap in safe/read-only mode.
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">    
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">    Prioritize findings by CVSS score. Output a structured vulnerability report.
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">    &#34;&#34;&#34;</span>,
</span></span><span style="display:flex;"><span>    tools<span style="color:#f92672">=</span>[nikto_scan, sqlmap_probe],
</span></span><span style="display:flex;"><span>)
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>recon_agent<span style="color:#f92672">.</span>handoffs <span style="color:#f92672">=</span> [create_handoff(vuln_agent, condition<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;recon_complete&#34;</span>)]
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>result <span style="color:#f92672">=</span> run(recon_agent, target<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;10.10.10.10&#34;</span>)
</span></span></code></pre></div><p>When recon finishes, the framework automatically invokes the handoff condition, packages the recon context, and starts the vuln agent — no manual wiring required.</p>
<h2 id="tools-and-handoffs-modular-security-automation-at-scale">Tools and Handoffs: Modular Security Automation at Scale</h2>
<p>CAI&rsquo;s tool system is one of its strongest design decisions. Every action an agent can take is a Python function registered as a tool — this means every capability is auditable, testable, and replaceable without touching the agent logic.</p>
<p>The built-in tool library covers the full offensive security kill chain. Network tools include nmap, masscan, and netcat wrappers. Web tools cover dirbusting, parameter fuzzing, and HTTP request manipulation. Exploitation tools integrate Metasploit module execution and custom payload delivery. Post-exploitation tools handle shell management, file transfer, and privilege escalation checks.</p>
<p>Registering a custom tool requires a single decorator:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-python" data-lang="python"><span style="display:flex;"><span><span style="color:#f92672">from</span> cai <span style="color:#f92672">import</span> cai_tool
</span></span><span style="display:flex;"><span><span style="color:#f92672">import</span> subprocess
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#a6e22e">@cai_tool</span>
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">def</span> <span style="color:#a6e22e">run_custom_scan</span>(target: str, port: int, flags: str <span style="color:#f92672">=</span> <span style="color:#e6db74">&#34;&#34;</span>) <span style="color:#f92672">-&gt;</span> str:
</span></span><span style="display:flex;"><span>    <span style="color:#e6db74">&#34;&#34;&#34;
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">    Run a custom nmap scan against a specific port.
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">    
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">    Args:
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">        target: IP address or hostname to scan
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">        port: Port number to probe
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">        flags: Additional nmap flags (e.g., &#39;-sV -sC&#39;)
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">    
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">    Returns:
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">        Raw nmap output as string
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">    &#34;&#34;&#34;</span>
</span></span><span style="display:flex;"><span>    cmd <span style="color:#f92672">=</span> <span style="color:#e6db74">f</span><span style="color:#e6db74">&#34;nmap </span><span style="color:#e6db74">{</span>flags<span style="color:#e6db74">}</span><span style="color:#e6db74"> -p </span><span style="color:#e6db74">{</span>port<span style="color:#e6db74">}</span><span style="color:#e6db74"> </span><span style="color:#e6db74">{</span>target<span style="color:#e6db74">}</span><span style="color:#e6db74">&#34;</span>
</span></span><span style="display:flex;"><span>    result <span style="color:#f92672">=</span> subprocess<span style="color:#f92672">.</span>run(cmd<span style="color:#f92672">.</span>split(), capture_output<span style="color:#f92672">=</span><span style="color:#66d9ef">True</span>, text<span style="color:#f92672">=</span><span style="color:#66d9ef">True</span>, timeout<span style="color:#f92672">=</span><span style="color:#ae81ff">60</span>)
</span></span><span style="display:flex;"><span>    <span style="color:#66d9ef">return</span> result<span style="color:#f92672">.</span>stdout
</span></span></code></pre></div><p>The docstring is critical — CAI&rsquo;s underlying LLM reads it to decide when and how to use the tool. Clear argument descriptions and return value documentation directly impact agent decision quality.</p>
<p>Handoffs solve the coordination problem in multi-agent security automation. When a recon agent discovers a web application, it shouldn&rsquo;t try to exploit it — that&rsquo;s a different role, different tools, different reasoning. Handoffs let you enforce this separation while preserving context continuity:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-python" data-lang="python"><span style="display:flex;"><span><span style="color:#f92672">from</span> cai.handoffs <span style="color:#f92672">import</span> Handoff
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#75715e"># Define a conditional handoff: only trigger if a web service was found</span>
</span></span><span style="display:flex;"><span>web_handoff <span style="color:#f92672">=</span> Handoff(
</span></span><span style="display:flex;"><span>    target<span style="color:#f92672">=</span>web_exploitation_agent,
</span></span><span style="display:flex;"><span>    condition<span style="color:#f92672">=</span><span style="color:#66d9ef">lambda</span> ctx: <span style="color:#e6db74">&#34;http&#34;</span> <span style="color:#f92672">in</span> ctx<span style="color:#f92672">.</span>discovered_services,
</span></span><span style="display:flex;"><span>    context_transform<span style="color:#f92672">=</span><span style="color:#66d9ef">lambda</span> ctx: {
</span></span><span style="display:flex;"><span>        <span style="color:#e6db74">&#34;target_url&#34;</span>: ctx<span style="color:#f92672">.</span>web_endpoints,
</span></span><span style="display:flex;"><span>        <span style="color:#e6db74">&#34;recon_data&#34;</span>: ctx<span style="color:#f92672">.</span>full_report
</span></span><span style="display:flex;"><span>    }
</span></span><span style="display:flex;"><span>)
</span></span></code></pre></div><p>The <code>context_transform</code> function shapes what context gets passed to the next agent — you can strip irrelevant data, add metadata, or enrich with external lookups before the hand-off occurs.</p>
<h2 id="guardrails-and-hitl-running-cai-safely-in-production">Guardrails and HITL: Running CAI Safely in Production</h2>
<p>Running autonomous offensive AI agents without guardrails is how you end up in a compliance incident. CAI&rsquo;s guardrail system provides layered protection: input validation that blocks out-of-scope targets before any tool executes, output filtering that catches accidental credential logging, and semantic guardrails that use a secondary LLM call to classify action risk before execution.</p>
<p>Setting up scope-enforcement guardrails is the first thing to configure on any CAI deployment:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-python" data-lang="python"><span style="display:flex;"><span><span style="color:#f92672">from</span> cai.guardrails <span style="color:#f92672">import</span> ScopeGuardrail, PromptInjectionGuardrail
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>scope_guard <span style="color:#f92672">=</span> ScopeGuardrail(
</span></span><span style="display:flex;"><span>    allowed_targets<span style="color:#f92672">=</span>[<span style="color:#e6db74">&#34;10.10.10.0/24&#34;</span>, <span style="color:#e6db74">&#34;vulnbank.org&#34;</span>],
</span></span><span style="display:flex;"><span>    block_on_violation<span style="color:#f92672">=</span><span style="color:#66d9ef">True</span>,
</span></span><span style="display:flex;"><span>    alert_on_violation<span style="color:#f92672">=</span><span style="color:#66d9ef">True</span>,
</span></span><span style="display:flex;"><span>)
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>injection_guard <span style="color:#f92672">=</span> PromptInjectionGuardrail(
</span></span><span style="display:flex;"><span>    sensitivity<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;high&#34;</span>,  <span style="color:#75715e"># block if injection probability &gt; 0.7</span>
</span></span><span style="display:flex;"><span>)
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>agent <span style="color:#f92672">=</span> Agent(
</span></span><span style="display:flex;"><span>    name<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;PentestAgent&#34;</span>,
</span></span><span style="display:flex;"><span>    model<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;claude-sonnet-4-6&#34;</span>,
</span></span><span style="display:flex;"><span>    instructions<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;...&#34;</span>,
</span></span><span style="display:flex;"><span>    tools<span style="color:#f92672">=</span>[<span style="color:#f92672">...</span>],
</span></span><span style="display:flex;"><span>    input_guardrails<span style="color:#f92672">=</span>[scope_guard, injection_guard],
</span></span><span style="display:flex;"><span>)
</span></span></code></pre></div><p>The HITL system is where CAI&rsquo;s approach to responsible automation becomes operationally concrete. Rather than letting agents execute any action autonomously, you define a risk taxonomy and assign checkpoint requirements:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-python" data-lang="python"><span style="display:flex;"><span><span style="color:#f92672">from</span> cai.hitl <span style="color:#f92672">import</span> HITLConfig, RiskLevel
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>hitl_config <span style="color:#f92672">=</span> HITLConfig(
</span></span><span style="display:flex;"><span>    require_approval<span style="color:#f92672">=</span>[
</span></span><span style="display:flex;"><span>        RiskLevel<span style="color:#f92672">.</span>DESTRUCTIVE,      <span style="color:#75715e"># data deletion, service disruption</span>
</span></span><span style="display:flex;"><span>        RiskLevel<span style="color:#f92672">.</span>EXFILTRATION,     <span style="color:#75715e"># file downloads, data extraction</span>
</span></span><span style="display:flex;"><span>        RiskLevel<span style="color:#f92672">.</span>PRIVILEGE_ESC,    <span style="color:#75715e"># sudo, privilege escalation attempts</span>
</span></span><span style="display:flex;"><span>    ],
</span></span><span style="display:flex;"><span>    auto_approve<span style="color:#f92672">=</span>[
</span></span><span style="display:flex;"><span>        RiskLevel<span style="color:#f92672">.</span>PASSIVE_RECON,    <span style="color:#75715e"># whois, DNS, passive scanning</span>
</span></span><span style="display:flex;"><span>        RiskLevel<span style="color:#f92672">.</span>ACTIVE_RECON,     <span style="color:#75715e"># nmap, service fingerprinting</span>
</span></span><span style="display:flex;"><span>    ],
</span></span><span style="display:flex;"><span>    approval_timeout<span style="color:#f92672">=</span><span style="color:#ae81ff">300</span>,           <span style="color:#75715e"># seconds before auto-deny</span>
</span></span><span style="display:flex;"><span>    approval_channel<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;slack&#34;</span>,       <span style="color:#75715e"># where approval requests go</span>
</span></span><span style="display:flex;"><span>)
</span></span></code></pre></div><p>When an agent attempts a destructive action, CAI pauses, serializes the action context, sends an approval request to your configured channel (Slack, email, webhook), and waits. If no approval arrives within the timeout, the action is denied and the agent proceeds to the next available path.</p>
<h2 id="multi-agent-patterns-swarm-hierarchical-and-parallel-coordination">Multi-Agent Patterns: Swarm, Hierarchical, and Parallel Coordination</h2>
<p>CAI supports three primary multi-agent coordination patterns, each suited to different security automation scenarios. Choosing the wrong pattern is the most common mistake teams make when scaling beyond single-agent workflows.</p>
<p><strong>Swarm coordination</strong> deploys multiple peer agents against the same target simultaneously. Each agent works a different attack vector — one probing web services, another testing authentication, a third checking for known CVEs in discovered software. Swarm is ideal for broad vulnerability assessment where time is the constraint. CAI&rsquo;s Dragos OT CTF performance (Rank 1 in hours 7-8, 32/34 challenges, 37% velocity advantage over top human teams) was largely achieved through swarm coordination across different challenge categories.</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-python" data-lang="python"><span style="display:flex;"><span><span style="color:#f92672">from</span> cai.patterns <span style="color:#f92672">import</span> SwarmPattern
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>swarm <span style="color:#f92672">=</span> SwarmPattern(
</span></span><span style="display:flex;"><span>    agents<span style="color:#f92672">=</span>[web_recon_agent, auth_probe_agent, cve_scan_agent],
</span></span><span style="display:flex;"><span>    coordination<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;parallel&#34;</span>,
</span></span><span style="display:flex;"><span>    merge_strategy<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;union&#34;</span>,  <span style="color:#75715e"># combine all findings into one report</span>
</span></span><span style="display:flex;"><span>)
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>result <span style="color:#f92672">=</span> swarm<span style="color:#f92672">.</span>run(target<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;10.10.10.10&#34;</span>)
</span></span></code></pre></div><p><strong>Hierarchical coordination</strong> uses a manager agent to direct specialist agents. The manager receives the initial objective, decomposes it into subtasks, assigns agents, and synthesizes results. This pattern is best for complex, multi-phase engagements where the sequence matters:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-python" data-lang="python"><span style="display:flex;"><span><span style="color:#f92672">from</span> cai.patterns <span style="color:#f92672">import</span> HierarchicalPattern
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>pentest_manager <span style="color:#f92672">=</span> Agent(
</span></span><span style="display:flex;"><span>    name<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;PentestManager&#34;</span>,
</span></span><span style="display:flex;"><span>    model<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;claude-opus-4-7&#34;</span>,  <span style="color:#75715e"># stronger model for orchestration</span>
</span></span><span style="display:flex;"><span>    instructions<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;Decompose the pentest objective into phases. Coordinate specialists.&#34;</span>,
</span></span><span style="display:flex;"><span>    handoffs<span style="color:#f92672">=</span>[recon_agent, web_agent, exploitation_agent, reporting_agent],
</span></span><span style="display:flex;"><span>)
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>engagement <span style="color:#f92672">=</span> HierarchicalPattern(manager<span style="color:#f92672">=</span>pentest_manager)
</span></span><span style="display:flex;"><span>result <span style="color:#f92672">=</span> engagement<span style="color:#f92672">.</span>run(objective<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;Full pentest of 10.10.10.10&#34;</span>, scope<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;10.10.10.0/24&#34;</span>)
</span></span></code></pre></div><p><strong>Chain-of-Thought coordination</strong> passes enriched context sequentially through a pipeline. Output from each agent becomes the input to the next. Use this for workflows where each phase depends entirely on the previous one&rsquo;s findings:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-python" data-lang="python"><span style="display:flex;"><span><span style="color:#f92672">from</span> cai.patterns <span style="color:#f92672">import</span> ChainPattern
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>pentest_chain <span style="color:#f92672">=</span> ChainPattern(
</span></span><span style="display:flex;"><span>    stages<span style="color:#f92672">=</span>[recon_agent, vuln_agent, exploitation_agent, post_exploit_agent, reporting_agent]
</span></span><span style="display:flex;"><span>)
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>result <span style="color:#f92672">=</span> pentest_chain<span style="color:#f92672">.</span>run(target<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;10.10.10.10&#34;</span>)
</span></span></code></pre></div><p>For CTF scenarios where challenges are independent, swarm wins. For red team engagements with clear kill-chain phases, hierarchical with a strong orchestrator model delivers better results. Chain is best for bug bounty automation where the pipeline is fixed and sequential.</p>
<h2 id="cais-ctf-track-record-benchmark-results-that-matter">CAI&rsquo;s CTF Track Record: Benchmark Results That Matter</h2>
<p>CTF performance is the most rigorous available benchmark for offensive security AI — challenges are designed by experts, scored objectively, and run against identical conditions for all participants. CAI&rsquo;s 2025 results are the most compelling case for production adoption of any open-source security framework.</p>
<p>The headline numbers tell a clear story. At Neurogrid CTF 2025, CAI captured 41 of 45 available flags and took the $50,000 first prize — outright, not in a separate &ldquo;AI track.&rdquo; At Cyber Apocalypse CTF 2025, competing against 8,129 teams from the global security community, CAI outperformed 99.7% of teams at peak performance while only running for 3 of the 72 available hours. Had it competed for the full duration, the gap would have been larger. Across all tracked CTF events in 2025, CAI maintained a 99.04% mean percentile ranking.</p>
<p>The Dragos ICS/OT CTF is particularly significant because OT security is considered one of the hardest domains — specialized protocols, legacy systems, limited public tooling. CAI achieved Rank 1 during the competition&rsquo;s peak hours (7-8), completing 32 of 34 challenges with a 37% velocity advantage over top human teams. This is not a domain where general-purpose AI typically excels.</p>
<p>Speed benchmarks reveal the magnitude of the capability gap. CAI solves specific challenge types up to 3,600 times faster than human competitors, with an average of 11x faster across all challenge categories. CAI v0.6.0 beats the next-best AI agent framework by 2.6x in Attack &amp; Defense CTF formats — the most operationally realistic competition format.</p>
<p>The practical implication for security teams: CAI&rsquo;s CTF performance correlates with real-world pentest efficiency. The same multi-agent coordination, tool chains, and reasoning patterns that flag CTF challenges also identify vulnerabilities in production systems.</p>
<h2 id="cai-vs-pentestgpt-vs-pentagi-choosing-the-right-framework">CAI vs. PentestGPT vs. PentAGI: Choosing the Right Framework</h2>
<p>Three open-source AI security frameworks dominate the 2026 landscape. Choosing between them requires honest evaluation of your team&rsquo;s constraints and use case requirements.</p>
<table>
  <thead>
      <tr>
          <th>Framework</th>
          <th>CTF Performance</th>
          <th>Real-World Testing</th>
          <th>Model Support</th>
          <th>Air-Gap Capable</th>
          <th>Active Maintenance</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td><strong>CAI</strong></td>
          <td>99.04% mean percentile</td>
          <td>High (vulnbank.org SQL injection, multiple CVEs)</td>
          <td>300+ models</td>
          <td>Yes (Ollama)</td>
          <td>Active (Alias Robotics)</td>
      </tr>
      <tr>
          <td><strong>PentestGPT</strong></td>
          <td>Limited benchmarks</td>
          <td>Moderate</td>
          <td>OpenAI/Anthropic</td>
          <td>No</td>
          <td>Moderate</td>
      </tr>
      <tr>
          <td><strong>PentAGI</strong></td>
          <td>Competitive</td>
          <td>Moderate</td>
          <td>OpenAI primary</td>
          <td>No</td>
          <td>Active</td>
      </tr>
      <tr>
          <td><strong>hackingBuddyGPT</strong></td>
          <td>Research-focused</td>
          <td>Limited</td>
          <td>OpenAI</td>
          <td>No</td>
          <td>Research cadence</td>
      </tr>
  </tbody>
</table>
<p><strong>CAI</strong> is the right choice when you need production-grade performance, multi-agent orchestration, air-gapped deployment, or broad model flexibility. The HITL and guardrail system is the most mature of the three. The trade-off: steeper learning curve due to richer architecture.</p>
<p><strong>PentestGPT</strong> is optimized for web application testing with a simpler mental model. Good for teams that primarily do bug bounty web testing and want to start quickly without learning CAI&rsquo;s 8-pillar architecture. Limited multi-agent support.</p>
<p><strong>PentAGI</strong> takes a more general-purpose agent approach and performs well on structured pentest methodologies. Better documentation for teams new to AI-assisted pentesting. Less proven in competitive benchmarks.</p>
<p>For security teams doing serious offensive work — red team engagements, bug bounty at scale, CTF competition, OT security assessment — CAI is the only production-ready option. For teams exploring AI-assisted pentesting for the first time, PentestGPT offers a lower entry point.</p>
<h2 id="responsible-use-legal-boundaries-and-ethical-considerations">Responsible Use, Legal Boundaries, and Ethical Considerations</h2>
<p>AI-powered offensive security tools amplify capability dramatically — which means the legal and ethical requirements that apply to traditional pentesting apply with greater force to CAI-based automation. The framework will do what you tell it to, faster and more thoroughly than most human teams. This is the point. It also means the consequences of misuse are proportionally larger.</p>
<p>The fundamental legal requirement is unchanged: you must have written authorization before scanning or testing any system you do not own. CAI&rsquo;s speed and automation mean an accidental misconfiguration of the <code>allowed_targets</code> scope can cause more unintended scanning in 60 seconds than a careless human could cause in an hour. Configure the <code>ScopeGuardrail</code> before running any agent in a non-local environment, and treat the scope configuration as a critical security control, not an optional feature.</p>
<p>Responsible disclosure applies to AI-discovered vulnerabilities. If you run CAI in bug bounty mode and it finds a critical vulnerability, the same responsible disclosure obligations that apply to human-discovered bugs apply to AI-discovered ones. CAI does not change the ethics of vulnerability research — it changes the speed at which you accumulate findings that require responsible handling.</p>
<p>For red team engagements, establish HITL checkpoints at every phase boundary. The HITL system exists precisely for this: a human should confirm that the engagement is proceeding within scope and intent before moving from recon to exploitation, and again before post-exploitation activities. CAI&rsquo;s configurability makes this easy to enforce; skipping it to save time is a professional risk management failure.</p>
<p>Air-gapped deployments via Ollama address a real enterprise concern: many security teams cannot send target data and scan results to external model providers due to data handling policies. CAI&rsquo;s multi-model support makes local deployment practical — modern 70B models running on on-premises hardware deliver sufficient reasoning quality for most pentest automation tasks.</p>
<h2 id="frequently-asked-questions">Frequently Asked Questions</h2>
<p><strong>Is CAI legal to use for security testing?</strong>
CAI is legal to use when you have explicit written authorization to test the target system. Like all offensive security tools, using CAI against systems you do not have permission to test is illegal under the Computer Fraud and Abuse Act (US), Computer Misuse Act (UK), and equivalent laws in most jurisdictions. The framework includes scope guardrails specifically to help prevent accidental out-of-scope testing.</p>
<p><strong>What hardware do I need to run CAI locally without a cloud API?</strong>
For local deployment via Ollama, you need a machine with at least 16GB RAM for smaller models (7B-13B parameter range) or 40GB+ VRAM for larger models (70B) that deliver production-quality reasoning. An NVIDIA GPU with CUDA support significantly improves inference speed. For cloud-connected deployments, standard developer hardware is sufficient — CAI itself is lightweight.</p>
<p><strong>How does CAI handle credential management?</strong>
CAI does not store credentials in plaintext logs or agent context by default. The framework&rsquo;s output filtering guardrail can be configured to redact credential patterns before they appear in traces or reports. For production use, integrate with your secrets management system (HashiCorp Vault, AWS Secrets Manager) and inject credentials via environment variables rather than passing them in agent instructions.</p>
<p><strong>Can CAI be used for bug bounty automation?</strong>
Yes — this is one of CAI&rsquo;s primary production use cases. The multi-agent handoff system is well-suited to the recon → vulnerability identification → proof-of-concept → reporting pipeline that bug bounty work requires. Configure strict scope guardrails matching the program&rsquo;s in-scope definition, enable HITL before any exploitation step, and use the built-in reporting tools to generate disclosure-ready vulnerability reports.</p>
<p><strong>What models work best for different CAI agent roles?</strong>
The community consensus in 2026: use Claude Opus 4.7 or GPT-4o for orchestrator/manager agents where complex reasoning and multi-step planning are needed. Use Claude Sonnet 4.6 for specialist agents (recon, exploitation, post-exploit) where speed and tool use efficiency matter more than deep reasoning. For air-gapped deployments, Qwen2.5-72B via Ollama performs comparably to GPT-4o for most security automation tasks.</p>
]]></content:encoded></item></channel></rss>