<?xml version="1.0" encoding="utf-8" standalone="yes"?><rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom" xmlns:content="http://purl.org/rss/1.0/modules/content/"><channel><title>Image Conversion on RockB</title><link>https://baeseokjae.github.io/tags/image-conversion/</link><description>Recent content in Image Conversion on RockB</description><image><title>RockB</title><url>https://baeseokjae.github.io/images/og-default.png</url><link>https://baeseokjae.github.io/images/og-default.png</link></image><generator>Hugo</generator><language>en-us</language><lastBuildDate>Sun, 06 Sep 2026 16:02:14 +0000</lastBuildDate><atom:link href="https://baeseokjae.github.io/tags/image-conversion/index.xml" rel="self" type="application/rss+xml"/><item><title>Vecline: Bit-Exact SVG and Document Toolkit for Measurable AI Image Output</title><link>https://baeseokjae.github.io/posts/vecline-bit-exact-svg-ai-image-toolkit-2026/</link><pubDate>Sun, 06 Sep 2026 16:02:14 +0000</pubDate><guid>https://baeseokjae.github.io/posts/vecline-bit-exact-svg-ai-image-toolkit-2026/</guid><description>Vecline is a bit-exact SVG and document toolkit that turns logos and icons into lossless SVG (SSIM 1.0) and measures its own output with real metrics.</description><content:encoded><![CDATA[<p>Vecline is a bit-exact SVG and document toolkit that converts flat art — logos, icons, UI, screenshots, pixel art — into editable SVG with perfectly measured output: SSIM 1.0000, PSNR infinity, zero differing pixels. Unlike most vectorizers that claim close-enough accuracy, Vecline re-renders every SVG it produces and scores it against the source, and its lossless mode fails rather than returning a near-miss. Built for JavaScript as a zero-dependency core with an MCP server, it is the honest answer to overhyped &ldquo;100% accuracy&rdquo; vectorizers.</p>
<h2 id="what-is-vecline-and-why-measured-not-asserted-matters">What Is Vecline and Why &ldquo;Measured, Not Asserted&rdquo; Matters</h2>
<p>When you vectorize an image, almost every tool on the market leans on a single, unverifiable promise: &ldquo;trust us, it&rsquo;s accurate.&rdquo; Weirdly, in the age of AI that produces images by the thousands — whether you are <a href="https://baeseokjae.github.io/posts/best-ai-image-generators-2026/">choosing a generator</a> or building a <a href="https://baeseokjae.github.io/posts/comfyui-workflow-ai-image-generation-guide-2026/">ComfyUI pipeline</a> — most raster-to-SVG conversion remains an act of faith. You run potrace or vtracer, eyeball the result, and hope the curves look fine at print size.</p>
<p>Vecline is built on a different assumption. Instead of claiming accuracy, it <em>measures</em> it. The vectorizer renders the SVG it just produced back to pixels, then compares that render against the input using structural similarity (SSIM), peak signal-to-noise ratio (PSNR), and the CIEDE2000 colour-difference metric. If the result is bit-exact, the tool says so and you can see the numbers. If it is not, it tells you what it actually achieved.</p>
<p>This distinction is the whole product thesis. Most vectorizers guarantee their <em>method</em> — &ldquo;we trace with Bézier curves.&rdquo; Vecline guarantees an <em>outcome</em> that it can prove. That is a meaningful difference when you are feeding generated images from an AI model into a design pipeline where one dropped pixel of a logo&rsquo;s edge becomes a visible artifact at 300 DPI.</p>
<h2 id="the-bit-exact-promise-ssim-10000-and-the-five-conversion-modes">The Bit-Exact Promise: SSIM 1.0000 and the Five Conversion Modes</h2>
<p>Vecline ships four conversion modes, plus a fifth behaviour for photographs, and each one is enforced by measurement rather than construction.</p>
<p><strong><code>auto</code> (default).</strong> Vecline inspects the artwork, guesses what strategy preserves the most information, and applies it. For flat graphics it does the exact work; for photos it falls back to embedding.</p>
<p><strong><code>lossless</code>.</strong> This is the mode that separates Vecline from everything else. It returns bit-exact SVG or it <em>errors out</em>. There is no silent near-miss. On a real logo, lossless mode returns a 24.0 KB SVG at SSIM 1.0000 — about a third of what imagetracerjs (64 KB) and vtracer (60 KB) spend while <em>only approximating</em> the same image. You are not trading quality for size here; you are getting a smaller file that is literally pixel-identical.</p>
<p><strong><code>pixel</code>.</strong> Produces genuine editable geometry — real rectangle and path shapes, one per pixel run — that rasterises back to the input with zero differing pixels. This is not &ldquo;visually identical&rdquo;; it is bit-exact, and it is editable, unlike a flat bitmap inside an SVG.</p>
<p><strong><code>trace</code>.</strong> The Bézier mode, used for photographs and organic shapes. This is approximate by nature and Vecline says so plainly. It reports the measured SSIM/PSNR after tracing, so you always know exactly how close the approximation landed.</p>
<p><strong><code>embed</code>.</strong> For photographs where vector geometry would be absurdly large, Vecline puts the real bitmap inside the SVG. This is bit-exact by construction, and it reports the size ratio so you understand the cost.</p>
<p>A single fact explains why photographs can never be auto-vectorised to exact geometry: a photograph holds more independent information than any compact set of Bézier curves can encode. Vecline documents this honestly rather than pretending otherwise. If you force it, it can emit a photo as exact geometry — but a 320x240 photo becomes a 1.99 MB file of 42,933 shapes, which is why auto mode uses embed and tells you the ratio instead.</p>
<h2 id="how-it-stacks-up-against-potrace-imagetracerjs-and-vtracer">How It Stacks Up Against potrace, imagetracerjs, and vtracer</h2>
<p>The best evidence is the official comparison harness at vecline.xyz/compare.html. Crucially, every tool&rsquo;s SVG is rendered with the <em>same</em> renderer and scored with the <em>same</em> metrics on the same white ground — so the comparison is apples to apples, not &ldquo;our renderer vs our renderer.&rdquo;</p>
<p><strong>On flat art, Vecline is the only bit-exact contender.</strong> potrace, imagetracerjs, and vtracer all approximate curved edges and antialiased pixels. Vecline lossless mode reaches SSIM 1.0000 where the others top out &ldquo;visually identical.&rdquo; For logos, icons, favicons, and UI assets, that is the difference between shipping something exact and shipping something close.</p>
<p><strong>On photographs, Vecline leads SSIM across the board.</strong> In the Kodak set at 480px, auto mode scored 0.9140 / 0.9453 / 0.8460 against vtracer&rsquo;s 0.7652 / 0.7624 / 0.7949 and imagetracerjs&rsquo;s 0.7093 / 0.7465 / 0.7615. PSNR tells the same story: 34.8 / 36.3 / 31.2 dB versus vtracer&rsquo;s 25.9 / 24.3 / 23.2. That is a materially better perceptual result on real photos.</p>
<table>
  <thead>
      <tr>
          <th>Tool</th>
          <th>Flat-art exactness</th>
          <th>Photo SSIM (best)</th>
          <th>Photo PSNR (dB)</th>
          <th>Small-logo speed</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>Vecline (auto/lossless)</td>
          <td><strong>Bit-exact (SSIM 1.0)</strong></td>
          <td><strong>0.9453</strong></td>
          <td><strong>36.3</strong></td>
          <td>280 ms</td>
      </tr>
      <tr>
          <td>vtracer (Rust/OpenCV)</td>
          <td>Approximate</td>
          <td>0.7652</td>
          <td>25.9</td>
          <td><strong>69 ms</strong></td>
      </tr>
      <tr>
          <td>imagetracerjs</td>
          <td>Approximate</td>
          <td>0.7093</td>
          <td>23.2</td>
          <td>~180 ms</td>
      </tr>
      <tr>
          <td>potrace</td>
          <td>Approximate</td>
          <td>N/A (binary)</td>
          <td>N/A</td>
          <td>—</td>
      </tr>
  </tbody>
</table>
<p><strong>Now the honest parts where Vecline is not the winner.</strong> The maintainer publishes these trade-offs openly, and you should know them before adopting it:</p>
<ul>
<li>Vecline is slower than imagetracerjs on every fixture — 1.10x to 1.55x.</li>
<li>On a small logo it is 4.08x slower than vtracer (280 ms vs 69 ms), largely due to Node module load overhead on small inputs.</li>
<li>But on the two heavier photographs, Vecline beats vtracer in wall-clock time (portrait: 1001 ms vs 1386 ms), because the Node startup cost amortises away on bigger workloads.</li>
</ul>
<p>So the honest rule of thumb: if you need raw speed on thousands of tiny icons, vtracer holds the crown. If you need any degree of exactness, or high-quality photo output, Vecline wins where it counts — in the output, not just the timer.</p>
<h2 id="a-full-document-toolkit-not-just-a-vectorizer">A Full Document Toolkit, Not Just a Vectorizer</h2>
<p>What makes Vecline more than a one-trick SVG tool is the breadth of the pipeline around it. The package reads and writes an 11x11 = 121-format conversion matrix — every format it reads, it writes: PNG, JPEG, WebP, AVIF, TIFF, GIF, BMP, ICO/CUR, PNM, TGA, and SVG — all run in CI on every commit so the claim never rots.</p>
<p>The measurement discipline extends to geometry. A plain disc renders as a real <code>&lt;circle&gt;</code> rather than a path — 68% smaller — and a four-slice pie chart goes from 2,323 bytes of Bézier curves down to 488 bytes of genuine arc shapes. That matters for CAD and DXF because real arcs and circles stay editable and true, rather than being flattened into hundreds of curve segments. Gradients get the same treatment: a synthetic vignette reconstructs as a radial SVG gradient at 0.998 SSIM in about 350 bytes, where flat bands would take 3–8 KB and score only 0.73–0.88.</p>
<p>For makers, Vecline Studio exports DXF with real physical cut size, alongside EPS, PDF, G-code, colour separations, sprite sheets, favicons, and BlurHash. That turns it into a real raster-to-fabrication pipeline — from a screenshot to a laser cut or CNC path — not just a developer utility. It also converts PDFs (via mupdf-WASM) and Office documents (via <code>vecline serve</code>, a local LibreOffice bridge), and <code>doc_to_images</code> and <code>images_to_pdf</code> round out the document side.</p>
<h2 id="vecline-for-ai-agents-an-mcp-server-that-lets-models-verify-their-own-output">Vecline for AI Agents: An MCP Server That Lets Models Verify Their Own Output</h2>
<p>The most interesting angle for anyone building on LLMs is the MCP server. <code>vecline mcp</code> is officially registered under <code>io.github.shunyagatha/vecline</code> on the Model Context Protocol registry and on Smithery, and it exposes twelve tools: <code>vectorize</code>, <code>convert</code>, <code>centerline</code>, <code>measure</code>, <code>diff</code>, <code>crop</code>, <code>doc_to_images</code>, <code>office_convert</code>, <code>images_to_pdf</code>, <code>palette</code>, <code>placeholder</code>, and <code>image_info</code>.</p>
<p>The two tools that matter for AI agents are <code>measure</code> and <code>diff</code>. Both work by rendering the result back and computing real SSIM / PSNR / CIEDE2000. That means an agent — Claude, Codex, VS Code, Cursor — can call <code>vectorize</code>, then call <code>measure</code> or <code>diff</code> on its own output and <em>verify numerically</em> that the conversion actually succeeded instead of asserting it. That is a genuinely rare capability: most image tools return success because the function returned, not because the output is correct.</p>
<p>All twelve tools run with <code>openWorldHint: false</code> — nothing touches the network. Three are read-only and eight are destructive, and the destructive ones are annotated. There is even a one-click <code>.mcpb</code> launcher. For a team worried about a model silently corrupting an image asset, this is the closest thing to a self-checking loop in the ecosystem.</p>
<p>This dovetails with the wider theme of reliable tool use in agentic pipelines, the same &ldquo;measure, don&rsquo;t assume&rdquo; philosophy that applies to <a href="https://baeseokjae.github.io/posts/flux-1-image-generation-developer-api-guide-2026/">generating images with open models like FLUX.1</a>. Just as MCP itself has become the connective tissue of agent workflows, a tool that lets the model verify geometry numerically fits squarely in that camp.</p>
<h2 id="vecline-studio-free-private-in-browser-and-offline">Vecline Studio: Free, Private, In-Browser and Offline</h2>
<p>Vecline Studio (vecline.xyz) is the free, private, in-browser companion. Everything runs inside the tab in a Web Worker — there is no backend, no upload, no account. It works offline once cached. It measures its own output the same way the CLI does, rendering the SVG it produced and reporting real SSIM/PSNR/CIEDE2000 alongside a bit-exact badge. It even handles TGA/PNM/ICO formats that browsers cannot natively read, so the format breadth survives in the browser too.</p>
<p>The privacy model is worth calling out because cloud vectorizers are the norm. Competitors that host the conversion upload your artwork to their servers, often charging per image and retaining whatever they process. Vecline&rsquo;s Studio keeps the entire operation local-first: no upload means nothing can be retained, leaked, or used to train anything. That is a real contract for teams working with confidential UI or client logos.</p>
<h2 id="privacy-licensing-and-the-honest-engineering-culture">Privacy, Licensing, and the Honest Engineering Culture</h2>
<p>Vecline collects nothing — no server, no analytics, no telemetry, no account. Every conversion, tracing, measurement, crop, PDF, and Office render runs on your own machine. The core <code>vecline/core</code> engine has zero dependencies and no Node built-ins, and CI asserts it bundles for a browser at about 96 KB minified, with a size tripwire so it cannot creep up silently.</p>
<p>The npm package <code>vecline</code> is v2.1.4, MIT-licensed, Node.js &gt;= 18.17, with a <code>vecline</code> binary. Vecline Studio is source-available rather than open source, so be clear-eyed there: the CLI and core are MIT; the studio app is not FOSS in the strict sense.</p>
<p>The honest engineering culture is arguably the most distinctive thing about the project. The author publishes measured trade-offs rather than burying them, calls out vtracer&rsquo;s genuinely better small-logo speed and potrace&rsquo;s smaller output, and has even corrected earlier false claims in the changelog. In a tooling market full of inflated benchmarks, that willingness to publish where you lose is rare and worth rewarding.</p>
<h2 id="limitations-and-caveats-you-should-know-before-adopting-it">Limitations and Caveats You Should Know Before Adopting It</h2>
<p>Be honest about what Vecline is <em>not</em> before you bet a pipeline on it.</p>
<ul>
<li><strong>Photographs cannot be bit-exact in vector form.</strong> The information-theoretic limit is real. If you need lossless photo <em>vectors</em>, you are really asking for embedded bitmaps, not geometry. Vecline will tell you the truth; make sure your expectations match.</li>
<li><strong>Speed.</strong> The core is slower than imagetracerjs on every fixture, and 4x slower than vtracer on small logos due to Node startup. If you are batch-converting tens of thousands of 16px icons, that overhead compounds and vtracer may be the pragmatic choice.</li>
<li><strong>License asymmetry.</strong> The CLI and core are MIT, but Vecline Studio is only source-available. If your company mandates strict FOSS, verify which component you are allowed to redistribute or extend.</li>
<li><strong>Brittle edges in exotic formats.</strong> The 121-format matrix is impressive, but the CI coverage is about format <em>round-trips</em>, not about matching what Adobe Photoshop writes for every possible TIFF flag. Treat obscure-feature edge cases as verify-before-trust territory.</li>
<li><strong>DXF at physical size is a feature, not a guarantee.</strong> It computes a real cut size, but that still assumes your machine setup matches; double-check units before you hit run on a laser cutter.</li>
</ul>
<h2 id="verdict-is-bit-exact-svg-the-2026-standard-for-ai-image-output">Verdict: Is Bit-Exact SVG the 2026 Standard for AI Image Output?</h2>
<p>I believe yes, or at least it should be. When AI models now generate thousands of images per hour, &ldquo;looks approximately right&rdquo; is no longer an acceptable default for anything that will be printed, exported, fabricated, or reused as an asset. Measured, falsifiable output — with SSIM and PSNR numbers you can read on a badge — turns vectorization from a subjective craft into an objective operation you can put in a CI pipeline and sleep on.</p>
<p>Vecline is not the fastest tool in every corner, and it does not pretend to be. It is the tool that makes exact output the <em>default guarantee</em> for anything flat, gives you honest measurements when exactness is impossible, and — via the MCP server — lets an AI agent check its own work numerically. For a developer feeding generated images into a design-to-fabrication pipeline, that combination of verified quality, local-first privacy, and self-measuring agents is a genuinely stronger default than the &ldquo;trust us&rdquo; vectorizers.</p>
<h2 id="frequently-asked-questions">Frequently Asked Questions</h2>
<p><strong>What is Vecline?</strong>
Vecline is an MIT-licensed JavaScript toolkit that converts rasters (PNG, JPEG, WebP, AVIF, TIFF, GIF, BMP, ICO, PNM, TGA) into SVG and measures its own output. On flat art it produces bit-exact SVG — SSIM 1.0000, PSNR infinity, zero differing pixels — by rendering the result back and comparing it to the source.</p>
<p><strong>How is bit-exact different from &ldquo;visually identical&rdquo;?</strong> Bit-exact means every output pixel equals the input with zero differing pixels, verified numerically, not judged by eye. Most vectorizers (potrace, imagetracerjs, vtracer) approximate curves and antialiasing; Vecline&rsquo;s lossless and pixel modes reach literal pixel-perfect output and fail rather than deliver a near-miss.</p>
<p><strong>Which Vecline mode should I use?</strong> Start with <code>auto</code> for mixed workloads. Use <code>lossless</code> when you need guaranteed bit-exact output on flat art (it errors out if it cannot achieve it), <code>pixel</code> for editable bit-exact geometry, <code>trace</code> for photographs where you accept an approximation, and <code>embed</code> when a photo&rsquo;s exact geometry would be impractically large.</p>
<p><strong>Is Vecline faster than vtracer?</strong> No. Vecline is 4.08x slower than vtracer on small logos (280 ms vs 69 ms, mostly Node startup) and 1.10x–1.55x slower than imagetracerjs. But on heavier photographs it wins in wall-clock time (1001 ms vs 1386 ms). Choose vtracer for bulk tiny-icon speed; choose Vecline for exactness and photo quality.</p>
<p><strong>Can Vecline vectorise photographs bit-exactly?</strong> No, and it is upfront about it: a photograph contains more independent information than Bézier curves can compactly encode. It can emit a photo as exact geometry but that costs one rectangle per pixel (a 320x240 photo becomes a 1.99 MB file of 42,933 shapes), so auto mode embeds the bitmap and reports the size ratio instead.</p>
]]></content:encoded></item></channel></rss>