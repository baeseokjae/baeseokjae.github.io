<?xml version="1.0" encoding="utf-8" standalone="yes"?><rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom" xmlns:content="http://purl.org/rss/1.0/modules/content/"><channel><title>MicroVM on RockB</title><link>https://baeseokjae.github.io/tags/microvm/</link><description>Recent content in MicroVM on RockB</description><image><title>RockB</title><url>https://baeseokjae.github.io/images/og-default.png</url><link>https://baeseokjae.github.io/images/og-default.png</link></image><generator>Hugo</generator><language>en-us</language><lastBuildDate>Thu, 30 Jul 2026 10:02:01 +0000</lastBuildDate><atom:link href="https://baeseokjae.github.io/tags/microvm/index.xml" rel="self" type="application/rss+xml"/><item><title>CubeSandbox: Tencent Cloud's Secure Sandbox Runtime for AI Agents — Full Review</title><link>https://baeseokjae.github.io/posts/cubesandbox-tencent-sandbox-ai-agents-2026/</link><pubDate>Thu, 30 Jul 2026 10:02:01 +0000</pubDate><guid>https://baeseokjae.github.io/posts/cubesandbox-tencent-sandbox-ai-agents-2026/</guid><description>CubeSandbox is Tencent Cloud&amp;#39;s open-source microVM runtime for AI agents with sub-60ms cold starts, hardware isolation, and E2B SDK compatibility.</description><content:encoded><![CDATA[<h2 id="what-is-cubesandbox">What is CubeSandbox?</h2>
<p>CubeSandbox is an open-source, hardware-isolated sandbox runtime purpose-built for AI agents. Developed by Tencent Cloud and released under the Apache 2.0 license in April 2026, it provides a secure execution environment for LLM-generated code, agent tool calls, and reinforcement learning training workloads. Built on RustVMM and KVM, each sandbox instance boots a dedicated Guest OS kernel in under 60 milliseconds with less than 5 MB of memory overhead, offering hardware-level isolation that container-based approaches cannot match. CubeSandbox is fully compatible with the E2B SDK, meaning developers can switch from E2B to CubeSandbox by changing a single environment variable with zero code modifications.</p>
<h2 id="why-ai-agents-need-a-new-kind-of-sandbox">Why AI Agents Need a New Kind of Sandbox</h2>
<p>AI agents today execute code, run shell commands, browse the web, and interact with APIs on behalf of users. Every one of these actions is a potential attack surface. When an LLM generates and executes code, that code may contain unintended side effects — file system access, network calls, or resource exhaustion. Traditional sandboxing approaches like Docker containers share a host kernel, making them vulnerable to container escape exploits. CubeSandbox addresses this by providing a dedicated microVM per sandbox instance, ensuring that even if an agent&rsquo;s code is compromised, the host system and other sandboxes remain completely isolated.</p>
<h3 id="the-security-problem-with-llm-generated-code">The Security Problem with LLM-Generated Code</h3>
<p>LLM-generated code is inherently unpredictable. Models can hallucinate API calls, generate infinite loops, or produce code that attempts to access restricted resources. In production agent systems, this code runs autonomously — often without human review for every execution. A single malicious or buggy code execution in a shared environment can compromise an entire agent fleet. CubeSandbox&rsquo;s hardware-level isolation means each agent execution gets its own dedicated kernel, its own virtualized hardware, and its own network stack. If an agent&rsquo;s code crashes or attempts an exploit, it affects only that single sandbox instance.</p>
<h3 id="docker-vs-vm-vs-microvm--the-isolation-spectrum">Docker vs VM vs MicroVM — The Isolation Spectrum</h3>
<table>
	<thead>
			<tr>
					<th>Technology</th>
					<th>Isolation Level</th>
					<th>Boot Time</th>
					<th>Memory Overhead</th>
					<th>Kernel Sharing</th>
					<th>Escape Risk</th>
			</tr>
	</thead>
	<tbody>
			<tr>
					<td>Docker / Containers</td>
					<td>OS-level (namespaces)</td>
					<td>Instant</td>
					<td>~1-5 MB</td>
					<td>Shared host kernel</td>
					<td>High (CVEs like CVE-2024-21626)</td>
			</tr>
			<tr>
					<td>Traditional VMs (QEMU/KVM)</td>
					<td>Hardware-level</td>
					<td>30-60 seconds</td>
					<td>128 MB+</td>
					<td>Dedicated kernel per VM</td>
					<td>Very Low</td>
			</tr>
			<tr>
					<td>MicroVM (Firecracker)</td>
					<td>Hardware-level</td>
					<td>125-200 ms</td>
					<td>~5 MB</td>
					<td>Dedicated kernel per VM</td>
					<td>Very Low</td>
			</tr>
			<tr>
					<td><strong>CubeSandbox (RustVMM)</strong></td>
					<td><strong>Hardware-level</strong></td>
					<td><strong>&lt;60 ms</strong></td>
					<td><strong>&lt;5 MB</strong></td>
					<td><strong>Dedicated kernel per VM</strong></td>
					<td><strong>Very Low</strong></td>
			</tr>
	</tbody>
</table>
<p>CubeSandbox occupies a unique position: it delivers the isolation of a full virtual machine with the startup speed and memory footprint of a container. This combination is critical for AI agent workloads where sandboxes are created and destroyed at high frequency — sometimes hundreds of thousands per minute.</p>
<h2 id="architecture-deep-dive">Architecture Deep Dive</h2>
<p>CubeSandbox&rsquo;s architecture is divided into a control plane and a data plane, with several specialized components handling networking, security, and state management.</p>
<h3 id="control-plane-cubeapi-cubemaster-and-webui">Control Plane: CubeAPI, CubeMaster, and WebUI</h3>
<p>The control plane manages sandbox lifecycle, scheduling, and user interactions. <strong>CubeAPI</strong> is the RESTful API layer that accepts sandbox creation, execution, and management requests. <strong>CubeMaster</strong> is the central scheduler that assigns sandbox instances to available nodes, manages capacity, and handles failover. The <strong>WebUI Dashboard</strong> provides a graphical interface for monitoring active sandboxes, viewing logs, managing templates, and configuring egress rules. This three-tier control plane architecture mirrors Kubernetes&rsquo; design philosophy, making CubeSandbox familiar to DevOps teams already comfortable with cluster management.</p>
<h3 id="data-plane-cubelet-cubehypervisor-and-cubeshim">Data Plane: Cubelet, CubeHypervisor, and CubeShim</h3>
<p>On each host node, the data plane components handle the actual sandbox lifecycle. <strong>Cubelet</strong> is the node agent that communicates with CubeMaster, similar to kubelet in Kubernetes. <strong>CubeHypervisor</strong> manages the RustVMM-based microVM instances, handling boot, pause, resume, and teardown. <strong>CubeShim</strong> provides the OCI-compatible interface that allows CubeSandbox to work with standard container tooling and images. This design means CubeSandbox can use OCI container images as templates, converting them into bootable microVM snapshots.</p>
<h3 id="network-cubevs-ebpf-virtual-switch">Network: CubeVS eBPF Virtual Switch</h3>
<p>CubeSandbox&rsquo;s networking layer is built on <strong>CubeVS</strong>, an eBPF-based virtual switch that provides kernel-level network isolation. CubeVS uses three distinct eBPF programs to enforce network policies: one for ingress filtering, one for egress control, and one for inter-sandbox isolation. This approach achieves near-native network performance while maintaining strict isolation between sandboxes. Egress control rules can be configured per sandbox or per template, allowing administrators to whitelist specific domains or IP ranges that an agent is allowed to contact.</p>
<h3 id="security-cubeegress-credential-vault-and-audit">Security: CubeEgress, Credential Vault, and Audit</h3>
<p>Security is a first-class concern in CubeSandbox. <strong>CubeEgress</strong> enforces outbound network policies at the hypervisor level, preventing agents from exfiltrating data to unauthorized destinations. The <strong>Credential Vault</strong> stores API keys, tokens, and secrets outside the sandbox — secrets never enter the sandbox&rsquo;s environment variables, filesystem, or process space. Instead, the vault injects credentials directly into authorized API calls at the proxy level. The <strong>Audit Trail</strong> system records every action within a sandbox session, including which model was called, what code was executed, and the token usage, providing end-to-end accountability for enterprise compliance requirements.</p>
<h3 id="state-management-cubecow-snapshotclonerollback">State Management: CubeCoW Snapshot/Clone/Rollback</h3>
<p>CubeSandbox uses <strong>CubeCoW</strong>, a copy-on-write layer built on XFS reflink (FICLONE), to manage sandbox state. Snapshots, clones, and rollbacks all complete in hundreds of milliseconds. This enables powerful workflows: an agent can snapshot its state before a risky operation, clone a sandbox to parallelize testing, or roll back to a known good state after a failure. For RL training workloads, this means thousands of agent environments can be cloned from a single base snapshot in seconds, dramatically reducing the overhead of environment setup.</p>
<h2 id="key-features-and-performance">Key Features and Performance</h2>
<h3 id="sub-60ms-cold-start--how-template-snapshots-work">Sub-60ms Cold Start — How Template Snapshots Work</h3>
<p>CubeSandbox achieves its remarkable cold start performance through a three-phase template lifecycle: <strong>Init → Boot &amp; Snapshot → Deploy</strong>. First, a base template is created from an OCI image. The template is booted once, and a clean snapshot of the booted microVM state is saved. When a new sandbox is requested, CubeSandbox clones this pre-booted snapshot rather than booting from scratch. The result is a fully serviceable sandbox in under 60 milliseconds. Under 50 concurrent creations, CubeSandbox averages 67 ms with a P95 of 90 ms and a P99 of 137 ms. These numbers were measured on Tencent Cloud&rsquo;s bare-metal lab infrastructure and represent production-grade performance.</p>
<h3 id="auto-pauseresume-for-zero-idle-cost">Auto-Pause/Resume for Zero Idle Cost</h3>
<p>CubeSandbox automatically pauses sandbox instances when they are idle and resumes them in hundreds of milliseconds when activity resumes. This auto-pause/resume feature eliminates the cost of running idle sandboxes, which is particularly important for agent workloads that may have long pauses between tool calls. A single node can support over 2,000 sandbox instances simultaneously, and the auto-pause mechanism ensures that memory is only consumed by actively running sandboxes.</p>
<h3 id="e2b-sdk-compatibility--drop-in-migration">E2B SDK Compatibility — Drop-in Migration</h3>
<p>One of CubeSandbox&rsquo;s most strategic features is its compatibility with the E2B SDK. Developers using E2B&rsquo;s Code Interpreter SDK can switch to CubeSandbox by changing a single environment variable — no code changes required. This compatibility extends to the sandbox API, file operations, and execution semantics. For teams already invested in the E2B ecosystem, this means they can migrate to CubeSandbox&rsquo;s hardware-level isolation without rewriting their agent code. It also means CubeSandbox immediately benefits from the entire E2B developer tooling ecosystem.</p>
<h3 id="template-system-and-volume-framework">Template System and Volume Framework</h3>
<p>CubeSandbox&rsquo;s template system allows teams to define reusable sandbox environments from OCI container images. Templates can include pre-installed packages, environment configurations, and network policies. The Volume Framework, introduced in v0.6, allows persistent data volumes to be attached to sandbox instances, enabling stateful agent workloads. Template aliases provide a convenient naming mechanism for managing multiple template versions.</p>
<h2 id="deployment-options">Deployment Options</h2>
<p>CubeSandbox supports four deployment modes, ranging from single-node development setups to production Kubernetes clusters.</p>
<h3 id="single-node-bare-metal-setup">Single-Node Bare-Metal Setup</h3>
<p>For development and testing, CubeSandbox can be deployed on a single bare-metal or virtual machine. The setup requires KVM support and a Linux kernel with eBPF capabilities. This mode is ideal for evaluating CubeSandbox, running local agent tests, or small-scale deployments.</p>
<h3 id="multi-node-cluster-deployment">Multi-Node Cluster Deployment</h3>
<p>For production workloads, CubeSandbox supports multi-node cluster deployments with CubeMaster coordinating sandbox scheduling across nodes. The cluster mode provides horizontal scaling, high availability, and load balancing. Nodes can be added or removed without disrupting running sandboxes.</p>
<h3 id="kubernetes-deployment-via-helm">Kubernetes Deployment via Helm</h3>
<p>CubeSandbox v0.6 introduced native Kubernetes deployment via a Helm chart. This allows teams to deploy CubeSandbox on existing Kubernetes clusters, treating sandbox instances as custom resources managed by the Kubernetes control plane. The Helm chart handles CubeAPI, CubeMaster, Cubelet, and supporting services, integrating with existing Kubernetes networking and storage infrastructure.</p>
<h3 id="terraform-on-tencent-cloud">Terraform on Tencent Cloud</h3>
<p>For teams deploying on Tencent Cloud, CubeSandbox provides a Terraform provider that automates the provisioning of the underlying infrastructure — bare-metal instances, networking, and storage. This is the recommended deployment path for production workloads on Tencent Cloud, as it ensures optimal performance by using Tencent&rsquo;s optimized hardware and network configurations.</p>
<h2 id="real-world-use-cases">Real-World Use Cases</h2>
<h3 id="minimax-rl-training-at-10k-concurrency">MiniMax RL Training at 10K+ Concurrency</h3>
<p>MiniMax, a leading AI company, uses CubeSandbox&rsquo;s Agent Runtime for reinforcement learning training at massive scale. According to Tencent Cloud, MiniMax runs RL training workloads with over 10,000 concurrent sandbox instances and million-level throughput, achieving a &ldquo;zero-incident&rdquo; security record. For RL training, each training step requires thousands of parallel environment executions, and CubeSandbox&rsquo;s sub-60ms cold start and snapshot/clone capabilities make it feasible to create and destroy environments at this scale without becoming a training bottleneck.</p>
<h3 id="swe-bench-and-coding-agent-evaluation">SWE-bench and Coding Agent Evaluation</h3>
<p>CubeSandbox is well-suited for running coding agent benchmarks like SWE-bench, where each test case requires an isolated environment with specific dependencies, file structures, and network configurations. The template system allows each benchmark to define its environment as a template, and the snapshot/rollback feature ensures clean state between test runs. The E2B SDK compatibility means existing SWE-bench runners built on E2B can switch to CubeSandbox with minimal changes.</p>
<h3 id="enterprise-agent-hosting-with-governance">Enterprise Agent Hosting with Governance</h3>
<p>For enterprises deploying AI agents in production, CubeSandbox provides the governance tooling that security teams require. The credential vault ensures that API keys never leak into agent environments. The egress control prevents data exfiltration. The audit trail provides a complete record of every action taken by every agent. Tencent Cloud positions CubeSandbox as an &ldquo;Agent Runtime&rdquo; control plane that separates agent state from compute, allowing enterprises to manage agent fleets with the same rigor they apply to their production infrastructure.</p>
<h2 id="cubesandbox-vs-the-competition">CubeSandbox vs The Competition</h2>
<h3 id="vs-e2b-the-direct-competitor">vs E2B (The Direct Competitor)</h3>
<table>
	<thead>
			<tr>
					<th>Feature</th>
					<th>CubeSandbox</th>
					<th>E2B</th>
			</tr>
	</thead>
	<tbody>
			<tr>
					<td>Isolation Technology</td>
					<td>RustVMM MicroVM</td>
					<td>Firecracker MicroVM</td>
			</tr>
			<tr>
					<td>Cold Start Time</td>
					<td>&lt;60 ms</td>
					<td>~125 ms</td>
			</tr>
			<tr>
					<td>Memory Overhead</td>
					<td>&lt;5 MB</td>
					<td>~5 MB</td>
			</tr>
			<tr>
					<td>License</td>
					<td>Apache 2.0 (Open Source)</td>
					<td>Proprietary</td>
			</tr>
			<tr>
					<td>SDK Compatibility</td>
					<td>E2B SDK compatible</td>
					<td>Native SDK</td>
			</tr>
			<tr>
					<td>Self-Hosting</td>
					<td>Yes (multiple deployment options)</td>
					<td>Limited (E2B Cloud)</td>
			</tr>
			<tr>
					<td>Credential Vault</td>
					<td>Built-in</td>
					<td>Separate service</td>
			</tr>
			<tr>
					<td>Audit Trail</td>
					<td>Built-in</td>
					<td>Available in enterprise plan</td>
			</tr>
			<tr>
					<td>ARM64 Support</td>
					<td>Full native support</td>
					<td>Limited</td>
			</tr>
			<tr>
					<td>Kubernetes Deployment</td>
					<td>Yes (Helm chart)</td>
					<td>No</td>
			</tr>
	</tbody>
</table>
<p>CubeSandbox&rsquo;s open-source nature and self-hosting capabilities give it a significant advantage for enterprises that need to run agent workloads on their own infrastructure. E2B&rsquo;s advantage lies in its mature ecosystem, broader SDK language support, and managed cloud service.</p>
<h3 id="vs-opensandbox-microsandbox-and-daytona">vs OpenSandbox, Microsandbox, and Daytona</h3>
<p>OpenSandbox and Microsandbox are smaller open-source projects that provide container-based sandboxing for AI agents. They lack the hardware-level isolation that CubeSandbox provides, making them suitable for development and testing but potentially insufficient for production security requirements. Daytona is a more established open-source project focused on development environments rather than agent execution, with a different target use case.</p>
<h3 id="vs-firecracker-kata-containers-and-gvisor">vs Firecracker, Kata Containers, and gVisor</h3>
<p>Firecracker is the microVM technology that powers AWS Lambda and Fargate. It provides similar hardware-level isolation but lacks the agent-specific features that CubeSandbox offers — credential vault, egress control, audit trails, and E2B SDK compatibility. Kata Containers provides hardware-virtualized containers but has a heavier footprint and slower startup times. gVisor is a user-space kernel that provides a security layer for containers but does not offer true hardware isolation. CubeSandbox&rsquo;s combination of agent-specific features, sub-60ms startup, and hardware isolation makes it the most complete solution for AI agent workloads.</p>
<h2 id="pros-cons-and-when-to-use-it">Pros, Cons, and When to Use It</h2>
<p><strong>Pros:</strong></p>
<ul>
<li>Hardware-level isolation with sub-60ms cold starts</li>
<li>Open source (Apache 2.0) with no vendor lock-in</li>
<li>E2B SDK compatibility enables zero-friction migration</li>
<li>Comprehensive security features (credential vault, egress control, audit trails)</li>
<li>Multiple deployment options including Kubernetes and Terraform</li>
<li>Full ARM64 support</li>
<li>Active development with monthly releases</li>
</ul>
<p><strong>Cons:</strong></p>
<ul>
<li>Relatively new project (April 2026) — ecosystem still maturing</li>
<li>Performance benchmarks from Tencent Cloud&rsquo;s lab, not independently verified</li>
<li>Requires KVM-capable hardware — not suitable for all environments</li>
<li>Smaller community compared to established projects like Firecracker</li>
<li>Documentation primarily in Chinese for some advanced features</li>
<li>E2B SDK compatibility may lag behind E2B&rsquo;s latest API changes</li>
</ul>
<p><strong>When to use CubeSandbox:</strong></p>
<ul>
<li>You need hardware-level isolation for production AI agent workloads</li>
<li>You are running RL training at scale and need fast environment creation</li>
<li>You want to self-host agent sandboxes on your own infrastructure</li>
<li>You are already using E2B and want to migrate to an open-source alternative</li>
<li>You need enterprise-grade audit and governance for agent execution</li>
</ul>
<h2 id="the-bigger-picture--agent-infrastructure-in-2026">The Bigger Picture — Agent Infrastructure in 2026</h2>
<p>CubeSandbox represents a broader trend in AI infrastructure: the emergence of specialized execution environments for AI agents. Just as Kubernetes became the standard control plane for containerized applications, CubeSandbox and similar projects are vying to become the standard control plane for agentic workloads. The key requirements — hardware isolation, fast startup, state management, and governance — are fundamentally different from both traditional container orchestration and serverless computing.</p>
<p>Tencent Cloud&rsquo;s decision to open-source CubeSandbox under Apache 2.0 is a strategic move to set de facto standards for agent infrastructure. By making the technology freely available and compatible with the existing E2B ecosystem, Tencent is positioning itself at the center of the agent infrastructure market. The company&rsquo;s focus on governance tooling — credential management, audit trails, egress control — reflects the enterprise demand for secure, auditable agent deployments.</p>
<p>The open-source strategy also accelerates adoption. With 10,774 GitHub stars and 979 forks in just three months, CubeSandbox has attracted significant community interest. The monthly release cadence (v0.4 in May, v0.5 in June, v0.6 in July 2026) demonstrates active development and rapid feature iteration.</p>
<h2 id="conclusion-and-recommendations">Conclusion and Recommendations</h2>
<p>CubeSandbox is a compelling solution for teams that need secure, fast, and scalable sandbox environments for AI agents. Its hardware-level isolation, sub-60ms cold starts, and comprehensive security features make it suitable for production agent workloads, particularly in enterprise environments where security and governance are paramount.</p>
<p>For teams currently using E2B, CubeSandbox offers a migration path that requires minimal code changes, making it worth evaluating as an open-source alternative. For teams building new agent infrastructure, CubeSandbox&rsquo;s combination of features — credential vault, egress control, audit trails, template system, and multiple deployment options — provides a complete solution that would otherwise require integrating multiple separate tools.</p>
<p>The main caveat is CubeSandbox&rsquo;s relative youth. The project is only three months old, and while development is active, the ecosystem, documentation, and community support are still maturing. Teams should evaluate CubeSandbox against their specific requirements, particularly around performance, and verify that the benchmarks align with their own infrastructure.</p>
<p>For RL training workloads, enterprise agent hosting, and any scenario requiring hardware-level isolation at scale, CubeSandbox is worth serious consideration. It represents a significant step forward in agent infrastructure and signals the direction the industry is heading.</p>
<h2 id="faq">FAQ</h2>
<h3 id="what-is-cubesandbox-and-how-does-it-work">What is CubeSandbox and how does it work?</h3>
<p>CubeSandbox is an open-source microVM runtime from Tencent Cloud designed specifically for AI agent execution. It uses RustVMM and KVM to create hardware-isolated sandbox environments that boot in under 60 milliseconds. Each sandbox runs its own dedicated Guest OS kernel, providing stronger isolation than containers while maintaining the startup speed and low memory footprint that agent workloads require.</p>
<h3 id="how-does-cubesandbox-compare-to-docker-for-ai-agent-sandboxing">How does CubeSandbox compare to Docker for AI agent sandboxing?</h3>
<p>Docker containers share the host kernel and provide OS-level isolation through namespaces and cgroups, which is vulnerable to container escape exploits. CubeSandbox provides hardware-level isolation with a dedicated kernel per sandbox, making it significantly more secure. While Docker starts instantly, CubeSandbox&rsquo;s sub-60ms boot time is close enough for most agent workloads, and the security benefits of hardware isolation outweigh the marginal startup time difference.</p>
<h3 id="is-cubesandbox-compatible-with-e2bs-sdk">Is CubeSandbox compatible with E2B&rsquo;s SDK?</h3>
<p>Yes, CubeSandbox is fully compatible with the E2B Code Interpreter SDK. Developers can switch from E2B to CubeSandbox by changing a single environment variable with zero code modifications. This compatibility extends to the sandbox API, file operations, and execution semantics, making migration straightforward for teams already using E2B.</p>
<h3 id="can-cubesandbox-be-deployed-on-kubernetes">Can CubeSandbox be deployed on Kubernetes?</h3>
<p>Yes, CubeSandbox v0.6 introduced native Kubernetes deployment via a Helm chart. The Helm chart handles CubeAPI, CubeMaster, Cubelet, and supporting services, integrating with existing Kubernetes networking and storage infrastructure. CubeSandbox also supports single-node bare-metal deployment, multi-node cluster deployment, and Terraform-based deployment on Tencent Cloud.</p>
<h3 id="what-are-the-hardware-requirements-for-running-cubesandbox">What are the hardware requirements for running CubeSandbox?</h3>
<p>CubeSandbox requires a Linux host with KVM support and a kernel that supports eBPF. A single node can support over 2,000 sandbox instances. For production deployments, Tencent Cloud recommends bare-metal instances with KVM virtualization extensions. ARM64 is fully supported across compilation, build, and deployment. The memory overhead per sandbox is less than 5 MB, making it efficient even on resource-constrained hosts.</p>
]]></content:encoded></item></channel></rss>