<?xml version="1.0" encoding="utf-8" standalone="yes"?><rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom" xmlns:content="http://purl.org/rss/1.0/modules/content/"><channel><title>Kv Cache on RockB</title><link>https://baeseokjae.github.io/tags/kv-cache/</link><description>Recent content in Kv Cache on RockB</description><image><title>RockB</title><url>https://baeseokjae.github.io/images/og-default.png</url><link>https://baeseokjae.github.io/images/og-default.png</link></image><generator>Hugo</generator><language>en-us</language><lastBuildDate>Wed, 19 Aug 2026 04:02:50 +0000</lastBuildDate><atom:link href="https://baeseokjae.github.io/tags/kv-cache/index.xml" rel="self" type="application/rss+xml"/><item><title>LLM Inference Engineering: From KV Cache to PagedAttention</title><link>https://baeseokjae.github.io/posts/llm-inference-engineering-guide/</link><pubDate>Wed, 19 Aug 2026 04:02:50 +0000</pubDate><guid>https://baeseokjae.github.io/posts/llm-inference-engineering-guide/</guid><description>LLM inference engineering optimizes prefill, decode, KV cache, and PagedAttention to cut memory waste and boost serving throughput by up to 24x.</description><content:encoded><![CDATA[<p>LLM inference engineering is the discipline of making large language models run fast, cheap, and reliably in production. It centers on two phases — prefill and decode — and on the KV cache, the memory structure that stores attention keys and values. The single biggest win in the field is PagedAttention, which manages the KV cache like an operating system manages virtual memory, cutting memory waste from up to 60–80% down to near zero and boosting serving throughput by up to 24x over naive implementations.</p>
<h2 id="what-is-llm-inference-engineering-and-why-it-matters">What Is LLM Inference Engineering (and Why It Matters)</h2>
<p>LLM inference engineering is the practice of optimizing the serving stack that runs trained models — the batching, memory management, attention kernels, and hardware scheduling that turn a model checkpoint into a responsive API. It matters because inference cost and latency, not training, dominate the operating budget of deployed AI products. Every token a user sees must be generated, cached, and streamed back, and the difference between a well-engineered and a naive serving stack is often an order of magnitude in throughput.</p>
<p>The stakes are concrete. A model that serves 10x more requests on the same GPU hardware directly cuts cost per token and improves user experience. This is why companies like OpenAI, Anthropic, and Meta invest heavily in inference engineering teams, and why open-source projects like vLLM, TensorRT-LLM, and llama.cpp have become foundational infrastructure. Understanding inference engineering lets you choose the right serving framework, tune it correctly, and reason about GPU memory budgets instead of treating them as a black box.</p>
<h2 id="inference-fundamentals--prefill-vs-decode-phases">Inference Fundamentals — Prefill vs Decode Phases</h2>
<p>Every LLM generation runs in two distinct phases with very different computational profiles. The <strong>prefill phase</strong> processes the entire input prompt in parallel, computing the key and value vectors for every input token at once. Because all tokens are processed simultaneously, prefill is highly parallelized and makes excellent use of GPU compute — it is compute-bound and fast relative to the number of tokens it handles.</p>
<p>The <strong>decode phase</strong> is where the model generates output tokens one at a time, autoregressively. Each new token depends on all previous tokens, so the GPU must process the full context sequentially. This phase is memory-bound and severely underutilizes GPU compute, as NVIDIA&rsquo;s inference optimization guide notes. The decode phase is typically the bottleneck in real serving workloads because it is serial and cannot be parallelized across tokens.</p>
<p>The practical consequence is that inference engineers must optimize the two phases differently. Prefill benefits from high-throughput matrix multiplication kernels and large batch sizes. Decode benefits from reducing memory traffic — which is exactly what the KV cache, attention optimizations, and quantization techniques in this guide address. Understanding which phase dominates your workload tells you which optimization to prioritize.</p>
<h2 id="the-kv-cache--why-its-the-memory-bottleneck">The KV Cache — Why It&rsquo;s the Memory Bottleneck</h2>
<p>During decode, the model needs the key and value vectors of every previous token to compute attention for the current token. Rather than recomputing these vectors on every step — which would be enormously wasteful — the inference engine stores them in a structure called the <strong>KV cache</strong>. This cache grows with every generated token and is the single largest memory consumer in LLM serving.</p>
<p>The problem is how traditional serving frameworks allocate this memory. Most reserve space for the <strong>maximum</strong> context length (for example, 2048 or 4096 tokens) for every request, regardless of how many tokens the request actually uses. Because real requests vary wildly in length, this reservation wastes up to 60–80% of KV-cache memory, as detailed in analyses of vLLM&rsquo;s design. The wasted memory is fragmented and cannot be reused by other requests, so the GPU fills up with idle reserved space while new requests are rejected.</p>
<p>The KV cache also grows and shrinks dynamically as requests generate tokens, which causes fragmentation and redundant duplication across requests that share prefixes. This is the core memory-management problem that PagedAttention was designed to solve. Before PagedAttention, engineers had to over-provision memory, accept low batch sizes, or implement complex custom allocators — all of which left significant performance on the table.</p>
<h2 id="pagedattention--os-style-paging-for-the-kv-cache">PagedAttention — OS-Style Paging for the KV Cache</h2>
<p>PagedAttention, introduced by the vLLM team at UC Berkeley, applies a classic operating-systems idea to the KV cache: <strong>virtual memory paging</strong>. Just as an OS stores process memory in fixed-size pages that can be scattered across physical RAM and tracked by a page table, PagedAttention stores the KV cache in fixed-size blocks that can be scattered across GPU memory and tracked by a block table.</p>
<p>This design eliminates the two biggest sources of waste. First, because blocks are allocated on demand as tokens are generated, the engine no longer reserves memory for the maximum context length — it only uses what each request actually needs. Second, because blocks are non-contiguous and tracked by a table, memory fragmentation disappears; freed blocks can be reused by any other request. The result is near-zero waste in KV-cache memory, as the PagedAttention paper reports.</p>
<p>The throughput gains are dramatic. vLLM with PagedAttention delivers up to 24x higher throughput than HuggingFace Transformers and up to 3.5x higher than Text Generation Inference (TGI) without any model architecture changes, according to the vLLM blog. The academic paper reports 2–4x higher throughput at the same latency compared to FasterTransformer and Orca, with the improvement growing for longer sequences, larger models, and complex decoding algorithms. PagedAttention also enables flexible sharing of KV cache within and across requests, which is valuable for parallel sampling and beam search.</p>
<h2 id="attention-optimizations--gqa-mqa-and-flashattention">Attention Optimizations — GQA, MQA, and FlashAttention</h2>
<p>Beyond memory allocation, inference engineers optimize the attention computation itself. Two families of techniques matter most: reducing the KV cache footprint through shared attention heads, and reducing memory movement through fused kernels.</p>
<p><strong>Multi-query attention (MQA)</strong> and <strong>grouped-query attention (GQA)</strong> reduce the KV cache by sharing key and value heads across multiple query heads. In standard multi-head attention, every query head has its own key and value heads, which multiplies the cache size. MQA uses a single key/value head shared by all query heads, while GQA uses a small group of key/value heads shared by several query heads each. Both dramatically shrink the KV cache with minimal accuracy loss, and GQA in particular has become the default in modern models like Llama 2 and Llama 3.</p>
<p><strong>FlashAttention</strong> takes a different approach: it fuses the attention computation into a single kernel that never materializes the full attention matrix in memory. Because attention is IO-bound — the bottleneck is moving data between GPU memory and compute units, not the arithmetic itself — FlashAttention minimizes memory movement and delivers large speedups. It is now a standard building block in nearly every modern inference stack.</p>
<p>These techniques are complementary. GQA and MQA reduce how much KV cache you need to store, while FlashAttention reduces how much memory traffic the attention computation generates. Together with PagedAttention, they form the core of a modern, memory-efficient inference engine.</p>
<h2 id="speculative-decoding--draft-and-verify-for-faster-generation">Speculative Decoding — Draft-and-Verify for Faster Generation</h2>
<p>The decode phase is serial and memory-bound, which makes it hard to speed up directly. <strong>Speculative decoding</strong> attacks this problem by having a small, fast <strong>draft model</strong> propose a sequence of tokens, which a large <strong>target model</strong> then verifies in parallel. Because the target model can check multiple proposed tokens at once, the GPU&rsquo;s parallelism is finally put to use during decode.</p>
<p>The key insight is that verifying tokens in parallel is much cheaper than generating them one at a time. When the draft model is accurate, the target model accepts several tokens per step, effectively generating multiple tokens per forward pass. Google&rsquo;s research on speculative decoding reports meaningful speedups, and the technique is now widely deployed in production serving systems, including Cloudflare&rsquo;s Workers AI, which combines speculative decoding with KV cache compression.</p>
<p>Speculative decoding is complementary to the memory optimizations above. It does not reduce the KV cache size, but it reduces the number of serial decode steps, which lowers latency and improves throughput. In practice, the best inference stacks combine PagedAttention for memory efficiency, GQA/FlashAttention for attention efficiency, and speculative decoding for decode-phase speed.</p>
<h2 id="kv-cache-quantization--squeezing-more-into-less-vram">KV Cache Quantization — Squeezing More Into Less VRAM</h2>
<p>Quantization reduces the memory footprint of the KV cache by storing keys and values at lower precision. Instead of storing every value as a 16-bit or 32-bit float, the engine stores them as INT8, INT4, or even 3.5-bit values, dramatically shrinking the cache and allowing larger batch sizes or longer contexts on the same GPU.</p>
<p>The tradeoff is accuracy. Aggressive quantization can degrade output quality, but modern techniques achieve near-zero accuracy loss at INT8 and INT4, and even 3.5-bit quantization is viable for many workloads. Projects like Huawei&rsquo;s KVarN provide native vLLM backends for KV-cache quantization, making it practical to deploy in production. The choice of precision is a classic latency-versus-quality tradeoff that inference engineers tune per workload.</p>
<p>KV cache quantization is especially valuable for long-context applications, where the cache can grow to dominate memory. By shrinking the cache, quantization enables longer sequences, larger batches, and lower cost per token — all without changing the model architecture. It is one of the most impactful levers available to a production inference engineer.</p>
<h2 id="hands-on-running-vllm-and-measuring-throughput">Hands-On: Running vLLM and Measuring Throughput</h2>
<p>The best way to understand these concepts is to measure them. vLLM is the reference implementation of PagedAttention and is straightforward to run locally. Install it with <code>pip install vllm</code>, then start a server with a model such as Llama-3-8B:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-bash" data-lang="bash"><span style="display:flex;"><span>python -m vllm.entrypoints.openai.api_server <span style="color:#ae81ff">\
</span></span></span><span style="display:flex;"><span><span style="color:#ae81ff"></span>  --model meta-llama/Llama-3-8B-Instruct <span style="color:#ae81ff">\
</span></span></span><span style="display:flex;"><span><span style="color:#ae81ff"></span>  --gpu-memory-utilization 0.9 <span style="color:#ae81ff">\
</span></span></span><span style="display:flex;"><span><span style="color:#ae81ff"></span>  --max-model-len <span style="color:#ae81ff">8192</span>
</span></span></code></pre></div><p>To measure the throughput gain, run the same generation workload through both vLLM and HuggingFace Transformers and compare tokens-per-second and requests-per-second. The vLLM team&rsquo;s benchmarks show up to 24x higher throughput than HuggingFace Transformers on the same hardware, so the difference should be dramatic. You can also experiment with the <code>--block-size</code> parameter to see how block size affects memory granularity and throughput.</p>
<p>A useful experiment is to monitor GPU memory utilization while serving concurrent requests. With a naive implementation, you will see memory reserved for maximum context length and requests rejected as the cache fills. With vLLM&rsquo;s PagedAttention, memory is allocated on demand and utilization stays high, allowing a much larger batch of concurrent requests. This hands-on comparison makes the theory concrete and is the fastest way to internalize why PagedAttention matters.</p>
<h2 id="production-serving-best-practices-and-tuning">Production Serving Best Practices and Tuning</h2>
<p>In production, inference engineering is about balancing throughput, latency, and cost under real traffic. Several practices consistently matter. First, use <strong>continuous batching</strong>, which adds and removes requests from the batch as they finish, rather than waiting for a full batch to complete. This keeps the GPU busy and is a major throughput lever. Second, tune <strong>GPU memory utilization</strong> — vLLM&rsquo;s <code>--gpu-memory-utilization</code> flag controls how much VRAM is reserved for the KV cache versus model weights and activations.</p>
<p>Third, choose the right <strong>block size</strong> for your workload. Smaller blocks reduce memory waste for short requests but add overhead; larger blocks are more efficient for long contexts. Fourth, enable <strong>speculative decoding</strong> and <strong>KV cache quantization</strong> where accuracy allows, as both reduce the dominant costs of the decode phase. Fifth, monitor the right metrics: tokens per second, time to first token (prefill latency), inter-token latency, and GPU memory utilization.</p>
<p>Finally, match the serving framework to your needs. vLLM is excellent for high-throughput serving with PagedAttention. TensorRT-LLM offers deep NVIDIA kernel optimization. llama.cpp is ideal for CPU and edge deployment. The best engineers benchmark their specific workload across frameworks rather than assuming one is universally best, because the optimal choice depends on your model, hardware, and traffic pattern.</p>
<h2 id="the-learning-path--from-kv-cache-to-production-inference-engineer">The Learning Path — From KV Cache to Production Inference Engineer</h2>
<p>Becoming an inference engineer is a progression from fundamentals to production systems. Start by understanding the prefill and decode phases and why decode is memory-bound. Then study the KV cache and its fragmentation problem, which motivates everything that follows. Next, learn PagedAttention and how OS-style paging solves the memory waste problem — this is the conceptual core of modern serving.</p>
<p>From there, layer on the attention optimizations: GQA and MQA to shrink the cache, and FlashAttention to reduce memory movement. Add speculative decoding to speed up the serial decode phase, and KV cache quantization to squeeze more into limited VRAM. Finally, get hands-on: run vLLM, benchmark it against naive serving, and tune block size, memory utilization, and batching for your workload.</p>
<p>The field rewards hands-on experimentation. Every optimization in this guide is measurable, and the fastest way to learn is to run the benchmarks yourself. By the end of this path, you will be able to reason about GPU memory budgets, choose the right serving stack, and tune it for real production traffic — the core skills of an LLM inference engineer.</p>
<h2 id="faq">FAQ</h2>
<p><strong>What is LLM inference engineering?</strong>
LLM inference engineering is the practice of optimizing how trained language models are served in production — covering batching, memory management, attention kernels, and hardware scheduling — to maximize throughput and minimize latency and cost per token.</p>
<p><strong>What is the KV cache in LLM inference?</strong>
The KV cache stores the key and value vectors of all previous tokens during generation, so the model does not recompute them at every decode step. It is the largest memory consumer in LLM serving and grows with every generated token.</p>
<p><strong>How does PagedAttention improve LLM inference?</strong>
PagedAttention manages the KV cache in fixed-size, non-contiguous blocks tracked by a block table, like OS virtual memory paging. This eliminates the 60–80% memory waste from reserving maximum context length and removes fragmentation, enabling up to 24x higher throughput.</p>
<p><strong>What is the difference between prefill and decode in LLM inference?</strong>
Prefill processes the entire input prompt in parallel and is compute-bound, while decode generates output tokens one at a time and is memory-bound. Decode is typically the bottleneck because it is serial and underutilizes GPU compute.</p>
<p><strong>What are the best LLM inference optimization techniques?</strong>
The most impactful techniques are PagedAttention for memory efficiency, grouped-query attention (GQA) and multi-query attention (MQA) to shrink the KV cache, FlashAttention to reduce memory movement, speculative decoding to speed up decode, and KV cache quantization to reduce VRAM usage.</p>
]]></content:encoded></item></channel></rss>