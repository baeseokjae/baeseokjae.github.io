<?xml version="1.0" encoding="utf-8" standalone="yes"?><rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom" xmlns:content="http://purl.org/rss/1.0/modules/content/"><channel><title>Firecracker MicroVM on RockB</title><link>https://baeseokjae.github.io/tags/firecracker-microvm/</link><description>Recent content in Firecracker MicroVM on RockB</description><image><title>RockB</title><url>https://baeseokjae.github.io/images/og-default.png</url><link>https://baeseokjae.github.io/images/og-default.png</link></image><generator>Hugo</generator><language>en-us</language><lastBuildDate>Mon, 21 Sep 2026 22:01:20 +0000</lastBuildDate><atom:link href="https://baeseokjae.github.io/tags/firecracker-microvm/index.xml" rel="self" type="application/rss+xml"/><item><title>Reverse-Engineering Claude Web's Hidden Antspace MicroVM: What Firecracker Isolation Tells Us</title><link>https://baeseokjae.github.io/posts/claude-web-antspace-microvm-2026/</link><pubDate>Mon, 21 Sep 2026 22:01:20 +0000</pubDate><guid>https://baeseokjae.github.io/posts/claude-web-antspace-microvm-2026/</guid><description>Claude Code Web runs each session in a Firecracker microVM on Anthropic&amp;#39;s undocumented Antspace platform. Here&amp;#39;s how the architecture was reverse-engineered, layer by layer.</description><content:encoded><![CDATA[<p>Every Claude Code Web session you run doesn&rsquo;t execute on a shared server — it boots inside a dedicated Firecracker microVM managed by an undocumented Anthropic platform codenamed Antspace. That is the finding that emerged in March 2026 when developer AprilNEA ran a single <code>strace -p 1</code> command inside a Claude Code Web sandbox and exposed a five-layer architecture: a Firecracker microVM, an unstripped 27MB Go runner binary, a Rust process supervisor, a mature but completely undocumented deployment PaaS, and a web-app builder codenamed Baku. This post walks through how the discovery was made, what each layer reveals, and why it changes how you should think about isolating your own agent workloads.</p>
<h2 id="what-a-casual-strace--p-1-uncovered-inside-claude-code-web">What a casual <code>strace -p 1</code> uncovered inside Claude Code Web</h2>
<p>The entire investigation began with a single tool and a single process. AprilNEA attached <code>strace</code> to process ID 1 inside a Claude Code Web sandbox session. What looked like a normal container turned out to be something far more interesting. Instead of a generic container runtime, PID 1 was a custom binary named <code>environment-runner</code>, and the system underneath it was a full hardware-virtualized microVM — not a shared kernel container.</p>
<p>That one strace exposed three things immediately: the host ACPI tables carried a tell-tale OEM ID, the runner was an unstripped Go binary leaking Anthropic&rsquo;s internal package names, and a second binary called <code>process_api</code> was acting as a supervisor for spawned processes. This wasn&rsquo;t a hobbyist misconfiguration. It was the outward face of a product Anthropic had never publicly documented.</p>
<h2 id="layer-1--fingerprinting-the-firecracker-microvm-via-acpi-fireck-and-fcat">Layer 1 — Fingerprinting the Firecracker MicroVM via ACPI FIRECK and FCAT</h2>
<p>The first layer is the compute substrate. By reading the ACPI tables inside the session, the researcher found the OEM ID <code>FIRECK</code> and the creator ID <code>FCAT</code>. These are the fingerprints of Amazon&rsquo;s Firecracker, the open-source microVM technology that powers AWS Lambda and Fargate. Anthropic is not running its own hypervisor; it is orchestrating Firecracker, the same virtualization layer that already handles millions of AWS serverless functions.</p>
<p>A detailed inspection of the session confirmed the scale of this isolation. The Claude Code Web session ran a 4 vCPU Intel Xeon Cascade Lake machine at 2.80 GHz, with 16GB of RAM and a 252GB disk, on Linux kernel 6.18.5. In other words, each coding session gets a private, purpose-built VM — full hardware isolation between tenants rather than the process-isolated containers most agent platforms use.</p>
<p>The practical implication is significant. Firecracker boots in milliseconds and allocates as little as 5MB of memory overhead beyond the guest, which is precisely why it is the default for high-churn, per-request computing like serverless. Anthropic choosing it for agent sessions signals that it treats every Claude Code session like a short-lived, disposable unit of compute.</p>
<h2 id="layer-2--the-unstripped-go-binary-and-anthropics-internal-package-tree">Layer 2 — The unstripped Go binary and Anthropic&rsquo;s internal package tree</h2>
<p>The second layer is the executable that actually initializes the environment. The <code>environment-runner</code> binary is a 27MB unstripped Go binary built with full debug symbols, and it was shipped to production without stripping. That single oversight is what made the entire architecture recoverable.</p>
<p>With debug symbols intact, the researcher could use <code>go version -m</code> and symbol-table greps to read Anthropic&rsquo;s internal package structure directly out of the binary. It was built from <code>github.com/anthropics/anthropic/api-go/environment-manager/</code>, giving away internal repo naming and a wealth of untagged structs and functions.</p>
<p>The lesson is a standing warning for any team shipping agent runtimes: stripping binaries is not an afterthought. Anthropic&rsquo;s entire internal architecture leaked because a Go binary hit production without <code>-ldflags=&quot;-s -w&quot;</code>. If you build a sandbox or agent runner, strip symbols, obfuscate where practical, and treat the compiled artifact as a source of intelligence rather than a black box — because your competitors and curious engineers will.</p>
<h2 id="layer-3--antspace-anthropics-undocumented-deployment-platform">Layer 3 — Antspace: Anthropic&rsquo;s undocumented deployment platform</h2>
<p>The third layer is the most consequential finding: Antspace. Reading the decompiled client code reveals Antspace is an Anthropic PaaS that lets users deploy code to the cloud. Its deployment protocol is mature — three phases: create a deployment, upload a build artifact, and stream deployment status over <code>application/x-ndjson</code>. File upload is a single tar.gz archive, unlike Vercel&rsquo;s SHA-deduplication per file.</p>
<p>Remarkably, Antspace has zero public documentation. A search across Anthropic&rsquo;s site, GitHub, blog, docs, LinkedIn, job postings, conference talks, and patents returned nothing, and the version string was prefixed <code>staging-</code>. The protocol is fully developed and shipping, yet nothing about it is discoverable publicly. This strongly suggests it is real internal infrastructure that was pulled in to production to serve Claude Code Web before any formal product announcement.</p>
<h2 id="layer-4--baku-the-claudeai-web-app-builder-and-its-supabase-backend">Layer 4 — Baku, the claude.ai web app builder, and its Supabase backend</h2>
<p>The fourth layer ties Antspace to the broader Anthropic ecosystem. Baku is the internal codename for the claude.ai web app builder. The decompiled binaries show it uses a Vite + React + TypeScript template with a supervisord dev server. When you ask Claude to build a web app, Baku auto-provisions a Supabase backend through six built-in MCP tools.</p>
<p>The commercial implication is direct: Baku&rsquo;s default deploy target is Antspace, not Vercel. Vercel is only an alternative. That means Anthropic has quietly built a fully vertical pipeline — intent → Claude → Baku → Supabase → Antspace → live app — where the user never leaves Anthropic&rsquo;s ecosystem. This reframes Anthropic as a platform competitor and not merely a model provider.</p>
<h2 id="layer-5--byoc-bringing-your-own-cloud">Layer 5 — BYOC: bringing your own cloud</h2>
<p>The fifth layer is enterprise-oriented. The decompiled code contains an <code>envtype/byoc</code> package that implements bring-your-own-cloud (BYOC). This lets enterprises run <code>environment-runner</code> on their own infrastructure while Anthropic API continues to orchestrate. For regulated, hybrid-cloud, or compliance-heavy buyers, this is the control point: you keep the agents&rsquo; execution substrate inside your own trust boundary while Anthropic manages the orchestration layer.</p>
<p>BYOC explains how Anthropic reconciles delivering cloud compute with enterprise data-sovereignty demands. Rather than forcing every customer onto its own Firecracker fleet, it offers a deployment mode where the runtime stays behind your firewall.</p>
<h2 id="the-snapstart-pattern-how-sessions-boot-in-under-30ms">The Snapstart pattern: how sessions boot in under 30ms</h2>
<p>None of this would be usable for interactive coding if every session required a full VM cold boot. Fast VM boots are the difference between a responsive coding loop and a waiting-to-answer lag. Anthropic solves this with a pattern it calls Snapstart.</p>
<p>Instead of booting a fresh VM and installing the runtime per session, the system boots a template once, freezes it, and restores it from a snapshot. The session&rsquo;s <code>dmesg</code> showed a 48.5-hour gap between the template boot and the actual session restore, and the ext4 rootfs mount count was 11 — meaning the same rootfs image had been reused across 11 sessions. Block devices are hot-swapped at restore time. This deferred-mount pattern achieves near-instant session restoration, far faster than a traditional multi-gigabyte VM boot.</p>
<p>Snapstart matters because cold-start latency is the hidden cost of per-session isolation. Snapshot/restore trades a larger upfront boot for a near-zero subsequent boot, and it is the pattern any platform building disposable agent environments should replicate.</p>
<h2 id="process_api-the-remote-process-supervisor-wire-protocol">process_api: the remote process supervisor wire protocol</h2>
<p>Inside the microVM, <code>process_api</code> is the 3.1MB Rust/tokio binary that runs as PID 1 — the init and supervisor for anything the coding agent spawns. Decompiled via Ghidra, it became 3,599 functions and roughly 446,000 lines of C pseudocode. Its modules reveal the full lifecycle: <code>firecracker_init</code>, <code>control_server</code>, <code>cgroup</code>, <code>oom_killer</code>, <code>proc_handle</code>, <code>state</code>, and <code>adopter</code>.</p>
<p>The host talks to it over a WebSocket wire protocol. Authentication uses JWT, then a <code>ProcessConnection</code> handshake establishes a session for <code>CreateProcess</code>. From there, stdin/stdout/stderr are carried in binary framing, and the client can send <code>SendSignal</code>, <code>Resize</code>, <code>Detach</code>, and <code>KeepAlive</code> messages. A separate HTTP control API on port 2025 exposes <code>/status</code>, <code>/fs_sync</code>, <code>/shutdown</code>, <code>/auth_public_key</code>, <code>/mount_root</code> (the snapstart path), and <code>/container_name</code>. Disks are laid out as three block devices: <code>vda</code> is an ext4 read-write rootfs, <code>vdb</code> a squashfs containing the Claude Code binary, and <code>vdc</code> a squashfs for the environment runner.</p>
<h2 id="security-hardening-init_on_free-cap_sys_resource-crng-reseed-localhost-blocking">Security hardening: init_on_free, CAP_SYS_RESOURCE, CRNG reseed, localhost blocking</h2>
<p>For a product that would worry security reviewers, Antspace shows deliberate hardening. The guest enables <code>init_on_free</code>, which zeroes freed memory pages to make cold-boot and memory-extraction attacks harder. It drops <code>CAP_SYS_RESOURCE</code> after initialization so a compromised process cannot exhaust host or container resources. On snapshot restore it reseeds the CRNG so the restored VM cannot reuse a stale random state across sessions. And it passes <code>--block-local-connections</code> to prevent the coding agent from probing other tenants&rsquo; local network services.</p>
<p>It is worth restating what this is not: no exploit was demonstrated, and no privilege escalation was found. This is standard operating-system introspection of an unstripped binary. The security story is hardening best practice you can crib for your own agent sandboxes — not a vulnerability disclosure.</p>
<h2 id="a-reusable-reverse-engineering-methodology-the-exact-commands">A reusable reverse-engineering methodology: the exact commands</h2>
<p>The value of the AprilNEA writeup is that the technique is fully reusable. If you want to find out what really runs inside a sandboxed agent environment, this is the cookbook:</p>
<ul>
<li><code>strace -p 1</code> on the init process to see system calls and hint at the supervisor.</li>
<li><code>dmesg | grep FIRECK</code> and check ACPI tables for the OEM ID <code>FIRECK</code> and creator ID <code>FCAT</code> to fingerprint a Firecracker microVM.</li>
<li><code>cat /proc/1/cmdline</code> to see how PID 1 was invoked.</li>
<li><code>go version -m</code> on any Go binary to read embedded build and module metadata.</li>
<li><code>objdump -t</code> and symbol-table greps against unstripped binaries to map out internal package and function names.</li>
<li>Pull the binary to disk and run Ghidra for full decompilation of native supervisors.</li>
</ul>
<p>These steps turn an opaque sandbox into a documented architecture in an afternoon, and they apply to any agent platform that ships binaries into customer-visible sessions.</p>
<h2 id="why-this-matters-for-ai-platform-builders">Why this matters for AI platform builders</h2>
<p>The aggregate picture is that Firecracker-based microVM sandboxes are becoming the default infrastructure layer for serious coding-agent platforms. Anthropic is not experimenting; it built a deployment protocol, a web-app builder, a BYOC escape hatch, and a snapshot-restore runtime around it. The engineering discipline — full hardware isolation, near-instant restores, hardened memory handling — sets a new baseline for what users should expect from an agent platform.</p>
<p>For competitors and startups, Antspace demonstrates the endgame: a vertical pipeline where an AI handles intent and delivery, and the user never leaves the platform. Whether or not Antspace ever launches publicly, the architecture is the blueprint.</p>
<h2 id="what-it-means-for-you-isolating-your-own-agent-sandboxes">What it means for you: isolating your own agent sandboxes</h2>
<p>If you are building agents that can execute code, untrusted or not, the concrete takeaways are: give each session hardware isolation via Firecracker or similar rather than process-level containers, use snapshot/restore to hide the boot cost, strip symbols before shipping any runtime binary, zero freed memory and drop dangerous capabilities, and reseed entropy on restore. Most importantly, decide up front whether you will host the runtime or let enterprises bring their own cloud — because BYOC is fast becoming a purchase requirement, not a nice-to-have.</p>
<h2 id="faq">FAQ</h2>
<p><strong>Is the Antspace MicroVM a real security vulnerability in Claude Web?</strong>
No. The investigation found no exploit, no privilege escalation, and no data-leak bug. It is introspection of an unstripped binary and standard OS-level inspection, not a vulnerability disclosure.</p>
<p><strong>What does the FIRECK ACPI fingerprint prove?</strong>
The ACPI OEM ID <code>FIRECK</code> and creator ID <code>FCAT</code> identify the session as a Firecracker microVM — the same technology powering AWS Lambda and Fargate. It proves Anthropic uses hardware virtualization, not just container isolation, for Claude Code Web sessions.</p>
<p><strong>Why is Antspace undocumented if it ships in production?</strong>
Antspace has zero public documentation across Anthropic&rsquo;s site, GitHub, docs, and job postings, and its version string is prefixed <code>staging-</code>. It appears to be real internal infrastructure that got pressed into production to serve Claude Code Web before any formal product announcement.</p>
<p><strong>How does a Claude Code session boot in under 30ms?</strong>
It uses a Snapstart-style snapshot/restore pattern. A template VM boots once, freezes, and resumes from snapshot with block devices hot-swapped at restore. <code>dmesg</code> showed a 48.5-hour gap between template boot and session restore, and a rootfs reused across 11 sessions.</p>
<p><strong>Can I isolate my own agent workloads this way?</strong>
Yes. The same stack — Firecracker for hardware isolation, snapshot/restore for fast boot, symbol stripping, hardened memory init, and capability dropping — is available to any team and is increasingly the baseline for serious agent platforms.</p>
]]></content:encoded></item></channel></rss>