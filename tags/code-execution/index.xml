<?xml version="1.0" encoding="utf-8" standalone="yes"?><rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom" xmlns:content="http://purl.org/rss/1.0/modules/content/"><channel><title>Code Execution on RockB</title><link>https://baeseokjae.github.io/tags/code-execution/</link><description>Recent content in Code Execution on RockB</description><image><title>RockB</title><url>https://baeseokjae.github.io/images/og-default.png</url><link>https://baeseokjae.github.io/images/og-default.png</link></image><generator>Hugo</generator><language>en-us</language><lastBuildDate>Sun, 26 Jul 2026 19:10:00 +0000</lastBuildDate><atom:link href="https://baeseokjae.github.io/tags/code-execution/index.xml" rel="self" type="application/rss+xml"/><item><title>BlastRadius AI Agent Execution Audit: Find Every Place an AI Agent Can Run Code Without Your Approval</title><link>https://baeseokjae.github.io/posts/blastradius-agent-execution-audit-2026/</link><pubDate>Sun, 26 Jul 2026 19:10:00 +0000</pubDate><guid>https://baeseokjae.github.io/posts/blastradius-agent-execution-audit-2026/</guid><description>A comprehensive blastradius AI agent execution audit mapping every surface where agents run code — shell, filesystem, MCP, cloud CLIs, sub-agents — and how to secure them.</description><content:encoded><![CDATA[<h2 id="introduction--the-blast-radius-problem">Introduction — The Blast Radius Problem</h2>
<p>A blastradius AI agent execution audit reveals a sobering truth: modern AI agents can execute code across at least ten distinct surfaces without explicit human approval at each step. From shell commands and file system operations to package managers, cloud CLIs, browser automation, MCP servers, and even spawning sub-agents, the attack surface is vast and largely ungoverned. The core problem is that convenience-driven design in local AI agents collapses the trust layers that traditional security models depend on — UI boundaries, network segmentation, and user confirmation gates are all bypassed in the name of productivity. Understanding every surface where an agent can run code is the first step toward building a real security model for autonomous systems.</p>
<h2 id="shell-command-execution--the-most-direct-attack-surface">Shell Command Execution — The Most Direct Attack Surface</h2>
<p>The shell is the most powerful and dangerous tool an AI agent wields. When an agent has access to <code>bash</code>, <code>zsh</code>, or PowerShell, it can execute arbitrary commands — <code>rm -rf</code>, <code>curl</code> to exfiltration endpoints, <code>chmod</code> on sensitive files, and <code>sudo</code> operations. Most agent frameworks grant shell access by default because it is the simplest way to let agents install dependencies, run scripts, and interact with the operating system.</p>
<p>The OpenClaw RCE incident from February 2026 demonstrated this vividly: a crafted link caused an agent to connect to an attacker-controlled gateway, leak its API token, and allow the attacker to reconfigure the execution environment and run arbitrary commands. The attack succeeded because the agent had unrestricted shell access and no policy layer checked what commands were being executed or why.</p>
<p><strong>Key blast radius factors for shell execution:</strong></p>
<table>
	<thead>
			<tr>
					<th>Factor</th>
					<th>Risk Level</th>
					<th>Mitigation</th>
			</tr>
	</thead>
	<tbody>
			<tr>
					<td>Arbitrary command execution</td>
					<td>Critical</td>
					<td>Policy engine with allow/deny lists</td>
			</tr>
			<tr>
					<td>Pipe to network (curl, nc)</td>
					<td>Critical</td>
					<td>Network egress controls</td>
			</tr>
			<tr>
					<td>File deletion/modification</td>
					<td>High</td>
					<td>Read-only filesystem policies</td>
			</tr>
			<tr>
					<td>Privilege escalation (sudo)</td>
					<td>Critical</td>
					<td>Drop capabilities, no-root execution</td>
			</tr>
			<tr>
					<td>Background processes</td>
					<td>Medium</td>
					<td>Process lifecycle management</td>
			</tr>
	</tbody>
</table>
<h2 id="file-system-access--reading-and-writing-without-permission">File System Access — Reading and Writing Without Permission</h2>
<p>File system access is the second most common execution surface. Agents read and write files constantly — configuration files, source code, credentials, environment variables, and secrets. The danger is that most agents have unfettered read access to the entire user home directory, including <code>.ssh/</code>, <code>.aws/</code>, <code>.env</code>, and <code>~/.config/</code> directories.</p>
<p>The Clinejection attack from February 2026 exploited this: a prompt injection hidden in a GitHub issue title caused an AI agent to read local credentials, modify source code, and publish backdoored npm packages. Within 8 hours, 4,000 developer machines were compromised. The agent had no policy preventing it from reading SSH keys or modifying package.json files before publishing.</p>
<p><strong>Critical file system surfaces to audit:</strong></p>
<ul>
<li><strong>~/.ssh/</strong> — Private keys, authorized_keys, known_hosts</li>
<li><strong>~/.aws/</strong> — AWS credentials and config</li>
<li><strong>~/.env</strong> or <strong>.env</strong> — Environment variables with API keys</li>
<li><strong>~/.config/</strong> — Application configs with tokens</li>
<li><strong>/etc/</strong> — System configuration (requires sudo)</li>
<li><strong>/var/run/secrets/</strong> — Container secrets (Kubernetes)</li>
<li><strong>~/.npmrc</strong>, <strong>~/.pypirc</strong> — Package registry credentials</li>
</ul>
<h2 id="package-management--npm-pip-and-the-supply-chain">Package Management — npm, pip, and the Supply Chain</h2>
<p>Package managers are a uniquely dangerous execution surface because they combine code download, code execution, and network access in a single command. When an agent runs <code>npm install</code> or <code>pip install</code>, it executes arbitrary code from the internet with the same privileges as the agent itself.</p>
<p>The Trivy/LiteLLM cascade from March 2026 showed how devastating this can be. A compromised security scanner cascaded credentials to LiteLLM, which has 3.4 million daily PyPI downloads. The attack affected five ecosystems and 36% of cloud environments. The agent had no policy preventing it from installing untrusted packages or checking package integrity before execution.</p>
<p><strong>Package manager attack vectors:</strong></p>
<table>
	<thead>
			<tr>
					<th>Vector</th>
					<th>Example</th>
					<th>Impact</th>
			</tr>
	</thead>
	<tbody>
			<tr>
					<td>Malicious dependency</td>
					<td>Typosquatted npm package</td>
					<td>Credential theft, backdoor</td>
			</tr>
			<tr>
					<td>Compromised maintainer</td>
					<td>Legitimate package with malicious update</td>
					<td>Supply chain compromise</td>
			</tr>
			<tr>
					<td>Pre/post-install scripts</td>
					<td>npm <code>preinstall</code> hook</td>
					<td>Arbitrary code execution</td>
			</tr>
			<tr>
					<td>Dependency confusion</td>
					<td>Private package name taken on public registry</td>
					<td>Internal credential exposure</td>
			</tr>
			<tr>
					<td>Transitive dependency</td>
					<td>Dependency of a dependency</td>
					<td>Deep supply chain infiltration</td>
			</tr>
	</tbody>
</table>
<h2 id="infrastructure-tools--terraform-kubectl-docker-and-cloud-clis">Infrastructure Tools — Terraform, kubectl, Docker, and Cloud CLIs</h2>
<p>Infrastructure tools represent the highest-impact execution surface because they can destroy or compromise production environments. An agent with access to <code>terraform</code>, <code>kubectl</code>, <code>awscli</code>, <code>gcloud</code>, or <code>docker</code> can modify, delete, or exfiltrate cloud infrastructure.</p>
<p>The Terraform destroy incident from February 2026 is the canonical example. An AI agent unpacked old Terraform configs from a project directory and ran <code>terraform destroy</code> without any human approval. The command wiped a production VPC, RDS database, and ECS cluster. The agent had no policy preventing destructive infrastructure operations — it simply saw a Terraform configuration file and executed the most dangerous possible command against it.</p>
<p><strong>Infrastructure tools blast radius:</strong></p>
<table>
	<thead>
			<tr>
					<th>Tool</th>
					<th>Destructive Capability</th>
					<th>Policy Needed</th>
			</tr>
	</thead>
	<tbody>
			<tr>
					<td>terraform</td>
					<td><code>destroy</code>, <code>apply -destroy</code></td>
					<td>Block all destroy operations</td>
			</tr>
			<tr>
					<td>kubectl</td>
					<td><code>delete</code>, <code>exec</code>, <code>apply -f</code></td>
					<td>Namespace-scoped RBAC</td>
			</tr>
			<tr>
					<td>docker</td>
					<td><code>rm -f</code>, <code>exec</code>, <code>build</code></td>
					<td>Read-only container access</td>
			</tr>
			<tr>
					<td>awscli</td>
					<td><code>s3 rm</code>, <code>ec2 terminate</code>, <code>iam delete</code></td>
					<td>Resource-level IAM policies</td>
			</tr>
			<tr>
					<td>gcloud</td>
					<td><code>compute instances delete</code>, <code>iam</code></td>
					<td>Scoped service accounts</td>
			</tr>
	</tbody>
</table>
<h2 id="browser-automation--agents-navigating-the-web-for-you">Browser Automation — Agents Navigating the Web for You</h2>
<p>Browser automation agents — like browser-use, Playwright, and Puppeteer — can navigate websites, fill forms, click buttons, and extract data. This surface is often overlooked in security audits because it does not involve direct code execution, but browser automation can be weaponized for credential theft, session hijacking, and data exfiltration.</p>
<p>An agent with browser access can log into your bank, transfer money, post on social media, delete cloud resources through web consoles, and exfiltrate data from SaaS applications. The IDEsaster research from December 2025 found 30+ vulnerabilities across Cursor, Windsurf, and GitHub Copilot, with 24 CVEs assigned. Many of these vulnerabilities involved browser-based agent actions that bypassed user consent.</p>
<p><strong>Browser automation risks:</strong></p>
<ul>
<li><strong>Credential autofill</strong> — Agent can read saved passwords from browser</li>
<li><strong>Session cookie access</strong> — Agent can hijack authenticated sessions</li>
<li><strong>Form submission</strong> — Agent can submit destructive forms without review</li>
<li><strong>File download/upload</strong> — Agent can exfiltrate or inject files</li>
<li><strong>Extension access</strong> — Agent can interact with browser extensions</li>
</ul>
<h2 id="mcp-servers--the-new-attack-vector">MCP Servers — The New Attack Vector</h2>
<p>Model Context Protocol (MCP) servers are a relatively new but rapidly growing attack surface. MCP servers provide agents with structured access to external tools, databases, and APIs. When an agent connects to an MCP server, it can invoke any tool the server exposes — database queries, file operations, API calls, and more.</p>
<p>The danger is that MCP servers often have broad capabilities and minimal access controls. An agent connected to a filesystem MCP server can read and write any file the server has access to. A database MCP server can run arbitrary SQL queries. A cloud MCP server can provision or destroy resources.</p>
<p><strong>MCP server security checklist:</strong></p>
<ul>
<li><input disabled="" type="checkbox"> Audit every MCP server your agent connects to</li>
<li><input disabled="" type="checkbox"> Restrict MCP server capabilities to minimum required</li>
<li><input disabled="" type="checkbox"> Implement tool-level allow/deny policies for MCP invocations</li>
<li><input disabled="" type="checkbox"> Use read-only MCP servers where possible</li>
<li><input disabled="" type="checkbox"> Monitor MCP server invocation patterns for anomalies</li>
<li><input disabled="" type="checkbox"> Isolate MCP servers in separate containers or namespaces</li>
</ul>
<h2 id="sub-agent-spawning--when-agents-create-agents">Sub-Agent Spawning — When Agents Create Agents</h2>
<p>Multi-agent systems introduce a terrifying blast radius expansion: a compromised agent can spawn sub-agents, which can spawn more sub-agents, creating a recursive attack tree. Each sub-agent inherits the parent&rsquo;s capabilities — or potentially gains new ones.</p>
<p>The Gulama framework addresses this with isolated sub-agents and 5 autonomy levels, but most agent frameworks have no governance model for sub-agent creation. An agent compromised by prompt injection can spawn a fleet of sub-agents to exfiltrate data, attack other systems, or establish persistence.</p>
<p><strong>Sub-agent governance requirements:</strong></p>
<ul>
<li><strong>Creation policies</strong> — Which agents can create sub-agents?</li>
<li><strong>Capability inheritance</strong> — What capabilities do sub-agents inherit?</li>
<li><strong>Resource limits</strong> — How many sub-agents can exist?</li>
<li><strong>Lifecycle management</strong> — When and how are sub-agents terminated?</li>
<li><strong>Audit trail</strong> — Complete lineage tracking for all spawned agents</li>
</ul>
<h2 id="credential-and-secret-access--the-crown-jewels">Credential and Secret Access — The Crown Jewels</h2>
<p>Credentials are the ultimate prize for an attacker targeting AI agents. An agent with access to API keys, database passwords, cloud access keys, or OAuth tokens can pivot from a single compromised agent to a full infrastructure takeover.</p>
<p>The IDEsaster research found that IDE-based AI agents had access to all environment variables, including production database credentials and cloud API keys. The OpenClaw RCE attack succeeded because the attacker extracted the agent&rsquo;s API token and used it to reconfigure the execution environment.</p>
<p><strong>Credential exposure surfaces:</strong></p>
<table>
	<thead>
			<tr>
					<th>Source</th>
					<th>Risk</th>
					<th>Protection</th>
			</tr>
	</thead>
	<tbody>
			<tr>
					<td>Environment variables</td>
					<td>High</td>
					<td>Scoped environment injection</td>
			</tr>
			<tr>
					<td>.env files</td>
					<td>High</td>
					<td>File access policies</td>
			</tr>
			<tr>
					<td>Cloud provider metadata</td>
					<td>Critical</td>
					<td>IMDSv2 with hop limit</td>
			</tr>
			<tr>
					<td>Password managers</td>
					<td>High</td>
					<td>Agent isolation from credential stores</td>
			</tr>
			<tr>
					<td>SSH agent socket</td>
					<td>High</td>
					<td>Socket access policies</td>
			</tr>
			<tr>
					<td>Docker socket</td>
					<td>Critical</td>
					<td>Never mount docker socket</td>
			</tr>
	</tbody>
</table>
<h2 id="data-exfiltration--curl-uploads-and-network-egress">Data Exfiltration — curl, Uploads, and Network Egress</h2>
<p>Data exfiltration is the end goal of most agent compromises. An agent that can execute shell commands can use <code>curl</code>, <code>wget</code>, <code>nc</code>, or <code>scp</code> to send data to an attacker-controlled server. An agent with file system access can read sensitive files and upload them. An agent with browser access can navigate to a web form and paste stolen data.</p>
<p>The Clinejection attack resulted in backdoored npm packages being published — a form of data exfiltration where malicious code was injected into the supply chain. The agent had no network egress controls preventing it from publishing packages or sending data to external servers.</p>
<p><strong>Network egress controls for agents:</strong></p>
<ul>
<li><strong>DNS filtering</strong> — Block known malicious domains</li>
<li><strong>IP allowlisting</strong> — Only allow connections to approved endpoints</li>
<li><strong>Rate limiting</strong> — Prevent bulk data exfiltration</li>
<li><strong>Content inspection</strong> — Detect sensitive data in outbound traffic</li>
<li><strong>Proxy enforcement</strong> — Route all agent traffic through a secure proxy</li>
</ul>
<h2 id="real-world-incidents--clinejection-terraform-destroy-openclaw-rce">Real-World Incidents — Clinejection, Terraform Destroy, OpenClaw RCE</h2>
<p>The incidents that have shaped the blastradius AI agent execution audit landscape are not theoretical — they have caused real damage to real organizations.</p>
<p><strong>Clinejection (February 2026):</strong> A prompt injection in a GitHub issue title caused an AI agent to publish backdoored npm packages. 4,000 developer machines were compromised in 8 hours. The attack vector was a simple text file — a GitHub issue — that the agent read and acted upon without any validation or policy check.</p>
<p><strong>Terraform Destroy (February 2026):</strong> An AI agent unpacked old Terraform configs and ran <code>terraform destroy</code>, wiping a production VPC, RDS database, and ECS cluster. The agent had no policy preventing destructive infrastructure operations. The entire incident took less than 60 seconds from reading the config to destroying production.</p>
<p><strong>IDEsaster (December 2025):</strong> Researchers found 30+ vulnerabilities across Cursor, Windsurf, and GitHub Copilot. 24 CVEs were assigned. The vulnerabilities ranged from credential exposure to arbitrary code execution through IDE extensions.</p>
<p><strong>Trivy/LiteLLM Cascade (March 2026):</strong> A compromised security scanner cascaded credentials to LiteLLM (3.4 million daily PyPI downloads). Five ecosystems were affected, and 36% of cloud environments were impacted. The attack demonstrated how a single compromised dependency can cascade through the entire software supply chain.</p>
<p><strong>OpenClaw RCE (February 2026):</strong> A crafted link caused an agent to connect to an attacker-controlled gateway, leak its token, and allow the attacker to reconfigure the execution environment and run arbitrary commands. The attack exploited the agent&rsquo;s unrestricted network access and lack of connection validation.</p>
<h2 id="defense-strategies--policy-engines-sandboxing-and-verification">Defense Strategies — Policy Engines, Sandboxing, and Verification</h2>
<p>Defending against the blast radius requires a layered approach that combines deterministic policy enforcement, runtime sandboxing, and post-execution verification.</p>
<p><strong>Deterministic Policy Enforcement:</strong> The most effective defense is a policy engine that evaluates every agent action against a predefined set of rules before execution. Cedar-based policy engines like Vectimus can evaluate policies in under 10ms, making them practical for real-time agent action authorization. Policies should cover destructive operations, secrets access, supply chain actions, infrastructure changes, data exfiltration, file integrity, database operations, git safety, MCP safety, and agent governance.</p>
<p><strong>Runtime Sandboxing:</strong> Running agents in isolated environments — containers, VMs, or sandboxed processes — limits the blast radius of any single compromise. Vett.sh runs each agent on an isolated VM with vaulted credentials, booting in under 150ms. Gulama offers sandboxed execution with AES-256-GCM encryption and 5 autonomy levels from &ldquo;ask before everything&rdquo; to &ldquo;autopilot.&rdquo;</p>
<p><strong>Post-Execution Verification:</strong> Predicate-Secure uses mathematical state diffing to verify that agent actions did not violate policies after execution. This catches cases where a policy bypass or race condition allowed an unauthorized action. The approach is 100% offline and works with browser-use, LangChain, PydanticAI, OpenClaw, and raw Playwright.</p>
<p><strong>Network Segmentation:</strong> Agents should operate on isolated networks with strict egress controls. No agent should have unfettered internet access. DNS filtering, IP allowlisting, and proxy enforcement should be standard.</p>
<h2 id="tools-of-the-trade--vectimus-vett-predicate-secure-gulama-execwall">Tools of the Trade — Vectimus, Vett, Predicate-Secure, Gulama, Execwall</h2>
<p>Several tools have emerged to address the blastradius AI agent execution audit challenge:</p>
<p><strong>Vectimus</strong> provides Cedar-based policy enforcement with 11 policy packs covering Destructive Ops, Secrets, Supply Chain, Infrastructure, Code Execution, Data Exfiltration, File Integrity, Database, Git Safety, MCP Safety, and Agent Governance. Every policy references the real-world incident that motivated it. Policies map to OWASP Agentic Top 10, SOC 2, NIST AI RMF, NIST CSF 2.0, ISO 27001, and the EU AI Act.</p>
<p><strong>Vett.sh</strong> scans every agent skill for malicious code using 40+ static analysis rules at the AST level. Skills are signed with Sigstore before publishing, and each agent runs on an isolated VM with vaulted credentials. Vett has indexed 8,000+ skills and supports 46 agents.</p>
<p><strong>Predicate-Secure</strong> is a 3-line drop-in Python wrapper that adds deterministic pre-execution authorization and post-execution verification. It uses YAML policy files (allow/deny) for OS and browser actions, with mathematical state diffing instead of LLM-as-a-judge. A Rust sidecar (predicate-authorityd) provides sub-millisecond policy evaluations in production.</p>
<p><strong>Gulama</strong> is a security-first open-source AI agent with 15+ security mechanisms including AES-256-GCM encryption, sandboxed execution, and prompt injection detection. It offers 5 autonomy levels and supports MCP Server and Client protocols with multi-agent orchestration using isolated sub-agents.</p>
<p><strong>Execwall</strong> is a dedicated firewall that blocks command injection attacks targeting AI agent frameworks, specifically designed to stop exploits like CVE-2026-2256 (ModelScope AI agent command injection).</p>
<h2 id="compliance-and-standards--owasp-agentic-top-10-nist-ai-rmf">Compliance and Standards — OWASP Agentic Top 10, NIST AI RMF</h2>
<p>The blastradius AI agent execution audit landscape is increasingly shaped by regulatory frameworks and industry standards:</p>
<p><strong>OWASP Agentic Top 10 (2026):</strong> The OWASP Agentic Top 10 identifies the most critical security risks for agentic AI systems. Vectimus maps all 11 of its policy packs to all 10 OWASP categories, providing a compliance-ready security posture. The top risks include prompt injection, insecure output handling, excessive agency, and supply chain vulnerabilities.</p>
<p><strong>NIST AI RMF:</strong> The NIST AI Risk Management Framework provides a structured approach to identifying, assessing, and managing AI risks. For agentic systems, the RMF&rsquo;s focus on mapping, measuring, and managing risks is particularly relevant. Organizations deploying AI agents should conduct NIST AI RMF assessments as part of their security program.</p>
<p><strong>SOC 2 for Agentic Systems:</strong> SOC 2 compliance for agentic AI systems requires demonstrating that security controls cover agent actions, data access, and system changes. Policy engines that log every agent action provide the audit trail needed for SOC 2 evidence.</p>
<p><strong>EU AI Act:</strong> The EU AI Act classifies AI systems by risk level. Agentic systems that can execute code or control infrastructure likely fall into high-risk or limited-risk categories, requiring conformity assessments, human oversight, and transparency obligations.</p>
<p><strong>NIST CSF 2.0:</strong> The NIST Cybersecurity Framework 2.0&rsquo;s govern, identify, protect, detect, respond, and recover functions map naturally to agent security. Policy enforcement covers protect, monitoring covers detect, and incident response plans must account for compromised agents.</p>
<h2 id="conclusion--building-a-security-model-for-autonomous-agents">Conclusion — Building a Security Model for Autonomous Agents</h2>
<p>A blastradius AI agent execution audit reveals that the attack surface is vast but not unmanageable. The key insight is that local execution is not a security boundary — an agent running on your laptop has the same capabilities as an agent running in the cloud. The difference must come from policy enforcement, sandboxing, and verification.</p>
<p>The tools exist today: Vectimus for policy enforcement, Vett for skill scanning, Predicate-Secure for pre- and post-execution verification, Gulama for security-first agent design, and Execwall for agent-specific firewalls. What has been missing is the organizational commitment to using them.</p>
<p>Every organization deploying AI agents should conduct a blastradius AI agent execution audit covering all ten surfaces: shell, filesystem, package managers, infrastructure tools, browser automation, MCP servers, sub-agents, credentials, network egress, and supply chain. For each surface, implement deterministic policies, runtime isolation, and post-execution verification. Map your controls to OWASP Agentic Top 10, NIST AI RMF, and SOC 2 requirements.</p>
<p>The era of trusting AI agents by default is over. The era of verifying every action through policy is here.</p>
<h2 id="faq">FAQ</h2>
<p><strong>Q: What is a blastradius AI agent execution audit?</strong>
A: A blastradius AI agent execution audit is a systematic security assessment that identifies every surface where an AI agent can execute code or perform actions without explicit human approval. It covers shell commands, file system operations, package managers, infrastructure tools, browser automation, MCP servers, sub-agent spawning, credential access, network egress, and supply chain interactions.</p>
<p><strong>Q: How did the Clinejection attack work?</strong>
A: The Clinejection attack (February 2026) used a prompt injection hidden in a GitHub issue title. When an AI agent read the issue, the injected prompt caused the agent to read local credentials, modify source code, and publish backdoored npm packages. Within 8 hours, 4,000 developer machines were compromised. The attack succeeded because the agent had no policy preventing it from reading SSH keys or publishing packages.</p>
<p><strong>Q: What is the difference between deterministic policy enforcement and LLM-as-a-judge?</strong>
A: Deterministic policy enforcement uses predefined rules (like Cedar policies or YAML allow/deny lists) evaluated in under 10ms to authorize or block agent actions before execution. LLM-as-a-judge uses another AI model to evaluate whether an action is safe, which introduces latency, cost, and the possibility of the judge model itself being compromised or hallucinating incorrect decisions. Deterministic approaches are faster, cheaper, and more reliable for security-critical decisions.</p>
<p><strong>Q: Can AI agents be safely used with infrastructure tools like Terraform and kubectl?</strong>
A: Yes, but only with strict policy enforcement. Agents should never have unrestricted access to infrastructure tools. Policies should block destructive operations (terraform destroy, kubectl delete), require human approval for any infrastructure changes, use read-only credentials by default, and log every infrastructure action for audit. The Terraform destroy incident of February 2026 demonstrated what happens without these controls.</p>
<p><strong>Q: What compliance frameworks apply to AI agent security?</strong>
A: The key frameworks are OWASP Agentic Top 10 (specific to agentic AI risks), NIST AI RMF (AI risk management), SOC 2 (security controls and audit trails), NIST CSF 2.0 (cybersecurity framework), ISO 27001 (information security management), and the EU AI Act (regulatory compliance for high-risk AI systems). Policy engines like Vectimus map their controls to all of these frameworks.</p>
]]></content:encoded></item><item><title>Code Execution Sandbox Pricing 2026: Cost, Cold Starts, and Persistence for AI Agents</title><link>https://baeseokjae.github.io/posts/code-execution-sandbox-pricing-comparison-2026/</link><pubDate>Tue, 07 Jul 2026 12:00:00 +0000</pubDate><guid>https://baeseokjae.github.io/posts/code-execution-sandbox-pricing-comparison-2026/</guid><description>A practical 2026 comparison of E2B, Modal, and Fly.io for AI agent code execution — covering per-second pricing, cold start latency, persistence models, and total cost for real agent workloads.</description><content:encoded><![CDATA[<p>If you&rsquo;re building AI agents that execute code — whether it&rsquo;s a coding agent running tests, a data analysis pipeline spinning up Python interpreters, or a multi-agent system that needs isolated environments per task — the sandbox you pick directly determines your cost structure, your latency profile, and how much state management code you have to write yourself. Three providers dominate this space in 2026: E2B, Modal, and Fly.io. Each one has a fundamentally different pricing model, cold start story, and persistence strategy. I&rsquo;ve been running production agent workloads on all three for the past six months, and the differences are bigger than the marketing suggests.</p>
<h2 id="the-pricing-models-at-a-glance">The Pricing Models at a Glance</h2>
<p>The first thing to understand is that these three providers don&rsquo;t just charge different rates — they charge in fundamentally different ways.</p>
<p><strong>E2B</strong> uses a hybrid model: a monthly subscription tier ($150/month for Pro) plus per-second compute on top. You pay the subscription for the right to run sandboxes at scale, then you pay per-second for actual CPU and RAM usage. The Hobby tier is free with $100 one-time credits, but you&rsquo;re limited to 1-hour sandbox sessions and 20 concurrent sandboxes.</p>
<p><strong>Modal</strong> is pure per-second billing with no subscription fee. You get $30/month in free compute credits on the Starter plan, and after that you pay only for what you use. There&rsquo;s no upfront commitment, no minimum spend. This makes Modal the most flexible option for variable or bursty workloads.</p>
<p><strong>Fly.io</strong> is also per-second billing but with a suspend/resume model that changes the economics significantly. Their smallest shared-CPU VM (256MB) costs $0.00000078/second — roughly $2.02/month if it runs continuously. But the real trick is that you can suspend VMs when they&rsquo;re idle and pay only $0.15/month for metadata retention.</p>
<p>Here&rsquo;s the raw per-second pricing for CPU-only workloads:</p>
<table>
	<thead>
			<tr>
					<th>Provider</th>
					<th>1 vCPU/core</th>
					<th>2 vCPU</th>
					<th>4 vCPU</th>
			</tr>
	</thead>
	<tbody>
			<tr>
					<td>E2B</td>
					<td>$0.000014/s</td>
					<td>$0.000028/s</td>
					<td>$0.000056/s</td>
			</tr>
			<tr>
					<td>Modal</td>
					<td>$0.0000131/core/s</td>
					<td>$0.0000262/s</td>
					<td>$0.0000524/s</td>
			</tr>
			<tr>
					<td>Fly.io (shared)</td>
					<td>$0.00000429/s (1x 2GB)</td>
					<td>$0.00000859/s (2x 4GB)</td>
					<td>—</td>
			</tr>
	</tbody>
</table>
<p>E2B and Modal are within 7% of each other on raw CPU pricing. Fly.io is 3-4x cheaper on paper, but that comparison is misleading — Fly.io&rsquo;s shared-CPU VMs are not equivalent to dedicated vCPUs, and you&rsquo;re sharing the physical core with other tenants. Their dedicated performance instances close the gap: a performance-1x 2GB VM costs $0.00001056/s, which is about 75% of E2B&rsquo;s 1 vCPU rate.</p>
<h2 id="cold-starts-the-hidden-cost-that-wastes-your-budget">Cold Starts: The Hidden Cost That Wastes Your Budget</h2>
<p>Cold starts matter more for AI agents than for traditional serverless workloads, and here&rsquo;s why: an interactive agent that calls a sandbox, runs a 200ms Python snippet, and waits for the result feels sluggish if the sandbox takes 2 seconds to boot. For batch processing, cold starts are a throughput problem. For interactive agents, they&rsquo;re a user experience problem.</p>
<p><strong>E2B</strong> claims sub-second cold starts for their lightweight Firecracker microVMs. In my testing, I consistently saw 400-800ms from request to first byte of execution. This is good enough for most interactive agent use cases. The sandboxes are purpose-built for AI agent code execution, so the boot path is optimized for exactly this scenario — spin up a minimal VM, run the code, return the result.</p>
<p><strong>Modal</strong> has a split story. CPU functions cold start in 200-500ms, which is actually faster than E2B for simple workloads. GPU functions are a different story: 5-15 seconds for model loading. If your agent needs GPU inference (say, running a vision model on a screenshot), you&rsquo;ll want to use Modal&rsquo;s container pooling or scheduled functions to keep a warm instance ready. The $30/month free credits help offset the cost of keeping a GPU warm, but it&rsquo;s something you have to design for.</p>
<p><strong>Fly.io</strong> has the most interesting cold start story because of their suspend/resume model. A suspended VM resumes in 100-500ms — faster than either E2B or Modal for CPU workloads. A true cold start (first boot) takes 1-3 seconds. The key insight is that if you keep a VM suspended rather than destroyed, the resume is nearly instant. This makes Fly.io uniquely suited for agent workloads where the same sandbox is used repeatedly with gaps between executions.</p>
<p>The practical takeaway: if your agents make frequent, short-duration calls to the same sandbox, Fly.io&rsquo;s suspend/resume model saves you both latency and money. If your agents are stateless and ephemeral — spin up, run, destroy — E2B or Modal are better fits.</p>
<h2 id="persistence-session-based-vs-volume-based-vs-suspendresume">Persistence: Session-Based vs. Volume-Based vs. Suspend/Resume</h2>
<p>How your sandbox handles state across executions is the single most underestimated cost factor in agent infrastructure. I&rsquo;ve seen teams burn through thousands of dollars because they designed their agents around persistent state on a platform that charges per-execution.</p>
<p><strong>E2B</strong> is session-based. A sandbox session can last up to 1 hour on the Hobby plan or 24 hours on Pro. Within a session, the filesystem persists. Between sessions, everything is gone unless you explicitly export data. This works well for agents that complete their work within a single session — a coding agent that clones a repo, runs tests, and reports results in under an hour, for example. But if your agent needs to maintain state across days or weeks, you&rsquo;ll need to build your own persistence layer on top.</p>
<p><strong>Modal</strong> is stateless by default. Functions don&rsquo;t retain filesystem state between invocations. If you need persistence, you pay $0.09/GiB/month for Modal Volumes, with the first 1 TiB free. This is cheap for long-term storage but adds latency for frequent reads and writes. Modal&rsquo;s model works best for agents that process data in discrete chunks — pull input from a volume, process, write results back — rather than agents that maintain a long-running interactive session.</p>
<p><strong>Fly.io</strong> supports both ephemeral storage (6GB included per VM) and persistent volumes. The suspend/resume model means a VM&rsquo;s in-memory state is preserved across suspensions. This is the closest you&rsquo;ll get to a &ldquo;persistent agent&rdquo; without building custom infrastructure. I&rsquo;ve run agents that maintain database connections, in-memory caches, and accumulated state across hours of wall-clock time with no additional cost beyond the $0.15/month suspension fee.</p>
<h2 id="total-cost-for-real-workloads">Total Cost for Real Workloads</h2>
<p>Let me give you three concrete scenarios based on workloads I&rsquo;ve actually run.</p>
<p><strong>Scenario 1: Interactive coding agent (CPU-only, 500 executions/day, 30 seconds each)</strong></p>
<ul>
<li>E2B Pro ($150/month) + usage: ~$0.000028/s × 30s × 500 = $0.42/day = ~$12.60/month. Total: ~$163/month.</li>
<li>Modal (no subscription): ~$0.0000131/core/s × 30s × 500 = $0.20/day = ~$6/month. Total: ~$6/month (under $30 free tier).</li>
<li>Fly.io (performance-1x 2GB): ~$0.00001056/s × 30s × 500 = $0.16/day = ~$4.80/month. Total: ~$5/month.</li>
</ul>
<p>For this workload, Modal and Fly.io are dramatically cheaper because there&rsquo;s no subscription overhead. E2B&rsquo;s $150/month Pro tier only makes sense if you need the 24-hour session limit or 100+ concurrent sandboxes.</p>
<p><strong>Scenario 2: Batch data processing (CPU-only, 10,000 executions/day, 5 seconds each)</strong></p>
<ul>
<li>E2B Pro ($150/month) + usage: ~$0.000028/s × 5s × 10,000 = $1.40/day = ~$42/month. Total: ~$192/month.</li>
<li>Modal: ~$0.0000131/core/s × 5s × 10,000 = $0.66/day = ~$20/month. Total: ~$20/month.</li>
<li>Fly.io (performance-2x 4GB): ~$0.00002111/s × 5s × 10,000 = $1.06/day = ~$32/month. Total: ~$32/month.</li>
</ul>
<p>Modal wins on raw volume because there&rsquo;s no subscription floor. E2B&rsquo;s per-second rate is competitive, but the $150/month subscription dominates the total cost.</p>
<p><strong>Scenario 3: GPU inference agent (H100, 1,000 executions/day, 10 seconds each)</strong></p>
<ul>
<li>Modal H100: $0.001097/s × 10s × 1,000 = $10.97/day = ~$329/month.</li>
<li>E2B: No GPU tier publicly listed.</li>
<li>Fly.io: GPU available but pricing not public.</li>
</ul>
<p>If your agent needs GPU, Modal is essentially the only option with transparent pricing. E2B and Fly.io either don&rsquo;t offer GPU or don&rsquo;t publish rates.</p>
<h2 id="when-to-pick-each-one">When to Pick Each One</h2>
<p>After running production workloads on all three, here&rsquo;s my honest assessment of where each provider fits.</p>
<p><strong>Choose E2B when</strong> you need purpose-built sandbox isolation for untrusted code execution, you&rsquo;re running long-lived agent sessions (up to 24 hours), and your workload justifies the $150/month Pro subscription. E2B&rsquo;s sandboxes are the most secure out of the box for running untrusted AI-generated code. If you&rsquo;re building a platform where users can submit arbitrary code, E2B should be your first look.</p>
<p><strong>Choose Modal when</strong> you need GPU compute, your workloads are bursty or variable, and you don&rsquo;t want to commit to a monthly subscription. Modal&rsquo;s GPU selection is unmatched — they offer everything from T4s to the latest B300s — and the pure per-second billing means you never pay for idle capacity. The $30/month free tier is generous enough for prototyping and light production use.</p>
<p><strong>Choose Fly.io when</strong> you&rsquo;re optimizing for cost at low utilization, you need suspend/resume for persistent agent state, and your workloads are CPU-only. Fly.io&rsquo;s free tier (3 always-free VMs) and absurdly cheap shared-CPU pricing make it the best choice for small-scale deployments and personal projects. The suspend/resume model is genuinely innovative for agent workloads.</p>
<h2 id="the-bottom-line">The Bottom Line</h2>
<p>The sandbox pricing landscape in 2026 has settled into three distinct models, and the right choice depends more on your workload pattern than on raw per-second rates. E2B and Modal are within spitting distance on CPU pricing, but E2B&rsquo;s subscription model changes the economics at scale. Fly.io is the cost leader for low-utilization CPU workloads but falls short on GPU and sandbox isolation.</p>
<p>If I were starting a new agent project today, I&rsquo;d default to Modal for anything involving GPU, Fly.io for CPU-only personal projects or low-traffic services, and E2B for production platforms that need to run untrusted code at scale. And I&rsquo;d keep an eye on Cloudflare Sandboxes — they reached GA in April 2026 and their <a href="/posts/cloudflare-outbound-workers-sandboxes-zero-trust-egress-guide-2026/">zero-trust egress model</a> is a compelling addition to the space, especially for teams that already run on Cloudflare.</p>
<p>For a deeper look at how E2B compares with other purpose-built sandbox providers, check out my <a href="/posts/e2b-vs-daytona-vs-blaxel-2026/">E2B vs Daytona vs Blaxel comparison</a>. And if sandbox security is your primary concern — especially for AI-generated code — the <a href="/posts/crawl4ai-rce-sandbox-escape-guide-2026/">Crawl4AI RCE sandbox escape</a> is a sobering case study in why isolation matters.</p>
]]></content:encoded></item><item><title>E2B vs Daytona vs Blaxel: AI Agent Code Execution Sandbox Comparison 2026</title><link>https://baeseokjae.github.io/posts/e2b-vs-daytona-vs-blaxel-2026/</link><pubDate>Sun, 10 May 2026 00:00:00 +0000</pubDate><guid>https://baeseokjae.github.io/posts/e2b-vs-daytona-vs-blaxel-2026/</guid><description>E2B vs Daytona vs Blaxel: a head-to-head sandbox comparison covering cold starts, state persistence, pricing, and which provider fits which agentic work...</description><content:encoded><![CDATA[<p>On April 15, 2026, OpenAI shipped Agents SDK v2 with seven native sandbox providers baked directly into the framework — Blaxel, Cloudflare, Daytona, E2B, Modal, Runloop, and Vercel. That single release turned sandbox selection from a niche infrastructure decision into a routine engineering choice that every AI agent team now has to make. Three providers dominate early conversations: E2B, Daytona, and Blaxel. Each is production-ready, each has native SDK integration, and each is built around a fundamentally different runtime model. This article breaks down exactly where they diverge, which workload profile each one is optimized for, and how to pick the right one without running a month of expensive benchmarks.</p>
<h2 id="e2b-vs-daytona-vs-blaxel-2026-the-ai-agent-sandbox-comparison">E2B vs Daytona vs Blaxel 2026: The AI Agent Sandbox Comparison</h2>
<p>The AI agent sandbox market now has more viable options than at any point in its short history, and the differences between providers matter more than they did two years ago. In 2026, more teams ran production autonomous coding agents in a single quarter than in all prior years combined, and the infrastructure those agents run on directly determines cost, latency, and reliability at scale. The three providers shaping this conversation — E2B, Daytona, and Blaxel — share a common premise (isolated, reproducible execution environments for agents) but differ sharply on architecture. E2B is built on Firecracker microVMs for lightweight stateless execution. Daytona uses pre-warmed Docker snapshots tuned for developer workflows and per-second billing. Blaxel maintains persistent environment state enabling a 25ms resume time for paused agents. These are not marketing variations on the same underlying approach — they are genuinely different architectural bets on what agentic workloads look like at scale. Understanding those bets is the fastest path to a defensible infrastructure decision.</p>
<h2 id="e2b-firecracker-powered-lightweight-sandboxes-with-100-free-credits">E2B: Firecracker-Powered Lightweight Sandboxes with $100 Free Credits</h2>
<p>E2B gives every new account $100 in free credits — enough compute to run thousands of sandbox sessions at evaluation scale without a credit card. That alone makes it the de facto first sandbox most agent developers reach for, and the sub-second cold start time (typically under one second for a fresh Firecracker microVM) means the onboarding experience is immediately satisfying. E2B is built on Firecracker, the open-source microVM technology developed by AWS for Lambda and Fargate. Firecracker boots a minimal Linux kernel in milliseconds, provides hardware-level isolation without the overhead of a full VM, and tears down equally fast. For agent workloads, this maps directly to ephemeral code execution: an agent calls a tool, E2B spins up a sandbox, the tool runs, the result is returned, and the sandbox is discarded. No state accumulates between calls unless you explicitly manage it. The developer API is one of the cleanest in the market — Python and TypeScript SDKs with intuitive <code>Sandbox</code> objects, streaming stdout/stderr, file upload/download, and port exposure for running web services. E2B&rsquo;s documentation is thorough, the community is active, and the OpenAI Agents SDK v2 integration is first-class. For teams starting their agentic sandbox journey, E2B&rsquo;s combination of free credits, developer ergonomics, and sub-second startup makes it the lowest-risk entry point.</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-python" data-lang="python"><span style="display:flex;"><span><span style="color:#f92672">from</span> e2b_code_interpreter <span style="color:#f92672">import</span> Sandbox
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">with</span> Sandbox() <span style="color:#66d9ef">as</span> sandbox:
</span></span><span style="display:flex;"><span>    execution <span style="color:#f92672">=</span> sandbox<span style="color:#f92672">.</span>run_code(<span style="color:#e6db74">&#34;import pandas as pd; print(pd.__version__)&#34;</span>)
</span></span><span style="display:flex;"><span>    print(execution<span style="color:#f92672">.</span>text)
</span></span></code></pre></div><p>The stateless model means each sandbox run is independent by default. This is a feature for short-lived tasks and a limitation for long-running workflows that need persistent context. E2B does support longer-running sandboxes with timeouts up to 24 hours, but the underlying model is still closer to a function invocation than a persistent environment. For workloads that fit the ephemeral model — code interpretation, data analysis, single-session agent tasks — E2B is hard to beat on simplicity and cost efficiency.</p>
<h2 id="daytona-developer-first-sandboxes-with-integrated-workflows">Daytona: Developer-First Sandboxes with Integrated Workflows</h2>
<p>Daytona&rsquo;s headline number in 2026 is sub-90ms cold starts, with optimized production configurations reaching as low as 27ms — a meaningful improvement over E2B&rsquo;s sub-second baseline for latency-critical agent orchestration loops. Founded by the team that built Codeanywhere and backed by a $24M Series A in February 2026, Daytona approaches sandboxes through a developer-environment lens rather than a pure compute lens. Where E2B asks you to think of sandboxes as function invocations, Daytona asks you to think of them as persistent development environments with a full lifecycle: Running, Stopped, Archived, Deleted. Sandboxes auto-stop after 15 minutes of inactivity, auto-archive after seven days, and can be forked into parallel branches — all behaviors that map naturally to how developer workflows actually operate. The per-second billing model is a meaningful differentiator for teams with bursty or unpredictable workloads: you pay only for active compute seconds, not for minimum session durations or idle time. Daytona publishes SDKs for Python, TypeScript, Ruby, and Go, with LangChain packages on both npm and PyPI, and native Language Server Protocol support inside sandboxes enables code analysis (completions, diagnostics, go-to-definition) without additional infrastructure. For teams running coding agents that need to understand code structure — not just execute it — that LSP integration eliminates a class of tooling problems. Daytona also supports forking sandbox state into parallel branches, enabling multi-agent exploration patterns where different reasoning paths need isolated but identically-initialized environments.</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-python" data-lang="python"><span style="display:flex;"><span><span style="color:#f92672">from</span> daytona_sdk <span style="color:#f92672">import</span> Daytona
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>daytona <span style="color:#f92672">=</span> Daytona()  <span style="color:#75715e"># reads DAYTONA_API_KEY from env</span>
</span></span><span style="display:flex;"><span>sandbox <span style="color:#f92672">=</span> daytona<span style="color:#f92672">.</span>create()
</span></span><span style="display:flex;"><span>response <span style="color:#f92672">=</span> sandbox<span style="color:#f92672">.</span>process<span style="color:#f92672">.</span>code_run(<span style="color:#e6db74">&#34;print(&#39;hello from daytona&#39;)&#34;</span>)
</span></span><span style="display:flex;"><span>print(response<span style="color:#f92672">.</span>result)
</span></span><span style="display:flex;"><span>daytona<span style="color:#f92672">.</span>remove(sandbox)
</span></span></code></pre></div><p>The developer experience is notably strong for teams managing multiple concurrent environments. Daytona&rsquo;s sandbox lifecycle management, auto-cleanup behavior, and per-second billing combine into a model that works well for mid-length agent sessions — longer than a single function call but shorter than the multi-day workflows where Blaxel&rsquo;s persistence advantages become decisive.</p>
<h2 id="blaxel-persistent-sandboxes-with-25ms-resume-for-long-running-agents">Blaxel: Persistent Sandboxes with 25ms Resume for Long-Running Agents</h2>
<p>Blaxel&rsquo;s defining capability is a 25ms resume time for paused environments — fast enough that pause-resume cycles in an agent orchestration loop become operationally invisible. That number represents something most sandbox providers do not offer: genuinely persistent environment state. With E2B or a basic container platform, resuming a &ldquo;paused&rdquo; agent typically means cold-booting a new sandbox and reconstructing context from an external store. With Blaxel, the environment itself holds the state — filesystem contents, in-memory data, running processes, and network configuration all persist between sessions. When an orchestrator resumes an agent, it picks up in under 30ms from exactly the state it left in. This architecture was built for a specific workload profile that is increasingly common in 2026: autonomous agents that operate in phases, check in for human review between phases, and need to resume into a deterministic environment without re-running expensive setup steps. Blaxel became one of the seven native providers in the OpenAI Agents SDK v2 on April 15, 2026, which eliminated the custom adapter work previously required to route SDK-managed agents to Blaxel sandboxes. For enterprise teams running production autonomous coding agents — multi-phase refactoring pipelines, multi-day research agents, browser automation with authenticated session state — Blaxel&rsquo;s 25ms resume and native state persistence remove an entire category of infrastructure complexity that stateless sandbox users build manually. The platform exposes REST APIs and Python/TypeScript SDKs following the same ergonomic patterns as E2B and Daytona, making the migration surface manageable for teams switching from a stateless model.</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-python" data-lang="python"><span style="display:flex;"><span><span style="color:#75715e"># Blaxel sandbox with persistent state</span>
</span></span><span style="display:flex;"><span><span style="color:#f92672">import</span> blaxel
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>env <span style="color:#f92672">=</span> blaxel<span style="color:#f92672">.</span>Environment<span style="color:#f92672">.</span>get(<span style="color:#e6db74">&#34;my-agent-env&#34;</span>)
</span></span><span style="display:flex;"><span>env<span style="color:#f92672">.</span>resume()  <span style="color:#75715e"># 25ms resume from last checkpoint</span>
</span></span><span style="display:flex;"><span>result <span style="color:#f92672">=</span> env<span style="color:#f92672">.</span>run(<span style="color:#e6db74">&#34;python agent_phase_2.py&#34;</span>)
</span></span><span style="display:flex;"><span>env<span style="color:#f92672">.</span>pause()   <span style="color:#75715e"># checkpoint state before waiting for review</span>
</span></span></code></pre></div><p>The persistence model requires a mental shift: design your agent to treat the Blaxel environment like a container that will still be running when you come back, not like a clean-room execution context that gets wiped on each invocation. For teams whose agents genuinely need that persistence, Blaxel removes the need to build external state serialization infrastructure. For teams whose agents complete in a single session, Blaxel&rsquo;s architectural advantages are irrelevant and the cost model is unfavorable compared to E2B or Daytona.</p>
<h2 id="performance-benchmarks-cold-start-throughput-and-state-management">Performance Benchmarks: Cold Start, Throughput, and State Management</h2>
<p>Performance comparison across these three providers requires separating two distinct latency concepts: cold start time (how long until a fresh sandbox accepts its first instruction) and resume time (how long until a paused sandbox is back to full operating state). These are measured differently and matter for different workloads. For cold starts, Daytona leads with sub-90ms measured p99 and 27ms in optimized configurations. E2B follows with sub-second cold starts using Firecracker microVMs — typically in the 150–500ms range depending on image complexity. Blaxel&rsquo;s cold start for a brand-new environment is comparable to E2B, but its resume time of 25ms for paused environments is the fastest in the market for the warm-state scenario. For throughput on stateless workloads — running many independent sandbox sessions concurrently — all three providers scale horizontally without hard limits at the session count levels most teams operate at in 2026. E2B&rsquo;s Firecracker architecture is particularly efficient here: microVMs have a smaller memory footprint than full containers, so E2B can pack more concurrent sandboxes onto the same underlying hardware. For state management overhead, the difference is stark. E2B and Daytona require external state stores (S3, Redis, a database) if you want context to persist across sandbox sessions. Blaxel&rsquo;s persistence is native: no external store required, no serialization code to write, no deserialization logic to maintain. For workloads with complex state — large file trees, running databases, accumulated in-memory data — the engineering cost of external state management with E2B or Daytona is non-trivial and often underestimated at the outset of a project.</p>
<table>
	<thead>
			<tr>
					<th>Provider</th>
					<th>Cold Start</th>
					<th>Resume (Warm)</th>
					<th>Billing</th>
					<th>Free Tier</th>
					<th>State Model</th>
			</tr>
	</thead>
	<tbody>
			<tr>
					<td>E2B</td>
					<td>~150–500ms</td>
					<td>N/A (stateless)</td>
					<td>Per usage</td>
					<td>$100 credit</td>
					<td>Ephemeral</td>
			</tr>
			<tr>
					<td>Daytona</td>
					<td>27–90ms</td>
					<td>Fast (snapshotted)</td>
					<td>Per-second</td>
					<td>Free tier</td>
					<td>Lifecycle-managed</td>
			</tr>
			<tr>
					<td>Blaxel</td>
					<td>~E2B range</td>
					<td>25ms</td>
					<td>Custom</td>
					<td>Contact sales</td>
					<td>Persistent</td>
			</tr>
	</tbody>
</table>
<p>One performance dimension that benchmarks rarely surface is compound latency across multi-step agent workflows. A 15-tool-call agent loop with 500ms cold starts per call accumulates 7.5 seconds of pure infrastructure latency. The same loop with 27ms Daytona cold starts adds under half a second total. For production agents running thousands of sessions per day, this differential compounds into meaningful cost and response-time differences.</p>
<h2 id="pricing-comparison-free-credits-vs-per-second-vs-subscription">Pricing Comparison: Free Credits vs Per-Second vs Subscription</h2>
<p>Pricing structures across the three providers reflect their different architectural models and target customers. E2B offers the most developer-friendly entry point: $100 in free credits for new accounts, with no credit card required to start evaluating. The free credits cover meaningful experimentation — thousands of sandbox-minutes at typical task lengths — and the pay-per-use model that follows is straightforward to reason about at scale. E2B charges by compute time, with pricing that varies based on sandbox size (CPU and memory configuration). For cost-sensitive workloads where sandboxes run for seconds to minutes per session and traffic is predictable, E2B&rsquo;s per-usage model is easy to budget. Daytona&rsquo;s per-second billing is the strongest fit for bursty workloads with irregular session lengths. Per-second billing eliminates the over-payment problem that minute-minimum billing creates: a 3-second sandbox session costs 3 seconds of compute, not 60. For teams running many short-lived sandboxes with unpredictable runtimes, the cost efficiency advantage over minimum-increment competitors is real and compounds at scale. Daytona also offers a free tier, making it accessible for evaluation without the upfront $100 commitment of E2B. Blaxel&rsquo;s pricing is the least transparent of the three as of May 2026, reflecting the enterprise focus of its product positioning. Blaxel charges based on active persistent environments, compute resources allocated per environment, and resume frequency — a model that rewards teams whose agents actually use the persistence features. For teams with hundreds of concurrent long-running agents that pause and resume frequently, Blaxel&rsquo;s infrastructure cost can be lower than repeatedly cold-starting equivalent state with E2B, but this calculation requires workload-specific benchmarking to validate. Teams evaluating Blaxel should request pricing directly and run a parallel cost benchmark using E2B&rsquo;s $100 free credit as a baseline.</p>
<h2 id="when-to-use-each-sandbox-provider">When to Use Each Sandbox Provider</h2>
<p>The choice between E2B, Daytona, and Blaxel reduces to three questions: How long do your agents run? How much state do they accumulate? How frequently do they start fresh versus resume? If your agents complete work in a single session — a coding task, a data analysis job, a document review — and discard their environment afterward, <strong>E2B</strong> is the correct default. The $100 free credit, developer-friendly API, and sub-second cold starts make it the easiest sandbox to adopt, and the stateless model is not a limitation for workloads that have no state to preserve. E2B is also the right choice when cost predictability matters more than latency, and for teams building multi-tenant systems where tenant isolation at the VM level is a security requirement. Choose <strong>Daytona</strong> when your agents run in development-environment-style workflows: code environments that need to persist across tool calls within a session, teams managing many parallel sandboxes simultaneously, or workloads with bursty patterns where per-second billing delivers better unit economics than per-minute or per-session minimums. Daytona&rsquo;s LSP integration also makes it the better choice for coding agents that need semantic code understanding rather than just execution. If you are building agents that clone repositories, make changes across multiple files, run test suites, and iterate — all within a single session — Daytona&rsquo;s sandbox lifecycle model is more natural than E2B&rsquo;s ephemeral approach. Choose <strong>Blaxel</strong> when your agents genuinely pause and resume across multiple sessions, when reconstructing state from scratch is expensive enough to matter, and when you want to eliminate custom state serialization infrastructure. The canonical Blaxel workload is a multi-phase autonomous agent: phase one completes and pauses for human review, phase two resumes into the exact same environment hours or days later. Without Blaxel, teams build serialization/deserialization logic, checkpoint storage, and restore validation. With Blaxel, the environment persists and the 25ms resume eliminates the cold-boot penalty entirely.</p>
<p><strong>Summary decision matrix:</strong></p>
<ul>
<li>Stateless, short-lived agent tasks → <strong>E2B</strong></li>
<li>Dev-environment workflows, per-second billing, semantic code tooling → <strong>Daytona</strong></li>
<li>Long-running agents with pause/resume cycles and complex state → <strong>Blaxel</strong></li>
</ul>
<h2 id="getting-started-openai-agents-sdk-v2-sandbox-integration">Getting Started: OpenAI Agents SDK v2 Sandbox Integration</h2>
<p>The OpenAI Agents SDK v2, released April 15, 2026, is the fastest path to integrating any of the three providers, because it handles sandbox lifecycle management (provisioning, health checks, cleanup, retry logic) automatically through its native provider adapters. Selecting a sandbox provider is a configuration decision rather than an architecture decision when you start from the SDK. All three providers — E2B, Daytona, and Blaxel — require an API key from their respective platforms and a one-line configuration change in the SDK to activate. Here is the pattern for E2B, which works identically for Daytona and Blaxel by substituting the provider name:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-python" data-lang="python"><span style="display:flex;"><span><span style="color:#f92672">from</span> openai_agents <span style="color:#f92672">import</span> Agent, SandboxConfig
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#75715e"># E2B configuration</span>
</span></span><span style="display:flex;"><span>agent <span style="color:#f92672">=</span> Agent(
</span></span><span style="display:flex;"><span>    model<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;gpt-5&#34;</span>,
</span></span><span style="display:flex;"><span>    sandbox<span style="color:#f92672">=</span>SandboxConfig(provider<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;e2b&#34;</span>, api_key<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;e2b_your_key_here&#34;</span>),
</span></span><span style="display:flex;"><span>    tools<span style="color:#f92672">=</span>[code_interpreter],
</span></span><span style="display:flex;"><span>)
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#75715e"># Daytona — swap provider name, same pattern</span>
</span></span><span style="display:flex;"><span>agent <span style="color:#f92672">=</span> Agent(
</span></span><span style="display:flex;"><span>    model<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;gpt-5&#34;</span>,
</span></span><span style="display:flex;"><span>    sandbox<span style="color:#f92672">=</span>SandboxConfig(provider<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;daytona&#34;</span>, api_key<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;your_daytona_key&#34;</span>),
</span></span><span style="display:flex;"><span>    tools<span style="color:#f92672">=</span>[code_interpreter],
</span></span><span style="display:flex;"><span>)
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#75715e"># Blaxel — persistent sandboxes via same interface</span>
</span></span><span style="display:flex;"><span>agent <span style="color:#f92672">=</span> Agent(
</span></span><span style="display:flex;"><span>    model<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;gpt-5&#34;</span>,
</span></span><span style="display:flex;"><span>    sandbox<span style="color:#f92672">=</span>SandboxConfig(
</span></span><span style="display:flex;"><span>        provider<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;blaxel&#34;</span>,
</span></span><span style="display:flex;"><span>        api_key<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;your_blaxel_key&#34;</span>,
</span></span><span style="display:flex;"><span>        persistent<span style="color:#f92672">=</span><span style="color:#66d9ef">True</span>,
</span></span><span style="display:flex;"><span>        environment_id<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;my-agent-env&#34;</span>,
</span></span><span style="display:flex;"><span>    ),
</span></span><span style="display:flex;"><span>    tools<span style="color:#f92672">=</span>[code_interpreter],
</span></span><span style="display:flex;"><span>)
</span></span></code></pre></div><p>For teams not using the OpenAI Agents SDK v2, each provider exposes a standalone Python and TypeScript SDK with similar ergonomics. Start with E2B if you have no existing sandbox infrastructure — the $100 free credit covers a full evaluation cycle, the documentation is the most complete of the three, and switching to Daytona or Blaxel later is a contained migration. If you already know your agents will be long-running with frequent checkpoints, start with Blaxel and avoid building state serialization infrastructure you will have to remove later. For Daytona, the free tier and per-second billing make it easy to run a parallel benchmark against E2B before committing to either. The SDK&rsquo;s provider abstraction means you can benchmark all three on representative workloads by changing a single line of configuration, then pick the provider whose performance and pricing fit your production requirements.</p>
<hr>
<h2 id="faq">FAQ</h2>
<p><strong>Q: Can I use E2B, Daytona, and Blaxel with the same agent code?</strong></p>
<p>Yes. The OpenAI Agents SDK v2&rsquo;s <code>SandboxConfig</code> abstraction treats all seven native providers through the same interface. Switching between E2B, Daytona, and Blaxel is a configuration change — you substitute the provider name and API key without touching agent logic. For teams using provider-specific SDKs directly (not through the OpenAI Agents SDK), the APIs differ in method naming and lifecycle model, but the core operations (create, run, read output, destroy) are present on all three platforms with comparable ergonomics.</p>
<p><strong>Q: Is E2B&rsquo;s $100 free credit enough to evaluate it properly?</strong></p>
<p>Yes, for most teams. At E2B&rsquo;s typical per-compute-unit pricing, $100 covers thousands of sandbox-minutes — sufficient to run representative agent workloads, measure cold start latency at your task complexity level, and estimate monthly production costs with reasonable accuracy. The credit requires no credit card to claim, which removes the friction of expensing an evaluation. For unusually large-scale evaluations (millions of short sandbox sessions or very long-running tasks), you may exhaust the credit faster, but standard evaluation use cases are well within the $100 ceiling.</p>
<p><strong>Q: What happens to Blaxel state if my process crashes mid-run?</strong></p>
<p>Blaxel&rsquo;s persistent environment model checkpoints at the pause point — state that accumulated during an active session before a crash may not be fully preserved unless your agent explicitly checkpoints (pauses) at logical intervals. Blaxel recommends treating explicit <code>pause()</code> calls as checkpoints analogous to database commits: call them at the end of every completed logical phase, not just at the end of a full workflow. If a process crashes during an active phase, Blaxel can recover the last paused state, but work done after the last pause may be lost. This is a different failure model than stateless sandboxes (which lose everything on crash) but requires the same discipline around checkpoint placement.</p>
<p><strong>Q: How does Daytona&rsquo;s per-second billing compare to E2B&rsquo;s per-usage pricing in practice?</strong></p>
<p>The cost difference depends heavily on session length distribution. For very short sessions (under 10 seconds), Daytona&rsquo;s per-second billing often delivers better unit economics because it charges only for active seconds without minimum increments. For longer sessions (minutes to hours), the difference narrows and depends on the per-second rates each provider charges for your compute configuration. The most reliable approach is to run a parallel benchmark using Daytona&rsquo;s free tier and E2B&rsquo;s $100 credit on your actual workload distribution, then extrapolate to your expected monthly volume. Do not rely on list prices alone — compute configuration (CPU cores, memory) affects the per-second rate significantly, and the configuration that matches your workload may price differently than the baseline example in each provider&rsquo;s documentation.</p>
<p><strong>Q: Will Blaxel, Daytona, and E2B remain supported in future OpenAI Agents SDK versions?</strong></p>
<p>As of the April 2026 v2 release, all seven native providers — including all three covered in this article — are first-class supported providers in the SDK. OpenAI has not announced any plans to reduce the provider list in upcoming releases, and the modular provider architecture in SDK v2 is designed to add providers rather than remove them. That said, the sandbox infrastructure market is moving quickly: provider consolidation, acquisitions, or shifts in OpenAI&rsquo;s partnership strategy could affect the provider list on timescales of months to a year. The safest architectural posture is to keep sandbox provider selection isolated behind the <code>SandboxConfig</code> abstraction, which makes switching providers a configuration change rather than a code rewrite if your preferred provider&rsquo;s status changes.</p>
]]></content:encoded></item></channel></rss>