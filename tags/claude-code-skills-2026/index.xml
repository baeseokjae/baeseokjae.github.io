<?xml version="1.0" encoding="utf-8" standalone="yes"?><rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom" xmlns:content="http://purl.org/rss/1.0/modules/content/"><channel><title>Claude Code Skills 2026 on RockB</title><link>https://baeseokjae.github.io/tags/claude-code-skills-2026/</link><description>Recent content in Claude Code Skills 2026 on RockB</description><image><title>RockB</title><url>https://baeseokjae.github.io/images/og-default.png</url><link>https://baeseokjae.github.io/images/og-default.png</link></image><generator>Hugo</generator><language>en-us</language><lastBuildDate>Mon, 21 Sep 2026 07:08:55 +0000</lastBuildDate><atom:link href="https://baeseokjae.github.io/tags/claude-code-skills-2026/index.xml" rel="self" type="application/rss+xml"/><item><title>Autoprompt Skill Review 2026: A Coding-Agent Skill That Cuts Failures by 45%</title><link>https://baeseokjae.github.io/posts/autoprompt-skill-coding-agent-2026/</link><pubDate>Mon, 21 Sep 2026 07:08:55 +0000</pubDate><guid>https://baeseokjae.github.io/posts/autoprompt-skill-coding-agent-2026/</guid><description>Autoprompt is a coding-agent skill that orchestrates a plan-build-test-review-repair loop. This 2026 review examines the 45% failure reduction claim, real costs, and who should use it.</description><content:encoded><![CDATA[<p>Autoprompt is an open-source skill for coding agents that wraps planning, implementation, testing, review, and repair around tools like Claude Code, Codex, and OpenCode. According to the project&rsquo;s own benchmark run, it solved 73 of 89 Terminal-Bench 2.1 tasks versus a 60-task baseline, raising pass rate from 67.42% to 82.02% and cutting failures by 45%. The trade-off is roughly 3x execution time and 2x tokens, making it most valuable on hard, ambiguous tasks.</p>
<h2 id="what-is-autoprompt-a-multi-agent-orchestration-layer-for-coding-agents">What Is Autoprompt? (A Multi-Agent Orchestration Layer for Coding Agents)</h2>
<p>Autoprompt is not a single coding agent. It is an agent-skill orchestration layer that sits on top of existing coding tools and sequences a full development lifecycle inside one invocation. The core idea is separation of concerns: the same model is prevented from planning a task, implementing it, approving its own work, and then verifying that approval.</p>
<p>The design deliberately stops short of unlimited autonomy. The orchestration loop runs until a decision requires authority the skill does not hold — for example, choosing between two competing designs or confirming a destructive action — at which point it pauses and asks the human. Everything else, from scoping to verification, is delegated to sub-agents that can run code and check real outcomes.</p>
<p>Because Agent Skills load only when relevant, Autoprompt stays out of the way during ordinary requests. It must be invoked explicitly. On Codex you call <code>$autoprompt</code>; on Oh My Pi you use <code>/skill:autoprompt</code>. This opt-in model means the orchestration loop is isolated from everyday prompts rather than hijacking every query you make.</p>
<p>The broader context matters. As a Dev.to analysis of the most popular AI coding skills notes, agent skills went from a quiet Anthropic format to repositories with more stars than many of the frameworks we build on. Autoprompt is riding that wave, which itself grew out of the Agent Skills format Anthropic popularized.</p>
<h2 id="the-benchmark-claim-45-fewer-failures-on-terminal-bench-21">The Benchmark Claim: 45% Fewer Failures on Terminal-Bench 2.1</h2>
<p>The headline number behind the &ldquo;Autoprompt coding agent skill&rdquo; search comes from a single measured run published by the author. On Terminal-Bench 2.1, the raw harness solved 60 of 89 tasks (67.42%). With Autoprompt wrapping the same harness, the count rose to 73 of 89 (82.02%).</p>
<p>That is +13 solved tasks and +14.61 percentage points. Failures dropped from 29 tasks to 16, which is where the 45% figure originates (13 fewer failures on a base of 29 is roughly 44.8%).</p>
<p>Here is the data in one table:</p>
<table>
  <thead>
      <tr>
          <th>Metric</th>
          <th>Baseline (OpenCode 1.18.7)</th>
          <th>With Autoprompt</th>
          <th>Change</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>Tasks solved</td>
          <td>60 / 89</td>
          <td>73 / 89</td>
          <td>+13</td>
      </tr>
      <tr>
          <td>Pass rate</td>
          <td>67.42%</td>
          <td>82.02%</td>
          <td>+14.61 pp</td>
      </tr>
      <tr>
          <td>Failures</td>
          <td>29</td>
          <td>16</td>
          <td>-44.8% (~45%)</td>
      </tr>
      <tr>
          <td>Harness</td>
          <td>OpenCode 1.18.7</td>
          <td>OpenCode 1.18.7</td>
          <td>Same setup</td>
      </tr>
  </tbody>
</table>
<p>It is important to read this number honestly. This is one benchmark run, on one harness, published by the project&rsquo;s own author. The GitHub README is explicit that the measured run used OpenCode 1.18.7 and that DeepSeek&rsquo;s 82.7% on the leaderboard used its own separate setup and should be treated as a reference point only, not a comparable measurement. The result is strong evidence the design helps, not a promise you will reproduce exactly on your codebase.</p>
<h2 id="how-autoprompt-works-the-plan-build-test-review-repair-verify-loop">How Autoprompt Works: The Plan-Build-Test-Review-Repair-Verify Loop</h2>
<p>Under the hood, Autoprompt runs a layered pipeline that mirrors the way a small engineering team works. The loop, expressed as a sequence of stages, looks like this:</p>
<ul>
<li>Plan: a sub-agent scopes the task, producing a plan before any code is written.</li>
<li>Build: an implementation sub-agent writes the changes against that plan.</li>
<li>Test: the work is executed and tests are run against real output.</li>
<li>Review: a reviewer sub-agent reads the diff with evidence in hand.</li>
<li>Repair: if the reviewer or the test run finds a defect, a repair stage fixes it.</li>
<li>Verify: the loop repeats until tests pass and the reviewer agrees, or a human decision is needed.</li>
</ul>
<p>The &ldquo;judge-with-evidence&rdquo; property is what makes this different from a single agent that simply double-checks its own answer. The reviewer can actually run the code and disagree with the builder, so the verification is grounded in observable behavior rather than the model&rsquo;s confidence about itself.</p>
<p>This carries an important corollary that shapes where you should use it: the review-and-repair stages only add value when there is something concrete to run and check. On tasks with no testable output, half the loop has nothing to do. The value concentrates on problems where correctness can be demonstrated.</p>
<h2 id="the-real-cost-3x-time-and-2x-tokens">The Real Cost: 3x Time and 2x Tokens</h2>
<p>Accuracy does not come free. The expected trade-off, disclosed as a planning estimate rather than a measured figure, is roughly 3x wall-clock execution time and 2x token consumption. The author is explicit that timing and token logs were not retained, so these are planning estimates, not audited numbers.</p>
<p>Why is it so expensive? Every stage of the loop invokes the model again. A single task that one agent might complete in one pass becomes a sequence of plan, build, test, review, and possibly repair passes, each consuming tokens. Additional model calls, not faster reasoning, are what buy the accuracy.</p>
<p>That trade-off has a clear practical implication. If a task is trivial — a one-line fix, a rename, a mechanical edit — wrapping it in Autoprompt multiplies the cost for no benefit. The value is highest on hard, ambiguous, or easily-broken work where a failed attempt costs an expensive repair loop or rework downstream.</p>
<h2 id="which-coding-agents-does-autoprompt-support">Which Coding Agents Does Autoprompt Support?</h2>
<p>Autoprompt is not tied to one vendor. The repository reports nine audited coding agents:</p>
<table>
  <thead>
      <tr>
          <th>Agent</th>
          <th>Version floor</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>Claude Code</td>
          <td>2.1.219+</td>
      </tr>
      <tr>
          <td>Codex</td>
          <td>Current</td>
      </tr>
      <tr>
          <td>OpenCode</td>
          <td>1.18.7+</td>
      </tr>
      <tr>
          <td>Kilo Code</td>
          <td>7.4.22+</td>
      </tr>
      <tr>
          <td>VS Code</td>
          <td>1.133+</td>
      </tr>
      <tr>
          <td>Prime Agent</td>
          <td>Current</td>
      </tr>
      <tr>
          <td>Oh My Pi</td>
          <td>Current</td>
      </tr>
      <tr>
          <td>DeepSeek Harness</td>
          <td>Current</td>
      </tr>
      <tr>
          <td>Reasonix</td>
          <td>Current</td>
      </tr>
  </tbody>
</table>
<p>The measured benchmark run used OpenCode 1.18.7. Invocation differs slightly by host: Codex uses <code>$autoprompt</code>, while Oh My Pi uses <code>/skill:autoprompt</code>. The skill runs comfortably on modest hardware; the SSD Nodes walkthrough demonstrates it on a small VPS using <code>mode=custom max_subs=2</code> to keep concurrency low enough for the box.</p>
<h2 id="controlling-concurrency-modetokensaver-modewide-and-modecustom">Controlling Concurrency: mode=tokensaver, mode=wide, and mode=custom</h2>
<p>Because the orchestration loop fans out into multiple sub-agents, concurrency control is a first-class feature. Autoprompt exposes three modes:</p>
<ul>
<li><strong>mode=tokensaver</strong> caps the number of concurrent sub-agents at 6, balancing speed against token burn.</li>
<li><strong>mode=wide</strong> opens all lanes, maximizing parallelism at the highest token cost.</li>
<li><strong>mode=custom</strong> lets you set <code>max_subs=N</code> directly and pass <code>agents=</code> for explicit model routing, giving fine-grained control.</li>
</ul>
<p>For constrained environments, <code>mode=custom max_subs=2</code> is the recommended setting mentioned in the SSD Nodes walkthrough — enough parallelism to help accuracy while staying inside small-VPS memory and token budgets. Model routing via the <code>agents=</code> argument also lets you assign cheap models to mechanical stages and stronger models to planning or review.</p>
<h2 id="when-autoprompt-shines-and-when-it-doesnt">When Autoprompt Shines (and When It Doesn&rsquo;t)</h2>
<p>Community discussion, including a Reddit thread on having coding agents autonomously verify their own work, positions this class of tool as best suited to difficult tasks. The evidence supports a few clear boundaries.</p>
<p><strong>Use Autoprompt when:</strong></p>
<ul>
<li>The task is hard, ambiguous, or easy to get subtly wrong.</li>
<li>Correctness can be demonstrated (there is something to run and verify).</li>
<li>A failed first attempt would be expensive to repair manually.</li>
<li>You have token and time budget to spend on verification.</li>
</ul>
<p><strong>Skip it when:</strong></p>
<ul>
<li>The change is trivial or mechanical.</li>
<li>There is nothing concrete to run and check.</li>
<li>You are highly token- or latency-sensitive.</li>
<li>You need maximum throughput for many small, independent tasks.</li>
</ul>
<p>The honest summary from the research angles file is that the 45% reduction is one author-published run on one harness — evidence the design helps, not a number you will reproduce on any codebase. Budget the 3x time and 2x tokens into your decision, and reserve Autoprompt for problems where accuracy beats throughput.</p>
<h2 id="how-to-get-started-install-invoke-and-uninstall">How to Get Started (Install, Invoke, and Uninstall)</h2>
<p>Installation is a single interactive command:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-bash" data-lang="bash"><span style="display:flex;"><span>npm install -g autoprompt-skill <span style="color:#f92672">&amp;&amp;</span> autoprompt
</span></span></code></pre></div><p>The npm package <code>autoprompt-skill</code> is at version 1.0.4, has zero dependencies, and records roughly 535 weekly downloads. The project is MIT-licensed, which is worth noting if you plan to modify or embed it.</p>
<p>After installing, invoke it explicitly depending on your host — <code>$autoprompt</code> on Codex, <code>/skill:autoprompt</code> on Oh My Pi. Explicit invocation is deliberate: it keeps the orchestration loop isolated from ordinary requests. Uninstall follows standard npm behavior with <code>npm uninstall -g autoprompt-skill</code>.</p>
<p>Adoption is visible. The project reported roughly 1,170 GitHub stars as of August 2026 per the SkillsLLM scan and about 419 listings on a JavaScript GitHub radar at the time of the ArsEntev summary, so it is growing quickly as part of the broader agent-skill ecosystem.</p>
<h2 id="final-verdict-who-should-use-autoprompt-in-2026">Final Verdict: Who Should Use Autoprompt in 2026</h2>
<p>Autoprompt earns its attention for one reason: it addresses a real weakness of single coding agents, the blind spot where the same model plans, executes, and then approves its own work. By splitting those roles and making the reviewer run actual code, it builds genuine verification into the loop. The result, on the author&rsquo;s published run, is a meaningful accuracy gain.</p>
<p>But &ldquo;autoprompt coding agent skill&rdquo; reviews must be candid about the caveats. The headline figures come from one harness and one author-run. The 3x time and 2x token costs are estimates, not audited metrics. And the skill&rsquo;s value collapses on trivial tasks or any work with nothing to run.</p>
<p>The verdict for 2026: if you handle hard, verifiable coding tasks, have token budget to spare, and want a controlled, opt-in orchestration layer rather than a tool that rewrites every prompt, Autoprompt is worth adding to your stack. If your workload is mostly small, mechanical, or throughput-bound, spend your tokens elsewhere. Test it on one genuinely hard task first, measure the real time and token cost on your setup, and decide with your own numbers rather than a benchmark from the README.</p>
<h2 id="faq">FAQ</h2>
<p><strong>What is the autoprompt coding agent skill?</strong>
Autoprompt is an open-source Agent Skills module that wraps a plan, build, test, review, repair, and verify loop around coding agents like Claude Code, Codex, and OpenCode, running until the work passes or a human decision is required.</p>
<p><strong>Does Autoprompt really cut failures by 45%?</strong>
In the author&rsquo;s published run on Terminal-Bench 2.1, failures fell from 29 to 16 tasks, which is about 45% fewer. It is a single measured run on OpenCode 1.18.7 published by the author, so treat it as evidence the design helps rather than a universal guarantee.</p>
<p><strong>What are the costs of using Autoprompt?</strong>
The disclosed planning estimates are roughly 3x wall-clock time and 2x token consumption, driven by the extra model calls each pipeline stage makes. Timing and token logs were not retained, so these are estimates rather than measured figures.</p>
<p><strong>Which tools does Autoprompt work with?</strong>
The repository reports nine audited coding agents including Claude Code 2.1.219+, Codex, OpenCode 1.18.7+, Kilo Code 7.4.22+, VS Code 1.133+, Prime Agent, Oh My Pi, DeepSeek Harness, and Reasonix.</p>
<p><strong>How do I install Autoprompt?</strong>
Run <code>npm install -g autoprompt-skill &amp;&amp; autoprompt</code> interactively. The package is at version 1.0.4 with zero dependencies and is MIT-licensed.</p>
]]></content:encoded></item></channel></rss>