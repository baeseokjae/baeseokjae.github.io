<?xml version="1.0" encoding="utf-8" standalone="yes"?><rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom" xmlns:content="http://purl.org/rss/1.0/modules/content/"><channel><title>Multi-Session Workflows on RockB</title><link>https://baeseokjae.github.io/tags/multi-session-workflows/</link><description>Recent content in Multi-Session Workflows on RockB</description><image><title>RockB</title><url>https://baeseokjae.github.io/images/og-default.png</url><link>https://baeseokjae.github.io/images/og-default.png</link></image><generator>Hugo</generator><language>en-us</language><lastBuildDate>Wed, 16 Sep 2026 16:02:58 +0000</lastBuildDate><atom:link href="https://baeseokjae.github.io/tags/multi-session-workflows/index.xml" rel="self" type="application/rss+xml"/><item><title>Claude Code Session Messaging: How to Send Messages to Your Other Sessions</title><link>https://baeseokjae.github.io/posts/message-your-other-claude-code-sessions/</link><pubDate>Wed, 16 Sep 2026 16:02:58 +0000</pubDate><guid>https://baeseokjae.github.io/posts/message-your-other-claude-code-sessions/</guid><description>Claude Code cross-session messaging lets one terminal session tell another what changed. Learn how SendMessage, ListAgents, and @mentions work.</description><content:encoded><![CDATA[<p>If you run more than one Claude Code session at a time, you have probably played human courier: switching between terminals, copying a finding from one window, and pasting it into another. Claude Code session messaging, shipped in v2.1.224 on August 7, 2026, removes that copy-paste loop. One session can now tell another itself — by name — with a plain-text note that arrives at the other session&rsquo;s next tool round. This guide explains what cross-session messaging is, how to use it, what it can and cannot do, and how to coordinate parallel worktrees without becoming the messenger.</p>
<h2 id="why-your-claude-code-sessions-werent-talking-the-copy-paste-problem">Why Your Claude Code Sessions Weren&rsquo;t Talking (the Copy-Paste Problem)</h2>
<p>Before v2.1.224, every Claude Code session lived in its own communication bubble. Two terminals working on the same repository could not exchange information directly. If you renamed a database column in terminal A and expected the migration code in terminal B to compile, you had to physically relay that change yourself — highlight the snippet, switch windows, paste it, and hope the receiving session incorporated it correctly.</p>
<p>This is the core problem cross-session messaging eliminates. The most common scenario the research brief highlights is the &ldquo;two terminals, one renamed column&rdquo; case: one session makes a breaking schema change while another session waits on a long migration or test run in the same codebase. Rather than babysitting both windows, you describe the outcome you want in plain language and let Claude find the target session and write the note.</p>
<p>Messaging also solves three recurring coordination patterns: handing off a finding or rename across sessions, waiting on long-running migration or test work, and safely working the same codebase from two terminals at once. In every case the human is removed from the message loop — Claude composes and delivers the communication for you.</p>
<h2 id="what-cross-session-messaging-actually-is-a-note-not-context">What Cross-Session Messaging Actually Is (a Note, Not Context)</h2>
<p>The single most important distinction is that a session-to-session message is a plain text note, never the sender&rsquo;s conversation history, files, or context. When you ask one session to message another, only the message body travels.</p>
<p>This has a practical consequence: if you need to hand off full working context, messaging is the wrong tool. To transfer the entire context of a session, use <code>/resume</code> instead. Messaging is for notifications, findings, headers, and pointers — the short &ldquo;I changed X, you should know&rdquo; notes that keep parallel work synchronized without duplicating an entire conversation.</p>
<p>The recommended pattern is push the pointer, not the payload. If a session has produced a long artifact such as a schema, a design doc, or a test report, write that artifact to disk first, then send a short message containing the file path. The receiving session pulls the payload from disk. This keeps messages small, keeps files on your machine, and matches how SendMessage is designed to behave.</p>
<p>Because a message is a note rather than a full handoff, a send does not end with a delivered conversation. A message is delivered automatically — there is no polling — and is enqueued to be processed at the receiver&rsquo;s next tool round. If the receiving agent has completed already, sending it a message resumes it from its transcript, which makes messaging useful for waking a finished worker back into a task.</p>
<h2 id="requirements-before-you-start">Requirements: Before You Start</h2>
<p>Not every Claude Code installation can use cross-session messaging. Confirm these five requirements first:</p>
<ol>
<li><strong>Version.</strong> Cross-session messaging requires Claude Code v2.1.224 or later (shipped August 7, 2026). Cross-machine initiation additionally needs v2.1.225+. Native Windows support requires v2.1.234 or later.</li>
<li><strong>Operating system.</strong> Messaging works on macOS, Linux, and WSL 2. Native Windows support arrived later in v2.1.234. WSL2 and native Windows cannot reach each other because discovery is filesystem-based.</li>
<li><strong>Provider.</strong> Messaging is not available on Bedrock, AWS, GCP, or Foundry deployments. It requires the standard Anthropic-backed path.</li>
<li><strong>Feature flags.</strong> The feature-flag evaluation that enables messaging must stay enabled. Disabling the underlying eval removes discovery and sending.</li>
<li><strong>Shared filesystem for local messaging.</strong> Two sessions can message each other locally only when they share a filesystem, because discovery is filesystem-based (see below). A container session cannot reach a host session.</li>
</ol>
<p>If you tick all five boxes, the feature is on by default — there is nothing to enable.</p>
<h2 id="how-to-ask-one-session-to-message-another">How to Ask One Session to Message Another</h2>
<p>You do not compose a session-to-session message yourself. You describe the intent, and Claude calls the two tools that power the feature:</p>
<ul>
<li><strong>ListAgents</strong> discovers reachable sessions and agents. In the slash-command surface this appears as <code>/list-agents</code> (and historically <code>/peers</code>). It builds the list of names you can message.</li>
<li><strong>SendMessage</strong> delivers a message to a named target. The tool accepts a teammate name, the name &ldquo;main&rdquo;, any name returned by ListAgents, or a <code>worker [ref]</code> form for disambiguation.</li>
</ul>
<p>A typical instruction looks like: &ldquo;<em>Tell the session working on the migration that I renamed the <code>created_at</code> column to <code>created_on</code> in <code>schema.sql</code>.</em>&rdquo; Claude runs ListAgents to resolve the target, then calls SendMessage to deliver the note.</p>
<p>Two behaviors are worth noting. First, printing output does not communicate — SendMessage must actually be called; the message is pushed, not pulled. Second, if you deny the SendMessage tool, the ListAgents tool also disappears, and with it messaging to subagents and agent-team teammates. The two tools are coupled for safety.</p>
<h2 id="naming-sessions-and-the--mention-shortcut">Naming Sessions and the @-Mention Shortcut</h2>
<p>Names are the address in cross-session messaging. A session&rsquo;s name comes from an explicit <code>/rename</code> or the <code>--name</code> flag; otherwise Claude derives a name from the working folder. When two sessions would collide on the same name, Claude disambiguates them by working directory.</p>
<p>Because the name is the addressing mechanism, deliberate naming matters in multi-session workflows. Sessions you plan to coordinate should get intentional, stable names via <code>/rename</code> before you depend on messaging them. A session called &ldquo;migration-worker&rdquo; is far less likely to be confused than two sessions both defaulting to a folder-derived label.</p>
<p>To name a target directly in your prompt, use the <strong>@-mention typeahead</strong>, which arrived in v2.1.232 on August 13, 2026. As you type <code>@</code>, Claude suggests reachable session and agent names, and you pick the target inline. This replaces the need to describe a session indirectly and reduces the chance of messaging the wrong worker.</p>
<h2 id="what-happens-when-a-message-arrives-delivery--what-it-looks-like">What Happens When a Message Arrives (Delivery + What It Looks Like)</h2>
<p>When a message is sent to an active session, it is queued and delivered at the receiver&rsquo;s next tool round. The receiving agent sees a short note composed of one sentence, the sender&rsquo;s name, and a reply address — the message surface collapses to a Ctrl+O one-liner so its context footprint stays tiny.</p>
<p>Messages can also arrive proactive. Claude may send a note on its own when it notices that a change it made will affect another session, and a receiving session can be configured to auto-reply. This proactive behavior is what turns a set of independent sessions into something closer to a cooperating team.</p>
<p>Delivery limits keep the system safe against runaway chatter:</p>
<ul>
<li><strong>Rate limiting.</strong> The receiving session rate-limits repeated messages from a given sender.</li>
<li><strong>Duplicate suppression.</strong> Identical repeats within a short window are dropped.</li>
<li><strong>Queue cap.</strong> At most 50 accepted messages are queued.</li>
<li><strong>Idle-notice expiry.</strong> One-shot idle notices expire after 12 hours.</li>
<li><strong>Size cap.</strong> Same-machine messages are capped at roughly one million characters; the sending session refuses oversized messages before they leave.</li>
</ul>
<h2 id="the-safety-and-permission-model-what-a-message-cant-do">The Safety and Permission Model (What a Message Can&rsquo;t Do)</h2>
<p>A message carries information, not authority. The design treats this as a feature, and it is the reason you can let sessions message each other without opening a security hole. An incoming message:</p>
<ul>
<li><strong>Cannot approve a permission prompt.</strong> Sending a message never authorizes an action in the receiving session.</li>
<li><strong>Cannot change config or CLAUDE.md.</strong> Incoming messages cannot modify settings.</li>
<li><strong>Cannot execute slash commands.</strong> A <code>/compact</code> or any other slash command arrives as literal text and is never executed.</li>
<li><strong>Retains per-session permissions.</strong> The receiving session&rsquo;s own permission prompts and rules still apply to anything it chooses to do.</li>
</ul>
<p>The research brief&rsquo;s permission-laundering rule summarizes the model: never ask a peer to perform an action that is denied or blocked in your own session. Messaging is a communication channel, not a privilege escalation path — if you cannot do something, a peer cannot be used to do it around your restrictions.</p>
<h2 id="getting-a-notice-when-a-session-goes-idle">Getting a Notice When a Session Goes Idle</h2>
<p>Because you cannot watch every session, messaging is useful for getting a notice when a long-running worker changes state. You can ask a session to message you or another session when it finishes a migration, completes a test run, or otherwise goes idle. This converts babysitting into a notification: you stay on the board until the session reports in, and you do not have to poll for status.</p>
<p>The anti-loop throttle described above keeps this from becoming a livelock — repeated identical notices are rate-limited and deduplicated, so an idle agent cannot flood the channel.</p>
<h2 id="messaging-sessions-on-other-machines-and-cloud-remote-control">Messaging Sessions on Other Machines and Cloud (Remote Control)</h2>
<p>Cross-session messaging is not limited to one machine, but the transport differs and it costs an extra prerequisite.</p>
<p>Local, same-machine messages travel over a per-session Unix socket (a named pipe on Windows) and <strong>never pass through Anthropic servers</strong>. This is the privacy win: two sessions coordinating on your laptop exchange notes entirely on your machine.</p>
<p>Cross-machine and cloud messages route through Anthropic servers over <strong>Remote Control</strong>. To initiate a message to another machine:</p>
<ol>
<li>Run Claude Code <strong>v2.1.225 or later</strong>.</li>
<li>Sign in to claude.ai so the target is visible to Remote Control.</li>
<li>Ensure the target session is connected to Remote Control; cloud and other-machine sessions appear in the listing only while that connection is live.</li>
</ol>
<p>A session registered on a shared machine discovers peers by a JSON file and a per-user Unix socket bound when the session registers — that is the filesystem-based discovery that makes container-to-host messaging impossible and cross-machine messaging impossible without Remote Control.</p>
<h2 id="controlling-inbound-messages-crosssessioninbound-approval-isolatepeermachines">Controlling Inbound Messages (crossSessionInbound, Approval, isolatePeerMachines)</h2>
<p>You control what inbound messages a non-interactive session accepts. For unattended workers, you can set <code>--settings crossSessionInbound=accept</code> so a <code>-p</code> (non-interactive) session accepts incoming messages without an approval dialog. By default, interactive sessions can present an approval dialog for inbound requests, and a bare (non-interactive) mode binds no socket at all, so it can receive nothing.</p>
<p>The <code>isolatePeerMachines</code> setting restricts cross-machine peer visibility, and the standard per-session permission model still governs what a receiving session will actually do with a message. Combined, <code>crossSessionInbound</code> and the permission system let you make a worker either fully open to coordination or locked down, depending on the task&rsquo;s sensitivity.</p>
<h2 id="non-interactive-sessions-and-the-inbox-socket-claude_code_messaging_socket-hooks">Non-Interactive Sessions and the Inbox Socket (CLAUDE_CODE_MESSAGING_SOCKET Hooks)</h2>
<p>Beyond Claude-to-Claude messaging, you can post into a session from code. The <code>CLAUDE_CODE_MESSAGING_SOCKET</code> environment variable points at a session&rsquo;s inbox socket, and any hook or shell script that knows the variable can write into the session.</p>
<p>This is the automation leverage in the feature set. A CI script or a <code>post_hook</code> can push a status line straight into a session&rsquo;s message queue, letting your build pipeline talk directly to the agent working on the code instead of leaving a notice the agent must notice. Because the incoming message is still just a note, the same safety rules apply — a script posting to the inbox can announce work, but it cannot approve actions or change config.</p>
<h2 id="messaging-vs-the-other-multi-session-features-decision-table">Messaging vs the Other Multi-Session Features (Decision Table)</h2>
<p>Claude Code now offers several ways to coordinate sessions, and messaging is only one. Use this table to pick the right tool for the job.</p>
<table>
  <thead>
      <tr>
          <th>Feature</th>
          <th>Best for</th>
          <th>Carries full context?</th>
          <th>Cross-machine?</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>Cross-session messaging (SendMessage)</td>
          <td>Short notes, findings, pointers, wake-ups</td>
          <td>No — plain text only</td>
          <td>Yes, via Remote Control (v2.1.225+)</td>
      </tr>
      <tr>
          <td><code>/resume</code></td>
          <td>Handing off a full conversation</td>
          <td>Yes</td>
          <td>Locally / across terminals on same machine</td>
      </tr>
      <tr>
          <td>Agent teams</td>
          <td>Multiple agents collaborating as a named team</td>
          <td>Partial</td>
          <td>Depends on setup</td>
      </tr>
      <tr>
          <td><code>/list-agents</code> + naming</td>
          <td>Discovering and addressing peers</td>
          <td>—</td>
          <td>Lists connected sessions</td>
      </tr>
      <tr>
          <td>Remote Control</td>
          <td>Controlling sessions from another machine</td>
          <td>Yes</td>
          <td>Yes (requires claude.ai sign-in)</td>
      </tr>
      <tr>
          <td>Channels</td>
          <td>Dedicated communication surfaces</td>
          <td>Varies</td>
          <td>Yes</td>
      </tr>
  </tbody>
</table>
<p>The rule of thumb: if you need to move a decision or a notification, message it; if you need to move an entire conversation, <code>/resume</code> it; if you are building a persistent collaborating unit, use an agent team.</p>
<h2 id="common-problems-and-fixes-troubleshooting-table">Common Problems and Fixes (Troubleshooting Table)</h2>
<table>
  <thead>
      <tr>
          <th>Symptom</th>
          <th>Likely cause</th>
          <th>Fix</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>Sessions can&rsquo;t find each other on the same machine</td>
          <td>Container vs host, or WSL2 vs native Windows — no shared filesystem</td>
          <td>Run both sessions on a shared filesystem</td>
      </tr>
      <tr>
          <td>Cross-machine send fails</td>
          <td>v2.1.224, missing sign-in, or target not Remote Control-connected</td>
          <td>Upgrade to v2.1.225+, sign in to claude.ai, keep target connected</td>
      </tr>
      <tr>
          <td>SendMessage reports success but nothing arrives</td>
          <td>Delivery failure edge (fixed in v2.1.225)</td>
          <td>Upgrade to v2.1.225+ and re-send</td>
      </tr>
      <tr>
          <td>Messaging unavailable entirely</td>
          <td>Provider is Bedrock/AWS/GCP/Foundry, or feature-flag eval disabled</td>
          <td>Move to standard provider; re-enable the eval</td>
      </tr>
      <tr>
          <td>Can&rsquo;t message a peer</td>
          <td>SendMessage tool denied (removes ListAgents too)</td>
          <td>Grant SendMessage</td>
      </tr>
      <tr>
          <td>Wrong session addressed</td>
          <td>Name collision between folder-derived names</td>
          <td><code>/rename</code> sessions deliberately</td>
      </tr>
  </tbody>
</table>
<h2 id="wrap-up-stop-copy-pasting-between-terminals">Wrap-Up: Stop Copy-Pasting Between Terminals</h2>
<p>Claude Code session messaging, live since v2.1.224, finally turns your scattered terminals into a coordinated set of workers that can tell each other what changed, hand off findings, and wake one another up when long jobs finish. Remember the three rules that keep it safe and effective: a message is a note, not a full context handoff (use <code>/resume</code> for that); send the pointer, write the payload to disk; and never ask a peer to do what your own session cannot. With deliberate session names, the <code>@</code>-mention shortcut, and the message socket for hooks, the copy-paste loop between your own sessions is gone.</p>
<h2 id="faq">FAQ</h2>
<p><strong>What is Claude Code session messaging?</strong>
It is a feature, shipped in Claude Code v2.1.224 on August 7, 2026, that lets one Claude Code session send a short plain-text note to another session on the same machine (or across machines via Remote Control), eliminating manual copy-paste coordination between terminals.</p>
<p><strong>How do I send a message to another Claude Code session?</strong>
Describe the outcome in plain language and let Claude handle it. Claude uses ListAgents to find the reachable target session by name and SendMessage to deliver the note. For direct targeting, use the <code>@</code>-mention typeahead (v2.1.232+) in your prompt.</p>
<p><strong>Does a Claude Code message carry full context or files?</strong>
No. A message is plain text only and never includes the sender&rsquo;s conversation history or files. To move full context, use <code>/resume</code>; to move a long artifact, write it to disk and send a pointer in a short message.</p>
<p><strong>Is Claude Code messaging safe?</strong>
Yes, by design. An incoming message cannot approve a permission prompt, cannot change config or CLAUDE.md, and slash commands arrive as literal text that is never executed; the receiving session&rsquo;s own permissions still apply. Local messages travel over a Unix socket and never pass through Anthropic servers.</p>
<p><strong>What are the requirements for Claude Code cross-session messaging?</strong>
You need Claude Code v2.1.224 or later on macOS, Linux, or WSL 2 (native Windows needs v2.1.234+), a non-Bedrock/AWS/GCP/Foundry provider, the messaging feature-flag eval enabled, and a shared filesystem for local messaging. Cross-machine messaging additionally requires v2.1.225+ and a Remote Control connection with claude.ai sign-in.</p>
]]></content:encoded></item></channel></rss>