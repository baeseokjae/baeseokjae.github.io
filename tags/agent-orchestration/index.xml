<?xml version="1.0" encoding="utf-8" standalone="yes"?><rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom" xmlns:content="http://purl.org/rss/1.0/modules/content/"><channel><title>Agent Orchestration on RockB</title><link>https://baeseokjae.github.io/tags/agent-orchestration/</link><description>Recent content in Agent Orchestration on RockB</description><image><title>RockB</title><url>https://baeseokjae.github.io/images/og-default.png</url><link>https://baeseokjae.github.io/images/og-default.png</link></image><generator>Hugo</generator><language>en-us</language><lastBuildDate>Sun, 06 Sep 2026 22:01:18 +0000</lastBuildDate><atom:link href="https://baeseokjae.github.io/tags/agent-orchestration/index.xml" rel="self" type="application/rss+xml"/><item><title>Agent Workflow MCP Orchestrator: Building a Planner-Executor Multi-Agent System</title><link>https://baeseokjae.github.io/posts/agent-workflow-mcp-planner-executor-2026/</link><pubDate>Sun, 06 Sep 2026 22:01:18 +0000</pubDate><guid>https://baeseokjae.github.io/posts/agent-workflow-mcp-planner-executor-2026/</guid><description>Learn how to build a planner-executor agent workflow MCP orchestrator: split strategy from tool execution, connect agents via MCP tool servers, and scale.</description><content:encoded><![CDATA[<p>A planner-executor agent workflow MCP orchestrator splits a hard task into two roles: a planner that decomposes the goal into a step-by-step plan, and one or more executors that carry out each step with tool access, all connected through Model Context Protocol (MCP) tool servers. This pattern handles open-ended, path-dependent work that a linear pipeline cannot, because the planner can re-plan mid-run based on what executors return. It is the canonical multi-agent orchestration architecture for tasks where the full sequence of steps is not known in advance.</p>
<h2 id="what-is-the-planner-executor-pattern">What Is the Planner-Executor Pattern?</h2>
<p>The planner-executor pattern is a core multi-agent orchestration architecture in which a single &ldquo;planner&rdquo; agent owns strategy and a separate &ldquo;executor&rdquo; agent (or a pool of executors) owns tool execution. The planner receives the high-level goal, decomposes it into discrete steps, and hands each step to an executor. The executor performs the work — often calling tools, searching, or running code — and returns a condensed result. The planner then decides whether to proceed, adjust, or re-plan.</p>
<p>LangGraph documents planner-executor as a core agent architecture pattern: the planner decomposes a task into a plan, the executor carries out each step with tool access, and the planner can re-plan based on executor results, enabling dynamic, adaptive execution. It is well-suited for tasks where the full sequence of steps is not known in advance.</p>
<p>Anthropic&rsquo;s &ldquo;Building Effective Agents&rdquo; taxonomy places planner-executor as a specialization of the orchestrator-workers pattern. In that taxonomy, the orchestrator decomposes a task, delegates subtasks to worker agents, and synthesizes their results. The planner-executor variant makes the planning loop explicit: the planner is the orchestrator, and the executors are the workers, with a feedback loop that allows re-planning.</p>
<p>The key distinction from a simple prompt chain is the feedback loop. In a chain, each step feeds the next in a fixed order. In planner-executor, the planner observes executor output and can change the plan entirely — adding steps, removing dead ends, or spawning parallel executors.</p>
<h2 id="why-mcp-is-the-glue-for-multi-agent-orchestration">Why MCP Is the Glue for Multi-Agent Orchestration</h2>
<p>Model Context Protocol (MCP) is an open standard created by David Soria Parra and Justin Spahr-Summers, released under the MIT license, with the schema defined in TypeScript and published as JSON Schema. MCP provides a standardized way for LLM applications to connect to external tools, data sources, and context.</p>
<p>For multi-agent orchestration, MCP solves a portability problem. Without a standard, each agent framework invents its own tool interface, so a planner built in one framework cannot reuse tools written for another. MCP makes the tool layer portable: an MCP tool server exposes capabilities over a standard protocol, and any MCP-compatible agent — planner or executor — can call it.</p>
<p>This is the philosophy behind mcp-agent (lastmile-ai), a composable framework that implements every pattern from Anthropic&rsquo;s Building Effective Agents, including planner-executor and orchestrator-workers, in a composable way. Its stated philosophy: &ldquo;MCP is all you need to build agents, and simple patterns are more robust than complex architectures.&rdquo; mcp-agent manages the full lifecycle of MCP server connections and scales to sophisticated workflows built on Temporal for pause, resume, and recovery.</p>
<p>The practical benefit: you build your tool servers once as MCP servers, then both the planner and the executors consume them through the same protocol. Adding a new capability (a database, a search API, a code runner) means adding one MCP server, not rewriting agent glue.</p>
<h2 id="architecture-planner-executor-and-mcp-tool-servers">Architecture: Planner, Executor, and MCP Tool Servers</h2>
<p>A reference implementation of a multi-agent workflow orchestrator with MCP tool servers uses dedicated planner and executor agents connected through MCP. The architecture has three layers:</p>
<p><strong>Planner agent.</strong> Owns the goal, the plan, and the decision loop. It does not execute tools directly; it decomposes the task, dispatches steps, and synthesizes results. Its context window stays clean because it only receives condensed executor outputs.</p>
<p><strong>Executor agents.</strong> Own tool execution. Each executor runs in its own context window, explores freely, calls MCP tools, and returns a compressed summary to the planner. This is the compression mechanism Anthropic describes: subagents explore in isolated context windows and condense the most important tokens back to the lead agent.</p>
<p><strong>MCP tool servers.</strong> The shared capability layer. Both planner and executors connect to these servers over the MCP protocol. A tool server might wrap a search API, a database, a file system, or a code interpreter.</p>
<p>The separation of concerns is the point. The planner owns strategy; the executors own tool execution. This gives each role distinct tools, prompts, and exploration trajectories, which reduces path dependency — a failure in one executor&rsquo;s exploration does not corrupt the planner&rsquo;s plan or another executor&rsquo;s work.</p>
<table>
  <thead>
      <tr>
          <th>Layer</th>
          <th>Responsibility</th>
          <th>Context window</th>
          <th>Example tools</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>Planner</td>
          <td>Decompose, dispatch, re-plan, synthesize</td>
          <td>Clean, receives summaries only</td>
          <td>Planning, routing, evaluation</td>
      </tr>
      <tr>
          <td>Executor</td>
          <td>Execute steps, call tools, explore</td>
          <td>Isolated per executor</td>
          <td>Search, code, DB, file I/O</td>
      </tr>
      <tr>
          <td>MCP tool server</td>
          <td>Expose capabilities over standard protocol</td>
          <td>N/A (stateless)</td>
          <td>Search API, database, runner</td>
      </tr>
  </tbody>
</table>
<h2 id="step-by-step-building-a-planner-executor-with-mcp">Step-by-Step: Building a Planner-Executor with MCP</h2>
<p>Here is a concrete path to build a planner-executor agent workflow MCP orchestrator.</p>
<p><strong>Step 1: Define the MCP tool servers.</strong> Identify the capabilities your executors need and wrap each as an MCP server. Start with the smallest set — a search tool and a code runner are enough for most research and automation tasks. Register each server so both planner and executors can discover it.</p>
<p><strong>Step 2: Build the executor.</strong> The executor is the simpler agent: it receives a single step, calls the relevant MCP tools, and returns a condensed result. Keep its prompt focused on execution, not strategy. Give it a hard output contract — a short summary, a list of findings, or a status — so the planner can parse it reliably.</p>
<p><strong>Step 3: Build the planner.</strong> The planner receives the goal, produces an initial plan, and enters a loop: dispatch a step to an executor, read the result, decide whether to proceed, adjust, or re-plan. The planner should be able to spawn multiple executors in parallel for independent steps, mirroring Anthropic&rsquo;s research system where a lead agent spawns parallel subagents to search simultaneously.</p>
<p><strong>Step 4: Wire the loop.</strong> Connect planner and executors through MCP. The planner calls an executor as if it were a tool, and the executor calls MCP tool servers as its own tools. This keeps the interface uniform: everything is a tool call over MCP.</p>
<p><strong>Step 5: Add re-planning.</strong> The critical differentiator. When an executor returns a result that invalidates part of the plan, the planner must be able to revise it. Implement a re-plan trigger: after each executor result, the planner evaluates whether the plan still holds and, if not, regenerates the remaining steps.</p>
<p><strong>Step 6: Test with a real task.</strong> Run the system on an open-ended task — a research question, a multi-step build, a data-analysis job — and observe where it stalls. Most failures come from ambiguous executor output contracts or a planner that re-plans too aggressively.</p>
<h2 id="best-practices-and-common-pitfalls">Best Practices and Common Pitfalls</h2>
<p><strong>Start simple.</strong> Anthropic&rsquo;s Building Effective Agents explicitly advises starting with the simplest pattern and adding complexity only when needed. Do not build a five-agent system when a single agent with tools suffices. The planner-executor pattern earns its complexity only on open-ended, path-dependent tasks.</p>
<p><strong>Give executors a strict output contract.</strong> The most common failure is an executor returning unstructured prose the planner cannot act on. Define a schema: findings, status, confidence, next-step suggestions. This makes the planner&rsquo;s decision loop reliable.</p>
<p><strong>Keep the planner&rsquo;s context clean.</strong> The whole point of executors is compression. If the planner accumulates raw executor output, you lose the benefit. Enforce that executors return condensed summaries, not full transcripts.</p>
<p><strong>Isolate executor context windows.</strong> Each executor should explore in its own context window. This is what gives you separation of concerns and reduces path dependency. A failure in one executor should not poison another.</p>
<p><strong>Bound the re-plan loop.</strong> A planner that re-plans on every result can thrash. Add a budget: a maximum number of re-plans, or a confidence threshold below which re-planning is allowed. Otherwise you trade a linear pipeline for an infinite loop.</p>
<p><strong>Manage MCP server lifecycle.</strong> If you use a framework like mcp-agent, it manages MCP server connections for you. If you build from scratch, handle connection, reconnection, and timeouts explicitly — a dropped tool server is a common silent failure in multi-agent systems.</p>
<p><strong>Watch for coordination overhead.</strong> Multi-agent systems introduce new challenges in coordination, evaluation, and reliability, as Anthropic notes. Every agent boundary is a place where information is lost or corrupted. Measure whether the added complexity is paying for itself.</p>
<h2 id="when-to-use-planner-executor-vs-other-agent-patterns">When to Use Planner-Executor vs. Other Agent Patterns</h2>
<p>Anthropic&rsquo;s taxonomy gives you a decision ladder. Use the simplest pattern that solves the problem:</p>
<table>
  <thead>
      <tr>
          <th>Pattern</th>
          <th>Best for</th>
          <th>When to reach for planner-executor instead</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>Prompt chaining</td>
          <td>Fixed, known sequence of steps</td>
          <td>When steps depend on intermediate results you cannot predict</td>
      </tr>
      <tr>
          <td>Routing</td>
          <td>Classify input, pick one path</td>
          <td>When the task needs multiple cooperating paths, not one</td>
      </tr>
      <tr>
          <td>Parallelization</td>
          <td>Independent subtasks, same input</td>
          <td>When subtasks are interdependent and need a coordinator</td>
      </tr>
      <tr>
          <td>Orchestrator-workers</td>
          <td>Decompose into independent subtasks</td>
          <td>When the decomposition itself is dynamic and needs re-planning</td>
      </tr>
      <tr>
          <td>Evaluator-optimizer</td>
          <td>Iterate on one output against a judge</td>
          <td>When the work is multi-step tool execution, not single-output refinement</td>
      </tr>
      <tr>
          <td>Planner-executor</td>
          <td>Open-ended, path-dependent, tool-heavy tasks</td>
          <td>This is the pattern</td>
      </tr>
  </tbody>
</table>
<p>Use planner-executor when the full sequence of steps is not known in advance and the work is tool-heavy. Use a simple orchestrator-workers when the decomposition is static. Use a single agent when the task is small enough that orchestration overhead exceeds the benefit.</p>
<p>The rule of thumb from Anthropic and mcp-agent is the same: simple patterns are more robust than complex architectures. Start with the simplest pattern that works, and only graduate to planner-executor when a linear or single-agent approach demonstrably fails on open-ended, path-dependent work.</p>
<h2 id="faq">FAQ</h2>
<p><strong>What is a planner-executor agent workflow MCP orchestrator?</strong>
It is a multi-agent architecture where a planner agent decomposes a goal into steps, executor agents carry out each step with tool access, and MCP tool servers provide a portable, standard capability layer connecting both roles. The planner can re-plan based on executor results.</p>
<p><strong>How does MCP help multi-agent orchestration?</strong>
MCP standardizes how agents connect to tools, data, and context. Because both planner and executors consume the same MCP tool servers through one protocol, tool capabilities become portable and reusable across agents and frameworks instead of being locked into one vendor&rsquo;s interface.</p>
<p><strong>What is the difference between planner-executor and orchestrator-workers?</strong>
Orchestrator-workers decomposes a task into independent subtasks and delegates them to workers. Planner-executor is a specialization that makes the planning loop explicit: the planner observes executor results and can re-plan dynamically, which suits tasks where the step sequence is not known in advance.</p>
<p><strong>When should I use planner-executor instead of a single agent?</strong>
Use it for open-ended, path-dependent, tool-heavy tasks where a single agent&rsquo;s context window would overflow or where a linear pipeline cannot adapt. For small or fixed tasks, a single agent or a simple chain is more robust and cheaper.</p>
<p><strong>What are the main pitfalls of the planner-executor pattern?</strong>
The main pitfalls are unstructured executor output the planner cannot parse, planner context bloat from raw results, unbounded re-planning loops, dropped MCP server connections, and coordination overhead that outweighs the benefit. Strict output contracts, isolated executor contexts, and re-plan budgets mitigate these.</p>
]]></content:encoded></item><item><title>DeepSeek Harness Agent Workflow Plugin: Orchestrate Multi-Step Agent Runs</title><link>https://baeseokjae.github.io/posts/dsh-agent-workflow-plugin-2026/</link><pubDate>Sat, 29 Aug 2026 16:01:16 +0000</pubDate><guid>https://baeseokjae.github.io/posts/dsh-agent-workflow-plugin-2026/</guid><description>The DeepSeek Harness Agent Workflow plugin visualizes multi-step agent runs as an execution chain, with token and cache stats for context-reuse analysis.</description><content:encoded><![CDATA[<p>The DeepSeek Harness Agent Workflow plugin (dsh-plugin-agent-workflow) is a standalone, installable Web UI plugin that adds a dedicated &ldquo;Workflow&rdquo; tab to DeepSeek Harness, letting you orchestrate and inspect multi-step agent runs as a clear execution chain. It renders your real recorded Session events — model requests, model responses, and tool calls — in chronological order, split by user conversation turn, with token and cache statistics that reveal exactly how context is reused across steps. Because it is read-only and non-invasive, it never adds messages, prompts, or tools to your requests, making it a trustworthy observability layer for anyone running complex, multi-step agent workflows on DeepSeek Harness.</p>
<h2 id="what-is-deepseek-harness-and-the-everything-is-a-plugin-philosophy">What is DeepSeek Harness and the &ldquo;Everything is a Plugin&rdquo; philosophy?</h2>
<p>DeepSeek Harness (dsh) is DeepSeek&rsquo;s open-source agent harness, currently in developer preview at version 0.1.0-rc.x. The project has roughly 203,000 GitHub stars, and its defining architectural idea is that &ldquo;everything is a plugin.&rdquo; Every capability — models, tools, skills, sessions, sandboxes, storage, loops, scheduling, and even the UI itself — is a swappable, recomposable plugin built on the Cordis kernel, which manages plugin mounting, unmounting, and dependencies.</p>
<p>The conceptual shift is significant compared to other harnesses. There is no separate manifest format per capability type; one plugin mechanism covers everything. A plugin is simply a JavaScript or TypeScript module that exports <code>apply(ctx, config)</code>. What that plugin registers determines what it becomes:</p>
<ul>
<li><code>ctx.tools.register()</code> makes it a tool</li>
<li><code>ctx.commands</code> makes it a slash command</li>
<li>A prompt section plus a tool makes it a skill</li>
<li>One plugin per MCP server connects an MCP server</li>
<li>A listener on <code>agent/pre-step</code> or <code>tools/pre-execute</code> makes it a lifecycle hook</li>
<li><code>ctx.llm.registerAdapter()</code> adds a new LLM provider</li>
<li><code>ctx.jobs</code> adds a background or cron job</li>
</ul>
<p>This means skills, commands, and MCP connections are all just code that calls <code>ctx</code>. The Agent Workflow plugin fits squarely into this philosophy: it is a UI plugin that registers a new tab and reads the session event stream to give you a visual, chronological view of your agent runs.</p>
<h2 id="introducing-the-agent-workflow-plugin-dsh-plugin-agent-workflow">Introducing the Agent Workflow plugin (dsh-plugin-agent-workflow)</h2>
<p>The dsh-plugin-agent-workflow plugin, developed by xuanyuanzhifeng, is a standalone installable DeepSeek Harness Web UI plugin that adds a &ldquo;Workflow&rdquo; tab alongside the built-in &ldquo;Conversation&rdquo; and &ldquo;Trajectory&rdquo; tabs. It has roughly 126 GitHub stars, is written in TypeScript, and is MIT-licensed.</p>
<p>Its core purpose is to make multi-step agent runs legible. Instead of forcing you to read raw logs or reconstruct what happened from a flat conversation, the plugin uses your user conversation turns as the entry point and renders model requests, model responses, and tool calls as a clear execution chain. The left panel organizes tasks by user conversation turn; the right panel shows model and tool calls chronologically; and the top summarizes counts and total time for the whole run.</p>
<p>The plugin is deliberately read-only and non-invasive. It visualizes real recorded Session events and never re-infers behavior from Harness source code. It adds no messages, prompts, or tools to your requests, which makes it a trust and observability layer rather than an active orchestrator. If you want to understand what your agent actually did across a long, multi-step run, this plugin gives you the ground truth.</p>
<h2 id="how-the-workflow-tab-visualizes-multi-step-agent-runs">How the Workflow tab visualizes multi-step agent runs</h2>
<p>The Workflow tab is organized around a two-panel layout that mirrors how you actually think about a run. On the left, tasks are grouped by user conversation turn — each turn is a distinct unit of work. On the right, the model and tool calls for the selected turn appear in chronological order. At the top of the view, a summary bar shows counts and total time, giving you an at-a-glance sense of how long the run took and how many steps it involved.</p>
<p>This structure is especially valuable for multi-step agent runs, where a single user request can trigger a long chain of reasoning, tool calls, subagent scheduling, and context injections. By grouping work by turn and then by call, the plugin lets you trace exactly which model call produced which tool call, and in what order. Independent scrolling for the turn list and the model-call list means you can keep a long chain in view while inspecting a specific step, and virtualized rendering keeps the interface responsive even for very long chains.</p>
<p>Because the data comes from real recorded Session events, the view is always accurate. DeepSeek Harness records everything the model sees in an append-only session log — system prompts, reasoning, tool calls and results, subagent scheduling, and context injections — and the Workflow tab reads that same event stream. You are never looking at a reconstruction; you are looking at the actual execution trace.</p>
<h2 id="inspecting-model-requests-responses-and-tool-execution-in-detail">Inspecting model requests, responses, and tool execution in detail</h2>
<p>Beyond the high-level chain, the plugin lets you drill into each individual step. Request details expose the real recorded system prompt, the provider-agnostic <code>messages[]</code> body, and the tool definitions as collapsible JSON trees with copy and zoom controls. This is useful when you need to verify exactly what context was sent to the model at a given step, or when you are debugging why a tool call was malformed.</p>
<p>Response inspection shows the reasoning, content, tool calls, and raw records for each model response. Tool execution states distinguish running, completed, and failed steps, and each tool call shows its parameters, results, duration, and an error summary when something went wrong. This makes it straightforward to spot a slow tool, a failed call, or a step where the model&rsquo;s reasoning diverged from what you expected.</p>
<p>For a multi-step agent run, this level of detail is the difference between guessing and knowing. When a run fails or produces an unexpected result, you can open the Workflow tab, find the exact step where things went sideways, and inspect the request, the response, and the tool execution state in one place.</p>
<h2 id="token-and-cache-statistics-for-context-reuse-analysis">Token and cache statistics for context-reuse analysis</h2>
<p>One of the most practical features of the Agent Workflow plugin is its token and cache statistics. For each step, the plugin splits token usage into input, uncached input, cache-read, cache-write, and output tokens. This breakdown is essential for context-reuse and cost analysis.</p>
<p>DeepSeek&rsquo;s pricing model rewards context reuse: tokens read from cache are dramatically cheaper than uncached input tokens. By seeing how many tokens were cache-read versus uncached input, you can tell whether your workflow is actually reusing context effectively across steps. A high cache-read ratio means your agent is building on prior context efficiently; a high uncached-input ratio suggests you are re-sending context that could be cached or trimmed.</p>
<p>This is particularly relevant for multi-step agent runs, where context accumulates across turns. The plugin&rsquo;s per-step breakdown lets you identify which steps are the most expensive, whether context is being reused as intended, and where you might restructure your workflow to improve cache hits and reduce cost.</p>
<h2 id="installing-the-plugin-local-tgz-and-github-install-paths">Installing the plugin (local .tgz and GitHub install paths)</h2>
<p>Installing the Agent Workflow plugin follows the standard DeepSeek Harness plugin workflow. The plugin is compatible only with <a href="mailto:dsh@0.1.0-rc.8">dsh@0.1.0-rc.8</a> (0.1.x), so you must be on that version. You can install from a local <code>.tgz</code> package or directly from GitHub:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-bash" data-lang="bash"><span style="display:flex;"><span>npx @deepseek-ai/dsh@0.1.0-rc.8 plugin --profile web add &lt;path-to.tgz&gt; --workspace-root
</span></span></code></pre></div><p>or, for a GitHub install pinned to a release tag:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-bash" data-lang="bash"><span style="display:flex;"><span>npx @deepseek-ai/dsh@0.1.0-rc.8 plugin --profile web add github:&lt;owner&gt;/&lt;repo&gt;#v0.1.1 --workspace-root
</span></span></code></pre></div><p>To remove the plugin, use <code>plugin remove</code>. The broader ecosystem uses a similar pattern — for example, the independent DSH Plugin Registry (dshplugin.app) documents installs as <code>dsh plugin --profile web add github:&lt;owner&gt;/&lt;repo&gt;</code>. Because DSH is pre-release, always pin your plugin version to the matching RC version of the harness.</p>
<h2 id="compatibility-versioning-and-known-limitations">Compatibility, versioning, and known limitations</h2>
<p>The most important thing to understand about the Agent Workflow plugin is that it is compatible only with <a href="mailto:dsh@0.1.0-rc.8">dsh@0.1.0-rc.8</a> (0.1.x). DeepSeek Harness is pre-release, which means client interfaces can change across RC versions. A plugin built against one RC may break on the next, so you must track plugin versions against your harness version.</p>
<p>The plugin is also read-only by design. It does not orchestrate agent runs; it visualizes them. If you need active orchestration — assigning work, managing dependencies, or running multi-model teams — you will want a different tool (see the comparison below). And because it reads the session event stream, its fidelity depends on the harness recording those events correctly, which is a core DeepSeek Harness guarantee but still worth noting for pre-release software.</p>
<h2 id="how-it-compares-to-other-deepseek-harness-workflow-plugins">How it compares to other DeepSeek Harness workflow plugins</h2>
<p>The DeepSeek Harness plugin ecosystem has several workflow-oriented tools, and they serve different purposes. Here is how the main ones compare:</p>
<table>
  <thead>
      <tr>
          <th>Plugin</th>
          <th>Primary purpose</th>
          <th>Stars</th>
          <th>Language</th>
          <th>License</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>dsh-plugin-agent-workflow</td>
          <td>Per-turn execution chain visualization, read-only</td>
          <td>~126</td>
          <td>TypeScript</td>
          <td>MIT</td>
      </tr>
      <tr>
          <td>dsh-agent-team-gui</td>
          <td>Persistent multi-model agent teams with orchestration</td>
          <td>~159</td>
          <td>TypeScript</td>
          <td>MIT</td>
      </tr>
      <tr>
          <td>dsh-harness-one</td>
          <td>Visual multi-agent DAG orchestrator with live execution</td>
          <td>~17</td>
          <td>JavaScript</td>
          <td>—</td>
      </tr>
  </tbody>
</table>
<p>The Agent Workflow plugin is the observability choice: it shows you what happened, step by step, with token and cache stats. The dsh-agent-team-gui plugin is the orchestration choice: it lets you build persistent, reusable multi-model teams where each member gets its own model, role, fallback route, token limit, and tool policy, with dynamic workflow planning and bounded DAG execution. The dsh-harness-one plugin is a visual AI workflow orchestrator that builds multi-agent DAGs with live execution, recovery, and Feishu integration.</p>
<p>If your goal is to understand and optimize a single agent&rsquo;s multi-step run, the Agent Workflow plugin is the right fit. If your goal is to coordinate multiple specialized agents, look at dsh-agent-team-gui or dsh-harness-one. Many teams use both: an orchestrator to run the work, and the Workflow tab to inspect and optimize it.</p>
<h2 id="local-development-and-packaging-pnpm-typecheck-test-pack">Local development and packaging (pnpm, typecheck, test, pack)</h2>
<p>If you want to build or modify the plugin yourself, the development workflow is standard for a TypeScript plugin. The project requires Node ^22.19.0 or &gt;=24.0.0 and pnpm 11. The typical loop is:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-bash" data-lang="bash"><span style="display:flex;"><span>pnpm install
</span></span><span style="display:flex;"><span>pnpm typecheck
</span></span><span style="display:flex;"><span>pnpm test
</span></span><span style="display:flex;"><span>pnpm pack
</span></span></code></pre></div><p><code>pnpm pack</code> produces the <code>.tgz</code> file you can install with the <code>plugin add</code> command. Because the plugin is MIT-licensed and open source, you can fork it, adjust the visualization to your needs, and repackage it for your own workspace. The same &ldquo;everything is a plugin&rdquo; philosophy means your modifications remain a self-contained plugin rather than a fork of the harness itself.</p>
<h2 id="faq-and-troubleshooting">FAQ and troubleshooting</h2>
<p><strong>Is the DeepSeek Harness Agent Workflow plugin read-only?</strong>
Yes. It visualizes real recorded Session events and adds no messages, prompts, or tools to your model requests. It is a pure observability layer, so it cannot alter the behavior of your agent runs.</p>
<p><strong>Which version of DeepSeek Harness does the plugin require?</strong>
The plugin is compatible only with <a href="mailto:dsh@0.1.0-rc.8">dsh@0.1.0-rc.8</a> (0.1.x). Because DeepSeek Harness is pre-release, client interfaces may change across RC versions, so you must match the plugin version to your harness version.</p>
<p><strong>How is the Workflow tab different from the built-in Trajectory tab?</strong>
The Trajectory tab inspects the session log by source, while the Workflow tab organizes the same event stream by user conversation turn and renders model requests, responses, and tool calls as a chronological execution chain with token and cache statistics.</p>
<p><strong>Can the plugin help me reduce token costs?</strong>
Yes. It splits token usage into input, uncached input, cache-read, cache-write, and output tokens per step, so you can identify where context is not being reused and restructure your workflow to improve cache hits and lower cost.</p>
<p><strong>Does the plugin orchestrate multi-agent teams?</strong>
No. It visualizes and inspects runs. For active orchestration of multi-model teams, use a tool like dsh-agent-team-gui or dsh-harness-one, which handle team composition, dependency planning, and DAG execution.</p>
]]></content:encoded></item><item><title>Your Agents Should Be Multiplayer: Collaborative AI Workflows (2026)</title><link>https://baeseokjae.github.io/posts/multiplayer-agents-2026/</link><pubDate>Tue, 14 Jul 2026 10:00:00 +0000</pubDate><guid>https://baeseokjae.github.io/posts/multiplayer-agents-2026/</guid><description>Single-agent AI is hitting a wall. Here&amp;#39;s why the next frontier is agent teams — and the protocols, tools, and patterns that make multiplayer AI workflows work in production.</description><content:encoded><![CDATA[<p>I&rsquo;ve been running production AI agent systems for over a year now, and the single biggest shift I&rsquo;ve seen in 2026 is this: the best agents don&rsquo;t work alone. The teams getting real leverage out of AI aren&rsquo;t the ones with one super-agent — they&rsquo;re the ones running five, ten, or twenty specialized agents that talk to each other.</p>
<p>This isn&rsquo;t a prediction. It&rsquo;s already happening. Meta&rsquo;s HyperAgents paper (arXiv:2603.19461) proved that multi-agent systems can solve problems no single agent can touch. A production field study from Calx showed six agents building 82,000 lines of code in 20 days for $250. And the infrastructure to make this work — protocols, SDKs, open-source orchestrators — is already here, just not widely adopted yet.</p>
<p>Let me walk through what &ldquo;multiplayer AI&rdquo; actually means in practice, the stack that makes it work, and the hard lessons I&rsquo;ve learned about where it breaks.</p>
<h2 id="the-multiplayer-paradigm-shift">The Multiplayer Paradigm Shift</h2>
<p>Think about how you actually build software. You don&rsquo;t have one developer do everything. You have a frontend person, a backend person, someone on DevOps, a reviewer. They talk in Slack, leave PR comments, sync on architecture decisions. The whole is smarter than any individual.</p>
<p>AI agents work the same way — or at least, they should. A single agent has a context window, a tool set, and a personality (its system prompt). Ask it to do everything and you get a jack-of-all-trades that&rsquo;s mediocre at each task. Specialize your agents — one for research, one for code generation, one for review, one for deployment — and suddenly each one can be excellent at its job.</p>
<p>The gaming analogy is apt. We went from single-player (one agent, one session) to local multiplayer (multiple agents on the same machine) to online multiplayer (agents across machines, communicating over protocols). Most teams are still in the single-player era. The ones who&rsquo;ve moved to multiplayer are seeing 3-4x throughput improvements.</p>
<h2 id="the-emerging-protocol-stack-mcp--a2a">The Emerging Protocol Stack: MCP + A2A</h2>
<p>The biggest question when I started building multi-agent systems was: how do they talk to each other? The answer in 2026 is a two-protocol stack that&rsquo;s rapidly becoming standard.</p>
<p><strong>MCP (Model Context Protocol)</strong> — Anthropic&rsquo;s protocol for agents to access tools. Your agent talks to a database, a file system, an API. MCP is the &ldquo;how do I interact with the world&rdquo; layer.</p>
<p><strong>A2A (Agent-to-Agent Protocol)</strong> — Google&rsquo;s protocol, donated to the Linux Foundation in June 2025, for agents to talk to other agents. A2A is the &ldquo;how do I coordinate with my peers&rdquo; layer.</p>
<p>I&rsquo;ve written a <a href="/posts/mcp-vs-a2a-protocol-2026/">detailed comparison of MCP vs A2A</a> if you want the full technical breakdown, but the short version is: you need both. MCP without A2A means your agent can use tools but can&rsquo;t delegate work. A2A without MCP means your agents can talk but can&rsquo;t do anything useful.</p>
<p>In production, I run agents that expose both interfaces. A research agent exposes an A2A endpoint that other agents call with &ldquo;research this topic and return a brief.&rdquo; Internally, that agent uses MCP to query web search, read documentation, and write files. The calling agent never needs to know about those tools — it just sends an A2A message and gets a result back.</p>
<h2 id="what-hyperagents-and-the-calx-study-actually-tell-us">What HyperAgents and the Calx Study Actually Tell Us</h2>
<p>Meta&rsquo;s HyperAgents paper dropped in March 2026 and it&rsquo;s one of those papers that changes how you think about agent architecture. The headline finding: on math transfer tasks, HyperAgents achieved imp@50 = 0.630 where traditional transfer methods scored 0.0. That&rsquo;s not an incremental improvement — it&rsquo;s a phase change.</p>
<p>The key insight is that <strong>documentation is not behavior</strong>. You can write the most detailed system prompt in the world, but an agent won&rsquo;t actually follow it the way you expect. Meta found that executable mechanisms — actual code, rules enforced at runtime, behavioral guardrails — transfer agent improvement across domains. Written rules don&rsquo;t.</p>
<p>The Calx field study drives this home with real numbers. A team ran six AI agents that built 82,000 lines of code in 20 days for $250. Impressive, right? But when they transferred 237 correction rules to a new agent, it still made 44 new mistakes — 13 of which were in categories the rules explicitly covered. The rules were documented. The agent just didn&rsquo;t follow them.</p>
<p>This is the single most important lesson for multi-agent systems: <strong>don&rsquo;t trust prompts to enforce behavior</strong>. If you want an agent to follow a rule, make it executable. Use runtime validation, output guards, and tool-level constraints. A prompt is a suggestion. A code check is a guarantee.</p>
<h2 id="claude-codes-hidden-multi-agent-infrastructure">Claude Code&rsquo;s Hidden Multi-Agent Infrastructure</h2>
<p>Here&rsquo;s something that surprised me: Claude Code&rsquo;s agent SDK already ships with full multi-agent coordination built in. I&rsquo;m talking about in-process agent runners, mailbox messaging between agents, team-based architecture with team leads, and shutdown coordination. It&rsquo;s all there in <code>@anthropic-ai/claude-agent-sdk</code>.</p>
<p>Most people use Claude Code as a single-agent REPL. You type a prompt, it does a thing. But the SDK supports spawning child agents, routing messages between them, and coordinating work across a team of agents. The infrastructure for multiplayer agents is already in your node_modules — most people just haven&rsquo;t wired it up.</p>
<p>I&rsquo;ve been running a setup where a &ldquo;team lead&rdquo; agent receives a task, breaks it into subtasks, and dispatches each one to a specialized child agent. The research agent gathers context, the coding agent writes code, the review agent checks for issues, and the documentation agent writes the docs. They communicate through the SDK&rsquo;s mailbox system. The team lead handles conflicts and merges results.</p>
<p>If you&rsquo;re already using Claude Code, you&rsquo;re one config file away from a multi-agent setup. The SDK documentation covers the <code>AgentTeam</code>, <code>AgentRunner</code>, and <code>Mailbox</code> classes. Start there.</p>
<h2 id="the-open-source-ecosystem">The Open-Source Ecosystem</h2>
<p>The protocol stack and commercial SDKs are one path. But the open-source ecosystem is where the real experimentation is happening, and it&rsquo;s moving fast.</p>
<p><strong>OpenSwarm</strong> is an open-source multi-agent Claude CLI orchestrator that coordinates multiple Claude Code agents as a team with task distribution. It hit 34 points on Hacker News — strong community interest in this space. It&rsquo;s designed for Linear/GitHub workflows, which makes sense: those are the most common multi-agent use cases right now.</p>
<p><strong>Egregore</strong> tackles the isolation problem head-on. Every Claude Code session starts from scratch — no memory of what happened before. Egregore provides shared memory and coordination for multiplayer Claude Code sessions. If you&rsquo;ve ever had an agent redo work because it didn&rsquo;t remember what another agent already did, you know exactly why this matters.</p>
<p><strong>Muster</strong> takes a different approach entirely. It&rsquo;s a multi-agent product team built on markdown files. The coordination layer is literally <code>.md</code> files — agents write plans, specs, and status updates to markdown, and other agents read them. It&rsquo;s lightweight, zero-infrastructure, and surprisingly effective. I&rsquo;ve used this pattern myself for quick experiments before investing in a full protocol stack.</p>
<p>I covered the major frameworks in my <a href="/posts/multi-agent-framework-comparison-2026/">multi-agent framework comparison</a>, but the open-source tools above are worth watching because they&rsquo;re solving real pain points that the big frameworks don&rsquo;t address.</p>
<h2 id="real-world-use-cases">Real-World Use Cases</h2>
<p>Let me give you three patterns I&rsquo;ve seen work in production.</p>
<p><strong>Code review pipelines.</strong> One agent writes code, another reviews it. The reviewer agent has a different system prompt, different tools (security scanner, linter, style checker), and a different temperature setting. It catches things the writer agent misses because it&rsquo;s looking at the code from a different angle. I&rsquo;ve seen this catch subtle race conditions and security issues that a single agent would have missed.</p>
<p><strong>Document collaboration.</strong> Platforms like odocs are pioneering multiplayer doc editing where humans and AI agents edit together in real-time. Partial document updates preserve version history without full rewrites — a critical feature when you have multiple agents touching the same document. Agents can share context directly to other agents through the platform, which solves the &ldquo;I need to know what agent B already figured out&rdquo; problem.</p>
<p><strong>Research-to-code pipelines.</strong> A research agent gathers context, a planning agent creates a spec, a coding agent implements it, and a testing agent validates it. Each agent passes artifacts to the next through A2A messages or shared files. I run this exact pipeline for my blog&rsquo;s content production — a strategist agent identifies topics, a researcher gathers data, a writer drafts the post, and a publisher deploys it. Each agent is specialized and excellent at its one job.</p>
<h2 id="the-trust-layer-problem">The Trust Layer Problem</h2>
<p>Here&rsquo;s the hard truth: multi-agent systems introduce trust problems that single-agent systems don&rsquo;t have.</p>
<p>When you have one agent, you review its output. When you have ten agents, you can&rsquo;t review everything. You need automated trust mechanisms.</p>
<p>The Calx approach — tracking corrections and compiling them into enforced rules — is one solution. Every time a human corrects an agent&rsquo;s output, that correction becomes a rule that all agents in the system must follow. It&rsquo;s executable, not documented. It&rsquo;s enforced at the tool level, not the prompt level.</p>
<p>Another approach is the behavioral guardrail pattern. Each agent has a &ldquo;guard&rdquo; that validates its output before passing it to the next agent. The guard checks for policy violations, security issues, and format compliance. If the output fails, the agent retries with the guard&rsquo;s feedback. This is the pattern I use in production, and it catches about 95% of issues before they propagate.</p>
<p>The remaining 5% require human review. The trick is knowing which 5% — and that&rsquo;s where correction tracking becomes essential. Log every agent output, every human correction, and use that data to improve your guards.</p>
<h2 id="the-road-ahead">The Road Ahead</h2>
<p>Looking at 2027, I see three trends that will define multiplayer AI workflows.</p>
<p>First, <strong>protocol consolidation</strong>. MCP and A2A are winning, but there&rsquo;s still fragmentation in how agents discover each other, authenticate, and negotiate capabilities. Expect a registry layer — something like a DNS for agents — to emerge.</p>
<p>Second, <strong>behavioral guardrails become standard</strong>. The HyperAgents insight — documentation is not behavior — will drive a new category of tools that enforce agent behavior at runtime. Every production multi-agent system will have a guard layer.</p>
<p>Third, <strong>the cost curve flips</strong>. Right now, running multiple agents costs more than running one. But as models get cheaper and specialized small models outperform general large models on specific tasks, running a team of specialized agents will be cheaper than one generalist agent. The Calx study already hints at this: $250 for 82,000 lines of code across six agents. That math gets better every quarter.</p>
<p>If you&rsquo;re still running single-agent workflows, start experimenting with multiplayer patterns today. Wire up two agents that talk to each other. Add a reviewer agent to your code generation pipeline. Try the Claude Code SDK&rsquo;s team features. The infrastructure is ready. The protocols are stable. The only thing missing is your architecture.</p>
<p>For more context on how to set up parallel agent workflows, check out my <a href="/posts/multi-agent-coding-workflow-2026/">multi-agent coding workflow guide</a> — it covers the practical setup patterns I use daily.</p>
]]></content:encoded></item></channel></rss>