<?xml version="1.0" encoding="utf-8" standalone="yes"?><rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom" xmlns:content="http://purl.org/rss/1.0/modules/content/"><channel><title>MCP File Cache on RockB</title><link>https://baeseokjae.github.io/tags/mcp-file-cache/</link><description>Recent content in MCP File Cache on RockB</description><image><title>RockB</title><url>https://baeseokjae.github.io/images/og-default.png</url><link>https://baeseokjae.github.io/images/og-default.png</link></image><generator>Hugo</generator><language>en-us</language><lastBuildDate>Tue, 04 Aug 2026 20:38:27 +0000</lastBuildDate><atom:link href="https://baeseokjae.github.io/tags/mcp-file-cache/index.xml" rel="self" type="application/rss+xml"/><item><title>Cachebro: File Cache with Diff Tracking for AI Coding Agents — Cuts Token Usage by 26%</title><link>https://baeseokjae.github.io/posts/cachebro-file-cache-diff-ai-agents-2026/</link><pubDate>Tue, 04 Aug 2026 20:38:27 +0000</pubDate><guid>https://baeseokjae.github.io/posts/cachebro-file-cache-diff-ai-agents-2026/</guid><description>Cachebro is a drop-in MCP server that caches file reads and tracks diffs for AI coding agents, cutting token usage by 26% with zero configuration.</description><content:encoded><![CDATA[<p>Cachebro is an open-source MCP (Model Context Protocol) server that acts as a file cache with intelligent diff tracking for AI coding agents, reducing token consumption by an average of 26% in controlled benchmarks. It intercepts file read requests from tools like Claude Code, Cursor, and OpenCode, serves cached content when files are unchanged, and delivers compact diffs when files have been modified — all without any configuration or changes to how the agent operates.</p>
<h2 id="what-is-cachebro">What is Cachebro?</h2>
<p>Cachebro is a drop-in MCP server built by Glauber Costa, the founder and CEO of Turso. It sits between your AI coding agent and the filesystem, caching every file read and tracking changes using SHA-256 content hashing. When an agent requests a file it has already read, Cachebro serves the cached version instead of re-reading the file from disk. When a file has changed, it computes a diff and sends only the changed lines to the agent.</p>
<p>The core idea is simple: AI coding agents waste a significant portion of their token budget re-reading files they have already seen. Cachebro eliminates this waste by remembering what the agent has already consumed and only sending new or changed content.</p>
<p>Cachebro is distributed as an npm package and implements the Model Context Protocol, meaning any MCP-compatible agent discovers and adopts it automatically. There is no need to modify agent instructions, add tool descriptions, or configure anything beyond a single initialization command.</p>
<h2 id="the-token-waste-problem">The Token Waste Problem</h2>
<p>AI coding agents operate on a token budget. Every file they read, every line they process, and every response they generate consumes tokens. In a typical coding session, an agent may read the same file dozens of times — once to understand its structure, again when checking imports, again when verifying a change, and repeatedly across multiple tasks in the same session.</p>
<p>The numbers are striking. In a controlled A/B test on a 268-file TypeScript codebase using Claude Opus, Cachebro reduced total token consumption from 158,248 to 117,188 tokens — a 26% reduction. That is roughly one in every four tokens saved, purely by avoiding redundant file reads.</p>
<p>The waste compounds across sequential tasks. In the same benchmark, the first task saved 2,925 tokens, the second saved 15,571 tokens, and the third saved 35,355 tokens. As the cache warms and the agent works through more files, the savings grow. Over a three-task sequence, Cachebro saved 53,851 cumulative tokens out of 166,526 total consumed — 24% of the entire budget.</p>
<p>For teams running AI coding agents at scale, these numbers translate directly into cost savings. A 26% reduction in token usage means 26% lower API bills, faster task completion, and less context window pressure on the model.</p>
<h2 id="how-cachebro-works">How Cachebro Works</h2>
<p>Cachebro&rsquo;s architecture is built on three core components: content-addressed caching, diff-on-change detection, and per-session tracking.</p>
<h3 id="sha-256-content-hashing">SHA-256 Content Hashing</h3>
<p>Every file read by the agent is hashed using SHA-256. The hash serves as a content address — if the file content has not changed, the hash matches the cached entry, and Cachebro returns the cached content without touching the filesystem. This guarantees zero false positives: if the hash matches, the content is identical.</p>
<h3 id="turso-embedded-database">Turso Embedded Database</h3>
<p>Cachebro uses Turso&rsquo;s embedded database (a SQLite-compatible edge database) as its storage backend. The database stores cached file content, SHA-256 hashes, session state, and diff history. Turso was chosen for its performance characteristics — it is fast enough to serve cached reads with negligible latency while being lightweight enough to embed directly in the MCP server process.</p>
<h3 id="diff-on-change-architecture">Diff-on-Change Architecture</h3>
<p>When a file has been modified (the SHA-256 hash does not match), Cachebro does not send the entire file. Instead, it computes a diff between the cached version and the current version, sending only the changed lines to the agent. This is where the bulk of the token savings come from — large files with small edits only cost the agent the size of the diff, not the size of the file.</p>
<h3 id="per-session-tracking">Per-Session Tracking</h3>
<p>Cachebro maintains separate read pointers for each session. This means branch switching works correctly: if an agent switches from one branch to another, Cachebro recognizes that the session state has changed and re-reads files as needed. Each session tracks its own cache state independently, preventing cross-contamination between different agent contexts.</p>
<h3 id="token-estimation">Token Estimation</h3>
<p>Cachebro uses a token estimation formula of <code>ceil(characters * 0.75)</code> to approximate token counts. This is a standard approximation that closely matches the tokenization behavior of most modern LLMs, including Claude and GPT models.</p>
<h2 id="benchmark-results">Benchmark Results</h2>
<p>The benchmark data comes from a controlled A/B test on a 268-file TypeScript codebase using Claude Opus. The test compared token consumption across three sequential coding tasks with and without Cachebro enabled.</p>
<table>
	<thead>
			<tr>
					<th>Metric</th>
					<th>Without Cachebro</th>
					<th>With Cachebro</th>
					<th>Savings</th>
			</tr>
	</thead>
	<tbody>
			<tr>
					<td>Total tokens (3 tasks)</td>
					<td>158,248</td>
					<td>117,188</td>
					<td>41,060 (26%)</td>
			</tr>
			<tr>
					<td>Task 1 tokens saved</td>
					<td>—</td>
					<td>2,925</td>
					<td>2,925</td>
			</tr>
			<tr>
					<td>Task 2 tokens saved</td>
					<td>—</td>
					<td>15,571</td>
					<td>15,571</td>
			</tr>
			<tr>
					<td>Task 3 tokens saved</td>
					<td>—</td>
					<td>35,355</td>
					<td>35,355</td>
			</tr>
			<tr>
					<td>Cumulative tokens saved</td>
					<td>—</td>
					<td>53,851</td>
					<td>24% of 166,526 total</td>
			</tr>
			<tr>
					<td>Average tokens saved per task</td>
					<td>—</td>
					<td>~33,000</td>
					<td>—</td>
			</tr>
	</tbody>
</table>
<p>The pattern is clear: the savings increase with each successive task. Task 1 saves relatively few tokens because the cache is cold — the agent is reading files for the first time. By task 3, the cache is fully warmed, and the agent benefits from every previous read. The 35,355 tokens saved in task 3 alone represents a 36% reduction in that single task.</p>
<p>These results are particularly impressive because they come from a real codebase, not a synthetic benchmark. The 268-file TypeScript project represents a realistic development scenario, and the sequential task structure mirrors how developers actually use AI coding agents.</p>
<h2 id="installation-and-setup">Installation and Setup</h2>
<p>Cachebro is distributed as an npm package and can be installed with a single command.</p>
<h3 id="quick-start">Quick Start</h3>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-bash" data-lang="bash"><span style="display:flex;"><span>npx cachebro init
</span></span></code></pre></div><p>This command initializes Cachebro in the current directory, creating the necessary configuration and database files. After initialization, Cachebro is ready to use.</p>
<h3 id="auto-discovery-with-mcp-clients">Auto-Discovery with MCP Clients</h3>
<p>Because Cachebro implements the Model Context Protocol, MCP-compatible clients discover it automatically. The <code>npx cachebro init</code> command registers Cachebro in the MCP configuration file that tools like Claude Code, Cursor, and OpenCode read on startup.</p>
<p>For <strong>Claude Code</strong>, Cachebro is picked up automatically after initialization — no additional configuration needed.</p>
<p>For <strong>Cursor</strong>, Cachebro works through Cursor&rsquo;s MCP integration. Note that Cursor&rsquo;s auto-discovery of MCP servers is still evolving, and some users report needing to manually specify the MCP server path in Cursor&rsquo;s configuration.</p>
<p>For <strong>OpenCode</strong> and other MCP-compatible agents, the same auto-discovery mechanism applies. If auto-discovery does not work, you can manually add the Cachebro MCP server configuration to your agent&rsquo;s MCP settings file.</p>
<h3 id="verifying-installation">Verifying Installation</h3>
<p>After running <code>npx cachebro init</code>, you can verify that Cachebro is running by checking the cache status:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-bash" data-lang="bash"><span style="display:flex;"><span>npx cachebro status
</span></span></code></pre></div><p>This displays the current cache state, including the number of cached files, total tokens saved, and session information.</p>
<h2 id="mcp-tools-deep-dive">MCP Tools Deep Dive</h2>
<p>Cachebro exposes four MCP tools that agents can call. These tools are automatically available to any MCP-compatible agent after installation.</p>
<h3 id="read_file">read_file</h3>
<p>The primary tool for reading files. When an agent calls <code>read_file</code>, Cachebro checks its cache:</p>
<ul>
<li>If the file has not changed since the last read, the cached content is returned immediately — no filesystem access, no token cost for re-reading.</li>
<li>If the file has changed, a diff is computed and only the changed lines are returned.</li>
<li>Supports partial reads with <code>offset</code> and <code>limit</code> parameters, so even large file edits only cost the agent the size of the changed portion.</li>
</ul>
<h3 id="read_files">read_files</h3>
<p>A batch version of <code>read_file</code> that accepts multiple file paths in a single call. This is useful when an agent needs to check several files at once, such as when understanding a module&rsquo;s dependencies or reviewing a pull request. Each file is independently cached and diffed.</p>
<h3 id="cache_status">cache_status</h3>
<p>Returns the current state of the cache, including:</p>
<ul>
<li>Number of files cached</li>
<li>Total tokens saved</li>
<li>Cache hit rate</li>
<li>Per-session statistics</li>
<li>Database size</li>
</ul>
<p>This tool is useful for monitoring Cachebro&rsquo;s effectiveness and understanding how much token waste is being eliminated.</p>
<h3 id="cache_clear">cache_clear</h3>
<p>Resets the cache for the current session. This is useful when the agent needs to re-read files from scratch, such as after a significant refactor or when switching to a completely different context. Cachebro&rsquo;s per-session design means clearing one session does not affect others.</p>
<h2 id="sdk-and-programmatic-usage">SDK and Programmatic Usage</h2>
<p>Cachebro includes a TypeScript SDK for developers who want to integrate its caching capabilities into custom tools and workflows.</p>
<h3 id="typescript-sdk">TypeScript SDK</h3>
<p>The SDK provides programmatic access to Cachebro&rsquo;s caching engine, allowing developers to:</p>
<ul>
<li>Initialize and manage cache instances</li>
<li>Read files through the cache with automatic diff tracking</li>
<li>Query cache status and statistics</li>
<li>Clear cache entries programmatically</li>
<li>Configure cache behavior (session tracking, hash algorithm, storage backend)</li>
</ul>
<h3 id="use-cases-for-the-sdk">Use Cases for the SDK</h3>
<p>The SDK is useful for teams building custom AI coding tools, CI/CD pipelines that use AI agents, or any workflow where file caching with change detection would reduce token consumption. Because Cachebro is MIT-licensed, the SDK can be freely integrated into both open-source and proprietary projects.</p>
<h2 id="competitor-comparison">Competitor Comparison</h2>
<p>Cachebro is not the only tool in this space, but it stands out on several dimensions. Here is how it compares to similar projects.</p>
<table>
	<thead>
			<tr>
					<th>Feature</th>
					<th>Cachebro</th>
					<th>linksee-memory</th>
					<th>project-oracle</th>
					<th>agentpack</th>
					<th>TOON-context</th>
			</tr>
	</thead>
	<tbody>
			<tr>
					<td>GitHub Stars</td>
					<td>216</td>
					<td>13</td>
					<td>2</td>
					<td>22</td>
					<td>4</td>
			</tr>
			<tr>
					<td>Language</td>
					<td>TypeScript</td>
					<td>JavaScript</td>
					<td>Python</td>
					<td>TypeScript</td>
					<td>TypeScript</td>
			</tr>
			<tr>
					<td>License</td>
					<td>MIT</td>
					<td>MIT</td>
					<td>MIT</td>
					<td>MIT</td>
					<td>MIT</td>
			</tr>
			<tr>
					<td>File Caching</td>
					<td>Yes</td>
					<td>Yes</td>
					<td>Yes</td>
					<td>No</td>
					<td>No</td>
			</tr>
			<tr>
					<td>Diff Tracking</td>
					<td>Yes</td>
					<td>Yes</td>
					<td>Yes</td>
					<td>No</td>
					<td>No</td>
			</tr>
			<tr>
					<td>Per-Session Tracking</td>
					<td>Yes</td>
					<td>No</td>
					<td>Partial</td>
					<td>No</td>
					<td>No</td>
			</tr>
			<tr>
					<td>MCP Auto-Discovery</td>
					<td>Yes</td>
					<td>Yes</td>
					<td>Yes</td>
					<td>No</td>
					<td>Yes</td>
			</tr>
			<tr>
					<td>Partial Read Support</td>
					<td>Yes</td>
					<td>No</td>
					<td>No</td>
					<td>No</td>
					<td>No</td>
			</tr>
			<tr>
					<td>Token Optimization</td>
					<td>26% reduction</td>
					<td>Unspecified</td>
					<td>Unspecified</td>
					<td>Prompt caching</td>
					<td>30-60% format opt.</td>
			</tr>
			<tr>
					<td>Created</td>
					<td>Feb 2026</td>
					<td>Apr 2026</td>
					<td>Mar 2026</td>
					<td>—</td>
					<td>—</td>
			</tr>
			<tr>
					<td>Creator</td>
					<td>Turso CEO</td>
					<td>Individual</td>
					<td>Individual</td>
					<td>Individual</td>
					<td>Individual</td>
			</tr>
	</tbody>
</table>
<p><strong>linksee-memory</strong> is the closest competitor, offering a local-first agent memory MCP server with file diff caching. However, it has only 13 GitHub stars and lacks per-session tracking and partial read support.</p>
<p><strong>project-oracle</strong> provides stateful MCP with file caching and delta diffing, but has only 2 stars and is written in Python, which may be less convenient for the TypeScript-heavy MCP ecosystem.</p>
<p><strong>agentpack</strong> takes a different approach, focusing on routing tasks to relevant files rather than caching file reads. It supports prompt caching and compact context packs but does not provide file-level diff tracking.</p>
<p><strong>TOON-context</strong> optimizes data files into a more token-efficient format (30-60% improvement) but targets structured data rather than general code file caching.</p>
<p>Cachebro&rsquo;s combination of per-session tracking, partial read support, SHA-256 content addressing, and the credibility of its creator gives it a significant edge over these alternatives.</p>
<h2 id="who-built-it">Who Built It</h2>
<p>Cachebro was created by Glauber Costa, the founder and CEO of Turso. Costa&rsquo;s background gives Cachebro unusual credibility for a relatively new open-source project.</p>
<p>Before founding Turso, Costa worked on the Linux Kernel, contributed to ScyllaDB (the high-performance NoSQL database), and served as a Staff Engineer at Datadog. This systems infrastructure background is directly relevant to Cachebro&rsquo;s design — the tool is built with the same attention to performance, correctness, and reliability that you would expect from someone who has worked on operating systems and distributed databases.</p>
<p>Turso itself is a SQLite-compatible edge database platform, and Cachebro uses Turso&rsquo;s embedded database as its storage engine. This is not a coincidence — Cachebro serves as both a useful tool in its own right and a demonstration of what Turso&rsquo;s embedded database can do.</p>
<p>The project has attracted 216 GitHub stars and 19 forks as of August 2026, with 369 monthly npm downloads. It was first released on February 14, 2026, and the latest release is v0.2.2 from May 20, 2026.</p>
<h2 id="limitations-and-open-issues">Limitations and Open Issues</h2>
<p>Cachebro is a young project (v0.2.2) and has several limitations worth noting.</p>
<h3 id="windows-support">Windows Support</h3>
<p>Cachebro&rsquo;s current release has limited Windows support. The initialization command and MCP auto-discovery have been primarily tested on macOS and Linux. Windows users may need to manually configure the MCP server path.</p>
<h3 id="cursor-auto-discovery">Cursor Auto-Discovery</h3>
<p>While Cachebro works with Cursor through its MCP integration, auto-discovery is not as seamless as with Claude Code. Cursor&rsquo;s MCP server discovery mechanism is still evolving, and some users report needing to manually specify the Cachebro configuration.</p>
<h3 id="multi-chat-support">Multi-Chat Support</h3>
<p>Cachebro&rsquo;s per-session tracking works well for single-agent workflows, but multi-chat scenarios (where multiple agent instances operate in the same workspace) are less tested. Each chat session gets its own cache state, but the interaction between concurrent sessions has not been extensively benchmarked.</p>
<h3 id="token-estimation-accuracy">Token Estimation Accuracy</h3>
<p>The <code>ceil(characters * 0.75)</code> formula is an approximation. Actual token counts vary by model and tokenizer. While the approximation is close enough for practical purposes, the reported savings may differ slightly from what your specific model reports.</p>
<h3 id="maturity">Maturity</h3>
<p>As a v0.2.2 release, Cachebro is still in active development. The API and tool set may change between versions. The project is MIT-licensed and open to contributions, but users should expect some instability as the project matures.</p>
<h2 id="verdict">Verdict</h2>
<p>Cachebro addresses a real and measurable problem: AI coding agents waste a significant portion of their token budget re-reading files they have already seen. The benchmark data is compelling — a 26% reduction in token usage on a realistic codebase, with savings that compound across sequential tasks.</p>
<p>The zero-configuration setup is a major advantage. Because Cachebro implements the MCP protocol, agents discover and adopt it automatically. There is no need to modify agent instructions, add custom tools, or change workflows. The installation is a single command, and the benefits are immediate.</p>
<p>The creator&rsquo;s background adds credibility. Glauber Costa&rsquo;s experience with the Linux Kernel, ScyllaDB, and Datadog means Cachebro is built by someone who understands systems performance at a deep level. The choice of Turso&rsquo;s embedded database as the storage engine is both technically sound and strategically aligned.</p>
<p>The main caveat is maturity. Cachebro is at v0.2.2, and some features (Windows support, Cursor auto-discovery, multi-chat handling) are still evolving. For teams using Claude Code on macOS or Linux, the experience is smooth. For other configurations, some manual configuration may be needed.</p>
<p>For any team using AI coding agents regularly, Cachebro is worth adding to the workflow. The installation takes seconds, the token savings are real and measurable, and the tool is free and open-source. The 26% reduction in token usage translates directly into lower costs and faster task completion — a rare combination of immediate benefit and zero configuration cost.</p>
<h2 id="faq">FAQ</h2>
<h3 id="what-exactly-does-cachebro-do-for-ai-coding-agents">What exactly does Cachebro do for AI coding agents?</h3>
<p>Cachebro acts as a smart file cache between your AI coding agent and the filesystem. It remembers every file the agent has read, serves cached content when files are unchanged, and sends only the changed lines (diffs) when files have been modified. This eliminates the token waste caused by agents re-reading the same files repeatedly.</p>
<h3 id="how-much-can-cachebro-reduce-my-token-usage">How much can Cachebro reduce my token usage?</h3>
<p>In controlled benchmarks on a 268-file TypeScript codebase with Claude Opus, Cachebro reduced token consumption by 26% (from 158,248 to 117,188 tokens). Savings compound across sequential tasks — the third task in a sequence saved 35,355 tokens (36% reduction) as the cache warmed.</p>
<h3 id="does-cachebro-require-any-changes-to-my-agents-configuration">Does Cachebro require any changes to my agent&rsquo;s configuration?</h3>
<p>No. Cachebro implements the Model Context Protocol (MCP), so MCP-compatible agents like Claude Code, Cursor, and OpenCode discover and adopt it automatically after running <code>npx cachebro init</code>. No agent instructions, tool descriptions, or workflow changes are needed.</p>
<h3 id="is-cachebro-free-and-open-source">Is Cachebro free and open-source?</h3>
<p>Yes. Cachebro is released under the MIT license, meaning it is free to use, modify, and distribute. The source code is available on GitHub at github.com/glommer/cachebro, and the npm package can be installed with <code>npx cachebro init</code>.</p>
<h3 id="what-are-cachebros-main-limitations">What are Cachebro&rsquo;s main limitations?</h3>
<p>Cachebro is at version 0.2.2 and has limited Windows support, evolving Cursor auto-discovery, and less-tested multi-chat scenarios. The token estimation formula (<code>ceil(characters * 0.75)</code>) is an approximation that may vary by model. These limitations are expected to improve as the project matures.</p>
]]></content:encoded></item></channel></rss>