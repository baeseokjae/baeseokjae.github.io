<?xml version="1.0" encoding="utf-8" standalone="yes"?><rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom" xmlns:content="http://purl.org/rss/1.0/modules/content/"><channel><title>AI Code Attribution on RockB</title><link>https://baeseokjae.github.io/tags/ai-code-attribution/</link><description>Recent content in AI Code Attribution on RockB</description><image><title>RockB</title><url>https://baeseokjae.github.io/images/og-default.png</url><link>https://baeseokjae.github.io/images/og-default.png</link></image><generator>Hugo</generator><language>en-us</language><lastBuildDate>Mon, 31 Aug 2026 19:04:02 +0000</lastBuildDate><atom:link href="https://baeseokjae.github.io/tags/ai-code-attribution/index.xml" rel="self" type="application/rss+xml"/><item><title>Code as an Artifact: Versioning AI-Generated Code in the Agentic Era</title><link>https://baeseokjae.github.io/posts/code-as-an-artifact-ai-code/</link><pubDate>Mon, 31 Aug 2026 19:04:02 +0000</pubDate><guid>https://baeseokjae.github.io/posts/code-as-an-artifact-ai-code/</guid><description>Code is now a regenerable output of AI agents, not the end product. Learn how to version, attribute, and review AI-generated code with provenance-first workflows.</description><content:encoded><![CDATA[<p>Code as an artifact means treating the source you commit as a regenerable output of a process — the prompts, plans, and agent context that produced it — rather than the durable end product of software development. When AI agents generate most of your code, the spec and the conversation become the real artifact, and versioning must capture both the code and the process behind it. This guide explains why traditional Git falls short, how to attribute every line to AI or human, and which workflows and tools keep AI-generated code trustworthy at scale.</p>
<h2 id="what-does-code-as-an-artifact-actually-mean">What Does &ldquo;Code as an Artifact&rdquo; Actually Mean?</h2>
<p>For most of software history, code was the end product. You wrote it, reviewed it, shipped it, and it lived on as the canonical record of what your system does. Agentic LLMs have inverted that relationship. As one widely shared argument puts it, &ldquo;Agentic LLMs have changed what &lsquo;code&rsquo; means — it used to be the end product of the software lifecycle, but now LLMs write code and the spec becomes the durable artifact.&rdquo;</p>
<p>The shift is stark. In August 2026, Elon Musk argued that &ldquo;source code is on the verge of becoming like assembly. The next step is getting rid of source code entirely and just making an efficient binary directly with AI.&rdquo; Whether or not you agree that source code will disappear, the direction is clear: the human-authored, durable layer is moving up the stack. Your instructions, your prompt, and your context become the &ldquo;code&rdquo; — one higher level of abstraction, but still code.</p>
<p>This reframing matters for versioning. If code is a regenerable output, then committing only the output is like saving a compiled binary without the source. You lose the ability to understand why a change happened, to reproduce it, and to audit it. The artifact you should be versioning is the full chain: the intent, the plan, the prompt, the agent&rsquo;s reasoning, and the resulting diff.</p>
<h3 id="why-programming-languages-wont-disappear">Why Programming Languages Won&rsquo;t Disappear</h3>
<p>A common objection is that if AI writes everything, we can skip languages entirely. But programming languages exist to reduce ambiguity in natural-language specification — they make execution unambiguous. LLMs can trivially generate binaries, but what you assert the binary will do still depends on &ldquo;code&rdquo; at a higher level. Languages are the precision layer that lets a machine (or an agent) execute intent without guessing. So the artifact hierarchy becomes: intent → spec → prompt → code → binary, and each layer is a candidate for versioning.</p>
<h2 id="why-traditional-git-falls-short-for-ai-generated-code">Why Traditional Git Falls Short for AI-Generated Code</h2>
<p>Git was designed to version the artifact — the code — not the process that produced it. That worked when a human authored each commit and the commit message captured intent. With AI agents, the gap becomes critical.</p>
<table>
  <thead>
      <tr>
          <th>Capability</th>
          <th>Traditional Git</th>
          <th>What AI Code Needs</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>Captures the code diff</td>
          <td>Yes</td>
          <td>Yes</td>
      </tr>
      <tr>
          <td>Captures the prompt that produced it</td>
          <td>No</td>
          <td>Yes</td>
      </tr>
      <tr>
          <td>Captures the plan and agent reasoning</td>
          <td>No</td>
          <td>Yes</td>
      </tr>
      <tr>
          <td>Attributes lines to AI vs. human</td>
          <td>No</td>
          <td>Yes</td>
      </tr>
      <tr>
          <td>Survives squash, rebase, cherry-pick</td>
          <td>Partially</td>
          <td>Must preserve causality</td>
      </tr>
      <tr>
          <td>Explains &ldquo;why&rdquo; behind a change</td>
          <td>Via commit message</td>
          <td>Via full exchange record</td>
      </tr>
  </tbody>
</table>
<p>The core problem: Git captures the artifact but not the process. A PR summary cannot capture the tradeoffs, alternatives, and dead ends an agent explored. When natural language (prompting and planning) becomes the new &ldquo;programming language,&rdquo; only putting the generated code up for review is incomplete — you are reviewing the output of a process you cannot see.</p>
<h3 id="the-squash-problem">The Squash Problem</h3>
<p>Git&rsquo;s history-flattening operations — squash, rebase, cherry-pick — destroy the micro-steps that explain how code evolved. For human code this was acceptable because the final commit message summarized intent. For AI code, the intermediate steps often contain the reasoning that makes the final diff comprehensible. A provenance system must survive these operations, which is why the design goal is to preserve causality before commit flattening happens.</p>
<h2 id="the-provenance-problem-who-changed-this-line-ai-or-human">The Provenance Problem: Who Changed This Line, AI or Human?</h2>
<p>The most concrete new requirement is line-level attribution. Teams need to answer a simple question: &ldquo;Who changed this line, AI or human, and why?&rdquo; This is not just an audit nicety — it drives review priority, liability decisions, security triage, and debugging.</p>
<h3 id="the-dual-layer-provenance-model">The Dual-Layer Provenance Model</h3>
<p>Git-native provenance systems like SpecStory recommend a dual-layer data model:</p>
<ol>
<li><strong>Attribution map</strong> — stored in Git Notes for a fast blame path. This answers &ldquo;who touched this line&rdquo; in milliseconds without slowing down normal Git operations.</li>
<li><strong>Exchange records</strong> — the explainability path. These capture the full agent exchange: the prompt, the tool calls, the reasoning, and the resulting change.</li>
</ol>
<p>These two layers are backed by <strong>always-on micro-versioning</strong> (a journal plus checkpoints) and a <strong>deterministic correlation engine</strong> that matches agent activity to the final committed lines.</p>
<h3 id="how-attribution-is-computed">How Attribution Is Computed</h3>
<p>A scored deterministic matcher correlates agent exchanges with committed lines using several signals:</p>
<ul>
<li><strong>Path match</strong> — which file the agent touched</li>
<li><strong>Content hash continuity</strong> — whether the line content traces back to an agent&rsquo;s output</li>
<li><strong>Time-window proximity</strong> — how close the agent action was to the commit</li>
<li><strong>Session affinity</strong> — whether the same agent session produced the change</li>
<li><strong>Change-type compatibility</strong> — whether the edit type matches the agent&rsquo;s operation</li>
</ul>
<p>The hybrid capture stack uses explicit agent hooks plus provider watchers and parsers, all normalized into a single exchange-event schema. This is the practical answer to &ldquo;git blame for AI code.&rdquo;</p>
<h3 id="standards-you-should-know">Standards You Should Know</h3>
<p>Provenance is converging on existing standards rather than inventing new ones:</p>
<ul>
<li><strong>C2PA</strong> (Coalition for Content Provenance and Authenticity) — content provenance for media, extending to code</li>
<li><strong>SLSA</strong> (Supply-chain Levels for Software Artifacts) — integrity of build and release chains</li>
<li><strong>in-toto</strong> — attestation of the software supply chain steps</li>
<li><strong>OpenTelemetry GenAI semantic conventions</strong> — tracing agent and model calls</li>
</ul>
<p>These give you a vocabulary and tooling base for recording who or what produced each artifact.</p>
<h2 id="the-review-bottleneck-trusting-code-you-didnt-write">The Review Bottleneck: Trusting Code You Didn&rsquo;t Write</h2>
<p>The bottleneck in software engineering has shifted from writing code to trusting it. AI generation speed has surpassed human review capacity — you can produce more code in an hour than a team can meaningfully review in a day.</p>
<p>This is not a new problem made worse; it is an old problem finally exposed. Humans never truly scaled code review. Reviewers overloaded with backlogs approve PRs with critical bugs, often focusing on style rather than substance. Code review — human or LLM — was not designed for autonomous coding. It does not verify that the code accomplishes what was prompted or aligns with the spec.</p>
<p>Even five-axis best practices (correctness, architecture, security, readability, performance) fall short of verifying the original acceptance criteria. The question is not &ldquo;is this code good?&rdquo; but &ldquo;does this code do what we asked, and can we prove it?&rdquo;</p>
<h3 id="the-missing-context-problem">The Missing Context Problem</h3>
<p>LLM-written code is an artifact of the process that created it. Reviewing only the generated artifact misses the context of how it was produced. The missing piece is the plans, prompting, and full dialog behind a change. PR summaries do not capture the tradeoffs and alternatives the agent considered. This is why provenance and review must be coupled: you cannot meaningfully review AI code without the process record.</p>
<h2 id="practical-workflows-for-versioning-ai-generated-code">Practical Workflows for Versioning AI-Generated Code</h2>
<p>Theory is useful, but teams need concrete patterns. Here are the workflows that are actually working in production.</p>
<h3 id="the-backwards-build">The Backwards Build</h3>
<p>One proven approach inverts the traditional order. Start with business slides → PRD → documentation → tests → <strong>then</strong> code. Generate code only after the product makes human sense. This ensures the durable artifacts (spec, docs, tests) exist before the regenerable output (code) is produced — which is exactly the &ldquo;code as an artifact&rdquo; philosophy applied to workflow.</p>
<h3 id="the-four-phase-pipeline-with-plan-scoring">The Four-Phase Pipeline with Plan Scoring</h3>
<p>A 6-month-old startup running a 300k-line Next.js monorepo with 3-6 AI coding agents in parallel across git worktrees — and zero traditional SWE backgrounds among founders — enforces a four-phase pipeline:</p>
<ol>
<li><strong>Discussion</strong> — align on intent</li>
<li><strong>Plan</strong> — produce a concrete plan</li>
<li><strong>Implement</strong> — generate code</li>
<li><strong>Review</strong> — verify before merge</li>
</ol>
<p>They use custom Claude Code slash commands and score every plan 1-10 for one-pass implementation confidence. Plans scoring below a threshold go back for revision before any code is written. This is a lightweight, enforceable version of &ldquo;spec first, code second.&rdquo;</p>
<h3 id="verification-gates">Verification Gates</h3>
<p>Require AI to produce tests, docs, and diagrams with mandatory human review before proceeding. Do not let an agent&rsquo;s code merge without its supporting artifacts. This turns the review bottleneck into a structured gate rather than an open-ended slog.</p>
<h3 id="size-limits-and-conventions">Size Limits and Conventions</h3>
<p>Standardize aggressively: minimal invention, strong typing, follow existing conventions, strict linting with no exceptions. Enforce size limits — for example, 50 lines per function and 200 lines per file. Keep the whole chain in sync daily and run human-AI joint code reviews. Small, conventional, well-attributed changes are far easier to trust than large generated diffs.</p>
<h3 id="human-ai-joint-review">Human-AI Joint Review</h3>
<p>Do not replace human review with AI review. Run both. The human verifies intent and acceptance criteria; the AI verifies consistency, style, and mechanical correctness. The human is the only party that can confirm the code matches the original prompt&rsquo;s intent.</p>
<h2 id="tooling-and-standards-for-ai-code-provenance">Tooling and Standards for AI Code Provenance</h2>
<p>The tooling landscape is maturing quickly. Here is what to look for when building your stack.</p>
<table>
  <thead>
      <tr>
          <th>Tool / Standard</th>
          <th>What It Solves</th>
          <th>When to Use</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>Git Notes-based attribution map</td>
          <td>Fast line-level blame</td>
          <td>Always, as the base layer</td>
      </tr>
      <tr>
          <td>Exchange records (SpecStory-style)</td>
          <td>Explainability of agent actions</td>
          <td>When you need &ldquo;why&rdquo; behind a change</td>
      </tr>
      <tr>
          <td>Micro-versioning (journal + checkpoints)</td>
          <td>Survive squash/rebase/cherry-pick</td>
          <td>For high-churn agent workflows</td>
      </tr>
      <tr>
          <td>C2PA</td>
          <td>Content provenance</td>
          <td>For media and generated assets</td>
      </tr>
      <tr>
          <td>SLSA + in-toto</td>
          <td>Supply-chain integrity</td>
          <td>For release and build chains</td>
      </tr>
      <tr>
          <td>OpenTelemetry GenAI semconv</td>
          <td>Trace agent/model calls</td>
          <td>For observability and debugging</td>
      </tr>
  </tbody>
</table>
<p>The key insight: do not bolt provenance on after the fact. It must be captured at the moment the agent acts, before commit flattening destroys the causal chain. Always-on micro-versioning is the mechanism that makes this possible.</p>
<h2 id="building-a-versioning-strategy-that-scales-with-ai-agents">Building a Versioning Strategy That Scales with AI Agents</h2>
<p>A versioning strategy for AI-generated code is not a single tool — it is a layered system. Here is a practical blueprint.</p>
<h3 id="layer-1-capture-the-process">Layer 1: Capture the Process</h3>
<p>Instrument your agents to record every exchange: the prompt, the plan, the tool calls, the reasoning, and the resulting diff. Normalize these into a single event schema. This is the raw material for everything else.</p>
<h3 id="layer-2-attribute-the-output">Layer 2: Attribute the Output</h3>
<p>Correlate agent exchanges with committed lines using the scored matcher (path, content hash, time window, session affinity, change type). Store the attribution map in Git Notes for fast blame, and keep exchange records for deep explainability.</p>
<h3 id="layer-3-gate-the-review">Layer 3: Gate the Review</h3>
<p>Make review a structured gate, not a backlog. Require tests, docs, and diagrams. Score plans before implementation. Enforce size limits and conventions. Run human-AI joint review with the human owning intent verification.</p>
<h3 id="layer-4-preserve-causality">Layer 4: Preserve Causality</h3>
<p>Use micro-versioning so that squash, rebase, and cherry-pick do not destroy the causal chain. The goal is to survive history-flattening operations while keeping the process record intact.</p>
<h3 id="layer-5-standardize-the-vocabulary">Layer 5: Standardize the Vocabulary</h3>
<p>Adopt C2PA, SLSA, in-toto, and OpenTelemetry GenAI conventions so your provenance is interoperable and auditable by external tools and future systems.</p>
<h2 id="conclusion-code-as-a-means-not-an-end">Conclusion: Code as a Means, Not an End</h2>
<p>The paradigm shift is real: code is no longer the end product — the spec, prompt, and context are the durable artifact, and code is a regenerable output. This changes everything about how you version, review, and trust software.</p>
<p>The practical takeaway is that versioning AI-generated code requires capturing the process, not just the output. Attribute every line to AI or human, preserve causality across history-flattening operations, and gate review with structured verification. The teams that treat code as an artifact — a means to an end, backed by full provenance — will be the ones that can trust the code they did not write.</p>
<h2 id="faq">FAQ</h2>
<h3 id="what-does-code-as-an-artifact-mean">What does &ldquo;code as an artifact&rdquo; mean?</h3>
<p>It means treating source code as a regenerable output of a process — the prompts, plans, and agent context that produced it — rather than the durable end product. The spec and the conversation become the real artifact that needs versioning.</p>
<h3 id="how-do-you-version-ai-generated-code">How do you version AI-generated code?</h3>
<p>Version both the code and the process: capture agent exchanges (prompts, plans, reasoning), attribute lines to AI or human via a Git Notes-based attribution map, preserve causality with micro-versioning, and gate review with structured verification.</p>
<h3 id="why-is-traditional-git-insufficient-for-ai-code">Why is traditional Git insufficient for AI code?</h3>
<p>Git captures the artifact but not the process. It cannot record the prompt, plan, or agent reasoning behind a change, and history-flattening operations like squash and rebase destroy the causal chain that explains how AI code evolved.</p>
<h3 id="how-do-you-attribute-code-to-ai-vs-human">How do you attribute code to AI vs. human?</h3>
<p>Use a scored deterministic matcher over path match, content hash continuity, time-window proximity, session affinity, and change-type compatibility, backed by always-on micro-versioning. Store the attribution map in Git Notes for fast blame.</p>
<h3 id="what-are-the-best-practices-for-reviewing-ai-generated-code">What are the best practices for reviewing AI-generated code?</h3>
<p>Use a backwards build (spec, docs, tests before code), enforce a four-phase pipeline with plan scoring, require verification gates (tests, docs, diagrams), enforce size limits and conventions, and run human-AI joint review where the human verifies intent and acceptance criteria.</p>
]]></content:encoded></item><item><title>How to Trace File Lineage: Find Which Script, Notebook, Data, Command, or AI Agent Produced a File</title><link>https://baeseokjae.github.io/posts/trace-file-lineage-agent-provenance-2026/</link><pubDate>Sun, 02 Aug 2026 10:02:06 +0000</pubDate><guid>https://baeseokjae.github.io/posts/trace-file-lineage-agent-provenance-2026/</guid><description>81% of enterprises cannot trace which AI agent produced their code. Learn how to trace file lineage across scripts, notebooks, commands, and AI agents using open-source tools and emerging standards.</description><content:encoded><![CDATA[<p>When you open a file in your project and wonder which script generated it, which AI agent wrote it, or which command produced its contents, you are facing the file lineage problem. File lineage is the practice of tracing a file back to its origin — the exact script, notebook, data source, shell command, or AI agent that created or modified it. Without this trace, teams lose visibility into how their code and data evolved, creating compliance risks, debugging nightmares, and audit failures. Emerging open-source tools like trace-file-lineage, the Agent Trace specification, and academic frameworks like PROV-AGENT now make it possible to answer this question with evidence and honest uncertainty.</p>
<h2 id="the-invisible-code-problem--why-81-of-enterprises-cant-trace-their-ai-outputs">The Invisible Code Problem — Why 81% of Enterprises Can&rsquo;t Trace Their AI Outputs</h2>
<p>The scale of the file lineage problem is staggering. According to the 2026 State of Product Security report, <strong>81% of enterprises cannot track which AI agent produced their code</strong>, and only 19% have complete visibility into AI-generated contributions. Even more striking, <strong>100% of surveyed organizations already have AI-generated code running in production</strong>. This means the vast majority of companies are operating blind — they have AI-written code in their critical systems but no way to trace which agent, model, or conversation produced it.</p>
<p>This &ldquo;invisible code&rdquo; crisis creates several concrete risks:</p>
<table>
  <thead>
      <tr>
          <th>Risk</th>
          <th>Impact</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>Compliance violations</td>
          <td>EU AI Act and SEC regulations require attribution of AI-generated content</td>
      </tr>
      <tr>
          <td>Audit failures</td>
          <td>No way to prove which AI agent wrote which lines during an audit</td>
      </tr>
      <tr>
          <td>Debugging dead ends</td>
          <td>Cannot trace a bug back to the agent or prompt that introduced it</td>
      </tr>
      <tr>
          <td>Security blind spots</td>
          <td>Malicious or compromised agents can inject code with no audit trail</td>
      </tr>
      <tr>
          <td>Reproducibility loss</td>
          <td>Cannot recreate the conditions that produced a given output</td>
      </tr>
  </tbody>
</table>
<p>The 81 percentage point gap between AI code adoption and provenance tracking represents one of the most significant compliance and operational risks in modern software development.</p>
<h2 id="what-is-file-lineage--actor-timestamp-action-hash-and-parent-references">What Is File Lineage? — Actor, Timestamp, Action, Hash, and Parent References</h2>
<p>File lineage, at its core, records five essential pieces of information for every file modification:</p>
<ol>
<li><strong>Actor</strong> — Who or what created the file? This could be a human developer, a specific AI agent (Claude Code, Codex CLI, ChatGPT), a shell script, or a data pipeline.</li>
<li><strong>Timestamp</strong> — When was the file created or last modified? Precise UTC timestamps enable chronological reconstruction.</li>
<li><strong>Action</strong> — What type of operation occurred? Create, modify, delete, rename, or copy.</li>
<li><strong>File Hash</strong> — A cryptographic hash (SHA-256 or similar) of the file contents at the time of the action. This provides tamper evidence — if the hash changes, the file was modified after recording.</li>
<li><strong>Parent References</strong> — What input files, data sources, or previous versions produced this file? This creates the lineage chain.</li>
</ol>
<p>The commercial guide from Fast.io formalizes this schema, adding that downstream agents and human reviewers should be able to trace outputs back through their inputs. When every file carries this metadata, you can reconstruct the full provenance chain from original data source to final output.</p>
<h2 id="the-vcs-gap--why-git-blame-shows-the-wrong-author">The VCS Gap — Why git blame Shows the Wrong Author</h2>
<p>Traditional version control systems like Git and jujutsu (jj) were designed for human collaboration. When an AI agent writes code, git blame shows the human who committed the code — not the AI agent, model, or conversation that actually produced it. As highlighted in a detailed implementation guide on Classmethod DevIO, &ldquo;git blame shows human operator, not AI model or conversation.&rdquo;</p>
<p>This gap is fundamental. Current VCS tools have no mechanism to distinguish AI-written lines from human-written lines. When a developer uses Claude Code to generate a function, commits it, and pushes, git blame attributes those lines to the developer. The AI agent&rsquo;s identity, the model version, the prompt context, and the conversation history are all lost.</p>
<p>The problem compounds in multi-agent workflows. If Agent A generates code, Agent B reviews and modifies it, and Agent C tests and commits it, git blame shows only Agent C&rsquo;s human operator. The provenance of each line — which agent wrote it, which model generated it, which conversation produced it — is invisible.</p>
<h2 id="agent-trace--the-emerging-open-standard-for-ai-code-attribution">Agent Trace — The Emerging Open Standard for AI Code Attribution</h2>
<p>The Agent Trace specification (RFC v0.1.0), published by Cursor in January 2026, directly addresses the VCS gap. It is a vendor-neutral, open format for recording AI contributions in version control. The specification defines a JSONL sidecar file stored at <code>.agent-trace/traces.jsonl</code> within a repository, using a TraceRecord schema that captures:</p>
<ul>
<li>Which AI agent or model wrote each line</li>
<li>Which conversation or session produced the code</li>
<li>The exact prompt and response that generated it</li>
<li>Timestamps and file paths for every modification</li>
</ul>
<p>Agent Trace has already gained significant industry backing. Partners include <strong>Cognition (Devin)</strong> and <strong>Google Jules</strong>, and the specification was featured on <strong>ThoughtWorks Technology Radar</strong> as an &ldquo;Assess&rdquo; entry — indicating it is worth exploring and piloting in real projects.</p>
<p>The key innovation of Agent Trace is that it works alongside existing VCS workflows rather than replacing them. The <code>.agent-trace/traces.jsonl</code> file is committed alongside the code, creating a parallel attribution layer that git blame cannot provide. When you run <code>git blame</code> on a file, you see the human committer. When you check the Agent Trace file, you see the AI agent, model, and conversation that actually produced each line.</p>
<h2 id="trace-file-lineage--local-first-provenance-with-honest-uncertainty">trace-file-lineage — Local-First Provenance with Honest Uncertainty</h2>
<p>Released in July 2026, the <strong>trace-file-lineage</strong> open-source Python tool (172 GitHub stars and rapidly growing) takes a different approach. Rather than integrating with VCS, it works locally to answer a single question: which script, notebook, data, command, or AI agent produced this file?</p>
<p>The tool&rsquo;s design philosophy emphasizes two principles:</p>
<ul>
<li><strong>Evidence</strong> — It searches your local environment for clues: shell history, running processes, file metadata, notebook kernels, and AI agent logs.</li>
<li><strong>Honest uncertainty</strong> — When it cannot determine the origin with confidence, it says so. It does not fabricate answers or guess.</li>
</ul>
<p>This local-first approach has several advantages. It requires no cloud dependency, works entirely offline, respects privacy by keeping all analysis local, and can trace files produced by any tool — not just those that explicitly support provenance recording. It is designed specifically for AI coding assistants like Claude Code, Codex CLI, and ChatGPT, but works with any file on your system.</p>
<p>The honest uncertainty feature is particularly important. In a world where AI agents operate across multiple tools and environments, perfect provenance is often impossible. trace-file-lineage acknowledges this limitation rather than hiding it, giving users a realistic assessment of what can and cannot be determined.</p>
<h2 id="prov-agent--academic-provenance-model-for-agentic-workflows">PROV-AGENT — Academic Provenance Model for Agentic Workflows</h2>
<p>On the academic front, the <strong>PROV-AGENT</strong> framework (arXiv 2508.02866, published at IEEE International Conference on e-Science 2025) extends the W3C PROV standard — the World Wide Web Consortium&rsquo;s provenance model — specifically for AI agent workflows.</p>
<p>PROV-AGENT introduces several innovations:</p>
<ul>
<li><strong>MCP Integration</strong> — It leverages the Model Context Protocol to capture provenance data from AI agents in near real-time, without requiring agents to explicitly log their actions.</li>
<li><strong>Data Observability</strong> — It connects provenance tracking with data observability practices, enabling teams to monitor data lineage as part of their operational infrastructure.</li>
<li><strong>Cross-Facility Evaluation</strong> — The framework was tested across edge, cloud, and HPC (high-performance computing) environments, demonstrating that provenance tracking works at scale in diverse computing contexts.</li>
</ul>
<p>As the first academic provenance model specifically designed for AI agent workflows, PROV-AGENT bridges the gap between established W3C standards and the new reality of agentic software development. It provides a theoretical foundation that practical tools like trace-file-lineage and Agent Trace can build upon.</p>
<h2 id="agent-provenance-chain-and-ai-execution-lineage--immutable-audit-trails">Agent Provenance Chain and AI-Execution-Lineage — Immutable Audit Trails</h2>
<p>The <strong>Agent Provenance Chain</strong> (agent-provenance-chain on GitHub) takes a blockchain-inspired approach to file lineage. It creates an immutable, file-based record of data lineage specifically for AI agent outputs. Each modification is recorded as a link in a chain, with cryptographic hashes ensuring that past records cannot be altered without detection.</p>
<p>This approach is designed for:</p>
<ul>
<li><strong>Auditing</strong> — External auditors can verify the complete history of a file&rsquo;s creation and modification</li>
<li><strong>Verification</strong> — Downstream consumers can verify that a file&rsquo;s provenance chain is intact and unbroken</li>
<li><strong>Multi-agent workflows</strong> — When multiple agents interact with the same file, the chain records each agent&rsquo;s contribution in sequence</li>
</ul>
<p>The immutable chain approach is particularly valuable for regulated industries where audit trails are legally required. Financial services, healthcare, and government agencies that deploy AI agents need tamper-evident records of agent behavior, and Agent Provenance Chain provides exactly that.</p>
<h2 id="practical-implementation--claude-code-hooks--jujutsu-integration">Practical Implementation — Claude Code Hooks + jujutsu Integration</h2>
<p>For teams that want to implement file lineage today, the most practical approach combines Claude Code hooks with the jujutsu (jj) version control system. A detailed guide on Classmethod DevIO demonstrates this exact workflow:</p>
<ol>
<li><strong>Write traces via Claude Code hooks</strong> — Claude Code supports custom hooks that fire after each agent action. These hooks can write trace records to the <code>.agent-trace/traces.jsonl</code> file, capturing the agent&rsquo;s identity, the model used, the conversation context, and the files modified.</li>
<li><strong>Read traces with a TUI tool</strong> — A terminal user interface (TUI) tool reads the trace file and displays the provenance of any file in the repository, showing which AI agent wrote which lines.</li>
<li><strong>Commit traces alongside code</strong> — The trace file is committed to the repository, creating a permanent record that survives branch switches, rebases, and merges.</li>
</ol>
<p>This approach works today with existing tools. No new infrastructure, no cloud services, no proprietary platforms. Any team using Claude Code and jujutsu (or Git) can implement file lineage in an afternoon.</p>
<h2 id="compliance-and-regulatory-drivers--eu-ai-act-sec-and-supply-chain-security">Compliance and Regulatory Drivers — EU AI Act, SEC, and Supply Chain Security</h2>
<p>File lineage is not just a technical convenience — it is becoming a legal requirement. Several regulatory frameworks are driving provenance adoption:</p>
<p><strong>EU AI Act</strong> — The European Union&rsquo;s AI Act requires transparency and documentation for AI-generated content. Organizations deploying AI agents must be able to demonstrate which AI systems produced which outputs, and file lineage provides the evidence needed for compliance.</p>
<p><strong>SEC Regulations</strong> — The U.S. Securities and Exchange Commission has signaled increased scrutiny of AI use in financial services. When AI agents generate code that processes financial data or makes trading decisions, regulators expect clear attribution and audit trails.</p>
<p><strong>Software Supply Chain Security</strong> — Executive orders and industry standards (EO 14028, NIST SSDF) require organizations to maintain provenance records for all software components. AI-generated code is software, and it must meet the same supply chain security standards as human-written code.</p>
<p>The compliance angle transforms file lineage from a &ldquo;nice to have&rdquo; debugging tool into a &ldquo;must have&rdquo; regulatory requirement. Organizations that cannot trace their AI outputs face legal exposure, audit failures, and potential fines.</p>
<h2 id="choosing-the-right-approach--local-vs-cloud-open-vs-proprietary">Choosing the Right Approach — Local vs Cloud, Open vs Proprietary</h2>
<table>
  <thead>
      <tr>
          <th>Approach</th>
          <th>Best For</th>
          <th>Tradeoffs</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>trace-file-lineage (local)</td>
          <td>Individual developers, privacy-sensitive environments</td>
          <td>No VCS integration, honest uncertainty may be frustrating</td>
      </tr>
      <tr>
          <td>Agent Trace (VCS-integrated)</td>
          <td>Teams using Git/jj, multi-agent workflows</td>
          <td>Requires agent support, adds sidecar files to repos</td>
      </tr>
      <tr>
          <td>PROV-AGENT (academic)</td>
          <td>Research environments, HPC, cross-facility deployments</td>
          <td>More complex setup, requires MCP infrastructure</td>
      </tr>
      <tr>
          <td>Agent Provenance Chain (immutable)</td>
          <td>Regulated industries, compliance-heavy environments</td>
          <td>Overhead of chain management, less mature ecosystem</td>
      </tr>
      <tr>
          <td>Commercial solutions (Fast.io, etc.)</td>
          <td>Enterprise teams, managed compliance</td>
          <td>Vendor lock-in, cloud dependency, ongoing cost</td>
      </tr>
  </tbody>
</table>
<p>The right choice depends on your team&rsquo;s specific needs. For most development teams, a combination of Agent Trace for VCS attribution and trace-file-lineage for local investigation provides the best coverage.</p>
<h2 id="the-future-of-file-lineage--c2pa-watermarking-mcp-integration-and-cross-tool-standards">The Future of File Lineage — C2PA Watermarking, MCP Integration, and Cross-Tool Standards</h2>
<p>File lineage is evolving rapidly. Several trends will shape its future:</p>
<p><strong>C2PA Watermarking</strong> — The Coalition for Content Provenance and Authenticity (C2PA) is developing standards for cryptographic watermarking of AI-generated content. Applied to code, C2PA could embed provenance information directly in file metadata, making it portable across tools and platforms.</p>
<p><strong>MCP Integration</strong> — The Model Context Protocol, already leveraged by PROV-AGENT, is becoming a standard interface for AI agents. As more agents support MCP, provenance capture will become automatic rather than requiring explicit instrumentation.</p>
<p><strong>Cross-Tool Standards</strong> — The proliferation of provenance tools (Agent Trace, trace-file-lineage, PROV-AGENT, C2PA) creates an interoperability challenge. Industry-wide standards that allow these tools to share provenance data will be essential for the ecosystem to scale.</p>
<p><strong>AI-Native VCS</strong> — Some researchers are exploring version control systems designed from the ground up for AI collaboration, where provenance is a first-class concept rather than an afterthought. These systems would make file lineage automatic and universal.</p>
<h2 id="faq">FAQ</h2>
<h3 id="what-is-file-lineage-in-the-context-of-ai-agents">What is file lineage in the context of AI agents?</h3>
<p>File lineage is the practice of tracing a file back to its origin — identifying which script, notebook, data source, shell command, or AI agent created or modified it. For AI agents, this means recording which agent, model version, conversation, and prompt produced each file, enabling full audit trails and reproducibility.</p>
<h3 id="how-does-agent-trace-work-with-git">How does Agent Trace work with Git?</h3>
<p>Agent Trace stores provenance data in a <code>.agent-trace/traces.jsonl</code> sidecar file that lives alongside your code in the repository. When an AI agent modifies a file, it writes a TraceRecord to this JSONL file. The sidecar file is committed with the code, creating a permanent attribution layer that survives all VCS operations.</p>
<h3 id="can-i-trace-file-lineage-without-cloud-services">Can I trace file lineage without cloud services?</h3>
<p>Yes. The trace-file-lineage open-source tool works entirely locally with no cloud dependency. It searches your shell history, running processes, file metadata, notebook kernels, and AI agent logs to determine a file&rsquo;s origin. It operates offline and respects your privacy by keeping all analysis on your machine.</p>
<h3 id="why-is-file-lineage-important-for-compliance">Why is file lineage important for compliance?</h3>
<p>Regulations like the EU AI Act and SEC rules require organizations to document AI-generated content and demonstrate which AI systems produced which outputs. Without file lineage, organizations cannot prove the origin of their AI-generated code, creating legal exposure, audit failures, and potential fines.</p>
<h3 id="what-is-the-difference-between-agent-trace-and-prov-agent">What is the difference between Agent Trace and PROV-AGENT?</h3>
<p>Agent Trace is a practical, VCS-integrated specification for recording AI code contributions in repositories, backed by Cursor, Cognition (Devin), and Google Jules. PROV-AGENT is an academic framework that extends the W3C PROV standard for agentic workflows, integrating with the Model Context Protocol (MCP) for near real-time provenance capture across edge, cloud, and HPC environments.</p>
]]></content:encoded></item></channel></rss>