<?xml version="1.0" encoding="utf-8" standalone="yes"?><rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom" xmlns:content="http://purl.org/rss/1.0/modules/content/"><channel><title>Scriptc on RockB</title><link>https://baeseokjae.github.io/tags/scriptc/</link><description>Recent content in Scriptc on RockB</description><image><title>RockB</title><url>https://baeseokjae.github.io/images/og-default.png</url><link>https://baeseokjae.github.io/images/og-default.png</link></image><generator>Hugo</generator><language>en-us</language><lastBuildDate>Mon, 27 Jul 2026 07:01:51 +0000</lastBuildDate><atom:link href="https://baeseokjae.github.io/tags/scriptc/index.xml" rel="self" type="application/rss+xml"/><item><title>Scriptc by Vercel: The TypeScript-to-Native Compiler That Skips JavaScript Entirely</title><link>https://baeseokjae.github.io/posts/scriptc-vercel-typescript-native-compiler/</link><pubDate>Mon, 27 Jul 2026 07:01:51 +0000</pubDate><guid>https://baeseokjae.github.io/posts/scriptc-vercel-typescript-native-compiler/</guid><description>Scriptc by Vercel Labs compiles TypeScript directly to native binaries with no JavaScript engine, achieving 2ms startup and 170KB binaries — a detailed review.</description><content:encoded><![CDATA[<h2 id="what-is-scriptc--vercel-labs-zero-runtime-typescript-compiler">What Is Scriptc? — Vercel Labs&rsquo; Zero-Runtime TypeScript Compiler</h2>
<p>Scriptc is an experimental compiler from Vercel Labs that takes ordinary TypeScript code and produces native executables — with no Node.js, no V8, and no JavaScript engine required at runtime. Released on July 22, 2026, Scriptc represents a radical departure from how TypeScript has been deployed for the past decade. Instead of transpiling to JavaScript and running it inside a VM, Scriptc compiles TypeScript directly to machine code via C and LLVM backends. The result is a standalone binary that starts in roughly 2 milliseconds, occupies 170–200KB of disk space, and consumes 1–4MB of resident memory — a fraction of what Node.js requires. Install it with a single npm command (<code>npm install -g scriptc</code>), and your existing TypeScript projects compile without code changes.</p>
<h2 id="the-three-tier-compilation-model-static-dynamic-rejected">The Three-Tier Compilation Model: Static, Dynamic, Rejected</h2>
<p>Scriptc&rsquo;s most honest and innovative feature is its three-tier compilation model. Rather than claiming to compile everything perfectly, Scriptc categorizes every piece of TypeScript code into one of three tiers.</p>
<h3 id="tier-1-compiled-statically-default">Tier 1: Compiled Statically (Default)</h3>
<p>The default compilation mode produces a fully static binary. No runtime engine is embedded. This works for the vast majority of TypeScript code — classes, closures, async/await, generics, discriminated unions, exception handling, and the full Node API surface including <code>fs</code>, <code>path</code>, <code>process</code>, <code>child_process</code>, <code>os</code>, <code>crypto</code>, <code>http</code>, <code>net</code>, <code>tls</code>, <code>fetch</code>, and <code>zlib</code>. In a coverage analysis of a sample application, Scriptc analyzed 4,481 statements and found that 4,451 (99%) compiled statically. This means nearly all real-world TypeScript can run without any dynamic fallback.</p>
<h3 id="tier-2-runs-dynamically-dynamic">Tier 2: Runs Dynamically (&ndash;dynamic)</h3>
<p>For code that requires dynamic evaluation — <code>eval()</code>, <code>new Function()</code>, dynamic <code>import()</code>, or npm dependencies that rely on runtime metaprogramming — Scriptc offers the <code>--dynamic</code> flag. This embeds a stripped-down QuickJS engine (~620KB) into the binary. The dynamic island handles the small fraction of code that cannot be statically analyzed, while the rest of the program runs as native code. Dependencies are bundled into the binary at build time, so the resulting executable is still self-contained — typically around 3MB with embedded packages.</p>
<h3 id="tier-3-rejected-at-compile-time">Tier 3: Rejected at Compile Time</h3>
<p>Some patterns are genuinely incompatible with ahead-of-time compilation. Scriptc rejects these at compile time with clear error messages rather than silently producing incorrect output. This includes certain reflection-heavy patterns and code that depends on V8-specific behavior. The rejection is a feature, not a bug — it guarantees that what compiles, compiles correctly.</p>
<table>
	<thead>
			<tr>
					<th>Tier</th>
					<th>Mechanism</th>
					<th>Binary Size</th>
					<th>Startup Time</th>
					<th>Use Case</th>
			</tr>
	</thead>
	<tbody>
			<tr>
					<td>Static (default)</td>
					<td>Native code via C/LLVM</td>
					<td>170–200KB</td>
					<td>~2ms</td>
					<td>Most TypeScript code</td>
			</tr>
			<tr>
					<td>Dynamic (&ndash;dynamic)</td>
					<td>Embedded QuickJS (~620KB)</td>
					<td>~3MB</td>
					<td>~4ms</td>
					<td>npm deps, eval, dynamic import</td>
			</tr>
			<tr>
					<td>Rejected</td>
					<td>Compile-time error</td>
					<td>N/A</td>
					<td>N/A</td>
					<td>Reflection-heavy, V8-specific patterns</td>
			</tr>
	</tbody>
</table>
<h2 id="performance-benchmarks-startup-time-binary-size-memory">Performance Benchmarks: Startup Time, Binary Size, Memory</h2>
<p>The performance numbers are where Scriptc makes its strongest case. On M-series Macs, Scriptc&rsquo;s static binaries start in approximately 2 milliseconds. Compare that to Node.js, which takes roughly 47 milliseconds to boot — a 23x difference. For serverless and edge functions that are frequently cold-started, this is transformative.</p>
<table>
	<thead>
			<tr>
					<th>Metric</th>
					<th>Scriptc (Static)</th>
					<th>Scriptc (Dynamic)</th>
					<th>Node.js SEA</th>
					<th>Go Binary</th>
			</tr>
	</thead>
	<tbody>
			<tr>
					<td>Startup Time</td>
					<td>~2ms</td>
					<td>~4ms</td>
					<td>~47ms</td>
					<td>~1ms</td>
			</tr>
			<tr>
					<td>Binary Size</td>
					<td>170–200KB</td>
					<td>~3MB</td>
					<td>60–100MB</td>
					<td>~2MB</td>
			</tr>
			<tr>
					<td>RSS Memory</td>
					<td>1–4MB</td>
					<td>5–10MB</td>
					<td>67–116MB</td>
					<td>2–5MB</td>
			</tr>
	</tbody>
</table>
<p>The binary size comparison is equally striking. A Node.js Single Executable Application (SEA) weighs 60–100MB because it bundles the entire V8 runtime. Scriptc&rsquo;s static binary is 170–200KB — roughly 400x smaller. Even with the embedded QuickJS engine and npm dependencies, a dynamic Scriptc binary is around 3MB, still 20–30x smaller than a Node SEA.</p>
<p>Memory consumption follows the same pattern. Node.js processes typically use 67–116MB of resident memory. Scriptc static binaries use 1–4MB. For deployments at scale — thousands of microservices or edge functions — this memory efficiency translates directly into cost savings.</p>
<h2 id="language-and-api-surface-coverage">Language and API Surface Coverage</h2>
<p>Scriptc aims for full TypeScript language compatibility, and the coverage is impressive for a project that is only days old. The compiler handles:</p>
<ul>
<li><strong>Classes and inheritance</strong> — including <code>extends</code>, <code>implements</code>, and abstract classes</li>
<li><strong>Closures and lexical scoping</strong> — first-class functions, arrow functions, and captured variables</li>
<li><strong>Async/await and Promises</strong> — full event loop integration</li>
<li><strong>Generics</strong> — type parameters, constraints, and conditional types</li>
<li><strong>Discriminated unions and pattern matching</strong> — type narrowing with <code>switch</code>/<code>if</code></li>
<li><strong>Exception handling</strong> — <code>try/catch/finally</code> with stack traces</li>
<li><strong>Standard library</strong> — <code>Array</code>, <code>Map</code>, <code>Set</code>, <code>Promise</code>, <code>WeakRef</code>, <code>FinalizationRegistry</code></li>
</ul>
<p>On the Node API side, Scriptc implements a substantial subset of the Node.js standard library: <code>fs</code> (sync and async), <code>path</code>, <code>process</code>, <code>child_process</code>, <code>os</code>, <code>crypto</code>, <code>http</code>, <code>net</code>, <code>tls</code>, <code>fetch</code>, and <code>zlib</code>. The <code>--dynamic</code> flag extends this to any npm package that doesn&rsquo;t rely on native Node addons.</p>
<p>The compiler uses the real TypeScript compiler for type-checking, so your existing <code>tsconfig.json</code> and type definitions work as expected. Scriptc is not a new type system — it is a new backend for the same TypeScript you already write.</p>
<h2 id="correctness-strategy-differential-testing-and-memory-safety">Correctness Strategy: Differential Testing and Memory Safety</h2>
<p>One of the most ambitious aspects of Scriptc is its approach to correctness. The project maintains a corpus of over 800 differential tests. Every program in the corpus runs under Node.js and as a native Scriptc binary. The stdout, stderr, and exit codes must match byte-for-byte. This is not a &ldquo;mostly works&rdquo; guarantee — it is a rigorous, automated verification that Scriptc&rsquo;s output behaves identically to Node.js.</p>
<p>The testing infrastructure covers:</p>
<ul>
<li><strong>Differential test corpus</strong> — 800+ programs covering language features and API calls</li>
<li><strong>Diagnostic tests</strong> — verifying error messages and edge cases</li>
<li><strong>Snapshot tests</strong> — ensuring output stability across compiler changes</li>
<li><strong>CI/CD pipeline</strong> — automated on every commit</li>
</ul>
<p>Memory safety is enforced through AddressSanitizer (ASan) and a reference count audit on every change. Because Scriptc generates C code that is then compiled with Clang, it inherits Clang&rsquo;s memory safety tooling. The reference counting system for the runtime is audited to prevent use-after-free and memory leaks.</p>
<h2 id="how-to-get-started-with-scriptc">How to Get Started with Scriptc</h2>
<p>Getting started with Scriptc is straightforward, especially if you already have a TypeScript project:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-bash" data-lang="bash"><span style="display:flex;"><span><span style="color:#75715e"># Install Scriptc globally</span>
</span></span><span style="display:flex;"><span>npm install -g scriptc
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#75715e"># Compile a TypeScript file to a native binary</span>
</span></span><span style="display:flex;"><span>scriptc build src/index.ts -o my-app
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#75715e"># Run it</span>
</span></span><span style="display:flex;"><span>./my-app
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#75715e"># Check what compiles statically vs dynamically</span>
</span></span><span style="display:flex;"><span>scriptc coverage src/index.ts
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#75715e"># Compile with dynamic support for npm dependencies</span>
</span></span><span style="display:flex;"><span>scriptc build src/index.ts --dynamic -o my-app-dynamic
</span></span></code></pre></div><p><strong>Prerequisites:</strong> Scriptc requires Clang to be installed. The primary development target is macOS arm64 (Apple Silicon), with cross-compilation support for Linux and Windows. The <code>scriptc coverage</code> command is particularly useful — it analyzes your codebase and reports exactly which statements compile statically, which need the dynamic engine, and which are rejected.</p>
<h2 id="the-competitive-landscape-porffor-perryts-and-the-native-typescript-movement">The Competitive Landscape: Porffor, PerryTS, and the Native TypeScript Movement</h2>
<p>Scriptc is not the first attempt to compile TypeScript to native code, but it may be the most practical. The emerging &ldquo;native TypeScript&rdquo; ecosystem includes several projects:</p>
<table>
	<thead>
			<tr>
					<th>Project</th>
					<th>Approach</th>
					<th>Status</th>
					<th>Key Differentiator</th>
			</tr>
	</thead>
	<tbody>
			<tr>
					<td><strong>Scriptc</strong> (Vercel Labs)</td>
					<td>TypeScript → C → LLVM</td>
					<td>Active, early stage</td>
					<td>Three-tier model, Node API coverage, differential testing</td>
			</tr>
			<tr>
					<td><strong>Porffor</strong></td>
					<td>TypeScript → WebAssembly</td>
					<td>Active</td>
					<td>WebAssembly target, browser-friendly</td>
			</tr>
			<tr>
					<td><strong>PerryTS / pry</strong></td>
					<td>TypeScript → native</td>
					<td>Active</td>
					<td>Alternative approach to native compilation</td>
			</tr>
			<tr>
					<td><strong>ts2c</strong></td>
					<td>TypeScript → C</td>
					<td>Mature but limited</td>
					<td>Manual memory management, limited Node API</td>
			</tr>
			<tr>
					<td><strong>AssemblyScript</strong></td>
					<td>TypeScript subset → Wasm</td>
					<td>Mature</td>
					<td>Strict subset, WebAssembly focus</td>
			</tr>
	</tbody>
</table>
<p>Scriptc&rsquo;s main differentiator is its three-tier model and the depth of its Node API coverage. While Porffor targets WebAssembly (which has its own runtime overhead), Scriptc produces true native binaries. And while ts2c requires manual memory management and supports a limited subset of TypeScript, Scriptc handles the full language automatically.</p>
<p>Vercel&rsquo;s involvement is also significant. As the company behind Next.js, Edge Functions, and a major cloud platform, Vercel has a direct incentive to make TypeScript deployment faster and cheaper. Scriptc could eventually power Vercel&rsquo;s edge runtime, replacing the current V8-based isolation with native binaries.</p>
<h2 id="use-cases-edge-computing-ai-agents-cli-tools-and-microservices">Use Cases: Edge Computing, AI Agents, CLI Tools, and Microservices</h2>
<h3 id="edge-computing-and-serverless-functions">Edge Computing and Serverless Functions</h3>
<p>The most obvious use case for Scriptc is edge computing. Cold starts are the bane of serverless architectures. A 2ms startup time means that every invocation is effectively warm. For platforms like Vercel Edge Functions, Cloudflare Workers, or AWS Lambda@Edge, Scriptc could reduce p99 latency by an order of magnitude.</p>
<h3 id="ai-agent-code">AI Agent Code</h3>
<p>AI agents are increasingly written in TypeScript. Scriptc could compile agent code into tiny, fast binaries suitable for deployment on resource-constrained edge devices or in high-density server environments. The 1–4MB memory footprint means hundreds of agent instances can run on a single machine.</p>
<h3 id="cli-tools">CLI Tools</h3>
<p>Command-line tools written in TypeScript currently require Node.js to be installed. With Scriptc, you distribute a single binary. No runtime dependency, no <code>npx</code>, no version conflicts. The 170KB binary size is comparable to Rust or Go CLI tools.</p>
<h3 id="microservices">Microservices</h3>
<p>For lightweight microservices that don&rsquo;t need the full Node.js ecosystem, Scriptc offers dramatic resource savings. A fleet of 100 microservices running on Node.js would consume 6.7–11.6GB of RAM. The same fleet with Scriptc would use 100–400MB — a 95% reduction.</p>
<h2 id="limitations-and-current-gaps">Limitations and Current Gaps</h2>
<p>Scriptc is an experimental project, and it has real limitations:</p>
<ul>
<li><strong>macOS arm64 only</strong> — The primary target is Apple Silicon. Cross-compilation to Linux and Windows is available but less tested.</li>
<li><strong>No native addons</strong> — npm packages that use native Node.js addons (C++ bindings) cannot be compiled statically. The <code>--dynamic</code> flag handles some cases, but performance may suffer.</li>
<li><strong>Young project</strong> — As of July 27, 2026, Scriptc is only 5 days old. The codebase has 750 GitHub stars and 10 forks. It has not been battle-tested in production.</li>
<li><strong>Limited ecosystem</strong> — No IDE plugins, no debugging support, no profiling tools specific to Scriptc binaries.</li>
<li><strong>&ldquo;Claudisms&rdquo; controversy</strong> — Hacker News commenters noted that the README appears to be AI-generated, raising questions about documentation accuracy and long-term maintenance.</li>
<li><strong>No Windows native support</strong> — Windows builds require cross-compilation from macOS.</li>
<li><strong>Dynamic tier performance</strong> — Code running in the QuickJS dynamic island is significantly slower than native code. If your application has a hot path that falls into the dynamic tier, the performance advantage diminishes.</li>
</ul>
<h2 id="verdict--is-scriptc-ready-for-production">Verdict — Is Scriptc Ready for Production?</h2>
<p>Scriptc is not ready for production today, but it is one of the most promising TypeScript projects in years. The three-tier compilation model is a thoughtful solution to a hard problem. The performance numbers — 2ms startup, 170KB binaries, 1–4MB RSS — are genuinely impressive. The differential testing strategy gives confidence that what compiles, compiles correctly.</p>
<p>For early adopters, Scriptc is worth experimenting with today. CLI tools, single-purpose edge functions, and AI agent code are good candidates. For production workloads, wait for Linux native support, broader npm ecosystem compatibility, and a longer track record of stability.</p>
<p>Vercel&rsquo;s involvement suggests that Scriptc has institutional backing and a clear path to maturity. If the project continues at its current pace, it could fundamentally change how TypeScript is deployed — not as JavaScript with types, but as a genuine systems programming language.</p>
<h2 id="frequently-asked-questions">Frequently Asked Questions</h2>
<h3 id="what-is-scriptc-and-how-does-it-differ-from-regular-typescript-compilation">What is Scriptc and how does it differ from regular TypeScript compilation?</h3>
<p>Scriptc is a compiler from Vercel Labs that converts TypeScript directly into native machine code without requiring Node.js or a JavaScript engine at runtime. Unlike traditional TypeScript compilation (which transpiles to JavaScript and runs in V8), Scriptc produces standalone executables that start in ~2ms and use 1–4MB of memory.</p>
<h3 id="does-scriptc-support-all-typescript-features">Does Scriptc support all TypeScript features?</h3>
<p>Scriptc supports the full TypeScript language including classes, closures, async/await, generics, discriminated unions, and exception handling. It also implements a substantial subset of the Node.js API (fs, path, http, crypto, fetch, and more). A coverage analysis showed 99% of statements in a sample app compiled statically.</p>
<h3 id="how-does-the-dynamic-flag-work">How does the &ndash;dynamic flag work?</h3>
<p>The <code>--dynamic</code> flag embeds a stripped-down QuickJS engine (~620KB) into the binary to handle code that requires runtime evaluation, such as <code>eval()</code>, <code>new Function()</code>, dynamic <code>import()</code>, or npm dependencies with metaprogramming. The rest of the program still runs as native code, and dependencies are bundled at build time.</p>
<h3 id="how-does-scriptc-compare-to-nodejs-in-terms-of-performance">How does Scriptc compare to Node.js in terms of performance?</h3>
<p>Scriptc&rsquo;s static binaries start in ~2ms versus Node.js&rsquo;s ~47ms (23x faster). Binary sizes are 170–200KB versus 60–100MB for Node SEA (400x smaller). Memory usage is 1–4MB versus 67–116MB for Node.js (20–30x less). Go binaries are comparable in size (~2MB) and startup (~1ms) but require writing in Go instead of TypeScript.</p>
<h3 id="is-scriptc-free-and-open-source">Is Scriptc free and open source?</h3>
<p>Yes, Scriptc is available as an open-source project under the Vercel Labs organization on GitHub. It can be installed via npm (<code>npm install -g scriptc</code>) and requires Clang for compilation. The primary development target is macOS arm64, with cross-compilation support for Linux and Windows.</p>
]]></content:encoded></item></channel></rss>