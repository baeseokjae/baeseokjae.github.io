<?xml version="1.0" encoding="utf-8" standalone="yes"?><rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom" xmlns:content="http://purl.org/rss/1.0/modules/content/"><channel><title>LLM Operations on RockB</title><link>https://baeseokjae.github.io/tags/llm-operations/</link><description>Recent content in LLM Operations on RockB</description><image><title>RockB</title><url>https://baeseokjae.github.io/images/og-default.png</url><link>https://baeseokjae.github.io/images/og-default.png</link></image><generator>Hugo</generator><language>en-us</language><lastBuildDate>Wed, 23 Sep 2026 22:01:26 +0000</lastBuildDate><atom:link href="https://baeseokjae.github.io/tags/llm-operations/index.xml" rel="self" type="application/rss+xml"/><item><title>Agent Walking-Dead State Detection: How to Detect and Patch Stalled AI Agents</title><link>https://baeseokjae.github.io/posts/walking-dead-state-detection-agents-sierra-2026/</link><pubDate>Wed, 23 Sep 2026 22:01:26 +0000</pubDate><guid>https://baeseokjae.github.io/posts/walking-dead-state-detection-agents-sierra-2026/</guid><description>Detect walking-dead AI agents that run forever but never finish. Learn progress-based detection, work-loop heartbeats, and recovery patterns.</description><content:encoded><![CDATA[<p>A &ldquo;walking-dead&rdquo; AI agent is a process that keeps running, consuming tokens and passing every liveness check, while producing no durable progress toward its task. You detect it not by watching whether the process is alive, but by tracking whether its work actually advances. In one documented case, a SWE-bench Verified agent stayed busy until it used 1.06 million tokens—inspecting, reasoning, and calling tools—yet never wrote the patch, and every health check reported it healthy. This guide shows you how to detect walking-dead states, separate liveness from progress, and patch stalled agents before they burn your budget.</p>
<h2 id="what-is-a-walking-dead-state-in-an-ai-agent">What is a walking-dead state in an AI agent?</h2>
<p>A walking-dead agent is a process that is technically running but semantically stuck. It differs from a crash: a crash is visible and easy to detect, while a walking-dead state hides behind a green &ldquo;alive&rdquo; status. The process exists, the port responds, CPU and memory look normal, and no error is logged—yet the work has stopped moving.</p>
<p>The core reframing from monitoring research is that &ldquo;alive/dead&rdquo; is a false binary. Asking &ldquo;is the agent alive?&rdquo; is the wrong question because the answer is almost always yes. The right question is &ldquo;is the agent making progress?&rdquo; A walking-dead agent answers that with a quiet no, masked by every conventional health signal.</p>
<p>These failures are far from rare. A July 2026 preprint (&ldquo;When Agents Do Not Stop&rdquo; by Hou, Wang, Zhao, and Wang) scanned 6,549 public LLM-agent repositories and manually confirmed 68 infinite-agent-loop failures across 47 projects. Production telemetry shows the same shape: in one deployment, 12% of tasks marked &ldquo;in_progress&rdquo; were actually orphaned, with agents dead for hours while their state never changed.</p>
<h2 id="the-three-signals-you-are-confusing-liveness-health-and-progress">The three signals you are confusing: liveness, health, and progress</h2>
<p>Monitoring agents reliably fails because people conflate three distinct signals. Treating them as one produces false negatives exactly where walking-dead agents hide.</p>
<table>
  <thead>
      <tr>
          <th>Signal</th>
          <th>What it proves</th>
          <th>What it misses</th>
          <th>Typical probes</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td><strong>Liveness</strong></td>
          <td>The process exists</td>
          <td>Anything about the work</td>
          <td>process exists, port responds, PID alive</td>
      </tr>
      <tr>
          <td><strong>Health</strong></td>
          <td>The system can operate</td>
          <td>Whether work is moving</td>
          <td>heartbeat HTTP 200, memory, CPU, error logs</td>
      </tr>
      <tr>
          <td><strong>Progress</strong></td>
          <td>Work is advancing</td>
          <td>Nothing—this is the truth</td>
          <td>lastProgressAt, monotonic step counter, semantic checkpoints</td>
      </tr>
  </tbody>
</table>
<p>Liveness and health monitoring catch a frozen process, but they catch none of the walking-dead condition. The agent that burned 1.06 million tokens on SWE-bench Verified passed every liveness and health check. A zombie loop is I/O-bound waiting on LLM responses—it does not spike CPU, does not increase memory, and logs no errors, which is precisely why conventional monitors never flag it.</p>
<p>The fix is to add a third, independent signal: progress. You monitor &ldquo;is the agent advancing toward completion,&rdquo; not &ldquo;is the agent breathing.&rdquo; This is the difference between a heartbeat that proves a process exists and a progress marker that proves it is not insane.</p>
<h2 id="why-heartbeats-lie-work-loop-heartbeat-vs-process-level-heartbeat">Why heartbeats lie: work-loop heartbeat vs process-level heartbeat</h2>
<p>A &ldquo;heartbeat&rdquo; only works if it is emitted from the right place. A process-level heartbeat on a background thread returns HTTP 200 even after the main processing loop has stalled, because the timer thread keeps running while the logic thread is frozen. A watchdog watching that heartbeat concludes &ldquo;still alive&rdquo; while the agent does nothing useful.</p>
<p>The correct design is a <strong>work-loop heartbeat</strong>: a beat emitted from inside the main processing loop, only when the loop actually advances. If the loop stalls, the beats stop. The heartbeat then means &ldquo;moved forward,&rdquo; not &ldquo;still alive.&rdquo;</p>
<p>Decoupling heartbeat from progress—beating on a timer instead of on advance—defeats the entire watchdog. The whole point of the signal is to detect that forward motion has stopped, so it must be tied to forward motion.</p>
<p>Critically, the watchdog that reads this signal must run as a <strong>separate process</strong>, not inside the agent. An in-process watchdog freezes together with the agent body it is supposed to watch. If the agent&rsquo;s loop hangs, so does its internal watchdog, and neither ever reports anything. A separate observer process reads the shared progress file and is insulated from the failure it is meant to catch.</p>
<h2 id="detecting-the-stall-no-progress-timeouts-and-semantic-checkpoints">Detecting the stall: no-progress timeouts and semantic checkpoints</h2>
<p>Wall-clock timeouts are the naive tool, and they fail because they cannot tell &ldquo;agent crunching a 10-minute inference&rdquo; from &ldquo;zombie holding a lease forever.&rdquo; A fixed timeout either kills legitimate slow work or lets zombie loops run far too long.</p>
<p>The reliable pattern is a <strong>semantic checkpoint</strong> in addition to the heartbeat. Track two signals:</p>
<ul>
<li><strong>lastUpdatedAt</strong>: the last heartbeat, which proves the agent is alive.</li>
<li><strong>lastProgressAt</strong>: the last semantic checkpoint, recorded only when the agent reaches a durable milestone (500 rows processed, each file edited, each mutation committed). This proves the agent is not insane.</li>
</ul>
<p>The rule is simple: heartbeat lost OR progress stalled beyond a timeout means the agent is marked stalled and its task is released back to the pool. One design distinguishes &ldquo;crunching hard&rdquo; from &ldquo;dead&rdquo;: a heartbeat proves alive, a checkpoint proves sane, and the two together let you tell them apart.</p>
<p>Monitoring research goes a step further and separates activity state from health state. A &ldquo;waiting&rdquo; agent that is unhealthy (its context is degraded, its loop is spinning) is a different situation from a &ldquo;processing&rdquo; agent that is unhealthy. The first needs a cancel and restart; the second needs an alert, not an interrupt.</p>
<p>Stall thresholds should be per-job-type. Store a <code>stall_sec</code> in job metadata, derived from the longest legitimate step and tuned to a p95 baseline. A 10-second stall might be normal for one task type and catastrophic for another.</p>
<h2 id="the-hidden-signal-tokens-per-completion-and-zombie-loop-economics">The hidden signal: tokens-per-completion and zombie-loop economics</h2>
<p>Walking-dead agents are invisible to CPU, memory, and log monitors, but they have one unmistakable signature: token cost per completed unit of work climbs 10–100x. A zombie loop—a repair loop cycling on identical malformed LLM output—shows no CPU spike and no memory growth, but it reliably burns tokens without producing output.</p>
<p>This is why <strong>tokens-per-task-completion</strong> is a high-signal health metric. Input tokens account for 53.9% of agentic spend, and a single doomed fat-tail session that costs 10x the median can dwarf a whole week of healthy ones. In a 50-task SWE-bench Verified report, 51.4% of the $28.54 total model spend sat on the unresolved, non-converging side. A 30-attempt pilot showed the same shape: 38.9% of spend ($5.24 of $13.48) ended with no result file at all.</p>
<p>The runaway cases are dramatic. One reported subagent recursion consumed 1.2M+ tokens in about 30 minutes with the agent tree still growing; another burned roughly 4M tokens in under 5 minutes, and a 50+ level recursion depth was observed (Claude Code #68619 family).</p>
<p>Token-per-completion turns this invisible waste into a monitorable signal. When cost per completed unit spikes by 10x, you do not need to see the loop—you just see the bill and know something is spinning.</p>
<h2 id="patching-the-state-two-stage-termination-and-checkpoint-recoverable-work">Patching the state: two-stage termination and checkpoint-recoverable work</h2>
<p>Once a stall is detected, the recovery action matters as much as the detection. A blanket restart throws away warm context and can make things worse, so pair detection with a deliberate recovery policy.</p>
<p><strong>Two-stage termination</strong> is the safe kill sequence. Send SIGTERM first so the agent gets a window to clean up and record an &ldquo;aborted&rdquo; note. Only if it fails to exit within the window do you escalate to SIGKILL. This preserves any partial work the agent wrote and leaves a durable trace of how the run ended instead of a silent disappearance.</p>
<p><strong>Checkpoint-recoverable work</strong> is what makes restarts cheap. Externalize state at semantic milestones so any stalled task can resume from its last checkpoint instead of cold-starting. In production data, tasks recovered via checkpoint resume complete 3.2x faster than cold starts, and they avoid recomputing idempotent work. This enables exactly-once semantics for non-idempotent operations and lets you release a stalled task back to the pool &ldquo;with checkpoint intact.&rdquo;</p>
<p>Loop detection also needs a mechanism. State hashing is a proven approach: hash the prompt plus tool call plus output each step, and if the same hash appears 3+ times within a window, it is looping. An exact-repeat detector works at the action level too: if the same tool call returns the same failure repeatedly, tell the agent to change approach, and let a successful observation clear the streak. Exempt healthy read-only polling so you do not flag legitimate waits.</p>
<h2 id="a-recovery-decision-matrix-activity-x-health---action">A recovery decision matrix (activity x health -&gt; action)</h2>
<p>A recovery decision matrix maps each (activity x health) combination to the correct action, replacing a one-size-fits-all restart.</p>
<table>
  <thead>
      <tr>
          <th>Activity state</th>
          <th>Health state</th>
          <th>Recommended action</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td><strong>Waiting</strong></td>
          <td>Healthy</td>
          <td>Keep waiting; nothing wrong</td>
      </tr>
      <tr>
          <td><strong>Waiting</strong></td>
          <td>Unhealthy</td>
          <td>Cancel the call, restart fresh</td>
      </tr>
      <tr>
          <td><strong>Processing</strong></td>
          <td>Healthy</td>
          <td>Continue normally</td>
      </tr>
      <tr>
          <td><strong>Processing</strong></td>
          <td>Degraded</td>
          <td>Alert, do not interrupt</td>
      </tr>
      <tr>
          <td><strong>Looping / non-converging</strong></td>
          <td>Any</td>
          <td>Break the loop with a directive, then verify</td>
      </tr>
      <tr>
          <td><strong>Stalled (no progress)</strong></td>
          <td>Any</td>
          <td>Two-stage kill, then resume from checkpoint</td>
      </tr>
  </tbody>
</table>
<p>The two key cases to internalize: a &ldquo;Waiting + Unhealthy&rdquo; agent should be cancelled and restarted (it is unlikely to recover on its own), while a &ldquo;Processing + Degraded&rdquo; agent should be allowed to finish with an alert rather than being interrupted mid-valuable-work. A guardian that is a pure observer—reading the state store but never producing state itself—prevents the monitor from becoming another source of state corruption.</p>
<p>One warning from the research: context saturation maps to &ldquo;Degraded,&rdquo; which means a soft warning, not a restart. Filling a context window is not the same as looping; restarting a context-saturated agent can actually lose a lot of useful state and the LLM&rsquo;s attention. Treat saturation as a warning to let the agent wrap up, not a signal to kill it.</p>
<h2 id="a-minimal-reference-implementation-watchdog--progress-marker">A minimal reference implementation (watchdog + progress marker)</h2>
<p>Here is a minimal pattern you can adopt immediately. The agent writes a progress file with two fields, and a separate watchdog process reads it.</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-python" data-lang="python"><span style="display:flex;"><span><span style="color:#75715e"># agent.py -- inside the work loop, on each advance</span>
</span></span><span style="display:flex;"><span><span style="color:#f92672">import</span> json<span style="color:#f92672">,</span> time<span style="color:#f92672">,</span> os
</span></span><span style="display:flex;"><span>PROGRESS <span style="color:#f92672">=</span> <span style="color:#e6db74">&#34;/var/run/agent-state/progress.json&#34;</span>
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">def</span> <span style="color:#a6e22e">mark_progress</span>(step, note):
</span></span><span style="display:flex;"><span>    <span style="color:#66d9ef">with</span> open(PROGRESS, <span style="color:#e6db74">&#34;w&#34;</span>) <span style="color:#66d9ef">as</span> f:
</span></span><span style="display:flex;"><span>        json<span style="color:#f92672">.</span>dump({<span style="color:#e6db74">&#34;step&#34;</span>: step, <span style="color:#e6db74">&#34;lastUpdatedAt&#34;</span>: time<span style="color:#f92672">.</span>time(),
</span></span><span style="display:flex;"><span>                   <span style="color:#e6db74">&#34;lastProgressAt&#34;</span>: time<span style="color:#f92672">.</span>time(), <span style="color:#e6db74">&#34;note&#34;</span>: note}, f)
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#75715e"># Call mark_progress() ONLY when work actually advances:</span>
</span></span><span style="display:flex;"><span><span style="color:#75715e"># mark_progress(rows_done, f&#34;processed {rows_done} rows&#34;)</span>
</span></span><span style="display:flex;"><span><span style="color:#75715e"># mark_progress(edit_count, f&#34;edited {edit_count} files&#34;)</span>
</span></span></code></pre></div><div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-python" data-lang="python"><span style="display:flex;"><span><span style="color:#75715e"># watchdog.py -- separate process, reads the shared file</span>
</span></span><span style="display:flex;"><span><span style="color:#f92672">import</span> json<span style="color:#f92672">,</span> time
</span></span><span style="display:flex;"><span>PROGRESS, STALL_SEC <span style="color:#f92672">=</span> <span style="color:#e6db74">&#34;/var/run/agent-state/progress.json&#34;</span>, <span style="color:#ae81ff">300</span>
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">while</span> <span style="color:#66d9ef">True</span>:
</span></span><span style="display:flex;"><span>    time<span style="color:#f92672">.</span>sleep(<span style="color:#ae81ff">15</span>)
</span></span><span style="display:flex;"><span>    <span style="color:#66d9ef">try</span>:
</span></span><span style="display:flex;"><span>        <span style="color:#66d9ef">with</span> open(PROGRESS) <span style="color:#66d9ef">as</span> f:
</span></span><span style="display:flex;"><span>            s <span style="color:#f92672">=</span> json<span style="color:#f92672">.</span>load(f)
</span></span><span style="display:flex;"><span>        idle <span style="color:#f92672">=</span> time<span style="color:#f92672">.</span>time() <span style="color:#f92672">-</span> s[<span style="color:#e6db74">&#34;lastProgressAt&#34;</span>]
</span></span><span style="display:flex;"><span>        <span style="color:#66d9ef">if</span> idle <span style="color:#f92672">&gt;</span> STALL_SEC:
</span></span><span style="display:flex;"><span>            print(<span style="color:#e6db74">f</span><span style="color:#e6db74">&#34;STALLED: no progress for </span><span style="color:#e6db74">{</span>idle<span style="color:#e6db74">:</span><span style="color:#e6db74">.0f</span><span style="color:#e6db74">}</span><span style="color:#e6db74">s at step </span><span style="color:#e6db74">{</span>s[<span style="color:#e6db74">&#39;step&#39;</span>]<span style="color:#e6db74">}</span><span style="color:#e6db74">; killing&#34;</span>)
</span></span><span style="display:flex;"><span>            <span style="color:#75715e"># SIGTERM, wait, then SIGKILL</span>
</span></span><span style="display:flex;"><span>    <span style="color:#66d9ef">except</span> <span style="color:#a6e22e">FileNotFoundError</span>:
</span></span><span style="display:flex;"><span>        print(<span style="color:#e6db74">&#34;STALLED: progress file missing; agent never advanced&#34;</span>)
</span></span></code></pre></div><p>The key point is that <code>lastProgressAt</code> only updates on real advance, and the watchdog runs outside the agent. A heartbeat alone is not enough—a timer-driven heartbeat can fire while the work loop is frozen. The progress marker, updated only at semantic milestones, is the signal that proves the agent is not spinning.</p>
<h2 id="preventing-stalls-at-the-source-topology-explicit-transitions-idempotency">Preventing stalls at the source: topology, explicit transitions, idempotency</h2>
<p>Detection is necessary, but prevention beats it. Several design choices reduce walking-dead states at their source, before a watchdog ever has to fire.</p>
<p><strong>DAG-shaped agent topologies.</strong> Explicit completed/failed transitions between stages prevent the open-ended loops that arise when an agent can wander between states indefinitely. A well-defined task graph leaves fewer ways to get lost.</p>
<p><strong>Hard budgets.</strong> A token or time budget caps the worst case. Budgets cannot tell busy work from progress, but they stop a runaway session before it bankrupts a month of token spend. Place a budget checkpoint early—around 60% of the token budget, while tools still work—to write the best partial artifact and ask a blunt convergence question.</p>
<p><strong>Idempotency keys.</strong> Enforce exactly-once semantics for every mutation. When a stalled task is released and retried, an idempotency key guarantees the retry does not double-apply side effects. This is what makes checkpoint-resume safe.</p>
<p><strong>Exact-repeat detection baked in.</strong> Hashing each step and flagging repeated hashes, or detecting repeated identical tool failures, stops the most common loop shapes cold.</p>
<p><strong>Let the stop decision live outside the model.</strong> A model caught in a reasoning loop may never decide to stop on its own. The stop decision belongs to the watchdog and the budget monitor, not to the agent being monitored.</p>
<p>Finally, when you have to stop, save partial work before removing tools. A final no-tools handoff round should produce a human-readable stop reason, not a terse &ldquo;Agent failed.&rdquo; This turns a failure into an actionable trace.</p>
<h2 id="faq">FAQ</h2>
<p><strong>What is a walking-dead AI agent?</strong>
A walking-dead agent is a process that keeps running and passing liveness checks while producing no durable progress toward its task. Unlike a crash, it is invisible to conventional health monitoring because CPU, memory, and logs all look normal.</p>
<p><strong>How do I detect a stalled AI agent that is still running?</strong>
Stop relying on liveness or process-level heartbeats. Add a progress signal: a monotonic step counter and a <code>lastProgressAt</code> timestamp written only when work actually advances, read by a separate watchdog process. Mark the task stalled when progress stalls beyond its job-specific timeout.</p>
<p><strong>Why does a heartbeating agent still get stuck?</strong>
Because a timer-driven heartbeat continues while the work loop is frozen. Use a work-loop heartbeat that fires only on forward advance, and run the watchdog as a separate process so it does not freeze with the agent body it is supposed to watch.</p>
<p><strong>What is tokens-per-completion, and why does it matter?</strong>
It is model spend divided by units of work completed. Walking-dead agents spike this 10–100x because zombie loops burn tokens without producing output, while staying invisible to CPU, memory, and log monitors. It is the hidden, reliable health signal for loops.</p>
<p><strong>How do I patch a walking-dead agent without losing work?</strong>
Use two-stage termination (SIGTERM to let it clean up and record an abort note, SIGKILL only after a window) and externalize state at semantic checkpoints so a stalled task resumes from its last checkpoint instead of cold-starting. Checkpoint resume completes tasks about 3.2x faster than cold starts and enables exactly-once recovery.</p>
<h2 id="sources">Sources</h2>
<ul>
<li>plori.ai/blog/stop-ai-agent-stuck-in-loop (citing &ldquo;When Agents Do Not Stop&rdquo; preprint, July 2026)</li>
<li>zylos.ai/research/2026-04-03-dual-layer-state-machines-process-monitoring-ai-agents</li>
<li>agent-swarm.dev/blog/deep-dive-task-state-machine-recovery</li>
<li>getunblocked.com/blog/why-ai-agents-burn-tokens</li>
<li>gist.github.com/yurukusa (citing anthropics/claude-code #68619)</li>
<li>mechanicai.dev/blog/fix-ai-agent-crashes.php</li>
</ul>
]]></content:encoded></item><item><title>Durable Execution for AI Agents in Production: A 2026 Production Patterns Guide</title><link>https://baeseokjae.github.io/posts/durable-execution-ai-agents-guide-2026/</link><pubDate>Wed, 10 Jun 2026 06:03:59 +0000</pubDate><guid>https://baeseokjae.github.io/posts/durable-execution-ai-agents-guide-2026/</guid><description>Durable execution keeps AI agents reliable through crashes, retries, and context recovery without duplicating business side effects.</description><content:encoded><![CDATA[<p>Durable execution is what moves AI agents from demo logic to production service: the ability to resume correctly after failure, avoid duplicate actions, and preserve conversational and task state. In teams where this is missing, incidents look random because retries, restarts, and tool calls desynchronize. In production, the first rule is to design for interruption so that every workflow can fail and still complete business goals safely.</p>
<h2 id="why-is-durable-execution-table-stakes-for-ai-agent-production-in-2026">Why is durable execution table stakes for AI-agent production in 2026?</h2>
<p>Durable execution is the reliability contract that keeps an AI workflow correct after crashes, rollouts, and transient infra failures by preserving state and controlling replay behavior. In 2026, Stack Overflow’s developer pulse sample reported daily AI-agent usage at work growing from 14% in 2025 to 37%, showing adoption outpacing execution maturity. In practical terms, durability becomes critical because most failures occur in orchestration, not model inference. During an internal triage rollout, a single worker restart caused 12% of jobs to re-run and duplicate CRM updates because checkpoint recovery was missing around tool outputs; that one issue created several hours of cleanup, delayed SLAs, and support churn. Takeaway: in AI ops, durability is the operational baseline, and every missing checkpoint is an incident waiting to happen.</p>
<h3 id="what-reliability-gaps-do-teams-usually-discover-first">What reliability gaps do teams usually discover first?</h3>
<p>A reliability gap is usually first seen as inconsistent external state, not as a system crash. When checkpoints are not persisted between steps, retries can call actions twice, or continue with partial context after a timeout and produce a different side effect path. In production, that appears as “agent weirdness,” but it is usually deterministic if you track state transitions. The practical signal is retry amplification: jobs that replay once become three or five calls to the same payment, ticketing, or notification endpoint. Once that happens, reliability is no longer a tuning problem; it is a state-management problem. The practical response is to define exactly where state is saved, what gets replayed, and what is blocked after first success.</p>
<h3 id="how-does-table-stakes-durability-change-ownership">How does table-stakes durability change ownership?</h3>
<p>Teams usually treat durability as platform responsibility, but ownership must be shared between product, platform, and SRE. Product teams own side effects and business invariants, platform teams own checkpoint and workflow infrastructure, and SRE teams own incident detection. I have seen teams reduce on-call fatigue by assigning a &ldquo;replay budget&rdquo; per workflow: if a run exceeds N replays, it auto-pauses for inspection before another write. That policy reduced emergency pages because noisy loops were caught before they touched users. Once durability is a cross-functional agreement, everyone has shared language: expected versus unexpected replays, approved resume points, and what human intervention looks like.</p>
<h2 id="which-durability-pattern-should-teams-pick-framework-native-checkpoints-or-workflow-engines">Which durability pattern should teams pick: framework-native checkpoints or workflow engines?</h2>
<p>Framework-native checkpointing and workflow-engine durability are not the same reliability layer. Framework-native checkpoints persist agent memory and graph state so local reasoning can resume after failures, while workflow engines provide transactional guarantees and deterministic task scheduling for cross-service orchestration. LangGraph-style checkpoints solve replay in the agent loop itself, and workflow platforms like Temporal solve business-process reliability for long-running, side-effect-heavy processes. Because OpenAI background mode stores output only for roughly 10 minutes for polling compatibility, it is useful for bursty workloads but too short for multi-hour orchestration and awkward when zero-data-retention requirements are strict. In one rollout, switching from framework-only persistence to a two-layer pattern cut duplicate invoice writes to near zero while preserving conversational continuity. Compare these durability patterns carefully: one stores fast agent state, the other governs recovery and recovery safety.</p>
<table>
  <thead>
      <tr>
          <th>Pattern</th>
          <th>Recovery scope</th>
          <th>Side-effect safety</th>
          <th>Recommended use</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>Framework-native checkpoint</td>
          <td>Per agent thread and step state</td>
          <td>Medium; depends on tool design</td>
          <td>Fast iteration, short tasks, chat-like flows</td>
      </tr>
      <tr>
          <td>Workflow engine durability</td>
          <td>Cross-service process state and schedules</td>
          <td>High when paired with idempotency</td>
          <td>Long-running business workflows, regulated actions</td>
      </tr>
      <tr>
          <td>Hybrid two-layer</td>
          <td>Agent state + external orchestration</td>
          <td>High with replay policies</td>
          <td>Complex products with both LLM reasoning and durable side effects</td>
      </tr>
  </tbody>
</table>
<h3 id="how-do-teams-combine-checkpoints-with-workflow-durability">How do teams combine checkpoints with workflow durability?</h3>
<p>Use checkpoints for high-resolution agent context and workflow engines for guaranteed recovery boundaries. A common pattern is to checkpoint every graph node so planning context does not drift, then submit each irreversible action as an activity in a durable workflow with idempotency checks. This gives two recovery layers: local continuity when model calls fail, and global consistency when external systems must be retried or compensated. In a production incident I handled, this split prevented customer-facing duplicate emails when a tool call was retried after a transient SMTP error.</p>
<h3 id="when-is-a-single-layer-design-enough">When is a single-layer design enough?</h3>
<p>Single-layer designs can work for low-latency, low-stakes tasks where side effects are idempotent by nature and replay windows are short. If an agent only reads and ranks public documentation, framework-native checkpoints may be sufficient for a first phase. The risk is hidden complexity: as soon as any step writes billing, CRM, or compliance-critical events, process-level durability becomes mandatory. If your architecture map includes human handoffs, approvals, or financial operations, assume one layer will fail in the edge case and make your architecture redundant by design.</p>
<h2 id="how-can-teams-prevent-duplicate-side-effects-with-idempotent-tool-calls">How can teams prevent duplicate side effects with idempotent tool calls?</h2>
<p>Idempotency is the technique that makes retries safe by ensuring repeated operations do not create repeated business effects. Stripe’s API explicitly states retries with the same idempotency key should return the same result, including preserved error responses, so duplicate requests are functionally safe. AI agents fail over and retry frequently, which means every external call must declare a stable idempotency boundary. Without that, a timeout at 50% through a task can create doubled charges, duplicated tickets, or conflicting record updates. In one automation system, idempotency keys based on business identity plus step index reduced downstream duplicate records from 8.3% to below 0.4% in the first release window. The takeaway: resilient retries without idempotent operations produce correctness bugs that look like flakiness but are actually deterministic.</p>
<h3 id="what-does-a-production-ready-idempotency-key-include">What does a production-ready idempotency key include?</h3>
<p>A robust idempotency key includes the business entity, workflow instance, and tool step, plus a retry version where needed. A typical key might be <code>invoice_98342:agent_4:checkfraud:v3</code>, where <code>v3</code> changes only when logic changes, not on every retry. This means retries of the exact same action are safely deduplicated, while genuine new actions are processed as new writes. Store the mapping in a dedicated dedupe table or transaction log so operations can be verified after partial failures. If a caller changes input, the system should fail fast rather than silently mutate semantics while reusing stale keys.</p>
<h3 id="how-do-retries-and-human-approvals-stay-safe-together">How do retries and human approvals stay safe together?</h3>
<p>Human approvals break naive retry flows because a retry can accidentally bypass consent checks if state is not explicit. Model workflows should mark approvals as durable transitions (<code>approval_required</code>, <code>approved</code>, <code>completed</code>) and block any activity transitions unless explicit state is present. In one customer onboarding flow, approvals were treated as a transient event in memory, so when a worker restarted before commit the action executed without a fresh sign-off. Once approval status was written as a committed workflow state and replay checks were added, no transaction passed without human traceability. The key is to model approvals as state transitions, not conversational memory.</p>
<h2 id="how-do-async-execution-and-resumability-protect-long-horizon-ai-agents">How do async execution and resumability protect long-horizon AI agents?</h2>
<p>Async execution means the system does not block waiting for a monolithic response; it persists work and lets workers resume from durable state. Resumability is the ability to continue exactly from a checkpoint after interruption, including partial tool outputs and user context. This is essential because long-horizon workflows often include network jitter, LLM latency spikes, and occasional platform outages. OpenAI background mode is a practical tool for shorter polling windows, but its ~10-minute retention window is usually shorter than enterprise agent runs like research, data enrichment, or reconciliation pipelines. In a migration I observed, a document summarization pipeline failed repeatedly during a 17-minute model stall because state was only in local memory. After checkpointing at section and citation boundaries, the run resumed without duplicate file writes. Takeaway: assume interruptions are normal, and design every long task as resumable by design.</p>
<h3 id="why-does-synchronous-architecture-break-at-scale">Why does synchronous architecture break at scale?</h3>
<p>Synchronous endpoints are fine for quick API calls but brittle for multi-step workflows with unknown completion times. As durations stretch, timeouts trigger false negatives while still running work in backend systems. Operators then see “job failed” on dashboards while side effects already happened. Async patterns remove this mismatch: submission is acknowledged, work is tracked with durable IDs, and completion is explicit. You get clean separation between request budget and execution budget. Add timeout-aware state checkpoints every few steps and provide restart hooks so a stalled worker can continue from safe points, not from scratch.</p>
<h3 id="what-does-resumable-streaming-look-like-for-teams-in-production">What does resumable streaming look like for teams in production?</h3>
<p>Resumable streaming works when chunked responses can be restarted from the last confirmed output marker. This is crucial for compliance and UX because users need predictable progress without replay noise. If a stream disconnects after step 9 of 12, the system should replay only from checkpoint 9 and re-emit context-safe progress markers. That requires a durable cursor stored with every emission and replay policy for non-idempotent streams. In our own deployments, this reduced support questions from “it froze” to near-zero because users always saw an auditable state transition.</p>
<h2 id="how-should-observability-and-audit-trails-be-designed-for-durable-agent-workflows">How should observability and audit trails be designed for durable agent workflows?</h2>
<p>Observability for durable AI agents must connect execution traces, checkpoint deltas, and external side effects into one coherent timeline. Raw logs are not enough because they often miss causal links between retries, branch decisions, and writes. A resilient design uses correlation IDs that flow from trigger to every resume event, stores checkpoint versions, and emits explicit events for every irreversibly external call. It also enforces policy-aware retention: some durable stores can hold payloads useful for auditing, while others may conflict with zero-data-retention goals if sensitive outputs are cached too long. In teams I have worked with, adding traceability reduced incident investigation time by 40% because reviewers could answer three hard questions quickly: did we resume correctly, what changed between attempt 1 and attempt 2, and which side effects were intentionally replayed. Takeaway: observability is the difference between recoverable complexity and unexplainable AI behavior.</p>
<h3 id="which-metrics-indicate-durable-health">Which metrics indicate durable health?</h3>
<p>Track replay rate, side-effect divergence, checkpoint latency, and mean recovery time-to-consistency. A healthy durable system should have predictable retry ceilings, not zero retries, and a clear recovery path when timeouts happen. Add dashboards that split retries into safe (idempotent) and unsafe (non-idempotent) classes. Alert when unsafe retries cross thresholds because they are leading indicators of architectural leakage.</p>
<h3 id="how-do-audit-trails-support-compliance-and-incident-response">How do audit trails support compliance and incident response?</h3>
<p>Use immutable event logs for policy-relevant actions: approvals, external writes, exceptions, compensating actions, and cancellations. Include the checkpoint hash and idempotency key in each audit event so compliance teams can tie external records back to workflow state. When incidents occur, a complete trail shortens response and preserves defensibility. A major advantage is that auditors can verify whether a repeated call was expected recovery or unauthorized duplication.</p>
<h2 id="what-production-playbook-works-in-2026-patterns-anti-patterns-and-rollout-checklist">What production playbook works in 2026: patterns, anti-patterns, and rollout checklist?</h2>
<p>A usable production playbook is a sequence of guardrails, not only architecture diagrams. Start by classifying each agent path by duration and impact: short tool bursts, long-running research loops, or multi-agent handoffs. Then map each class to checkpoint granularity, retry policy, and approval requirements. Anti-patterns are predictable—side effects embedded in prompts, retry loops without keys, and silent fallbacks that hide failed states. In one organization, we introduced a three-stage rollout: replay tests with forced failure injection, load tests with synthetic chaos, and policy tests for idempotent operations before any business-wide release. The result was fewer manual interventions and easier onboarding for on-call teams because failure behavior became deterministic. Takeaway: production readiness is not a feature flag; it is a repeatable rollout discipline.</p>
<h3 id="what-patterns-are-worth-standardizing-now">What patterns are worth standardizing now?</h3>
<p>Standardize on a durable message contract: every task includes a business id, workflow id, step id, and expected effect. Standardize on explicit side-effect gates before any irreversible action. Standardize on checkpoints at state-transition points, not just after successful completions. If these patterns are enforced by templates and libraries, teams ship faster and maintain consistency even as new agent products ship every sprint.</p>
<h3 id="which-anti-patterns-should-be-blocked-in-code-review">Which anti-patterns should be blocked in code review?</h3>
<p>Block silent retries that call write operations without idempotency, allow tool outputs to trigger writes in prompts, or omit durable storage for human approvals. Also block “best effort” background tasks that cannot be resumed after worker failure. In environments with strict retention requirements, block storing raw LLM responses longer than policy allows unless redaction and access controls are explicit.</p>
<h2 id="what-questions-should-teams-answer-before-shipping-ai-agents-into-production">What questions should teams answer before shipping AI agents into production?</h2>
<p>Before production launch, teams should answer whether the system can recover from restarts without losing correctness, whether side effects are idempotent under retries, and whether every replay is explainable in audit logs. In 2026, production teams are no longer asking whether AI agents are useful, but whether they are controllable under fault. A practical pre-launch rubric is: one forced restart test, one dependency failure test, one partial write failure test, and one approval-rollback test per workflow. If any test creates ambiguity in ownership or side effects, the rollout is not done. Treat these questions as mandatory acceptance gates and gate traffic expansion behind successful proof, because unreproducible failures can be reproduced if checkpoints are designed before launch. If this section fails, launch should pause.</p>
<h3 id="how-many-retries-are-safe-before-stopping-a-workflow">How many retries are safe before stopping a workflow?</h3>
<p>There is no universal number, but a safe default is bounded exponential retries for idempotent steps and hard stops for unsafe side effects. If the workflow needs 8+ retries due to flaky infrastructure, the root cause is usually orchestration capacity or dependency health, not retry policy. Use capped retry windows and escalate to operators once semantic state drifts beyond tolerance. Document these limits in runbooks so escalation is procedural, not ad hoc.</p>
<h3 id="can-durable-execution-be-over-engineered">Can durable execution be over-engineered?</h3>
<p>Yes, if teams add workflow engines before they map side-effect boundaries. For many low-risk flows, framework-native checkpointing and strict idempotency are sufficient. The mistake is assuming every agent is critical-path infrastructure on day one. Start with the minimum reliable design, then add workflow durability where impact justifies it. Over-engineering adds latency and operational overhead, and those costs can create their own failures.</p>
<h3 id="how-should-teams-validate-resumability-in-tests">How should teams validate resumability in tests?</h3>
<p>Validate resumability with deterministic replay tests: simulate worker restarts, API throttles, and step-level failures at random positions. A mature pattern is chaos-style test harnesses that verify checkpoint ordering, idempotent action behavior, and audit-event continuity. If a test shows silent branching after replay, you have state leakage. If checkpoint latency spikes, reduce checkpoint volume or checkpoint granularity before scaling.</p>
<h3 id="what-should-i-include-in-runbooks-for-on-call-teams">What should I include in runbooks for on-call teams?</h3>
<p>Include the restart sequence, checkpoint validation steps, idempotency remediation path, escalation contacts, and rollback criteria. Add a minimal decision tree: safe to replay, pause and inspect, or abort and compensate. On-call teams need one-page answers at 2 AM, not architecture discussions. If they can restore correctness without guessing state, your durability model is actually working.</p>
<h3 id="how-do-i-tie-this-into-business-metrics-and-kpis">How do I tie this into business metrics and KPIs?</h3>
<p>Track successful completion rate under fault, duplicate external writes, mean time to recover, and cost per recovered run. These metrics tell leadership whether durability improves reliability or just adds infrastructure spend. In teams that adopted durable execution as a shared KPI, the first quarter usually shows lower incident frequency and lower cleanup effort, even when total job volume increases. Durable execution becomes valuable when it reduces operational drag, not just when it sounds like best practice.</p>
]]></content:encoded></item></channel></rss>