<?xml version="1.0" encoding="utf-8" standalone="yes"?><rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom" xmlns:content="http://purl.org/rss/1.0/modules/content/"><channel><title>Context Retriever on RockB</title><link>https://baeseokjae.github.io/tags/context-retriever/</link><description>Recent content in Context Retriever on RockB</description><image><title>RockB</title><url>https://baeseokjae.github.io/images/og-default.png</url><link>https://baeseokjae.github.io/images/og-default.png</link></image><generator>Hugo</generator><language>en-us</language><lastBuildDate>Fri, 17 Jul 2026 01:06:53 +0000</lastBuildDate><atom:link href="https://baeseokjae.github.io/tags/context-retriever/index.xml" rel="self" type="application/rss+xml"/><item><title>Redis Iris Agent Memory 2026: Building a Pydantic AI Agent with Redis Iris Context Layer</title><link>https://baeseokjae.github.io/posts/redis-iris-agent-memory-2026/</link><pubDate>Fri, 17 Jul 2026 01:06:53 +0000</pubDate><guid>https://baeseokjae.github.io/posts/redis-iris-agent-memory-2026/</guid><description>Step-by-step guide to building a Pydantic AI agent with Redis Iris Context Retriever and Agent Memory for auto-generated MCP tools and cross-session recall.</description><content:encoded><![CDATA[<h2 id="what-is-redis-iris-context-retriever--agent-memory-explained">What is Redis Iris? Context Retriever + Agent Memory Explained</h2>
<p>Redis Iris is a managed context layer for AI agents that combines two core services: <strong>Context Retriever</strong> and <strong>Agent Memory</strong>. Context Retriever auto-generates MCP (Model Context Protocol) tools from your entity model — turning database schemas into callable tools without writing a single line of data-access code. Agent Memory provides both short-term session memory (with TTL) and long-term semantic memory with vector search, background fact promotion, and metadata filtering. Together, they solve the &ldquo;under-informed agent&rdquo; problem by giving AI agents real-time access to fresh data and persistent cross-session context.</p>
<h2 id="why-redis-iris-for-pydantic-ai-agents-the-context-layer-problem">Why Redis Iris for Pydantic AI Agents? The Context Layer Problem</h2>
<p>Pydantic AI is a popular Python framework for building type-safe, structured LLM agents. However, every agent faces the same fundamental challenge: <strong>how does it access the data it needs to answer questions?</strong> Without a context layer, developers must write custom API calls, manage conversation history manually, and build bespoke retrieval pipelines for every agent. This approach is brittle, repetitive, and does not scale across multiple agents or sessions.</p>
<p>Redis Iris solves this by providing a unified context layer that sits between your Pydantic AI agent and your data. The Context Retriever exposes your database entities as MCP tools that the agent can discover and call dynamically. Agent Memory handles conversation persistence automatically — session memory keeps the current conversation flowing, while long-term memory enables the agent to recall facts from conversations days or weeks ago. The result is a Pydantic AI agent that is genuinely informed, context-aware, and production-ready with minimal custom code.</p>
<h2 id="prerequisites--redis-cloud-context-retriever-service-agent-memory-service-llm-key">Prerequisites — Redis Cloud, Context Retriever Service, Agent Memory Service, LLM Key</h2>
<p>Before building your Redis Iris agent, you need the following:</p>
<table>
	<thead>
			<tr>
					<th>Component</th>
					<th>Purpose</th>
					<th>How to Get It</th>
			</tr>
	</thead>
	<tbody>
			<tr>
					<td><strong>Redis Cloud account</strong></td>
					<td>Hosts your data and provides the underlying Redis infrastructure</td>
					<td>Sign up at redis.com/cloud (free tier available)</td>
			</tr>
			<tr>
					<td><strong>Context Retriever surface</strong></td>
					<td>Auto-generates MCP tools from your entity model</td>
					<td>Provision via Redis Iris console or API</td>
			</tr>
			<tr>
					<td><strong>Agent Memory service</strong></td>
					<td>Manages session and long-term memory</td>
					<td>Enabled per surface in Redis Iris</td>
			</tr>
			<tr>
					<td><strong>LLM API key</strong></td>
					<td>Powers the Pydantic AI agent</td>
					<td>Anthropic, OpenAI, or any supported provider</td>
			</tr>
			<tr>
					<td><strong>Pydantic AI SDK</strong></td>
					<td>Agent framework</td>
					<td><code>pip install pydantic-ai-slim&gt;=2.2.0</code></td>
			</tr>
	</tbody>
</table>
<p>The Context Retriever MCP endpoint defaults to <code>gcp-us-east4.context-surfaces.redis.io/mcp</code> and authenticates via an X-API-Key header. Agent Memory uses the <code>redis-agent-memory</code> SDK (&gt;=0.1.1) with async methods for session events and semantic search.</p>
<h2 id="architecture-overview--pydantic-ai-agent--mcptoolset--context-retriever--redis-cloud">Architecture Overview — Pydantic AI Agent ↔ MCPToolset ↔ Context Retriever ↔ Redis Cloud</h2>
<p>The architecture follows a clean layered pattern:</p>
<pre tabindex="0"><code>┌─────────────────────────────────────────────┐
│            Pydantic AI Agent                 │
│  (agent.run_sync(&#34;Find customer orders...&#34;)) │
└─────────────────────┬───────────────────────┘
                      │
              ┌───────▼────────┐
              │  MCPToolset    │  ← pydantic-ai-slim &gt;= 2.2.0
              │ (MCP client)   │
              └───────┬────────┘
                      │
              ┌───────▼────────────────┐
              │  Context Retriever     │  ← streamable-HTTP at /mcp
              │  (auto-generated MCP)  │     X-API-Key auth
              └───────┬────────────────┘
                      │
         ┌────────────┼────────────┐
         ▼            ▼            ▼
   ┌──────────┐ ┌──────────┐ ┌──────────┐
   │ Customer │ │  Order   │ │ Product  │  ← 5+ entities
   │ (get,    │ │ (get,    │ │ (get,    │     ~29 MCP tools
   │  filter, │ │  filter, │ │  filter, │
   │  search) │ │  search) │ │  search) │
   └──────────┘ └──────────┘ └──────────┘
</code></pre><p>The Pydantic AI agent connects to the Context Retriever MCP endpoint via <code>MCPToolset.from_url()</code>. The Context Retriever automatically exposes CRUD and search operations for each entity in your model. Agent Memory runs alongside, capturing session events and promoting durable facts to long-term storage in the background.</p>
<h2 id="step-1--set-up-redis-cloud-and-context-retriever-surface">Step 1 — Set Up Redis Cloud and Context Retriever Surface</h2>
<p>Start by creating a Redis Cloud subscription. The free tier (30 MB) is sufficient for development and demos.</p>
<ol>
<li><strong>Create a Redis Cloud database</strong> at app.redislabs.com. Note your endpoint URL and port.</li>
<li><strong>Provision a Context Retriever surface</strong> via the Redis Iris console. This requires:
<ul>
<li>A name for your surface (e.g., &ldquo;northpeak-outfitters&rdquo;)</li>
<li>Your Redis Cloud database endpoint</li>
<li>An entity model definition (JSON schema of your data entities)</li>
</ul>
</li>
<li><strong>Generate an agent key</strong> — this is your X-API-Key for authenticating MCP requests. Agent keys can be scoped to specific surfaces and users, enabling multi-tenant access control without custom auth code.</li>
<li><strong>Enable Agent Memory</strong> on the surface. This provisions the session and long-term memory stores.</li>
</ol>
<p>Once provisioned, your Context Retriever surface is accessible at a URL like:</p>
<pre tabindex="0"><code>https://gcp-us-east4.context-surfaces.redis.io/mcp
</code></pre><h2 id="step-2--configure-agent-memory-session--long-term">Step 2 — Configure Agent Memory (Session + Long-term)</h2>
<p>Agent Memory has two tiers:</p>
<p><strong>Session Memory (Working Memory):</strong></p>
<ul>
<li>Stores conversation events with a configurable TTL (default: 24 hours)</li>
<li>Accessed via <code>add_session_event_async()</code> and <code>search_session_memory_async()</code></li>
<li>Automatically cleaned up after TTL expires — no manual deletion needed</li>
</ul>
<p><strong>Long-term Memory (Semantic Memory):</strong></p>
<ul>
<li>Stores durable facts with vector embeddings for semantic search</li>
<li>Accessed via <code>search_long_term_memory_async()</code> and <code>bulk_create_long_term_memories_async()</code></li>
<li>Supports metadata filters (e.g., filter by user_id, entity type, or confidence score)</li>
<li>Facts are auto-promoted from session memory by the managed service in the background</li>
</ul>
<p>Configuration is minimal — the <code>redis-agent-memory</code> SDK handles connection pooling, retries, and serialization:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-python" data-lang="python"><span style="display:flex;"><span><span style="color:#f92672">from</span> redis_agent_memory <span style="color:#f92672">import</span> AgentMemoryClient
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>memory <span style="color:#f92672">=</span> AgentMemoryClient(
</span></span><span style="display:flex;"><span>    surface_id<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;northpeak-outfitters&#34;</span>,
</span></span><span style="display:flex;"><span>    agent_key<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;your-agent-key&#34;</span>,
</span></span><span style="display:flex;"><span>    session_ttl_seconds<span style="color:#f92672">=</span><span style="color:#ae81ff">86400</span>  <span style="color:#75715e"># 24 hours</span>
</span></span><span style="display:flex;"><span>)
</span></span></code></pre></div><h2 id="step-3--build-the-pydantic-ai-agent-with-mcptoolset">Step 3 — Build the Pydantic AI Agent with MCPToolset</h2>
<p>With Pydantic AI&rsquo;s native MCP support (available since <code>pydantic-ai-slim&gt;=2.2.0</code>), connecting to Redis Iris is straightforward. The <code>MCPToolset</code> class handles the MCP protocol handshake, tool discovery, and function calling automatically.</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-python" data-lang="python"><span style="display:flex;"><span><span style="color:#f92672">from</span> pydantic_ai <span style="color:#f92672">import</span> Agent
</span></span><span style="display:flex;"><span><span style="color:#f92672">from</span> pydantic_ai.mcp <span style="color:#f92672">import</span> MCPToolset
</span></span><span style="display:flex;"><span><span style="color:#f92672">from</span> pydantic_ai.models.openai <span style="color:#f92672">import</span> OpenAIModel
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>model <span style="color:#f92672">=</span> OpenAIModel(<span style="color:#e6db74">&#34;gpt-4o&#34;</span>)
</span></span><span style="display:flex;"><span>agent <span style="color:#f92672">=</span> Agent(model<span style="color:#f92672">=</span>model)
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#75715e"># Connect to Redis Iris Context Retriever</span>
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">async</span> <span style="color:#66d9ef">with</span> MCPToolset<span style="color:#f92672">.</span>from_url(
</span></span><span style="display:flex;"><span>    url<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;https://gcp-us-east4.context-surfaces.redis.io/mcp&#34;</span>,
</span></span><span style="display:flex;"><span>    headers<span style="color:#f92672">=</span>{<span style="color:#e6db74">&#34;X-API-Key&#34;</span>: <span style="color:#e6db74">&#34;your-agent-key&#34;</span>}
</span></span><span style="display:flex;"><span>) <span style="color:#66d9ef">as</span> toolset:
</span></span><span style="display:flex;"><span>    <span style="color:#75715e"># All ~29 MCP tools are now available to the agent</span>
</span></span><span style="display:flex;"><span>    agent<span style="color:#f92672">.</span>register_toolset(toolset)
</span></span><span style="display:flex;"><span>    
</span></span><span style="display:flex;"><span>    result <span style="color:#f92672">=</span> <span style="color:#66d9ef">await</span> agent<span style="color:#f92672">.</span>run_sync(
</span></span><span style="display:flex;"><span>        <span style="color:#e6db74">&#34;Find all orders for customer John Smith that are still pending&#34;</span>
</span></span><span style="display:flex;"><span>    )
</span></span><span style="display:flex;"><span>    print(result<span style="color:#f92672">.</span>data)
</span></span></code></pre></div><p>The agent can now call tools like <code>get_customer_by_id</code>, <code>filter_order_by_status</code>, and <code>search_product_by_text</code> without any additional code. Pydantic AI handles the type-safe serialization of tool inputs and outputs.</p>
<h2 id="step-4--wire-agent-memory-as-search_memory--store_memory-tools">Step 4 — Wire Agent Memory as search_memory / store_memory Tools</h2>
<p>To give the agent explicit memory access, wrap Agent Memory operations as Pydantic AI tools:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-python" data-lang="python"><span style="display:flex;"><span><span style="color:#f92672">from</span> pydantic_ai <span style="color:#f92672">import</span> RunContext, Tool
</span></span><span style="display:flex;"><span><span style="color:#f92672">from</span> redis_agent_memory <span style="color:#f92672">import</span> AgentMemoryClient
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>memory <span style="color:#f92672">=</span> AgentMemoryClient(surface_id<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;northpeak&#34;</span>, agent_key<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;...&#34;</span>)
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">async</span> <span style="color:#66d9ef">def</span> <span style="color:#a6e22e">search_memory</span>(ctx: RunContext, query: str, user_id: str <span style="color:#f92672">=</span> <span style="color:#e6db74">&#34;&#34;</span>) <span style="color:#f92672">-&gt;</span> str:
</span></span><span style="display:flex;"><span>    <span style="color:#e6db74">&#34;&#34;&#34;Search the agent&#39;s long-term memory for relevant context.&#34;&#34;&#34;</span>
</span></span><span style="display:flex;"><span>    results <span style="color:#f92672">=</span> <span style="color:#66d9ef">await</span> memory<span style="color:#f92672">.</span>search_long_term_memory_async(
</span></span><span style="display:flex;"><span>        query<span style="color:#f92672">=</span>query,
</span></span><span style="display:flex;"><span>        metadata_filters<span style="color:#f92672">=</span>{<span style="color:#e6db74">&#34;user_id&#34;</span>: user_id} <span style="color:#66d9ef">if</span> user_id <span style="color:#66d9ef">else</span> <span style="color:#66d9ef">None</span>,
</span></span><span style="display:flex;"><span>        top_k<span style="color:#f92672">=</span><span style="color:#ae81ff">5</span>
</span></span><span style="display:flex;"><span>    )
</span></span><span style="display:flex;"><span>    <span style="color:#66d9ef">return</span> <span style="color:#e6db74">&#34;</span><span style="color:#ae81ff">\n</span><span style="color:#e6db74">&#34;</span><span style="color:#f92672">.</span>join(
</span></span><span style="display:flex;"><span>        <span style="color:#e6db74">f</span><span style="color:#e6db74">&#34;[</span><span style="color:#e6db74">{</span>r[<span style="color:#e6db74">&#39;created_at&#39;</span>]<span style="color:#e6db74">}</span><span style="color:#e6db74">] </span><span style="color:#e6db74">{</span>r[<span style="color:#e6db74">&#39;content&#39;</span>]<span style="color:#e6db74">}</span><span style="color:#e6db74">&#34;</span>
</span></span><span style="display:flex;"><span>        <span style="color:#66d9ef">for</span> r <span style="color:#f92672">in</span> results
</span></span><span style="display:flex;"><span>    )
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">async</span> <span style="color:#66d9ef">def</span> <span style="color:#a6e22e">store_memory</span>(ctx: RunContext, fact: str, user_id: str <span style="color:#f92672">=</span> <span style="color:#e6db74">&#34;&#34;</span>) <span style="color:#f92672">-&gt;</span> str:
</span></span><span style="display:flex;"><span>    <span style="color:#e6db74">&#34;&#34;&#34;Store a durable fact in long-term memory.&#34;&#34;&#34;</span>
</span></span><span style="display:flex;"><span>    <span style="color:#66d9ef">await</span> memory<span style="color:#f92672">.</span>bulk_create_long_term_memories_async([
</span></span><span style="display:flex;"><span>        {<span style="color:#e6db74">&#34;content&#34;</span>: fact, <span style="color:#e6db74">&#34;metadata&#34;</span>: {<span style="color:#e6db74">&#34;user_id&#34;</span>: user_id, <span style="color:#e6db74">&#34;source&#34;</span>: <span style="color:#e6db74">&#34;agent&#34;</span>}}
</span></span><span style="display:flex;"><span>    ])
</span></span><span style="display:flex;"><span>    <span style="color:#66d9ef">return</span> <span style="color:#e6db74">f</span><span style="color:#e6db74">&#34;Stored: </span><span style="color:#e6db74">{</span>fact<span style="color:#e6db74">}</span><span style="color:#e6db74">&#34;</span>
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>agent<span style="color:#f92672">.</span>register_tool(Tool(search_memory))
</span></span><span style="display:flex;"><span>agent<span style="color:#f92672">.</span>register_tool(Tool(store_memory))
</span></span></code></pre></div><p>Now the agent can search its own memory during conversations and store important facts for future sessions. The background promotion service also automatically identifies and promotes durable facts from session memory, so even without explicit <code>store_memory</code> calls, important information persists.</p>
<h2 id="step-5--run-the-agent-cli-chat-and-cross-session-recall-demo">Step 5 — Run the Agent: CLI Chat and Cross-Session Recall Demo</h2>
<p>Here is a complete CLI agent that demonstrates cross-session memory:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-python" data-lang="python"><span style="display:flex;"><span><span style="color:#f92672">import</span> asyncio
</span></span><span style="display:flex;"><span><span style="color:#f92672">from</span> pydantic_ai <span style="color:#f92672">import</span> Agent
</span></span><span style="display:flex;"><span><span style="color:#f92672">from</span> pydantic_ai.mcp <span style="color:#f92672">import</span> MCPToolset
</span></span><span style="display:flex;"><span><span style="color:#f92672">from</span> pydantic_ai.models.anthropic <span style="color:#f92672">import</span> AnthropicModel
</span></span><span style="display:flex;"><span><span style="color:#f92672">from</span> redis_agent_memory <span style="color:#f92672">import</span> AgentMemoryClient
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>model <span style="color:#f92672">=</span> AnthropicModel(<span style="color:#e6db74">&#34;claude-sonnet-4-20260501&#34;</span>)
</span></span><span style="display:flex;"><span>agent <span style="color:#f92672">=</span> Agent(model<span style="color:#f92672">=</span>model)
</span></span><span style="display:flex;"><span>memory <span style="color:#f92672">=</span> AgentMemoryClient(surface_id<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;northpeak&#34;</span>, agent_key<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;...&#34;</span>)
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">async</span> <span style="color:#66d9ef">def</span> <span style="color:#a6e22e">main</span>():
</span></span><span style="display:flex;"><span>    <span style="color:#66d9ef">async</span> <span style="color:#66d9ef">with</span> MCPToolset<span style="color:#f92672">.</span>from_url(
</span></span><span style="display:flex;"><span>        url<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;https://gcp-us-east4.context-surfaces.redis.io/mcp&#34;</span>,
</span></span><span style="display:flex;"><span>        headers<span style="color:#f92672">=</span>{<span style="color:#e6db74">&#34;X-API-Key&#34;</span>: <span style="color:#e6db74">&#34;...&#34;</span>}
</span></span><span style="display:flex;"><span>    ) <span style="color:#66d9ef">as</span> toolset:
</span></span><span style="display:flex;"><span>        agent<span style="color:#f92672">.</span>register_toolset(toolset)
</span></span><span style="display:flex;"><span>        
</span></span><span style="display:flex;"><span>        <span style="color:#75715e"># Load previous session context</span>
</span></span><span style="display:flex;"><span>        past <span style="color:#f92672">=</span> <span style="color:#66d9ef">await</span> memory<span style="color:#f92672">.</span>search_long_term_memory_async(
</span></span><span style="display:flex;"><span>            query<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;customer preferences&#34;</span>,
</span></span><span style="display:flex;"><span>            top_k<span style="color:#f92672">=</span><span style="color:#ae81ff">3</span>
</span></span><span style="display:flex;"><span>        )
</span></span><span style="display:flex;"><span>        <span style="color:#66d9ef">for</span> p <span style="color:#f92672">in</span> past:
</span></span><span style="display:flex;"><span>            print(<span style="color:#e6db74">f</span><span style="color:#e6db74">&#34;  [Recall] </span><span style="color:#e6db74">{</span>p[<span style="color:#e6db74">&#39;content&#39;</span>]<span style="color:#e6db74">}</span><span style="color:#e6db74">&#34;</span>)
</span></span><span style="display:flex;"><span>        
</span></span><span style="display:flex;"><span>        <span style="color:#66d9ef">while</span> <span style="color:#66d9ef">True</span>:
</span></span><span style="display:flex;"><span>            user_input <span style="color:#f92672">=</span> input(<span style="color:#e6db74">&#34;</span><span style="color:#ae81ff">\n</span><span style="color:#e6db74">You: &#34;</span>)
</span></span><span style="display:flex;"><span>            <span style="color:#66d9ef">if</span> user_input<span style="color:#f92672">.</span>lower() <span style="color:#f92672">in</span> (<span style="color:#e6db74">&#34;exit&#34;</span>, <span style="color:#e6db74">&#34;quit&#34;</span>):
</span></span><span style="display:flex;"><span>                <span style="color:#66d9ef">break</span>
</span></span><span style="display:flex;"><span>            
</span></span><span style="display:flex;"><span>            result <span style="color:#f92672">=</span> <span style="color:#66d9ef">await</span> agent<span style="color:#f92672">.</span>run_sync(
</span></span><span style="display:flex;"><span>                user_input,
</span></span><span style="display:flex;"><span>                system_prompt<span style="color:#f92672">=</span><span style="color:#e6db74">f</span><span style="color:#e6db74">&#34;Previous context: </span><span style="color:#e6db74">{</span>past<span style="color:#e6db74">}</span><span style="color:#e6db74">&#34;</span>
</span></span><span style="display:flex;"><span>            )
</span></span><span style="display:flex;"><span>            print(<span style="color:#e6db74">f</span><span style="color:#e6db74">&#34;Agent: </span><span style="color:#e6db74">{</span>result<span style="color:#f92672">.</span>data<span style="color:#e6db74">}</span><span style="color:#e6db74">&#34;</span>)
</span></span><span style="display:flex;"><span>            
</span></span><span style="display:flex;"><span>            <span style="color:#75715e"># Auto-store facts</span>
</span></span><span style="display:flex;"><span>            <span style="color:#66d9ef">await</span> memory<span style="color:#f92672">.</span>add_session_event_async(
</span></span><span style="display:flex;"><span>                event_type<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;user_query&#34;</span>,
</span></span><span style="display:flex;"><span>                content<span style="color:#f92672">=</span>user_input,
</span></span><span style="display:flex;"><span>                metadata<span style="color:#f92672">=</span>{<span style="color:#e6db74">&#34;response&#34;</span>: result<span style="color:#f92672">.</span>data}
</span></span><span style="display:flex;"><span>            )
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>asyncio<span style="color:#f92672">.</span>run(main())
</span></span></code></pre></div><p>When you run this agent, it loads relevant long-term memory at startup, enabling cross-session recall. The agent remembers who you are, your preferences, and past interactions — even if you last chatted days ago.</p>
<h2 id="how-context-retriever-auto-generates-mcp-tools-entity--tool-mapping">How Context Retriever Auto-Generates MCP Tools (Entity → Tool Mapping)</h2>
<p>The Context Retriever generates four types of MCP tools for each entity in your model:</p>
<table>
	<thead>
			<tr>
					<th>Tool Pattern</th>
					<th>Example</th>
					<th>Description</th>
			</tr>
	</thead>
	<tbody>
			<tr>
					<td><code>get_{entity}_by_id</code></td>
					<td><code>get_customer_by_id</code></td>
					<td>Fetch a single record by primary key</td>
			</tr>
			<tr>
					<td><code>filter_{entity}_by_{field}</code></td>
					<td><code>filter_order_by_status</code></td>
					<td>Filter records by exact field match</td>
			</tr>
			<tr>
					<td><code>search_{entity}_by_text</code></td>
					<td><code>search_product_by_text</code></td>
					<td>Full-text search across text fields</td>
			</tr>
			<tr>
					<td><code>find_{entity}_by_{field}_range</code></td>
					<td><code>find_order_by_date_range</code></td>
					<td>Range queries on numeric/date fields</td>
			</tr>
	</tbody>
</table>
<p>For the Northpeak Outfitters demo with 5 entities (Customer, Product, Order, Shipment, Ticket), this generates approximately 29 MCP tools. Each tool includes a JSON schema derived from the entity model, so Pydantic AI can validate inputs and outputs automatically.</p>
<p>Tool names are sanitized client-side to meet LLM provider constraints. For example, Anthropic requires tool names matching <code>^[a-zA-Z0-9_-]{1,128}$</code>, so the Context Retriever SDK normalizes names accordingly.</p>
<h2 id="how-agent-memory-works--session-events-background-promotion-semantic-search">How Agent Memory Works — Session Events, Background Promotion, Semantic Search</h2>
<p>Agent Memory operates on three layers:</p>
<p><strong>1. Session Events (Working Memory)</strong>
Every user query, agent response, and system event is recorded as a session event with a TTL. These events are searchable within the current session and provide the agent with immediate conversational context. Session events are stored in Redis with automatic expiration.</p>
<p><strong>2. Background Fact Promotion</strong>
The managed service continuously analyzes session events to identify durable facts — information that is likely to be useful across sessions (user preferences, resolved issues, personal details). These facts are automatically extracted and promoted to long-term memory without developer intervention. The promotion runs as a background process, so there is no latency impact on the agent.</p>
<p><strong>3. Long-term Semantic Memory</strong>
Promoted facts are stored as vector embeddings with metadata. The <code>search_long_term_memory_async()</code> method performs semantic (vector) search, finding relevant facts even when the query uses different wording than the original fact. Metadata filters allow scoping searches to specific users, entities, or time ranges.</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-python" data-lang="python"><span style="display:flex;"><span><span style="color:#75715e"># Semantic search with metadata filtering</span>
</span></span><span style="display:flex;"><span>results <span style="color:#f92672">=</span> <span style="color:#66d9ef">await</span> memory<span style="color:#f92672">.</span>search_long_term_memory_async(
</span></span><span style="display:flex;"><span>    query<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;prefers express shipping&#34;</span>,
</span></span><span style="display:flex;"><span>    metadata_filters<span style="color:#f92672">=</span>{
</span></span><span style="display:flex;"><span>        <span style="color:#e6db74">&#34;user_id&#34;</span>: <span style="color:#e6db74">&#34;user_123&#34;</span>,
</span></span><span style="display:flex;"><span>        <span style="color:#e6db74">&#34;entity_type&#34;</span>: <span style="color:#e6db74">&#34;preference&#34;</span>
</span></span><span style="display:flex;"><span>    },
</span></span><span style="display:flex;"><span>    top_k<span style="color:#f92672">=</span><span style="color:#ae81ff">5</span>
</span></span><span style="display:flex;"><span>)
</span></span></code></pre></div><h2 id="complete-code-walkthrough--agentpy-memorypy-configpy-clipy">Complete Code Walkthrough — agent.py, memory.py, config.py, cli.py</h2>
<p>The reference implementation at <code>github.com/coleam00/redis-iris-agent</code> organizes the code into four modules:</p>
<p><strong>config.py</strong> — Central configuration for Redis Cloud connection, Context Retriever URL, Agent Memory surface ID, and LLM model selection. Default model is <code>anthropic:claude-sonnet-4-6</code>.</p>
<p><strong>memory.py</strong> — Wraps the <code>redis-agent-memory</code> SDK with convenience methods for session events, long-term memory search, and bulk memory creation. Handles serialization and error handling.</p>
<p><strong>agent.py</strong> — Builds the Pydantic AI agent, connects to the Context Retriever via MCPToolset, registers memory tools, and configures system prompts. This is the core orchestration module.</p>
<p><strong>cli.py</strong> — A command-line chat interface that loads previous memory at startup, runs the agent loop, and stores session events after each interaction. Demonstrates the full cross-session memory flow.</p>
<p>The total codebase is under 500 lines, illustrating how much Redis Iris abstracts away.</p>
<h2 id="running-the-northpeak-outfitters-demo-full-reproducible-example">Running the Northpeak Outfitters Demo (Full Reproducible Example)</h2>
<p>The Northpeak Outfitters demo is a support-desk scenario with 134 records across 5 entities:</p>
<ul>
<li><strong>Customer</strong> (24 records) — name, email, phone, address, loyalty tier</li>
<li><strong>Product</strong> (45 records) — name, category, price, stock, description</li>
<li><strong>Order</strong> (38 records) — customer_id, product_ids, status, total, date</li>
<li><strong>Shipment</strong> (18 records) — order_id, carrier, tracking, status, date</li>
<li><strong>Ticket</strong> (9 records) — customer_id, subject, status, priority, messages</li>
</ul>
<p>To run the demo:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-bash" data-lang="bash"><span style="display:flex;"><span>git clone https://github.com/coleam00/redis-iris-agent
</span></span><span style="display:flex;"><span>cd redis-iris-agent
</span></span><span style="display:flex;"><span>pip install -r requirements.txt
</span></span><span style="display:flex;"><span>cp .env.example .env  <span style="color:#75715e"># Add your Redis Cloud, Context Retriever, and LLM keys</span>
</span></span><span style="display:flex;"><span>python cli.py
</span></span></code></pre></div><p>The agent can answer questions like:</p>
<ul>
<li>&ldquo;What orders does John Smith have that are still pending?&rdquo;</li>
<li>&ldquo;Find all products in the camping category under $100&rdquo;</li>
<li>&ldquo;Show me the shipment status for order ORD-2026-0042&rdquo;</li>
<li>&ldquo;What support tickets are high priority and unresolved?&rdquo;</li>
</ul>
<p>Each query triggers auto-generated MCP tools without any custom data-access code.</p>
<h2 id="best-practices--entity-naming-tool-sanitization-multi-user-scoping">Best Practices — Entity Naming, Tool Sanitization, Multi-User Scoping</h2>
<p><strong>Entity Naming:</strong></p>
<ul>
<li>Use singular nouns for entity names (Customer, not Customers)</li>
<li>Keep field names consistent (camelCase or snake_case, but not mixed)</li>
<li>Include proper types in your entity model (string, number, date, array)</li>
</ul>
<p><strong>Tool Sanitization:</strong></p>
<ul>
<li>The Context Retriever SDK automatically sanitizes tool names for LLM provider constraints</li>
<li>Test with your target LLM provider — Anthropic and OpenAI have different naming rules</li>
<li>Avoid special characters in entity and field names to prevent sanitization conflicts</li>
</ul>
<p><strong>Multi-User Scoping:</strong></p>
<ul>
<li>Generate separate agent keys for each user or tenant</li>
<li>Use metadata filters on Agent Memory searches to scope results by user_id</li>
<li>The Context Retriever supports server-side access control via agent key permissions</li>
<li>This eliminates the need for custom auth middleware in your agent</li>
</ul>
<h2 id="redis-iris-vs-alternatives--langchain-memory-mem0-folder-of-markdown">Redis Iris vs Alternatives — LangChain Memory, Mem0, Folder-of-Markdown</h2>
<table>
	<thead>
			<tr>
					<th>Feature</th>
					<th>Redis Iris</th>
					<th>LangChain Memory</th>
					<th>Mem0</th>
					<th>Folder-of-Markdown</th>
			</tr>
	</thead>
	<tbody>
			<tr>
					<td>Auto-generated MCP tools</td>
					<td>✅ Yes</td>
					<td>❌ No</td>
					<td>❌ No</td>
					<td>❌ No</td>
			</tr>
			<tr>
					<td>Session memory with TTL</td>
					<td>✅ Yes</td>
					<td>✅ Yes</td>
					<td>✅ Yes</td>
					<td>❌ Manual</td>
			</tr>
			<tr>
					<td>Long-term semantic memory</td>
					<td>✅ Yes</td>
					<td>❌ No</td>
					<td>✅ Yes</td>
					<td>❌ No</td>
			</tr>
			<tr>
					<td>Background fact promotion</td>
					<td>✅ Yes (managed)</td>
					<td>❌ No</td>
					<td>❌ No</td>
					<td>❌ No</td>
			</tr>
			<tr>
					<td>Vector search with metadata</td>
					<td>✅ Yes</td>
					<td>❌ No</td>
					<td>✅ Yes</td>
					<td>❌ No</td>
			</tr>
			<tr>
					<td>Data integration (live DB)</td>
					<td>✅ Yes (Context Retriever)</td>
					<td>❌ No</td>
					<td>❌ No</td>
					<td>❌ No</td>
			</tr>
			<tr>
					<td>Multi-user scoping</td>
					<td>✅ Server-side</td>
					<td>❌ Manual</td>
					<td>❌ Manual</td>
					<td>❌ Manual</td>
			</tr>
			<tr>
					<td>Setup complexity</td>
					<td>Low (managed service)</td>
					<td>Medium</td>
					<td>Medium</td>
					<td>High</td>
			</tr>
			<tr>
					<td>Production readiness</td>
					<td>Preview (proof-of-concept)</td>
					<td>Production</td>
					<td>Production</td>
					<td>DIY</td>
			</tr>
	</tbody>
</table>
<p>Redis Iris stands out for its <strong>all-in-one approach</strong> — it combines data access (Context Retriever) with memory (Agent Memory) in a single managed service. Alternatives require stitching together multiple libraries and handling data integration separately.</p>
<h2 id="limitations-and-preview-caveats">Limitations and Preview Caveats</h2>
<p>Redis Iris is currently in <strong>preview</strong> and marked as a proof-of-concept. Key limitations to be aware of:</p>
<ol>
<li><strong>Preview status</strong> — Both Context Retriever and Agent Memory are not yet production-ready. Expect API changes and potential downtime.</li>
<li><strong>Limited regions</strong> — The MCP endpoint is currently only available in <code>gcp-us-east4</code>. Latency may be significant for agents deployed in other regions.</li>
<li><strong>Entity model constraints</strong> — Complex relational queries (joins across entities) are not supported. Each tool operates on a single entity type.</li>
<li><strong>No streaming MCP</strong> — The current implementation uses streamable-HTTP, not SSE streaming. This may impact real-time applications.</li>
<li><strong>SDK maturity</strong> — The <code>redis-agent-memory</code> and <code>redis-context-retriever</code> SDKs are early-stage. Error handling and documentation are still evolving.</li>
<li><strong>Pricing</strong> — As a preview service, pricing for production use has not been announced.</li>
</ol>
<p>Despite these caveats, Redis Iris represents a significant step forward in agent context management and is worth evaluating for your AI agent architecture.</p>
<h2 id="conclusion--when-to-use-redis-iris-for-your-ai-agents">Conclusion — When to Use Redis Iris for Your AI Agents</h2>
<p>Redis Iris is an excellent choice when you are building Pydantic AI agents that need:</p>
<ul>
<li><strong>Live data access</strong> without writing custom API or database code</li>
<li><strong>Cross-session memory</strong> that persists and recalls relevant context automatically</li>
<li><strong>Multi-tenant support</strong> with server-side access control</li>
<li><strong>Rapid prototyping</strong> — the Northpeak Outfitters demo can be running in under 30 minutes</li>
</ul>
<p>It is less suitable for production workloads today (given its preview status), complex multi-entity join queries, or teams that need full control over their retrieval pipeline.</p>
<p>For developers building the next generation of AI agents, Redis Iris demonstrates a compelling vision: agents that are genuinely informed by your data, remember every interaction, and require minimal boilerplate to set up. As the service matures toward production, it could become the default context layer for Pydantic AI agents.</p>
<h2 id="faq">FAQ</h2>
<p><strong>Q: What is the difference between Context Retriever and Agent Memory in Redis Iris?</strong>
A: Context Retriever auto-generates MCP tools from your entity model, giving the agent live access to your database. Agent Memory manages conversation history — short-term session memory with TTL and long-term semantic memory with vector search and background fact promotion. They are complementary services that together form the Redis Iris context layer.</p>
<p><strong>Q: Does Redis Iris work with any LLM provider?</strong>
A: Yes. Redis Iris is provider-agnostic. The Context Retriever exposes standard MCP tools, and Pydantic AI supports OpenAI, Anthropic, Google, and custom models. The reference implementation defaults to Anthropic&rsquo;s Claude Sonnet 4, but you can switch providers by changing the model in your Pydantic AI agent configuration.</p>
<p><strong>Q: How many MCP tools does Context Retriever generate?</strong>
A: The Context Retriever generates four tool types per entity: get_by_id, filter_by, search_by_text, and find_by_range. For the Northpeak Outfitters demo with 5 entities, this produces approximately 29 MCP tools. The exact count depends on the number of fields in each entity that support each operation type.</p>
<p><strong>Q: Can I use Redis Iris without Pydantic AI?</strong>
A: Yes. The Context Retriever exposes standard MCP tools over streamable-HTTP, which any MCP-compatible client can consume. However, Pydantic AI&rsquo;s native MCPToolset support (since pydantic-ai-slim&gt;=2.2.0) provides the most seamless integration. Other frameworks like LangGraph also work with Redis Iris, as demonstrated by several community reference implementations.</p>
<p><strong>Q: Is Redis Iris free to use?</strong>
A: Redis Iris is currently in preview and marked as a proof-of-concept. You need a Redis Cloud subscription (free tier available) for the underlying database. The Context Retriever and Agent Memory services are available during the preview period, but pricing for production use has not been announced. Check redis.com/iris for the latest pricing information.</p>
]]></content:encoded></item></channel></rss>