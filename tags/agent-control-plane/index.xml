<?xml version="1.0" encoding="utf-8" standalone="yes"?><rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom" xmlns:content="http://purl.org/rss/1.0/modules/content/"><channel><title>Agent-Control-Plane on RockB</title><link>https://baeseokjae.github.io/tags/agent-control-plane/</link><description>Recent content in Agent-Control-Plane on RockB</description><image><title>RockB</title><url>https://baeseokjae.github.io/images/og-default.png</url><link>https://baeseokjae.github.io/images/og-default.png</link></image><generator>Hugo</generator><language>en-us</language><lastBuildDate>Sat, 05 Sep 2026 13:01:48 +0000</lastBuildDate><atom:link href="https://baeseokjae.github.io/tags/agent-control-plane/index.xml" rel="self" type="application/rss+xml"/><item><title>Swarm: Local-First Control Plane Agent Watching Every Claude Code Session Live</title><link>https://baeseokjae.github.io/posts/swarm-local-control-plane-claude-code-2026/</link><pubDate>Sat, 05 Sep 2026 13:01:48 +0000</pubDate><guid>https://baeseokjae.github.io/posts/swarm-local-control-plane-claude-code-2026/</guid><description>Swarm is a local-first control plane agent that watches every Claude Code session live, stops collisions, and enforces CLAUDE.md rules offline.</description><content:encoded><![CDATA[<p>A local-first control plane agent like Swarm watches every Claude Code session on your machine live so you can see each agent&rsquo;s tool calls, reasoning, token spend, and cost from one dashboard instead of cycling through terminal tabs. It keeps a ledger of who holds which task and worktree, turns CLAUDE.md &ldquo;never do X&rdquo; prose into real permission decisions, and runs entirely offline with no account and no telemetry.</p>
<h2 id="what-is-a-local-first-control-plane-for-coding-agents">What Is a Local-First Control Plane for Coding Agents?</h2>
<p>A control plane is the management layer that decides how the agents underneath it behave. In IT operations, the control plane and the data plane are separated: the data plane does the work, while the control plane watches, meters, and governs. <a href="https://github.com/ra3orblade/swarm">ra3orblade/swarm</a> applies that same separation to AI coding agents, with one crucial twist: the entire control plane runs on your own machine.</p>
<p>Swarm is a TypeScript/Bun project, Apache-2.0 licensed, that installs with a single command — <code>bunx @ra3orblade/swarm setup</code>. Once running, one daemon watches every Claude Code session live, along with Codex CLI, Gemini, Grok, Aider, and opencode sessions. For each one it captures the live reasoning stream, tool calls, token spend, and running cost. It does not add anything to your repositories, requires no account, no telemetry, and works fully offline.</p>
<p>The word &ldquo;swarm&rdquo; here means the control plane manages a <em>group</em> of agents, not a single session. The tagline says it best: &ldquo;See every agent. Stop the collisions.&rdquo;</p>
<h2 id="the-problem-losing-the-thread-across-multiple-agent-sessions">The Problem: Losing the Thread Across Multiple Agent Sessions</h2>
<p>Anyone who runs more than one or two coding agents in parallel has felt this specific frustration. You have three terminal tabs open, each running a different Claude Code or Codex session. To know what any of them is doing you have to switch tabs, scroll through logs, and piece together what the agent thinks it is doing versus what it is actually doing. When two agents touch the same file or the same port, the results are silent merge conflicts and a mystery about who killed your dev server.</p>
<p>The problem compounds as teams grow. Anthropic made agent <em>teams</em> a first-class Claude Code feature in early 2026 — a lead agent that delegates to parallel teammates that research, debug, and build while coordinating (<a href="https://addyosmani.com/blog/claude-code-agent-teams/">Addy Osmani, Feb 2026</a>). Community tooling had already been calling these workflows &ldquo;swarms&rdquo; — coordinated teams of specialist agents. But a team of agents running against the same repository without a shared view of state is a recipe for collisions.</p>
<h2 id="swarms-core-screens-fleet-session-board">Swarm&rsquo;s Core Screens: Fleet, Session, Board</h2>
<p>Swarm organizes its observability around four main screens, each answering a different operational question.</p>
<p><strong>Fleet</strong> is the at-a-glance view of every agent running on your machine. You see the full list of sessions, which CLI each one is using, and its live status. This is the replacement for the terminal-tab shuffle — instead of guessing which terminal holds which agent, you get one pane that answers &ldquo;what is running right now?&rdquo;</p>
<p><strong>Session</strong> is the deep-dive view for a single agent. It shows the live reasoning stream, every tool call, and the running token and cost counters. Because Swarm is local-first, it records the full transcript to disk, which enables session replay — you can rewatch what an agent did even after it finished — and a &ldquo;resume-where-it-died&rdquo; flow that lets you restart an interrupted session from its exact state.</p>
<p><strong>Board</strong> is the coordination surface: which task each agent holds, which worktree, and which runtime resources. This is where Swarm&rsquo;s collision-prevention story lives. A collision graph shows conflicts explicitly rather than letting them surface as surprise merge conflicts later.</p>
<p>Together these three screens form what the project calls the missing &ldquo;observability layer&rdquo; for the multi-agent age — one daemon watching every session instead of cycling through terminal tabs.</p>
<h2 id="turning-claudemd-guardrails-into-real-permission-decisions">Turning CLAUDE.md Guardrails into Real Permission Decisions</h2>
<p>The cleverest idea in Swarm is that it converts documentation into enforcement. A CLAUDE.md file often says things like &ldquo;never run <code>git push --force</code>&rdquo; or &ldquo;don&rsquo;t kill random processes&rdquo; — but that is prose. Claude Code reads it as a style hint. Nothing structurally stops a session from running a destructive command.</p>
<p>Swarm adds a <code>Rules</code> set that turns those instructions into real permission decisions. The rules target Bash commands a Claude Code session runs: <code>shared_tree</code>, <code>destructive_git</code>, <code>pattern_kill</code>, <code>protected_ports</code>, <code>no_foreign_worktree</code>, and the opt-in <code>claim_required_to_write</code>. Each rule is set to <code>ask</code>, <code>deny</code>, or <code>off</code> per repo in a <code>.swarm.toml</code>. When a rule fires, Swarm returns an actual permission denial to Claude Code rather than leaving the decision to chance.</p>
<p>This is explicitly not a sandbox. Swarm is guardrails against accidents, not a security boundary. A determined agent could still work around it. But for the common failure mode — an agent running <code>git clean -fdx</code> in the wrong directory, or binding a port that your dev server already owns — a <code>deny</code> rule stops the accident before it starts.</p>
<h2 id="coordination-through-git-worktrees-and-runtime-resources">Coordination Through Git Worktrees and Runtime Resources</h2>
<p>Collisions are the core coordination story, and Swarm attacks them from two directions at once: task claims in isolated git worktrees, and runtime resources.</p>
<p>Git worktrees are Swarm&rsquo;s default isolation mechanism. Each agent gets its own worktree, so parallel sessions are less likely to write over each other&rsquo;s files. The <code>claim_required_to_write</code> rule (opt-in) reinforces this: an agent must hold an explicit claim on a task and its worktree before it may write. This is a concrete answer to the classic multi-agent problem of two agents editing the same file.</p>
<p>Runtime resources are the second half. Ports, dev servers, and databases become named singletons. Because Swarm is watching every session, it knows which agent holds which resource. When a second agent tries to start on a port that is already claimed, the collision graph flags it instead of letting both agents silently fight over the same socket. For anyone who has lost a dev server to a rampaging agent this is the feature that pays for itself.</p>
<h2 id="run--dispatch-handoffs-and-the-built-in-reviewer">Run &amp; Dispatch, Handoffs, and the Built-in Reviewer</h2>
<p>Beyond observation, Swarm ships orchestration features that make it a genuine control plane rather than just a telescope.</p>
<p><strong>Run &amp; Dispatch</strong> covers the spawning side. <code>swarm run</code> and <code>swarm dispatch</code> let you start agent tasks from the control plane, including headless agents running in git worktrees. Declarative workflows let you define multi-step processes the agents follow.</p>
<p><strong>Handoffs</strong> and <strong>agent-to-agent messaging</strong> let one agent pass a task to another cleanly. This mirrors the delegation patterns that make agent teams productive: a lead agent plans, delegates, and synthesizes rather than writing everything itself, which keeps its context clean and reduces token usage.</p>
<p><strong>Verification gates</strong> come with a built-in reviewer. When an agent finishes work, the control plane can require a review pass before the task is accepted. This connects directly to Cursor&rsquo;s finding that stacked review is high-return because review is cheaper than the work it audits — the reviewer agent is a cheap insurance policy against low-quality output.</p>
<h2 id="local-first-by-design-no-account-no-telemetry-works-offline">Local-First by Design: No Account, No Telemetry, Works Offline</h2>
<p>The privacy architecture is central to Swarm&rsquo;s identity. Everything runs entirely on your machine: no account, no telemetry, works offline, and nothing is added to your repositories. For a tool whose entire job is to record complete agent conversation histories, that local-first stance is not a footnote — it is the point.</p>
<p>The contrast with built-in tooling is stark. Claude Code&rsquo;s &ldquo;Swarms&rdquo; feature (which leaked via <a href="https://news.ycombinator.com/item?id=46743908">Hacker News in Jan 2026</a>, 521 points and 335 comments) ships behind a feature flag that <em>phones home to the backend.</em> The controversy centered on privacy: the telemetry captures &ldquo;Claude session JSONL files (when accessible)&rdquo; — complete conversation histories. With Swarm, those same transcripts never leave your machine.</p>
<p>If your agents work on proprietary codebase, or if you are under a compliance regime that forbids sending source context to a third party, a local-first control plane is the difference between being able to run swarms at all and being blocked.</p>
<h2 id="the-economics-of-agent-swarms-cost-tokens-and-review">The Economics of Agent Swarms: Cost, Tokens, and Review</h2>
<p>Swarm&rsquo;s live token spend, cost-per-turn, cache hit rate, and budgets directly address the economics of running many agents. <a href="https://cursor.com/blog/agent-swarm-model-economics">Cursor&rsquo;s agent-swarm economics post</a> (2026-07-20) is the anchor here: an earlier-2026 browser swarm peaked at roughly 1,000 commits per hour on Git, while the new system peaks at around 1,000 commits per <em>second</em>. Better coordination delivers similar quality at a fraction of the cost.</p>
<p>Delegation is the mechanism that makes swarms cheaper. When a team lead delegates, subagents get fresh, focused contexts, which leads to less token usage and better reasoning. Swarm&rsquo;s observability lets you watch that cost in real time rather than discovering it in a monthly invoice.</p>
<p>The economics argument is not uncontested. An <a href="https://news.ycombinator.com/item?id=48982535">Hacker News thread from Jul 2026</a> pushed back: only models trusted to work autonomously still cost more than a human employee, and there is real concern about reviewing copious generated code. The mitigation is automated reviewer agents and verification gates — which is exactly the stack Swarm ships.</p>
<h2 id="swarm-vs-claude-codes-built-in-swarms-and-other-control-planes">Swarm vs. Claude Code&rsquo;s Built-in Swarms and Other Control Planes</h2>
<p>Swarm is not the only local-first control plane on the market, and it sits alongside — not against — Anthropic&rsquo;s built-in Swarms. Here is how they compare.</p>
<table>
  <thead>
      <tr>
          <th>Capability</th>
          <th>Swarm (ra3orblade)</th>
          <th>Swarmery</th>
          <th>Claude Code built-in Swarms</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>Locality</td>
          <td>Local-first, offline, no account</td>
          <td>Local-first, 100% local data</td>
          <td>Phones home, feature-flagged</td>
      </tr>
      <tr>
          <td>Runtime</td>
          <td>TypeScript/Bun</td>
          <td>Go 1.25 binary + React 19 UI</td>
          <td>Native Claude Code feature</td>
      </tr>
      <tr>
          <td>Session observation</td>
          <td>Tool calls, reasoning, tokens, cost</td>
          <td>Tool calls, diffs, cost, sub-agents</td>
          <td>Within the harness</td>
      </tr>
      <tr>
          <td>Permission enforcement</td>
          <td>CLAUDE.md → ask/deny rules</td>
          <td>Approve/deny queue, auto-approve</td>
          <td>Built-in prompt approvals</td>
      </tr>
      <tr>
          <td>Isolation</td>
          <td>Git worktrees + runtime claims</td>
          <td>Git worktrees, cards</td>
          <td>Delegation mode</td>
      </tr>
      <tr>
          <td>License</td>
          <td>Apache-2.0</td>
          <td>Control plane PolyForm NC, framework Apache-2.0</td>
          <td>Proprietary</td>
      </tr>
      <tr>
          <td>Cost tracking</td>
          <td>Live spend, budgets, cache rate</td>
          <td>Cost per session</td>
          <td>Via usage metrics</td>
      </tr>
  </tbody>
</table>
<p>Swarmery is the closest direct competitor — a Go/React local-first control plane that listens on <code>:7777</code>, backfills from <code>~/.claude/projects</code> JSONL transcripts with zero instrumentation, and gives you a permission-approval queue. The <a href="https://news.ycombinator.com/item?id=46743908">HN debate</a> about built-in Swarms vs third-party wrappers (Conductor, Gas Town, claude-flow, GSD, and others) framed the deciding factor as tight integration into the harness plus delegation mode and a mailbox system. Swarm&rsquo;s bet is that a neutral, CLI-agnostic control plane that watches <em>six</em> different CLIs is more valuable than being locked into one harness.</p>
<h2 id="privacy-and-the-sovereignty-argument">Privacy and the Sovereignty Argument</h2>
<p>Sovereignty is the deepest reason to prefer a local-first control plane. When your agent transcripts and source context stay on your machine, you retain full control over who can access them, how long they are retained, and what happens to them when you stop using the tool. You are not at the mercy of a vendor&rsquo;s retention policy or a backend outage.</p>
<p>This matters more as agents become more capable and are trusted with more sensitive work. If an agent is going to read your private schema, customer data references, or proprietary algorithms, the tool that records that work should not silently upload it. Swarm&rsquo;s design — no account, no telemetry, fully offline — is engineered to make that guarantee structural rather than aspirational.</p>
<h2 id="who-its-for-limitations-and-getting-started">Who It&rsquo;s For, Limitations, and Getting Started</h2>
<p>Swarm is early but real. As of its launch window it had eleven releases in roughly two weeks, is dogfooded daily (the project notes &ldquo;Swarm dispatches its own tasks&rdquo;), and covers observability across six agent CLIs. It is a rapidly moving, shipping-fast tool — but it is also young, with a small star count and a single-digit release history.</p>
<p>It is for developers and teams who run multiple coding agents in parallel and have felt the collision problem: parallel worktrees fighting over files, agents stealing ports, and a nagging inability to see the whole fleet at once. It is especially attractive if you need offline operation or cannot send code to a third party.</p>
<p>Its limitations are honest ones. With a small community and young codebase, you should expect rough edges and rapid change rather than a mature product. Its rules are guardrails, not a security sandbox. And the ecosystem is young enough that you will likely need to configure and shape it to your workflow.</p>
<p>Getting started is a one-liner: <code>bunx @ra3orblade/swarm setup</code>. From there you point it at your repositories, define your <code>.swarm.toml</code> rules, and start watching your sessions from the Fleet. There is no account to create and nothing to host.</p>
<h2 id="verdict-is-swarm-worth-running-today">Verdict: Is Swarm Worth Running Today?</h2>
<p>If you run only a single agent occasionally, a full control plane is probably overkill. But if you already run multiple agents in parallel — or you are planning a real agent team — Swarm is worth serious consideration today. It solves a problem built-in tooling leaves open, it does it locally and offline, and it is moving fast enough that its early rough edges are likely to harden quickly.</p>
<p>The honest caveats remain: it is early, the community is small, and the enforcement rules are guardrails rather than a sandbox. But the core value proposition — see every agent, stop the collisions, keep it all on your machine — is exactly the missing observability layer for the multi-agent age. For teams ready to run code agents like a fleet rather than a pile of terminal tabs, Swarm is today&rsquo;s most credible local-first answer.</p>
<h2 id="faq">FAQ</h2>
<p><strong>What exactly does a local control plane agent like Swarm do?</strong>
It runs a daemon on your machine that watches every Claude Code (and Codex, Gemini, Grok, Aider, opencode) session live, capturing reasoning, tool calls, token spend, and cost, and surfaces them all in one Fleet dashboard. It also coordinates tasks so parallel agents don&rsquo;t collide.</p>
<p><strong>Is Swarm safe to use with proprietary code?</strong>
Yes. Swarm is local-first by design — no account, no telemetry, works offline, and adds nothing to your repositories. Your agent transcripts and source context never leave your machine, which is a structural contrast to built-in features that phone home.</p>
<p><strong>How does Swarm stop multiple agents from colliding?</strong>
It issues task claims in isolated git worktrees and manages runtime resources (ports, dev servers, databases) as named singletons. A collision graph flags conflicts explicitly, and the opt-in <code>claim_required_to_write</code> rule forces an agent to hold a claim before writing.</p>
<p><strong>Can Swarm enforce my CLAUDE.md rules?</strong>
Yes. Swarm converts CLAUDE.md &ldquo;never do X&rdquo; prose into real permission decisions via rules like <code>destructive_git</code>, <code>pattern_kill</code>, and <code>protected_ports</code>, each set to ask, deny, or off per repo in <code>.swarm.toml</code>. It returns actual permission denials rather than leaving behavior to chance.</p>
<p><strong>Which agent CLIs does Swarm observe?</strong>
Swarm watches every Claude Code session plus Codex CLI, Gemini, Grok, Aider, and opencode — six CLI types in total. It is CLI-agnostic, which distinguishes it from control planes locked into a single harness.</p>
]]></content:encoded></item></channel></rss>