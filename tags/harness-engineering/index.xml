<?xml version="1.0" encoding="utf-8" standalone="yes"?><rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom" xmlns:content="http://purl.org/rss/1.0/modules/content/"><channel><title>Harness Engineering on RockB</title><link>https://baeseokjae.github.io/tags/harness-engineering/</link><description>Recent content in Harness Engineering on RockB</description><image><title>RockB</title><url>https://baeseokjae.github.io/images/og-default.png</url><link>https://baeseokjae.github.io/images/og-default.png</link></image><generator>Hugo</generator><language>en-us</language><lastBuildDate>Thu, 17 Sep 2026 07:01:48 +0000</lastBuildDate><atom:link href="https://baeseokjae.github.io/tags/harness-engineering/index.xml" rel="self" type="application/rss+xml"/><item><title>Better-Harness: An Open-Source Harness Engineering Platform for Better Harness Coding Agents</title><link>https://baeseokjae.github.io/posts/better-harness-open-source-harness-engineering-platform/</link><pubDate>Thu, 17 Sep 2026 07:01:48 +0000</pubDate><guid>https://baeseokjae.github.io/posts/better-harness-open-source-harness-engineering-platform/</guid><description>Better-Harness gives better harness coding agents an open-source, host-agnostic loop: feedforward guides, feedback sensors, and evidence-bounded reports across five work dimensions.</description><content:encoded><![CDATA[<p>If you want better harness coding agents, the fastest lever is rarely a newer model — it is the harness that surrounds the model. Better-Harness is an MIT-licensed, open-source harness engineering platform from QoderAI that runs inside your existing coding agents (Claude Code, Codex, Cursor, and more) rather than replacing them. It combines feedforward guides with feedback sensors, then evaluates your agent across five work-dimension scores that keep missing evidence explicit instead of fabricating plausible numbers.</p>
<h2 id="what-is-the-agent-harness-anyway">What Is the Agent Harness, Anyway?</h2>
<p>The phrase &ldquo;Agent = Model + Harness&rdquo; is not marketing spin; it is a structural claim about where reliability actually comes from. Martin Fowler defines the harness as everything around the model — the outer user harness that shapes how an agent behaves before, during, and after a coding task. That outer harness serves two goals: raise the odds of first-try success, and build a self-correction loop that works before a human ever reviews the result.</p>
<p>Feedforward mechanisms push guidance in ahead of action: AGENTS.md files, task specs, acceptance criteria, and reusable skills. Feedback sensors pull signals back after action: linters, tests, CI gateways, review agents, and evaluation agents. The engineering skill is not adding more of either — it is pairing the right guide with the right sensor so the loop closes quickly.</p>
<p>The same principle appears across the industry. OpenAI documented shipping a product built with roughly 1 million lines of code across about 1,500 pull requests with zero manually written code, running at about 3.5 PRs per engineer per day. Their sharpest lesson was not &ldquo;try harder&rdquo;; it was that early progress stayed slow because the environment was underspecified, and the fix was better legibility — observability and CDP-based introspection — not a stronger prompt.</p>
<p>A widely-cited teardown of Claude Code went further, finding that roughly 98 percent of the tool is the harness, not the model. That number is a useful heuristic even if you discount it: when your agent misbehaves, the surrounding system of guides, sensors, and gates is where most of the fix lives.</p>
<h2 id="what-better-harness-actually-is">What Better-Harness Actually Is</h2>
<p>Better-Harness is an open-source harness engineering platform written in JavaScript, released under the MIT license, and added to GitHub in late July 2026. It sits at roughly 2,261 stars and about 180 forks, with three npm versions and ten dependents. It does not wrap your coding agent in a new client; instead, it runs through whichever agent you already use.</p>
<p>Supported hosts include Claude Code, Codex Desktop and CLI, Qoder, Cursor, qwen-code, GitHub Copilot CLI, Kimi Code, Pi, WorkBuddy, and Grok. That host-agnostic design matters because it means your existing provider, workflow, and prompts survive; Better-Harness layers the evaluation loop on top of them.</p>
<p>Conceptually, the platform formalizes the same feedforward-and-feedback architecture that Fowler and Augment both describe. Guides steer the agent before it acts; sensors — linters, tests, hooks, and evaluation agents — trigger self-correction when output drifts. The whole system is meant to be iterated on: when a failure recurs, you adjust the harness, not the model.</p>
<h2 id="the-five-dimensions-of-the-agent-work-loop">The Five Dimensions of the Agent Work Loop</h2>
<p>Better-Harness evaluates your agent on five Agent Work Loop dimensions rather than a single headline number. That split matters because a coding agent can be excellent at one stage and weak at another, and a total score hides the failure.</p>
<ul>
<li>Task understanding — did the agent correctly interpret the request and the acceptance criteria before acting?</li>
<li>Controlled execution — did it act within the guardrails and scope you set, or did it wander?</li>
<li>Change validation — did linters, tests, and hooks actually run, and did the change pass them?</li>
<li>Reliable delivery — did the agent deliver a mergeable, working result on the first real attempt?</li>
<li>Learning capture — did the loop record what went wrong so the next run avoids the same pitfall?</li>
</ul>
<p>These five dimensions line up with wider measurement thinking. Augment Code recommends tracking task resolution rate, code churn, verification tax, defect escape rate, and pass@1. Better-Harness&rsquo;s dimensions are effectively a practical mapping of those ideas onto a single runnable loop, evaluated in your own repository rather than on a benchmark you never ship.</p>
<h2 id="why-evidence-bounded-findings-matter">Why Evidence-Bounded Findings Matter</h2>
<p>One of the more refreshing properties of Better-Harness is what it refuses to do: it will not fabricate a score. When evidence for a dimension is missing, the report says so explicitly instead of assigning a plausible-looking number. That &ldquo;missing evidence stays explicit&rdquo; stance is genuinely different from opaque evaluation suites that return confident grades with no trail to reproduce them.</p>
<p>The contrast matters because the industry is full of inflated-looking numbers. Top coding agents post 65% to 76.8% resolution rates on SWE-bench-verified Python tasks, yet METR has warned that many benchmark-passing PRs would never actually merge in a real codebase. A DORA study found that 30% of developers reported little to no trust in AI-generated code. When every vendor quotes a benchmark, the only trustworthy signal is one you can reproduce against your own repository — and that is exactly what a harness with explicit &ldquo;evidence missing&rdquo; markers forces you to look at.</p>
<p>Reproducibility is the point: if your report cannot tell you which dimension was never measured, you cannot know whether your harness improved or your benchmark got easier.</p>
<h2 id="where-better-harness-sits-vs-prompt-and-context-engineering">Where Better-Harness Sits vs. Prompt and Context Engineering</h2>
<p>Prompt engineering, context engineering, and harness engineering are often conflated, but they operate at different temporal scopes. Augment Code offers a clean way to slice them: prompt engineering shapes a single turn, context engineering shapes a single context, and harness engineering shapes the full task lifetime. A prompt is a snapshot of intent; a harness is a system that runs before, during, and after the model acts.</p>
<p>In that framing, Better-Harness is unambiguously a harness-engineering tool. It does not improve your one-shot prompt, and it does not curate your working context. It builds the loop around the lifetime of the task: guides in ahead, sensors out after, and a steering loop for you to tighten each time a failure recurs. Monolithic instruction files tend to fail, as OpenAI observed — skills-as-map beats encyclopedia context. Better-Harness operationalizes the map rather than the encyclopedia.</p>
<h2 id="the-open-source-advantage-harness-as-code">The Open-Source Advantage: Harness as Code</h2>
<p>Because Better-Harness is MIT-licensed and written in JavaScript with npm packaging, your harness configuration becomes version-controlled infrastructure rather than a private configuration blob. Teams can treat guides, sensors, and quality gates as code: reviewed, diffed, and reverted like any other change. That is the natural fit for the &ldquo;harness as code&rdquo; idea — your reliability system belongs in the same repo discipline as your application.</p>
<p>The open license also means you are not locked into a closed evaluation platform. If the built-in gates do not cover your compliance checks, your linters, or your own review agents, you extend the platform instead of filing a feature request. The ~180 forks and 10 npm dependents are early evidence that the community is already bending it to fit real workflows rather than adopting a one-size-fits-all score.</p>
<p>This matters for teams that treat coding agents as an ongoing system to improve, not an event to benchmark once. Longitudinal validation — running the loop repeatedly to show the loop itself got better — is only practical when the harness is cheap, local, and yours to modify.</p>
<h2 id="caveats-and-honest-limits">Caveats and Honest Limits</h2>
<p>Before you adopt Better-Harness, keep three limits in mind.</p>
<p>First, evidence is not causal proof. A high score across the five dimensions shows your agent and harness behave well under your conditions; it does not prove the loop caused the improvement. You still need controlled before-and-after runs to attribute gains.</p>
<p>Second, harnessability is not uniform. Martin Fowler&rsquo;s point holds here: not every codebase is equally amenable to harness controls. A greenfield service with fast tests and clean interfaces is far more harnessable than a legacy monolith with flaky suites and tangled dependencies. If your feedback sensors are unreliable, the loop will tell you that loudly — but it cannot repair poor test infrastructure for you.</p>
<p>Third, Better-Harness optimizes what you can measure. It is designed to keep missing evidence explicit, but it cannot force you to define the right dimensions in the first place. Teams that skip the guide-and-sensor design will get an honest report of an underbuilt harness, which is useful information but not a solution.</p>
<h2 id="verdict-who-should-adopt-better-harness">Verdict: Who Should Adopt Better-Harness</h2>
<p>Adopt Better-Harness if you already run one or more of the supported coding agents and you want a reproducible, host-agnostic way to measure and tighten the loop around them. It is especially well-suited to teams that value explicit, evidence-bounded reports over benchmark-friendly but unverifiable scores, and to engineering organizations that want their quality gates version-controlled as open, extendable code.</p>
<p>Skip it if you are looking for a benchmark to quote, or if your codebase lacks the feedback infrastructure (tests, linters, hooks) that any harness loop depends on — the platform will expose that gap, not close it for you.</p>
<p>For most teams running Claude Code, Codex, or Cursor in earnest, the practical verdict is positive: at zero license cost, with a mental model that matches how reliability actually improves — via the harness, not the model — Better-Harness is one of the most honest tools available for better harness coding agents.</p>
<h2 id="how-to-get-started-with-better-harness">How to Get Started with Better-Harness</h2>
<p>Starting takes minutes and follows the same shape on every supported host.</p>
<p>First, install the platform through your existing agent workflow (npm-based, per the repository). Because it is host-agnostic, the installation pattern for Claude Code differs only trivially from Codex CLI or Cursor.</p>
<p>Second, run a baseline pass on a representative task without changing anything about how you normally work. Capture the initial score across the five dimensions — this baseline is your point of comparison, and it will already tell you which dimensions were never actually measured.</p>
<p>Third, read the report for &ldquo;evidence missing&rdquo; markers. Those gaps are your highest-value work because they point at the sensors you lack, not the guides you should write more of.</p>
<p>Fourth, tighten one loop: add a guide where the agent misread intent (task understanding), add a pre-push linter or hook where it shipped sloppy changes (change validation), or add an eval agent where it delivered the wrong shape repeatedly (reliable delivery). Re-run the baseline.</p>
<p>Fifth, repeat and compare longitudinally. The goal is not a one-time score but a rising floor: each run should show the loop measurably improving, with every score traceable to evidence in your repository.</p>
<h2 id="faq">FAQ</h2>
<p><strong>What is Better-Harness?</strong>
Better-Harness is an MIT-licensed, open-source harness engineering platform from QoderAI that runs inside existing coding agents — Claude Code, Codex, Cursor, and others — combining feedforward guides with feedback sensors and reporting evidence-bounded scores across five work-dimensions.</p>
<p><strong>What does &ldquo;Agent = Model + Harness&rdquo; mean?</strong>
It means the reliability of a coding agent depends on the system around the model — guides in ahead of action and sensors after it — more than on the model itself. A popular teardown attributed roughly 98% of Claude Code to the harness, not the model.</p>
<p><strong>Is Better-Harness free to use?</strong>
Yes. It is released under the MIT license with npm packaging, so there is no license cost, and teams can extend its quality gates for their own compliance checks instead of buying a closed evaluation platform.</p>
<p><strong>Which coding agents does Better-Harness support?</strong>
It is host-agnostic and runs through Claude Code, Codex Desktop and CLI, Qoder, Cursor, qwen-code, GitHub Copilot CLI, Kimi Code, Pi, WorkBuddy, and Grok — it layers the evaluation loop on top without replacing your client or workflow.</p>
<p><strong>How does Better-Harness keep scores honest?</strong>
It refuses to fabricate numbers: when evidence for a dimension is missing, the report says so explicitly instead of assigning a plausible score. That &ldquo;missing evidence stays explicit&rdquo; stance keeps every claim reproducible in your own repository.</p>
]]></content:encoded></item><item><title>Archon AI Benchmark: Open-Source Harness Builder for Reproducible AI Coding</title><link>https://baeseokjae.github.io/posts/archon-ai-benchmark-tool-2026/</link><pubDate>Tue, 19 May 2026 18:04:15 +0000</pubDate><guid>https://baeseokjae.github.io/posts/archon-ai-benchmark-tool-2026/</guid><description>Archon is the open-source YAML workflow harness that lifts AI coding PR acceptance from 6.7% to 70% — a 10x improvement with the same LLM.</description><content:encoded><![CDATA[<p>Archon is an open-source AI coding harness builder that wraps LLMs like Claude Code and OpenAI Codex inside deterministic YAML workflows, lifting the PR acceptance rate from a raw 6.7% to nearly 70% — without changing the underlying model. If you&rsquo;ve ever wondered why AI-generated code works brilliantly one day and catastrophically fails the next, the answer is the absence of structure. Archon provides that structure.</p>
<h2 id="what-is-archon-the-first-open-source-ai-coding-harness-builder">What Is Archon? The First Open-Source AI Coding Harness Builder</h2>
<p>Archon is an open-source framework that converts ad-hoc AI coding sessions into reproducible, version-controlled workflows by wrapping LLM calls in a directed acyclic graph (DAG) of YAML-defined steps. Released by Cole Medin in early 2026 and rewritten entirely in TypeScript in April 2026, Archon reached 21,600+ GitHub stars — briefly trending #1 on GitHub — because it addresses a problem every developer using AI coding tools encounters immediately: the same prompt produces wildly different results across runs. Instead of accepting that variance as inevitable, Archon treats the workflow itself as a first-class engineering artifact. A <code>.archon/workflows/</code> directory in your repository holds YAML files that define exactly how the AI plans, implements, tests, reviews, and submits a change. These workflow files are reviewed in pull requests alongside the code they generate. The analogy to Dockerfiles for infrastructure is deliberate: Archon is what Dockerfiles did for reproducible environments, applied to AI-generated code.</p>
<h2 id="the-reproducibility-crisis-that-archon-was-built-to-solve">The Reproducibility Crisis That Archon Was Built to Solve</h2>
<p>The reproducibility crisis in AI-generated code is measurably severe, documented in a peer-reviewed arXiv study (2512.22387, March 2026) covering 300 LLM-generated projects across multiple languages. That study found 31.7% of AI-generated code projects fail to execute out of the box due to dependency gaps alone. Python fares best at 89.2% reproducibility success, while Java collapses to just 44.0%. The root cause is not model capability — it is structural: LLMs declare dependencies in code but the actual runtime dependency graph expands by an average of 13.5x from declared to actual. One <code>import requests</code> in a requirements file can silently require 14 transitive packages that differ between developer laptops, CI runners, and production. Without a harness that enforces environment setup, dependency pinning, test execution, and review gates, even a correct implementation fails on the second run on a different machine. The research makes the problem concrete: AI-generated code is not inherently unreliable, but AI coding <em>workflows</em> — or the absence of them — are. Archon&rsquo;s core thesis is that you cannot benchmark or rely on AI coding output until you first standardize how the output is produced.</p>
<h2 id="how-archon-works-yaml-workflows-as-deterministic-ai-pipelines">How Archon Works: YAML Workflows as Deterministic AI Pipelines</h2>
<p>An Archon workflow is a YAML file that defines a DAG of nodes, each corresponding to a step in the AI coding process. A minimal workflow file looks like this:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-yaml" data-lang="yaml"><span style="display:flex;"><span><span style="color:#f92672">name</span>: <span style="color:#ae81ff">feature-implementation</span>
</span></span><span style="display:flex;"><span><span style="color:#f92672">version</span>: <span style="color:#ae81ff">1</span>
</span></span><span style="display:flex;"><span><span style="color:#f92672">nodes</span>:
</span></span><span style="display:flex;"><span>  <span style="color:#f92672">plan</span>:
</span></span><span style="display:flex;"><span>    <span style="color:#f92672">type</span>: <span style="color:#ae81ff">plan</span>
</span></span><span style="display:flex;"><span>    <span style="color:#f92672">prompt</span>: <span style="color:#e6db74">&#34;Analyze the issue and write a step-by-step implementation plan&#34;</span>
</span></span><span style="display:flex;"><span>    <span style="color:#f92672">model</span>: <span style="color:#ae81ff">claude-sonnet-4-6</span>
</span></span><span style="display:flex;"><span>  <span style="color:#f92672">implement</span>:
</span></span><span style="display:flex;"><span>    <span style="color:#f92672">type</span>: <span style="color:#ae81ff">implement</span>
</span></span><span style="display:flex;"><span>    <span style="color:#f92672">depends_on</span>: [<span style="color:#ae81ff">plan]</span>
</span></span><span style="display:flex;"><span>    <span style="color:#f92672">prompt</span>: <span style="color:#e6db74">&#34;Implement the plan from the previous step&#34;</span>
</span></span><span style="display:flex;"><span>  <span style="color:#f92672">test</span>:
</span></span><span style="display:flex;"><span>    <span style="color:#f92672">type</span>: <span style="color:#ae81ff">test</span>
</span></span><span style="display:flex;"><span>    <span style="color:#f92672">depends_on</span>: [<span style="color:#ae81ff">implement]</span>
</span></span><span style="display:flex;"><span>    <span style="color:#f92672">runner</span>: <span style="color:#ae81ff">bun test</span>
</span></span><span style="display:flex;"><span>  <span style="color:#f92672">review</span>:
</span></span><span style="display:flex;"><span>    <span style="color:#f92672">type</span>: <span style="color:#ae81ff">review</span>
</span></span><span style="display:flex;"><span>    <span style="color:#f92672">depends_on</span>: [<span style="color:#ae81ff">test]</span>
</span></span><span style="display:flex;"><span>    <span style="color:#f92672">prompt</span>: <span style="color:#e6db74">&#34;Check the implementation against the original requirements&#34;</span>
</span></span><span style="display:flex;"><span>  <span style="color:#f92672">pr</span>:
</span></span><span style="display:flex;"><span>    <span style="color:#f92672">type</span>: <span style="color:#ae81ff">pull-request</span>
</span></span><span style="display:flex;"><span>    <span style="color:#f92672">depends_on</span>: [<span style="color:#ae81ff">review]</span>
</span></span><span style="display:flex;"><span>    <span style="color:#f92672">title_template</span>: <span style="color:#e6db74">&#34;feat: {{issue.title}}&#34;</span>
</span></span></code></pre></div><p>Archon ships with 17 default workflows covering the full lifecycle: planning, implementation, testing, review, and PR creation. Each node type has documented retry logic and fallback behavior. When a <code>test</code> node fails, Archon can re-enter the <code>implement</code> node with the error output as additional context rather than surfacing a raw failure to the user. The workflow engine runs in a TypeScript/Bun runtime, executes nodes in parallel where the DAG allows, and reports status through a real-time dashboard. The YAML syntax is explicit about which model runs at each step — you can use Claude for planning, GPT-4 for implementation, and a local Ollama model for review, mixing providers per node.</p>
<h2 id="key-features-dag-architecture-multi-model-support-and-error-recovery">Key Features: DAG Architecture, Multi-Model Support, and Error Recovery</h2>
<p>Archon&rsquo;s architecture centers on four capabilities that distinguish it from running LLMs directly. First, the DAG execution engine ensures steps run in dependency order and parallelizes independent nodes automatically — a <code>test</code> and a <code>docs</code> node that both depend on <code>implement</code> will run concurrently without any configuration. Second, multi-model support lets teams assign the cheapest adequate model to each step: a weak model for boilerplate test scaffolding, a powerful model for architecture planning. Supported providers include Anthropic Claude (all tiers), OpenAI GPT-4 and Codex, and any model served via Ollama locally. Third, structured error recovery replaces &ldquo;it failed, retry from scratch&rdquo; with targeted re-entry: when a node fails, Archon passes the failure details back to the relevant upstream node as context, enabling the LLM to self-correct within the same workflow run. Fourth, the <code>.archon/workflows/</code> directory makes workflows version-controlled artifacts. Teams can review changes to a workflow definition in a pull request, roll back a workflow to a previous version, and audit exactly which workflow version produced a given merged PR.</p>
<h3 id="model-flexibility-in-practice">Model Flexibility in Practice</h3>
<p>The multi-model routing in Archon is more nuanced than a single model selection at setup. Each node in a workflow YAML specifies a <code>model</code> field, and that field accepts any model string Archon understands. A team running cost-sensitive CI pipelines might assign <code>haiku-4-5</code> to the <code>test</code> node (where the prompt is formulaic) and <code>claude-opus-4-7</code> only to the <code>review</code> node (where reasoning quality matters most). Because the model choice is in the YAML file, changing the model for one step of one workflow is a one-line diff — it shows up in code review, gets recorded in git history, and can be reverted if the cheaper model introduces regressions.</p>
<h2 id="real-world-results--from-67-to-70-pr-acceptance-rate">Real-World Results — From 6.7% to 70% PR Acceptance Rate</h2>
<p>The most striking data point in Archon&rsquo;s launch story is not a benchmark score — it is a production outcome: the same LLM, given the same coding task, achieves a 6.7% PR acceptance rate without a structured harness and nearly 70% PR acceptance with Archon wrapping the workflow. That is roughly a 10x improvement from workflow structure alone, with no change to the underlying model or prompts. The explanation is not that Archon makes the LLM smarter. It is that the harness eliminates the modes of failure that account for the gap: generated code that was never tested, code that passed tests locally but failed in CI due to missing dependencies, code that addressed the wrong interpretation of a requirement because no planning step forced clarification, and code submitted as a PR without a review step catching obvious regressions. Stripe provides the enterprise-scale reference: their internal harness architecture, called Minions, merges over 1,300 pull requests per week that contain zero human-written code. Archon is positioned as the open-source path to the same outcome — a repeatable, auditable AI coding pipeline that teams without Stripe&rsquo;s engineering resources can adopt today.</p>
<h2 id="archon-vs-traditional-benchmarks-why-a-harness-is-different">Archon vs. Traditional Benchmarks: Why a Harness Is Different</h2>
<p>Traditional AI coding benchmarks like SWE-bench, HumanEval, and LiveCodeBench measure model capability by presenting a model with a task and scoring the output against a reference solution. They are model-centric: the question is &ldquo;which model is best?&rdquo; Archon is workflow-centric: the question is &ldquo;given a fixed model, how do we maximize the reliability of real-world code delivery?&rdquo; This is a categorical distinction. SWE-bench tells you that Model A solves 42% of GitHub issues in its test set. Archon tells you that your team&rsquo;s chosen model, wrapped in a structured plan-implement-test-review-PR pipeline, will deliver merge-ready code at a predictable rate. SWE-bench is useful for model selection; Archon is useful for production engineering. They answer different questions. The framing of Archon as a &ldquo;benchmark tool&rdquo; in early coverage is slightly misleading — it is better understood as a harness builder that makes AI coding results reproducible and measurable. The measurability is a side effect of the structure, not the primary goal. Because every Archon run executes the same workflow definition and logs the same structured output, you can compare workflow versions, model assignments, and task types in a way that raw LLM usage never permits.</p>
<h2 id="getting-started-with-archon-installation-and-first-workflow">Getting Started with Archon: Installation and First Workflow</h2>
<p>Archon runs on the TypeScript/Bun runtime. The install sequence is four commands:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-bash" data-lang="bash"><span style="display:flex;"><span><span style="color:#75715e"># Install Bun if not already installed</span>
</span></span><span style="display:flex;"><span>curl -fsSL https://bun.sh/install | bash
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#75715e"># Clone and install Archon</span>
</span></span><span style="display:flex;"><span>git clone https://github.com/coleam00/Archon
</span></span><span style="display:flex;"><span>cd Archon
</span></span><span style="display:flex;"><span>bun install
</span></span></code></pre></div><p>Set your model provider API keys as environment variables:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-bash" data-lang="bash"><span style="display:flex;"><span>export ANTHROPIC_API_KEY<span style="color:#f92672">=</span>your_key_here
</span></span><span style="display:flex;"><span>export OPENAI_API_KEY<span style="color:#f92672">=</span>your_key_here  <span style="color:#75715e"># optional</span>
</span></span></code></pre></div><p>Start the Archon dashboard:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-bash" data-lang="bash"><span style="display:flex;"><span>bun run dev
</span></span></code></pre></div><p>The dashboard runs at <code>localhost:3000</code> and shows active workflow runs, node execution status, and output logs in real time. To run a workflow against a task, pass the issue description and the workflow name:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-bash" data-lang="bash"><span style="display:flex;"><span>bunx archon run --workflow feature-implementation --issue <span style="color:#e6db74">&#34;Add rate limiting to the /api/upload endpoint&#34;</span>
</span></span></code></pre></div><p>Archon will execute the DAG, stream progress to the dashboard, and — if the workflow includes a <code>pull-request</code> node — submit a PR to the configured repository when the review step passes. The 17 default workflows in <code>.archon/workflows/defaults/</code> are a working starting point for most teams. Customizing them is a YAML edit, not a code change.</p>
<h3 id="connecting-to-your-repository">Connecting to Your Repository</h3>
<p>Archon integrates with GitHub via a personal access token or GitHub App. Configure the repository target in <code>archon.config.ts</code> at the project root:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-typescript" data-lang="typescript"><span style="display:flex;"><span><span style="color:#66d9ef">export</span> <span style="color:#66d9ef">default</span> {
</span></span><span style="display:flex;"><span>  <span style="color:#a6e22e">repository</span><span style="color:#f92672">:</span> <span style="color:#e6db74">&#34;your-org/your-repo&#34;</span>,
</span></span><span style="display:flex;"><span>  <span style="color:#a6e22e">baseBranch</span><span style="color:#f92672">:</span> <span style="color:#e6db74">&#34;main&#34;</span>,
</span></span><span style="display:flex;"><span>  <span style="color:#a6e22e">github</span><span style="color:#f92672">:</span> {
</span></span><span style="display:flex;"><span>    <span style="color:#a6e22e">token</span>: <span style="color:#66d9ef">process.env.GITHUB_TOKEN</span>,
</span></span><span style="display:flex;"><span>  },
</span></span><span style="display:flex;"><span>};
</span></span></code></pre></div><p>From that point, every workflow run that reaches the <code>pull-request</code> node will open a real PR against your repository. The PR description includes a structured summary of the plan, a list of files changed, and a link to the Archon workflow run log — giving reviewers full context on what the AI did and why.</p>
<h2 id="who-should-use-archon-and-who-should-not">Who Should Use Archon (And Who Should Not)</h2>
<p>Archon is the right tool for teams that are already using AI coding tools at scale and have hit the reliability ceiling of ad-hoc prompting. If your team is consistently seeing AI-generated PRs fail CI, require extensive human rework, or produce code that works locally but not in other environments, Archon addresses exactly those failure modes. It is also the right tool for teams building internal AI coding pipelines who want to avoid vendor lock-in to proprietary agent platforms — Archon workflows are plain YAML files in your repository, owned entirely by your team. Archon is a poor fit for teams still evaluating whether AI coding tools are useful at all. The harness adds real setup cost: you need to author or adapt workflow YAML files, configure model credentials, and integrate with your CI/CD system. That investment only pays back at scale. Similarly, Archon is overkill for one-off code generation tasks. If you need to ask Claude to write a single utility function, open the chat interface. Archon&rsquo;s value is in the 50th through 500th task, where workflow structure compounds into a measurably higher merge rate.</p>
<hr>
<h2 id="faq">FAQ</h2>
<p><strong>What is Archon used for in AI coding?</strong>
Archon is used to wrap AI coding agents like Claude Code and OpenAI Codex inside structured YAML workflow definitions, ensuring that every code generation task follows a repeatable plan-implement-test-review pipeline rather than a single unconstrained LLM call.</p>
<p><strong>How does Archon improve PR acceptance rates?</strong>
Archon improves PR acceptance by enforcing structured workflow gates — planning, implementation, testing, and review — before a PR is submitted. The same LLM without a harness achieves 6.7% PR acceptance; wrapped in an Archon workflow, acceptance jumps to nearly 70%.</p>
<p><strong>Is Archon a benchmark tool or a workflow engine?</strong>
Archon is primarily a workflow engine and harness builder. It is called a benchmark tool because its structured logging makes AI coding results measurable and comparable across workflow versions, but the primary purpose is making AI code generation reproducible and reliable in production.</p>
<p><strong>What models does Archon support?</strong>
Archon supports Anthropic Claude (all tiers), OpenAI GPT-4 and Codex variants, and any model served locally via Ollama. Different nodes within a single workflow can use different models, allowing cost and capability optimization per step.</p>
<p><strong>How do I version-control Archon workflows?</strong>
Archon workflows are YAML files stored in the <code>.archon/workflows/</code> directory of your repository. They are committed and reviewed in pull requests like any other code artifact, giving teams full git history, rollback capability, and code review for workflow changes.</p>
]]></content:encoded></item></channel></rss>