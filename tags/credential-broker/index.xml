<?xml version="1.0" encoding="utf-8" standalone="yes"?><rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom" xmlns:content="http://purl.org/rss/1.0/modules/content/"><channel><title>Credential Broker on RockB</title><link>https://baeseokjae.github.io/tags/credential-broker/</link><description>Recent content in Credential Broker on RockB</description><image><title>RockB</title><url>https://baeseokjae.github.io/images/og-default.png</url><link>https://baeseokjae.github.io/images/og-default.png</link></image><generator>Hugo</generator><language>en-us</language><lastBuildDate>Sun, 02 Aug 2026 23:02:35 +0000</lastBuildDate><atom:link href="https://baeseokjae.github.io/tags/credential-broker/index.xml" rel="self" type="application/rss+xml"/><item><title>Kontext Credential Broker Guide 2026: Secure Credential Management for AI Coding Agents</title><link>https://baeseokjae.github.io/posts/kontext-credential-broker-guide-2026/</link><pubDate>Sun, 02 Aug 2026 23:02:35 +0000</pubDate><guid>https://baeseokjae.github.io/posts/kontext-credential-broker-guide-2026/</guid><description>Learn how Kontext CLI acts as a credential broker for AI coding agents, providing short-lived credentials, runtime policy enforcement, and audit trails without exposing secrets.</description><content:encoded><![CDATA[<h2 id="what-is-kontext-cli">What is Kontext CLI?</h2>
<p>Kontext CLI is an open-source credential broker and runtime security tool built specifically for AI coding agents. It intercepts every tool call made by agents like Claude Code, Codex, and Claude Cowork, brokers short-lived credentials at request time, and enforces security policies before any action reaches your infrastructure. Unlike traditional secrets managers that just store credentials, Kontext actively controls how and when agents use them.</p>
<h2 id="what-is-a-credential-broker-for-ai-agents">What is a Credential Broker for AI Agents?</h2>
<p>A credential broker is a security layer that sits between an AI coding agent and the services it needs to access. Instead of giving the agent a long-lived API key or database password, the broker issues short-lived, scoped credentials on demand for each individual tool call. The agent never holds the actual secret — it receives a temporary token valid only for the specific operation it needs to perform.</p>
<p>This pattern is rapidly becoming essential infrastructure for teams deploying AI coding agents in production. The IETF is formalizing the concept through the CB4A (Credential Broker for Agents) Internet-Draft, which specifies that agents should never hold real long-lived credentials and should use workload identity systems like SPIFFE/SPIRE for authentication.</p>
<h3 id="how-kontext-differs-from-traditional-secrets-management">How Kontext Differs from Traditional Secrets Management</h3>
<p>Traditional secrets managers like HashiCorp Vault, AWS Secrets Manager, or 1Password are designed for human operators and CI/CD pipelines. They provide credential storage, rotation, and access control, but they assume the consumer is a trusted process with deterministic behavior. AI agents break this assumption because:</p>
<ul>
<li><strong>Agents are non-deterministic</strong> — the same prompt can produce different tool calls each time</li>
<li><strong>Agents can be manipulated</strong> — prompt injection can trick an agent into calling tools the user never intended</li>
<li><strong>Agents operate at machine speed</strong> — a compromised agent can exfiltrate hundreds of credentials in seconds</li>
</ul>
<p>Kontext addresses these gaps by combining credential brokering with runtime policy enforcement. It doesn&rsquo;t just hand over credentials — it evaluates each request against a policy engine before releasing them.</p>
<h2 id="why-do-ai-agents-need-a-credential-broker">Why Do AI Agents Need a Credential Broker?</h2>
<p>The four most common credential patterns used with AI coding agents are all fundamentally broken:</p>
<table>
	<thead>
			<tr>
					<th>Pattern</th>
					<th>How It Works</th>
					<th>The Problem</th>
			</tr>
	</thead>
	<tbody>
			<tr>
					<td>Hardcoded API keys</td>
					<td>Keys embedded in <code>.env</code> files or agent config</td>
					<td>Any prompt injection leaks every key instantly</td>
			</tr>
			<tr>
					<td>Shared service accounts</td>
					<td>One token used by the whole team</td>
					<td>No audit trail, no per-user revocation</td>
			</tr>
			<tr>
					<td>Token passthrough</td>
					<td>Agent forwards the user&rsquo;s personal access token</td>
					<td>Agent can use the token for anything, anywhere</td>
			</tr>
			<tr>
					<td>Long-lived PATs</td>
					<td>Personal access tokens with no expiration</td>
					<td>Compromise means indefinite access until manually revoked</td>
			</tr>
	</tbody>
</table>
<h3 id="prompt-injection-becomes-a-systems-attack-with-credentials">Prompt Injection Becomes a Systems Attack with Credentials</h3>
<p>When an AI agent holds credentials, prompt injection stops being a content-quality problem and becomes a systems security incident. An attacker who injects a malicious instruction into a file the agent is reading can trick it into:</p>
<ul>
<li>Reading every secret from the secrets manager</li>
<li>Making unauthorized API calls to production services</li>
<li>Exfiltrating data through HTTP requests or git pushes</li>
<li>Modifying infrastructure configurations</li>
</ul>
<p>Kontext&rsquo;s pre-tool-use blocking architecture prevents this by evaluating every tool call against policy <em>before</em> the credential is released. If the agent tries to make an API call that doesn&rsquo;t match the allowed scope, Kontext blocks it regardless of what the agent&rsquo;s prompt says.</p>
<h3 id="unlimited-blast-radius-and-no-surgical-revocation">Unlimited Blast Radius and No Surgical Revocation</h3>
<p>Without a credential broker, a compromised agent has access to everything the credential it holds can do. If an agent is using a user&rsquo;s personal access token with read-write scope on all repositories, a single successful injection attack can modify any codebase, delete resources, or exfiltrate data across the entire organization.</p>
<p>With Kontext, each credential is scoped to a specific operation. If an agent is compromised mid-session, the blast radius is limited to the credentials already issued — and those credentials expire within minutes. The attacker cannot request new credentials because the policy engine blocks requests that don&rsquo;t match the approved scope.</p>
<h2 id="kontext-architecture-overview">Kontext Architecture Overview</h2>
<p>Kontext uses a control plane / data plane architecture inspired by NIST SP 800-207 (Zero Trust Architecture), the same framework that informs the CB4A IETF draft.</p>
<h3 id="control-plane-vs-data-plane">Control Plane vs Data Plane</h3>
<p>The <strong>control plane</strong> handles policy evaluation, credential brokering, and audit logging. It runs as a local daemon on the developer&rsquo;s machine and can optionally sync to the Kontext cloud dashboard for team-wide visibility.</p>
<p>The <strong>data plane</strong> is the actual API calls made by the AI agent. Kontext intercepts these at the agent integration layer — through Claude Code&rsquo;s pre-tool-use hooks, Codex&rsquo;s command wrappers, or MCP server interceptors. The data plane never touches credentials directly; it only receives short-lived tokens from the control plane.</p>
<h3 id="local-first-decision-path-with-optional-cloud-export">Local-First Decision Path with Optional Cloud Export</h3>
<p>Kontext is designed to work entirely offline. All policy decisions are evaluated locally on the developer&rsquo;s machine, which means:</p>
<ul>
<li><strong>Zero latency</strong> — policy checks complete in ~22ms for the SVM classifier</li>
<li><strong>No cloud dependency</strong> — the agent keeps working even without internet access</li>
<li><strong>Privacy by default</strong> — sensitive API call details never leave the machine</li>
</ul>
<p>For enterprise teams, Kontext offers a managed mode where audit logs and policy configurations sync to a cloud dashboard. This gives security teams visibility across the organization without compromising the local-first performance model.</p>
<h3 id="the-3-layer-authorization-model">The 3-Layer Authorization Model</h3>
<p>Kontext enforces policies at three levels:</p>
<ol>
<li>
<p><strong>Org-level policies</strong> — defined by the security team, apply to all agents in the organization. These might include &ldquo;block all write operations to production databases&rdquo; or &ldquo;require approval for any deployment command.&rdquo;</p>
</li>
<li>
<p><strong>User-level policies</strong> — defined by individual developers for their own agents. These might include &ldquo;allow read access to staging API but require confirmation for write operations.&rdquo;</p>
</li>
<li>
<p><strong>Application-level policies</strong> — defined per agent or per project. These might include &ldquo;Claude Code can access the GitHub API but only for pull request operations.&rdquo;</p>
</li>
</ol>
<p>This layered model means a single Kontext installation can serve different security postures for different teams, projects, and agent types.</p>
<h2 id="installation-and-setup">Installation and Setup</h2>
<h3 id="macos-quickstart-with-homebrew">macOS Quickstart with Homebrew</h3>
<p>Installing Kontext on macOS takes less than a minute:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-bash" data-lang="bash"><span style="display:flex;"><span>brew tap kontext-security/tap
</span></span><span style="display:flex;"><span>brew install kontext
</span></span></code></pre></div><p>This installs the <code>kontext</code> CLI binary, the local daemon, and the default policy set. After installation, you need to create an install token to link your local instance to your Kontext account.</p>
<h3 id="creating-an-install-token-in-the-kontext-dashboard">Creating an Install Token in the Kontext Dashboard</h3>
<ol>
<li>Navigate to the Kontext dashboard at <a href="https://kontext.security">https://kontext.security</a></li>
<li>Create an account or sign in with GitHub OAuth</li>
<li>Go to Settings → Install Tokens → Generate New Token</li>
<li>Copy the token and run:</li>
</ol>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-bash" data-lang="bash"><span style="display:flex;"><span>kontext auth --token YOUR_INSTALL_TOKEN
</span></span></code></pre></div><p>This links your local CLI to your Kontext account, enabling cloud sync for audit logs and policy management.</p>
<h3 id="verifying-setup-with-kontext-doctor">Verifying Setup with kontext doctor</h3>
<p>Kontext includes a diagnostic command that verifies every component is working correctly:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-bash" data-lang="bash"><span style="display:flex;"><span>kontext doctor
</span></span></code></pre></div><p>This checks:</p>
<ul>
<li>The local daemon is running and healthy</li>
<li>The install token is valid</li>
<li>The default policy set is loaded</li>
<li>Agent integrations are properly configured</li>
<li>The SVM risk classifier model is downloaded</li>
</ul>
<p>If everything passes, you&rsquo;ll see a green checkmark for each component. If any check fails, <code>kontext doctor</code> provides specific remediation steps.</p>
<h2 id="supported-ai-coding-agents">Supported AI Coding Agents</h2>
<p>Kontext supports three major AI coding agents with deep integrations that go beyond simple credential injection.</p>
<h3 id="claude-code-integration-pre-tool-use-blocking">Claude Code Integration (Pre-Tool-Use Blocking)</h3>
<p>Claude Code integration is Kontext&rsquo;s flagship feature. It uses Claude Code&rsquo;s pre-tool-use hook system to intercept every tool call <em>before</em> it executes. This means:</p>
<ul>
<li>Kontext evaluates the tool call against policy before Claude Code sends it to the API</li>
<li>If the call is blocked, Claude Code never sees the credential and never makes the request</li>
<li>The user sees a clear explanation of why the call was blocked</li>
</ul>
<p>Configuration is done through Claude Code&rsquo;s project settings:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-json" data-lang="json"><span style="display:flex;"><span>{
</span></span><span style="display:flex;"><span>  <span style="color:#f92672">&#34;hooks&#34;</span>: {
</span></span><span style="display:flex;"><span>    <span style="color:#f92672">&#34;preToolUse&#34;</span>: <span style="color:#e6db74">&#34;kontext hook claude&#34;</span>
</span></span><span style="display:flex;"><span>  }
</span></span><span style="display:flex;"><span>}
</span></span></code></pre></div><h3 id="codex-hook-configuration">Codex Hook Configuration</h3>
<p>For OpenAI Codex, Kontext provides a command wrapper that intercepts tool calls at the shell level:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-bash" data-lang="bash"><span style="display:flex;"><span>codex --hook <span style="color:#e6db74">&#34;kontext hook codex&#34;</span>
</span></span></code></pre></div><p>This wraps every Codex tool execution in a Kontext policy check. The integration supports all Codex tool types including file operations, shell commands, and HTTP requests.</p>
<h3 id="claude-cowork-support">Claude Cowork Support</h3>
<p>Claude Cowork, Anthropic&rsquo;s collaborative coding environment, is supported through the same pre-tool-use hook mechanism as Claude Code. The setup is identical:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-bash" data-lang="bash"><span style="display:flex;"><span>kontext hook install claude-cowork
</span></span></code></pre></div><p>This installs the hook globally so all Claude Cowork sessions are automatically protected.</p>
<h2 id="kontext-guard--runtime-security">Kontext Guard — Runtime Security</h2>
<p>Kontext Guard is the runtime security engine that evaluates every tool call against policy before releasing credentials. It uses a three-tier evaluation pipeline.</p>
<h3 id="deterministic-policy-for-credential-access-and-destructive-commands">Deterministic Policy for Credential Access and Destructive Commands</h3>
<p>The first tier is a deterministic policy engine that evaluates tool calls against explicit allow/deny rules. These rules are defined in YAML and can cover:</p>
<ul>
<li><strong>Credential access</strong> — which credentials can be requested, for which services, by which agents</li>
<li><strong>Destructive commands</strong> — blocking <code>rm -rf</code>, <code>DROP TABLE</code>, <code>DELETE FROM</code>, and other dangerous operations</li>
<li><strong>Network destinations</strong> — which hosts and ports the agent can connect to</li>
<li><strong>File system access</strong> — which directories the agent can read or write</li>
</ul>
<p>Deterministic policies are evaluated in under 1ms and provide hard guarantees. If a rule explicitly denies an operation, it is blocked regardless of what the risk classifier or guardrail LLM says.</p>
<h3 id="svm-risk-classifier-0987-precision">SVM Risk Classifier (0.987 Precision)</h3>
<p>The second tier is a machine learning risk classifier based on a Support Vector Machine (SVM) model. It evaluates each tool call and assigns a risk score based on:</p>
<ul>
<li>The type of operation (read vs write vs delete)</li>
<li>The target service or resource</li>
<li>The historical context of similar operations</li>
<li>The agent&rsquo;s recent behavior patterns</li>
</ul>
<p>The SVM classifier achieves 0.987 precision, meaning it correctly identifies risky operations 98.7% of the time. It processes each decision in approximately 22ms, adding negligible latency to every tool call.</p>
<h3 id="guardrail-llm-qwen3-06b-for-edge-cases">Guardrail LLM (Qwen3-0.6B) for Edge Cases</h3>
<p>The third tier is a small guardrail LLM (Qwen3-0.6B) that handles edge cases the deterministic policy and SVM classifier can&rsquo;t resolve confidently. This model runs locally and evaluates:</p>
<ul>
<li>Ambiguous tool calls that don&rsquo;t clearly match any policy rule</li>
<li>Novel operation patterns the SVM hasn&rsquo;t been trained on</li>
<li>Natural language descriptions in tool call parameters</li>
</ul>
<p>The guardrail LLM has 0.585 precision and takes approximately 200ms per decision. It is only invoked when the first two tiers produce a low-confidence result, so it doesn&rsquo;t impact the common case.</p>
<h3 id="observe-mode-vs-enforce-mode">Observe Mode vs Enforce Mode</h3>
<p>Kontext Guard operates in two modes:</p>
<p><strong>Observe mode</strong> logs every tool call and shows what would have been blocked, but allows the operation to proceed. This is ideal for:</p>
<ul>
<li>Initial setup and policy tuning</li>
<li>Understanding what your agents actually do</li>
<li>Building confidence before enabling enforcement</li>
</ul>
<p><strong>Enforce mode</strong> actively blocks policy-violating operations. The agent receives a clear error message explaining why the call was blocked, and the user can review the decision in the audit log.</p>
<p>The recommended adoption path is to start in observe mode for one week, review the audit log, adjust policies, then switch to enforce mode.</p>
<h2 id="credential-brokering-with-kontext-sdk">Credential Brokering with Kontext SDK</h2>
<p>Beyond runtime security, Kontext provides a credential brokering SDK that integrates with your own applications and MCP servers.</p>
<h3 id="short-lived-scoped-credentials-per-request">Short-Lived, Scoped Credentials Per Request</h3>
<p>When an agent needs to call an API, Kontext issues a credential that is:</p>
<ul>
<li><strong>Short-lived</strong> — expires in minutes (configurable, default 5 minutes)</li>
<li><strong>Scoped</strong> — limited to the specific operation requested</li>
<li><strong>Auditable</strong> — every credential issuance is logged with the full context of the request</li>
</ul>
<p>The credential is delivered directly to the target service, not to the agent. The agent receives a reference or one-time use token that it can exchange for the credential at the service endpoint.</p>
<h3 id="oauth-20-with-pkce-for-user-delegation">OAuth 2.0 with PKCE for User Delegation</h3>
<p>Kontext uses OAuth 2.0 with PKCE (Proof Key for Code Exchange) for user delegation flows. When an agent needs to act on behalf of a user:</p>
<ol>
<li>The agent requests a credential through Kontext</li>
<li>Kontext initiates an OAuth 2.0 PKCE flow with the identity provider</li>
<li>The user authorizes the specific scope requested</li>
<li>Kontext receives the access token and issues a short-lived derived credential to the agent</li>
<li>The agent uses the credential for the single operation</li>
</ol>
<p>This means the user&rsquo;s long-lived OAuth tokens never leave Kontext&rsquo;s secure storage, and every agent action is explicitly authorized by the user.</p>
<h3 id="integration-with-mcp-servers">Integration with MCP Servers</h3>
<p>Kontext integrates with the Model Context Protocol (MCP) to broker credentials for MCP server connections. When an agent connects to an MCP server, Kontext:</p>
<ol>
<li>Intercepts the MCP connection request</li>
<li>Evaluates the requested resource access against policy</li>
<li>Brokers the appropriate credential for the MCP session</li>
<li>Monitors MCP tool calls for policy violations</li>
</ol>
<p>This makes Kontext a natural fit for teams building MCP-based agent architectures, as it provides a unified credential and policy layer across all MCP servers.</p>
<h2 id="kontext-vs-competitors-2026-landscape">Kontext vs Competitors (2026 Landscape)</h2>
<p>The credential broker space has seen rapid growth in 2026, with multiple open-source tools emerging. Here&rsquo;s how Kontext compares:</p>
<table>
	<thead>
			<tr>
					<th>Feature</th>
					<th>Kontext</th>
					<th>Postern</th>
					<th>Sesame OSS</th>
					<th>BlindVault</th>
					<th>AgentWrit</th>
			</tr>
	</thead>
	<tbody>
			<tr>
					<td>Runtime policy enforcement</td>
					<td>✅ Yes (3-tier Guard)</td>
					<td>❌ No</td>
					<td>❌ No</td>
					<td>❌ No</td>
					<td>❌ No</td>
			</tr>
			<tr>
					<td>Pre-tool-use blocking</td>
					<td>✅ Yes</td>
					<td>❌ No</td>
					<td>❌ No</td>
					<td>❌ No</td>
					<td>❌ No</td>
			</tr>
			<tr>
					<td>Short-lived credentials</td>
					<td>✅ Yes</td>
					<td>✅ Yes</td>
					<td>✅ Yes</td>
					<td>✅ Yes</td>
					<td>✅ Yes</td>
			</tr>
			<tr>
					<td>Audit trail</td>
					<td>✅ Yes</td>
					<td>✅ Yes</td>
					<td>✅ Yes</td>
					<td>❌ No</td>
					<td>❌ No</td>
			</tr>
			<tr>
					<td>Local-first architecture</td>
					<td>✅ Yes</td>
					<td>✅ Yes</td>
					<td>✅ Yes</td>
					<td>✅ Yes</td>
					<td>✅ Yes</td>
			</tr>
			<tr>
					<td>Cloud dashboard (optional)</td>
					<td>✅ Yes</td>
					<td>❌ No</td>
					<td>✅ Yes</td>
					<td>❌ No</td>
					<td>❌ No</td>
			</tr>
			<tr>
					<td>Risk classification (ML)</td>
					<td>✅ Yes (SVM 0.987)</td>
					<td>❌ No</td>
					<td>❌ No</td>
					<td>❌ No</td>
					<td>❌ No</td>
			</tr>
			<tr>
					<td>Guardrail LLM</td>
					<td>✅ Yes (Qwen3-0.6B)</td>
					<td>❌ No</td>
					<td>❌ No</td>
					<td>❌ No</td>
					<td>❌ No</td>
			</tr>
			<tr>
					<td>Claude Code integration</td>
					<td>✅ Deep (pre-tool-use)</td>
					<td>❌ No</td>
					<td>❌ No</td>
					<td>❌ No</td>
					<td>❌ No</td>
			</tr>
			<tr>
					<td>Codex integration</td>
					<td>✅ Yes</td>
					<td>❌ No</td>
					<td>❌ No</td>
					<td>❌ No</td>
					<td>❌ No</td>
			</tr>
			<tr>
					<td>MCP server support</td>
					<td>✅ Yes</td>
					<td>❌ No</td>
					<td>❌ No</td>
					<td>❌ No</td>
					<td>❌ No</td>
			</tr>
			<tr>
					<td>IETF draft alignment</td>
					<td>✅ Yes (CB4A)</td>
					<td>❌ No</td>
					<td>❌ No</td>
					<td>❌ No</td>
					<td>❌ No</td>
			</tr>
			<tr>
					<td>GitHub stars</td>
					<td>210</td>
					<td>9</td>
					<td>9</td>
					<td>3</td>
					<td>3</td>
			</tr>
			<tr>
					<td>License</td>
					<td>MIT</td>
					<td>MIT</td>
					<td>Source-available</td>
					<td>MIT</td>
					<td>MIT</td>
			</tr>
	</tbody>
</table>
<h3 id="postern--https-proxy-approach">Postern — HTTPS Proxy Approach</h3>
<p>Postern is a credential-brokering HTTPS proxy for AI agents written in Go. It brokers secrets from 1Password and Bitwarden at request time, meaning agents call authenticated APIs without ever holding credentials. Postern&rsquo;s proxy-based approach works well for HTTP-based tool calls but doesn&rsquo;t provide runtime policy enforcement or pre-tool-use blocking. It has 9 GitHub stars as of August 2026.</p>
<h3 id="sesame--self-hosted-broker">Sesame — Self-Hosted Broker</h3>
<p>Sesame OSS is a minimal, source-available, self-hosted edition of the Sesame credential broker. It includes a broker, dashboard, and CLI, all written in Python. Sesame provides credential brokering and audit logging but lacks runtime security features like risk classification or pre-tool-use blocking. It has 9 GitHub stars.</p>
<h3 id="cb4a-ietf-draft--emerging-standard">CB4A IETF Draft — Emerging Standard</h3>
<p>The CB4A (Credential Broker for Agents) Internet-Draft is an IETF specification that formalizes the credential broker pattern. It specifies that agents should never hold real long-lived credentials, uses SPIFFE/SPIRE for workload identity, follows the PDP/CDP separation pattern from NIST SP 800-207, and uses DPoP (RFC 9449) for sender-constrained token binding. Kontext is an early implementation of this emerging standard.</p>
<h3 id="blindvault-and-agentwrit">BlindVault and AgentWrit</h3>
<p>BlindVault is a secrets manager for AI agents that lets LLM agents use API keys and database passwords without ever seeing plaintext, protected by a master password. AgentWrit provides temporary, task-scoped tokens with automatic revocation and is free for internal use. Both are smaller projects with 3 GitHub stars each and limited integration support.</p>
<h2 id="best-practices-for-production-deployments">Best Practices for Production Deployments</h2>
<h3 id="start-in-observe-mode-inspect-the-ledger">Start in Observe Mode, Inspect the Ledger</h3>
<p>The safest path to production with Kontext is to start in observe mode. Run your AI coding agents normally for one to two weeks while Kontext logs every tool call and shows what would have been blocked. Review the audit ledger to understand:</p>
<ul>
<li>What services your agents actually access</li>
<li>What operations are most common</li>
<li>Which operations would be blocked under your intended policies</li>
<li>Whether any legitimate operations would be disrupted</li>
</ul>
<p>This observation period is critical for building policies that protect without breaking workflows.</p>
<h3 id="enable-enforcement-gradually">Enable Enforcement Gradually</h3>
<p>Once you understand your agent&rsquo;s behavior patterns, enable enforcement in stages:</p>
<ol>
<li><strong>Start with read-only policies</strong> — block destructive commands first (delete, drop, rm -rf)</li>
<li><strong>Add credential scope restrictions</strong> — limit which credentials each agent can request</li>
<li><strong>Enable risk classification</strong> — turn on the SVM classifier for suspicious patterns</li>
<li><strong>Enable the guardrail LLM</strong> — add the Qwen3-0.6B model for edge case handling</li>
</ol>
<p>Each stage should be tested for at least a few days before moving to the next.</p>
<h3 id="managed-deployment-for-enterprise-teams">Managed Deployment for Enterprise Teams</h3>
<p>For enterprise teams, Kontext&rsquo;s managed mode provides:</p>
<ul>
<li>Centralized policy management across the organization</li>
<li>Real-time audit dashboard with search and filtering</li>
<li>Alerting for policy violations and suspicious patterns</li>
<li>Role-based access control for policy administration</li>
<li>Integration with SIEM systems through webhook exports</li>
</ul>
<p>The managed mode syncs policy decisions and audit logs to the cloud dashboard while keeping the actual credential brokering and policy evaluation local to each developer&rsquo;s machine.</p>
<h2 id="faq">FAQ</h2>
<h3 id="what-is-a-credential-broker-for-ai-agents-1">What is a credential broker for AI agents?</h3>
<p>A credential broker is a security layer that issues short-lived, scoped credentials to AI agents on demand for each individual tool call. The agent never holds long-lived secrets — it receives temporary tokens valid only for the specific operation it needs to perform. This limits the blast radius of any compromise and provides full auditability of every credential usage.</p>
<h3 id="how-does-kontext-differ-from-hashicorp-vault-or-1password">How does Kontext differ from HashiCorp Vault or 1Password?</h3>
<p>Traditional secrets managers like Vault and 1Password are designed for human operators and CI/CD pipelines — they store and rotate credentials but assume the consumer is a trusted process. Kontext is built for the non-deterministic nature of AI agents: it combines credential brokering with runtime policy enforcement, risk classification, and pre-tool-use blocking. Vault can store the credentials, but Kontext controls how and when agents use them.</p>
<h3 id="does-kontext-work-offline">Does Kontext work offline?</h3>
<p>Yes. Kontext is designed as a local-first tool. All policy decisions, credential brokering, and risk classification happen on the developer&rsquo;s machine with no cloud dependency. The SVM classifier processes decisions in ~22ms locally. Cloud sync is optional and only used for team-wide audit dashboards and centralized policy management.</p>
<h3 id="which-ai-coding-agents-does-kontext-support">Which AI coding agents does Kontext support?</h3>
<p>Kontext supports Claude Code (with pre-tool-use blocking hooks), OpenAI Codex (through command wrappers), and Claude Cowork (through the same hook system as Claude Code). It also integrates with MCP servers for broader agent ecosystem support. The integration depth varies — Claude Code has the deepest integration with pre-tool-use blocking, while Codex uses shell-level command wrapping.</p>
<h3 id="is-kontext-free-and-open-source">Is Kontext free and open source?</h3>
<p>Yes. Kontext CLI is released under the MIT license and is free to use. The source code is available on GitHub at github.com/kontext-security/kontext-cli. As of August 2026, the project has 210 GitHub stars, 7 forks, and has shipped 20 releases from v0.5.0 to v0.16.0. The cloud dashboard for managed deployments has separate pricing for enterprise teams.</p>
<h2 id="conclusion">Conclusion</h2>
<p>The credential broker is rapidly becoming essential infrastructure for teams deploying AI coding agents in production. As the IETF formalizes the pattern through the CB4A draft and multiple open-source tools emerge, the industry is converging on a simple truth: AI agents should never hold long-lived credentials.</p>
<p>Kontext stands out in this landscape by combining credential brokering with runtime security. Its three-tier Guard engine — deterministic policy, SVM risk classification, and guardrail LLM — provides defense in depth that no other credential broker offers. With support for Claude Code, Codex, Claude Cowork, and MCP servers, Kontext covers the major AI coding agent platforms while maintaining a local-first architecture that keeps decisions fast and private.</p>
<p>For teams already using AI coding agents in development or production, adding a credential broker is one of the highest-impact security improvements available in 2026. Start in observe mode, review the audit ledger, and enable enforcement gradually. The credential broker pattern isn&rsquo;t just a nice-to-have — it&rsquo;s becoming the standard way to secure AI agent operations.</p>
]]></content:encoded></item></channel></rss>