<?xml version="1.0" encoding="utf-8" standalone="yes"?><rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom" xmlns:content="http://purl.org/rss/1.0/modules/content/"><channel><title>Documentation on RockB</title><link>https://baeseokjae.github.io/tags/documentation/</link><description>Recent content in Documentation on RockB</description><image><title>RockB</title><url>https://baeseokjae.github.io/images/og-default.png</url><link>https://baeseokjae.github.io/images/og-default.png</link></image><generator>Hugo</generator><language>en-us</language><lastBuildDate>Sun, 23 Aug 2026 19:01:17 +0000</lastBuildDate><atom:link href="https://baeseokjae.github.io/tags/documentation/index.xml" rel="self" type="application/rss+xml"/><item><title>Project Wiki: Durable Traceable Project Memory for AI Coding Agents</title><link>https://baeseokjae.github.io/posts/project-wiki-durable-project-memory/</link><pubDate>Sun, 23 Aug 2026 19:01:17 +0000</pubDate><guid>https://baeseokjae.github.io/posts/project-wiki-durable-project-memory/</guid><description>A project wiki gives AI coding agents durable, traceable memory. Learn how AGENTS.md and CLAUDE.md keep context persistent, versioned, and honest across sessions.</description><content:encoded><![CDATA[<p>A project wiki is a durable, versioned set of instructions and context that AI coding agents load into every session, so they never start from zero. The open AGENTS.md format — used by over 60,000 open-source projects — and Claude Code&rsquo;s CLAUDE.md files give agents a predictable place to find build steps, conventions, and architecture. Because these files live in git, they are traceable: every change is recorded, reviewable, and honest. This guide shows you how to build one.</p>
<h2 id="why-ai-coding-agents-forget-your-project">Why AI Coding Agents Forget Your Project</h2>
<p>Every time an AI coding agent starts a session, it begins with a blank slate. Unless you give it durable context, it has to rediscover your build system, your testing conventions, and your architectural decisions from scratch — or worse, guess. This is the core problem that project memory solves.</p>
<p>The cost of forgetting is real. An agent that does not know your project conventions will:</p>
<ul>
<li>Run the wrong build command and fail</li>
<li>Write tests that do not match your testing framework</li>
<li>Refactor code in a style that conflicts with your team&rsquo;s standards</li>
<li>Repeat mistakes that were already documented and solved</li>
</ul>
<p>The open-source community has converged on a simple answer: a dedicated, predictable file that agents read at the start of every session. The AGENTS.md project describes this as &ldquo;a README for agents&rdquo; — a single, well-known location where context and instructions live. README.md is written for humans; AGENTS.md is written for the agents that work on your code.</p>
<h2 id="what-is-a-project-wiki-for-agents-agentsmd-as-the-open-standard">What Is a Project Wiki for Agents? (AGENTS.md as the Open Standard)</h2>
<p>An agent project wiki is not a sprawling documentation site. It is a focused, machine-readable set of instructions that an agent loads automatically. The most important standard today is AGENTS.md, an open format adopted by more than 60,000 open-source projects.</p>
<p>The power of AGENTS.md is that it is tool-agnostic. One file works across a growing ecosystem of agents, including Codex, Jules, Factory, Aider, goose, opencode, Zed, Warp, VS Code, and Devin. This means you write your project memory once, and every agent that touches your repository reads the same source of truth.</p>
<p>A good AGENTS.md typically contains:</p>
<ul>
<li>Build and test commands</li>
<li>Project structure and architecture overview</li>
<li>Coding conventions and style rules</li>
<li>Common pitfalls and known gotchas</li>
<li>Commands that must never be run</li>
</ul>
<p>Because it is a plain markdown file in your repository, it is versioned in git, reviewed in pull requests, and traceable. You can see exactly when a convention was added, who added it, and why.</p>
<h2 id="claudemd-vs-auto-memory-two-complementary-systems">CLAUDE.md vs Auto Memory: Two Complementary Systems</h2>
<p>Claude Code offers two complementary memory systems, and understanding the difference is key to building durable project memory.</p>
<p>The first is CLAUDE.md files, which you write. These are explicit, curated instructions that you control. They are loaded into every session and treated as context. Claude Code reads the first 200 lines or 25KB of these files, so keeping them concise matters.</p>
<p>The second is auto memory, which Claude writes automatically. It saves four note types — user, feedback, project, and reference — stored per-repository at <code>~/.claude/projects/&lt;project&gt;/memory/</code>. This captures lessons learned during real work, such as &ldquo;the user prefers X&rdquo; or &ldquo;this project uses Y.&rdquo;</p>
<table>
  <thead>
      <tr>
          <th>Feature</th>
          <th>CLAUDE.md (you write)</th>
          <th>Auto Memory (Claude writes)</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>Author</td>
          <td>Human developer</td>
          <td>AI agent</td>
      </tr>
      <tr>
          <td>Location</td>
          <td><code>./CLAUDE.md</code> or <code>./.claude/CLAUDE.md</code></td>
          <td><code>~/.claude/projects/&lt;project&gt;/memory/</code></td>
      </tr>
      <tr>
          <td>Content</td>
          <td>Curated instructions and conventions</td>
          <td>Captured lessons and feedback</td>
      </tr>
      <tr>
          <td>Versioned in git</td>
          <td>Yes</td>
          <td>No (local)</td>
      </tr>
      <tr>
          <td>Loaded every session</td>
          <td>Yes (first 200 lines / 25KB)</td>
          <td>Yes</td>
      </tr>
      <tr>
          <td>Best for</td>
          <td>Stable project truth</td>
          <td>Evolving, experiential knowledge</td>
      </tr>
  </tbody>
</table>
<p>The two systems are complementary. Use CLAUDE.md for the durable, stable facts about your project that you want to control and version. Use auto memory for the organic lessons that emerge during work. Together they give you both explicit control and automatic capture.</p>
<h2 id="scoping-your-project-memory-org-user-and-project-levels">Scoping Your Project Memory: Org, User, and Project Levels</h2>
<p>Not all project memory belongs in the same place. Claude Code scopes CLAUDE.md files across three levels, and understanding when to use each keeps your memory clean and relevant.</p>
<ul>
<li><strong>Managed policy (org level):</strong> Instructions that apply to every project in your organization. This is where you put company-wide standards, security rules, and compliance requirements.</li>
<li><strong>User level (<code>~/.claude/CLAUDE.md</code>):</strong> Personal preferences that apply across all your projects. This is where you put your own workflow preferences and tooling habits.</li>
<li><strong>Project level (<code>./CLAUDE.md</code> or <code>./.claude/CLAUDE.md</code>):</strong> Instructions specific to one repository. This is where the bulk of your project wiki lives.</li>
</ul>
<p>The same scoping logic applies to AGENTS.md. A global AGENTS.md can hold your personal or organizational defaults, while a project-level AGENTS.md holds repository-specific truth.</p>
<p>The rule of thumb is simple: put memory as close to the code it describes as possible. Project-specific conventions belong in the project file. Cross-project standards belong at the org or user level. This prevents your project wiki from becoming bloated with irrelevant instructions.</p>
<h2 id="how-to-keep-project-memory-durable-and-traceable">How to Keep Project Memory Durable and Traceable</h2>
<p>Durability and traceability are the two qualities that separate a real project wiki from a throwaway note. Here is how to achieve both.</p>
<p><strong>Durability means the memory survives context resets.</strong> Because AGENTS.md and CLAUDE.md live in your repository, they survive every session reset, every new agent, and every team member change. The memory is not in a model&rsquo;s ephemeral context window — it is in your source control.</p>
<p><strong>Traceability means you can see how the memory evolved.</strong> Because these files are versioned in git, every change is a commit. You can review a pull request that adds a new convention, see who approved it, and understand the reasoning. This is the &ldquo;honest&rdquo; part of project memory — the instructions reflect what the team actually decided, not what an agent hallucinated.</p>
<p>To keep memory single-sourced and honest, some teams use small tooling that keeps AGENTS.md and CLAUDE.md in sync. The agent-standard-oss project, for example, provides dependency-free bash tools that keep these files consistent and prevent them from drifting out of date. The goal is to avoid the problem of coding agents following stale instructions.</p>
<p>A practical traceability workflow:</p>
<ol>
<li>Treat AGENTS.md and CLAUDE.md as code, not documentation</li>
<li>Require pull requests for any change to project memory</li>
<li>Review memory changes with the same rigor as code changes</li>
<li>Add a &ldquo;last verified&rdquo; note or test that confirms the instructions still work</li>
<li>Archive obsolete instructions rather than silently deleting them</li>
</ol>
<h2 id="when-to-use-a-wiki-vs-skills-vs-path-scoped-rules">When to Use a Wiki vs Skills vs Path-Scoped Rules</h2>
<p>Project memory is not the only tool for guiding agents. Knowing when to use a wiki versus skills versus path-scoped rules keeps your setup clean.</p>
<ul>
<li><strong>Project wiki (AGENTS.md / CLAUDE.md):</strong> Use for stable, project-wide facts and conventions that every agent should know. This is your default home for durable memory.</li>
<li><strong>Skills:</strong> Use for reusable, procedural knowledge that applies across projects — for example, &ldquo;how to run our deployment&rdquo; or &ldquo;how to write a release note.&rdquo; Skills are portable and can be shared.</li>
<li><strong>Path-scoped rules:</strong> Use for instructions that apply only to specific directories or files. For example, a rule that says &ldquo;never modify files in <code>vendor/</code>&rdquo; belongs in a path-scoped rule, not in the global wiki.</li>
</ul>
<p>The decision framework is: if it applies to the whole project, put it in the wiki. If it is a reusable procedure, make it a skill. If it applies to a specific path, scope it there. This keeps each mechanism focused and prevents overlap.</p>
<h2 id="a-practical-workflow-for-building-your-project-wiki">A Practical Workflow for Building Your Project Wiki</h2>
<p>Building a project wiki does not have to be a big-bang effort. Start small and iterate.</p>
<p><strong>Step 1: Start with the essentials.</strong> Create an AGENTS.md or CLAUDE.md with just three things: how to build, how to test, and how to run the project. This alone eliminates the most common agent failures.</p>
<p><strong>Step 2: Add conventions as you encounter them.</strong> When an agent makes a mistake, add the correction to the wiki. When you discover a gotcha, document it. This turns the wiki into a living record of what you have learned.</p>
<p><strong>Step 3: Review and prune regularly.</strong> Stale instructions are worse than no instructions, because agents trust them. Schedule a periodic review to verify that every instruction still reflects reality.</p>
<p><strong>Step 4: Version and trace everything.</strong> Keep the wiki in git, require reviews for changes, and archive obsolete content. This is what makes the memory traceable.</p>
<p><strong>Step 5: Let auto memory complement the wiki.</strong> Enable auto memory so the agent captures lessons you might not think to document, then periodically promote the valuable ones into your curated wiki.</p>
<h2 id="common-pitfalls-and-how-to-avoid-stale-instructions">Common Pitfalls and How to Avoid Stale Instructions</h2>
<p>The biggest risk with project memory is staleness. An agent that follows an outdated instruction can cause more harm than an agent with no instructions at all. Here are the common pitfalls and how to avoid them.</p>
<p><strong>Pitfall 1: Instructions that drift from reality.</strong> The build command changes, but the wiki still says the old one. Avoid this by treating the wiki as code and updating it in the same pull request that changes the build.</p>
<p><strong>Pitfall 2: Bloated, unfocused memory.</strong> A wiki that tries to document everything becomes noise. Keep it focused on what agents actually need, and move reusable procedures to skills.</p>
<p><strong>Pitfall 3: Duplicated, conflicting sources.</strong> If AGENTS.md and CLAUDE.md say different things, agents get confused. Use tooling to keep them single-sourced and in sync.</p>
<p><strong>Pitfall 4: No review process.</strong> If anyone can edit project memory without review, it degrades quickly. Require pull requests and reviews for memory changes.</p>
<p><strong>Pitfall 5: Ignoring auto memory.</strong> If you rely only on hand-written files, you miss the lessons the agent learns during real work. Review auto memory periodically and promote valuable insights.</p>
<h2 id="conclusion-make-memory-a-first-class-project-artifact">Conclusion: Make Memory a First-Class Project Artifact</h2>
<p>AI coding agents are only as good as the context you give them. A project wiki built on AGENTS.md and CLAUDE.md turns your repository into a durable, traceable source of truth that survives context resets and multi-agent collaboration. Because these files live in git, your project memory is versioned, reviewable, and honest — never stale, never lost.</p>
<p>The open standard is already proven: more than 60,000 open-source projects use AGENTS.md, and it works across Codex, Claude Code, Cursor, and a dozen other agents. Start with the essentials, add conventions as you learn them, and keep everything versioned and traceable. Make project memory a first-class artifact of your repository, and your agents will stop forgetting — and start shipping.</p>
<h2 id="faq">FAQ</h2>
<p><strong>What is a project wiki for AI coding agents?</strong>
A project wiki is a durable, versioned set of instructions and context — typically an AGENTS.md or CLAUDE.md file — that AI coding agents load at the start of every session. It gives agents a predictable place to find build steps, conventions, and architecture so they do not start from zero each time.</p>
<p><strong>What is the difference between AGENTS.md and CLAUDE.md?</strong>
AGENTS.md is an open, tool-agnostic standard used by over 60,000 projects and works across many agents including Codex, Cursor, and Devin. CLAUDE.md is Claude Code&rsquo;s project memory file. Both serve the same purpose — durable project context — but AGENTS.md is the cross-tool open standard.</p>
<p><strong>How do I make project memory traceable?</strong>
Keep your project wiki in git and treat it as code. Require pull requests for any change, review memory edits with the same rigor as code changes, and archive obsolete instructions. Because every change is a commit, you can always see when and why a convention was added.</p>
<p><strong>What is Claude Code auto memory?</strong>
Auto memory is a system where Claude Code automatically saves lessons it learns during work, stored per-repository in <code>~/.claude/projects/&lt;project&gt;/memory/</code>. It captures four note types — user, feedback, project, and reference — and complements the hand-written CLAUDE.md files you control.</p>
<p><strong>How do I prevent stale instructions in my project wiki?</strong>
Treat the wiki as code and update it in the same pull request that changes the underlying behavior. Schedule periodic reviews to verify every instruction still reflects reality, keep AGENTS.md and CLAUDE.md single-sourced and in sync, and archive obsolete content instead of leaving it to mislead agents.</p>
]]></content:encoded></item></channel></rss>