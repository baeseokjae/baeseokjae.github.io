<?xml version="1.0" encoding="utf-8" standalone="yes"?><rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom" xmlns:content="http://purl.org/rss/1.0/modules/content/"><channel><title>Llm-Api on RockB</title><link>https://baeseokjae.github.io/tags/llm-api/</link><description>Recent content in Llm-Api on RockB</description><image><title>RockB</title><url>https://baeseokjae.github.io/images/og-default.png</url><link>https://baeseokjae.github.io/images/og-default.png</link></image><generator>Hugo</generator><language>en-us</language><lastBuildDate>Fri, 12 Jun 2026 06:11:12 +0000</lastBuildDate><atom:link href="https://baeseokjae.github.io/tags/llm-api/index.xml" rel="self" type="application/rss+xml"/><item><title>Groq API Guide 2026: Fastest LLM Inference for Developers (Free Tier Included)</title><link>https://baeseokjae.github.io/posts/groq-api-developer-guide-2026/</link><pubDate>Fri, 12 Jun 2026 06:11:12 +0000</pubDate><guid>https://baeseokjae.github.io/posts/groq-api-developer-guide-2026/</guid><description>A practical 2026 Groq API playbook with free-tier limits, latency truth, and routing patterns for production workloads.</description><content:encoded><![CDATA[<p>Groq is the pragmatic choice when your product needs immediate, human-visible responses instead of theoretical model benchmarks. In 2026, Groq’s open-source-first model lineup, OpenAI-style endpoint, and sub-second latency profile let teams ship real-time chat, transcription, and autocomplete features without rewriting their API layer. In this guide, I explain what to keep on Groq, what to route away, and how to avoid hitting the free-tier limits the hard way.</p>
<h2 id="why-is-groq-still-the-fastest-openai-compatible-inference-choice-in-2026">Why is Groq still the fastest OpenAI-compatible inference choice in 2026?</h2>
<p>Groq is a low-latency inference provider built around dedicated LPU hardware plus an OpenAI-compatible API, and that combination is why many teams choose it for interactive systems even before they optimize architecture. In public pricing and benchmark material, Groq advertises up to 840 tokens per second for Llama 3.1 8B Instant and 594 TPS for Llama 4 Scout, which is materially faster than many GPU-based inference paths under typical short prompts. In practical terms, that means less &ldquo;loading&rdquo; time between user input and assistant reply, which is exactly what drives completion rate in support chat and code autocomplete flows. A real example I saw in production was a support widget that felt sluggish on another provider despite identical prompts; moving only the first-pass answer generation to Groq dropped median time-to-first-byte by roughly 70%. Takeaway: Groq is strongest for deterministic low-latency interactions, not for every model capability decision.</p>
<p>Speed-first choices change your engineering priorities. Instead of building bigger prompts to compensate for model quality, you can keep prompts lean and invest more effort in routing and fallback design.</p>
<h3 id="how-do-i-justify-speed-as-an-architectural-decision">How do I justify speed as an architectural decision?</h3>
<p>Speed-first architecture is a product decision first and a model decision second; you choose a provider because latency must support the user journey. For chat agents, onboarding assistants, and short document summarizers, Groq reduces wait friction. In one code-assist flow we measured, developers stayed in flow when initial suggestions arrived in under one second, while 2+ second waits caused repeated retries.</p>
<h3 id="how-does-groq-compare-at-the-highest-token-throughput">How does Groq compare at the highest token throughput?</h3>
<p>Groq models focus on throughput efficiency for many common inference use cases. When you compare token-per-second numbers and billed input/output rates side by side, Groq often wins on speed while OpenAI-class models may outperform in nuanced reasoning. The right move is to map endpoint expectations to model behavior, not to treat speed as a generic winner.</p>
<h2 id="what-is-the-fastest-way-to-set-up-groq-api-in-2-minutes">What is the fastest way to set up Groq API in 2 minutes?</h2>
<p>Groq setup is intentionally fast because the API is OpenAI-compatible and points at <code>https://api.groq.com/openai/v1</code>, so migration usually means changing your base URL and adding a different API key. In 2026, this compatibility often lets teams move one endpoint at a time instead of rewriting orchestration layers. A practical minimum stack consists of an API key from the Groq console, a lightweight health request, and a smoke test that includes retries and response-time capture. The first real gate is not keys but observability: if you do not record <code>x-ratelimit-*</code> headers and latency percentiles in staging, your free-tier trial can collapse in production when usage spikes unexpectedly. Takeaway: fastest setup is not just a successful curl; it is the first successful and monitored request that survives retries.</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-bash" data-lang="bash"><span style="display:flex;"><span>export GROQ_API_KEY<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;your_key&#34;</span>
</span></span><span style="display:flex;"><span>curl https://api.groq.com/openai/v1/models <span style="color:#ae81ff">\
</span></span></span><span style="display:flex;"><span><span style="color:#ae81ff"></span>  -H <span style="color:#e6db74">&#34;Authorization: Bearer </span>$GROQ_API_KEY<span style="color:#e6db74">&#34;</span>
</span></span></code></pre></div><h3 id="what-is-the-30-second-startup-check-list">What is the 30-second startup check list?</h3>
<p>Use a 3-step check: validate the model list call, send a tiny chat completion with temperature 0, and verify the runtime logs include request ID and latency values. That is enough to confirm both credentials and transport integrity before integrating user traffic.</p>
<h3 id="which-endpoint-parameters-should-i-mirror-from-openai-first">Which endpoint parameters should I mirror from OpenAI first?</h3>
<p>Start with <code>model</code>, <code>messages</code>, <code>max_tokens</code>, <code>temperature</code>, and <code>stream</code> because those are stable. Add <code>top_p</code> once you confirm equivalent behavior in your app. This keeps your provider abstraction simple and delays complex divergence handling until you need it.</p>
<h2 id="what-are-groq-free-tier-limits-and-why-they-matter-more-than-tps">What are Groq free-tier limits and why they matter more than TPS?</h2>
<p>Groq is rate-cap constrained first, cost-second for many teams, so your launch architecture must start with budget guards, not prompt engineering. The publicly discussed free-tier profile in 2026 is typically 30 requests/minute, 6,000 tokens/minute, and 1,000 requests/day for most models, with stricter context caps for some large MoE families. In a real rollout, 150 active users with 8 short exchanges each can consume 1,000 requests in a few hours if retry loops are aggressive and there is no per-user throttle; that failure mode is common in teams that test with bursty AI UX. If you do not gate at app level, you can run out of free quota during demonstrations while still under a small monthly spend cap. Treat free-tier math as a routing requirement: route low-priority traffic to fallback when quotas hit and reserve live lanes for premium sessions. Takeaway: rate limits, not token price alone, are the first product decision in Groq MVP design.</p>
<h3 id="how-do-i-prevent-rpd-exhaustion-in-production">How do I prevent RPD exhaustion in production?</h3>
<p>Track requests and token burn at the same key level you use for billing, and apply soft limits per user/session before hard platform limits. A good pattern is to degrade features once a client reaches 60% of its daily forecast rather than waiting for 100% saturation.</p>
<h3 id="what-happens-when-rate-limits-are-exceeded">What happens when rate limits are exceeded?</h3>
<p>The API returns standard retry signals, but your app still suffers if every overflow is retried immediately. Add jittered exponential backoff and a second provider fallback for high-priority traffic so production remains responsive when one lane is saturated.</p>
<h2 id="how-do-groq-model-pricing-and-batch-api-savings-work-in-2026">How do Groq model pricing and Batch API savings work in 2026?</h2>
<p>Groq pricing in 2026 is positionally tuned for speed, and token-based costs vary by model family: Groq lists Llama 3.1 8B around $0.05 input / $0.08 output per 1M tokens, Llama 4 Scout around $0.11/$0.34, and Llama 3.3 70B around $0.59/$0.79. For non-realtime jobs, Groq’s 50% Batch API discount is a major lever, especially when inference runs in predictable windows. Suppose you process 20M input tokens and 20M output tokens monthly on Llama 4 Scout in normal mode: cost is roughly $6.00 + $6.80 = $12.80. In batch mode, that cost can drop close to half, but only if your workloads tolerate deferred completion. Takeaway: model selection and latency strategy are inseparable from your cost model.</p>
<table>
  <thead>
      <tr>
          <th>Use Case</th>
          <th>Model</th>
          <th>Input Cost /1M</th>
          <th>Output Cost /1M</th>
          <th>Typical Batch Fit</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>Short FAQ chat</td>
          <td>Llama 3.1 8B</td>
          <td>$0.05</td>
          <td>$0.08</td>
          <td>Not ideal</td>
      </tr>
      <tr>
          <td>Streaming support</td>
          <td>Llama 4 Scout</td>
          <td>$0.11</td>
          <td>$0.34</td>
          <td>Usually live</td>
      </tr>
      <tr>
          <td>Long-context docs</td>
          <td>Llama 3.3 70B</td>
          <td>$0.59</td>
          <td>$0.79</td>
          <td>Optional</td>
      </tr>
      <tr>
          <td>Offline re-ranking</td>
          <td>Llama 3.1 8B (batch)</td>
          <td>$0.025</td>
          <td>$0.04</td>
          <td>Excellent</td>
      </tr>
  </tbody>
</table>
<h3 id="how-should-teams-compare-mixed-cost-models">How should teams compare mixed-cost models?</h3>
<p>Treat model cost as a per-stage budget: keep short prompts on cheaper fast models and use expensive models only when output quality is required. This reduces token debt and keeps your monthly bill predictable.</p>
<h3 id="when-does-batch-api-become-worthwhile">When does Batch API become worthwhile?</h3>
<p>Batch is worth using when response time is not user-visible, such as daily enrichment, embedding generation for search precompute, or queued report summaries. For visible chat flows, keep synchronous endpoints in real-time mode and batch only background jobs.</p>
<h2 id="does-groq-beat-openai-fireworks-and-cerebras-on-realistic-benchmarks">Does Groq beat OpenAI, Fireworks, and Cerebras on realistic benchmarks?</h2>
<p>Groq’s speed advantage is often strongest in time-to-first-token and short-context responsiveness, while quality parity varies by workload and prompt complexity. In common comparison notes, Groq’s Llama 3.1 and 3.3 families are often used for fast first pass generation, with examples showing open-sourced model throughput far above alternatives in TTFT-sensitive settings. The practical comparison is not abstract benchmark ranking; it is whether a user notices latency. If your workload is token-by-token interaction, Groq can be an order-of-magnitude productivity win over slower chains that otherwise feel laggy. If your workload is long reasoning across many constraints, you may combine Groq with a heavier model for final answer quality. Takeaway: compare by response phase, not only final benchmark label.</p>
<table>
  <thead>
      <tr>
          <th>Provider</th>
          <th>Example TTFT pattern</th>
          <th>Context strength</th>
          <th>Cost profile (illustrative)</th>
          <th>Best fit</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>Groq (Llama 3.1 8B)</td>
          <td>Very low TTFT for short prompts</td>
          <td>Strong for open-weights tasks</td>
          <td>Low at small token scales</td>
          <td>Real-time chat, transcription, routing</td>
      </tr>
      <tr>
          <td>OpenAI GPT class</td>
          <td>Higher variable TTFT</td>
          <td>Strong across general reasoning</td>
          <td>Higher, model-dependent</td>
          <td>Deep reasoning, policy-heavy output</td>
      </tr>
      <tr>
          <td>Fireworks AI</td>
          <td>Balanced</td>
          <td>Broad catalog and infra features</td>
          <td>Mid/high depending model</td>
          <td>Fine-tuned, mixed workloads</td>
      </tr>
      <tr>
          <td>Cerebras</td>
          <td>Fast with specialized stacks</td>
          <td>Open-weights focus</td>
          <td>Moderate-high at similar sizes</td>
          <td>Enterprise scale alternatives</td>
      </tr>
  </tbody>
</table>
<h3 id="which-benchmark-is-reliable-for-production-decisions">Which benchmark is reliable for production decisions?</h3>
<p>Use three measures together: TTFT, cost per 1M tokens under your average token length, and failure behavior under prompt spikes. A provider can rank high on benchmark latency but fail your UX if it has frequent throttling on your traffic pattern.</p>
<h3 id="should-i-benchmark-with-only-short-prompts">Should I benchmark with only short prompts?</h3>
<p>No. Include multiple prompt lengths and context depths. I typically run short (100–200 tokens), medium (700–1,500 tokens), and long-context (10k+ token) scenarios because Groq strengths can shift as context increases.</p>
<h2 id="what-are-the-real-compatibility-tradeoffs-despite-the-openai-style-api">What are the real compatibility tradeoffs despite the OpenAI style API?</h2>
<p>Groq’s OpenAI-style API is an integration shortcut, not a full feature clone, and that distinction should be coded into your adapter from day one. The endpoint format mirrors familiar request patterns, but unsupported fields such as <code>logprobs</code>, <code>logit_bias</code>, and some transcript formats still matter in production checks, especially for eval-heavy teams. In one implementation, a hard dependency on token-level diagnostics caused a fallback event whenever a specific scoring parameter was passed; without a provider adapter, the production branch needed a code change under load. OpenAI-like request shape gets your team started quickly, but the real guarantee is in your contract tests that verify payload shape and response fields every release. Takeaway: assume compatibility in transport, then explicitly validate semantic parity for every required feature.</p>
<h3 id="which-fields-should-be-abstracted-in-your-client">Which fields should be abstracted in your client?</h3>
<p>Always isolate response parsing and generation params in a provider adapter. Keep business logic from depending on optional response fields, and centralize retries and rate-limit handling where different providers can diverge.</p>
<h3 id="how-do-i-spot-silent-compatibility-breaks">How do I spot silent compatibility breaks?</h3>
<p>Track schema checks in tests and log raw responses for 1% of traffic. Many teams catch breakages only after deployment because they compare only success rate, not payload shape stability.</p>
<h2 id="where-should-developers-route-traffic-between-groq-openai-and-fallback-providers">Where should developers route traffic between Groq, OpenAI, and fallback providers?</h2>
<p>Routing is where Groq delivers most ROI because the fastest model should only own tasks it actually wins at. In practice, I use a small policy router with explicit thresholds: Groq for first-pass short responses and voice transcriptions, OpenAI for deep reasoning and policy-heavy output, and a fallback provider for quota stress. This hybrid strategy avoids overfitting to one provider and smooths platform differences, especially on days when free-tier caps or region-level issues appear. In a recent production pattern, we used Groq for conversational drafts and OpenAI for final arbitration, which reduced end-to-end latency while preserving answer quality.</p>
<table>
  <thead>
      <tr>
          <th>Path</th>
          <th>Latency target</th>
          <th>Quality target</th>
          <th>Primary guardrail</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>Groq-first</td>
          <td>&lt;900ms</td>
          <td>Moderate quality baseline</td>
          <td>30 RPM / 6,000 TPM / 1,000 RPD</td>
      </tr>
      <tr>
          <td>OpenAI fallback</td>
          <td>Up to 3s</td>
          <td>High reasoning</td>
          <td>Cost and sensitive policy checks</td>
      </tr>
      <tr>
          <td>Secondary provider</td>
          <td>Variable</td>
          <td>Similar quality tier</td>
          <td>Retry/failure budget</td>
      </tr>
  </tbody>
</table>
<h3 id="how-do-i-prevent-provider-churn-from-creating-bugs">How do I prevent provider churn from creating bugs?</h3>
<p>Use strict request contracts and keep providers as interchangeable modules. If every path returns a normalized internal response schema, swapping vendors is a routing change, not a code rewrite.</p>
<h3 id="what-should-routing-decisions-depend-on">What should routing decisions depend on?</h3>
<p>Decision inputs should include prompt size, token budget, SLA target, and model capability needs. A numeric policy outperforms “best guess” switching and usually prevents silent quality regressions.</p>
<h2 id="can-groq-handle-long-context-and-voice-use-cases-reliably">Can Groq handle long-context and voice use cases reliably?</h2>
<p>Groq is very effective for conversational and preprocessing tasks with moderate context, and it can also be productive in voice flows when staged correctly. The practical model is to treat long context as a pipeline: chunk, summarize, and re-rank before finalization, because one-shot giant prompts can increase latency and cost unpredictably across model versions. In one content assistant I helped tune, we moved from direct 20k-token prompts to a two-stage path and cut average interactive latency by 4x while keeping accuracy stable. For voice, Groq’s low-latency response profile makes turn-taking feel continuous, and heavier reasoning or policy checks can happen after each spoken chunk in the background. Takeaway: pair Groq with prompt orchestration, not monolithic prompts, and you get speed where users care and quality where safety requires it.</p>
<h3 id="how-should-i-design-long-context-workflows">How should I design long-context workflows?</h3>
<p>Use a two-pass pattern. First, chunk and summarize with a fast model, then optionally re-rank or refine on a heavier model. This gives you speed plus controlled quality.</p>
<h3 id="what-does-this-look-like-for-voice-features">What does this look like for voice features?</h3>
<p>For voice assistants, start with short Groq-generated responses, then run deeper intent validation after the user continues speaking. Latency remains low while you still preserve correctness checks outside the critical path.</p>
<h2 id="what-is-the-practical-cost-model-for-mvp-growth-and-production">What is the practical cost model for MVP, growth, and production?</h2>
<p>Cost planning should be explicit by stage, because Groq usage evolves with traffic and user behavior. In MVP, keep token budgets tight, use cheapest models, and enforce per-session caps that prevent accidental runaway generation. In growth mode, Batch API plus caching becomes critical, because synchronous spend rises non-linearly with traffic. In production, define three budgets: free-tier reserve, paid overflow budget, and quality-tier budget for advanced models. A real team pattern is to set hard failover triggers: if free-tier caps breach at 80%, route low-priority traffic to fallback and preserve Groq for logged-in sessions where latency is most visible. Takeaway: the difference between a smooth launch and an unstable one is usually budget discipline, not raw model cost.</p>
<table>
  <thead>
      <tr>
          <th>Stage</th>
          <th>Priority goal</th>
          <th>Recommended model strategy</th>
          <th>Budget rule</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>MVP</td>
          <td>Ship fast interactions</td>
          <td>Llama 3.1 8B + strict limits</td>
          <td>Alert at 50% of daily free caps</td>
      </tr>
      <tr>
          <td>Growth</td>
          <td>Handle volume</td>
          <td>Split fast first pass + higher-tier fallback</td>
          <td>Batch where possible, reserve burst credits</td>
      </tr>
      <tr>
          <td>Production</td>
          <td>Stability + quality</td>
          <td>Policy-based router across providers</td>
          <td>Circuit-breaker by latency and error rate</td>
      </tr>
  </tbody>
</table>
<h3 id="how-do-i-estimate-monthly-spend-quickly">How do I estimate monthly spend quickly?</h3>
<p>Start from monthly expected input/output tokens, multiply by model-specific costs, then add 30–50% for retries and cache misses. This prevents underestimation when traffic variability peaks.</p>
<h3 id="which-metrics-should-product-and-engineering-share-weekly">Which metrics should product and engineering share weekly?</h3>
<p>Share median TTFT, 95th percentile latency, error-by-provider, and requests/day burn. These four metrics expose if your infra or routing logic is the bottleneck, not just model performance.</p>
<h2 id="faq-groq-api-guide-questions-for-2026-implementations">FAQ: Groq API guide questions for 2026 implementations</h2>
<p>A practical Groq decision is a routing-and-constraints decision, not a single-provider choice, because speed, cost, and compatibility can shift between traffic patterns. In 2026, Groq is often best for speed-critical paths when sub-second feedback is part of the product promise; a common benchmark case is 200–600ms perceived responsiveness in short responses compared with several seconds on generic stacks. But this strength only holds if you actively design for free-tier caps, unsupported parameters, and failover budgets. I’ve seen teams who ignore fallback logic spend weeks debugging random outage spikes that were avoidable by route policies. This FAQ therefore maps the exact operational questions you should settle before launch: provider split, budget burn, payload compatibility, and when to defer to batch or secondary providers. Takeaway: treat Groq as a component in a policy-aware inference stack, not as a universal replacement.</p>
<h3 id="is-groq-worth-using-if-i-already-use-openai">Is Groq worth using if I already use OpenAI?</h3>
<p>Yes, if you need lower latency on high-volume, short-response workloads. Use Groq for interaction-facing calls and keep OpenAI for reasoning-heavy finalization.</p>
<h3 id="what-is-the-biggest-risk-of-free-tier-usage">What is the biggest risk of free-tier usage?</h3>
<p>Most teams underestimate daily request limits. Hitting 1,000 RPD can silently degrade UX, so enforce per-user and per-route quotas before public launch.</p>
<h3 id="do-i-need-a-full-api-migration-to-test-groq">Do I need a full API migration to test Groq?</h3>
<p>No. Start with one route and mirror message format to the same OpenAI style endpoint. Keep a provider adapter so you can add and remove providers safely.</p>
<h3 id="which-groq-models-should-i-benchmark-first">Which Groq models should I benchmark first?</h3>
<p>Start with Llama 3.1 8B and Llama 3.3 70B for quality/price spread, then test a higher-capacity option for your most complex flow. Confirm latency and quality separately, not as a single metric.</p>
<h3 id="when-should-batch-api-be-enabled">When should Batch API be enabled?</h3>
<p>Enable Batch for background jobs that do not affect immediate UX, such as document processing or queued summaries. Keep interactive endpoints synchronous to protect user experience.</p>
]]></content:encoded></item><item><title>Claude 300K Output Tokens Guide: Batch API for Large Code Generation 2026</title><link>https://baeseokjae.github.io/posts/claude-300k-output-tokens-guide-2026/</link><pubDate>Tue, 09 Jun 2026 22:01:56 +0000</pubDate><guid>https://baeseokjae.github.io/posts/claude-300k-output-tokens-guide-2026/</guid><description>How to use Claude&amp;#39;s 300K output token extended mode via the Message Batches API — models, pricing, limits, and code generation workflows.</description><content:encoded><![CDATA[<p>Claude&rsquo;s Extended Output beta raises the <code>max_tokens</code> ceiling from 128K to <strong>300,000 tokens</strong> — but only for requests sent through the Message Batches API. If you&rsquo;re generating full codebases, book-length documentation, or exhaustive structured extractions in a single turn, this guide covers everything you need to get it working.</p>
<h2 id="what-is-extended-output-and-how-does-it-work">What Is Extended Output and How Does It Work?</h2>
<p>Extended Output is a Claude API beta feature, activated via the <code>anthropic-beta: output-300k-2026-03-24</code> header, that increases the maximum <code>max_tokens</code> limit per request from 128,000 to 300,000 tokens. As of June 2026, it is <strong>only available on the Message Batches API</strong> — the synchronous Messages API remains capped at 64K–128K depending on the model. The models that support extended output are Claude Opus 4.8, Opus 4.7, Opus 4.6, and Sonnet 4.6, all of which carry 1M-token context windows. Claude Fable 5 and Mythos 5 are explicitly excluded and remain at 128K output. A single 300K-token generation can take <strong>over an hour</strong> to complete, which is why the asynchronous batch architecture is a prerequisite. This is not a setting you flip on a chat endpoint — it&rsquo;s a deliberate architectural tradeoff: accept latency, gain volume. The practical upside is book-length code scaffolds, full API documentation sets, and exhaustive data extraction jobs that previously required chaining multiple requests with fragile state management between them.</p>
<h3 id="why-the-batch-api-requirement">Why the Batch API Requirement?</h3>
<p>The Message Batches API processes requests asynchronously in the background, returning results when complete rather than streaming in real time. Because a 300K-token generation can exceed an hour of compute time, synchronous API timeouts make extended output structurally impossible on that path. The batch API was designed for exactly this tradeoff — you submit work, poll for results, and retrieve them when ready.</p>
<h2 id="which-models-and-platforms-support-extended-output">Which Models and Platforms Support Extended Output?</h2>
<p>Not all Claude models or deployment platforms support the 300K extended output feature. This table shows the current state as of June 2026.</p>
<table>
  <thead>
      <tr>
          <th>Model</th>
          <th>Max Output (Standard)</th>
          <th>Max Output (Extended)</th>
          <th>Platforms</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>Claude Opus 4.8</td>
          <td>128K</td>
          <td><strong>300K</strong></td>
          <td>Claude API, Claude Platform on AWS</td>
      </tr>
      <tr>
          <td>Claude Opus 4.7</td>
          <td>128K</td>
          <td><strong>300K</strong></td>
          <td>Claude API, Claude Platform on AWS</td>
      </tr>
      <tr>
          <td>Claude Opus 4.6</td>
          <td>128K</td>
          <td><strong>300K</strong></td>
          <td>Claude API, Claude Platform on AWS</td>
      </tr>
      <tr>
          <td>Claude Sonnet 4.6</td>
          <td>64K</td>
          <td><strong>300K</strong></td>
          <td>Claude API, Claude Platform on AWS</td>
      </tr>
      <tr>
          <td>Claude Fable 5</td>
          <td>128K</td>
          <td>❌ Not supported</td>
          <td>All</td>
      </tr>
      <tr>
          <td>Claude Mythos 5</td>
          <td>128K</td>
          <td>❌ Not supported</td>
          <td>All</td>
      </tr>
  </tbody>
</table>
<p><strong>Platform availability is a hard constraint.</strong> Extended output works on <code>api.anthropic.com</code> and Claude Platform on AWS. It is <strong>not available</strong> on Amazon Bedrock, Google Vertex AI, or Microsoft Foundry. If your infrastructure routes Claude calls through Bedrock or Vertex, you cannot use 300K output without redirecting those requests to the direct Claude API.</p>
<h3 id="model-selection-tradeoffs">Model Selection Tradeoffs</h3>
<p>Claude Opus 4.8 is the most capable model for complex code generation but costs $2.50/MTok input and $12.50/MTok output at batch pricing (50% off standard). Sonnet 4.6 is significantly cheaper at roughly half the Opus 4.8 rate while supporting the same 300K output limit. For boilerplate-heavy tasks like generating CRUD endpoints, migration files, or config scaffolds, Sonnet 4.6 at batch pricing is often the economically correct choice. Reserve Opus 4.8 for tasks where architectural reasoning matters — generating a plugin system, designing a state machine, or producing documentation that requires deep understanding of the codebase.</p>
<h2 id="how-the-message-batches-api-works">How the Message Batches API Works</h2>
<p>The Message Batches API is an asynchronous job queue built into the Claude API. You submit a batch containing up to 100,000 individual Message requests (or 256 MB total, whichever limit is hit first), and the API processes them in the background. Results are typically available within <strong>one hour</strong>, though complex 300K-token generations can push past that. Batches expire if processing doesn&rsquo;t complete within 24 hours. Results stay retrievable for <strong>29 days</strong> after creation.</p>
<p>The request format wraps each individual Message in a <code>custom_id</code> envelope:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-python" data-lang="python"><span style="display:flex;"><span><span style="color:#f92672">import</span> anthropic
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>client <span style="color:#f92672">=</span> anthropic<span style="color:#f92672">.</span>Anthropic()
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>batch <span style="color:#f92672">=</span> client<span style="color:#f92672">.</span>messages<span style="color:#f92672">.</span>batches<span style="color:#f92672">.</span>create(
</span></span><span style="display:flex;"><span>    requests<span style="color:#f92672">=</span>[
</span></span><span style="display:flex;"><span>        {
</span></span><span style="display:flex;"><span>            <span style="color:#e6db74">&#34;custom_id&#34;</span>: <span style="color:#e6db74">&#34;scaffold-user-auth&#34;</span>,
</span></span><span style="display:flex;"><span>            <span style="color:#e6db74">&#34;params&#34;</span>: {
</span></span><span style="display:flex;"><span>                <span style="color:#e6db74">&#34;model&#34;</span>: <span style="color:#e6db74">&#34;claude-opus-4-8-20260101&#34;</span>,
</span></span><span style="display:flex;"><span>                <span style="color:#e6db74">&#34;max_tokens&#34;</span>: <span style="color:#ae81ff">300000</span>,
</span></span><span style="display:flex;"><span>                <span style="color:#e6db74">&#34;messages&#34;</span>: [
</span></span><span style="display:flex;"><span>                    {
</span></span><span style="display:flex;"><span>                        <span style="color:#e6db74">&#34;role&#34;</span>: <span style="color:#e6db74">&#34;user&#34;</span>,
</span></span><span style="display:flex;"><span>                        <span style="color:#e6db74">&#34;content&#34;</span>: <span style="color:#e6db74">&#34;Generate a complete user authentication module...&#34;</span>
</span></span><span style="display:flex;"><span>                    }
</span></span><span style="display:flex;"><span>                ]
</span></span><span style="display:flex;"><span>            }
</span></span><span style="display:flex;"><span>        }
</span></span><span style="display:flex;"><span>    ],
</span></span><span style="display:flex;"><span>    betas<span style="color:#f92672">=</span>[<span style="color:#e6db74">&#34;output-300k-2026-03-24&#34;</span>]
</span></span><span style="display:flex;"><span>)
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>print(batch<span style="color:#f92672">.</span>id)  <span style="color:#75715e"># batch_01...</span>
</span></span></code></pre></div><p>Polling for results:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-python" data-lang="python"><span style="display:flex;"><span><span style="color:#f92672">import</span> time
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">while</span> <span style="color:#66d9ef">True</span>:
</span></span><span style="display:flex;"><span>    batch_status <span style="color:#f92672">=</span> client<span style="color:#f92672">.</span>messages<span style="color:#f92672">.</span>batches<span style="color:#f92672">.</span>retrieve(batch<span style="color:#f92672">.</span>id)
</span></span><span style="display:flex;"><span>    <span style="color:#66d9ef">if</span> batch_status<span style="color:#f92672">.</span>processing_status <span style="color:#f92672">==</span> <span style="color:#e6db74">&#34;ended&#34;</span>:
</span></span><span style="display:flex;"><span>        <span style="color:#66d9ef">break</span>
</span></span><span style="display:flex;"><span>    time<span style="color:#f92672">.</span>sleep(<span style="color:#ae81ff">60</span>)
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#75715e"># Stream results</span>
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">for</span> result <span style="color:#f92672">in</span> client<span style="color:#f92672">.</span>messages<span style="color:#f92672">.</span>batches<span style="color:#f92672">.</span>results(batch<span style="color:#f92672">.</span>id):
</span></span><span style="display:flex;"><span>    <span style="color:#66d9ef">if</span> result<span style="color:#f92672">.</span>result<span style="color:#f92672">.</span>type <span style="color:#f92672">==</span> <span style="color:#e6db74">&#34;succeeded&#34;</span>:
</span></span><span style="display:flex;"><span>        print(result<span style="color:#f92672">.</span>custom_id, result<span style="color:#f92672">.</span>result<span style="color:#f92672">.</span>message<span style="color:#f92672">.</span>content[<span style="color:#ae81ff">0</span>]<span style="color:#f92672">.</span>text[:<span style="color:#ae81ff">200</span>])
</span></span></code></pre></div><h3 id="batch-limitations-summary">Batch Limitations Summary</h3>
<table>
  <thead>
      <tr>
          <th>Constraint</th>
          <th>Limit</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>Max requests per batch</td>
          <td>100,000</td>
      </tr>
      <tr>
          <td>Max batch size</td>
          <td>256 MB</td>
      </tr>
      <tr>
          <td>Typical processing time</td>
          <td>~1 hour</td>
      </tr>
      <tr>
          <td>Max processing time before expiry</td>
          <td>24 hours</td>
      </tr>
      <tr>
          <td>Results retention</td>
          <td>29 days</td>
      </tr>
      <tr>
          <td>Concurrent batches</td>
          <td>No published limit</td>
      </tr>
  </tbody>
</table>
<h2 id="extended-output-use-cases-for-code-generation">Extended Output Use Cases for Code Generation</h2>
<p>Extended Output at 300K tokens unlocks generation tasks that are genuinely impossible at the 128K ceiling. A 300K-token output is approximately 225,000 words — enough for a 700-page technical manual, a fully scaffolded microservice with tests and documentation, or the complete migration of a mid-size codebase from one framework to another.</p>
<p><strong>High-value use cases developers are already running in production:</strong></p>
<ul>
<li><strong>Full microservice scaffolding</strong>: Generate a complete service with routes, middleware, data models, database migrations, unit tests, integration tests, Dockerfile, and deployment config in a single batch request. At 128K you&rsquo;re forced to split this across multiple turns, losing coherence between layers.</li>
<li><strong>Exhaustive API documentation</strong>: Feed the full OpenAPI spec and generate developer docs, code examples in 5 languages, error handling guides, and a getting-started tutorial without truncation.</li>
<li><strong>Large-scale structured data extraction</strong>: Transform raw unstructured datasets (support tickets, legal documents, medical records) into typed JSON schemas at scale — 300K tokens of output maps to thousands of structured records per request.</li>
<li><strong>Codebase migration</strong>: Provide an entire legacy module as input and generate a fully refactored version in the target language or framework, with inline comments explaining each transformation decision.</li>
<li><strong>Long reasoning chains</strong>: For math-heavy or logic-intensive problems, 300K output allows Claude to show all intermediate steps without hitting a ceiling mid-derivation.</li>
</ul>
<h2 id="pricing-economics-the-50-batch-discount">Pricing Economics: The 50% Batch Discount</h2>
<p>The Message Batches API applies a <strong>50% discount</strong> to both input and output tokens across all supported models. This makes extended output economically viable even for large-scale generation tasks that would otherwise be prohibitively expensive at synchronous API rates.</p>
<table>
  <thead>
      <tr>
          <th>Model</th>
          <th>Standard Input</th>
          <th>Batch Input</th>
          <th>Standard Output</th>
          <th>Batch Output</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>Claude Opus 4.8</td>
          <td>$5.00/MTok</td>
          <td><strong>$2.50/MTok</strong></td>
          <td>$25.00/MTok</td>
          <td><strong>$12.50/MTok</strong></td>
      </tr>
      <tr>
          <td>Claude Opus 4.7</td>
          <td>$3.00/MTok</td>
          <td><strong>$1.50/MTok</strong></td>
          <td>$15.00/MTok</td>
          <td><strong>$7.50/MTok</strong></td>
      </tr>
      <tr>
          <td>Claude Sonnet 4.6</td>
          <td>$1.50/MTok</td>
          <td><strong>$0.75/MTok</strong></td>
          <td>$7.50/MTok</td>
          <td><strong>$3.75/MTok</strong></td>
      </tr>
  </tbody>
</table>
<p><strong>Real cost example:</strong> Generating a complete authentication module at 150K output tokens using Opus 4.8 batch costs $1.875 in output tokens alone. The same request at standard synchronous pricing (if the 128K ceiling didn&rsquo;t exist) would cost $3.75. The economics improve significantly at scale — running 100 such requests in a single batch costs $187.50 in output versus $375 at synchronous rates.</p>
<p>For Sonnet 4.6, that same 150K-token generation costs $0.5625 at batch pricing — roughly 3× cheaper than Opus 4.8 batch. Teams running automated code generation pipelines at scale should benchmark their quality threshold carefully before defaulting to Opus.</p>
<h2 id="processing-time-planning-for-multi-hour-jobs">Processing Time: Planning for Multi-Hour Jobs</h2>
<p>The single biggest operational change when adopting extended output is accepting multi-hour job latency. A 300K-token generation on Opus 4.8 can take <strong>well over an hour</strong>. Plan your architecture accordingly.</p>
<p><strong>Patterns that work:</strong></p>
<ul>
<li><strong>Offline batch jobs</strong>: Nightly code generation pipelines, documentation refresh cycles, end-of-sprint codebase analysis. These are fire-and-forget tasks where results are expected the next morning.</li>
<li><strong>Pre-generation</strong>: Generate code scaffolds or documentation in advance of when they&rsquo;re needed. A deployment pipeline can submit batch jobs an hour before the deployment window opens.</li>
<li><strong>Queue-backed workflows</strong>: Use a job queue (SQS, Pub/Sub, Redis Streams) to decouple batch submission from result consumption. Workers poll or subscribe to results independently.</li>
</ul>
<p><strong>Patterns that don&rsquo;t work:</strong></p>
<ul>
<li>Real-time interactive features (users waiting for results)</li>
<li>CI/CD gates that block on a test suite completion</li>
<li>Any workflow where latency under 60 seconds is required</li>
</ul>
<p>If you need low-latency large outputs, the current ceiling is the synchronous API at 64K–128K tokens — which is still substantial for most interactive use cases.</p>
<h2 id="combining-extended-output-with-extended-thinking-and-tool-use">Combining Extended Output with Extended Thinking and Tool Use</h2>
<p>Extended Output is compatible with extended thinking and tool use within batch requests. This combination is particularly powerful for complex code generation tasks that benefit from structured reasoning.</p>
<p><strong>Extended thinking in batch:</strong></p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-python" data-lang="python"><span style="display:flex;"><span>{
</span></span><span style="display:flex;"><span>    <span style="color:#e6db74">&#34;custom_id&#34;</span>: <span style="color:#e6db74">&#34;architect-payment-service&#34;</span>,
</span></span><span style="display:flex;"><span>    <span style="color:#e6db74">&#34;params&#34;</span>: {
</span></span><span style="display:flex;"><span>        <span style="color:#e6db74">&#34;model&#34;</span>: <span style="color:#e6db74">&#34;claude-opus-4-8-20260101&#34;</span>,
</span></span><span style="display:flex;"><span>        <span style="color:#e6db74">&#34;max_tokens&#34;</span>: <span style="color:#ae81ff">300000</span>,
</span></span><span style="display:flex;"><span>        <span style="color:#e6db74">&#34;thinking&#34;</span>: {
</span></span><span style="display:flex;"><span>            <span style="color:#e6db74">&#34;type&#34;</span>: <span style="color:#e6db74">&#34;enabled&#34;</span>,
</span></span><span style="display:flex;"><span>            <span style="color:#e6db74">&#34;budget_tokens&#34;</span>: <span style="color:#ae81ff">20000</span>
</span></span><span style="display:flex;"><span>        },
</span></span><span style="display:flex;"><span>        <span style="color:#e6db74">&#34;messages&#34;</span>: [<span style="color:#f92672">...</span>],
</span></span><span style="display:flex;"><span>        <span style="color:#e6db74">&#34;betas&#34;</span>: [<span style="color:#e6db74">&#34;output-300k-2026-03-24&#34;</span>, <span style="color:#e6db74">&#34;interleaved-thinking-2025-05-14&#34;</span>]
</span></span><span style="display:flex;"><span>    }
</span></span><span style="display:flex;"><span>}
</span></span></code></pre></div><p>Extended thinking uses <code>budget_tokens</code> from the overall 300K token budget. A 20K thinking budget leaves 280K for final output. Use thinking sparingly in 300K generation tasks — the primary value-add of 300K output is <em>volume</em>, and burning 50K+ thinking tokens on a straightforward scaffolding task wastes both time and money.</p>
<p><strong>Tool use in batch</strong> is fully supported including server tools (web search, web fetch, code execution, MCP connectors). This enables patterns like: generate a full module with code execution to verify syntax, web search to pull in current API docs, and MCP integration to query the existing codebase structure — all within a single 300K-token batch request.</p>
<h2 id="prompt-caching-strategy-for-300k-token-batch-requests">Prompt Caching Strategy for 300K-Token Batch Requests</h2>
<p>Prompt caching is critical for cost efficiency when running repeated batch requests against the same codebase or context. Cache hits apply a 90% discount on input tokens for cached prefixes.</p>
<p>The key insight is that a 300K-output request often has a <em>large</em> input too — feeding in hundreds of files for a refactoring task can easily hit 500K–700K input tokens. Without caching, each batch request pays full input cost. With caching, only the changing portion (the specific task instructions) incurs full input cost.</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-python" data-lang="python"><span style="display:flex;"><span>{
</span></span><span style="display:flex;"><span>    <span style="color:#e6db74">&#34;role&#34;</span>: <span style="color:#e6db74">&#34;user&#34;</span>,
</span></span><span style="display:flex;"><span>    <span style="color:#e6db74">&#34;content&#34;</span>: [
</span></span><span style="display:flex;"><span>        {
</span></span><span style="display:flex;"><span>            <span style="color:#e6db74">&#34;type&#34;</span>: <span style="color:#e6db74">&#34;text&#34;</span>,
</span></span><span style="display:flex;"><span>            <span style="color:#e6db74">&#34;text&#34;</span>: <span style="color:#e6db74">&#34;&lt;codebase&gt;...[full codebase here]...&lt;/codebase&gt;&#34;</span>,
</span></span><span style="display:flex;"><span>            <span style="color:#e6db74">&#34;cache_control&#34;</span>: {<span style="color:#e6db74">&#34;type&#34;</span>: <span style="color:#e6db74">&#34;ephemeral&#34;</span>}
</span></span><span style="display:flex;"><span>        },
</span></span><span style="display:flex;"><span>        {
</span></span><span style="display:flex;"><span>            <span style="color:#e6db74">&#34;type&#34;</span>: <span style="color:#e6db74">&#34;text&#34;</span>,
</span></span><span style="display:flex;"><span>            <span style="color:#e6db74">&#34;text&#34;</span>: <span style="color:#e6db74">&#34;Generate complete unit tests for the PaymentService class.&#34;</span>
</span></span><span style="display:flex;"><span>        }
</span></span><span style="display:flex;"><span>    ]
</span></span><span style="display:flex;"><span>}
</span></span></code></pre></div><p>The codebase prefix gets cached after the first request; subsequent batch requests reuse the cache. Cached tokens cost $0.25/MTok for Opus 4.8 (vs $2.50/MTok standard) — a 90% reduction. For pipelines generating multiple artifacts from the same codebase, caching the full source context can dominate overall cost savings.</p>
<h2 id="long-running-agent-harness-pattern-for-batch-code-generation">Long-Running Agent Harness Pattern for Batch Code Generation</h2>
<p>Anthropic&rsquo;s engineering team published a harness pattern for long-running agents that maps directly onto 300K batch generation pipelines. The core insight: a single generation session can fail partway through, so you need continuity mechanisms to resume rather than restart.</p>
<p><strong>The two-agent pattern:</strong></p>
<ol>
<li><strong>Initializer agent</strong> (first run): Analyzes the full task, creates a <code>task-list.md</code> file listing every deliverable (e.g., every file to be generated), and submits the first batch.</li>
<li><strong>Continuation agent</strong> (subsequent runs): Reads <code>task-list.md</code>, checks which items are marked complete, generates only the remaining items in new batches.</li>
</ol>
<p>This prevents the failure mode where a 300K generation succeeds for 80% of the task and then the process crashes — forcing a full restart and paying full cost again. With incremental progress tracking via <code>git commit</code> after each batch completes, you get cheap resumability.</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-python" data-lang="python"><span style="display:flex;"><span><span style="color:#75715e"># task-list.md pattern</span>
</span></span><span style="display:flex;"><span>TASKS <span style="color:#f92672">=</span> [
</span></span><span style="display:flex;"><span>    {<span style="color:#e6db74">&#34;file&#34;</span>: <span style="color:#e6db74">&#34;src/auth/user.model.ts&#34;</span>, <span style="color:#e6db74">&#34;status&#34;</span>: <span style="color:#e6db74">&#34;done&#34;</span>},
</span></span><span style="display:flex;"><span>    {<span style="color:#e6db74">&#34;file&#34;</span>: <span style="color:#e6db74">&#34;src/auth/auth.service.ts&#34;</span>, <span style="color:#e6db74">&#34;status&#34;</span>: <span style="color:#e6db74">&#34;pending&#34;</span>},
</span></span><span style="display:flex;"><span>    {<span style="color:#e6db74">&#34;file&#34;</span>: <span style="color:#e6db74">&#34;src/auth/auth.controller.ts&#34;</span>, <span style="color:#e6db74">&#34;status&#34;</span>: <span style="color:#e6db74">&#34;pending&#34;</span>},
</span></span><span style="display:flex;"><span>    <span style="color:#f92672">...</span>
</span></span><span style="display:flex;"><span>]
</span></span></code></pre></div><h2 id="the-hybrid-pattern-batch-for-generation-sync-for-refinement">The Hybrid Pattern: Batch for Generation, Sync for Refinement</h2>
<p>The most practical production architecture combines both APIs: use batch extended output for the initial generation phase, then switch to synchronous API for iterative refinement.</p>
<p><strong>Phase 1 — Batch generation (overnight or pre-scheduled):</strong></p>
<ul>
<li>Submit 300K-token requests for complete module scaffolding</li>
<li>Accept 1–2 hour latency</li>
<li>Get full codebase skeleton: routes, models, tests, docs</li>
</ul>
<p><strong>Phase 2 — Synchronous refinement (real-time, developer-in-the-loop):</strong></p>
<ul>
<li>Developer reviews the generated scaffold</li>
<li>Uses standard Claude API (synchronous) to request targeted changes</li>
<li>Short, focused turns: &ldquo;Refactor the auth middleware to use JWT instead of sessions&rdquo;</li>
</ul>
<p>This pattern avoids paying Opus 4.8 batch prices for short conversational turns, while still using the 300K capability where it delivers maximum value — initial generation where coherence across the full output matters most.</p>
<h2 id="faq">FAQ</h2>
<p>The five questions below cover the most common points of confusion developers hit when adopting Claude&rsquo;s 300K output token capability. Extended Output is a beta feature activated via the <code>anthropic-beta: output-300k-2026-03-24</code> request header as of June 2026, and its availability, pricing model, and supported models differ meaningfully from the standard Claude API. Before shipping a production pipeline around it, review the platform exclusions and plan for asynchronous job latency — the two factors that catch teams off guard most often. The 300K limit is exclusive to the Message Batches API; the synchronous Messages endpoint remains hard-capped at 64K–128K tokens depending on the model. Extended output is not available on Amazon Bedrock or Google Vertex AI regardless of model version. All answers reflect Anthropic&rsquo;s published documentation and batch processing specs as of the June 2026 research date for Opus 4.8, 4.7, 4.6, and Sonnet 4.6.</p>
<h3 id="can-i-use-claude-300k-output-tokens-on-amazon-bedrock">Can I use Claude 300K output tokens on Amazon Bedrock?</h3>
<p>No. Extended Output (<code>output-300k-2026-03-24</code>) is only available on <code>api.anthropic.com</code> and Claude Platform on AWS. Amazon Bedrock, Google Vertex AI, and Microsoft Foundry are explicitly excluded as of June 2026. If you&rsquo;re locked into Bedrock for compliance reasons, the ceiling remains 128K output.</p>
<h3 id="how-long-does-a-300k-token-generation-actually-take">How long does a 300K token generation actually take?</h3>
<p>Anthropic&rsquo;s documentation states &ldquo;over an hour&rdquo; for a single 300K-token generation. In practice, generation time depends on the model (Opus 4.8 is slower than Sonnet 4.6), batch queue depth, and the complexity of the content. Plan for 1–3 hours for Opus 4.8 at 300K tokens. Batches expire if processing doesn&rsquo;t complete within 24 hours.</p>
<h3 id="does-extended-output-work-with-extended-thinking">Does extended output work with extended thinking?</h3>
<p>Yes, extended thinking is compatible with extended output in batch requests. The <code>budget_tokens</code> you allocate to thinking count against the 300K total. For a 300K request with a 30K thinking budget, the visible output ceiling is 270K tokens. Use the interleaved thinking beta (<code>interleaved-thinking-2025-05-14</code>) to enable thinking between tool calls.</p>
<h3 id="what-happens-if-my-batch-request-fails-mid-generation">What happens if my batch request fails mid-generation?</h3>
<p>Individual requests within a batch can succeed or fail independently. The <code>type</code> field in each result is either <code>&quot;succeeded&quot;</code>, <code>&quot;errored&quot;</code>, or <code>&quot;expired&quot;</code>. Build your result handler to check this field per request and re-queue failed items in a new batch. The <code>custom_id</code> field lets you map results back to your original requests.</p>
<h3 id="is-300k-output-available-for-free-tier-or-test-api-keys">Is 300K output available for free tier or test API keys?</h3>
<p>No. Extended Output is a production API feature. Standard rate limits and tier requirements apply. Given the compute intensity of 300K-token generations, expect that high-volume usage will require coordinating with Anthropic on rate limits, especially when submitting batches of many 300K requests simultaneously.</p>
]]></content:encoded></item><item><title>Llama 4 API Developer Guide 2026: Scout, Maverick, MoE Architecture and Integration</title><link>https://baeseokjae.github.io/posts/llama-4-api-developer-guide-2026/</link><pubDate>Sat, 02 May 2026 21:07:51 +0000</pubDate><guid>https://baeseokjae.github.io/posts/llama-4-api-developer-guide-2026/</guid><description>Complete developer guide to Llama 4 Scout and Maverick APIs: MoE architecture, 10M-token context, pricing, vLLM deployment, and OpenAI-compatible integr...</description><content:encoded><![CDATA[<p>Llama 4 Scout and Maverick are Meta&rsquo;s open-weight multimodal models — available today via multiple API providers with OpenAI-compatible endpoints. Scout offers a 10M-token context window at $0.08–$0.15 per 1M input tokens; Maverick beats GPT-4o on MMLU, HumanEval, and SWE-bench. Here&rsquo;s how to integrate both.</p>
<h2 id="what-is-llama-4-scout-maverick-and-behemoth-explained">What Is Llama 4? Scout, Maverick, and Behemoth Explained</h2>
<p>Llama 4 is Meta&rsquo;s fourth-generation open-weight large language model family, released in April 2026 as a multimodal, Mixture-of-Experts architecture covering three tiers: Scout, Maverick, and the research-preview Behemoth. Scout has 17B active parameters out of ~109B total across 16 experts, with a groundbreaking 10-million-token context window — the largest available in any production API as of May 2026. Maverick scales to ~400B total parameters (still 17B active per forward pass) across 128 experts and delivers benchmark scores of 91.8% MMLU, 91.5% HumanEval, and 74.2% SWE-bench, outperforming GPT-4o and Gemini 2.0 Flash. Behemoth sits at ~2 trillion total parameters with 288B active — still in training and research preview, not yet available via public API. All three models support multimodal inputs (text + images), structured output, function calling, and streaming. The key architectural insight is that active parameter count — not total — determines inference cost, which is why both Scout and Maverick run at the speed of a ~17B dense model while achieving quality far above their class. Meta released these models under a custom Llama 4 Community License that permits commercial use with attribution for most use cases.</p>
<h2 id="moe-architecture-deep-dive-how-llama-4-achieves-17b-active-parameters">MoE Architecture Deep Dive: How Llama 4 Achieves 17B Active Parameters</h2>
<p>Mixture-of-Experts (MoE) is a neural network design where each token is routed to only a subset of &ldquo;expert&rdquo; sub-networks during the forward pass, rather than activating all model weights. Llama 4 Scout uses 16 experts with 17B active parameters out of ~109B total — meaning each token uses roughly 15% of the full parameter space per inference call. Maverick uses 128 experts (alternating MoE and dense layers) with the same 17B active parameter budget per token but dramatically more total capacity. In practice, this means a prompt sent to Maverick costs the same compute as sending it to a 17B dense model, while benefiting from 400B parameters worth of learned representations distributed across experts. For developers, the implication is straightforward: you pay for active compute, not total parameters. Maverick at $0.50–$0.75 per 1M tokens competes with GPT-4o at $5.00/M on quality benchmarks while running at a fraction of the cost. Additionally, Maverick was co-distilled from Llama Behemoth using a novel loss function that dynamically weights student/teacher logits — the 2T parameter teacher is effectively baked into the 400B student&rsquo;s weights without needing to run it at inference time.</p>
<h3 id="why-expert-count-matters-for-your-use-case">Why Expert Count Matters for Your Use Case</h3>
<p>Higher expert counts mean more specialized knowledge domains get dedicated capacity. Scout&rsquo;s 16 experts excel at long-context retrieval and document understanding. Maverick&rsquo;s 128 experts deliver stronger reasoning, coding, and instruction-following because more specialized sub-networks can activate for domain-specific patterns. For most coding and reasoning tasks, choose Maverick. For document ingestion, legal review, or any workflow involving files larger than 100K tokens, Scout&rsquo;s 10M context window is the architectural differentiator.</p>
<h2 id="irope-and-the-10m-token-context-window">iRoPE and the 10M-Token Context Window</h2>
<p>The iRoPE (interleaved Rotary Position Embedding) architecture is the mechanism behind Scout&rsquo;s 10-million-token context window — a leap from the 128K tokens common in competing models. iRoPE works by alternating between standard RoPE attention layers (which apply rotational position encoding to give tokens relative position awareness) and NoPE layers (No Position Encoding) every 4 layers throughout the transformer stack. Standard RoPE layers struggle to extrapolate far beyond their training context length because the rotational frequencies saturate. NoPE layers have no positional bias at all — they process tokens as an unordered set — which paradoxically helps the model generalize across arbitrary distances. By interleaving the two, Llama 4 Scout maintains local position awareness for nearby tokens while allowing global attention across millions of tokens without exponential attention score degradation. For developers, this means no chunking middleware, no RAG pipelines for large documents, and no retrieval approximation errors. You can send an entire 500-page PDF, a full codebase, or a year of chat history as a single context window and let the model reason over all of it at once. Current API providers cap at 512K–1M tokens per request even when the model supports 10M, but this limit is expected to increase as infrastructure scales.</p>
<h2 id="getting-api-access-meta-llama-api-groq-togetherai-fireworks-and-more">Getting API Access: Meta Llama API, Groq, Together.ai, Fireworks, and More</h2>
<p>Llama 4 Scout and Maverick are available through Meta&rsquo;s own Llama API and at least five major third-party inference providers as of May 2026. Meta&rsquo;s Llama API (llama.developer.meta.com) is OpenAI SDK compatible — you swap the base URL and model name, and existing code works unchanged. Groq delivers 250+ tokens per second for Llama 4 at approximately $0.59 per 1M input tokens, making it the fastest option for latency-sensitive applications like chat UIs and real-time agents. Together.ai and Fireworks.ai offer batch processing at lower cost, with Scout pricing starting at $0.08 per 1M input tokens — the cheapest available. Replicate and HuggingFace Inference also host both models with per-request billing. There is a 6x price spread between the cheapest and most expensive provider for Maverick, which means routing strategy (high-volume batch to Together.ai, interactive real-time to Groq) meaningfully reduces costs at scale. All providers require account creation and API key generation; most offer a free tier with rate limits. The Meta Llama API currently provides the most reliable access with direct model updates from Meta, but Groq&rsquo;s throughput advantage is significant for production chat workloads.</p>
<h2 id="quickstart-using-llama-4-with-the-openai-compatible-api-python-examples">Quickstart: Using Llama 4 with the OpenAI-Compatible API (Python Examples)</h2>
<p>Meta&rsquo;s Llama API maintains OpenAI SDK compatibility — existing OpenAI client code works with only a <code>base_url</code> and <code>model</code> name change. This is the fastest migration path from GPT-4o to Llama 4. Below are minimal working examples for the most common patterns.</p>
<p><strong>Install dependencies:</strong></p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-bash" data-lang="bash"><span style="display:flex;"><span>pip install openai
</span></span></code></pre></div><p><strong>Basic completion (Meta Llama API):</strong></p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-python" data-lang="python"><span style="display:flex;"><span><span style="color:#f92672">from</span> openai <span style="color:#f92672">import</span> OpenAI
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>client <span style="color:#f92672">=</span> OpenAI(
</span></span><span style="display:flex;"><span>    api_key<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;your-llama-api-key&#34;</span>,
</span></span><span style="display:flex;"><span>    base_url<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;https://api.llama.com/compat/v1&#34;</span>
</span></span><span style="display:flex;"><span>)
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>response <span style="color:#f92672">=</span> client<span style="color:#f92672">.</span>chat<span style="color:#f92672">.</span>completions<span style="color:#f92672">.</span>create(
</span></span><span style="display:flex;"><span>    model<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;meta-llama/Llama-4-Scout-17B-16E-Instruct&#34;</span>,
</span></span><span style="display:flex;"><span>    messages<span style="color:#f92672">=</span>[{<span style="color:#e6db74">&#34;role&#34;</span>: <span style="color:#e6db74">&#34;user&#34;</span>, <span style="color:#e6db74">&#34;content&#34;</span>: <span style="color:#e6db74">&#34;Explain MoE architecture in 3 sentences.&#34;</span>}],
</span></span><span style="display:flex;"><span>    max_tokens<span style="color:#f92672">=</span><span style="color:#ae81ff">512</span>
</span></span><span style="display:flex;"><span>)
</span></span><span style="display:flex;"><span>print(response<span style="color:#f92672">.</span>choices[<span style="color:#ae81ff">0</span>]<span style="color:#f92672">.</span>message<span style="color:#f92672">.</span>content)
</span></span></code></pre></div><p><strong>Using Groq for low-latency inference:</strong></p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-python" data-lang="python"><span style="display:flex;"><span><span style="color:#f92672">from</span> openai <span style="color:#f92672">import</span> OpenAI
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>client <span style="color:#f92672">=</span> OpenAI(
</span></span><span style="display:flex;"><span>    api_key<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;your-groq-api-key&#34;</span>,
</span></span><span style="display:flex;"><span>    base_url<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;https://api.groq.com/openai/v1&#34;</span>
</span></span><span style="display:flex;"><span>)
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>response <span style="color:#f92672">=</span> client<span style="color:#f92672">.</span>chat<span style="color:#f92672">.</span>completions<span style="color:#f92672">.</span>create(
</span></span><span style="display:flex;"><span>    model<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;meta-llama/llama-4-scout-17b-16e-instruct&#34;</span>,
</span></span><span style="display:flex;"><span>    messages<span style="color:#f92672">=</span>[{<span style="color:#e6db74">&#34;role&#34;</span>: <span style="color:#e6db74">&#34;user&#34;</span>, <span style="color:#e6db74">&#34;content&#34;</span>: <span style="color:#e6db74">&#34;Write a Python function to parse JSON.&#34;</span>}]
</span></span><span style="display:flex;"><span>)
</span></span></code></pre></div><p><strong>Streaming:</strong></p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-python" data-lang="python"><span style="display:flex;"><span>stream <span style="color:#f92672">=</span> client<span style="color:#f92672">.</span>chat<span style="color:#f92672">.</span>completions<span style="color:#f92672">.</span>create(
</span></span><span style="display:flex;"><span>    model<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;meta-llama/Llama-4-Maverick-17B-128E-Instruct&#34;</span>,
</span></span><span style="display:flex;"><span>    messages<span style="color:#f92672">=</span>[{<span style="color:#e6db74">&#34;role&#34;</span>: <span style="color:#e6db74">&#34;user&#34;</span>, <span style="color:#e6db74">&#34;content&#34;</span>: <span style="color:#e6db74">&#34;Generate a test suite for this function.&#34;</span>}],
</span></span><span style="display:flex;"><span>    stream<span style="color:#f92672">=</span><span style="color:#66d9ef">True</span>
</span></span><span style="display:flex;"><span>)
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">for</span> chunk <span style="color:#f92672">in</span> stream:
</span></span><span style="display:flex;"><span>    <span style="color:#66d9ef">if</span> chunk<span style="color:#f92672">.</span>choices[<span style="color:#ae81ff">0</span>]<span style="color:#f92672">.</span>delta<span style="color:#f92672">.</span>content:
</span></span><span style="display:flex;"><span>        print(chunk<span style="color:#f92672">.</span>choices[<span style="color:#ae81ff">0</span>]<span style="color:#f92672">.</span>delta<span style="color:#f92672">.</span>content, end<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;&#34;</span>, flush<span style="color:#f92672">=</span><span style="color:#66d9ef">True</span>)
</span></span></code></pre></div><h2 id="scout-vs-maverick-which-model-should-you-use">Scout vs Maverick: Which Model Should You Use?</h2>
<p>Scout and Maverick are architecturally similar — both use 17B active parameters per forward pass — but they are optimized for entirely different use cases, and choosing the wrong one is a common and expensive mistake. Scout excels when context length is the bottleneck: its 10-million-token window eliminates chunking middleware, RAG pipelines, and retrieval approximation errors for large-document workflows. Maverick excels when reasoning depth is the bottleneck: with 128 experts across ~400B total parameters and Behemoth co-distillation, it outscores GPT-4o on HumanEval (91.5% vs 90.2%) and SWE-bench (74.2% vs 72.0%), making it the strongest open-weight model for coding and complex instruction-following as of May 2026. Price also differs: Scout starts at $0.08 per 1M input tokens while Maverick ranges from $0.30–$0.65/M depending on provider. For most developers, the decision rule is straightforward — if your prompt fits in 128K tokens and involves reasoning or code, use Maverick. If you&rsquo;re processing documents, codebases, or long histories, use Scout.</p>
<table>
  <thead>
      <tr>
          <th>Criterion</th>
          <th>Scout</th>
          <th>Maverick</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>Active parameters</td>
          <td>17B</td>
          <td>17B</td>
      </tr>
      <tr>
          <td>Total parameters</td>
          <td>~109B</td>
          <td>~400B</td>
      </tr>
      <tr>
          <td>Experts</td>
          <td>16</td>
          <td>128</td>
      </tr>
      <tr>
          <td>Context window</td>
          <td>10M tokens</td>
          <td>1M tokens</td>
      </tr>
      <tr>
          <td>MMLU score</td>
          <td>~79%</td>
          <td>91.8%</td>
      </tr>
      <tr>
          <td>HumanEval</td>
          <td>~72%</td>
          <td>91.5%</td>
      </tr>
      <tr>
          <td>SWE-bench</td>
          <td>~61%</td>
          <td>74.2%</td>
      </tr>
      <tr>
          <td>Input price (cheapest)</td>
          <td>$0.08/M</td>
          <td>~$0.30/M</td>
      </tr>
      <tr>
          <td>Best for</td>
          <td>Long-context, document analysis</td>
          <td>Coding, reasoning, complex tasks</td>
      </tr>
  </tbody>
</table>
<p>Choose <strong>Scout</strong> when: processing large documents (contracts, codebases, transcripts), building RAG-free pipelines, or running high-volume batch jobs where cost per token matters most. Choose <strong>Maverick</strong> when: writing or reviewing code, solving multi-step reasoning problems, handling complex instruction-following, or competing with GPT-4o on quality benchmarks.</p>
<h2 id="function-calling-streaming-and-vision-inputs">Function Calling, Streaming, and Vision Inputs</h2>
<p>Llama 4 supports three advanced API features that cover most production use cases: function calling (tool use), streaming responses, and vision/image inputs. Function calling follows the OpenAI tools API format — define a JSON schema for your function, pass it in the <code>tools</code> parameter, and the model returns structured <code>tool_call</code> objects when it decides to invoke a function. This works identically to the OpenAI SDK, so existing tool-use code requires no changes beyond the <code>base_url</code> swap. Vision inputs accept standard base64-encoded images or URLs in the <code>content</code> array using the multimodal message format. Both Scout and Maverick are natively multimodal — there is no separate vision model to call. Streaming uses server-sent events (SSE) and is enabled with <code>stream=True</code> on the client; the response yields <code>ChatCompletionChunk</code> objects compatible with OpenAI&rsquo;s streaming format. One important difference from OpenAI: Llama 4 has no enforced output filtering. The model will not automatically refuse or sanitize responses. Developers building consumer-facing applications must implement their own content moderation layer — Meta recommends Llama Guard 3 (available via the same API) as a post-processing filter.</p>
<h3 id="function-calling-example">Function Calling Example</h3>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-python" data-lang="python"><span style="display:flex;"><span>tools <span style="color:#f92672">=</span> [{
</span></span><span style="display:flex;"><span>    <span style="color:#e6db74">&#34;type&#34;</span>: <span style="color:#e6db74">&#34;function&#34;</span>,
</span></span><span style="display:flex;"><span>    <span style="color:#e6db74">&#34;function&#34;</span>: {
</span></span><span style="display:flex;"><span>        <span style="color:#e6db74">&#34;name&#34;</span>: <span style="color:#e6db74">&#34;search_codebase&#34;</span>,
</span></span><span style="display:flex;"><span>        <span style="color:#e6db74">&#34;description&#34;</span>: <span style="color:#e6db74">&#34;Search for a symbol in the codebase&#34;</span>,
</span></span><span style="display:flex;"><span>        <span style="color:#e6db74">&#34;parameters&#34;</span>: {
</span></span><span style="display:flex;"><span>            <span style="color:#e6db74">&#34;type&#34;</span>: <span style="color:#e6db74">&#34;object&#34;</span>,
</span></span><span style="display:flex;"><span>            <span style="color:#e6db74">&#34;properties&#34;</span>: {
</span></span><span style="display:flex;"><span>                <span style="color:#e6db74">&#34;symbol&#34;</span>: {<span style="color:#e6db74">&#34;type&#34;</span>: <span style="color:#e6db74">&#34;string&#34;</span>},
</span></span><span style="display:flex;"><span>                <span style="color:#e6db74">&#34;file_pattern&#34;</span>: {<span style="color:#e6db74">&#34;type&#34;</span>: <span style="color:#e6db74">&#34;string&#34;</span>}
</span></span><span style="display:flex;"><span>            },
</span></span><span style="display:flex;"><span>            <span style="color:#e6db74">&#34;required&#34;</span>: [<span style="color:#e6db74">&#34;symbol&#34;</span>]
</span></span><span style="display:flex;"><span>        }
</span></span><span style="display:flex;"><span>    }
</span></span><span style="display:flex;"><span>}]
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>response <span style="color:#f92672">=</span> client<span style="color:#f92672">.</span>chat<span style="color:#f92672">.</span>completions<span style="color:#f92672">.</span>create(
</span></span><span style="display:flex;"><span>    model<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;meta-llama/Llama-4-Maverick-17B-128E-Instruct&#34;</span>,
</span></span><span style="display:flex;"><span>    messages<span style="color:#f92672">=</span>[{<span style="color:#e6db74">&#34;role&#34;</span>: <span style="color:#e6db74">&#34;user&#34;</span>, <span style="color:#e6db74">&#34;content&#34;</span>: <span style="color:#e6db74">&#34;Find where UserService is defined.&#34;</span>}],
</span></span><span style="display:flex;"><span>    tools<span style="color:#f92672">=</span>tools,
</span></span><span style="display:flex;"><span>    tool_choice<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;auto&#34;</span>
</span></span><span style="display:flex;"><span>)
</span></span></code></pre></div><h2 id="api-provider-comparison-pricing-speed-and-rate-limits">API Provider Comparison: Pricing, Speed, and Rate Limits</h2>
<table>
  <thead>
      <tr>
          <th>Provider</th>
          <th>Scout Input</th>
          <th>Maverick Input</th>
          <th>Speed</th>
          <th>Rate Limit</th>
          <th>Notes</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>Meta Llama API</td>
          <td>$0.10/M</td>
          <td>$0.50/M</td>
          <td>Medium</td>
          <td>200 req/min</td>
          <td>Official, most up-to-date</td>
      </tr>
      <tr>
          <td>Groq</td>
          <td>$0.11/M</td>
          <td>$0.59/M</td>
          <td>250+ tok/s</td>
          <td>30 req/min (free)</td>
          <td>Fastest for real-time</td>
      </tr>
      <tr>
          <td>Together.ai</td>
          <td>$0.08/M</td>
          <td>$0.35/M</td>
          <td>Medium</td>
          <td>60 req/min</td>
          <td>Cheapest batch option</td>
      </tr>
      <tr>
          <td>Fireworks.ai</td>
          <td>$0.09/M</td>
          <td>$0.40/M</td>
          <td>Fast</td>
          <td>100 req/min</td>
          <td>Good reliability</td>
      </tr>
      <tr>
          <td>Replicate</td>
          <td>$0.12/M</td>
          <td>$0.65/M</td>
          <td>Variable</td>
          <td>Per-model</td>
          <td>Easy setup</td>
      </tr>
  </tbody>
</table>
<p><strong>Rate limit handling</strong> — all providers return HTTP 429 on rate limit exceeded. Use exponential backoff with jitter: start at 1 second, double on each retry, cap at 60 seconds, add ±10% random jitter. The <code>x-ratelimit-remaining</code> header tells you tokens remaining in the current window.</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-python" data-lang="python"><span style="display:flex;"><span><span style="color:#f92672">import</span> time<span style="color:#f92672">,</span> random
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">def</span> <span style="color:#a6e22e">call_with_backoff</span>(client, <span style="color:#f92672">**</span>kwargs, max_retries<span style="color:#f92672">=</span><span style="color:#ae81ff">5</span>):
</span></span><span style="display:flex;"><span>    <span style="color:#66d9ef">for</span> attempt <span style="color:#f92672">in</span> range(max_retries):
</span></span><span style="display:flex;"><span>        <span style="color:#66d9ef">try</span>:
</span></span><span style="display:flex;"><span>            <span style="color:#66d9ef">return</span> client<span style="color:#f92672">.</span>chat<span style="color:#f92672">.</span>completions<span style="color:#f92672">.</span>create(<span style="color:#f92672">**</span>kwargs)
</span></span><span style="display:flex;"><span>        <span style="color:#66d9ef">except</span> <span style="color:#a6e22e">Exception</span> <span style="color:#66d9ef">as</span> e:
</span></span><span style="display:flex;"><span>            <span style="color:#66d9ef">if</span> <span style="color:#e6db74">&#34;429&#34;</span> <span style="color:#f92672">in</span> str(e) <span style="color:#f92672">and</span> attempt <span style="color:#f92672">&lt;</span> max_retries <span style="color:#f92672">-</span> <span style="color:#ae81ff">1</span>:
</span></span><span style="display:flex;"><span>                wait <span style="color:#f92672">=</span> min(<span style="color:#ae81ff">60</span>, (<span style="color:#ae81ff">2</span> <span style="color:#f92672">**</span> attempt)) <span style="color:#f92672">*</span> (<span style="color:#ae81ff">1</span> <span style="color:#f92672">+</span> random<span style="color:#f92672">.</span>uniform(<span style="color:#f92672">-</span><span style="color:#ae81ff">0.1</span>, <span style="color:#ae81ff">0.1</span>))
</span></span><span style="display:flex;"><span>                time<span style="color:#f92672">.</span>sleep(wait)
</span></span><span style="display:flex;"><span>            <span style="color:#66d9ef">else</span>:
</span></span><span style="display:flex;"><span>                <span style="color:#66d9ef">raise</span>
</span></span></code></pre></div><h2 id="self-hosting-llama-4-vllm-and-ollama-deployment-guide">Self-Hosting Llama 4: vLLM and Ollama Deployment Guide</h2>
<p>Self-hosting Llama 4 Scout requires a single H100 80GB or two A100 40GB GPUs at FP8 precision — a realistic setup for teams with GPU infrastructure. Maverick requires at least 48GB VRAM for FP8 inference, typically 2x H100 80GB for practical throughput. The recommended self-hosting path is vLLM, which supports Llama 4&rsquo;s MoE architecture natively as of vLLM 0.5.x.</p>
<p><strong>vLLM Scout deployment (single H100):</strong></p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-bash" data-lang="bash"><span style="display:flex;"><span>pip install vllm&gt;<span style="color:#f92672">=</span>0.5.0
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>python -m vllm.entrypoints.openai.api_server <span style="color:#ae81ff">\
</span></span></span><span style="display:flex;"><span><span style="color:#ae81ff"></span>  --model meta-llama/Llama-4-Scout-17B-16E-Instruct <span style="color:#ae81ff">\
</span></span></span><span style="display:flex;"><span><span style="color:#ae81ff"></span>  --dtype float8 <span style="color:#ae81ff">\
</span></span></span><span style="display:flex;"><span><span style="color:#ae81ff"></span>  --max-model-len <span style="color:#ae81ff">131072</span> <span style="color:#ae81ff">\
</span></span></span><span style="display:flex;"><span><span style="color:#ae81ff"></span>  --tensor-parallel-size <span style="color:#ae81ff">1</span> <span style="color:#ae81ff">\
</span></span></span><span style="display:flex;"><span><span style="color:#ae81ff"></span>  --port <span style="color:#ae81ff">8000</span>
</span></span></code></pre></div><p><strong>vLLM Maverick deployment (2x H100):</strong></p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-bash" data-lang="bash"><span style="display:flex;"><span>python -m vllm.entrypoints.openai.api_server <span style="color:#ae81ff">\
</span></span></span><span style="display:flex;"><span><span style="color:#ae81ff"></span>  --model meta-llama/Llama-4-Maverick-17B-128E-Instruct <span style="color:#ae81ff">\
</span></span></span><span style="display:flex;"><span><span style="color:#ae81ff"></span>  --dtype float8 <span style="color:#ae81ff">\
</span></span></span><span style="display:flex;"><span><span style="color:#ae81ff"></span>  --max-model-len <span style="color:#ae81ff">65536</span> <span style="color:#ae81ff">\
</span></span></span><span style="display:flex;"><span><span style="color:#ae81ff"></span>  --tensor-parallel-size <span style="color:#ae81ff">2</span> <span style="color:#ae81ff">\
</span></span></span><span style="display:flex;"><span><span style="color:#ae81ff"></span>  --enable-chunked-prefill <span style="color:#ae81ff">\
</span></span></span><span style="display:flex;"><span><span style="color:#ae81ff"></span>  --port <span style="color:#ae81ff">8000</span>
</span></span></code></pre></div><p>Enable <code>--enable-chunked-prefill</code> for Maverick to handle long-context prompts without OOM errors by breaking prefill into chunks. For Ollama users, Scout is available as <code>ollama pull llama4:scout</code> and runs on a single A100 40GB in Q4 quantization — context window is capped at 128K by default in Ollama&rsquo;s current config.</p>
<h2 id="safety-and-guardrails-what-developers-must-handle-themselves">Safety and Guardrails: What Developers Must Handle Themselves</h2>
<p>Llama 4 has no enforced output filtering — there is no automatic refusal layer in the model weights or in the API. This is a deliberate design choice from Meta that distinguishes Llama 4 from OpenAI&rsquo;s GPT-4o and Google&rsquo;s Gemini, both of which bake content policies into the inference pipeline. For developers building internal tools, research applications, or B2B products, this is a feature: the model will not refuse legitimate but edge-case requests. For consumer-facing applications — anything accessible by the general public, minors, or users who may produce harmful outputs — developers are legally and operationally responsible for implementing their own content moderation. Meta provides Llama Guard 3, a 1B-parameter safety classifier, as a companion model available through the same API. Call it as a post-processing step on any response you show to users. You can also use it as a pre-filter on user inputs. The Llama Guard 3 API call costs roughly $0.001 per check at Meta&rsquo;s pricing — a negligible addition that substantially reduces liability. Additionally, Llama 4&rsquo;s system prompt is fully controllable: you can define persona, restrictions, and output format through the <code>system</code> role in the messages array. A well-designed system prompt is your first line of defense; Llama Guard 3 is your second.</p>
<h2 id="llama-4-vs-gpt-4o-vs-gemini-when-to-choose-open-weight">Llama 4 vs GPT-4o vs Gemini: When to Choose Open-Weight</h2>
<p>Llama 4 Maverick outperforms GPT-4o on MMLU (91.8% vs 88.7%), HumanEval (91.5% vs 90.2%), and SWE-bench (74.2% vs 72.0%) at 10–15% of the API cost for high-volume workloads. The open-weight architecture means you can self-host for zero marginal cost at scale, audit the model weights, and fine-tune on proprietary data without data leaving your infrastructure. Choose <strong>Llama 4</strong> when: cost is a primary constraint, you need self-hosting for data privacy or compliance (HIPAA, GDPR, SOC 2), you want to fine-tune on domain-specific data, or you are building high-volume batch processing pipelines where per-token costs compound. Choose <strong>GPT-4o or Gemini</strong> when: you need guaranteed SLA with enterprise support contracts, you require built-in content moderation without building your own, you use OpenAI-specific features (Assistants API, persistent threads, Code Interpreter), or you need Google&rsquo;s native search integration via Gemini. The 6x provider price spread for Llama 4 Maverick means that picking the right provider matters as much as picking the right model. At 1M requests per month, routing Maverick to Together.ai instead of a premium provider saves approximately $150K annually.</p>
<h2 id="faq">FAQ</h2>
<p>The following questions cover the most common developer concerns when integrating Llama 4 Scout and Maverick into production applications: API access options, the Scout vs Maverick decision, OpenAI migration compatibility, self-hosting hardware requirements, and multimodal input support. Each answer draws on current provider documentation and benchmark data as of May 2026. If you are evaluating Llama 4 for a new project, read the Scout vs Maverick section above first — picking the right model tier has a larger impact on cost and quality than any other configuration decision. For production deployments, review the Safety and Guardrails section carefully: unlike GPT-4o, Llama 4 has no built-in output filtering, and that responsibility falls entirely on the developer. Llama 4 represents the most capable open-weight model available today at production quality, and this FAQ is designed to help developers get unblocked quickly.</p>
<h3 id="is-llama-4-free-to-use">Is Llama 4 free to use?</h3>
<p>Llama 4 Scout and Maverick are free to download from Meta&rsquo;s model hub (HuggingFace, llama.com) for self-hosting. API usage through Meta&rsquo;s Llama API and third-party providers is paid, with Scout starting at $0.08 per 1M input tokens — among the cheapest frontier-quality models available. Most providers offer a free tier with rate limits suitable for development and testing.</p>
<h3 id="what-is-the-difference-between-llama-4-scout-and-maverick">What is the difference between Llama 4 Scout and Maverick?</h3>
<p>Scout and Maverick share the same 17B active parameter architecture but differ in total capacity and context window. Scout has ~109B total parameters, 16 experts, and a 10M-token context window — optimized for long-document processing. Maverick has ~400B total parameters, 128 experts, and a 1M-token context window — optimized for reasoning, coding, and complex instruction-following. Maverick scores significantly higher on reasoning benchmarks; Scout&rsquo;s main advantage is the extreme context window and lower price.</p>
<h3 id="can-i-migrate-from-openai-gpt-4o-to-llama-4-without-rewriting-my-code">Can I migrate from OpenAI GPT-4o to Llama 4 without rewriting my code?</h3>
<p>Yes. Meta&rsquo;s Llama API and Groq both expose OpenAI-compatible endpoints. Change <code>base_url</code> to the provider&rsquo;s URL and update the <code>model</code> parameter. Tool calling, streaming, system prompts, and message format are identical. The one difference is that Llama 4 does not enforce content filtering — you will need to add your own moderation if your current code relies on OpenAI&rsquo;s built-in refusals.</p>
<h3 id="what-hardware-do-i-need-to-self-host-llama-4">What hardware do I need to self-host Llama 4?</h3>
<p>Scout at FP8 precision fits on a single H100 80GB GPU or two A100 40GB GPUs. Maverick requires at least 48GB VRAM, typically two H100 80GBs for practical throughput. Use vLLM 0.5.x for best performance; enable <code>--enable-chunked-prefill</code> for Maverick to avoid OOM on long prompts. In Q4 quantization via Ollama, Scout runs on a single A100 40GB but with a reduced context window.</p>
<h3 id="does-llama-4-support-multimodal-image-inputs">Does Llama 4 support multimodal (image) inputs?</h3>
<p>Yes. Both Scout and Maverick are natively multimodal and accept image inputs via the standard OpenAI multimodal message format (base64 or URL in the <code>content</code> array). There is no separate vision model — the same endpoint handles text and image inputs. Image support is available through Meta&rsquo;s Llama API and most major providers; verify with your specific provider as image support may lag slightly behind text-only on some platforms.</p>
]]></content:encoded></item></channel></rss>