<?xml version="1.0" encoding="utf-8" standalone="yes"?><rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom" xmlns:content="http://purl.org/rss/1.0/modules/content/"><channel><title>Linux Security on RockB</title><link>https://baeseokjae.github.io/tags/linux-security/</link><description>Recent content in Linux Security on RockB</description><image><title>RockB</title><url>https://baeseokjae.github.io/images/og-default.png</url><link>https://baeseokjae.github.io/images/og-default.png</link></image><generator>Hugo</generator><language>en-us</language><lastBuildDate>Mon, 27 Jul 2026 04:02:16 +0000</lastBuildDate><atom:link href="https://baeseokjae.github.io/tags/linux-security/index.xml" rel="self" type="application/rss+xml"/><item><title>Sandbox bwrap Nix: Lightweight Sandbox for AI Agents and Experiments</title><link>https://baeseokjae.github.io/posts/sandbox-bwrap-nix-agent-sandbox-2026/</link><pubDate>Mon, 27 Jul 2026 04:02:16 +0000</pubDate><guid>https://baeseokjae.github.io/posts/sandbox-bwrap-nix-agent-sandbox-2026/</guid><description>sandbox-bwrap-nix combines Bubblewrap and Nix to create a lightweight, auditable sandbox for AI coding agents that boots in under a second.</description><content:encoded><![CDATA[<h2 id="what-is-sandbox-bwrap-nix">What is sandbox-bwrap-nix?</h2>
<p>sandbox-bwrap-nix is a lightweight, open-source sandboxing solution that combines Bubblewrap (bwrap) with the Nix package manager to create isolated environments for AI coding agents and experimental software. It boots in under a second, requires no daemon or root privileges, and gives you fine-grained control over what an AI agent can see, write, and execute. By mounting the Nix store as read-only and providing an isolated home directory with its own process namespace, it delivers three layers of protection against credential leaks, file system tampering, and cross-process snooping.</p>
<h2 id="why-sandbox-ai-agents-the-security-problem">Why Sandbox AI Agents? The Security Problem</h2>
<p>AI coding agents like Claude Code, OpenCode, and Codex operate with significant system access. They read files, execute commands, install packages, and communicate with external APIs. This capability creates a serious security surface: an agent with access to your SSH keys, AWS credentials, GPG signing keys, or personal files could inadvertently expose them through a prompt injection attack, a malicious package, or simply a bug in the agent&rsquo;s tool-use logic.</p>
<p>Traditional approaches to this problem have been heavy-handed. Running each agent inside a full Docker container provides isolation, but at the cost of image management, daemon overhead, and startup times measured in seconds to minutes. Virtual machines are even heavier. The result is that many developers skip sandboxing entirely, relying on trust in the agent and hoping nothing goes wrong.</p>
<p>The security industry has recognized this gap. Claude Code&rsquo;s own sandbox has used Bubblewrap on Linux since October 2025, and dedicated tools like ai-jail (977 GitHub stars, 86 forks as of July 2026) have emerged specifically to address the AI agent sandboxing problem. The trend is clear: lightweight, auditable sandboxing is becoming a standard requirement for AI development workflows.</p>
<h2 id="how-bubblewrap-works-the-zero-sandbox-principle">How Bubblewrap Works: The Zero Sandbox Principle</h2>
<p>Bubblewrap, originally developed as part of the Flatpak project, operates on what security researchers call the &ldquo;zero sandbox&rdquo; principle. By default, a Bubblewrap sandbox has access to nothing. Every file, directory, network interface, and process is explicitly granted rather than implicitly available. This is the opposite of Docker&rsquo;s approach, where a container inherits most of the host environment unless you explicitly remove access.</p>
<p>The core mechanism is simple: Bubblewrap uses Linux kernel namespaces and bind mounts to construct a restricted view of the system. A typical invocation looks like this:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-bash" data-lang="bash"><span style="display:flex;"><span>bwrap <span style="color:#ae81ff">\
</span></span></span><span style="display:flex;"><span>  --ro-bind /nix/store /nix/store <span style="color:#ae81ff">\
</span></span></span><span style="display:flex;"><span>  --bind /path/to/project /path/to/project <span style="color:#ae81ff">\
</span></span></span><span style="display:flex;"><span>  --tmpfs /tmp <span style="color:#ae81ff">\
</span></span></span><span style="display:flex;"><span>  --tmpfs /home <span style="color:#ae81ff">\
</span></span></span><span style="display:flex;"><span>  --unshare-pid <span style="color:#ae81ff">\
</span></span></span><span style="display:flex;"><span>  --unshare-net <span style="color:#ae81ff">\
</span></span></span><span style="display:flex;"><span>  --clearenv <span style="color:#ae81ff">\
</span></span></span><span style="display:flex;"><span>  --setenv HOME /home/sandbox <span style="color:#ae81ff">\
</span></span></span><span style="display:flex;"><span>  --chdir /path/to/project <span style="color:#ae81ff">\
</span></span></span><span style="display:flex;"><span>  nix develop
</span></span></code></pre></div><p>Each flag is a deliberate grant of access. <code>--ro-bind /nix/store /nix/store</code> gives read-only access to the Nix store. <code>--tmpfs /tmp</code> creates an empty, ephemeral temp directory. <code>--unshare-pid</code> creates a separate process namespace so the agent cannot see sibling processes. <code>--clearenv</code> strips all environment variables, preventing credential leaks through environment inheritance.</p>
<p>This approach has a significant advantage: it is auditable. Every permission is visible in the command line or configuration file. There is no implicit trust, no default-allow policy, and no opaque configuration layers. As one security researcher noted, &ldquo;Bubblewrap does in 30 lines what Docker does in 30 layers.&rdquo;</p>
<h2 id="why-nix-reproducible-dev-shells-inside-the-sandbox">Why Nix? Reproducible Dev Shells Inside the Sandbox</h2>
<p>Nix brings a critical capability to the sandbox: reproducibility. A Nix flake or shell definition specifies every tool and dependency your AI agent needs — git, bun, uv, gnumake, Python, Node.js, or any other runtime — with exact versions and hash-verified downloads. When combined with Bubblewrap, the Nix store is mounted as read-only, guaranteeing that the agent cannot tamper with its own toolchain.</p>
<p>This combination solves a real problem. AI agents frequently need to install packages, run build tools, and execute scripts. Without Nix, you would need to either pre-install everything on the host (defeating isolation) or allow the agent to install packages inside the sandbox (creating reproducibility issues). With Nix, the environment is declared, version-pinned, and immutable during the agent&rsquo;s run.</p>
<p>The sandbox-bwrap-nix project takes this further by providing a pre-configured sandbox-home directory with a <code>.bashrc</code>, <code>nix.conf</code>, and OpenCode configuration. This means the agent starts in a fully functional development environment without any setup steps.</p>
<h2 id="step-by-step-setup-guide">Step-by-Step Setup Guide</h2>
<h3 id="prerequisites-installing-bubblewrap-and-nix">Prerequisites: Installing Bubblewrap and Nix</h3>
<p>Before you can use sandbox-bwrap-nix, you need Bubblewrap and Nix installed on your Linux system.</p>
<p><strong>Install Bubblewrap:</strong></p>
<p>On most Linux distributions, Bubblewrap is available in the package manager:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-bash" data-lang="bash"><span style="display:flex;"><span><span style="color:#75715e"># Debian/Ubuntu</span>
</span></span><span style="display:flex;"><span>sudo apt install bubblewrap
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#75715e"># Fedora</span>
</span></span><span style="display:flex;"><span>sudo dnf install bubblewrap
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#75715e"># Arch Linux</span>
</span></span><span style="display:flex;"><span>sudo pacman -S bubblewrap
</span></span></code></pre></div><p><strong>Install Nix:</strong></p>
<p>The recommended approach is the official single-user installer:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-bash" data-lang="bash"><span style="display:flex;"><span>sh &lt;<span style="color:#f92672">(</span>curl -L https://nixos.org/nix/install<span style="color:#f92672">)</span> --no-daemon
</span></span></code></pre></div><p>This installs Nix in single-user mode, which is sufficient for sandbox-bwrap-nix and avoids the complexity of a multi-user Nix daemon setup.</p>
<h3 id="cloning-and-configuring-sandbox-bwrap-nix">Cloning and Configuring sandbox-bwrap-nix</h3>
<p>The sandbox-bwrap-nix project was created on July 25, 2026, and is available on GitHub:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-bash" data-lang="bash"><span style="display:flex;"><span>git clone https://github.com/your-username/sandbox-bwrap-nix.git
</span></span><span style="display:flex;"><span>cd sandbox-bwrap-nix
</span></span></code></pre></div><p>The project provides a <code>flake.nix</code> that defines the development environment. You can customize it to include the tools your AI agent needs:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-nix" data-lang="nix"><span style="display:flex;"><span>{
</span></span><span style="display:flex;"><span>  inputs <span style="color:#f92672">=</span> {
</span></span><span style="display:flex;"><span>    nixpkgs<span style="color:#f92672">.</span>url <span style="color:#f92672">=</span> <span style="color:#e6db74">&#34;github:NixOS/nixpkgs/nixos-unstable&#34;</span>;
</span></span><span style="display:flex;"><span>    flake-utils<span style="color:#f92672">.</span>url <span style="color:#f92672">=</span> <span style="color:#e6db74">&#34;github:numtide/flake-utils&#34;</span>;
</span></span><span style="display:flex;"><span>  };
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>  outputs <span style="color:#f92672">=</span> { self<span style="color:#f92672">,</span> nixpkgs<span style="color:#f92672">,</span> flake-utils }:
</span></span><span style="display:flex;"><span>    flake-utils<span style="color:#f92672">.</span>lib<span style="color:#f92672">.</span>eachDefaultSystem (system:
</span></span><span style="display:flex;"><span>      <span style="color:#66d9ef">let</span> pkgs <span style="color:#f92672">=</span> nixpkgs<span style="color:#f92672">.</span>legacyPackages<span style="color:#f92672">.</span><span style="color:#e6db74">${</span>system<span style="color:#e6db74">}</span>; <span style="color:#66d9ef">in</span> {
</span></span><span style="display:flex;"><span>        devShells<span style="color:#f92672">.</span>default <span style="color:#f92672">=</span> pkgs<span style="color:#f92672">.</span>mkShell {
</span></span><span style="display:flex;"><span>          buildInputs <span style="color:#f92672">=</span> <span style="color:#66d9ef">with</span> pkgs; [
</span></span><span style="display:flex;"><span>            git
</span></span><span style="display:flex;"><span>            nodejs
</span></span><span style="display:flex;"><span>            python3
</span></span><span style="display:flex;"><span>            go
</span></span><span style="display:flex;"><span>            bun
</span></span><span style="display:flex;"><span>            uv
</span></span><span style="display:flex;"><span>            gnumake
</span></span><span style="display:flex;"><span>            curl
</span></span><span style="display:flex;"><span>            jq
</span></span><span style="display:flex;"><span>          ];
</span></span><span style="display:flex;"><span>        };
</span></span><span style="display:flex;"><span>      });
</span></span><span style="display:flex;"><span>}
</span></span></code></pre></div><h3 id="understanding-the-bwrap-flags">Understanding the bwrap Flags</h3>
<p>The sandbox-bwrap-nix wrapper script uses a carefully constructed set of Bubblewrap flags. Here is what each one does:</p>
<table>
	<thead>
			<tr>
					<th>Flag</th>
					<th>Purpose</th>
			</tr>
	</thead>
	<tbody>
			<tr>
					<td><code>--ro-bind /nix/store /nix/store</code></td>
					<td>Mounts the Nix store read-only so the agent cannot corrupt its toolchain</td>
			</tr>
			<tr>
					<td><code>--bind /path/to/project /path/to/project</code></td>
					<td>Gives the agent write access only to the project directory</td>
			</tr>
			<tr>
					<td><code>--tmpfs /tmp</code></td>
					<td>Creates an empty, ephemeral temp directory that disappears when the sandbox exits</td>
			</tr>
			<tr>
					<td><code>--tmpfs /home</code></td>
					<td>Provides a clean home directory with no access to your real home</td>
			</tr>
			<tr>
					<td><code>--unshare-pid</code></td>
					<td>Creates a separate PID namespace so the agent cannot see other processes</td>
			</tr>
			<tr>
					<td><code>--unshare-ipc</code></td>
					<td>Isolates IPC resources</td>
			</tr>
			<tr>
					<td><code>--unshare-uts</code></td>
					<td>Gives the sandbox its own hostname</td>
			</tr>
			<tr>
					<td><code>--clearenv</code></td>
					<td>Strips all inherited environment variables</td>
			</tr>
			<tr>
					<td><code>--setenv HOME /home/sandbox</code></td>
					<td>Sets a clean HOME path inside the sandbox</td>
			</tr>
			<tr>
					<td><code>--share-net</code></td>
					<td>Re-enables network access (needed for LLM API calls)</td>
			</tr>
	</tbody>
</table>
<p>The <code>--share-net</code> flag is particularly important for AI agents. Without network access, the agent cannot call LLM APIs, install packages, or fetch remote resources. By explicitly granting network access rather than inheriting it, you maintain the zero-sandbox principle while enabling the agent to function.</p>
<h3 id="customizing-your-sandbox-environment">Customizing Your Sandbox Environment</h3>
<p>The sandbox-bwrap-nix project includes a pre-configured sandbox-home directory. You can customize it by editing the files in <code>sandbox-home/</code>:</p>
<ul>
<li><strong><code>.bashrc</code></strong>: Set aliases, environment variables, and shell prompts that help you identify when you are inside the sandbox (the hostname is changed to &ldquo;bubblewrap&rdquo; for easy identification).</li>
<li><strong><code>nix.conf</code></strong>: Configure Nix settings like substituters, trusted users, and experimental features.</li>
<li><strong><code>opencode.json</code></strong>: Pre-configure OpenCode with your preferred model, temperature, and other settings.</li>
</ul>
<p>For credential management, the recommended approach is to inject only what the agent needs. If your agent needs to push to GitHub, create a read-only <code>.gitconfig</code> with a limited-scope token:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-bash" data-lang="bash"><span style="display:flex;"><span>--ro-bind /path/to/sandbox-home/.gitconfig /home/sandbox/.gitconfig
</span></span></code></pre></div><p>This gives the agent access to git configuration without exposing your personal <code>.gitconfig</code> or SSH keys.</p>
<h2 id="what-you-get-isolation-layers-explained">What You Get: Isolation Layers Explained</h2>
<h3 id="filesystem-isolation">Filesystem Isolation</h3>
<p>The filesystem isolation in sandbox-bwrap-nix operates at three levels. First, the Nix store is mounted read-only, preventing the agent from modifying its own toolchain. Second, the project directory is mounted with read-write access, allowing the agent to create and modify files as needed for its work. Third, everything else — your home directory, system configuration files, mounted drives — is invisible to the agent.</p>
<p>This means an agent compromised by a prompt injection attack cannot read your SSH keys from <code>~/.ssh/</code>, cannot modify your system configuration in <code>/etc/</code>, and cannot access files in other projects. The only files it can read are the Nix store (read-only) and the project directory. The only files it can write are in the project directory.</p>
<h3 id="process-and-pid-namespace-isolation">Process and PID Namespace Isolation</h3>
<p>With <code>--unshare-pid</code>, the sandbox gets its own process ID namespace. The agent running inside the sandbox sees only its own process tree. It cannot see your shell sessions, your other running applications, or sibling processes. This prevents a class of attacks where an agent reads process information to discover credentials, API keys, or sensitive data in command-line arguments.</p>
<p>The <code>--unshare-ipc</code> flag extends this isolation to inter-process communication resources, preventing the agent from interacting with host processes through shared memory or semaphores.</p>
<h3 id="environment-variable-cleanup">Environment Variable Cleanup</h3>
<p>Environment variables are a common vector for credential leaks. Many developers store API keys, database passwords, and cloud provider credentials in environment variables. When an AI agent inherits these variables, it has implicit access to all of them.</p>
<p>The <code>--clearenv</code> flag in sandbox-bwrap-nix strips every environment variable before the agent starts. You then explicitly set only the variables the agent needs:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-bash" data-lang="bash"><span style="display:flex;"><span>--setenv HOME /home/sandbox <span style="color:#ae81ff">\
</span></span></span><span style="display:flex;"><span>--setenv USER sandbox <span style="color:#ae81ff">\
</span></span></span><span style="display:flex;"><span>--setenv OPENAI_API_KEY your-key-here
</span></span></code></pre></div><p>This whitelist approach means you can audit exactly which credentials are exposed to the agent. No more wondering whether <code>AWS_SECRET_ACCESS_KEY</code> or <code>GITHUB_TOKEN</code> leaked into the agent&rsquo;s environment.</p>
<h2 id="real-world-use-cases">Real-World Use Cases</h2>
<h3 id="running-ai-coding-agents-opencode-claude-code-codex">Running AI Coding Agents (OpenCode, Claude Code, Codex)</h3>
<p>The primary use case for sandbox-bwrap-nix is running AI coding agents in isolation. When you launch an agent inside the sandbox, it has access to the project files, the Nix-provided toolchain, and network connectivity for API calls. It does not have access to your SSH keys, GPG keys, personal files, or other projects.</p>
<p>This is particularly valuable for organizations that use AI agents in CI/CD pipelines. A compromised agent in CI could push malicious code, exfiltrate secrets, or modify build artifacts. With sandbox-bwrap-nix, the blast radius is limited to the project directory and the ephemeral sandbox environment.</p>
<h3 id="experimenting-with-nix-flakes-safely">Experimenting with Nix Flakes Safely</h3>
<p>Nix flakes are powerful but can be dangerous. A flake from an untrusted source can execute arbitrary code during evaluation or build. Running <code>nix develop</code> inside a Bubblewrap sandbox contains this risk. If the flake tries to access your home directory, SSH keys, or other sensitive resources, it will fail because those resources are not mounted in the sandbox.</p>
<p>This makes sandbox-bwrap-nix an excellent tool for evaluating Nix flakes from new or untrusted sources. You can test the flake, inspect its outputs, and decide whether to trust it — all without exposing your system.</p>
<h3 id="testing-untrusted-code">Testing Untrusted Code</h3>
<p>Beyond AI agents, sandbox-bwrap-nix is useful for any scenario where you need to run untrusted code. The combination of filesystem isolation, process isolation, and environment cleanup provides a safe environment for:</p>
<ul>
<li>Testing packages from unfamiliar sources</li>
<li>Running code snippets from forums or AI assistants</li>
<li>Evaluating open-source projects before installation</li>
<li>Debugging build scripts that might have side effects</li>
</ul>
<p>The key advantage over Docker for these use cases is speed. A Bubblewrap sandbox starts in under a second, making it practical for ad-hoc testing where Docker&rsquo;s startup overhead would be prohibitive.</p>
<h2 id="comparison-with-alternatives">Comparison with Alternatives</h2>
<h3 id="sandbox-bwrap-nix-vs-docker">sandbox-bwrap-nix vs Docker</h3>
<table>
	<thead>
			<tr>
					<th>Feature</th>
					<th>sandbox-bwrap-nix</th>
					<th>Docker</th>
			</tr>
	</thead>
	<tbody>
			<tr>
					<td>Startup time</td>
					<td>Under 1 second</td>
					<td>2-30 seconds</td>
			</tr>
			<tr>
					<td>Daemon required</td>
					<td>No</td>
					<td>Yes (dockerd)</td>
			</tr>
			<tr>
					<td>Root required</td>
					<td>No</td>
					<td>Yes (daemon runs as root)</td>
			</tr>
			<tr>
					<td>Image management</td>
					<td>None (uses host Nix store)</td>
					<td>Pull, store, update images</td>
			</tr>
			<tr>
					<td>Configuration</td>
					<td>Single shell script or config file</td>
					<td>Dockerfile, compose, volumes</td>
			</tr>
			<tr>
					<td>Network isolation</td>
					<td>Explicit grant (&ndash;share-net)</td>
					<td>Default isolated, explicit expose</td>
			</tr>
			<tr>
					<td>Filesystem isolation</td>
					<td>Bind mounts</td>
					<td>Union filesystem + volumes</td>
			</tr>
			<tr>
					<td>Auditability</td>
					<td>Every flag visible in command</td>
					<td>Multiple configuration layers</td>
			</tr>
			<tr>
					<td>Cross-platform</td>
					<td>Linux only</td>
					<td>Linux, macOS, Windows</td>
			</tr>
	</tbody>
</table>
<p>Docker is the right choice when you need a full operating system environment, complete network isolation, or cross-platform consistency. sandbox-bwrap-nix is the right choice when you need speed, simplicity, and auditability for Linux-native workloads.</p>
<h3 id="sandbox-bwrap-nix-vs-ai-jail">sandbox-bwrap-nix vs ai-jail</h3>
<p>ai-jail is a Rust tool (~880KB, 4 dependencies, 124 tests) that wraps Bubblewrap with a TOML configuration file. It supports Linux (bwrap), macOS (sandbox-exec), and Windows via WSL2. With 977 GitHub stars and 86 forks, it is the most popular Bubblewrap-based AI sandbox tool.</p>
<table>
	<thead>
			<tr>
					<th>Feature</th>
					<th>sandbox-bwrap-nix</th>
					<th>ai-jail</th>
			</tr>
	</thead>
	<tbody>
			<tr>
					<td>Language</td>
					<td>Shell + Nix</td>
					<td>Rust</td>
			</tr>
			<tr>
					<td>Configuration</td>
					<td>Nix flake + shell script</td>
					<td>TOML file</td>
			</tr>
			<tr>
					<td>Nix integration</td>
					<td>Native (Nix store mounted)</td>
					<td>No native Nix support</td>
			</tr>
			<tr>
					<td>Cross-platform</td>
					<td>Linux only</td>
					<td>Linux, macOS, Windows</td>
			</tr>
			<tr>
					<td>Per-project config</td>
					<td>Manual</td>
					<td>Committable .ai-jail TOML</td>
			</tr>
			<tr>
					<td>Lockdown mode</td>
					<td>Manual flag selection</td>
					<td>Built-in lockdown mode</td>
			</tr>
			<tr>
					<td>Bootstrap</td>
					<td>Manual</td>
					<td>Generates permission configs</td>
			</tr>
			<tr>
					<td>GitHub stars</td>
					<td>New project</td>
					<td>977 stars</td>
			</tr>
	</tbody>
</table>
<p>ai-jail is more mature and feature-rich, particularly for cross-platform use. sandbox-bwrap-nix offers deeper Nix integration and a simpler, more transparent configuration model.</p>
<h3 id="sandbox-bwrap-nix-vs-claudes-built-in-sandbox">sandbox-bwrap-nix vs Claude&rsquo;s Built-in Sandbox</h3>
<p>Claude Code has included Bubblewrap-based sandboxing since October 2025. It provides automatic sandboxing for Claude Code sessions on Linux.</p>
<table>
	<thead>
			<tr>
					<th>Feature</th>
					<th>sandbox-bwrap-nix</th>
					<th>Claude&rsquo;s Built-in Sandbox</th>
			</tr>
	</thead>
	<tbody>
			<tr>
					<td>Agent support</td>
					<td>Any agent</td>
					<td>Claude Code only</td>
			</tr>
			<tr>
					<td>Escape hatch</td>
					<td>No escape</td>
					<td><code>dangerouslyDisableSandbox</code> option</td>
			</tr>
			<tr>
					<td>Configuration</td>
					<td>Full control</td>
					<td>Limited to Claude&rsquo;s defaults</td>
			</tr>
			<tr>
					<td>Nix integration</td>
					<td>Native</td>
					<td>None</td>
			</tr>
			<tr>
					<td>Transparency</td>
					<td>Open source</td>
					<td>Proprietary</td>
			</tr>
			<tr>
					<td>Customization</td>
					<td>Full</td>
					<td>Minimal</td>
			</tr>
	</tbody>
</table>
<p>The critical difference is the escape hatch. Claude&rsquo;s sandbox includes a <code>dangerouslyDisableSandbox</code> option that completely bypasses protection. sandbox-bwrap-nix has no such escape — if the sandbox is configured, the agent operates within it.</p>
<h2 id="limitations-and-considerations">Limitations and Considerations</h2>
<p>sandbox-bwrap-nix is not a complete security solution. It relies on Linux kernel namespaces, which have had vulnerabilities in the past. A kernel-level exploit could potentially break out of the sandbox. For workloads requiring the highest security, consider combining Bubblewrap with Landlock or seccomp for defense-in-depth.</p>
<p>The tool is Linux-only. macOS users need Docker or a Linux VM, and Windows users need WSL2. This is a fundamental limitation of Bubblewrap, which uses Linux-specific kernel features.</p>
<p>Network isolation is minimal. The <code>--share-net</code> flag gives the agent full network access, which is necessary for LLM API calls but means the agent can communicate with any internet service. For stricter isolation, you could use a network namespace with iptables rules, but this adds complexity.</p>
<p>Finally, sandbox-bwrap-nix is a very new project (created July 25, 2026). It has not been battle-tested at scale. While the underlying technologies (Bubblewrap and Nix) are mature, the integration is fresh and may have edge cases that have not yet been discovered.</p>
<h2 id="conclusion-is-sandbox-bwrap-nix-right-for-you">Conclusion: Is sandbox-bwrap-nix Right for You?</h2>
<p>sandbox-bwrap-nix is an excellent choice if you are a Linux user running AI coding agents and want a lightweight, auditable sandbox that boots in under a second. It is particularly well-suited for Nix users who want reproducible development environments combined with strong isolation guarantees.</p>
<p>If you need cross-platform support, consider ai-jail. If you need full operating system isolation, consider Docker. If you only use Claude Code and are satisfied with its built-in sandbox, you may not need sandbox-bwrap-nix at all.</p>
<p>But if you value the zero-sandbox principle — the idea that every permission should be explicit, auditable, and minimal — sandbox-bwrap-nix delivers a clean, transparent implementation that is hard to beat. In an era where AI agents are becoming more capable and more autonomous, lightweight sandboxing is not a nice-to-have. It is a fundamental security practice.</p>
<h2 id="frequently-asked-questions">Frequently Asked Questions</h2>
<p><strong>Q: Does sandbox-bwrap-nix require root access?</strong></p>
<p>A: No. Bubblewrap supports unprivileged user namespaces, which means sandbox-bwrap-nix runs entirely without root privileges. Your Linux kernel must support user namespaces, which is the default on most modern distributions.</p>
<p><strong>Q: Can the AI agent access my SSH keys or AWS credentials inside the sandbox?</strong></p>
<p>A: No. The sandbox uses <code>--clearenv</code> to strip all environment variables and <code>--tmpfs /home</code> to provide an isolated home directory. Your real SSH keys and credentials are not mounted into the sandbox unless you explicitly bind them.</p>
<p><strong>Q: How does sandbox-bwrap-nix compare to running an agent in a Docker container?</strong></p>
<p>A: sandbox-bwrap-nix starts in under a second, requires no daemon, and has no image management overhead. Docker provides stronger isolation (full OS environment, network namespaces) but at the cost of startup time, disk space, and operational complexity.</p>
<p><strong>Q: Can I use sandbox-bwrap-nix with any AI coding agent?</strong></p>
<p>A: Yes. The sandbox is agent-agnostic. It works with Claude Code, OpenCode, Codex, and any other agent that runs in a Linux shell. You just need to configure the Nix flake to include the tools your agent requires.</p>
<p><strong>Q: What happens to files created inside the sandbox?</strong></p>
<p>A: Files written to the project directory (which is bind-mounted from the host) persist after the sandbox exits. Files written to <code>/tmp</code> or <code>/home</code> inside the sandbox are ephemeral and disappear when the sandbox process terminates.</p>
]]></content:encoded></item><item><title>Building a Linux eBPF Security Agent: Architecture and Implementation Guide</title><link>https://baeseokjae.github.io/posts/linux-ebpf-security-agent-2026/</link><pubDate>Sun, 26 Jul 2026 10:02:14 +0000</pubDate><guid>https://baeseokjae.github.io/posts/linux-ebpf-security-agent-2026/</guid><description>A complete guide to building a Linux eBPF security agent — architecture, syscall monitoring, file integrity, process tracking, and production deployment patterns.</description><content:encoded><![CDATA[<h2 id="introduction--why-ebpf-for-runtime-security">Introduction — Why eBPF for Runtime Security?</h2>
<p>Traditional Linux security agents rely on kernel modules, auditd, or userspace polling — each with significant trade-offs. Kernel modules can crash the kernel and require maintenance across kernel versions. Auditd generates massive log volumes and imposes high CPU overhead. Userspace polling misses events between sampling intervals. The extended Berkeley Packet Filter (eBPF) solves all three problems by providing a safe, programmable, and high-performance sandbox within the Linux kernel. An eBPF-based security agent can inspect every system call, monitor file access, track process execution, and enforce network policy — all with deterministic overhead under one microsecond per event and without loading a single kernel module. This guide walks through the complete architecture of a Linux eBPF security agent, from kernel-level program design to production deployment.</p>
<h2 id="understanding-ebpf-architecture-for-security">Understanding eBPF Architecture for Security</h2>
<h3 id="ebpf-program-types-relevant-to-security-kprobe-tracepoint-lsm-xdp">eBPF Program Types Relevant to Security (kprobe, tracepoint, LSM, XDP)</h3>
<p>eBPF offers several program types, each suited to different security monitoring surfaces:</p>
<table>
	<thead>
			<tr>
					<th>Program Type</th>
					<th>Hook Location</th>
					<th>Security Use Case</th>
					<th>Overhead</th>
			</tr>
	</thead>
	<tbody>
			<tr>
					<td><strong>kprobe</strong></td>
					<td>Any kernel function</td>
					<td>Monitor specific syscalls (openat, execve, connect)</td>
					<td>&lt; 1 µs per event</td>
			</tr>
			<tr>
					<td><strong>tracepoint</strong></td>
					<td>Static kernel tracepoints</td>
					<td>Stable ABI for syscall entry/exit monitoring</td>
					<td>&lt; 0.5 µs per event</td>
			</tr>
			<tr>
					<td><strong>LSM (BPF LSM)</strong></td>
					<td>Linux Security Module hooks</td>
					<td>Mandatory access control, policy enforcement</td>
					<td>&lt; 1 µs per check</td>
			</tr>
			<tr>
					<td><strong>XDP</strong></td>
					<td>Network driver layer</td>
					<td>DDoS mitigation, packet filtering at line rate</td>
					<td>Near-zero (driver level)</td>
			</tr>
			<tr>
					<td><strong>socket filter</strong></td>
					<td>Socket layer</td>
					<td>Per-process network monitoring and filtering</td>
					<td>&lt; 0.5 µs per packet</td>
			</tr>
			<tr>
					<td><strong>cgroup skb</strong></td>
					<td>Cgroup network</td>
					<td>Container-level network policy enforcement</td>
					<td>&lt; 0.5 µs per packet</td>
			</tr>
	</tbody>
</table>
<p>For a comprehensive security agent, you will typically attach programs to multiple hook points simultaneously. The LSM program type, added in kernel 5.7, is particularly powerful — it allows eBPF to enforce mandatory access control policies that cannot be bypassed by userspace processes, even by root.</p>
<h3 id="the-ebpf-verifier-and-safety-guarantees">The eBPF Verifier and Safety Guarantees</h3>
<p>Every eBPF program passes through a strict verifier before it is loaded into the kernel. The verifier performs a static analysis that guarantees:</p>
<ul>
<li><strong>Termination</strong>: The program contains no infinite loops (or bounded loops with a verified upper bound since kernel 5.3).</li>
<li><strong>Memory safety</strong>: All memory accesses are bounds-checked. The verifier tracks register states across every possible execution path.</li>
<li><strong>No kernel crashes</strong>: The program cannot dereference arbitrary kernel memory or call unsafe kernel functions.</li>
<li><strong>Limited instruction count</strong>: Programs are capped at 1 million instructions (raised from 4096 in kernel 5.2).</li>
</ul>
<p>These guarantees mean that a bug in your eBPF security agent will cause the program to be rejected by the verifier or safely unloaded — it will never panic the kernel. This is the fundamental advantage over kernel modules, where a single null pointer dereference can take down the entire system.</p>
<h3 id="maps-ring-buffers-and-event-delivery">Maps, Ring Buffers, and Event Delivery</h3>
<p>eBPF programs run in kernel context and cannot write to files, make network calls, or allocate memory dynamically. Instead, they communicate with userspace through data structures called BPF maps:</p>
<ul>
<li><strong>Hash maps</strong>: Key-value stores for tracking state (e.g., process whitelists, connection tracking tables).</li>
<li><strong>Per-CPU arrays</strong>: Lock-free counters for performance metrics.</li>
<li><strong>Ring buffers (BPF ringbuf)</strong>: The recommended mechanism for streaming security events to userspace. Introduced in kernel 5.8, ringbuf is per-CPU and reservation-based, offering better performance and ordering guarantees than the older perf buffer.</li>
<li><strong>Perf event arrays (BPF perfbuf)</strong>: Legacy event delivery mechanism, still widely used but being replaced by ringbuf.</li>
</ul>
<p>A typical security agent pipeline works as follows: an eBPF program attached to a tracepoint or kprobe captures an event, enriches it with context (PID, UID, command name), writes it into a ring buffer, and a lightweight userspace daemon reads the buffer, enriches the event with container metadata, and forwards it to a processing pipeline.</p>
<h2 id="core-security-monitoring-capabilities-with-ebpf">Core Security Monitoring Capabilities with eBPF</h2>
<h3 id="system-call-monitoring-and-anomaly-detection">System Call Monitoring and Anomaly Detection</h3>
<p>Syscall monitoring is the foundation of runtime security. By attaching tracepoints to <code>sys_enter_*</code> and <code>sys_exit_*</code> hooks, an eBPF agent can observe every system call made by every process on the system. The key syscalls to monitor for security include:</p>
<ul>
<li><strong>execve/execveat</strong>: Process execution — detect unauthorized binaries, reverse shells, and container escapes.</li>
<li><strong>openat/creat</strong>: File access — monitor sensitive file reads and writes.</li>
<li><strong>connect/bind/sendto</strong>: Network connections — detect beaconing to command-and-control servers.</li>
<li><strong>ptrace</strong>: Debugging operations — detect process injection attempts.</li>
<li><strong>mount/umount</strong>: Filesystem operations — detect container escape via host mount.</li>
<li><strong>setuid/setgid/capset</strong>: Privilege escalation attempts.</li>
</ul>
<p>The eBPF program captures the syscall arguments, the calling process PID, and the UID, then emits this data to a ring buffer. The userspace agent applies rules — for example, alerting when a container process calls <code>mount</code> or when a non-root process calls <code>ptrace</code> on a process in a different container.</p>
<h3 id="file-integrity-monitoring-at-kernel-level">File Integrity Monitoring at Kernel Level</h3>
<p>Traditional file integrity monitoring (FIM) tools like AIDE or Tripwire scan files on a schedule, creating windows where changes go undetected. eBPF-based FIM is event-driven: every <code>openat</code>, <code>write</code>, <code>rename</code>, and <code>unlink</code> syscall is captured in real time.</p>
<p>A practical eBPF FIM program monitors <code>security_file_open</code> LSM hook or the <code>sys_enter_openat</code> tracepoint, checks the file path against a monitored path list (stored in a BPF hash map), and emits an event when a protected file is accessed. The userspace agent can then compute a hash of the modified file and compare it against a known-good baseline.</p>
<p>This approach detects ransomware encryption in real time — before the encryption completes. SentinelOne reports detecting and stopping ransomware in under one second using eBPF-based file monitoring (eBPF Foundation 2026 Report).</p>
<h3 id="process-execution-and-tree-tracking">Process Execution and Tree Tracking</h3>
<p>Tracking process ancestry is critical for detecting supply chain attacks and lateral movement. An eBPF agent can attach to <code>sys_enter_execve</code> and capture the full process lineage: parent PID, child PID, executable path, command-line arguments, and environment variables.</p>
<p>By maintaining a process tree in a BPF hash map, the agent can answer questions like:</p>
<ul>
<li>Did a web server spawn a shell? (Common webshell attack pattern)</li>
<li>Did a container process execute a binary that was not part of the original image?</li>
<li>Is there an unexpected parent-child relationship (e.g., a network daemon spawning a compiler)?</li>
</ul>
<p>The eBPF Foundation 2026 report notes that GitHub uses eBPF to block circular dependency deploy scripts — a process execution monitoring pattern that prevents build pipeline abuse.</p>
<h3 id="network-security-with-xdp-and-socket-filters">Network Security with XDP and Socket Filters</h3>
<p>XDP (eXpress Data Path) attaches eBPF programs to the earliest point in the network stack — the network driver&rsquo;s receive queue. This allows packet inspection and filtering at line rate, before any kernel networking overhead. Security use cases include:</p>
<ul>
<li><strong>DDoS mitigation</strong>: Cloudflare mitigates DDoS attacks peaking above 7 Tbps using eBPF-based XDP programs (eBPF Foundation 2026 Report).</li>
<li><strong>Container network isolation</strong>: Cilium uses XDP and tc (traffic control) eBPF programs to enforce Kubernetes network policies without iptables.</li>
<li><strong>DNS monitoring</strong>: Socket filter programs can inspect DNS queries per container, detecting data exfiltration via DNS tunneling.</li>
</ul>
<h2 id="building-a-custom-ebpf-security-agent">Building a Custom eBPF Security Agent</h2>
<h3 id="architecture-overview--collection-aggregation-enforcement">Architecture Overview — Collection, Aggregation, Enforcement</h3>
<p>A production eBPF security agent follows a three-layer architecture:</p>
<pre tabindex="0"><code>┌─────────────────────────────────────────────────────┐
│  Layer 3: Enforcement &amp; Response                     │
│  (LSM policies, process termination, network blocks) │
├─────────────────────────────────────────────────────┤
│  Layer 2: Aggregation &amp; Detection                    │
│  (Rule engine, anomaly detection, alert correlation) │
├─────────────────────────────────────────────────────┤
│  Layer 1: Collection                                 │
│  (eBPF programs on kprobes, tracepoints, XDP, LSM)   │
└─────────────────────────────────────────────────────┘
</code></pre><p><strong>Layer 1 (Collection)</strong> runs in kernel context. eBPF programs attached to security-relevant hooks capture raw events and emit them to ring buffers. This layer must be minimal — the verifier limits complexity, and every instruction adds overhead.</p>
<p><strong>Layer 2 (Aggregation)</strong> runs in userspace. A daemon reads events from ring buffers, enriches them with container metadata (from the container runtime socket or Kubernetes API), applies detection rules, and correlates events across time windows.</p>
<p><strong>Layer 3 (Enforcement)</strong> can be either kernel-side (via BPF LSM programs that deny operations) or userspace-side (via process termination, network block rules, or SIEM alerts).</p>
<h3 id="writing-ebpf-programs-with-libbpf-and-co-re">Writing eBPF Programs with libbpf and CO-RE</h3>
<p>The modern approach to writing eBPF programs uses <strong>libbpf</strong> with <strong>CO-RE (Compile Once, Run Everywhere)</strong>. CO-RE allows you to compile a single eBPF binary that runs across different kernel versions without modification. Here is a minimal example of an eBPF program that monitors <code>openat</code> syscalls:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-c" data-lang="c"><span style="display:flex;"><span><span style="color:#75715e">// openat_monitor.bpf.c
</span></span></span><span style="display:flex;"><span><span style="color:#75715e">#include</span> <span style="color:#75715e">&#34;vmlinux.h&#34;</span><span style="color:#75715e">
</span></span></span><span style="display:flex;"><span><span style="color:#75715e">#include</span> <span style="color:#75715e">&lt;bpf/bpf_helpers.h&gt;</span><span style="color:#75715e">
</span></span></span><span style="display:flex;"><span><span style="color:#75715e">#include</span> <span style="color:#75715e">&lt;bpf/bpf_tracing.h&gt;</span><span style="color:#75715e">
</span></span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">char</span> LICENSE[] <span style="color:#a6e22e">SEC</span>(<span style="color:#e6db74">&#34;license&#34;</span>) <span style="color:#f92672">=</span> <span style="color:#e6db74">&#34;GPL&#34;</span>;
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">struct</span> event {
</span></span><span style="display:flex;"><span>    u32 pid;
</span></span><span style="display:flex;"><span>    u32 uid;
</span></span><span style="display:flex;"><span>    <span style="color:#66d9ef">char</span> comm[<span style="color:#ae81ff">16</span>];
</span></span><span style="display:flex;"><span>    <span style="color:#66d9ef">char</span> filename[<span style="color:#ae81ff">256</span>];
</span></span><span style="display:flex;"><span>};
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">struct</span> {
</span></span><span style="display:flex;"><span>    <span style="color:#a6e22e">__uint</span>(type, BPF_MAP_TYPE_RINGBUF);
</span></span><span style="display:flex;"><span>    <span style="color:#a6e22e">__uint</span>(max_entries, <span style="color:#ae81ff">256</span> <span style="color:#f92672">*</span> <span style="color:#ae81ff">1024</span>);
</span></span><span style="display:flex;"><span>} events <span style="color:#a6e22e">SEC</span>(<span style="color:#e6db74">&#34;.maps&#34;</span>);
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#a6e22e">SEC</span>(<span style="color:#e6db74">&#34;tracepoint/syscalls/sys_enter_openat&#34;</span>)
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">int</span> <span style="color:#a6e22e">trace_openat</span>(<span style="color:#66d9ef">struct</span> trace_event_raw_sys_enter <span style="color:#f92672">*</span>ctx)
</span></span><span style="display:flex;"><span>{
</span></span><span style="display:flex;"><span>    <span style="color:#66d9ef">struct</span> event <span style="color:#f92672">*</span>e;
</span></span><span style="display:flex;"><span>    e <span style="color:#f92672">=</span> <span style="color:#a6e22e">bpf_ringbuf_reserve</span>(<span style="color:#f92672">&amp;</span>events, <span style="color:#66d9ef">sizeof</span>(<span style="color:#f92672">*</span>e), <span style="color:#ae81ff">0</span>);
</span></span><span style="display:flex;"><span>    <span style="color:#66d9ef">if</span> (<span style="color:#f92672">!</span>e)
</span></span><span style="display:flex;"><span>        <span style="color:#66d9ef">return</span> <span style="color:#ae81ff">0</span>;
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>    e<span style="color:#f92672">-&gt;</span>pid <span style="color:#f92672">=</span> <span style="color:#a6e22e">bpf_get_current_pid_tgid</span>() <span style="color:#f92672">&gt;&gt;</span> <span style="color:#ae81ff">32</span>;
</span></span><span style="display:flex;"><span>    e<span style="color:#f92672">-&gt;</span>uid <span style="color:#f92672">=</span> <span style="color:#a6e22e">bpf_get_current_uid_gid</span>() <span style="color:#f92672">&amp;</span> <span style="color:#ae81ff">0xFFFFFFFF</span>;
</span></span><span style="display:flex;"><span>    <span style="color:#a6e22e">bpf_get_current_comm</span>(<span style="color:#f92672">&amp;</span>e<span style="color:#f92672">-&gt;</span>comm, <span style="color:#66d9ef">sizeof</span>(e<span style="color:#f92672">-&gt;</span>comm));
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>    <span style="color:#66d9ef">const</span> <span style="color:#66d9ef">char</span> <span style="color:#f92672">*</span>filename <span style="color:#f92672">=</span> (<span style="color:#66d9ef">const</span> <span style="color:#66d9ef">char</span> <span style="color:#f92672">*</span>)ctx<span style="color:#f92672">-&gt;</span>args[<span style="color:#ae81ff">1</span>];
</span></span><span style="display:flex;"><span>    <span style="color:#a6e22e">bpf_probe_read_user_str</span>(e<span style="color:#f92672">-&gt;</span>filename, <span style="color:#66d9ef">sizeof</span>(e<span style="color:#f92672">-&gt;</span>filename), filename);
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>    <span style="color:#a6e22e">bpf_ringbuf_submit</span>(e, <span style="color:#ae81ff">0</span>);
</span></span><span style="display:flex;"><span>    <span style="color:#66d9ef">return</span> <span style="color:#ae81ff">0</span>;
</span></span><span style="display:flex;"><span>}
</span></span></code></pre></div><p>This program reserves space in a ring buffer, captures the PID, UID, process name, and filename, then submits the event. The <code>vmlinux.h</code> header (generated by <code>bpftool btf dump file /sys/kernel/btf/vmlinux format c</code>) provides type definitions for the running kernel, enabling CO-RE.</p>
<h3 id="userspace-agent-design-in-gopython">Userspace Agent Design in Go/Python</h3>
<p>The userspace agent loads the eBPF program, reads events from the ring buffer, and applies security rules. Go is the most common choice for production agents due to its static binaries, strong concurrency model, and the <code>cilium/ebpf</code> library. Python with <code>bcc</code> is better suited for prototyping.</p>
<p>A Go userspace agent skeleton:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-go" data-lang="go"><span style="display:flex;"><span><span style="color:#f92672">package</span> <span style="color:#a6e22e">main</span>
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#f92672">import</span> (
</span></span><span style="display:flex;"><span>    <span style="color:#e6db74">&#34;encoding/binary&#34;</span>
</span></span><span style="display:flex;"><span>    <span style="color:#e6db74">&#34;fmt&#34;</span>
</span></span><span style="display:flex;"><span>    <span style="color:#e6db74">&#34;os&#34;</span>
</span></span><span style="display:flex;"><span>    <span style="color:#e6db74">&#34;os/signal&#34;</span>
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>    <span style="color:#e6db74">&#34;github.com/cilium/ebpf&#34;</span>
</span></span><span style="display:flex;"><span>    <span style="color:#e6db74">&#34;github.com/cilium/ebpf/link&#34;</span>
</span></span><span style="display:flex;"><span>    <span style="color:#e6db74">&#34;github.com/cilium/ebpf/ringbuf&#34;</span>
</span></span><span style="display:flex;"><span>    <span style="color:#e6db74">&#34;github.com/cilium/ebpf/rlimit&#34;</span>
</span></span><span style="display:flex;"><span>)
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">func</span> <span style="color:#a6e22e">main</span>() {
</span></span><span style="display:flex;"><span>    <span style="color:#75715e">// Allow the current process to lock memory for eBPF maps</span>
</span></span><span style="display:flex;"><span>    <span style="color:#a6e22e">rlimit</span>.<span style="color:#a6e22e">RemoveMemlock</span>()
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>    <span style="color:#75715e">// Load the compiled eBPF object</span>
</span></span><span style="display:flex;"><span>    <span style="color:#a6e22e">objs</span> <span style="color:#f92672">:=</span> <span style="color:#a6e22e">openatMonitorObjects</span>{}
</span></span><span style="display:flex;"><span>    <span style="color:#66d9ef">if</span> <span style="color:#a6e22e">err</span> <span style="color:#f92672">:=</span> <span style="color:#a6e22e">loadOpenatMonitorObjects</span>(<span style="color:#f92672">&amp;</span><span style="color:#a6e22e">objs</span>, <span style="color:#66d9ef">nil</span>); <span style="color:#a6e22e">err</span> <span style="color:#f92672">!=</span> <span style="color:#66d9ef">nil</span> {
</span></span><span style="display:flex;"><span>        panic(<span style="color:#a6e22e">err</span>)
</span></span><span style="display:flex;"><span>    }
</span></span><span style="display:flex;"><span>    <span style="color:#66d9ef">defer</span> <span style="color:#a6e22e">objs</span>.<span style="color:#a6e22e">Close</span>()
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>    <span style="color:#75715e">// Attach to the tracepoint</span>
</span></span><span style="display:flex;"><span>    <span style="color:#a6e22e">tp</span>, <span style="color:#a6e22e">err</span> <span style="color:#f92672">:=</span> <span style="color:#a6e22e">link</span>.<span style="color:#a6e22e">Tracepoint</span>(<span style="color:#e6db74">&#34;syscalls&#34;</span>, <span style="color:#e6db74">&#34;sys_enter_openat&#34;</span>, <span style="color:#a6e22e">objs</span>.<span style="color:#a6e22e">TraceOpenat</span>, <span style="color:#66d9ef">nil</span>)
</span></span><span style="display:flex;"><span>    <span style="color:#66d9ef">if</span> <span style="color:#a6e22e">err</span> <span style="color:#f92672">!=</span> <span style="color:#66d9ef">nil</span> {
</span></span><span style="display:flex;"><span>        panic(<span style="color:#a6e22e">err</span>)
</span></span><span style="display:flex;"><span>    }
</span></span><span style="display:flex;"><span>    <span style="color:#66d9ef">defer</span> <span style="color:#a6e22e">tp</span>.<span style="color:#a6e22e">Close</span>()
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>    <span style="color:#75715e">// Open the ring buffer reader</span>
</span></span><span style="display:flex;"><span>    <span style="color:#a6e22e">rd</span>, <span style="color:#a6e22e">err</span> <span style="color:#f92672">:=</span> <span style="color:#a6e22e">ringbuf</span>.<span style="color:#a6e22e">NewReader</span>(<span style="color:#a6e22e">objs</span>.<span style="color:#a6e22e">Events</span>)
</span></span><span style="display:flex;"><span>    <span style="color:#66d9ef">if</span> <span style="color:#a6e22e">err</span> <span style="color:#f92672">!=</span> <span style="color:#66d9ef">nil</span> {
</span></span><span style="display:flex;"><span>        panic(<span style="color:#a6e22e">err</span>)
</span></span><span style="display:flex;"><span>    }
</span></span><span style="display:flex;"><span>    <span style="color:#66d9ef">defer</span> <span style="color:#a6e22e">rd</span>.<span style="color:#a6e22e">Close</span>()
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>    <span style="color:#75715e">// Read events in a goroutine</span>
</span></span><span style="display:flex;"><span>    <span style="color:#66d9ef">go</span> <span style="color:#66d9ef">func</span>() {
</span></span><span style="display:flex;"><span>        <span style="color:#66d9ef">for</span> {
</span></span><span style="display:flex;"><span>            <span style="color:#a6e22e">record</span>, <span style="color:#a6e22e">err</span> <span style="color:#f92672">:=</span> <span style="color:#a6e22e">rd</span>.<span style="color:#a6e22e">Read</span>()
</span></span><span style="display:flex;"><span>            <span style="color:#66d9ef">if</span> <span style="color:#a6e22e">err</span> <span style="color:#f92672">!=</span> <span style="color:#66d9ef">nil</span> {
</span></span><span style="display:flex;"><span>                <span style="color:#66d9ef">return</span>
</span></span><span style="display:flex;"><span>            }
</span></span><span style="display:flex;"><span>            <span style="color:#66d9ef">var</span> <span style="color:#a6e22e">e</span> <span style="color:#a6e22e">openatEvent</span>
</span></span><span style="display:flex;"><span>            <span style="color:#66d9ef">if</span> <span style="color:#a6e22e">err</span> <span style="color:#f92672">:=</span> <span style="color:#a6e22e">binary</span>.<span style="color:#a6e22e">Read</span>(<span style="color:#a6e22e">bytes</span>.<span style="color:#a6e22e">NewReader</span>(<span style="color:#a6e22e">record</span>.<span style="color:#a6e22e">RawSample</span>), <span style="color:#a6e22e">binary</span>.<span style="color:#a6e22e">LittleEndian</span>, <span style="color:#f92672">&amp;</span><span style="color:#a6e22e">e</span>); <span style="color:#a6e22e">err</span> <span style="color:#f92672">!=</span> <span style="color:#66d9ef">nil</span> {
</span></span><span style="display:flex;"><span>                <span style="color:#66d9ef">continue</span>
</span></span><span style="display:flex;"><span>            }
</span></span><span style="display:flex;"><span>            <span style="color:#a6e22e">fmt</span>.<span style="color:#a6e22e">Printf</span>(<span style="color:#e6db74">&#34;PID=%d UID=%d COMM=%s FILE=%s\n&#34;</span>,
</span></span><span style="display:flex;"><span>                <span style="color:#a6e22e">e</span>.<span style="color:#a6e22e">Pid</span>, <span style="color:#a6e22e">e</span>.<span style="color:#a6e22e">Uid</span>, string(<span style="color:#a6e22e">e</span>.<span style="color:#a6e22e">Comm</span>[:]), string(<span style="color:#a6e22e">e</span>.<span style="color:#a6e22e">Filename</span>[:]))
</span></span><span style="display:flex;"><span>        }
</span></span><span style="display:flex;"><span>    }()
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>    <span style="color:#75715e">// Wait for interrupt</span>
</span></span><span style="display:flex;"><span>    <span style="color:#a6e22e">sig</span> <span style="color:#f92672">:=</span> make(<span style="color:#66d9ef">chan</span> <span style="color:#a6e22e">os</span>.<span style="color:#a6e22e">Signal</span>, <span style="color:#ae81ff">1</span>)
</span></span><span style="display:flex;"><span>    <span style="color:#a6e22e">signal</span>.<span style="color:#a6e22e">Notify</span>(<span style="color:#a6e22e">sig</span>, <span style="color:#a6e22e">os</span>.<span style="color:#a6e22e">Interrupt</span>)
</span></span><span style="display:flex;"><span>    <span style="color:#f92672">&lt;-</span><span style="color:#a6e22e">sig</span>
</span></span><span style="display:flex;"><span>}
</span></span></code></pre></div><h3 id="event-processing-enrichment-and-alerting-pipeline">Event Processing, Enrichment, and Alerting Pipeline</h3>
<p>Raw eBPF events contain kernel-level data (PID, UID, syscall arguments). The userspace agent enriches these with higher-level context:</p>
<ol>
<li><strong>Container metadata</strong>: Map PID to container ID via <code>/proc/&lt;pid&gt;/cgroup</code>, then query the container runtime or Kubernetes API for image name, namespace, pod name, and labels.</li>
<li><strong>Process ancestry</strong>: Walk <code>/proc/&lt;pid&gt;/status</code> to build the full process tree.</li>
<li><strong>File path resolution</strong>: Resolve the monitored file path to determine if it falls under a protected directory.</li>
<li><strong>Reputation checks</strong>: Hash the executable and check against a known-good or known-bad database.</li>
</ol>
<p>The enriched event is then evaluated against a rule engine. Rules can be simple (e.g., &ldquo;alert if any process in the web namespace calls execve on /bin/bash&rdquo;) or complex (e.g., &ldquo;alert if a process opens more than 100 files in 10 seconds — possible ransomware behavior&rdquo;).</p>
<h2 id="existing-ebpf-security-tools-and-when-to-use-them">Existing eBPF Security Tools and When to Use Them</h2>
<h3 id="falco--runtime-threat-detection">Falco — Runtime Threat Detection</h3>
<p>Falco, originally developed by Sysdig and now a CNCF graduated project, is the most widely deployed eBPF-based runtime security tool. It uses a driver (kernel module, eBPF probe, or modern eBPF) to capture syscall events and applies a rich rule engine with hundreds of built-in rules.</p>
<table>
	<thead>
			<tr>
					<th>Feature</th>
					<th>Falco</th>
					<th>Tetragon</th>
					<th>Tracee</th>
			</tr>
	</thead>
	<tbody>
			<tr>
					<td><strong>Engine</strong></td>
					<td>Syscall-driven rule engine</td>
					<td>Cilium eBPF + LSM</td>
					<td>eBPF tracing + CO-RE</td>
			</tr>
			<tr>
					<td><strong>Deployment</strong></td>
					<td>DaemonSet or standalone</td>
					<td>DaemonSet (Cilium-integrated)</td>
					<td>DaemonSet or standalone</td>
			</tr>
			<tr>
					<td><strong>Enforcement</strong></td>
					<td>Alert only (no blocking)</td>
					<td>Block via LSM</td>
					<td>Alert only</td>
			</tr>
			<tr>
					<td><strong>Rule language</strong></td>
					<td>YAML rules (Falco rules)</td>
					<td>JSON/YAML tracing policies</td>
					<td>Go-based signatures</td>
			</tr>
			<tr>
					<td><strong>Kernel deps</strong></td>
					<td>Kernel 4.14+ (eBPF probe)</td>
					<td>Kernel 5.7+ (LSM required)</td>
					<td>Kernel 4.4+</td>
			</tr>
			<tr>
					<td><strong>CNCF status</strong></td>
					<td>Graduated</td>
					<td>Sandbox</td>
					<td>Sandbox</td>
			</tr>
	</tbody>
</table>
<p><strong>When to use Falco</strong>: You need a mature, battle-tested runtime security solution with extensive built-in rules and SIEM integration. Falco is the best choice for teams that want to start with runtime security without writing custom eBPF programs.</p>
<h3 id="tetragon--kernel-level-enforcement">Tetragon — Kernel-Level Enforcement</h3>
<p>Tetragon, part of the Cilium ecosystem, is unique among eBPF security tools because it can actively block operations using BPF LSM programs. While Falco only alerts, Tetragon can prevent a malicious syscall from completing.</p>
<p><strong>When to use Tetragon</strong>: You need active enforcement (blocking, not just alerting) and are already running Cilium in your Kubernetes cluster. Tetragon&rsquo;s LSM-based enforcement is the closest you can get to a kernel-level security agent without loading kernel modules.</p>
<h3 id="tracee--tracing-and-security-monitoring">Tracee — Tracing and Security Monitoring</h3>
<p>Tracee, developed by Aqua Security, focuses on deep event visibility. It uses CO-RE eBPF programs to capture a comprehensive set of events — syscalls, kernel function calls, and network events — and exports them in a structured format.</p>
<p><strong>When to use Tracee</strong>: You need deep forensic visibility for incident response or compliance auditing. Tracee&rsquo;s event coverage is broader than Falco&rsquo;s, making it suitable for post-incident analysis.</p>
<h3 id="cilium--network-security-and-observability">Cilium — Network Security and Observability</h3>
<p>Cilium has become the default CNI for many Kubernetes clusters (eBPF Foundation 2026 Report). While primarily a networking and observability tool, Cilium&rsquo;s network policies are enforced at the kernel level via eBPF, providing security isolation between pods without iptables overhead.</p>
<p><strong>When to use Cilium</strong>: You need Kubernetes network policy enforcement with eBPF performance. Cilium is the de facto standard for eBPF-based container networking and is recommended for any new Kubernetes cluster.</p>
<h2 id="production-deployment-patterns">Production Deployment Patterns</h2>
<h3 id="daemonset-vs-sidecar-deployment-models">DaemonSet vs Sidecar Deployment Models</h3>
<table>
	<thead>
			<tr>
					<th>Aspect</th>
					<th>DaemonSet</th>
					<th>Sidecar</th>
			</tr>
	</thead>
	<tbody>
			<tr>
					<td><strong>Coverage</strong></td>
					<td>One agent per node, monitors all containers</td>
					<td>One agent per pod</td>
			</tr>
			<tr>
					<td><strong>Resource usage</strong></td>
					<td>Lower (shared across pods)</td>
					<td>Higher (per-pod overhead)</td>
			</tr>
			<tr>
					<td><strong>Isolation</strong></td>
					<td>Agent runs outside container boundaries</td>
					<td>Agent runs in the same pod</td>
			</tr>
			<tr>
					<td><strong>Kernel access</strong></td>
					<td>Direct (runs on host)</td>
					<td>Requires host PID namespace</td>
			</tr>
			<tr>
					<td><strong>Update strategy</strong></td>
					<td>Rolling node update</td>
					<td>Per-pod update</td>
			</tr>
			<tr>
					<td><strong>Best for</strong></td>
					<td>Cluster-wide security monitoring</td>
					<td>High-security workloads needing per-pod isolation</td>
			</tr>
	</tbody>
</table>
<p>For most production deployments, a <strong>DaemonSet</strong> is the right choice. It provides node-wide coverage with lower resource overhead. Sidecar deployment is reserved for high-security environments where each workload must have its own dedicated security agent.</p>
<h3 id="performance-tuning-and-overhead-management">Performance Tuning and Overhead Management</h3>
<p>eBPF security agents are remarkably efficient, but poor configuration can still cause performance issues. Key tuning parameters:</p>
<ol>
<li><strong>Ring buffer size</strong>: Start with 256 KB per CPU and monitor drop counts. Increase if events are being dropped.</li>
<li><strong>Sampling rate</strong>: For high-frequency syscalls like <code>read</code> and <code>write</code>, consider sampling 1 in N events rather than capturing every call.</li>
<li><strong>Filter early</strong>: Apply filters in the eBPF program itself (e.g., only monitor specific PIDs or file paths) to avoid flooding the ring buffer with irrelevant events.</li>
<li><strong>Per-CPU data structures</strong>: Use per-CPU maps for counters to avoid lock contention.</li>
</ol>
<p>Real-world overhead numbers are compelling. Datadog cut CPU usage by 35% after switching to an eBPF-based connection tracker (eBPF Foundation 2026 Report). DoorDash saw 40% less memory usage and 98% fewer restarts after migrating to eBPF monitoring. Companies report up to 90% CPU reduction compared with legacy agents (martinuke0 eBPF Tracing Guide).</p>
<h3 id="integration-with-siem-and-alerting-systems">Integration with SIEM and Alerting Systems</h3>
<p>eBPF security events are most valuable when integrated into a broader security operations pipeline. Common integration patterns:</p>
<ul>
<li><strong>Fluentd/Fluent Bit</strong>: Ship eBPF events to Elasticsearch, Splunk, or Loki.</li>
<li><strong>Kafka/Pulsar</strong>: Buffer events for downstream processing by stream processors (Flink, Spark).</li>
<li><strong>Prometheus/Alertmanager</strong>: Export metrics (event counts, drop rates, alert counts) for monitoring and alerting.</li>
<li><strong>Webhook/Slack/PagerDuty</strong>: Direct alerting for critical security events.</li>
</ul>
<p>LinkedIn reduced Kafka log volume by 70% using an eBPF observability agent that filters and aggregates events before writing to the message bus (eBPF Foundation 2026 Report).</p>
<h2 id="real-world-case-studies-and-statistics">Real-World Case Studies and Statistics</h2>
<h3 id="netflix--network-defense-at-325m-subscribers">Netflix — Network Defense at 325M Subscribers</h3>
<p>Netflix uses eBPF for network defense across 325 million subscribers. Their eBPF-based security agent monitors network traffic at the XDP layer, detecting and blocking malicious traffic before it reaches application servers. The system processes millions of packets per second per node with minimal CPU overhead, enabling Netflix to maintain high streaming quality while protecting their infrastructure.</p>
<h3 id="doordash--40-memory-reduction-with-ebpf-monitoring">DoorDash — 40% Memory Reduction with eBPF Monitoring</h3>
<p>DoorDash migrated from a legacy monitoring agent to an eBPF-based solution and achieved a 40% reduction in memory usage and a 98% reduction in agent restarts. The legacy agent relied on periodic polling and kernel modules, which consumed significant resources and frequently crashed. The eBPF replacement uses event-driven monitoring with ring buffers, eliminating polling overhead entirely.</p>
<h3 id="sentinelone--sub-second-ransomware-detection">SentinelOne — Sub-Second Ransomware Detection</h3>
<p>SentinelOne&rsquo;s eBPF-based security agent detects and stops ransomware in under one second. By monitoring file write patterns at the kernel level, the agent can identify the characteristic behavior of ransomware — rapid, sequential file encryption — and terminate the process before significant damage occurs. This is impossible with traditional scheduled-scan FIM tools, which may have detection windows of minutes or hours.</p>
<h3 id="github--blocking-circular-dependency-deploys">GitHub — Blocking Circular Dependency Deploys</h3>
<p>GitHub uses eBPF to block circular dependency deploy scripts in their CI/CD pipeline. When a deployment triggers a script that would create a circular dependency (e.g., a build script that triggers another build of itself), the eBPF agent detects the pattern via process execution tracking and terminates the deployment. This prevents infinite build loops that could exhaust CI resources.</p>
<h2 id="challenges-and-limitations">Challenges and Limitations</h2>
<h3 id="kernel-version-requirements-and-feature-gaps">Kernel Version Requirements and Feature Gaps</h3>
<p>eBPF&rsquo;s capabilities are tightly coupled to kernel version. Key features and their minimum kernel versions:</p>
<table>
	<thead>
			<tr>
					<th>Feature</th>
					<th>Minimum Kernel</th>
					<th>Notes</th>
			</tr>
	</thead>
	<tbody>
			<tr>
					<td>Basic eBPF (kprobes, tracepoints)</td>
					<td>3.18</td>
					<td>Limited to 4096 instructions</td>
			</tr>
			<tr>
					<td>Bounded loops</td>
					<td>5.3</td>
					<td>Required for complex programs</td>
			</tr>
			<tr>
					<td>BPF ring buffer</td>
					<td>5.8</td>
					<td>Recommended for event delivery</td>
			</tr>
			<tr>
					<td>BPF LSM</td>
					<td>5.7</td>
					<td>Required for enforcement</td>
			</tr>
			<tr>
					<td>CO-RE (BTF support)</td>
					<td>5.2</td>
					<td>Required for portable programs</td>
			</tr>
			<tr>
					<td>BPF iterator</td>
					<td>5.8</td>
					<td>For kernel data structure iteration</td>
			</tr>
			<tr>
					<td>BPF sk_lookup</td>
					<td>5.9</td>
					<td>For socket-level security</td>
			</tr>
	</tbody>
</table>
<p>If your production environment includes kernels older than 5.2, you will need to distribute per-kernel eBPF binaries or use BCC&rsquo;s on-the-fly compilation approach, which requires LLVM/clang on each node.</p>
<h3 id="verifier-constraints-and-debugging-ebpf-programs">Verifier Constraints and Debugging eBPF Programs</h3>
<p>The eBPF verifier is strict by design, but this creates practical challenges:</p>
<ul>
<li><strong>Complexity limits</strong>: Programs with deep branching or large loops may be rejected. You may need to split logic across multiple eBPF programs.</li>
<li><strong>No floating point</strong>: eBPF programs cannot use floating-point arithmetic.</li>
<li><strong>Limited stack</strong>: The stack is capped at 512 bytes. Complex data structures must be stored in maps.</li>
<li><strong>Debugging difficulty</strong>: Failed verifier logs are cryptic. Tools like <code>bpftool prog tracelog</code> and <code>bpf_printk()</code> are essential for debugging.</li>
</ul>
<p>The recommended debugging workflow is: prototype with Python/bcc for rapid iteration, then port to C/libbpf for production.</p>
<h3 id="security-of-the-ebpf-system-itself">Security of the eBPF System Itself</h3>
<p>eBPF is a powerful kernel mechanism, and its security depends on proper configuration:</p>
<ul>
<li><strong>Root-only by default</strong>: Loading eBPF programs requires <code>CAP_BPF</code> or root. In container environments, the security agent must run with elevated privileges.</li>
<li><strong>Unprivileged eBPF</strong>: Some eBPF program types (socket filters, cgroup skb) can be loaded by unprivileged users. If not needed, disable with <code>kernel.unprivileged_bpf_disabled=1</code>.</li>
<li><strong>BPF LSM risks</strong>: A misconfigured LSM program can lock legitimate users out of the system. Always test LSM policies in enforcement-only mode before enabling blocking.</li>
</ul>
<h2 id="future-of-ebpf-security-agents">Future of eBPF Security Agents</h2>
<p>The eBPF ecosystem is evolving rapidly. Several developments will shape the next generation of security agents:</p>
<ol>
<li><strong>eBPF on Windows</strong>: Microsoft is porting eBPF to Windows, enabling cross-platform security agents with a unified programming model.</li>
<li><strong>Rust-based eBPF</strong>: The <code>aya</code> crate allows writing eBPF programs in Rust, bringing memory safety guarantees to the eBPF program itself.</li>
<li><strong>eBPF in the kernel mainline</strong>: More eBPF program types are being added to the mainline kernel, reducing the need for out-of-tree patches.</li>
<li><strong>AI-assisted detection</strong>: Combining eBPF event streams with machine learning models for anomaly detection, running in the userspace agent layer.</li>
<li><strong>Standardized security profiles</strong>: The eBPF Foundation is working on standardized security profiles that can be shared across tools, similar to seccomp profiles.</li>
</ol>
<p>The eBPF Foundation, backed by Google, Meta, Microsoft, and Netflix as a Linux Foundation project, ensures that eBPF will remain a core Linux technology with long-term support and investment.</p>
<h2 id="conclusion-and-getting-started-checklist">Conclusion and Getting Started Checklist</h2>
<p>Building a Linux eBPF security agent is a practical way to achieve kernel-level runtime security without the risks of kernel modules. The architecture is proven at scale — Netflix, Cloudflare, DoorDash, LinkedIn, and GitHub all run eBPF security agents in production with measurable performance improvements over legacy approaches.</p>
<p><strong>Getting started checklist:</strong></p>
<ul>
<li><input disabled="" type="checkbox"> Verify kernel support: <code>bpftool feature probe</code> on your target systems</li>
<li><input disabled="" type="checkbox"> Choose your approach: build custom (this guide) or deploy Falco/Tetragon/Tracee</li>
<li><input disabled="" type="checkbox"> Set up the development environment: libbpf, clang, bpftool, Go or Python</li>
<li><input disabled="" type="checkbox"> Write your first eBPF program: start with syscall monitoring (openat or execve)</li>
<li><input disabled="" type="checkbox"> Implement the userspace agent: ring buffer reader, event enrichment, rule engine</li>
<li><input disabled="" type="checkbox"> Deploy as a DaemonSet on Kubernetes or as a systemd service on bare metal</li>
<li><input disabled="" type="checkbox"> Tune performance: monitor drop counts, adjust ring buffer sizes, add early filters</li>
<li><input disabled="" type="checkbox"> Integrate with your SIEM: ship events to Elasticsearch, Splunk, or Kafka</li>
<li><input disabled="" type="checkbox"> Test with attack simulations: use tools like stratus-red-team or atomic-red-team</li>
</ul>
<p>The barrier to entry has never been lower. With CO-RE, libbpf, and the <code>cilium/ebpf</code> Go library, you can build a production-quality eBPF security agent in a matter of weeks — not months.</p>
<h2 id="faq">FAQ</h2>
<p><strong>Q: What is a Linux eBPF security agent?</strong>
A: A Linux eBPF security agent is a runtime security tool that uses eBPF (extended Berkeley Packet Filter) programs to monitor system calls, file access, process execution, and network traffic directly within the Linux kernel. It provides real-time threat detection with deterministic overhead under one microsecond per event, without loading kernel modules.</p>
<p><strong>Q: How does eBPF compare to traditional Linux security modules like AppArmor or SELinux?</strong>
A: eBPF complements rather than replaces AppArmor and SELinux. While MAC systems enforce static policies defined in profiles, eBPF enables dynamic, programmable security logic that can adapt to runtime conditions. BPF LSM (since kernel 5.7) allows eBPF programs to attach to LSM hooks, combining the safety of eBPF with the enforcement points of traditional LSM frameworks.</p>
<p><strong>Q: Can eBPF security agents block malicious operations or only detect them?</strong>
A: Both. Detection-only agents (Falco, Tracee) alert on suspicious activity. Enforcement-capable agents (Tetragon, custom BPF LSM programs) can actively block operations by returning an error code from the eBPF program, preventing the syscall from completing. Blocking requires kernel 5.7+ and BPF LSM support.</p>
<p><strong>Q: What is the performance impact of running an eBPF security agent in production?</strong>
A: The performance impact is minimal when properly configured. eBPF programs have deterministic overhead of under 1 microsecond per event in kernel context. Companies report 35-90% CPU reduction compared with legacy monitoring agents. Datadog cut CPU usage by 35%, DoorDash reduced memory by 40%, and Seznam.cz cut CPU usage by 72x after migrating to eBPF-based solutions.</p>
<p><strong>Q: What kernel version do I need to run an eBPF security agent?</strong>
A: Basic syscall monitoring works on kernel 3.18+. For CO-RE (portable eBPF binaries), you need kernel 5.2+. For BPF LSM enforcement, you need kernel 5.7+. For the recommended ring buffer event delivery, kernel 5.8+. Most modern enterprise Linux distributions (Ubuntu 22.04+, RHEL 9+, Debian 12+) ship with kernels that support all eBPF features needed for a security agent.</p>
]]></content:encoded></item></channel></rss>