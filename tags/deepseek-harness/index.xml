<?xml version="1.0" encoding="utf-8" standalone="yes"?><rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom" xmlns:content="http://purl.org/rss/1.0/modules/content/"><channel><title>DeepSeek Harness on RockB</title><link>https://baeseokjae.github.io/tags/deepseek-harness/</link><description>Recent content in DeepSeek Harness on RockB</description><image><title>RockB</title><url>https://baeseokjae.github.io/images/og-default.png</url><link>https://baeseokjae.github.io/images/og-default.png</link></image><generator>Hugo</generator><language>en-us</language><lastBuildDate>Fri, 04 Sep 2026 13:01:40 +0000</lastBuildDate><atom:link href="https://baeseokjae.github.io/tags/deepseek-harness/index.xml" rel="self" type="application/rss+xml"/><item><title>dsh trace compare 2026: Visualizing Agent Exploration Paths in DeepSeek Harness</title><link>https://baeseokjae.github.io/posts/dsh-trace-compare-agent-exploration-2026/</link><pubDate>Fri, 04 Sep 2026 13:01:40 +0000</pubDate><guid>https://baeseokjae.github.io/posts/dsh-trace-compare-agent-exploration-2026/</guid><description>Learn how to use dsh trace compare in DeepSeek Harness to visualize and compare agent exploration paths, read session events, and debug divergent runs.</description><content:encoded><![CDATA[<p>DeepSeek Harness (dsh) records every agent run as a durable series of session events — reasoning deltas, tool calls, tool results, step starts and ends, and turn boundaries — and <code>dsh trace compare</code> is the workflow for visualizing and contrasting those exploration paths side by side. Instead of staring at linear chat logs, you render agent execution as a graph (a DAG) where parallel tool calls fan out from a single reasoning step and results fan back in, letting you pinpoint exactly where two runs diverge, stall, retry, or branch. This guide walks you through the session-event format, the native trajectory view, community comparison tools, and the pitfalls to avoid.</p>
<h2 id="what-is-a-deepseek-harness-trace-and-why-compare-them">What Is a DeepSeek Harness Trace (and Why Compare Them)?</h2>
<p>A dsh trace is the complete, replayable record of everything an agent did during one run, captured as durable session events rather than a flat text transcript. DeepSeek Harness defines the trajectory as its flagship feature: reasoning deltas, text deltas, tool calls, tool results, step start/end, and turn start/end are all persisted as first-class events. This is what makes trace comparison meaningful — because the events are structured, you can align two (or a hundred) runs on the same timeline and ask concrete questions: did both agents pick the same first tool? Did one take a different branch after the third reasoning step? Where did one stall and retry while the other moved on?</p>
<p>Why compare at all? Agent runs are non-deterministic and often parallel; a single successful run tells you almost nothing about whether that path was lucky or repeatable. Comparing exploration paths across runs is how you find divergence, stalls, retries, and branches — the places where behavior degrades or improves. For anyone debugging prompt changes, model swaps, or tool-config edits, trace comparison is the difference between guessing and observing.</p>
<h2 id="understanding-the-dsh-session-event-format-durable-trace-source-of-truth">Understanding the DSH Session Event Format (durable trace source of truth)</h2>
<p>Every dsh run writes a <code>session.jsonl</code> file — one JSON object per line, in chronological order. This file is the single durable source of truth for comparison, because every surface (the Web UI, headless mode, the ACP/SDK, custom UIs, and Hooks) consumes the same event stream. The core event types are documented in the official DeepSeek Harness docs and reused by every community plugin:</p>
<table>
  <thead>
      <tr>
          <th>Event type</th>
          <th>What it records</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td><code>reasoning-delta</code></td>
          <td>Incremental tokens of the model&rsquo;s chain-of-thought</td>
      </tr>
      <tr>
          <td><code>text-delta</code></td>
          <td>Incremental user-visible text tokens</td>
      </tr>
      <tr>
          <td><code>tool/call</code></td>
          <td>An outgoing tool invocation with its arguments</td>
      </tr>
      <tr>
          <td><code>tool/result</code></td>
          <td>The result returned for a tool call</td>
      </tr>
      <tr>
          <td><code>step/start</code> / <code>step/end</code></td>
          <td>Beginning and end of a discrete agent step</td>
      </tr>
      <tr>
          <td><code>turn/start</code> / <code>turn/end</code></td>
          <td>Beginning and end of a full user↔agent turn</td>
      </tr>
  </tbody>
</table>
<p>Because these events are timestamped and ordered, two <code>session.jsonl</code> files can be diffed structurally. In a comparison workflow you typically export each run&rsquo;s JSONL, align on event type and order, then render differences as an overlay or side-by-side graph. If a tool call fanned out into five parallel calls in run A but only two in run B, that difference shows up immediately as a structural diff in the trace.</p>
<h2 id="reading-an-agent-exploration-path--node-types-and-flow">Reading an Agent Exploration Path — Node Types and Flow</h2>
<p>To compare paths, you first need to read a single path correctly. Community viewers (notably the DAG-style <code>dsh-agent-trace</code> plugin) model agent execution as a graph rather than a line. The core thesis is that &ldquo;agent execution is a graph, not a line&rdquo; — parallel tool calls fan out, and their results fan back in. The common node types you will encounter are:</p>
<ul>
<li><strong>Turn</strong> — a full user-to-agent exchange; the outer container.</li>
<li><strong>Step</strong> — a discrete reasoning-or-action unit inside a turn.</li>
<li><strong>Reasoning</strong> — the model&rsquo;s chain-of-thought for a step.</li>
<li><strong>Tool Call</strong> — an outgoing invocation (which may be one of N parallel calls).</li>
<li><strong>Result</strong> — the returned tool output that feeds back into the next reasoning step.</li>
<li><strong>Response</strong> — the final user-facing output of a turn.</li>
</ul>
<p>The flow is a DAG: a Reasoning node can spawn multiple parallel Tool Call nodes; each Tool Call produces a Result; Results converge back into the next Reasoning node. When you compare two paths, you are really comparing two DAGs. Look first for structural differences — different out-degree on a Reasoning node (fan-out), different branch after a Result, or an extra Retry cycle that one run suffered. Tools like MiniMap and click-to-expand in the DAG viewer help you zoom in on exactly the divergence region without drowning in a wide trace.</p>
<h2 id="native-trajectory-view-in-dsh-web-ui-default-1270013080">Native Trajectory View in DSH Web UI (default 127.0.0.1:3080)</h2>
<p>DeepSeek Harness ships a Web UI that includes a native trajectory renderer. Install and launch it with:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-bash" data-lang="bash"><span style="display:flex;"><span>npx @deepseek-ai/dsh web
</span></span></code></pre></div><p>The default Web UI is served at <code>http://127.0.0.1:3080</code>. Inside it, the trajectory view renders the same session events as a browsable path. You can step through reasoning deltas, expand or collapse tool calls, and see the parallel fan-out of multi-tool steps. The native view is the fastest way to eyeball a single run: it requires no additional plugins, uses the exact official <code>@deepseek-ai/dsh-session</code> data model, and respects the same interpretation rules as every other dsh surface.</p>
<p>For comparison specifically, the native view is best as a starting point — it is excellent for single-run inspection but does not itself lay two runs on top of each other. To compare, you export the underlying <code>session.jsonl</code> (or use Headless/CLI, covered below) and feed it into a comparison-aware tool. If you are only ever debugging one run, the native view may be all you need.</p>
<h2 id="comparing-two-runs-side-by-side--manual-and-plugin-workflows">Comparing Two Runs Side by Side — Manual and Plugin Workflows</h2>
<p>There are two broad ways to compare dsh traces: manual (diff the JSONL / run the same task twice) and plugin-assisted (use a viewer that renders both).</p>
<p><strong>Manual workflow:</strong> Run the same task twice against the model or config you want to compare. Export both <code>session.jsonl</code> files. Diff them line-by-line or, better, normalize each event to a compact structural tuple (<code>eventType + tool + step index</code>) and compare the sequences. The places where the sequences diverge are your exploration-path differences. Manual comparison is transparent and dependency-free, but tedious at scale and hard to do visually.</p>
<p><strong>Plugin workflow:</strong> Use a comparison-aware tool that renders both traces as graphs and lets you align them. The DAG-style viewer and the plain-language <code>/visual-trace</code> command are the common choices (details in the next section). These convert the raw event stream into a readable path or graph, highlight pending-review markers, and let you spot divergence visually instead of by eye-diffing JSON.</p>
<p>Whichever you choose, keep the comparison fair: identical model, identical temperature where relevant, identical tools and ordering, and identical task input. The only variable you want to change is the one you are actually testing.</p>
<h2 id="community-plugins-for-trace-visualization-and-comparison">Community Plugins for Trace Visualization and Comparison</h2>
<p>Several community plugins extend dsh trace work beyond the native view. Each takes a slightly different stance on visualization, comparison, and sharing.</p>
<p><strong>dsh-agent-trace</strong> (goldgish) — the graph-centric DAG viewer. Built on react-flow with dagre auto-layout, it subscribes to durable session events and renders an interactive DAG inside the chat stream, with parallel tool-call fan-out and fan-in. Features include click-to-expand steps, a MiniMap, streaming nodes, and JSON export. Best when you want to think of exploration as a graph.</p>
<p><strong>dsh-visual-trace</strong> (wikiiizhao) — a cross-surface plain-language trajectory reviewer. It applies the same trace-interpretation rules across Web, headless, ACP/SDK, custom UI, and Hooks. Commands include <code>/visual-trace</code>, <code>/visual-trace markdown</code>, and <code>/visual-trace json</code>, with emoji node types (user/model/tool/system) and pending-review markers. Requires DeepSeek Harness 0.1.0-rc.6 and Node.js <code>^22.19.0 || &gt;=24.0.0</code>. Best for turning traces into readable, reviewable summaries.</p>
<p><strong>dsh-trace-viewer</strong> (li-zhixin) — an offline browser viewer for <code>session.jsonl</code>. You drop a file in and nothing is uploaded; it is a static client-only app deployed on Cloudflare Pages, built on <code>@deepseek-ai/dsh-session</code>, with a live demo at dsh-trace.lizhixin.top. Best for private, local trace inspection.</p>
<p><strong>dsh-trace-narrator</strong> (xiangyun0519) — turns trajectory logs into structured reports (summary, postmortem, tutorial, debug, executive) with five built-in schemas, trilingual output (CN/EN/JP), and redaction on by default. Positioned as the &ldquo;shareable, reusable, teachable&rdquo; layer on top of dsh trajectories. Best when you need to hand a run to someone else as a report.</p>
<h2 id="tool-by-tool-comparison-table">Tool-by-Tool Comparison Table</h2>
<table>
  <thead>
      <tr>
          <th>Capability</th>
          <th>Native DSH Web UI</th>
          <th>dsh-agent-trace</th>
          <th>dsh-visual-trace</th>
          <th>dsh-trace-viewer</th>
          <th>dsh-trace-narrator</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>Primary view</td>
          <td>Trajectory path</td>
          <td>Interactive DAG</td>
          <td>Plain-language path</td>
          <td>Offline browser viewer</td>
          <td>Structured report</td>
      </tr>
      <tr>
          <td>Parallel tool fan-out</td>
          <td>Yes</td>
          <td>Yes (core thesis)</td>
          <td>Yes</td>
          <td>Yes</td>
          <td>Summarized</td>
      </tr>
      <tr>
          <td>Side-by-side run compare</td>
          <td>Manual</td>
          <td>Manual + visual</td>
          <td>Command-based</td>
          <td>Manual</td>
          <td>Report-level</td>
      </tr>
      <tr>
          <td>Rendering model</td>
          <td>Web renderer</td>
          <td>react-flow + dagre</td>
          <td>Text/emoji</td>
          <td>Static client app</td>
          <td>Documents</td>
      </tr>
      <tr>
          <td>Data leaves your machine</td>
          <td>No</td>
          <td>No</td>
          <td>No</td>
          <td>No (client-only)</td>
          <td>No (redaction on)</td>
      </tr>
      <tr>
          <td>Best for</td>
          <td>Single-run inspection</td>
          <td>Graph thinking</td>
          <td>Reviewable summaries</td>
          <td>Offline privacy</td>
          <td>Shareable reports</td>
      </tr>
  </tbody>
</table>
<h2 id="headless-and-cli-trace-workflows-for-comparing-runs-at-scale">Headless and CLI Trace Workflows for Comparing Runs at Scale</h2>
<p>When &ldquo;compare two runs&rdquo; becomes &ldquo;compare fifty runs,&rdquo; the Web UI and manual diffing stop scaling. dsh exposes headless modes and ACP/SDK access that let you run tasks programmatically and collect <code>session.jsonl</code> per run without a browser. The comparison pattern at scale is:</p>
<ol>
<li>Script the runs: invoke dsh headless for each configuration you want to compare, capturing one <code>session.jsonl</code> per run.</li>
<li>Normalize the events into a common comparison schema (event type, tool name, step index, timing).</li>
<li>Aggregate and diff programmatically — compute per-run metrics like step count, tool-call count, retry count, and divergence points.</li>
<li>Render only the interesting differences (via <code>/visual-trace json</code> or a custom emitter) instead of every full trace.</li>
</ol>
<p>The CLI/<code>/visual-trace</code> command path is especially useful here because it emits structured output (<code>markdown</code> or <code>json</code>) that a script can consume. This turns trace comparison into a repeatable, automated quality gate on your prompt or tooling changes, rather than a manual eyeball exercise.</p>
<h2 id="privacy-redaction-and-sharing-trace-reports">Privacy, Redaction, and Sharing Trace Reports</h2>
<p>Traces frequently contain sensitive material — user prompts, retrieved documents, API inputs. Before you share or compare traces across a team, decide what can leave the machine. The community has built this in: <code>dsh-trace-viewer</code> is client-only (a dropped file is never uploaded), and <code>dsh-trace-narrator</code> enables redaction by default and keeps data local while producing shareable reports. The native view and DAG viewer also run locally. The practical rule: keep raw <code>session.jsonl</code> private, and share only redacted, summarized, or narrated derivatives. When comparing runs in a review, strip tool arguments and document contents first, then compare the structural path — you can usually diagnose divergence without exposing the underlying payloads.</p>
<h2 id="common-pitfalls-when-comparing-agent-exploration-paths">Common Pitfalls When Comparing Agent Exploration Paths</h2>
<ul>
<li><strong>Comparing apples to oranges.</strong> Different models, temperatures, tool sets, or task inputs make the comparison meaningless. Change only the one variable under test.</li>
<li><strong>Relying on a single run.</strong> Agent behavior is stochastic; one divergent path may be noise. Run each configuration several times before concluding.</li>
<li><strong>Treating the chat log as the source of truth.</strong> Chat text hides parallel tool calls and reasoning deltas. Compare the structured <code>session.jsonl</code>, not the transcript.</li>
<li><strong>Ignoring event ordering.</strong> Reasoning, tool calls, and results interleave; a reordering that looks like a divergence in raw text is often just different interleaving of the same events.</li>
<li><strong>Forgetting pending-review markers.</strong> Some tools flag steps awaiting review; reading those as finalized divergence will mislead you.</li>
<li><strong>Skipping redaction before sharing.</strong> Leaking tool arguments or document contents in a &ldquo;shareable&rdquo; trace defeats the purpose of safe review.</li>
</ul>
<h2 id="faq--dsh-trace-compare">FAQ — dsh trace compare</h2>
<p><strong>What is dsh trace compare?</strong>
dsh trace compare is the workflow of visualizing and contrasting DeepSeek Harness agent runs using their durable session-event traces, so you can see where exploration paths diverge, stall, retry, or branch.</p>
<p><strong>How do I export a dsh trace to compare runs?</strong>
Each run writes a <code>session.jsonl</code> file containing ordered events (reasoning-delta, tool/call, tool/result, step/start/end, turn/start/end). Export that file from each run and diff or render them side by side.</p>
<p><strong>Which tool is best for comparing dsh traces?</strong>
It depends on your goal: dsh-agent-trace for graph/DAG thinking, dsh-visual-trace for readable reviewable paths and CLI output, dsh-trace-viewer for offline privacy, and dsh-trace-narrator for shareable structured reports. The native Web UI is best for single-run inspection.</p>
<p><strong>Does dsh trace comparison work headless or at scale?</strong>
Yes. dsh&rsquo;s headless and ACP/SDK modes let you script many runs, capture one <code>session.jsonl</code> per run, normalize the events, and diff them programmatically — ideal for automated quality gates.</p>
<p><strong>Are dsh traces private?</strong>
Raw traces generally stay on your machine. dsh-trace-viewer is client-only (nothing uploaded), and dsh-trace-narrator enables redaction by default. Share only redacted or summarized derivatives.</p>
<p>&lt;｜DSML｜tool_calls&gt;
&lt;｜DSML｜invoke name=&ldquo;terminal&rdquo;&gt;
&lt;｜DSML｜parameter name=&ldquo;command&rdquo; string=&ldquo;true&rdquo;&gt;python3 -c &quot;
import re
content = open(&rsquo;/home/ubuntu/blog/content/posts/dsh-trace-compare-agent-exploration-2026.md&rsquo;).read()
korean = re.findall(r&rsquo;[\uAC00-\uD7A3]&rsquo;, content)
if korean:
print(f&rsquo;LANGUAGE ERROR: {len(korean)} Korean characters found.&rsquo;)
import sys; sys.exit(1)
else:
print(&lsquo;Language check passed: English only.&rsquo;)
&quot;
echo &ldquo;&mdash;WORD COUNT&mdash;&rdquo;
wc -w /home/ubuntu/blog/content/posts/dsh-trace-compare-agent-exploration-2026.md</p>
]]></content:encoded></item><item><title>DeepSeek Harness Desktop App 2026: A Local AI Workspace for DSH Sessions</title><link>https://baeseokjae.github.io/posts/deepseek-harness-desktop-app-2026/</link><pubDate>Wed, 02 Sep 2026 19:01:19 +0000</pubDate><guid>https://baeseokjae.github.io/posts/deepseek-harness-desktop-app-2026/</guid><description>Run DeepSeek Harness (dsh) in a native desktop app with zero setup — no Node, pnpm, or Docker. Compare the top 2026 wrappers and pick the right local AI workspace.</description><content:encoded><![CDATA[<p>A DeepSeek Harness desktop app gives you a local AI workspace for DSH sessions without touching Node.js, pnpm, or Docker. The leading Tauri-based wrapper ships a ~5MB installer for Windows, macOS, and Linux, bundles the latest Harness core, and keeps your API key and sessions on your own machine. This guide reviews the top desktop apps in 2026 and helps you pick the right one.</p>
<h2 id="what-is-deepseek-harness-and-why-a-desktop-app">What Is DeepSeek Harness and Why a Desktop App?</h2>
<p>DeepSeek Harness (dsh) is DeepSeek&rsquo;s open-source agent harness where &ldquo;everything is a plugin.&rdquo; Every capability — models, tools, skills, sessions, sandboxes, storage, loops, scheduling, and even the UI — is a swappable plugin built on the Cordis kernel, which manages plugin mounting, unmounting, and dependencies. The main repository has grown to <strong>209,475 stars and 24,455 forks as of 2026-09-02</strong>, making it one of the most-watched agent frameworks in the ecosystem.</p>
<p>The harness itself is a developer preview (0.1.0-rc.5), and compatibility-breaking changes are expected as the framework iterates. Running it from source means cloning the repo, installing a Node toolchain, and wiring up pnpm and Docker — a real barrier for anyone who just wants to try an AI coding agent.</p>
<p>That is exactly the gap desktop apps fill. A desktop wrapper turns the harness into a double-click application: you download an installer, launch a native window, and get a chat interface, file read/write, command execution, a terminal, and an approval flow — all without a single command-line setup step. For DSH sessions specifically, a native window makes the trajectory view, resume, fork, and replay features feel like a real product rather than a developer tool.</p>
<h2 id="the-zero-setup-local-workspace-no-node-no-pnpm-no-docker">The Zero-Setup Local Workspace (no Node, no pnpm, no Docker)</h2>
<p>The killer differentiator of the desktop approach is zero setup. The most popular wrapper, <strong>hairyf/deepseek-harness-desktop</strong>, is a Tauri-based application written in Rust. Tauri bundles the web UI into a small native binary, which is why the installer is only about <strong>5MB</strong> — a fraction of the size of an Electron app.</p>
<p>The workflow is deliberately simple:</p>
<ol>
<li>Download the installer for your platform.</li>
<li>Install and launch the app.</li>
<li>Enter your API key in the settings dialog.</li>
<li>Start a session.</li>
</ol>
<p>There is no Node.js, no pnpm, no Docker, and no source checkout. The app also handles core updates in-app: it syncs the latest upstream Harness version and can manage multiple core versions side by side, so you are not stuck on an old release when the framework ships a breaking change.</p>
<table>
  <thead>
      <tr>
          <th>Setup step</th>
          <th>Running dsh from source</th>
          <th>Desktop app (Tauri)</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>Install Node.js</td>
          <td>Required</td>
          <td>Not needed</td>
      </tr>
      <tr>
          <td>Install pnpm</td>
          <td>Required</td>
          <td>Not needed</td>
      </tr>
      <tr>
          <td>Install Docker</td>
          <td>Required</td>
          <td>Not needed</td>
      </tr>
      <tr>
          <td>Clone the repo</td>
          <td>Required</td>
          <td>Not needed</td>
      </tr>
      <tr>
          <td>Installer size</td>
          <td>N/A (source)</td>
          <td>~5MB</td>
      </tr>
      <tr>
          <td>Core updates</td>
          <td>Manual git pull</td>
          <td>In-app sync</td>
      </tr>
      <tr>
          <td>First launch to session</td>
          <td>30+ minutes</td>
          <td>Under 5 minutes</td>
      </tr>
  </tbody>
</table>
<h2 id="top-desktop-apps-for-deepseek-harness-in-2026">Top Desktop Apps for DeepSeek Harness in 2026</h2>
<p>Three community projects dominate the desktop space, each with a different platform focus and philosophy. Here is how they compare.</p>
<table>
  <thead>
      <tr>
          <th>App</th>
          <th>Platform</th>
          <th>Stack</th>
          <th>Stars</th>
          <th>Best for</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>DeepSeek Harness Desktop (hairyf)</td>
          <td>Windows, macOS, Linux</td>
          <td>Tauri (Rust)</td>
          <td>1,581</td>
          <td>All-platform zero-setup users</td>
      </tr>
      <tr>
          <td>Open-DeepSeek-Harness-Desktop</td>
          <td>macOS only</td>
          <td>TypeScript</td>
          <td>5</td>
          <td>Native macOS power users</td>
      </tr>
      <tr>
          <td>DeepSeekGUI</td>
          <td>Windows</td>
          <td>TypeScript</td>
          <td>27</td>
          <td>Windows users wanting a workbench</td>
      </tr>
  </tbody>
</table>
<h3 id="deepseek-harness-desktop-tauri--the-all-platform-pick">DeepSeek Harness Desktop (Tauri) — the all-platform pick</h3>
<p>This is the most mature and widely adopted wrapper. With <strong>1,581 stars, 102 forks, and roughly 1,163 release downloads</strong>, it is the default choice for most users. Key features:</p>
<ul>
<li><strong>Cross-platform</strong>: Windows, macOS, and Linux installers.</li>
<li><strong>~5MB installer</strong>: tiny footprint thanks to Tauri.</li>
<li><strong>In-app core update</strong>: syncs the latest upstream Harness version and manages multiple core versions.</li>
<li><strong>One config dialog</strong>: Debug, Profiles, Plugins, and Core settings in a single place, with a bilingual (zh/en) UI and dark mode.</li>
<li><strong>Preset plugins bundled</strong>: ships with a useful plugin set out of the box.</li>
<li><strong>MIT license</strong>: latest release is v0.11.0-beta.3, bundling dsh 0.1.2-alpha.</li>
</ul>
<h3 id="open-deepseek-harness-desktop--the-macos-native-shell">Open-DeepSeek-Harness-Desktop — the macOS native shell</h3>
<p>This project positions itself as a Codex / Claude Code desktop counterpart for macOS. It wraps the harness Web UI in a native window and adds macOS-specific extras: a menu bar integration, Dock badge and notifications, a <code>dsh-desktop://</code> deep link, and auto-update.</p>
<p>Two things set it apart. First, it is <strong>local-first with no account</strong>: the API key lives in <code>~/.dsh/.credentials.yaml</code>, and sessions stay on your machine. Second, it does <strong>not</strong> inherit <code>DEEPSEEK_API_KEY</code> from your shell environment — the key is read only from the in-app Settings → Models dialog, which is a deliberate privacy choice.</p>
<p>It also includes a <strong>plugin orchestration canvas</strong>: you pick nodes (skills, tools, event hooks), wire them into flows, and compile them into reusable agent presets. The project follows upstream rather than forking — dsh is used as a dependency with a pinned <code>@deepseek-ai/dsh</code> version. It is an early developer preview with 5 stars, so expect rough edges.</p>
<h3 id="deepseekgui--the-windows-workbench">DeepSeekGUI — the Windows workbench</h3>
<p>DeepSeekGUI is a Windows desktop client for DeepSeek Harness. V1 wraps the official Web UI, while a V2 independent workbench is in active development. With 27 stars, it is the least mature of the three, but it is the dedicated option if you are Windows-only and want a project focused specifically on that platform.</p>
<h2 id="local-first-privacy-where-your-api-key-and-sessions-live">Local-First Privacy: Where Your API Key and Sessions Live</h2>
<p>A major reason to use a desktop app is privacy. Because the harness runs locally, your API key and session data never leave your machine except for the API calls you explicitly make to the model provider.</p>
<p>In Open-DeepSeek-Harness-Desktop, the key is stored in <code>~/.dsh/.credentials.yaml</code> and is read only from the in-app Settings → Models dialog — it deliberately does not read <code>DEEPSEEK_API_KEY</code> from the shell environment. Sessions stay on the local filesystem, and there is no cloud account to sign up for.</p>
<p>This local-first model matters for anyone working with sensitive code or proprietary data. You get the full power of an AI coding agent without sending your repository or conversation history to a third-party SaaS platform. The trade-off is that you manage your own API key and billing directly with the model provider.</p>
<h2 id="managing-dsh-sessions-in-a-native-window-trajectory-resume-fork-replay">Managing DSH Sessions in a Native Window (trajectory, resume, fork, replay)</h2>
<p>The harness&rsquo;s session model is one of its strongest features, and a desktop window makes it genuinely usable. Every run is traceable: an append-only session log records system prompts, reasoning, tool calls, and subagent scheduling. The trajectory view lets you inspect records by source, and you can resume, fork, search, and replay on the same event stream.</p>
<p>In a native window, these capabilities become first-class interactions:</p>
<ul>
<li><strong>Trajectory view</strong>: see exactly what the agent did, step by step.</li>
<li><strong>Resume</strong>: pick up a session where it left off.</li>
<li><strong>Fork</strong>: branch a session into a new direction without losing the original.</li>
<li><strong>Replay</strong>: re-run a session on the same event stream to reproduce or debug.</li>
</ul>
<p>For long-running agent work, the desktop app also surfaces approvals in a native window, so you can review and approve tool calls without juggling a terminal and a browser tab.</p>
<h2 id="the-plugin-orchestration-canvas-composing-skills-and-tools-visually">The Plugin Orchestration Canvas: Composing Skills and Tools Visually</h2>
<p>One of the most interesting desktop-specific features is the plugin orchestration canvas in Open-DeepSeek-Harness-Desktop. Instead of editing configuration files, you pick nodes — skills, tools, and event hooks — and wire them into visual flows. The result compiles into a reusable agent preset.</p>
<p>This is a meaningful UX improvement over the harness&rsquo;s conceptual model, where &ldquo;skills, commands, and MCP connections are all just code that calls <code>ctx</code>.&rdquo; A canvas turns that code-first abstraction into something you can see and manipulate. For teams that want to standardize agent behavior, a visual preset is far easier to share and review than a block of plugin code.</p>
<h2 id="how-to-choose-the-right-desktop-app-for-your-workflow">How to Choose the Right Desktop App for Your Workflow</h2>
<p>Your choice comes down to platform and maturity:</p>
<ul>
<li><strong>Cross-platform and zero-setup</strong>: choose <strong>DeepSeek Harness Desktop (Tauri)</strong>. It is the most mature, supports all three OSes, and has the smallest installer.</li>
<li><strong>macOS native with deep-link and notifications</strong>: choose <strong>Open-DeepSeek-Harness-Desktop</strong> if you want native macOS integration and a plugin canvas, and you can tolerate an early preview.</li>
<li><strong>Windows-only workbench</strong>: choose <strong>DeepSeekGUI</strong> if you are Windows-only and want a project focused on that platform.</li>
</ul>
<p>If you are unsure, start with the Tauri app. It has the largest user base, the most releases, and the broadest platform support, which makes it the safest default.</p>
<h2 id="getting-started-install-configure-and-run-your-first-session">Getting Started: Install, Configure, and Run Your First Session</h2>
<p>Here is a minimal path to your first DSH session in a desktop app:</p>
<ol>
<li><strong>Download</strong> the installer for your OS from the project&rsquo;s releases page.</li>
<li><strong>Install</strong> and launch the app.</li>
<li><strong>Open Settings</strong> and enter your API key (in Open-DeepSeek-Harness-Desktop, use Settings → Models).</li>
<li><strong>Start a session</strong> and run your first prompt.</li>
<li><strong>Update the core</strong> in-app when a new Harness version is available.</li>
</ol>
<p>Because the app manages core versions for you, you can stay current with the fast-moving developer preview without manual git operations.</p>
<h2 id="faq">FAQ</h2>
<p><strong>Is a DeepSeek Harness desktop app free?</strong>
Yes. All three wrappers covered here are open source — DeepSeek Harness Desktop (Tauri) is MIT-licensed, and the others are open-source projects. You only pay for the model API usage you generate.</p>
<p><strong>Do I need Node.js, pnpm, or Docker to use a desktop app?</strong>
No. That is the main point of the desktop approach. The Tauri app bundles everything into a ~5MB installer, so you download, install, and run without any toolchain setup.</p>
<p><strong>Where does my API key and session data live?</strong>
Locally on your machine. In Open-DeepSeek-Harness-Desktop, the key is stored in <code>~/.dsh/.credentials.yaml</code> and sessions stay on your local filesystem. There is no cloud account required.</p>
<p><strong>Which desktop app supports Windows, macOS, and Linux?</strong>
DeepSeek Harness Desktop (Tauri) by hairyf supports all three platforms. Open-DeepSeek-Harness-Desktop is macOS-only, and DeepSeekGUI is Windows-only.</p>
<p><strong>Is DeepSeek Harness stable enough for production use?</strong>
The harness is still a developer preview (0.1.0-rc.5), and compatibility-breaking changes are expected. Desktop apps help by managing core versions in-app, but you should treat it as a fast-moving tool rather than a stable production dependency.</p>
]]></content:encoded></item><item><title>Rapid-MLX DSH Provider: Native Apple Silicon Inference for the DeepSeek Harness</title><link>https://baeseokjae.github.io/posts/rapid-mlx-dsh-provider/</link><pubDate>Sun, 30 Aug 2026 13:04:27 +0000</pubDate><guid>https://baeseokjae.github.io/posts/rapid-mlx-dsh-provider/</guid><description>Connect the DeepSeek Harness to Rapid-MLX on Apple Silicon with a native dsh provider plugin that auto-reads model facts, fixes reasoning truth, and does memory-aware compaction.</description><content:encoded><![CDATA[<p>The Rapid-MLX DSH Provider is a native npm plugin for the DeepSeek Harness (dsh) that lets the agent read its model configuration straight from a running Rapid-MLX server&rsquo;s <code>/v1/models</code> endpoint instead of hand-written <code>settings.yaml</code>. Instead of copying context windows, reasoning efforts, and <code>max_model_len</code> values by hand — and re-copying them every time you switch models — the provider serves those facts directly from Rapid-MLX&rsquo;s JSON schema, keeps reasoning control truthful, and compacts the context at the real memory limit of your Apple Silicon machine.</p>
<h2 id="what-is-the-rapid-mlx-dsh-provider-and-why-does-deepseek-harness-need-it">What is the Rapid-MLX DSH Provider and why does DeepSeek Harness need it</h2>
<p>DeepSeek Harness is a 204K-star LLM-agent harness built on a strict philosophy: &ldquo;Everything is a Plugin.&rdquo; That philosophy is what makes a separate provider necessary — and useful. When you want to run a local model inside dsh, you do not patch the harness. You write a conformant <code>LlmAdapter</code> plugin that tells dsh the name of the model, the size of its effective context, and how to route reasoning and tool calls.</p>
<p>The Rapid-MLX DSH Provider (<code>@raullenchai/dsh-provider</code>) is exactly that adapter, written natively for <a href="https://github.com/raullenchai/Rapid-MLX">Rapid-MLX</a>, the 3,584-star &ldquo;fastest local AI engine for Apple Silicon.&rdquo; The pairing removes the most error-prone step in local-agent setup: hand-maintaining per-model config. Without the provider, every model swap means opening <code>settings.yaml</code> and hand-writing <code>contextWindow</code>, <code>reasoningEfforts</code>, and <code>max_model_len</code>. With it, dsh asks the server what model facts are real, and the server answers from a single source of truth.</p>
<h2 id="before-you-begin-requirements">Before you begin: requirements</h2>
<p>The provider is small, but the surrounding stack is specific. Before you install anything, confirm you have each of these:</p>
<ul>
<li><strong>Node.js &gt;= 22.15.</strong> This is not optional. dsh imports Node&rsquo;s Zstd stream API without declaring it in its own dependency tree, so an older Node runtime will fail at import time rather than at boot.</li>
<li><strong>A running Rapid-MLX server.</strong> The provider reads model facts from the server&rsquo;s <code>/v1/models</code> endpoint, so Rapid-MLX must be up and listening before dsh starts.</li>
<li><strong>Apple Silicon.</strong> Rapid-MLX is an Apple-metal inference engine, so an M-series Mac (M1, M2, M3, M4 — including Ultra variants) is implied. The upstream verification ran on an M3 Ultra.</li>
<li><strong>A model already pulled into Rapid-MLX.</strong> Context and reasoning facts are per-model, so at least one model must be stored locally before the first provider run.</li>
</ul>
<p>These four are the whole setup surface. There is no separate compiler, no build step, and no additional Python environment, which distinguishes this provider from the MLX-LM-based alternative covered later in this guide.</p>
<h2 id="install-the-provider-plugin">Install the provider plugin</h2>
<p>You can install the provider either from npm or directly from source, and neither path involves a build step.</p>
<p><strong>From npm</strong> — the published package is at version <code>0.3.0</code> as of 2026-08-30:</p>



<div class="goat svg-container ">
  
    <svg
      xmlns="http://www.w3.org/2000/svg"
      font-family="Menlo,Lucida Console,monospace"
      
        viewBox="0 0 304 25"
      >
      <g transform='translate(8,16)'>
<text text-anchor='middle' x='0' y='4' fill='currentColor' style='font-size:1em'>n</text>
<text text-anchor='middle' x='8' y='4' fill='currentColor' style='font-size:1em'>p</text>
<text text-anchor='middle' x='16' y='4' fill='currentColor' style='font-size:1em'>m</text>
<text text-anchor='middle' x='32' y='4' fill='currentColor' style='font-size:1em'>i</text>
<text text-anchor='middle' x='40' y='4' fill='currentColor' style='font-size:1em'>n</text>
<text text-anchor='middle' x='48' y='4' fill='currentColor' style='font-size:1em'>s</text>
<text text-anchor='middle' x='56' y='4' fill='currentColor' style='font-size:1em'>t</text>
<text text-anchor='middle' x='64' y='4' fill='currentColor' style='font-size:1em'>a</text>
<text text-anchor='middle' x='72' y='4' fill='currentColor' style='font-size:1em'>l</text>
<text text-anchor='middle' x='80' y='4' fill='currentColor' style='font-size:1em'>l</text>
<text text-anchor='middle' x='96' y='4' fill='currentColor' style='font-size:1em'>@</text>
<text text-anchor='middle' x='104' y='4' fill='currentColor' style='font-size:1em'>r</text>
<text text-anchor='middle' x='112' y='4' fill='currentColor' style='font-size:1em'>a</text>
<text text-anchor='middle' x='120' y='4' fill='currentColor' style='font-size:1em'>u</text>
<text text-anchor='middle' x='128' y='4' fill='currentColor' style='font-size:1em'>l</text>
<text text-anchor='middle' x='136' y='4' fill='currentColor' style='font-size:1em'>l</text>
<text text-anchor='middle' x='144' y='4' fill='currentColor' style='font-size:1em'>e</text>
<text text-anchor='middle' x='152' y='4' fill='currentColor' style='font-size:1em'>n</text>
<text text-anchor='middle' x='160' y='4' fill='currentColor' style='font-size:1em'>c</text>
<text text-anchor='middle' x='168' y='4' fill='currentColor' style='font-size:1em'>h</text>
<text text-anchor='middle' x='176' y='4' fill='currentColor' style='font-size:1em'>a</text>
<text text-anchor='middle' x='184' y='4' fill='currentColor' style='font-size:1em'>i</text>
<text text-anchor='middle' x='192' y='4' fill='currentColor' style='font-size:1em'>/</text>
<text text-anchor='middle' x='200' y='4' fill='currentColor' style='font-size:1em'>d</text>
<text text-anchor='middle' x='208' y='4' fill='currentColor' style='font-size:1em'>s</text>
<text text-anchor='middle' x='216' y='4' fill='currentColor' style='font-size:1em'>h</text>
<text text-anchor='middle' x='224' y='4' fill='currentColor' style='font-size:1em'>-</text>
<text text-anchor='middle' x='232' y='4' fill='currentColor' style='font-size:1em'>p</text>
<text text-anchor='middle' x='240' y='4' fill='currentColor' style='font-size:1em'>r</text>
<text text-anchor='middle' x='248' y='4' fill='currentColor' style='font-size:1em'>o</text>
<text text-anchor='middle' x='256' y='4' fill='currentColor' style='font-size:1em'>v</text>
<text text-anchor='middle' x='264' y='4' fill='currentColor' style='font-size:1em'>i</text>
<text text-anchor='middle' x='272' y='4' fill='currentColor' style='font-size:1em'>d</text>
<text text-anchor='middle' x='280' y='4' fill='currentColor' style='font-size:1em'>e</text>
<text text-anchor='middle' x='288' y='4' fill='currentColor' style='font-size:1em'>r</text>
</g>

    </svg>
  
</div>
<p><strong>From source</strong> — clone the repository and install:</p>



<div class="goat svg-container ">
  
    <svg
      xmlns="http://www.w3.org/2000/svg"
      font-family="Menlo,Lucida Console,monospace"
      
        viewBox="0 0 512 57"
      >
      <g transform='translate(8,16)'>
<text text-anchor='middle' x='0' y='4' fill='currentColor' style='font-size:1em'>g</text>
<text text-anchor='middle' x='0' y='20' fill='currentColor' style='font-size:1em'>c</text>
<text text-anchor='middle' x='0' y='36' fill='currentColor' style='font-size:1em'>n</text>
<text text-anchor='middle' x='8' y='4' fill='currentColor' style='font-size:1em'>i</text>
<text text-anchor='middle' x='8' y='20' fill='currentColor' style='font-size:1em'>d</text>
<text text-anchor='middle' x='8' y='36' fill='currentColor' style='font-size:1em'>p</text>
<text text-anchor='middle' x='16' y='4' fill='currentColor' style='font-size:1em'>t</text>
<text text-anchor='middle' x='16' y='36' fill='currentColor' style='font-size:1em'>m</text>
<text text-anchor='middle' x='24' y='20' fill='currentColor' style='font-size:1em'>r</text>
<text text-anchor='middle' x='32' y='4' fill='currentColor' style='font-size:1em'>c</text>
<text text-anchor='middle' x='32' y='20' fill='currentColor' style='font-size:1em'>a</text>
<text text-anchor='middle' x='32' y='36' fill='currentColor' style='font-size:1em'>i</text>
<text text-anchor='middle' x='40' y='4' fill='currentColor' style='font-size:1em'>l</text>
<text text-anchor='middle' x='40' y='20' fill='currentColor' style='font-size:1em'>p</text>
<text text-anchor='middle' x='40' y='36' fill='currentColor' style='font-size:1em'>n</text>
<text text-anchor='middle' x='48' y='4' fill='currentColor' style='font-size:1em'>o</text>
<text text-anchor='middle' x='48' y='20' fill='currentColor' style='font-size:1em'>i</text>
<text text-anchor='middle' x='48' y='36' fill='currentColor' style='font-size:1em'>s</text>
<text text-anchor='middle' x='56' y='4' fill='currentColor' style='font-size:1em'>n</text>
<text text-anchor='middle' x='56' y='20' fill='currentColor' style='font-size:1em'>d</text>
<text text-anchor='middle' x='56' y='36' fill='currentColor' style='font-size:1em'>t</text>
<text text-anchor='middle' x='64' y='4' fill='currentColor' style='font-size:1em'>e</text>
<text text-anchor='middle' x='64' y='20' fill='currentColor' style='font-size:1em'>-</text>
<text text-anchor='middle' x='64' y='36' fill='currentColor' style='font-size:1em'>a</text>
<text text-anchor='middle' x='72' y='20' fill='currentColor' style='font-size:1em'>m</text>
<text text-anchor='middle' x='72' y='36' fill='currentColor' style='font-size:1em'>l</text>
<text text-anchor='middle' x='80' y='4' fill='currentColor' style='font-size:1em'>h</text>
<text text-anchor='middle' x='80' y='20' fill='currentColor' style='font-size:1em'>l</text>
<text text-anchor='middle' x='80' y='36' fill='currentColor' style='font-size:1em'>l</text>
<text text-anchor='middle' x='88' y='4' fill='currentColor' style='font-size:1em'>t</text>
<text text-anchor='middle' x='88' y='20' fill='currentColor' style='font-size:1em'>x</text>
<text text-anchor='middle' x='96' y='4' fill='currentColor' style='font-size:1em'>t</text>
<text text-anchor='middle' x='96' y='20' fill='currentColor' style='font-size:1em'>-</text>
<text text-anchor='middle' x='104' y='4' fill='currentColor' style='font-size:1em'>p</text>
<text text-anchor='middle' x='104' y='20' fill='currentColor' style='font-size:1em'>d</text>
<text text-anchor='middle' x='112' y='4' fill='currentColor' style='font-size:1em'>s</text>
<text text-anchor='middle' x='112' y='20' fill='currentColor' style='font-size:1em'>s</text>
<text text-anchor='middle' x='120' y='4' fill='currentColor' style='font-size:1em'>:</text>
<text text-anchor='middle' x='120' y='20' fill='currentColor' style='font-size:1em'>h</text>
<text text-anchor='middle' x='128' y='4' fill='currentColor' style='font-size:1em'>/</text>
<text text-anchor='middle' x='128' y='20' fill='currentColor' style='font-size:1em'>-</text>
<text text-anchor='middle' x='136' y='4' fill='currentColor' style='font-size:1em'>/</text>
<text text-anchor='middle' x='136' y='20' fill='currentColor' style='font-size:1em'>p</text>
<text text-anchor='middle' x='144' y='4' fill='currentColor' style='font-size:1em'>g</text>
<text text-anchor='middle' x='144' y='20' fill='currentColor' style='font-size:1em'>r</text>
<text text-anchor='middle' x='152' y='4' fill='currentColor' style='font-size:1em'>i</text>
<text text-anchor='middle' x='152' y='20' fill='currentColor' style='font-size:1em'>o</text>
<text text-anchor='middle' x='160' y='4' fill='currentColor' style='font-size:1em'>t</text>
<text text-anchor='middle' x='160' y='20' fill='currentColor' style='font-size:1em'>v</text>
<text text-anchor='middle' x='168' y='4' fill='currentColor' style='font-size:1em'>h</text>
<text text-anchor='middle' x='168' y='20' fill='currentColor' style='font-size:1em'>i</text>
<text text-anchor='middle' x='176' y='4' fill='currentColor' style='font-size:1em'>u</text>
<text text-anchor='middle' x='176' y='20' fill='currentColor' style='font-size:1em'>d</text>
<text text-anchor='middle' x='184' y='4' fill='currentColor' style='font-size:1em'>b</text>
<text text-anchor='middle' x='184' y='20' fill='currentColor' style='font-size:1em'>e</text>
<text text-anchor='middle' x='192' y='4' fill='currentColor' style='font-size:1em'>.</text>
<text text-anchor='middle' x='192' y='20' fill='currentColor' style='font-size:1em'>r</text>
<text text-anchor='middle' x='200' y='4' fill='currentColor' style='font-size:1em'>c</text>
<text text-anchor='middle' x='208' y='4' fill='currentColor' style='font-size:1em'>o</text>
<text text-anchor='middle' x='216' y='4' fill='currentColor' style='font-size:1em'>m</text>
<text text-anchor='middle' x='224' y='4' fill='currentColor' style='font-size:1em'>/</text>
<text text-anchor='middle' x='232' y='4' fill='currentColor' style='font-size:1em'>r</text>
<text text-anchor='middle' x='240' y='4' fill='currentColor' style='font-size:1em'>a</text>
<text text-anchor='middle' x='248' y='4' fill='currentColor' style='font-size:1em'>u</text>
<text text-anchor='middle' x='256' y='4' fill='currentColor' style='font-size:1em'>l</text>
<text text-anchor='middle' x='264' y='4' fill='currentColor' style='font-size:1em'>l</text>
<text text-anchor='middle' x='272' y='4' fill='currentColor' style='font-size:1em'>e</text>
<text text-anchor='middle' x='280' y='4' fill='currentColor' style='font-size:1em'>n</text>
<text text-anchor='middle' x='288' y='4' fill='currentColor' style='font-size:1em'>c</text>
<text text-anchor='middle' x='296' y='4' fill='currentColor' style='font-size:1em'>h</text>
<text text-anchor='middle' x='304' y='4' fill='currentColor' style='font-size:1em'>a</text>
<text text-anchor='middle' x='312' y='4' fill='currentColor' style='font-size:1em'>i</text>
<text text-anchor='middle' x='320' y='4' fill='currentColor' style='font-size:1em'>/</text>
<text text-anchor='middle' x='328' y='4' fill='currentColor' style='font-size:1em'>r</text>
<text text-anchor='middle' x='336' y='4' fill='currentColor' style='font-size:1em'>a</text>
<text text-anchor='middle' x='344' y='4' fill='currentColor' style='font-size:1em'>p</text>
<text text-anchor='middle' x='352' y='4' fill='currentColor' style='font-size:1em'>i</text>
<text text-anchor='middle' x='360' y='4' fill='currentColor' style='font-size:1em'>d</text>
<text text-anchor='middle' x='368' y='4' fill='currentColor' style='font-size:1em'>-</text>
<text text-anchor='middle' x='376' y='4' fill='currentColor' style='font-size:1em'>m</text>
<text text-anchor='middle' x='384' y='4' fill='currentColor' style='font-size:1em'>l</text>
<text text-anchor='middle' x='392' y='4' fill='currentColor' style='font-size:1em'>x</text>
<text text-anchor='middle' x='400' y='4' fill='currentColor' style='font-size:1em'>-</text>
<text text-anchor='middle' x='408' y='4' fill='currentColor' style='font-size:1em'>d</text>
<text text-anchor='middle' x='416' y='4' fill='currentColor' style='font-size:1em'>s</text>
<text text-anchor='middle' x='424' y='4' fill='currentColor' style='font-size:1em'>h</text>
<text text-anchor='middle' x='432' y='4' fill='currentColor' style='font-size:1em'>-</text>
<text text-anchor='middle' x='440' y='4' fill='currentColor' style='font-size:1em'>p</text>
<text text-anchor='middle' x='448' y='4' fill='currentColor' style='font-size:1em'>r</text>
<text text-anchor='middle' x='456' y='4' fill='currentColor' style='font-size:1em'>o</text>
<text text-anchor='middle' x='464' y='4' fill='currentColor' style='font-size:1em'>v</text>
<text text-anchor='middle' x='472' y='4' fill='currentColor' style='font-size:1em'>i</text>
<text text-anchor='middle' x='480' y='4' fill='currentColor' style='font-size:1em'>d</text>
<text text-anchor='middle' x='488' y='4' fill='currentColor' style='font-size:1em'>e</text>
<text text-anchor='middle' x='496' y='4' fill='currentColor' style='font-size:1em'>r</text>
</g>

    </svg>
  
</div>
<p>Whichever route you take, the provider registers itself as a dsh plugin. The upstream project verifies the source install against <code>dsh 0.1.0-rc.7</code> and reports API compatibility with <code>rc.8</code>. If you are on <code>npm latest</code> for the harness, the plugin remains API-compatible. Because the harness treats anything under <code>dsh.bundle</code> as a plugin, the provider becomes available the moment dsh starts with it on the bundle path — there is no manual registration dialog and no build output.</p>
<h2 id="point-the-agent-at-the-route-in-settingsyaml">Point the agent at the route in settings.yaml</h2>
<p>After installing the plugin, you tell dsh which HTTP route to use. This is the one piece of config you do write by hand, and it is intentionally small: a single endpoint pointing at your Rapid-MLX <code>/v1</code> OpenAPI-compatible route.</p>
<p>In <code>settings.yaml</code>, set the provider&rsquo;s route to your Rapid-MLX server&rsquo;s OpenAI-compatible base URL. Rapid-MLX is a drop-in OpenAI replacement, so the route you already use for Claude Code, Cursor, or Aider is the same one you hand to the dsh provider. From that point on, the heavy lifting — model names, context windows, reasoning efforts, and max model length — comes from the server rather than from your editor.</p>
<p>This single-line indirection is the entire point: you stop hand-writing model facts and start letting the server own them. When you switch from one pulled model to another, the provider follows the server&rsquo;s reported schema automatically.</p>
<h2 id="the-three-things-it-fixes">The three things it fixes</h2>
<p>The provider&rsquo;s value lands in three concrete behaviors.</p>
<h3 id="1-no-more-hand-written-model-config">1. No more hand-written model config</h3>
<p>The biggest win is eliminating per-model manual config. When <code>contextWindow</code>, <code>reasoningEfforts</code>, and <code>max_model_len</code> are all served by Rapid-MLX&rsquo;s <code>/v1/models</code> JSON schema, there is nothing to hand-write when you switch models. Swap the model, and dsh follows automatically because it reads the same facts the server uses to run the model.</p>
<h3 id="2-reasoning-control-finally-reports-the-truth">2. Reasoning control finally reports the truth</h3>
<p>Local model configs have a chronic lie: they declare reasoning settings for models that do not actually have a reasoning parser. The provider fixes this because Rapid-MLX reports whether a model really has a reasoning parser, and the dsh provider surfaces that truth in the reasoning-control selector. You stop telling the agent to reason with a model that physically cannot.</p>
<h3 id="3-compaction-timed-to-real-memory-not-advertised-windows">3. Compaction timed to real memory, not advertised windows</h3>
<p>The third fix is the subtlest and the most valuable on Apple Silicon: the provider prefers the server&rsquo;s <code>max_model_len</code> — a memory-fitted ceiling — over the model&rsquo;s advertised <code>context_window</code>. Apple Silicon unified memory is the real constraint on context length, and hardware-aware capacity beats the marketing number that hand-copied config frequently drifts away from. The result is compaction that actually matches your machine.</p>
<h2 id="manage-models-with-five-tools-and-the-rapid-mlx-command">Manage models with five tools and the /rapid-mlx command</h2>
<p>The provider keeps its surface deliberately small — five tools total, split by which surface owns each fact:</p>
<table>
  <thead>
      <tr>
          <th>Tool</th>
          <th>Surface</th>
          <th>What it does</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td><code>rapid_mlx_serving</code></td>
          <td>HTTP</td>
          <td>Interacts with the Rapid-MLX server over HTTP</td>
      </tr>
      <tr>
          <td><code>rapid_mlx_cached</code></td>
          <td>CLI</td>
          <td>Reads cached model state</td>
      </tr>
      <tr>
          <td><code>rapid_mlx_pull</code></td>
          <td>CLI</td>
          <td>Pulls a model into local storage</td>
      </tr>
      <tr>
          <td><code>rapid_mlx_remove</code></td>
          <td>CLI</td>
          <td>Removes a stored model</td>
      </tr>
      <tr>
          <td><code>rapid_mlx_health</code></td>
          <td>HTTP + CLI</td>
          <td>Health-checks the server and CLI surfaces</td>
      </tr>
  </tbody>
</table>
<p>In addition to these tools, the provider exposes a <code>/rapid-mlx</code> slash command in the agent interface for quick model management. The split is deliberate: facts owned by the server surface flow through the HTTP tools, and facts owned by the local process flow through the CLI tools. Keeping the two apart avoids the confusion that a single mixed-surface tool tends to create.</p>
<h2 id="verified-results-on-m3-ultra-with-dsh-010-rc7">Verified results on M3 Ultra with dsh 0.1.0-rc.7</h2>
<p>The provider is not theoretical. The upstream README documents an end-to-end verification on an M3 Ultra against <code>dsh 0.1.0-rc.7</code>. In that run, a multi-step bug-fixing task completed in 36 seconds on <code>qwen3.6-35b-8bit</code>. That number matters because it shows the plugin doing real work — a realistic agent task with multiple tool calls and reasoning steps — not just booting and idling. The 36-second figure is the best single proof that the provider&rsquo;s route plumbing, reasoning routing, and tool dispatch all function as a unit under load.</p>
<h2 id="what-it-does-not-do-yet">What it does not do yet</h2>
<p>Being precise about the current gaps saves you from assuming features that do not exist. The provider — and Rapid-MLX itself — still has roadmap items that are not wired up:</p>
<ul>
<li><strong><code>recommended_sampling</code>.</strong> Server-suggested sampling parameters are not yet consumed by the provider.</li>
<li><strong><code>tool_call_parser</code>.</strong> Fast tool-call parsing directly on the server is not yet exposed through the provider.</li>
<li><strong>Memory-aware capacity as a first-class feature.</strong> The provider prefers <code>max_model_len</code> today, but fully adaptive capacity derived purely from the memory footprint of the loaded model is the roadmap&rsquo;s biggest remaining win. Right now you get the memory-fitted ceiling the server already reports; the future step is capacity that actively recomputes with load.</li>
</ul>
<p>None of these block the core three fixes above, but they are worth knowing so your expectations match the current release.</p>
<h2 id="conformance-with-the-official-llmadapter-contract">Conformance with the official LlmAdapter contract</h2>
<p>Because DeepSeek Harness is plugin-driven, the bar for a new provider is conformance with the official <code>LlmAdapter</code> contract defined in <code>docs/cookbook/adding-an-llm-adapter.md</code>. The Rapid-MLX DSH Provider treats this as a test obligation: it ships per-protocol-obligation tests, one per rule in the contract, so conformance is not asserted in prose but demonstrated with executable tests.</p>
<p>This is a meaningful design choice. Most adapter plugins pass a smoke test and call it done. This provider structures its suite around the harness&rsquo;s own &ldquo;protocol obligations,&rdquo; which means each method the harness relies on — model enumeration, context negotiation, reasoning-effort handling, tool-call framing — has a targeted test backing it. For anyone who has been burned by an agent plugin that silently violates a harness contract, this is the difference between a provider you can trust and one you debug at 2am.</p>
<h2 id="three-gotchas-before-you-edit-the-provider">Three gotchas before you edit the provider</h2>
<p>If you plan to extend or debug the plugin, these three details will save you real time:</p>
<ol>
<li><strong><code>dsh.bundle</code> makes it a plugin.</strong> DeepSeek Harness treats anything under <code>dsh.bundle</code> as a plugin. If you fork the provider, keeping your code inside that bundle path is what makes the harness load it.</li>
<li><strong><code>LlmReasoningEffortInfo.name</code> is required.</strong> The reasoning-effort contract does not allow a missing <code>name</code> field. If the server reports a reasoning parser that maps to an effort entry without a name, the harness will reject it.</li>
<li><strong>DSH has no tool role.</strong> Unlike some OpenAI-style APIs, DeepSeek Harness has no dedicated &ldquo;tool&rdquo; role in its message protocol. If you extend the provider, route tool-call framing through the roles dsh actually supports rather than assuming a <code>tool</code> role exists.</li>
</ol>
<h2 id="comparison-rapid-mlx-dsh-provider-vs-dsh-llm-mlx-and-other-mlx-mac-agents">Comparison: rapid-mlx DSH provider vs dsh-llm-mlx and other MLX Mac agents</h2>
<p>The Rapid-MLX DSH provider is not the only way to run MLX models inside the DeepSeek Harness. The closest alternative is <code>dsh-llm-mlx</code>, which takes a different architectural route:</p>
<table>
  <thead>
      <tr>
          <th>Aspect</th>
          <th>Rapid-MLX DSH Provider</th>
          <th>dsh-llm-mlx</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>Underlying engine</td>
          <td>Rapid-MLX server</td>
          <td>mlx-lm / mlx-vlm</td>
      </tr>
      <tr>
          <td>Integration path</td>
          <td>Native <code>@raullenchai/dsh-provider</code> plugin</td>
          <td>DSH built-in OpenAI-compatible adapter</td>
      </tr>
      <tr>
          <td>Server ownership</td>
          <td>Rapid-MLX runs as its own engine</td>
          <td>Can manage <code>mlx_lm.server</code> / <code>mlx_vlm.server</code> for the lifetime of the dsh process</td>
      </tr>
      <tr>
          <td>Bind address (managed)</td>
          <td>Rapid-MLX default</td>
          <td><code>127.0.0.1</code></td>
      </tr>
      <tr>
          <td>External server reuse</td>
          <td>Yes (OpenAI-compatible)</td>
          <td>Yes, e.g. <code>http://127.0.0.1:18080/v1</code></td>
      </tr>
      <tr>
          <td>Extra runtime needed</td>
          <td>Node &gt;= 22.15 only</td>
          <td>Local Python env with <code>mlx-lm</code> / <code>mlx-vlm</code></td>
      </tr>
      <tr>
          <td>OS for managed startup</td>
          <td>Apple Silicon</td>
          <td>Apple Silicon macOS</td>
      </tr>
  </tbody>
</table>
<p>The deciding factor is usually which engine you already run. If you use Rapid-MLX (with its 4.2x-over-Ollama claim, 0.08s cached TTFT, and 17 tool parsers), the native provider is the natural fit. If you prefer the mlx-lm stack and want dsh to own the model server lifecycle, <code>dsh-llm-mlx</code> is the better match. Both sit above a separate category of MLX coding agents — projects like <code>mlx-code</code>, the 57-star git-native coding agent for Mac — that prove the broader demand for Apple Silicon native agentic inference even though they are not dsh providers themselves.</p>
<h2 id="faq-and-troubleshooting">FAQ and troubleshooting</h2>
<p><strong>Do I need a GPU to use the Rapid-MLX DSH provider?</strong>
No. The provider is a config/adapter plugin — it does no inference itself. It delegates all model execution to a running Rapid-MLX server on your Apple Silicon machine, so you need an M-series chip, but no separate discrete GPU.</p>
<p><strong>Can the provider work with dsh 0.1.0-rc.8 or the npm release?</strong>
Yes. The plugin was verified end-to-end against dsh <code>0.1.0-rc.7</code>, is API-compatible with <code>rc.8</code>, and the harness <code>npm latest</code> remains compatible with the published provider at <code>0.3.0</code>.</p>
<p><strong>Why does the provider prefer max_model_len over context_window?</strong>
Because on Apple Silicon the unified memory footprint is the real ceiling on how long a context can grow, and <code>max_model_len</code> is the server&rsquo;s memory-fitted ceiling. The model&rsquo;s advertised <code>context_window</code> is often a marketing maximum that hand-copied config drifts away from, so preferencing the server&rsquo;s figure makes compaction match your actual hardware.</p>
<p><strong>How is this different from the built-in OpenAI-compatible adapter in dsh?</strong>
The built-in adapter requires you to hand-maintain model facts in <code>settings.yaml</code> per model. The Rapid-MLX provider reads <code>contextWindow</code>, <code>reasoningEfforts</code>, and <code>max_model_len</code> from the server&rsquo;s <code>/v1/models</code> schema automatically, so switching models needs no manual re-config and reasoning settings report the server&rsquo;s truth.</p>
<p><strong>What happens if I run Node older than 22.15?</strong>
dsh imports Node&rsquo;s Zstd stream API without declaring it, so on an older Node the harness fails at import time. Upgrade to Node &gt;= 22.15 before troubleshooting anything else, because this error surfaces before the provider even loads.</p>
]]></content:encoded></item><item><title>DSH Lark Bridge: Chat with DeepSeek Harness Agents on Feishu/Lark</title><link>https://baeseokjae.github.io/posts/dsh-lark-bridge/</link><pubDate>Sun, 30 Aug 2026 13:01:45 +0000</pubDate><guid>https://baeseokjae.github.io/posts/dsh-lark-bridge/</guid><description>The DSH Lark bridge lets you chat with DeepSeek Harness agents on Feishu/Lark. This guide compares the top plugins and walks through a QR-scan setup with no public network.</description><content:encoded><![CDATA[<p>A DSH Lark bridge is a plugin that connects DeepSeek Harness (DSH) coding agents to Feishu or Lark group chats, so you can send a task from your phone, DM, or a group and the bridge runs it in the correct Harness project and session. You do not need a public IP, domain, webhook, or NAT tunnel: the bridge keeps a WebSocket long connection to the Feishu/Lark Open Platform and relays tool-call approvals, questions, files, and status updates back to a single native card.</p>
<h2 id="what-is-a-dsh-lark-bridge-and-why-use-one">What Is a DSH Lark Bridge (and Why Use One)</h2>
<p>DeepSeek Harness (the <code>deepseek-ai/deepseek-harness</code> repository) is a plugin-architecture coding-agent framework built around the motto &ldquo;Everything is a Plugin.&rdquo; It sits around a ~204k-star ecosystem and sees active development pushes through late August 2026. Because DSH is a local, self-hosted agent harness, it historically lived on your workstation or a private server — reachable only from a terminal.</p>
<p>A DSH Lark bridge changes that. It publishes a lightweight bot into Feishu (China) or Lark (international) and maps the messaging surface onto your harness:</p>
<ul>
<li><strong>A group or topic becomes a task inbox.</strong> Each Feishu/Lark group maps to a Project, and each topic or thread maps to an isolated Session.</li>
<li><strong>A message becomes one DSH Agent task.</strong> Send a request, and the harness picks it up and executes it.</li>
<li><strong>A single native card carries the whole lifecycle.</strong> The bridge updates one card as work progresses, routes tool-call approvals and structured <code>ask_user_question</code> prompts back to the reader, and relays files and images.</li>
</ul>
<p>The practical payoff is that you can drive a serious, long-running coding agent from a phone or a group chat — approve a risky file write, answer a clarifying question, watch intermediate tool calls stream in — without ever opening a terminal. For a solo developer with a laptop, or a small team sharing one harness host, this collapses the distance between &ldquo;I had an idea&rdquo; and &ldquo;the agent did the work.&rdquo;</p>
<h2 id="prerequisites-deepseek-harness-nodejs-api-key-and-a-feishulark-account">Prerequisites: DeepSeek Harness, Node.js, API Key, and a Feishu/Lark Account</h2>
<p>Before you can bridge DSH into Feishu/Lark, you need the pieces the harness and the plugins all assume are already running. The requirements are consistent across the leading bridge plugins:</p>
<table>
  <thead>
      <tr>
          <th>Requirement</th>
          <th>Typical version / note</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>DeepSeek Harness (dsh)</td>
          <td>Tested against 0.1.0-rc.6 (still a developer preview)</td>
      </tr>
      <tr>
          <td>Node.js</td>
          <td>≥ 22 for most plugins; several require ≥ 24</td>
      </tr>
      <tr>
          <td><code>DEEPSEEK_API_KEY</code></td>
          <td>Configured in the dsh harness so agents can call the model</td>
      </tr>
      <tr>
          <td>Feishu / Lark account</td>
          <td>A tenant where you can create a custom app / bot</td>
      </tr>
      <tr>
          <td>Package manager</td>
          <td><code>pnpm</code> for build-from-source projects, <code>npx</code> for registry installs</td>
      </tr>
  </tbody>
</table>
<p>The single most common onboarding blocker is not the model key — it is the networking story. Historically, any bridge that wants to receive events from Feishu needed a public endpoint. The modern generation of DSH–Feishu bridges sidesteps this entirely by using Feishu&rsquo;s WebSocket long-connection API through the official <code>@larksuiteoapi/node-sdk</code>, so <strong>no public IP, domain, webhook, or NAT tunnel is required</strong> — a detail we will unpack in the WebSocket section below.</p>
<h2 id="the-6-leading-dshfeishulark-bridge-plugins-compared">The 6 Leading DSH↔Feishu/Lark Bridge Plugins Compared</h2>
<p>As of August 2026 the ecosystem has produced at least half a dozen serious bridge projects. They differ meaningfully in setup friction, security model, reliability guarantees, and how aggressively they isolate workspaces. Here is the comparison matrix.</p>
<table>
  <thead>
      <tr>
          <th>Plugin</th>
          <th>Setup</th>
          <th>Networking</th>
          <th>Session / isolation</th>
          <th>Security model</th>
          <th>Standout feature</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td><code>imetn/dsh-lark-bridge</code></td>
          <td><code>pnpm dlx ... setup --project &quot;$PWD&quot; --brand larkoffice|lark</code></td>
          <td>WebSocket, no public server</td>
          <td>Group→Project, topic→isolated Session</td>
          <td>Card-button approval, structured question routing</td>
          <td>Bidirectional controller with card detail presets (compact / standard / developer)</td>
      </tr>
      <tr>
          <td><code>bihangchi9-creator/dsh-lark-bridge</code></td>
          <td><code>pnpm setup</code> — <strong>must build first</strong> (lib/ is git-ignored)</td>
          <td>WebSocket</td>
          <td>One group = one folder <code>&lt;workspaceRoot&gt;/&lt;chatId&gt;</code>; per-message <code>.attachments/&lt;messageId&gt;/</code></td>
          <td>QR registration wizard, allowlist /allow /disallow</td>
          <td>Strict file isolation; persistent per-chat sessions survive restarts</td>
      </tr>
      <tr>
          <td><code>chenxin105/dsh-lark-bridge</code></td>
          <td><code>dsh plugin --profile web add github:&lt;user&gt;/dsh-lark-bridge</code></td>
          <td>WebSocket</td>
          <td>Inbound private-chat → in-process agent</td>
          <td>Credential-resolved <code>tenant_access_token</code> with auto-refresh; <code>role('secret')</code> fields</td>
          <td>Model-facing tools: send message, read doc, Bitable read/write, call agent</td>
      </tr>
      <tr>
          <td><code>amlyczz/dsh-lark-link</code></td>
          <td>npm <code>dsh-lark-link</code>; <code>/lark setup</code> scan QR (30s)</td>
          <td>WebSocket</td>
          <td>Per-chat context</td>
          <td>Allowlist <code>open_id</code> control, <code>groupPolicy</code> (open/mention/keywords/reply), denyList</td>
          <td>Zero message loss: outbound Outbox + inbound WAL replay; <code>/doctor</code> diagnostic ZIP</td>
      </tr>
      <tr>
          <td><code>ailoushu666/dsh-feishu-bridge</code></td>
          <td><code>npx @deepseek-ai/dsh plugin --profile web add git+https://...</code></td>
          <td>WebSocket</td>
          <td>Same DSH Session reused per chat, threads independent</td>
          <td>Option-question cards for <code>ask_user_question</code></td>
          <td>Real-time 🔧 tool-call hints via throttle queue</td>
      </tr>
      <tr>
          <td><code>PlutoKeating/dsh-lark-bot</code></td>
          <td><code>npx dsh-lark-bot@latest setup --profile dsh-lark; dsh --profile dsh-lark</code></td>
          <td>WebSocket</td>
          <td>Per-session isolated git worktrees; <code>/session /archive /retention</code></td>
          <td>Multi-bot trusted handoff, /safemode core self-heal</td>
          <td>Crash safety-net: dsh crash still replies in Feishu; <code>/jobs</code> auto-retry queue</td>
      </tr>
  </tbody>
</table>
<p>The registries that validate these installs (dsharness.org, dshfind.com, dshbase.com, dsh-plugin.org) point to a fast-growing plugin ecosystem around DSH↔Feishu bridges in 2026.</p>
<h2 id="choosing-the-right-bridge-for-your-use-case">Choosing the Right Bridge for Your Use Case</h2>
<p>Your pick should hinge on three questions: who will install it, how much isolation you need, and whether you trust the reliability layer.</p>
<ul>
<li><strong>Choose <code>imetn/dsh-lark-bridge</code></strong> when you want a controller-grade bridge with a <code>--brand</code> flag for ByteDance <code>larkoffice.com</code> tenants and polished card presets (compact / standard / developer). It is tested against DSH 0.1.0-rc.6, so expect developer-preview rough edges.</li>
<li><strong>Choose <code>bihangchi9-creator/dsh-lark-bridge</code></strong> when strict file isolation is non-negotiable — each chat gets its own <code>&lt;workspaceRoot&gt;/&lt;chatId&gt;</code> folder and per-message <code>.attachments/</code> storage, so groups never touch each other&rsquo;s files. Remember the critical rule: <strong>build before install</strong>, because <code>lib/</code> is git-ignored.</li>
<li><strong>Choose <code>amlyczz/dsh-lark-link</code></strong> when you need at-least-once delivery guarantees and a real diagnostic path. Its outbound Outbox (JSONL, idempotent) plus inbound WAL replay and <code>/doctor</code> ZIP diagnostics make it the strongest reliability story.</li>
<li><strong>Choose <code>ailoushu666/dsh-feishu-bridge</code></strong> when you want the leanest setup (official dsh plugin add, official <code>@larksuiteoapi/node-sdk</code>) and live tool-call visibility with interactive option questions.</li>
<li><strong>Choose <code>PlutoKeating/dsh-lark-bot</code></strong> when you want parallel multi-task in one group, multi-role agents via <code>/role</code>, and a crash safety-net that keeps replying even if the harness dies.</li>
</ul>
<h2 id="step-by-step-setup-with-a-qr-scan-no-public-network-needed">Step-by-Step Setup with a QR Scan (No Public Network Needed)</h2>
<p>The lowest-friction path is the QR-scan onboarding shared by <code>amlyczz/dsh-lark-link</code> and <code>PlutoKeating/dsh-lark-bot</code>. The &ldquo;30-second setup&rdquo; story works because the bridge creates the Feishu app for you — it auto-subscribes to message events, group mention, and reaction permissions — so there is no portal spelunking.</p>
<ol>
<li>
<p><strong>Install the plugin.</strong></p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-bash" data-lang="bash"><span style="display:flex;"><span><span style="color:#75715e"># amlyczz/dsh-lark-link</span>
</span></span><span style="display:flex;"><span>npx dsh-lark-link setup --project <span style="color:#e6db74">&#34;</span>$PWD<span style="color:#e6db74">&#34;</span>  <span style="color:#75715e"># or the registry install for your repo</span>
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#75715e"># PlutoKeating/dsh-lark-bot</span>
</span></span><span style="display:flex;"><span>npx dsh-lark-bot@latest setup --profile dsh-lark
</span></span></code></pre></div><p>Both require Node.js ≥ 24 (lark-bot accepts ≥ 22) and <code>@deepseek-ai/dsh</code> installed.</p>
</li>
<li>
<p><strong>Scan the QR to create the Feishu app.</strong> The wizard auto-creates the bot and grants the message-event subscription perms it needs, so you never touch the Feishu Open Platform console for the common path.</p>
</li>
<li>
<p><strong>Start the harness with the bridge profile.</strong></p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-bash" data-lang="bash"><span style="display:flex;"><span>dsh --profile dsh-lark
</span></span></code></pre></div></li>
<li>
<p><strong>Add the bot to a group (or DM it) and confirm.</strong> Send <code>/help</code> or <code>/lark</code> to verify the connection, then send a real task.</p>
</li>
<li>
<p><strong>Run <code>/doctor</code> or <code>/where</code> if anything is off</strong> — the lark-link tool bundles a diagnostic ZIP (session log + redacted config) that is far faster to share than a wall of terminal output.</p>
</li>
</ol>
<h2 id="how-the-websocket-connection-eliminates-public-ip-domain-and-tunnels">How the WebSocket Connection Eliminates Public IP, Domain and Tunnels</h2>
<p>The #1 reason self-hosted agent bridges stall is networking: &ldquo;I don&rsquo;t want to expose my box.&rdquo; The DSH bridge ecosystem solved this at the platform level. Feishu&rsquo;s Open Platform supports <strong>WebSocket long connections</strong> for receiving events, and the plugins implement them via the official <code>@larksuiteoapi/node-sdk</code>.</p>
<p>On a WebSocket long-connection model, your client <em>initiates</em> the connection outbound to <code>open.feishu.cn</code> or <code>open.larksuite.com</code> and keeps it alive. Feishu pushes events (messages, mentions, reactions) down that existing connection. Because the connection is outbound, there is:</p>
<ul>
<li><strong>No public IP</strong> — your harness host stays behind your NAT/Firewall.</li>
<li><strong>No domain</strong> — nothing to buy, configure, or renew.</li>
<li><strong>No webhook endpoint</strong> — no ingress route to secure.</li>
<li><strong>No NAT tunnel / ngrok-style relay</strong> — one less moving part and one less attack surface.</li>
</ul>
<p>The bidirectional plugins layer the <em>sending</em> side on the same outbound channel. <code>imetn/dsh-lark-bridge</code>, for example, is explicitly designed as &ldquo;WebSocket long connections, no public webhook server.&rdquo; If your use case is Feishu (China), the base URL is <code>https://open.feishu.cn/open-apis</code>; for Lark (international) it is <code>https://open.larksuite.com/open-apis</code>. ByteDance <code>larkoffice.com</code> tenants instead pass <code>--brand larkoffice</code>, and <code>chenxin105/dsh-lark-bridge</code> exposes a <code>baseURL</code> switch for exactly this split.</p>
<h2 id="managing-sessions-projects-and-workspace-isolation">Managing Sessions, Projects and Workspace Isolation</h2>
<p>How a bridge scopes work determines whether five teammates can share one harness safely. The plugins converge on a few patterns:</p>
<ul>
<li><strong>Group → Project; topic/thread → Session.</strong> <code>imetn/dsh-lark-bridge</code> maps each group to a project and each topic to an isolated session, so parallel conversations do not bleed into each other.</li>
<li><strong>One chat = one project folder.</strong> <code>bihangchi9-creator/dsh-lark-bridge</code> pins each chat id to a stable <code>&lt;workspaceRoot&gt;/&lt;chatId&gt;</code> directory. Groups literally cannot touch each other&rsquo;s files. Files and images land in isolated <code>.attachments/&lt;messageId&gt;/</code> per message, with sane limits (5 attachments, images ≤ 10 MB, other files ≤ 20 MB) and a 7-day sweep.</li>
<li><strong>Session reuse with thread independence.</strong> <code>ailoushu666/dsh-feishu-bridge</code> reuses the same DSH Session across messages in a chat to preserve context, while different threads stay independent.</li>
<li><strong>Per-session git worktrees.</strong> <code>PlutoKeating/dsh-lark-bot</code> isolates each session in its own git worktree, with <code>/session</code>, <code>/archive</code>, and <code>/retention</code> commands to prune old workspaces.</li>
<li><strong>Persistent per-chat sessions.</strong> Sessions in <code>bihangchi9-creator/dsh-lark-bridge</code> survive restarts; a <code>/new</code> command clears the current one. This is the difference between a stateless bot that forgets everything and a durable workspace that resumes mid-task.</li>
</ul>
<p>If you are running parallel, long-lived agent jobs in shared groups, favor the folder-worktree isolation models over a single shared-session bridge.</p>
<h2 id="approving-tool-calls-and-answering-agent-questions-from-cards">Approving Tool Calls and Answering Agent Questions from Cards</h2>
<p>A coding agent without a human-in-the-loop approval gate is a liability. The bridge plugins build that gate into the Feishu/Lark card itself:</p>
<ul>
<li><strong>Card buttons approve or reject tool calls.</strong> <code>imetn/dsh-lark-bridge</code> renders card buttons that let you approve a pending tool invocation (or decline it) without touching a keyboard.</li>
<li><strong>Structured Agent questions resolve in-chat.</strong> When the harness calls <code>ask_user_question</code>, <code>ailoushu666/dsh-feishu-bridge</code> surfaces an interactive option list — reply with the option number or text and the agent proceeds. <code>amlyczz/dsh-lark-link</code> uses intent-confirm cards for the same purpose, so an ambiguous decision is confirmed before any side effect.</li>
<li><strong>Execution is visible.</strong> Real-time tool-call hints (🔧) and intermediate replies stream through a throttle queue so you are not staring at an opaque &ldquo;thinking&hellip;&rdquo; card.</li>
<li><strong>Replies link back to the triggering message</strong> (DM and group <code>@bot</code> by default), keeping the thread readable.</li>
</ul>
<p>The practical rule: choose a plugin whose approval/rejection flow runs on the card. If your agents can write to files or run commands, you want that gate to be one tap in Feishu, not a return trip to a terminal.</p>
<h2 id="security-credentials-permission-tiers-and-allowlists">Security: Credentials, Permission Tiers, and Allowlists</h2>
<p>Feishu/Lark bridge plugins handle two classes of secret: the platform app credential and tool permissions for the agent. The ecosystem&rsquo;s approach varies, so read the security model before choosing.</p>
<ul>
<li><strong>Credential-resolved <code>tenant_access_token</code> with auto-refresh.</strong> <code>chenxin105/dsh-lark-bridge</code> resolves tokens through an auth provider, refreshing automatically, and keeps each tenant&rsquo;s token fresh. App ID/Secret are read from env (<code>FEISHU_APP_ID</code> / <code>FEISHU_APP_SECRET</code>) rather than hard-coded, and the bridge stores them through a credentials domain after a one-time web UI onboarding dialog.</li>
<li><strong><code>role('secret')</code> fields stay out of <code>describe()</code>.</strong> Sensitive config fields carry an explicit <code>secret</code> role so they never leak into a harness <code>describe()</code> response or diagnostics bundle.</li>
<li><strong>Allowlists and permission tiers.</strong> <code>amlyczz/dsh-lark-link</code> exposes an <code>open_id</code> allowlist, a <code>groupPolicy</code> (open / mention / keywords / reply), and a denyList fallback — so a bot only responds to the right people in the right contexts. Several plugins add explicit permission tiers (read / write / full) and owner-only admin commands (<code>/allow</code>, <code>/disallow</code>, <code>/model</code>, <code>/preset</code>).</li>
<li><strong>No public ingress = smaller attack surface.</strong> Because the bridge connects outbound over WebSocket, there is no public webhook URL for attackers to discover or abuse.</li>
</ul>
<p>Before connecting a real repository, set an allowlist, keep the bot&rsquo;s group permissions narrow, and confirm which of your agent&rsquo;s tools require approval.</p>
<h2 id="reliability-outbox-wal-replay-and-connection-self-healing">Reliability: Outbox, WAL Replay, and Connection Self-Healing</h2>
<p>Agent bridges fail in unglamorous ways: a dropped WebSocket, a crashed harness, a reply lost between the model and the card. The strongest plugins engineer around each failure mode.</p>
<ul>
<li><strong>Outbound Outbox (at-least-once, idempotent).</strong> <code>amlyczz/dsh-lark-link</code> appends outbound messages to a JSONL outbox before sending, then de-duplicates on re-delivery — so a crash between &ldquo;I answered&rdquo; and &ldquo;Feishu delivered&rdquo; does not lose the message, and a retry does not double-send it.</li>
<li><strong>Inbound WAL replay.</strong> The same plugin logs inbound events to a write-ahead log and replays on crash, so a task sent right before a restart is not silently dropped.</li>
<li><strong>Connection self-heal with a QuotaGovernor.</strong> Long connections inevitably drop; a QuotaGovernor re-establishes them and paces reconnects so you do not hammer Feishu&rsquo;s rate limits during an outage.</li>
<li><strong>Crash safety-net.</strong> <code>PlutoKeating/dsh-lark-bot</code> keeps replying in Feishu even if <code>dsh</code> itself crashes; <code>/safemode</code> boots a core-only self-healing config, and <code>/jobs</code> auto-retries queued tasks.</li>
<li><strong>Reaction receipts.</strong> Confirmed delivery is surfaced as reaction receipts (received, then DONE ✅), so you see at a glance whether the agent actually accepted the job.</li>
</ul>
<p>For anything production-shaped, prefer a plugin with explicit outbox/WAL guarantees over one that fires a message and hopes.</p>
<h2 id="common-pitfalls-and-quick-troubleshooting-doctor-build-before-install-brand-flags">Common Pitfalls and Quick Troubleshooting (/doctor, build-before-install, brand flags)</h2>
<p>Even the smoothest bridge has a few sharp edges. These are the four that trip up first-time users.</p>
<table>
  <thead>
      <tr>
          <th>Pitfall</th>
          <th>Symptom</th>
          <th>Fix</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>Installing before building</td>
          <td>Harness fails to load the plugin</td>
          <td><strong>Build first</strong> — in <code>bihangchi9-creator/dsh-lark-bridge</code> the <code>lib/</code> directory is git-ignored, so <code>pnpm setup</code> (which builds, links, and registers) is mandatory, not optional</td>
      </tr>
      <tr>
          <td>Wrong brand / region</td>
          <td>App can&rsquo;t reach the Open Platform</td>
          <td>Set the right base URL: <code>open.feishu.cn</code> (China), <code>open.larksuite.com</code> (international), or pass <code>--brand larkoffice</code> for ByteDance tenants</td>
      </tr>
      <tr>
          <td>Stale or missing model key</td>
          <td>Agents error on first call</td>
          <td>Confirm <code>DEEPSEEK_API_KEY</code> is configured and hot-updatable in the harness (<code>/config</code> card on lark-bot)</td>
      </tr>
      <tr>
          <td>Mystery failure, no logs</td>
          <td>Slow debugging</td>
          <td>Run <code>/doctor</code> (<code>amlyczz/dsh-lark-link</code>) to export a diagnostic ZIP with session log + redacted config, or <code>/where</code> / <code>/models</code> to verify state</td>
      </tr>
  </tbody>
</table>
<p>Remember that DeepSeek Harness 0.1.0-rc.6 is a <strong>developer preview</strong> — API surfaces move. Pin your plugin and harness versions together.</p>
<h2 id="faq-feishu-vs-lark-costs-and-when-to-self-host-vs-use-a-plugin-registry">FAQ: Feishu vs Lark, Costs, and When to Self-Host vs Use a Plugin Registry</h2>
<p><strong>What is the difference between Feishu and Lark for this bridge?</strong>
Feishu is ByteDance&rsquo;s collaboration app for the China market; Lark is the international build. They share the same Open Platform API but different base URLs: <code>https://open.feishu.cn/open-apis</code> for Feishu and <code>https://open.larksuite.com/open-apis</code> for Lark. ByteDance <code>larkoffice.com</code> tenants use a separate <code>--brand larkoffice</code> flag. Choose your region&rsquo;s platform and match the plugin&rsquo;s brand/baseURL accordingly.</p>
<p><strong>Does the bridge need a public IP, domain, or webhook?</strong>
No. The modern plugins use Feishu&rsquo;s WebSocket long-connection API through <code>@larksuiteoapi/node-sdk</code>. Your harness connects outbound to the Open Platform and receives events over that persistent connection, so no public endpoint, domain, webhook, or NAT tunnel is required.</p>
<p><strong>Does it cost money to run a DSH Lark bridge?</strong>
There is usually a fee to enable Feishu/Lark bot or long-connection capabilities in some open-platform tiers, plus you pay for the model calls your DeepSeek Harness agents make via <code>DEEPSEEK_API_KEY</code>. The bridge software itself is open source; you self-host it on your own Node.js ≥ 22 / ≥ 24 machine.</p>
<p><strong>When should I self-host a branch from source vs install from a plugin registry?</strong>
Use a registry install (<code>dsh plugin add</code>, <code>npx</code>) for convenience and faster updates when the plugin validates on registries like dsharness.org, dshfind.com, dshbase.com, or dsh-plugin.org. Pull and build from source (e.g., with <code>pnpm setup</code>) when you need to patch behavior, need strict build-before-install guarantees, or the plugin has not been packaged.</p>
<p><strong>Which bridge should I pick for first contact?</strong>
For a first install, choose a QR-scan plugin that auto-creates the Feishu app (<code>amlyczz/dsh-lark-link</code> for its reliability and <code>/doctor</code>, or <code>PlutoKeating/dsh-lark-bot</code> for multi-task and crash safety). Both eliminate the Open Platform console work, connect over WebSocket with no public network, and get you to a working &ldquo;chat with an agent&rdquo; in under a few minutes.</p>
]]></content:encoded></item><item><title>DeepSeek Harness iOS Simulator Plugin: Drive iOS Apps from Your Coding Agent</title><link>https://baeseokjae.github.io/posts/dsh-ios-simulator-plugin-2026/</link><pubDate>Sat, 29 Aug 2026 20:32:11 +0000</pubDate><guid>https://baeseokjae.github.io/posts/dsh-ios-simulator-plugin-2026/</guid><description>The DSH iOS Simulator plugin turns your coding agent into a driver of live iOS apps: simulators and real iPhones, with 22 tools including AXe UI trees, OCR taps, and SwiftUI hot reload.</description><content:encoded><![CDATA[<p>The DeepSeek Harness iOS Simulator plugin is a live, interactive iOS simulator that runs inside a DeepSeek Harness (DSH) conversation — plus your real iPhone over USB — so your coding agent can see, tap, and verify the app it just changed. It ships 22 agent tools covering devices, screenshots, semantic UI navigation, build-and-run, SwiftUI preview hot reload, processes, and backtraces. This guide explains how it works, how to install it, and how it closes the verify gap that keeps coding agents from trusting their own Swift.</p>
<h2 id="what-is-the-deepseek-harness-ios-simulator-plugin">What Is the DeepSeek Harness iOS Simulator Plugin?</h2>
<p>The DeepSeek Harness iOS Simulator plugin (repo <code>ZSeven-W/dsh-ios</code>, npm package <code>@zseven-w/dsh-ios</code>, MIT license) is a plugin that plugs a full iOS runtime into a DSH conversation. Instead of your agent building an app and guessing whether the UI works, it opens a real simulator window you can watch, reads the accessibility tree, and taps elements by label — or drives a physical iPhone over USB through WebDriverAgent.</p>
<p>It is one plugin inside the DeepSeek Harness platform, the open-source &ldquo;Everything is a Plugin&rdquo; agent framework from deepseek-ai. DSH itself is one of the most-starred AI repos on GitHub, with roughly 203,297 stars and 23,448 forks as of the plugin&rsquo;s active development window (mid-to-late August 2026). The iOS plugin sits alongside siblings like the Android plugin (<code>dsh-android</code>, driven through adb) and desktop plugins, all installed into the same agent conversation.</p>
<p>The current plugin release is <code>0.1.0-rc.3</code>, tested against DSH <code>0.1.1-rc.1</code>. It is written in TypeScript and has gathered around 266 stars and 23 forks in the days since it was created on 2026-08-19. Because it is a release candidate, expect the tool set and API to evolve as the harness ecosystem matures.</p>
<h2 id="why-agents-need-a-real-ios-simulator-the-verify-gap">Why Agents Need a Real iOS Simulator (The Verify Gap)</h2>
<p>Coding agents are good at writing Swift. They are historically bad at <em>using</em> the app they wrote. The gap is simple: an agent can edit a view, but until the change is compiled, launched, rendered, and visible on a screen, it has no trustworthy signal about whether the edit actually works. This is the &ldquo;verify gap,&rdquo; and it is exactly what the iOS Simulator plugin is built to close.</p>
<p>Competitor tooling calls out the same premise. LIGH (<code>light-ios-simulator</code>) describes closing the loop &ldquo;write → build → run → interact → verify → fix&rdquo; and notes that &ldquo;coding agents can edit Swift but cannot reliably USE the app they just changed.&rdquo; DSH iOS attacks the identical problem: an agent that can see a running app and tap its real UI can produce evidence instead of speculation.</p>
<p>Without a live target, agents fall back on brittle heuristics: they inspect the code statically, reason about what the framework &ldquo;should&rdquo; render, and hope. With a live simulator in the conversation, the agent observes actual render output, extracts the accessibility tree, taps the element, and reads the log — a closed feedback loop that converts &ldquo;I think this works&rdquo; into &ldquo;this demonstrably works.&rdquo;</p>
<h2 id="how-the-plugin-drives-ios-apps-the-22-tools">How the Plugin Drives iOS Apps (The 22 Tools)</h2>
<p>The plugin registers 22 agent tools across device lifecycle, interaction, build, inspection, and diagnostics. Grouped by function, they cover:</p>
<ul>
<li><strong>Device lifecycle:</strong> list devices, boot and shut down simulators</li>
<li><strong>Screen capture:</strong> take screenshots of the current state</li>
<li><strong>Interaction:</strong> tap the screen, either by coordinates or semantically</li>
<li><strong>Build and run:</strong> compile and launch the target app on a chosen device</li>
<li><strong>Unified logs:</strong> stream device logs in one place</li>
<li><strong>UI inspection:</strong> dump the AXe-backed accessibility tree, enumerate list and feed rows</li>
<li><strong>Vision/OCR:</strong> find and tap text on screen when the tree is empty or degenerate</li>
<li><strong>Preview hot reload:</strong> SwiftUI previews that swap edits in seconds without relaunch</li>
<li><strong>Process and diagnostics:</strong> process listing, backtraces, leaks, and app info</li>
</ul>
<p>The build-and-run path is the entry point: the agent targets a device, compiles the project, installs, and launches it. From there the interaction tools take over. Because every tool returns machine-readable output — an a11y tree, a row list, a log line — the agent can make decisions and act on them in the same heartbeat rather than pausing for human help.</p>
<h2 id="semantic-ui-automation-trees-ocr-and-taps-that-arent-guesswork">Semantic UI Automation: Trees, OCR, and Taps That Aren&rsquo;t Guesswork</h2>
<p>The biggest usability win in the plugin is semantic UI automation. Rather than tapping at guessed pixel coordinates — which break the moment the layout changes — the plugin drives the UI through the accessibility tree.</p>
<ul>
<li><code>ios_sim_ui_tree</code> dumps an AXe-backed accessibility tree of the current screen.</li>
<li><code>ios_sim_tap_element</code> taps an element by its label or accessibility identifier.</li>
<li><code>ios_sim_find_text</code> runs OCR over the screen, used when the accessibility tree is empty or degenerate (for example, custom-drawn or GPU-rendered content).</li>
<li><code>ios_sim_tap_text</code> taps the matched text once found.</li>
</ul>
<p>This design means the agent interacts by meaning (&ldquo;tap the button labeled Submit&rdquo;) rather than by coordinates (&ldquo;tap pixel 314,502&rdquo;). Identity- and text-based taps are dramatically more durable across layout changes, device sizes, and orientation shifts.</p>
<p>For long, scrollable surfaces — feeds, inboxes, settings lists — the plugin adds <code>ios_sim_ui_rows</code>, which flattens a deep accessibility snapshot into indexed rows, and <code>ios_sim_tap_row</code>, which taps a row using relative coordinates and then verifies the tap landed by checking that the element&rsquo;s expected counter changed by ±1. That verification step is what separates reliable automation from fire-and-forget taps.</p>
<h2 id="driving-a-real-iphone-over-usb-with-webdriveragent">Driving a Real iPhone Over USB with WebDriverAgent</h2>
<p>The simulator is the easy path; the plugin also reaches physical devices. Via <code>ios_real_start_wda</code>, it builds and launches WebDriverAgent (the Appium control server) on a connected iPhone, then tunnels two ports over loopback <code>usbmux</code>: one for REST control and one for the MJPEG screen stream. The result is that the agent drives your actual device from the same conversation it uses for the simulator.</p>
<p>Real-device testing matters because the simulator does not replicate everything: sensors, network behavior, App Store signing, push notifications, and certain performance envelopes only exist on hardware. But real devices bring real maintenance costs, and the plugin documents them honestly. WebDriverAgent&rsquo;s free-team signing profiles expire after 7 days, at which point you must re-run <code>ios_real_start_wda</code> to recover from a &ldquo;profile-expired&rdquo; status. Every real-account tap is gated by identify-before-tap rules, so the agent confirms what it is about to touch rather than blindly interacting with production data.</p>
<h2 id="swiftui-preview-hot-reload-for-fast-verify-fix-loops">SwiftUI Preview Hot Reload for Fast Verify-Fix Loops</h2>
<p>One of the strongest features for iteration speed is <code>ios_sim_preview</code>. Instead of rebuilding and relaunching the entire app to test a change, the plugin generates a disposable host app, builds the changed SwiftUI previews as a dynamic library (dylib), and hot-swaps the edits into the running simulator in roughly 2 to 5 seconds.</p>
<p>This transforms the verify-fix loop. A full build-and-launch cycle can take a developer tens of seconds or minutes; preview hot reload collapses it to a few seconds. An agent that can iterate in 2–5 seconds per change can afford to try multiple approaches, observe each result, and converge on a working UI quickly — the kind of tight loop that makes autonomous UI work feasible at all.</p>
<h2 id="security-model-loopback-only-transport-and-signed-expiring-urls">Security Model: Loopback-Only Transport and Signed, Expiring URLs</h2>
<p>Giving an agent the ability to touch your device raises an obvious security question, and the plugin answers it with a defense-in-depth design.</p>
<ul>
<li><strong>Loopback-only transport:</strong> the streaming engine (<code>serve-sim</code>, by Expo&rsquo;s Evan Bacon, Apache-2.0) binds to <code>127.0.0.1</code> in a dedicated port range and enforces loopback Host and Fetch-Metadata checks.</li>
<li><strong>Signed, expiring URLs:</strong> visual bytes reach the browser UI only through HMAC-signed, expiring URLs under the <code>/_/_dsh/dsh-ios/*</code> routes — never as raw image blocks — and the capability URLs expire within 10 minutes.</li>
<li><strong>Gate on real devices:</strong> every real-account tap is gated by identify-before-tap rules.</li>
</ul>
<p>This trades convenience for a constrained attack surface: nothing is exposed beyond the local loopback, nothing is addressable without a short-lived signed capability, and physical-device automation requires explicit confirmation before each tap. For an agent tool that manipulates a device, that is a defensible posture.</p>
<h2 id="troubleshooting-common-issues-and-coded-error-statuses">Troubleshooting Common Issues and Coded Error Statuses</h2>
<p>Early-version plugins are expected to break in expected ways. The plugin exposes several coded statuses worth knowing:</p>
<ul>
<li><strong>profile-expired:</strong> the WebDriverAgent signing profile passed its 7-day lifetime; re-run <code>ios_real_start_wda</code>.</li>
<li><strong>Idle stream stopping:</strong> with zero consumers, the simulator MJPEG stream stops after 5 minutes and restarts automatically on the next tool call or panel open — this is idle management, not a crash.</li>
<li><strong>Crashed streams:</strong> a crashed stream restarts in about 5 seconds, so a brief video interruption is normal.</li>
<li><strong>Empty/degenerate accessibility tree:</strong> if OCR finds and taps text that the tree cannot see, that is the expected fallback path, not a failure of the plugin.</li>
</ul>
<p>When something looks stuck, the fastest diagnostic is to check screenshots (<code>ios_sim_screenshot</code>), read the unified log, and inspect the process list — the three pieces of evidence the plugin is designed to surface.</p>
<h2 id="how-it-compares-to-ligh-and-other-ios-agent-tools">How It Compares to LIGH and Other iOS Agent Tools</h2>
<p>DSH iOS is not alone in attacking the verify gap, but it takes a distinctly different shape than LIGH (<code>light-ios-simulator</code>, also by a third party) and other agent iOS tools. The table below summarizes the differences.</p>
<table>
  <thead>
      <tr>
          <th><strong>Aspect</strong></th>
          <th><strong>DSH iOS Simulator</strong></th>
          <th><strong>LIGH</strong></th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>Runtime</td>
          <td>DSH conversation plugin (TypeScript, MIT)</td>
          <td>Host-side control plane (<code>ligh_*</code> MCP tools)</td>
      </tr>
      <tr>
          <td>Device target</td>
          <td>iOS Simulator + real iPhone over USB (WebDriverAgent)</td>
          <td>Simulator + physical Expo debug builds</td>
      </tr>
      <tr>
          <td>Interaction model</td>
          <td>22 agent tools; AXe a11y tree, OCR tap, row actions</td>
          <td>Feel IR (structured interaction frame), Autopilot, TRAIL repair</td>
      </tr>
      <tr>
          <td>UI semantics</td>
          <td>AXe tree + OCR text taps</td>
          <td>Structured interaction frame, not screenshot dump</td>
      </tr>
      <tr>
          <td>Hot reload</td>
          <td>SwiftUI preview dylib hot-swap (~2–5s)</td>
          <td>N/A (repair engine does LLM-patched fixes)</td>
      </tr>
      <tr>
          <td>Ecosystem</td>
          <td>Siblings for Android (<code>dsh-android</code>) and desktop</td>
          <td>Standalone MCP control plane</td>
      </tr>
  </tbody>
</table>
<p>Both projects agree on the core claim: agents cannot be trusted to fix Swift UI unless they can verify it. DSH iOS leans on the plugin model and a 22-tool semantic interaction surface; LIGH leans on an interaction frame, an Autopilot mode, and a TRAIL repair engine that classifies failures, localizes them, and applies a bounded set of LLM patches.</p>
<p>The ecosystem trend behind both is worth noting. DeepSeek Harness&rsquo;s plugin architecture (&ldquo;Everything is a Plugin&rdquo;) has produced modular extensions — a VSCode client, an office plugin, an LLM-as-a-Verifier plugin, and now this mobile plugin. With iOS and Android plugins in place, a single agent conversation can drive both mobile platforms, using adb on the Android side and WebDriverAgent/the simulator on the Apple side.</p>
<h2 id="getting-started-install-configure-and-add-to-your-deepseek-harness">Getting Started: Install, Configure, and Add to Your DeepSeek Harness</h2>
<p>Because DSH is plugin-based, adding iOS driving capability is a matter of installing one plugin and configuring it, rather than adopting a separate platform.</p>
<ol>
<li><strong>Install the plugin</strong> from npm: <code>@zseven-w/dsh-ios</code> (current release <code>0.1.0-rc.3</code>). Confirm your DSH version matches the tested pairing, <code>0.1.1-rc.1</code>.</li>
<li><strong>Add the plugin</strong> to your DSH conversation so its 22 tools and presentation panel are registered.</li>
<li><strong>Choose your target.</strong> Boot a simulator with the device tools, or connect a real iPhone over USB and run <code>ios_real_start_wda</code> to build and launch WebDriverAgent.</li>
<li><strong>Build and launch</strong> your app on the selected device.</li>
<li><strong>Drive the UI semantically.</strong> Use <code>ios_sim_ui_tree</code> / <code>ios_sim_tap_element</code> for accessibility-aware taps, or <code>ios_sim_find_text</code> / <code>ios_sim_tap_text</code> when OCR is needed.</li>
<li><strong>Iterate fast</strong> with <code>ios_sim_preview</code> for SwiftUI hot reload.</li>
</ol>
<p>Real-device users should plan around the 7-day signing-profile cycle and re-run <code>ios_real_start_wda</code> when they hit <code>profile-expired</code>. Everyone should treat the 5-minute idle stream stop and ~5-second crash restart as normal behavior, not defects.</p>
<h2 id="faq-deepseek-harness-ios-simulator-plugin">FAQ: DeepSeek Harness iOS Simulator Plugin</h2>
<p><strong>Is DSH iOS Simulator a separate app, or a plugin for DeepSeek Harness?</strong>
It is a plugin. You install <code>@zseven-w/dsh-ios</code> into an existing DeepSeek Harness conversation, where it registers 22 agent tools and a live simulator presentation panel.</p>
<p><strong>Does it only work with simulators, or real iPhones too?</strong>
Both. The simulator is the primary target, and <code>ios_real_start_wda</code> extends it to a physical iPhone over USB by building WebDriverAgent and tunneling control and screen ports over loopback usbmux — with identify-before-tap gating on real accounts.</p>
<p><strong>How does the agent tap the right UI element reliably?</strong>
Through the accessibility tree. <code>ios_sim_ui_tree</code> dumps the AXe-backed tree, <code>ios_sim_tap_element</code> taps by label or identifier, and OCR text taps (<code>ios_sim_find_text</code>, <code>ios_sim_tap_text</code>) serve as the fallback when the tree is empty or degenerate. It targets meaning, not guessed coordinates.</p>
<p><strong>What is SwiftUI preview hot reload, and why does it matter?</strong>
<code>ios_sim_preview</code> builds changed SwiftUI previews as a dylib and hot-swaps them into the running simulator in roughly 2–5 seconds without relaunching the app, collapsing the verify-fix loop so agents can iterate quickly.</p>
<p><strong>What security protections does it have for a real device?</strong>
The streaming engine binds loopback-only in a dedicated port range, visual bytes reach the UI only through HMAC-signed expiring URLs (10-minute capability lifetime), and every real-account tap is gated by identify-before-tap rules.</p>
<p><strong>Is the plugin production-ready?</strong>
It is at <code>0.1.0-rc.3</code> — a release candidate tested against DSH <code>0.1.1-rc.1</code>. Expect the tool set and API to evolve. Known operational notes include a 7-day WebDriverAgent signing-profile lifetime and idle stream management that stops an unused stream after 5 minutes.</p>
]]></content:encoded></item><item><title>DeepSeek Harness Agent Workflow Plugin: Orchestrate Multi-Step Agent Runs</title><link>https://baeseokjae.github.io/posts/dsh-agent-workflow-plugin-2026/</link><pubDate>Sat, 29 Aug 2026 16:01:16 +0000</pubDate><guid>https://baeseokjae.github.io/posts/dsh-agent-workflow-plugin-2026/</guid><description>The DeepSeek Harness Agent Workflow plugin visualizes multi-step agent runs as an execution chain, with token and cache stats for context-reuse analysis.</description><content:encoded><![CDATA[<p>The DeepSeek Harness Agent Workflow plugin (dsh-plugin-agent-workflow) is a standalone, installable Web UI plugin that adds a dedicated &ldquo;Workflow&rdquo; tab to DeepSeek Harness, letting you orchestrate and inspect multi-step agent runs as a clear execution chain. It renders your real recorded Session events — model requests, model responses, and tool calls — in chronological order, split by user conversation turn, with token and cache statistics that reveal exactly how context is reused across steps. Because it is read-only and non-invasive, it never adds messages, prompts, or tools to your requests, making it a trustworthy observability layer for anyone running complex, multi-step agent workflows on DeepSeek Harness.</p>
<h2 id="what-is-deepseek-harness-and-the-everything-is-a-plugin-philosophy">What is DeepSeek Harness and the &ldquo;Everything is a Plugin&rdquo; philosophy?</h2>
<p>DeepSeek Harness (dsh) is DeepSeek&rsquo;s open-source agent harness, currently in developer preview at version 0.1.0-rc.x. The project has roughly 203,000 GitHub stars, and its defining architectural idea is that &ldquo;everything is a plugin.&rdquo; Every capability — models, tools, skills, sessions, sandboxes, storage, loops, scheduling, and even the UI itself — is a swappable, recomposable plugin built on the Cordis kernel, which manages plugin mounting, unmounting, and dependencies.</p>
<p>The conceptual shift is significant compared to other harnesses. There is no separate manifest format per capability type; one plugin mechanism covers everything. A plugin is simply a JavaScript or TypeScript module that exports <code>apply(ctx, config)</code>. What that plugin registers determines what it becomes:</p>
<ul>
<li><code>ctx.tools.register()</code> makes it a tool</li>
<li><code>ctx.commands</code> makes it a slash command</li>
<li>A prompt section plus a tool makes it a skill</li>
<li>One plugin per MCP server connects an MCP server</li>
<li>A listener on <code>agent/pre-step</code> or <code>tools/pre-execute</code> makes it a lifecycle hook</li>
<li><code>ctx.llm.registerAdapter()</code> adds a new LLM provider</li>
<li><code>ctx.jobs</code> adds a background or cron job</li>
</ul>
<p>This means skills, commands, and MCP connections are all just code that calls <code>ctx</code>. The Agent Workflow plugin fits squarely into this philosophy: it is a UI plugin that registers a new tab and reads the session event stream to give you a visual, chronological view of your agent runs.</p>
<h2 id="introducing-the-agent-workflow-plugin-dsh-plugin-agent-workflow">Introducing the Agent Workflow plugin (dsh-plugin-agent-workflow)</h2>
<p>The dsh-plugin-agent-workflow plugin, developed by xuanyuanzhifeng, is a standalone installable DeepSeek Harness Web UI plugin that adds a &ldquo;Workflow&rdquo; tab alongside the built-in &ldquo;Conversation&rdquo; and &ldquo;Trajectory&rdquo; tabs. It has roughly 126 GitHub stars, is written in TypeScript, and is MIT-licensed.</p>
<p>Its core purpose is to make multi-step agent runs legible. Instead of forcing you to read raw logs or reconstruct what happened from a flat conversation, the plugin uses your user conversation turns as the entry point and renders model requests, model responses, and tool calls as a clear execution chain. The left panel organizes tasks by user conversation turn; the right panel shows model and tool calls chronologically; and the top summarizes counts and total time for the whole run.</p>
<p>The plugin is deliberately read-only and non-invasive. It visualizes real recorded Session events and never re-infers behavior from Harness source code. It adds no messages, prompts, or tools to your requests, which makes it a trust and observability layer rather than an active orchestrator. If you want to understand what your agent actually did across a long, multi-step run, this plugin gives you the ground truth.</p>
<h2 id="how-the-workflow-tab-visualizes-multi-step-agent-runs">How the Workflow tab visualizes multi-step agent runs</h2>
<p>The Workflow tab is organized around a two-panel layout that mirrors how you actually think about a run. On the left, tasks are grouped by user conversation turn — each turn is a distinct unit of work. On the right, the model and tool calls for the selected turn appear in chronological order. At the top of the view, a summary bar shows counts and total time, giving you an at-a-glance sense of how long the run took and how many steps it involved.</p>
<p>This structure is especially valuable for multi-step agent runs, where a single user request can trigger a long chain of reasoning, tool calls, subagent scheduling, and context injections. By grouping work by turn and then by call, the plugin lets you trace exactly which model call produced which tool call, and in what order. Independent scrolling for the turn list and the model-call list means you can keep a long chain in view while inspecting a specific step, and virtualized rendering keeps the interface responsive even for very long chains.</p>
<p>Because the data comes from real recorded Session events, the view is always accurate. DeepSeek Harness records everything the model sees in an append-only session log — system prompts, reasoning, tool calls and results, subagent scheduling, and context injections — and the Workflow tab reads that same event stream. You are never looking at a reconstruction; you are looking at the actual execution trace.</p>
<h2 id="inspecting-model-requests-responses-and-tool-execution-in-detail">Inspecting model requests, responses, and tool execution in detail</h2>
<p>Beyond the high-level chain, the plugin lets you drill into each individual step. Request details expose the real recorded system prompt, the provider-agnostic <code>messages[]</code> body, and the tool definitions as collapsible JSON trees with copy and zoom controls. This is useful when you need to verify exactly what context was sent to the model at a given step, or when you are debugging why a tool call was malformed.</p>
<p>Response inspection shows the reasoning, content, tool calls, and raw records for each model response. Tool execution states distinguish running, completed, and failed steps, and each tool call shows its parameters, results, duration, and an error summary when something went wrong. This makes it straightforward to spot a slow tool, a failed call, or a step where the model&rsquo;s reasoning diverged from what you expected.</p>
<p>For a multi-step agent run, this level of detail is the difference between guessing and knowing. When a run fails or produces an unexpected result, you can open the Workflow tab, find the exact step where things went sideways, and inspect the request, the response, and the tool execution state in one place.</p>
<h2 id="token-and-cache-statistics-for-context-reuse-analysis">Token and cache statistics for context-reuse analysis</h2>
<p>One of the most practical features of the Agent Workflow plugin is its token and cache statistics. For each step, the plugin splits token usage into input, uncached input, cache-read, cache-write, and output tokens. This breakdown is essential for context-reuse and cost analysis.</p>
<p>DeepSeek&rsquo;s pricing model rewards context reuse: tokens read from cache are dramatically cheaper than uncached input tokens. By seeing how many tokens were cache-read versus uncached input, you can tell whether your workflow is actually reusing context effectively across steps. A high cache-read ratio means your agent is building on prior context efficiently; a high uncached-input ratio suggests you are re-sending context that could be cached or trimmed.</p>
<p>This is particularly relevant for multi-step agent runs, where context accumulates across turns. The plugin&rsquo;s per-step breakdown lets you identify which steps are the most expensive, whether context is being reused as intended, and where you might restructure your workflow to improve cache hits and reduce cost.</p>
<h2 id="installing-the-plugin-local-tgz-and-github-install-paths">Installing the plugin (local .tgz and GitHub install paths)</h2>
<p>Installing the Agent Workflow plugin follows the standard DeepSeek Harness plugin workflow. The plugin is compatible only with <a href="mailto:dsh@0.1.0-rc.8">dsh@0.1.0-rc.8</a> (0.1.x), so you must be on that version. You can install from a local <code>.tgz</code> package or directly from GitHub:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-bash" data-lang="bash"><span style="display:flex;"><span>npx @deepseek-ai/dsh@0.1.0-rc.8 plugin --profile web add &lt;path-to.tgz&gt; --workspace-root
</span></span></code></pre></div><p>or, for a GitHub install pinned to a release tag:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-bash" data-lang="bash"><span style="display:flex;"><span>npx @deepseek-ai/dsh@0.1.0-rc.8 plugin --profile web add github:&lt;owner&gt;/&lt;repo&gt;#v0.1.1 --workspace-root
</span></span></code></pre></div><p>To remove the plugin, use <code>plugin remove</code>. The broader ecosystem uses a similar pattern — for example, the independent DSH Plugin Registry (dshplugin.app) documents installs as <code>dsh plugin --profile web add github:&lt;owner&gt;/&lt;repo&gt;</code>. Because DSH is pre-release, always pin your plugin version to the matching RC version of the harness.</p>
<h2 id="compatibility-versioning-and-known-limitations">Compatibility, versioning, and known limitations</h2>
<p>The most important thing to understand about the Agent Workflow plugin is that it is compatible only with <a href="mailto:dsh@0.1.0-rc.8">dsh@0.1.0-rc.8</a> (0.1.x). DeepSeek Harness is pre-release, which means client interfaces can change across RC versions. A plugin built against one RC may break on the next, so you must track plugin versions against your harness version.</p>
<p>The plugin is also read-only by design. It does not orchestrate agent runs; it visualizes them. If you need active orchestration — assigning work, managing dependencies, or running multi-model teams — you will want a different tool (see the comparison below). And because it reads the session event stream, its fidelity depends on the harness recording those events correctly, which is a core DeepSeek Harness guarantee but still worth noting for pre-release software.</p>
<h2 id="how-it-compares-to-other-deepseek-harness-workflow-plugins">How it compares to other DeepSeek Harness workflow plugins</h2>
<p>The DeepSeek Harness plugin ecosystem has several workflow-oriented tools, and they serve different purposes. Here is how the main ones compare:</p>
<table>
  <thead>
      <tr>
          <th>Plugin</th>
          <th>Primary purpose</th>
          <th>Stars</th>
          <th>Language</th>
          <th>License</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>dsh-plugin-agent-workflow</td>
          <td>Per-turn execution chain visualization, read-only</td>
          <td>~126</td>
          <td>TypeScript</td>
          <td>MIT</td>
      </tr>
      <tr>
          <td>dsh-agent-team-gui</td>
          <td>Persistent multi-model agent teams with orchestration</td>
          <td>~159</td>
          <td>TypeScript</td>
          <td>MIT</td>
      </tr>
      <tr>
          <td>dsh-harness-one</td>
          <td>Visual multi-agent DAG orchestrator with live execution</td>
          <td>~17</td>
          <td>JavaScript</td>
          <td>—</td>
      </tr>
  </tbody>
</table>
<p>The Agent Workflow plugin is the observability choice: it shows you what happened, step by step, with token and cache stats. The dsh-agent-team-gui plugin is the orchestration choice: it lets you build persistent, reusable multi-model teams where each member gets its own model, role, fallback route, token limit, and tool policy, with dynamic workflow planning and bounded DAG execution. The dsh-harness-one plugin is a visual AI workflow orchestrator that builds multi-agent DAGs with live execution, recovery, and Feishu integration.</p>
<p>If your goal is to understand and optimize a single agent&rsquo;s multi-step run, the Agent Workflow plugin is the right fit. If your goal is to coordinate multiple specialized agents, look at dsh-agent-team-gui or dsh-harness-one. Many teams use both: an orchestrator to run the work, and the Workflow tab to inspect and optimize it.</p>
<h2 id="local-development-and-packaging-pnpm-typecheck-test-pack">Local development and packaging (pnpm, typecheck, test, pack)</h2>
<p>If you want to build or modify the plugin yourself, the development workflow is standard for a TypeScript plugin. The project requires Node ^22.19.0 or &gt;=24.0.0 and pnpm 11. The typical loop is:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-bash" data-lang="bash"><span style="display:flex;"><span>pnpm install
</span></span><span style="display:flex;"><span>pnpm typecheck
</span></span><span style="display:flex;"><span>pnpm test
</span></span><span style="display:flex;"><span>pnpm pack
</span></span></code></pre></div><p><code>pnpm pack</code> produces the <code>.tgz</code> file you can install with the <code>plugin add</code> command. Because the plugin is MIT-licensed and open source, you can fork it, adjust the visualization to your needs, and repackage it for your own workspace. The same &ldquo;everything is a plugin&rdquo; philosophy means your modifications remain a self-contained plugin rather than a fork of the harness itself.</p>
<h2 id="faq-and-troubleshooting">FAQ and troubleshooting</h2>
<p><strong>Is the DeepSeek Harness Agent Workflow plugin read-only?</strong>
Yes. It visualizes real recorded Session events and adds no messages, prompts, or tools to your model requests. It is a pure observability layer, so it cannot alter the behavior of your agent runs.</p>
<p><strong>Which version of DeepSeek Harness does the plugin require?</strong>
The plugin is compatible only with <a href="mailto:dsh@0.1.0-rc.8">dsh@0.1.0-rc.8</a> (0.1.x). Because DeepSeek Harness is pre-release, client interfaces may change across RC versions, so you must match the plugin version to your harness version.</p>
<p><strong>How is the Workflow tab different from the built-in Trajectory tab?</strong>
The Trajectory tab inspects the session log by source, while the Workflow tab organizes the same event stream by user conversation turn and renders model requests, responses, and tool calls as a chronological execution chain with token and cache statistics.</p>
<p><strong>Can the plugin help me reduce token costs?</strong>
Yes. It splits token usage into input, uncached input, cache-read, cache-write, and output tokens per step, so you can identify where context is not being reused and restructure your workflow to improve cache hits and lower cost.</p>
<p><strong>Does the plugin orchestrate multi-agent teams?</strong>
No. It visualizes and inspects runs. For active orchestration of multi-model teams, use a tool like dsh-agent-team-gui or dsh-harness-one, which handle team composition, dependency planning, and DAG execution.</p>
]]></content:encoded></item><item><title>DeepSeek Harness Plugin Hub: The 4000+ Plugin Marketplace Explained</title><link>https://baeseokjae.github.io/posts/dsh-plugin-hub-marketplace-2026/</link><pubDate>Sat, 29 Aug 2026 10:01:21 +0000</pubDate><guid>https://baeseokjae.github.io/posts/dsh-plugin-hub-marketplace-2026/</guid><description>The DeepSeek Harness plugin hub grew from 0 to 7,740 plugins in under 3 weeks. Here&amp;#39;s how the &amp;#39;everything is a plugin&amp;#39; marketplace works, how to install plugins, and what to check before you do.</description><content:encoded><![CDATA[<p>The DeepSeek Harness plugin hub is a decentralized, source-backed marketplace where every capability of the harness — models, tools, skills, sessions, sandboxes, storage, loops, scheduling, and the UI — is a swappable plugin. It grew from 0 to 7,740 plugins in under three weeks, making it one of the fastest-growing plugin ecosystems ever built. This guide explains how the hub works, how to install and publish plugins, and what to check before you trust one.</p>
<h2 id="what-is-the-deepseek-harness-plugin-hub">What Is the DeepSeek Harness Plugin Hub?</h2>
<p>The DeepSeek Harness plugin hub is the marketplace for plugins that extend the DeepSeek Harness, an open-source AI agent framework released in developer preview around August 12–13, 2026. Unlike a traditional app store with a central gatekeeper, the hub has no single owner: plugins are indexed directly from GitHub repositories tagged with <code>dsh-plugin</code>, which makes it a decentralized, source-backed marketplace.</p>
<p>The official project page describes the philosophy in three words: &ldquo;Everything is a plugin.&rdquo; Every capability you might want to change — the model that powers the agent, the tools it can call, the skills it has learned, the session and sandbox it runs in, the storage it uses, the loops and scheduling that drive it, and even the user interface — is a plugin that can be swapped or recomposed without touching source code.</p>
<p>Because the harness is in developer preview, the ecosystem is still settling. Multiple independent registries have sprung up to index plugins, each with different counts and features. The hub is best understood not as one website but as a network of registries, directories, and package indexes that all point back to the same GitHub-tagged plugin repos.</p>
<h2 id="the-everything-is-a-plugin-architecture-explained">The &ldquo;Everything Is a Plugin&rdquo; Architecture Explained</h2>
<p>The core idea behind the DeepSeek Harness is that you should never have to fork the framework to extend it. Instead, the harness is built on the Cordis kernel, which manages plugin mounting, unmounting, and dependency resolution. The design is described in the paper &ldquo;A Programming Paradigm for Spatiotemporal Composability&rdquo; (arXiv:2608.25512).</p>
<p>In practice, this means you compose the harness with configuration rather than code. You select, swap, or extend any capability by editing configuration, and the Cordis kernel handles the rest — loading the plugin, resolving its dependencies, and mounting it into the running harness.</p>
<p>This architecture has three practical consequences:</p>
<ol>
<li><strong>You extend by composing, not forking.</strong> To add a capability, you add a plugin. To change a capability, you swap one plugin for another. The harness core stays untouched.</li>
<li><strong>Capabilities are modular and recomposable.</strong> Because each capability slot is independent, you can mix and match — a different model, a different storage backend, a different UI — without breaking the rest.</li>
<li><strong>The barrier to contribution is low.</strong> Anyone can publish a plugin by tagging a GitHub repo with <code>dsh-plugin</code>. There is no approval process, which is both the ecosystem&rsquo;s greatest strength and its biggest risk.</li>
</ol>
<p>The quickest way to see this in action is to run the official quick start: <code>npx @deepseek-ai/dsh web</code>, which launches the Web UI at <code>http://127.0.0.1:3080</code> by default.</p>
<h2 id="the-9-capability-slots-what-each-one-does">The 9 Capability Slots: What Each One Does</h2>
<p>The DeepSeek Harness defines nine swappable capability slots. Each slot is a category of plugin that fills a specific role in the harness. Understanding these slots is the key to navigating the marketplace, because every plugin you see belongs to one of them.</p>
<table>
  <thead>
      <tr>
          <th>Capability Slot</th>
          <th>What It Controls</th>
          <th>Example Use</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td><strong>Model</strong></td>
          <td>The underlying language model powering the agent</td>
          <td>Swap in a different model provider or a fine-tuned variant</td>
      </tr>
      <tr>
          <td><strong>Tools</strong></td>
          <td>The functions the agent can call</td>
          <td>Add web search, code execution, or API integrations</td>
      </tr>
      <tr>
          <td><strong>Skills</strong></td>
          <td>Learned capabilities and workflows</td>
          <td>Install a pre-built skill for a specific domain</td>
      </tr>
      <tr>
          <td><strong>Session</strong></td>
          <td>How conversations and state are managed</td>
          <td>Change session persistence or context handling</td>
      </tr>
      <tr>
          <td><strong>Sandbox</strong></td>
          <td>The execution environment for code</td>
          <td>Tighten or loosen isolation for running untrusted code</td>
      </tr>
      <tr>
          <td><strong>Storage</strong></td>
          <td>Where data and artifacts are kept</td>
          <td>Swap local storage for cloud or database backends</td>
      </tr>
      <tr>
          <td><strong>Loop</strong></td>
          <td>How the agent iterates on tasks</td>
          <td>Change the reasoning or retry loop behavior</td>
      </tr>
      <tr>
          <td><strong>Scheduling</strong></td>
          <td>When and how tasks are triggered</td>
          <td>Add cron-style or event-driven scheduling</td>
      </tr>
      <tr>
          <td><strong>UI</strong></td>
          <td>The user interface</td>
          <td>Replace the default web UI with a custom interface</td>
      </tr>
  </tbody>
</table>
<p>The DSH Plugin Directory (dshplugin.online) organizes its 7,740 plugins across exactly these nine slots. The independent DSH Plugin Registry (dshplugin.app) uses a slightly different set of eight categories, which is one reason the two sites report different totals.</p>
<h2 id="how-big-is-the-marketplace-growth-from-0-to-7740-plugins">How Big Is the Marketplace? Growth From 0 to 7,740 Plugins</h2>
<p>The growth of the DeepSeek Harness plugin ecosystem is remarkable by any measure. The DSH Plugin Directory tracked the following daily plugin counts:</p>
<table>
  <thead>
      <tr>
          <th>Date (Aug 2026)</th>
          <th>Plugins Indexed</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>Aug 12</td>
          <td>0</td>
      </tr>
      <tr>
          <td>Aug 13</td>
          <td>202</td>
      </tr>
      <tr>
          <td>Aug 14</td>
          <td>1,635</td>
      </tr>
      <tr>
          <td>Aug 15</td>
          <td>2,713</td>
      </tr>
      <tr>
          <td>Aug 16</td>
          <td>3,744</td>
      </tr>
      <tr>
          <td>Aug 17</td>
          <td>4,473</td>
      </tr>
      <tr>
          <td>Aug 18</td>
          <td>4,931</td>
      </tr>
      <tr>
          <td>Aug 19</td>
          <td>5,329</td>
      </tr>
      <tr>
          <td>Aug 20</td>
          <td>6,055</td>
      </tr>
      <tr>
          <td>Aug 21</td>
          <td>6,466</td>
      </tr>
      <tr>
          <td>Aug 22</td>
          <td>6,651</td>
      </tr>
      <tr>
          <td>Aug 23</td>
          <td>6,842</td>
      </tr>
      <tr>
          <td>Aug 24</td>
          <td>7,000</td>
      </tr>
      <tr>
          <td>Aug 25</td>
          <td>7,164</td>
      </tr>
      <tr>
          <td>Aug 26</td>
          <td>7,335</td>
      </tr>
      <tr>
          <td>Aug 27</td>
          <td>7,472</td>
      </tr>
      <tr>
          <td>Aug 28</td>
          <td>7,666</td>
      </tr>
      <tr>
          <td>Aug 29</td>
          <td>7,740</td>
      </tr>
  </tbody>
</table>
<p>That is roughly 1,000+ plugins per day in the first week, and the total reached 7,740 plugins from 4,796 authors by August 29, 2026. For context, the official <code>deepseek-ai/deepseek-harness</code> GitHub repository itself has 202,666 stars and 23,326 forks under an MIT license — an extraordinary adoption rate for a project that only launched in mid-August.</p>
<p>The independent DSH Plugin Registry (dshplugin.app) reports a smaller but still substantial 1,545 plugins across 8 categories. The difference in counts is not a contradiction; it reflects that the two registries use different indexing methods, category schemes, and update schedules. The largest categories by plugin count are Developer Tools (1,446), UI &amp; Productivity (942), Terminal &amp; TUI (667), and Security &amp; Policy (604).</p>
<h2 id="the-major-plugin-registries-and-directories-and-how-they-differ">The Major Plugin Registries and Directories (and How They Differ)</h2>
<p>Because the DeepSeek Harness plugin hub is decentralized, there is no single canonical list. Instead, several independent services index the same GitHub-tagged plugin repos, and they differ in scope, features, and counts. Here is how the main ones compare:</p>
<table>
  <thead>
      <tr>
          <th>Registry</th>
          <th>What It Is</th>
          <th>Plugin Count</th>
          <th>Key Feature</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td><strong>dshplugin.online</strong> (DSH Plugin Directory)</td>
          <td>&ldquo;Every DeepSeek Harness Plugin&rdquo;</td>
          <td>7,740</td>
          <td>Indexes all 9 capability slots from 4,796 authors; tracks daily growth</td>
      </tr>
      <tr>
          <td><strong>dshplugin.app</strong> (DSH Plugin Registry)</td>
          <td>&ldquo;Find and compare community plugins&rdquo;</td>
          <td>1,545</td>
          <td>Source-backed capabilities, install paths, security signals, compatibility evidence</td>
      </tr>
      <tr>
          <td><strong>dsh-index.xlings.org</strong> (dsh index)</td>
          <td>Plugins and Agents package index</td>
          <td>119 packages / 188 versions</td>
          <td>Hosts ready-to-run Agents (Agent = Harness + Plugins)</td>
      </tr>
      <tr>
          <td><strong>deepseek-ai/deepseek-harness</strong> (GitHub)</td>
          <td>Official repository</td>
          <td>202,666 stars</td>
          <td>The source of truth for the harness itself</td>
      </tr>
  </tbody>
</table>
<p>The practical takeaway is that you should treat these as complementary tools. Use the directory (dshplugin.online) to see the full breadth of the ecosystem, use the registry (dshplugin.app) to compare plugins with security and compatibility signals, and use the package index (dsh-index) when you want a ready-to-run Agent rather than assembling plugins by hand.</p>
<h2 id="how-to-install-a-deepseek-harness-plugin-step-by-step">How to Install a DeepSeek Harness Plugin (Step by Step)</h2>
<p>Installing a plugin is straightforward and is done through the <code>dsh</code> command-line tool. The general pattern is:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-bash" data-lang="bash"><span style="display:flex;"><span>dsh plugin --profile &lt;profile&gt; add &lt;source&gt;
</span></span></code></pre></div><p>Here is a step-by-step walkthrough:</p>
<ol>
<li><strong>Install the harness.</strong> If you have not already, install the DeepSeek Harness. The official quick start is <code>npx @deepseek-ai/dsh web</code>, which launches the Web UI.</li>
<li><strong>Find a plugin.</strong> Browse one of the registries (dshplugin.app or dshplugin.online) to find a plugin that fills the capability slot you need. Note its source repository.</li>
<li><strong>Add the plugin to a profile.</strong> Use the <code>dsh plugin</code> command with a profile name and the plugin source:
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-bash" data-lang="bash"><span style="display:flex;"><span>dsh plugin --profile my-agent add &lt;source&gt;
</span></span></code></pre></div></li>
<li><strong>Verify the plugin mounted.</strong> Check the harness output or UI to confirm the plugin loaded without dependency errors.</li>
<li><strong>Compose with configuration.</strong> If you want to swap or extend capabilities, edit the harness configuration rather than the source code. The Cordis kernel handles mounting and dependency resolution.</li>
</ol>
<p>For ready-to-run Agents, the dsh-index package index supports installing a complete agent profile directly, for example <code>dsh:agent-web-coding</code>, which bundles the harness plus the plugins it needs. This is the fastest way to get a working agent without assembling plugins one by one.</p>
<h2 id="how-to-publish-your-own-plugin-to-the-hub">How to Publish Your Own Plugin to the Hub</h2>
<p>Publishing a plugin to the DeepSeek Harness plugin hub is deliberately low-friction, which is why the ecosystem grew so fast. The core requirement is to tag your GitHub repository with <code>dsh-plugin</code>. Once tagged, the independent registries and directories pick it up and index it.</p>
<p>The basic steps are:</p>
<ol>
<li><strong>Build your plugin.</strong> Create a plugin that fills one of the nine capability slots (Model, Tools, Skills, Session, Sandbox, Storage, Loop, Scheduling, or UI).</li>
<li><strong>Tag your repository.</strong> Add the <code>dsh-plugin</code> tag to your GitHub repo so the indexing services can find it.</li>
<li><strong>Provide good metadata.</strong> Include a clear description, the capability slot it fills, and any dependency or compatibility information. Registries like dshplugin.app surface this metadata as &ldquo;source-backed capabilities&rdquo; and &ldquo;compatibility evidence.&rdquo;</li>
<li><strong>Consider publishing to a package index.</strong> If you want your plugin to be installable as part of a ready-to-run Agent, you can also publish it to the dsh-index package index.</li>
</ol>
<p>Because there is no central approval process, the quality and safety of plugins vary widely. That makes the metadata you provide — and the security signals surfaced by registries — especially important for building trust with users.</p>
<h2 id="security-and-compatibility-what-to-check-before-installing">Security and Compatibility: What to Check Before Installing</h2>
<p>The decentralized nature of the DeepSeek Harness plugin hub is a double-edged sword. Anyone can publish, which means anyone can publish something malicious, broken, or abandoned. Before you install a plugin, check the following:</p>
<ul>
<li><strong>Dependency signals.</strong> Registries like dshplugin.app surface dependency information. A plugin with many or unusual dependencies is harder to audit and more likely to break.</li>
<li><strong>License signals.</strong> Check the plugin&rsquo;s license. The official harness is MIT-licensed, but community plugins may use other licenses with different obligations.</li>
<li><strong>Lifecycle-script signals.</strong> Some plugins run scripts during installation or execution. Be cautious with plugins that execute arbitrary lifecycle scripts, especially if you run them in a privileged environment.</li>
<li><strong>Compatibility evidence.</strong> The harness is in developer preview, and the official repo warns: &ldquo;THERE WILL BE COMPATIBILITY-BREAKING CHANGES.&rdquo; A plugin built for one version may not work on the next. Check the plugin&rsquo;s stated compatibility and pin versions where possible.</li>
<li><strong>Repository facts.</strong> Look at the plugin&rsquo;s source repository — its star count, activity, and maintainer history. A plugin with a few stars and no recent commits is a higher risk than an actively maintained one.</li>
</ul>
<p>The security-first posture of the registries exists precisely because the ecosystem is open. Treat every plugin as untrusted until you have reviewed its source and signals.</p>
<h2 id="top-community-plugins-worth-trying">Top Community Plugins Worth Trying</h2>
<p>A few community plugins have already risen to prominence in the young ecosystem. According to the DSH Plugin Registry, the most popular include:</p>
<ul>
<li><strong>dsh-routing-suite</strong> (6.9k stars) — A routing suite that helps direct agent requests to the right model or tool, useful for complex multi-step workflows.</li>
<li><strong>DSH Web UI</strong> (6.3k stars) — A popular alternative web interface for the harness, demonstrating the power of the swappable UI slot.</li>
<li><strong>ModLens</strong> (3.7k stars) — A vision plugin that adds image understanding capabilities to the harness.</li>
</ul>
<p>These plugins are worth trying because they are the most battle-tested in the ecosystem so far. Their high star counts reflect real usage and community validation, which is a useful signal in a marketplace where anyone can publish.</p>
<h2 id="the-future-of-the-deepseek-harness-plugin-ecosystem">The Future of the DeepSeek Harness Plugin Ecosystem</h2>
<p>The trajectory of the DeepSeek Harness plugin hub suggests several likely developments. First, the growth rate — 0 to 7,740 plugins in under three weeks — will almost certainly slow as the ecosystem matures, but the sheer volume already makes curation and discovery a real problem. Expect better search, filtering, and recommendation features across the registries.</p>
<p>Second, the fragmentation across multiple registries (dshplugin.app, dshplugin.online, dsh-index) is likely to consolidate or at least standardize. Differing counts and category schemes confuse users, and there is pressure for a shared canonical index.</p>
<p>Third, security will become more important as the plugin count grows. The registries already surface security signals, and this is likely to deepen — possibly with automated scanning, signing, or reputation systems.</p>
<p>Finally, the shift toward ready-to-run Agents (Agent = Harness + Plugins) is the most interesting trend. If installing a complete agent profile becomes the default, the plugin hub evolves from a component marketplace into a full agent marketplace, dramatically lowering the barrier to using DeepSeek Harness for real work.</p>
<h2 id="faq-deepseek-harness-plugin-hub">FAQ: DeepSeek Harness Plugin Hub</h2>
<p><strong>What is the DeepSeek Harness plugin hub?</strong>
The DeepSeek Harness plugin hub is the decentralized marketplace for plugins that extend the DeepSeek Harness AI agent framework. Plugins are indexed from GitHub repos tagged <code>dsh-plugin</code>, and every capability of the harness — models, tools, skills, sessions, sandboxes, storage, loops, scheduling, and UI — is a swappable plugin.</p>
<p><strong>How many plugins are in the DeepSeek Harness plugin hub?</strong>
Counts vary by registry. The DSH Plugin Directory (dshplugin.online) indexed 7,740 plugins from 4,796 authors as of August 29, 2026, while the DSH Plugin Registry (dshplugin.app) reported 1,545 plugins. The difference reflects different indexing methods and category schemes.</p>
<p><strong>How do I install a DeepSeek Harness plugin?</strong>
Use the <code>dsh</code> command-line tool with the pattern <code>dsh plugin --profile &lt;profile&gt; add &lt;source&gt;</code>. For ready-to-run agents, you can install a complete profile from the dsh-index package index, such as <code>dsh:agent-web-coding</code>.</p>
<p><strong>Is the DeepSeek Harness plugin hub safe?</strong>
The hub is decentralized with no central approval, so safety depends on the plugin. Check dependency, license, lifecycle-script, and compatibility signals surfaced by registries, and review the plugin&rsquo;s source repository before installing.</p>
<p><strong>How do I publish my own plugin to the hub?</strong>
Tag your GitHub repository with <code>dsh-plugin</code>. The independent registries and directories will index it automatically. Provide clear metadata about the capability slot it fills, its dependencies, and its compatibility to help users trust it.</p>
]]></content:encoded></item><item><title>TencentCloud AgentObs SDK for DeepSeek Harness: Zero-Collector GenAI Trace Observability</title><link>https://baeseokjae.github.io/posts/tencentcloud-agentobs-dsh-genai-traces/</link><pubDate>Thu, 20 Aug 2026 07:02:10 +0000</pubDate><guid>https://baeseokjae.github.io/posts/tencentcloud-agentobs-dsh-genai-traces/</guid><description>Add deepseek harness observability without an OTLP collector: the TencentCloud AgentObs DSH plugin pushes GenAI traces straight to CLS.</description><content:encoded><![CDATA[<p>DeepSeek Harness observability usually means standing up an OpenTelemetry collector, a tracing backend, and code instrumentation. The TencentCloud AgentObs SDK for DSH eliminates all of that: it is a DeepSeek Harness plugin that observes the harness&rsquo;s native session, agent-loop, LLM-stream, and tool lifecycles and uploads GenAI trace spans directly to Tencent Cloud Log Service (CLS) using Protobuf. There is no OTLP collector, no sidecar, and no extra service to deploy. If you already run agents on Tencent Cloud, this is the fastest path from &ldquo;where did my agent spend its tokens?&rdquo; to a dashboards-and-alerts answer.</p>
<h2 id="what-is-the-tencentcloud-agentobs-sdk-for-deepseek-harness">What is the TencentCloud AgentObs SDK for DeepSeek Harness?</h2>
<p>The TencentCloud AgentObs SDK for DSH (<code>tencentcloud-agentobs-sdk-dsh</code>) is a first-party observability plugin for DeepSeek Harness. Rather than treating DSH as a black box that you instrument from the outside, it hooks directly into the harness&rsquo;s own lifecycle events. Those events — a session starting, an agent being invoked, each ReAct round, an LLM stream, and every tool execution — are converted into spans that follow Tencent Cloud&rsquo;s AI Agent observability specification, then shipped to CLS through the official <code>tencentcloud-cls-sdk-js</code> transport.</p>
<p>The key architectural decision is that the plugin is transport-native to a cloud vendor&rsquo;s log service. Where most DSH observability plugins speak OpenTelemetry and require an OTLP endpoint, this one talks Protobuf directly to Tencent Cloud Log Service. For teams already paying for CLS, that removes an entire class of operational burden.</p>
<h2 id="how-does-dsh-genai-trace-observability-work-without-a-collector">How does DSH GenAI trace observability work without a collector?</h2>
<p>The zero-collector architecture is the plugin&rsquo;s defining feature. The data path is short and self-contained:</p>
<ol>
<li>DeepSeek Harness fires native lifecycle events during a session.</li>
<li>The AgentObs plugin&rsquo;s CLS Trace Coordinator receives those events.</li>
<li>Each event is normalized into Tencent Cloud&rsquo;s 5-layer span model.</li>
<li>Spans are batched and serialized with <code>tencentcloud-cls-sdk-js</code> (Protobuf).</li>
<li>Batches are uploaded directly to a CLS topic over HTTPS.</li>
</ol>
<p>There is no OpenTelemetry Collector to run, no Jaeger or SigNoz backend to maintain, and no sidecar container to schedule. Contrast that with the standard OpenTelemetry DeepSeek approach, which requires you to deploy an OTLP receiver and run <code>opentelemetry-distro</code> instrumentation on top of an OpenAI-compatible SDK. That approach is portable and vendor-neutral, but it is also infrastructure you must operate. The Tencent plugin trades portability for a dramatically simpler deploy.</p>
<h2 id="what-is-the-5-layer-span-model-for-ai-agent-observability">What is the 5-layer span model for AI agent observability?</h2>
<p>Tencent Cloud&rsquo;s AI Agent observability spec organizes traces into a five-layer span hierarchy: entry, agent, step, chat, and tool. The plugin maps DSH&rsquo;s native lifecycle onto that model, so a single DSH turn produces a nested trace:</p>



<div class="goat svg-container ">
  
    <svg
      xmlns="http://www.w3.org/2000/svg"
      font-family="Menlo,Lucida Console,monospace"
      
        viewBox="0 0 336 89"
      >
      <g transform='translate(8,16)'>
<text text-anchor='middle' x='0' y='4' fill='currentColor' style='font-size:1em'>E</text>
<text text-anchor='middle' x='0' y='20' fill='currentColor' style='font-size:1em'>└</text>
<text text-anchor='middle' x='8' y='4' fill='currentColor' style='font-size:1em'>N</text>
<text text-anchor='middle' x='8' y='20' fill='currentColor' style='font-size:1em'>─</text>
<text text-anchor='middle' x='16' y='4' fill='currentColor' style='font-size:1em'>T</text>
<text text-anchor='middle' x='16' y='20' fill='currentColor' style='font-size:1em'>─</text>
<text text-anchor='middle' x='24' y='4' fill='currentColor' style='font-size:1em'>R</text>
<text text-anchor='middle' x='32' y='4' fill='currentColor' style='font-size:1em'>Y</text>
<text text-anchor='middle' x='32' y='20' fill='currentColor' style='font-size:1em'>A</text>
<text text-anchor='middle' x='32' y='36' fill='currentColor' style='font-size:1em'>└</text>
<text text-anchor='middle' x='40' y='20' fill='currentColor' style='font-size:1em'>G</text>
<text text-anchor='middle' x='40' y='36' fill='currentColor' style='font-size:1em'>─</text>
<text text-anchor='middle' x='48' y='20' fill='currentColor' style='font-size:1em'>E</text>
<text text-anchor='middle' x='48' y='36' fill='currentColor' style='font-size:1em'>─</text>
<text text-anchor='middle' x='56' y='20' fill='currentColor' style='font-size:1em'>N</text>
<text text-anchor='middle' x='64' y='20' fill='currentColor' style='font-size:1em'>T</text>
<text text-anchor='middle' x='64' y='36' fill='currentColor' style='font-size:1em'>S</text>
<text text-anchor='middle' x='64' y='52' fill='currentColor' style='font-size:1em'>├</text>
<text text-anchor='middle' x='64' y='68' fill='currentColor' style='font-size:1em'>└</text>
<text text-anchor='middle' x='72' y='36' fill='currentColor' style='font-size:1em'>T</text>
<text text-anchor='middle' x='72' y='52' fill='currentColor' style='font-size:1em'>─</text>
<text text-anchor='middle' x='72' y='68' fill='currentColor' style='font-size:1em'>─</text>
<text text-anchor='middle' x='80' y='20' fill='currentColor' style='font-size:1em'>(</text>
<text text-anchor='middle' x='80' y='36' fill='currentColor' style='font-size:1em'>E</text>
<text text-anchor='middle' x='80' y='52' fill='currentColor' style='font-size:1em'>─</text>
<text text-anchor='middle' x='80' y='68' fill='currentColor' style='font-size:1em'>─</text>
<text text-anchor='middle' x='88' y='20' fill='currentColor' style='font-size:1em'>i</text>
<text text-anchor='middle' x='88' y='36' fill='currentColor' style='font-size:1em'>P</text>
<text text-anchor='middle' x='96' y='20' fill='currentColor' style='font-size:1em'>n</text>
<text text-anchor='middle' x='96' y='52' fill='currentColor' style='font-size:1em'>C</text>
<text text-anchor='middle' x='96' y='68' fill='currentColor' style='font-size:1em'>T</text>
<text text-anchor='middle' x='104' y='20' fill='currentColor' style='font-size:1em'>v</text>
<text text-anchor='middle' x='104' y='36' fill='currentColor' style='font-size:1em'>(</text>
<text text-anchor='middle' x='104' y='52' fill='currentColor' style='font-size:1em'>H</text>
<text text-anchor='middle' x='104' y='68' fill='currentColor' style='font-size:1em'>O</text>
<text text-anchor='middle' x='112' y='20' fill='currentColor' style='font-size:1em'>o</text>
<text text-anchor='middle' x='112' y='36' fill='currentColor' style='font-size:1em'>r</text>
<text text-anchor='middle' x='112' y='52' fill='currentColor' style='font-size:1em'>A</text>
<text text-anchor='middle' x='112' y='68' fill='currentColor' style='font-size:1em'>O</text>
<text text-anchor='middle' x='120' y='20' fill='currentColor' style='font-size:1em'>k</text>
<text text-anchor='middle' x='120' y='36' fill='currentColor' style='font-size:1em'>e</text>
<text text-anchor='middle' x='120' y='52' fill='currentColor' style='font-size:1em'>T</text>
<text text-anchor='middle' x='120' y='68' fill='currentColor' style='font-size:1em'>L</text>
<text text-anchor='middle' x='128' y='20' fill='currentColor' style='font-size:1em'>e</text>
<text text-anchor='middle' x='128' y='36' fill='currentColor' style='font-size:1em'>a</text>
<text text-anchor='middle' x='136' y='20' fill='currentColor' style='font-size:1em'>_</text>
<text text-anchor='middle' x='136' y='36' fill='currentColor' style='font-size:1em'>c</text>
<text text-anchor='middle' x='136' y='52' fill='currentColor' style='font-size:1em'>(</text>
<text text-anchor='middle' x='136' y='68' fill='currentColor' style='font-size:1em'>(</text>
<text text-anchor='middle' x='144' y='20' fill='currentColor' style='font-size:1em'>a</text>
<text text-anchor='middle' x='144' y='36' fill='currentColor' style='font-size:1em'>t</text>
<text text-anchor='middle' x='144' y='52' fill='currentColor' style='font-size:1em'>c</text>
<text text-anchor='middle' x='144' y='68' fill='currentColor' style='font-size:1em'>e</text>
<text text-anchor='middle' x='152' y='20' fill='currentColor' style='font-size:1em'>g</text>
<text text-anchor='middle' x='152' y='52' fill='currentColor' style='font-size:1em'>h</text>
<text text-anchor='middle' x='152' y='68' fill='currentColor' style='font-size:1em'>x</text>
<text text-anchor='middle' x='160' y='20' fill='currentColor' style='font-size:1em'>e</text>
<text text-anchor='middle' x='160' y='36' fill='currentColor' style='font-size:1em'>r</text>
<text text-anchor='middle' x='160' y='52' fill='currentColor' style='font-size:1em'>a</text>
<text text-anchor='middle' x='160' y='68' fill='currentColor' style='font-size:1em'>e</text>
<text text-anchor='middle' x='168' y='20' fill='currentColor' style='font-size:1em'>n</text>
<text text-anchor='middle' x='168' y='36' fill='currentColor' style='font-size:1em'>o</text>
<text text-anchor='middle' x='168' y='52' fill='currentColor' style='font-size:1em'>t</text>
<text text-anchor='middle' x='168' y='68' fill='currentColor' style='font-size:1em'>c</text>
<text text-anchor='middle' x='176' y='20' fill='currentColor' style='font-size:1em'>t</text>
<text text-anchor='middle' x='176' y='36' fill='currentColor' style='font-size:1em'>u</text>
<text text-anchor='middle' x='176' y='68' fill='currentColor' style='font-size:1em'>u</text>
<text text-anchor='middle' x='184' y='20' fill='currentColor' style='font-size:1em'>)</text>
<text text-anchor='middle' x='184' y='36' fill='currentColor' style='font-size:1em'>n</text>
<text text-anchor='middle' x='184' y='52' fill='currentColor' style='font-size:1em'>m</text>
<text text-anchor='middle' x='184' y='68' fill='currentColor' style='font-size:1em'>t</text>
<text text-anchor='middle' x='192' y='36' fill='currentColor' style='font-size:1em'>d</text>
<text text-anchor='middle' x='192' y='52' fill='currentColor' style='font-size:1em'>o</text>
<text text-anchor='middle' x='192' y='68' fill='currentColor' style='font-size:1em'>e</text>
<text text-anchor='middle' x='200' y='36' fill='currentColor' style='font-size:1em'>_</text>
<text text-anchor='middle' x='200' y='52' fill='currentColor' style='font-size:1em'>d</text>
<text text-anchor='middle' x='200' y='68' fill='currentColor' style='font-size:1em'>_</text>
<text text-anchor='middle' x='208' y='36' fill='currentColor' style='font-size:1em'>N</text>
<text text-anchor='middle' x='208' y='52' fill='currentColor' style='font-size:1em'>e</text>
<text text-anchor='middle' x='208' y='68' fill='currentColor' style='font-size:1em'>t</text>
<text text-anchor='middle' x='216' y='36' fill='currentColor' style='font-size:1em'>)</text>
<text text-anchor='middle' x='216' y='52' fill='currentColor' style='font-size:1em'>l</text>
<text text-anchor='middle' x='216' y='68' fill='currentColor' style='font-size:1em'>o</text>
<text text-anchor='middle' x='224' y='52' fill='currentColor' style='font-size:1em'>_</text>
<text text-anchor='middle' x='224' y='68' fill='currentColor' style='font-size:1em'>o</text>
<text text-anchor='middle' x='232' y='52' fill='currentColor' style='font-size:1em'>n</text>
<text text-anchor='middle' x='232' y='68' fill='currentColor' style='font-size:1em'>l</text>
<text text-anchor='middle' x='240' y='52' fill='currentColor' style='font-size:1em'>a</text>
<text text-anchor='middle' x='248' y='52' fill='currentColor' style='font-size:1em'>m</text>
<text text-anchor='middle' x='248' y='68' fill='currentColor' style='font-size:1em'>t</text>
<text text-anchor='middle' x='256' y='52' fill='currentColor' style='font-size:1em'>e</text>
<text text-anchor='middle' x='256' y='68' fill='currentColor' style='font-size:1em'>o</text>
<text text-anchor='middle' x='264' y='52' fill='currentColor' style='font-size:1em'>)</text>
<text text-anchor='middle' x='264' y='68' fill='currentColor' style='font-size:1em'>o</text>
<text text-anchor='middle' x='272' y='68' fill='currentColor' style='font-size:1em'>l</text>
<text text-anchor='middle' x='280' y='68' fill='currentColor' style='font-size:1em'>_</text>
<text text-anchor='middle' x='288' y='68' fill='currentColor' style='font-size:1em'>n</text>
<text text-anchor='middle' x='296' y='68' fill='currentColor' style='font-size:1em'>a</text>
<text text-anchor='middle' x='304' y='68' fill='currentColor' style='font-size:1em'>m</text>
<text text-anchor='middle' x='312' y='68' fill='currentColor' style='font-size:1em'>e</text>
<text text-anchor='middle' x='320' y='68' fill='currentColor' style='font-size:1em'>)</text>
</g>

    </svg>
  
</div>
<p>This nesting is what makes the trace useful. Instead of a flat list of spans, you see the whole shape of an agent turn: which round of ReAct a tool call happened in, which model answered which prompt, and where the time and tokens actually went. When you open a trace in CLS, you can drill from the top-level entry down to a single tool invocation in a specific reasoning step.</p>
<h2 id="what-span-semantics-are-recorded-under-the-hood">What span semantics are recorded under the hood?</h2>
<p>The plugin does not invent a proprietary trace format; it aligns with OpenTelemetry conventions. Each span carries the standard identifiers and metadata you would expect from a well-behaved tracer:</p>
<ul>
<li><code>traceID</code> as a 32-hex-character string</li>
<li><code>spanID</code> and <code>parentSpanID</code> as 16-hex-character strings</li>
<li>OpenTelemetry span kinds (<code>INTERNAL</code>, <code>CLIENT</code>, <code>SERVER</code>)</li>
<li>Status codes (<code>OK</code>, <code>ERROR</code>, <code>UNSET</code>)</li>
<li>Nanosecond-precision start, end, and duration fields</li>
<li>Token usage and model identifiers on chat spans</li>
</ul>
<p>This OTel alignment matters for two reasons. First, it keeps the data recognizable to engineers already comfortable with distributed tracing. Second, it makes correlation possible inside Tencent Cloud: because spans live in CLS, you can join GenAI traces against logs, metrics, and cost analytics in the same log platform, enabling session-to-token-cost analysis without a separate tracing database.</p>
<h2 id="how-do-i-install-the-dsh-plugin">How do I install the DSH plugin?</h2>
<p>Installation uses DeepSeek Harness&rsquo;s native plugin mechanism. You add the plugin to whichever profile you are running, then restart the harness for it to take effect:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-bash" data-lang="bash"><span style="display:flex;"><span>dsh plugin --profile web add tencentcloud-agentobs-sdk-dsh
</span></span><span style="display:flex;"><span><span style="color:#75715e"># or</span>
</span></span><span style="display:flex;"><span>dsh plugin --profile headless add tencentcloud-agentobs-sdk-dsh
</span></span><span style="display:flex;"><span><span style="color:#75715e"># or</span>
</span></span><span style="display:flex;"><span>dsh plugin --profile harness add tencentcloud-agentobs-sdk-dsh
</span></span></code></pre></div><p>Because the profile flag selects where the plugin applies, you can enable tracing only on the headless automation profile and leave interactive sessions untouched — or vice versa. The restart requirement is worth remembering: adding the plugin is not enough, DeepSeek Harness must be restarted before the observer attaches.</p>
<p>One installation pitfall applies to pnpm v9 or later. The plugin depends on <code>protobufjs</code>, which has a build script. Modern pnpm blocks build scripts by default, so you must approve them or the Protobuf serialization layer will not build:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-bash" data-lang="bash"><span style="display:flex;"><span>pnpm approve-builds
</span></span></code></pre></div><h2 id="how-do-i-configure-the-plugin">How do I configure the plugin?</h2>
<p>Configuration is handled either through environment variables or through a <code>cordis.patch.yml</code> file in the profile directory. The environment variable path is the quickest for a single deployment:</p>
<table>
  <thead>
      <tr>
          <th>Setting</th>
          <th>Environment variable</th>
          <th>Default</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>CLS endpoint</td>
          <td><code>CLS_ENDPOINT</code></td>
          <td>—</td>
      </tr>
      <tr>
          <td>CLS topic ID</td>
          <td><code>CLS_TOPIC_ID</code></td>
          <td>—</td>
      </tr>
      <tr>
          <td>Secret ID</td>
          <td><code>CLS_SECRET_ID</code></td>
          <td>—</td>
      </tr>
      <tr>
          <td>Secret key</td>
          <td><code>CLS_SECRET_KEY</code></td>
          <td>—</td>
      </tr>
      <tr>
          <td>Service name</td>
          <td><code>CLS_SERVICE_NAME</code></td>
          <td>—</td>
      </tr>
      <tr>
          <td>Content capture</td>
          <td><code>OTEL_INSTRUMENTATION_GENAI_CAPTURE_MESSAGE_CONTENT</code></td>
          <td><code>true</code></td>
      </tr>
  </tbody>
</table>
<p>The four credentials — endpoint, topic, secret ID, and secret key — are mandatory; without them the plugin has nowhere to write spans. The service name is how you identify this deployment inside CLS, so give it a value that distinguishes environments, for example <code>dsh-prod-web</code>.</p>
<p>For teams that prefer declarative config checked into the repo, <code>cordis.patch.yml</code> under the profile is the alternative. It carries the same options in YAML form and keeps secrets out of shell history — though you should still externalize the actual secret values through your secret manager rather than committing them.</p>
<h2 id="should-i-capture-prompts-responses-and-tool-content">Should I capture prompts, responses, and tool content?</h2>
<p>Content capture is on by default, and this is the feature that demands the most attention. With capture enabled, the plugin attaches prompts, LLM responses, and tool arguments and results to their spans. That gives you searchable, debuggable traces — but it also means credentials, source code, and personally identifiable information can be shipped to CLS as part of a trace.</p>
<p>The trade-off is real, and the brief is unambiguous: content capture can leak sensitive data into a cloud log service. You control the blast radius with two levers:</p>
<ul>
<li><code>captureContent: false</code> (or <code>OTEL_INSTRUMENTATION_GENAI_CAPTURE_MESSAGE_CONTENT=false</code>) disables content attachment entirely, leaving only span structure, timing, and metadata.</li>
<li><code>contentMaxChars</code>, defaulting to 128000, caps how much content a single span carries so a pathological tool result does not bloat a topic.</li>
</ul>
<p>For anything that touches production data, plan a content policy before you enable the plugin. Trace structure and timing usually survive with capture off; if you need content, apply the same redaction rules you use for your application logs.</p>
<h2 id="how-do-batching-buffering-and-retries-behave">How do batching, buffering, and retries behave?</h2>
<p>The plugin buffers spans in memory and flushes them in batches to avoid hammering CLS with a request per span. Four knobs control that behavior:</p>
<table>
  <thead>
      <tr>
          <th>Option</th>
          <th>Default</th>
          <th>Purpose</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td><code>batchMaxSize</code></td>
          <td>32</td>
          <td>Maximum spans per upload</td>
      </tr>
      <tr>
          <td><code>maxQueueSize</code></td>
          <td>2048</td>
          <td>In-memory queue cap; oldest span is dropped when full</td>
      </tr>
      <tr>
          <td><code>flushIntervalMs</code></td>
          <td>5000</td>
          <td>How often buffered spans are flushed</td>
      </tr>
      <tr>
          <td><code>retryTimes</code></td>
          <td>3</td>
          <td>Retries per failed upload</td>
      </tr>
  </tbody>
</table>
<p>The most important implication is the drop policy. When the in-memory queue hits <code>maxQueueSize</code>, the plugin drops the oldest span rather than blocking the agent. That is a deliberate availability choice: observability must never stall the harness. Under normal load 2048 buffered spans is generous, but a burst of heavy tool calls can fill it, so treat dropped spans as a signal that batching needs tuning or the flush interval needs lowering.</p>
<h2 id="what-are-the-compatibility-requirements">What are the compatibility requirements?</h2>
<p>The plugin targets a specific window of DeepSeek Harness releases and a recent Node.js runtime:</p>
<table>
  <thead>
      <tr>
          <th>Component</th>
          <th>Requirement</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>DeepSeek Harness</td>
          <td><code>&gt;=0.1.0-rc.6</code> and <code>&lt;0.2.0</code></td>
      </tr>
      <tr>
          <td>Node.js</td>
          <td><code>&gt;=18.0.0</code></td>
      </tr>
      <tr>
          <td>Language</td>
          <td>TypeScript</td>
      </tr>
      <tr>
          <td>License</td>
          <td>Apache-2.0</td>
      </tr>
      <tr>
          <td>Package manager note</td>
          <td>pnpm v9+ needs <code>approve-builds</code></td>
      </tr>
  </tbody>
</table>
<p>The version pin matters. Because the plugin hooks DSH&rsquo;s internal lifecycle events, it depends on the event shapes of a particular harness generation. If you are on a Harness older than <code>0.1.0-rc.6</code> or have already moved to <code>0.2.0</code> and beyond, verify plugin compatibility before assuming your traces will appear.</p>
<h2 id="how-does-the-tencent-plugin-compare-to-other-dsh-observability-options">How does the Tencent plugin compare to other DSH observability options?</h2>
<p>DeepSeek Harness already has an observability ecosystem, and the Tencent plugin is not the only choice. The realistic decision is less &ldquo;which tracer&rdquo; and more &ldquo;which architecture&rdquo;:</p>
<table>
  <thead>
      <tr>
          <th>Approach</th>
          <th>Transport</th>
          <th>Collector needed?</th>
          <th>Best for</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>TencentCloud AgentObs DSH</td>
          <td>Protobuf → CLS</td>
          <td>No</td>
          <td>Teams already on Tencent Cloud wanting CLS-native dashboards</td>
      </tr>
      <tr>
          <td>dsh-plugin-langfuse</td>
          <td>OpenTelemetry → Langfuse</td>
          <td>Langfuse self-host or cloud</td>
          <td>Teams standardizing on Langfuse as the LLM observability platform</td>
      </tr>
      <tr>
          <td>dsh-Agentlink</td>
          <td>OTel bridge to Codex</td>
          <td>OTLP endpoint</td>
          <td>Cross-framework observability bridging</td>
      </tr>
      <tr>
          <td>dsh-plugin-opentelemetry-tracing</td>
          <td>OTel → generic OTLP</td>
          <td>OTLP backend (e.g., SigNoz, Jaeger)</td>
          <td>Vendor-neutral, self-hosted stacks</td>
      </tr>
  </tbody>
</table>
<p>The structural difference is collector versus no collector. <code>dsh-plugin-langfuse</code> and the generic OpenTelemetry plugin both lean on OTLP and expect an endpoint you run or subscribe to. Langfuse, the incumbent open-source LLM observability platform, also auto-traces OpenAI-compatible DeepSeek API calls through <code>langfuse.openai</code>, but it still requires running Langfuse as a backend. The Tencent plugin is the first DSH plugin that targets a specific cloud vendor&rsquo;s native log service, so it carries no collector and no extra backend — you only need the CLS topic you already pay for.</p>
<h2 id="who-should-use-it-and-who-should-skip-it">Who should use it, and who should skip it?</h2>
<p>This plugin is a strong fit for teams that are already invested in Tencent Cloud. If your infrastructure, log pipeline, and alerting all live in CLS, pushing GenAI traces into the same platform gives you unified dashboards, cost correlation, and retention policy management without introducing a second observability silo. The zero-collector setup is genuinely attractive for a small agent fleet where running an OTLP stack is disproportionate overhead.</p>
<p>It is a less obvious fit when you are not on Tencent Cloud. The plugin locks your trace data to a single vendor&rsquo;s log service, and if your organization standardizes on OpenTelemetry or a self-hosted stack, <code>dsh-plugin-langfuse</code> or the generic OTel plugin keeps you portable. If your DeepSeek Harness version has drifted outside the supported window, portability also wins. In short: Tencent Cloud users get the cleanest setup; everyone else should weigh vendor lock-in against the convenience.</p>
<h2 id="hands-on-from-install-to-your-first-cls-trace">Hands-on: from install to your first CLS trace</h2>
<p>A minimal end-to-end run looks like this. First, add the plugin to the profile you run:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-bash" data-lang="bash"><span style="display:flex;"><span>dsh plugin --profile harness add tencentcloud-agentobs-sdk-dsh
</span></span><span style="display:flex;"><span>pnpm approve-builds   <span style="color:#75715e"># required on pnpm v9+</span>
</span></span></code></pre></div><p>Second, set the CLS credentials. With environment variables:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-bash" data-lang="bash"><span style="display:flex;"><span>export CLS_ENDPOINT<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;ap-guangzhou.cls.tencentcs.com&#34;</span>
</span></span><span style="display:flex;"><span>export CLS_TOPIC_ID<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;your-topic-id&#34;</span>
</span></span><span style="display:flex;"><span>export CLS_SECRET_ID<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;your-secret-id&#34;</span>
</span></span><span style="display:flex;"><span>export CLS_SECRET_KEY<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;your-secret-key&#34;</span>
</span></span><span style="display:flex;"><span>export CLS_SERVICE_NAME<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;dsh-prod&#34;</span>
</span></span></code></pre></div><p>Third, restart DeepSeek Harness so the observer attaches. Fourth, run a session that invokes the agent and at least one tool. Within the flush interval, spans will appear in your CLS topic, nested as entry → agent → step → chat → tool. From there, build a dashboard for token usage and per-tool latency, and add an alert on ERROR-status spans — you now have deepseek harness observability without a single extra service to run.</p>
<h2 id="what-pitfalls-should-i-watch-for">What pitfalls should I watch for?</h2>
<p>The practical gotchas cluster around configuration, privacy, and environment:</p>
<ul>
<li><strong>Content leakage.</strong> Capture is on by default; disable it or set <code>contentMaxChars</code> before tracing anything sensitive.</li>
<li><strong>pnpm build scripts.</strong> On pnpm v9+, forgetting <code>pnpm approve-builds</code> breaks the Protobuf layer silently at build time.</li>
<li><strong>Restart required.</strong> Adding the plugin does not attach it; you must restart DeepSeek Harness.</li>
<li><strong>Queue overflow drops.</strong> At <code>maxQueueSize</code> the oldest span is dropped to keep the harness responsive — not a sign of a network failure.</li>
<li><strong>Version drift.</strong> Outside <code>&gt;=0.1.0-rc.6 &lt;0.2.0</code> the lifecycle hooks may not fire, so traces can silently not appear.</li>
<li><strong>Secrets in config.</strong> Prefer a secret manager for <code>CLS_SECRET_ID</code> / <code>CLS_SECRET_KEY</code> over committing them to <code>cordis.patch.yml</code>.</li>
</ul>
<h2 id="faq">FAQ</h2>
<h3 id="what-does-deepseek-harness-observability-mean-in-practice">What does deepseek harness observability mean in practice?</h3>
<p>It means capturing the full lifecycle of a DeepSeek Harness run — session, agent invocation, each ReAct step, LLM calls, and tool executions — as structured trace spans so you can inspect latency, token usage, and errors per agent turn.</p>
<h3 id="does-the-tencentcloud-agentobs-sdk-require-an-otlp-collector">Does the TencentCloud AgentObs SDK require an OTLP collector?</h3>
<p>No. It uploads GenAI trace spans directly to Tencent Cloud Log Service via Protobuf using <code>tencentcloud-cls-sdk-js</code>. There is no OTLP collector, sidecar, or extra backend to deploy.</p>
<h3 id="what-span-hierarchy-does-the-plugin-produce-for-a-single-dsh-turn">What span hierarchy does the plugin produce for a single DSH turn?</h3>
<p>Each turn produces an ENTRY span containing an AGENT span, which contains STEP spans per ReAct round. Each STEP in turn has CHAT (model) and TOOL (tool execution) children, following Tencent Cloud&rsquo;s 5-layer entry → agent → step → chat → tool model.</p>
<h3 id="is-prompt-and-tool-content-captured-by-default">Is prompt and tool content captured by default?</h3>
<p>Yes, content capture is enabled by default, attaching prompts, responses, and tool arguments to spans. Disable it with <code>captureContent: false</code> or the <code>OTEL_INSTRUMENTATION_GENAI_CAPTURE_MESSAGE_CONTENT=false</code> environment variable.</p>
<h3 id="which-deepseek-harness-versions-does-the-plugin-support">Which DeepSeek Harness versions does the plugin support?</h3>
<p>It supports DeepSeek Harness <code>&gt;=0.1.0-rc.6</code> and <code>&lt;0.2.0</code>, with Node.js <code>&gt;=18.0.0</code>. It is Apache-2.0 licensed and written in TypeScript.</p>
]]></content:encoded></item><item><title>dsh whale meter tokens: Track DeepSeek Harness Token Usage &amp; Cost Tiers</title><link>https://baeseokjae.github.io/posts/dsh-whale-meter-token-usage-cost/</link><pubDate>Mon, 17 Aug 2026 16:13:59 +0000</pubDate><guid>https://baeseokjae.github.io/posts/dsh-whale-meter-token-usage-cost/</guid><description>dsh-whale-meter shows your DeepSeek Harness token usage in 5 gamified tiers and estimates cost in CNY or USD, fully on-device with no telemetry.</description><content:encoded><![CDATA[<p><strong>dsh-whale-meter is a one-line-install, zero-configuration plugin for DeepSeek Harness (DSH) that turns your raw token usage into five gamified tiers — from 🐟 small fry to 🐳 blue whale — and estimates your spend in CNY or USD, all stored locally on your machine with no telemetry and no network calls.</strong> It went from a nice-to-have to a budgeting necessity on August 17, 2026, when DeepSeek raised v4-flash and v4-pro prices and introduced peak/off-peak billing. This guide explains exactly what it tracks, how the new pricing works, and whether it beats the alternative cost-tracking tools.</p>
<h2 id="what-is-dsh-whale-meter-and-why-you-need-it-now-post-8-17-price-hike-context">What Is dsh-whale-meter and Why You Need It Now (Post-8-17 Price Hike Context)</h2>
<p>DeepSeek Harness is a powerful agentic coding harness, but for a long time it gave users almost no visibility into how many tokens they were actually consuming or what those tokens cost. That gap is exactly what dsh-whale-meter (仓库: <code>Shiye-10Pages/dsh-whale-meter</code>) was built to close. It is a native DSH plugin written in TypeScript under an MIT License, released on August 16, 2026, with a design goal of &ldquo;one-line install, zero config, all data local.&rdquo;</p>
<p>The timing is not accidental. On <strong>August 17, 2026</strong>, DeepSeek raised the prices of v4-flash and v4-pro and, for the first time, introduced <strong>peak/off-peak billing</strong> in China. That single change turned token spend from a background curiosity into a line item you must actively manage. As the broader industry scramble shows, teams are routinely blowing through their token budgets — TechCrunch reported in June 2026 that many teams were running at <strong>3x their 2026 token budgets by April</strong>, with individual engineers seeing monthly token spikes in the <strong>$40,000 range</strong>. The days of &ldquo;just let the model run&rdquo; are over.</p>
<p>dsh-whale-meter answers the three questions every heavy DSH user now asks daily: <em>How many tokens am I using?</em> <em>What am I actually spending?</em> <em>Is my usage tier creeping toward the top?</em></p>
<h2 id="key-features--token-usage-tiers-cost-dashboard-and-the-whale-score-card">Key Features — Token Usage Tiers, Cost Dashboard, and the Whale Score Card</h2>
<p>The headline feature is a <strong>gamified usage ladder</strong> with five tiers:</p>
<table>
  <thead>
      <tr>
          <th>Tier</th>
          <th>Emoji</th>
          <th>Meaning</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>Small fry</td>
          <td>🐟</td>
          <td>Light usage</td>
      </tr>
      <tr>
          <td>Tropical fish</td>
          <td>🐠</td>
          <td>Moderate usage</td>
      </tr>
      <tr>
          <td>Dolphin</td>
          <td>🐬</td>
          <td>Above-average usage</td>
      </tr>
      <tr>
          <td>Humpback</td>
          <td>🐋</td>
          <td>Heavy usage</td>
      </tr>
      <tr>
          <td>Blue whale</td>
          <td>🐳</td>
          <td>Extreme usage</td>
      </tr>
  </tbody>
</table>
<p>Each tier carries an <strong>estimated percentile</strong> — the project is careful to label this as an estimate, not a claimed global rank — plus a playful magnitude conversion that frames your usage as &ldquo;≈ N reads of Three-Body.&rdquo; It turns an abstract number of tokens into something you can actually feel.</p>
<p>The <strong>cost dashboard</strong> is the practical workhorse. It reports spend across four time windows — <strong>today, yesterday, month, and cumulative</strong> — in CNY by default, swappable to USD. Costs are estimated from the official DeepSeek pricing pages, so they track the real tariff rather than a stale hardcoded rate.</p>
<p>Two features make the dashboard genuinely useful rather than decorative:</p>
<ul>
<li><strong>8-17 price-hike comparison.</strong> The same usage is priced side-by-side under the old and new tariffs. Pre-August-17 usage is shown as a <em>preview</em> of what it would have cost under the new rate; post-August-17 usage gets a <em>retroactive back-calculation</em> of what it would have cost under the old rate. This makes the real impact of the hike measurable in one glance.</li>
<li><strong>Cross-price-day correctness.</strong> Price tables are organized by effective date ranges, so historical bills are never mis-computed against today&rsquo;s rates.</li>
</ul>
<h2 id="how-the-8-17-price-change-works-v4-flash--v4-pro-peak-vs-off-peak-before-after-compare">How the 8-17 Price Change Works (v4-flash / v4-pro, Peak vs Off-Peak, Before-After Compare)</h2>
<p>The August 17 change has two parts: a <strong>price hike</strong> and a <strong>new peak/off-peak structure</strong>. Both are baked into the whale-meter&rsquo;s built-in price table (version 2026-08-16 already contains the new rates).</p>
<p>For <strong>v4-flash</strong> (per million tokens, cache miss), the changes are:</p>
<table>
  <thead>
      <tr>
          <th>Cost component</th>
          <th>Before 8-17</th>
          <th>Peak (after 8-17)</th>
          <th>Off-peak (after 8-17)</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>Cache hit</td>
          <td>¥0.02</td>
          <td>¥0.10</td>
          <td>¥0.05</td>
      </tr>
      <tr>
          <td>Cache miss</td>
          <td>¥1</td>
          <td>¥3</td>
          <td>¥1.50</td>
      </tr>
      <tr>
          <td>Output</td>
          <td>¥2</td>
          <td>¥9</td>
          <td>¥4.50</td>
      </tr>
  </tbody>
</table>
<p><strong>v4-pro</strong> adjusted in parallel: peak pricing of ¥0.30 / ¥9 / ¥27 (hit / miss / output), with off-peak exactly half. <strong>Peak hours are Beijing 9:00–12:00 and 14:00–18:00.</strong></p>
<p>The <strong>non-obvious point most guides get wrong</strong> is this: even if you run entirely off-peak, the off-peak rate is <strong>still more expensive than the old flat pre-8-17 price</strong>. Look at the v4-flash table above — the off-peak miss rate of ¥1.50 beats the old ¥1.00 flat rate, and off-peak output at ¥4.50 is more than double the old ¥2.00. Off-peak is a discount <em>relative to the new peak rate</em>, not a return to the old prices. The whale-meter makes this visible with a <strong>⚡½ badge</strong> shown during off-peak hours, and its before/after comparison panel surfaces exactly how much the hike costs you even on the cheapest schedule.</p>
<h2 id="multi-vendor-precision-pricing-46-models-across-6-providers-including-china-tiered-rates">Multi-Vendor Precision Pricing: 46 Models Across 6 Providers, Including China Tiered Rates</h2>
<p>whale-meter is not limited to DeepSeek. It ships with a pricing database covering <strong>46 models across 6 vendors</strong>: DeepSeek, Anthropic, OpenAI, Google, Zhipu GLM, and Moonshot Kimi. Every model is itemized against the official pricing pages.</p>
<p>What sets it apart from most generic cost tools is how it handles <strong>tiered pricing from Chinese vendors</strong>, which most trackers simply ignore:</p>
<ul>
<li><strong>GLM-5.1</strong> input is priced by context length: <strong>&lt;32K tokens costs ¥6, ≥32K costs ¥8</strong> per million.</li>
<li><strong>GLM-4.7</strong> sub-divides even further, tiering by output length.</li>
</ul>
<p>Because the tier boundary depends on per-call context size, a flat price-table lookup produces wrong numbers. whale-meter applies the correct tier per call. When a model is not yet in the table, it is explicitly marked <strong>unpriced</strong> rather than silently estimated — an honest design choice that prevents fabricated cost figures.</p>
<h2 id="privacy--data-locality-fully-on-device-no-telemetry-raw-token-only-storage">Privacy &amp; Data Locality: Fully On-Device, No Telemetry, Raw-Token-Only Storage</h2>
<p>Privacy is a first-class design constraint, and it is the sharpest contrast with cloud-based dashboards. Everything runs on-device:</p>
<ul>
<li>Usage is stored in <code>~/.dsh/whale-meter/usage-YYYY-MM.jsonl</code>.</li>
<li>Only <strong>raw token counts</strong> are persisted — monetary amounts are recomputed on read, so no dollar figures are ever frozen into storage.</li>
<li>There are <strong>no network requests</strong> except the optional balance query you explicitly enable.</li>
<li>There is <strong>no telemetry</strong> of any kind.</li>
</ul>
<p>For a tool that sits next to all of your agent activity, this matters. A cloud dashboard necessarily exfiltrates at least aggregate usage data; whale-meter keeps the entire ledger local. The raw-token-only design also means a later price change can be applied retroactively to historical usage without storing contradictory amounts.</p>
<h2 id="installation-and-configuration-dsh-plugin-zero-config-cordispatchyml-options">Installation and Configuration (dsh plugin, zero-config, cordis.patch.yml options)</h2>
<p>Installation is a single command. If you run DSH with a profile:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-bash" data-lang="bash"><span style="display:flex;"><span>dsh plugin --profile &lt;profile&gt; add dsh-whale-meter
</span></span></code></pre></div><p>There is a <strong>web panel</strong> for TUI or headless setups at <code>http://127.0.0.1:3080/whale-meter/api/summary</code>. Configuration options live in <code>cordis.patch.yml</code>, letting you tweak behavior without touching source code. The project&rsquo;s stated goal is zero-config for the common case: install, open the panel, and you immediately see your tiers and spend.</p>
<h2 id="limitations-and-honest-caveats-estimates-not-exact-unknown-models-marked-unpriced-community-plugin">Limitations and Honest Caveats (Estimates Not Exact, Unknown Models Marked Unpriced, Community Plugin)</h2>
<p>No cost tracker is perfect, and whale-meter is transparent about its boundaries:</p>
<ul>
<li><strong>Costs are estimates, not invoices.</strong> Prices are drawn from official pages, but actual billing can differ due to rounding, promotions, or regional rules. Treat the dashboard as a budgeting guide, not an accounting record.</li>
<li><strong>Unknown models are marked unpriced.</strong> If whale-meter hasn&rsquo;t been updated for a newly released model, it will not invent a price — you&rsquo;ll see it flagged instead.</li>
<li><strong>It&rsquo;s a community plugin.</strong> The repo is young (created August 16, 2026, roughly 3 stars at time of research) and MIT-licensed. You&rsquo;re relying on an actively maintained community project rather than an official DeepSeek product.</li>
<li><strong>Tiers are percentile estimates</strong>, not confirmed global rankings. They give you a feel for where you sit, but &ldquo;blue whale&rdquo; is descriptive, not audited.</li>
</ul>
<h2 id="dsh-whale-meter-vs-alternative-cost-tracking-tools-cachelens-agentledger-agentic-metric">dsh-whale-meter vs Alternative Cost-Tracking Tools (CacheLens, AgentLedger, Agentic Metric)</h2>
<p>How does the DSH-native plugin compare to the wider field of agent cost trackers?</p>
<table>
  <thead>
      <tr>
          <th>Tool</th>
          <th>Approach</th>
          <th>Best for</th>
          <th>Data locality</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td><strong>dsh-whale-meter</strong></td>
          <td>DSH-native plugin, zero-config, gamified tiers</td>
          <td>DeepSeek Harness users who want instant local visibility</td>
          <td>Fully on-device</td>
      </tr>
      <tr>
          <td><strong>CacheLens</strong></td>
          <td>Local-first proxy that tracks cache-hit savings</td>
          <td>Anyone focused on cache economics as an observability layer</td>
          <td>Local proxy</td>
      </tr>
      <tr>
          <td><strong>AgentLedger</strong></td>
          <td>SDK-based observability of real agent actions (SSE streaming)</td>
          <td>Teams tracking actions, not just LLM calls</td>
          <td>Cloud/streaming</td>
      </tr>
      <tr>
          <td><strong>Agentic Metric</strong></td>
          <td>Token/cost tracking for AI coding agents, broader than DSH</td>
          <td>Multi-tool agent environments</td>
          <td>Varies</td>
      </tr>
  </tbody>
</table>
<p>The trade-off is clear. Generic proxy and ledger tools (CacheLens, AgentLedger, Agentic Metric) are broader — AgentLedger, for instance, tracks every real-world agent action with sub-second SSE latency rather than just LLM calls. But they require integration, a proxy, or an SDK, and they don&rsquo;t understand DSH&rsquo;s internals. whale-meter&rsquo;s advantage is that it is a <strong>zero-config native plugin</strong>: no proxy to configure, no SDK to wire in, and it inherently knows the DSH data layout. If you&rsquo;re all-in on DSH, the plugin approach wins on speed and locality; if you span many tools, a broader tracker makes more sense.</p>
<h2 id="verdict-and-roadmap">Verdict and Roadmap</h2>
<p>For anyone running DeepSeek Harness seriously, dsh-whale-meter earns its place immediately. The 8-17 price hike means <strong>running blind on tokens now costs real money</strong> — and the peak/off-peak structure adds a scheduling decision you can only make with a meter. The gamified tiers make the abstract tangible, the before/after price comparison makes the hike measurable, and the fully-local, no-telemetry design makes it trustworthy.</p>
<p>The roadmap points in sensible directions: <strong>CSV/Markdown export</strong> for records and reporting, <strong>off-peak scheduling suggestions</strong> to help you shift work into the half-price window, and an enhanced <strong>TUI</strong>. Combined with the cache-ledger view — whose value <em>grows</em> after the hike, since the cache hit/miss price gap widened — these additions would round it into a complete cost-control tool.</p>
<p>If you use DSH and haven&rsquo;t looked at your token bill yet, now is the moment. Install whale-meter, read your tier, and schedule your heavy runs off-peak. Your wallet will thank you.</p>
<h2 id="faq">FAQ</h2>
<p><strong>What exactly does dsh-whale-meter track?</strong>
It tracks your DeepSeek Harness token usage across five gamified tiers (🐟 small fry through 🐳 blue whale) and estimates your cost over today, yesterday, the current month, and cumulative totals, in CNY or USD.</p>
<p><strong>How does the 8-17 DeepSeek price change work?</strong>
On August 17, 2026, DeepSeek raised v4-flash and v4-pro prices and introduced peak/off-peak billing. Peak hours are Beijing 9:00–12:00 and 14:00–18:00; off-peak costs exactly half the peak rate but is still more expensive than the old flat pre-8-17 price.</p>
<p><strong>Is whale-meter data private?</strong>
Yes. All data is stored locally in <code>~/.dsh/whale-meter/usage-YYYY-MM.jsonl</code>, only raw token counts are persisted, and there is no telemetry or network traffic except an optional balance query you enable manually.</p>
<p><strong>Does whale-meter support models beyond DeepSeek?</strong>
Yes. It covers 46 models across 6 vendors — DeepSeek, Anthropic, OpenAI, Google, Zhipu GLM, and Moonshot Kimi — including China vendors&rsquo; tiered pricing such as GLM-5.1&rsquo;s &lt;32K vs ≥32K input rates.</p>
<p><strong>Is whale-meter free and where do I get it?</strong>
It is an MIT-licensed open-source project at <code>Shiye-10Pages/dsh-whale-meter</code>. Install with <code>dsh plugin --profile &lt;profile&gt; add dsh-whale-meter</code>, then open the web panel at <code>http://127.0.0.1:3080/whale-meter/api/summary</code>.</p>
]]></content:encoded></item><item><title>OpenTelemetry Tracing for DeepSeek Harness: A Complete Setup Guide</title><link>https://baeseokjae.github.io/posts/opentelemetry-tracing-for-deepseek-harness/</link><pubDate>Sun, 16 Aug 2026 10:01:56 +0000</pubDate><guid>https://baeseokjae.github.io/posts/opentelemetry-tracing-for-deepseek-harness/</guid><description>Learn how to add OpenTelemetry tracing to DeepSeek Harness with community plugins, OTLP export, and GenAI semantic conventions.</description><content:encoded><![CDATA[<p>OpenTelemetry tracing for DeepSeek Harness lets you export every agent session, LLM call, and tool invocation as a standard OTLP trace tree to backends like Jaeger, Grafana Tempo, SigNoz, or Langfuse. You add it by installing a community plugin that implements the official <code>@deepseek-ai/dsh-session-telemetry</code> seam, configure an OTLP endpoint and a privacy mode, and then read the GenAI trace tree to debug agent loops, retries, and token usage.</p>
<h2 id="what-is-deepseek-harness-and-why-it-needs-tracing">What is DeepSeek Harness and why it needs tracing</h2>
<p>DeepSeek Harness is the official open-source agent framework from DeepSeek, written in TypeScript with the tagline &ldquo;Everything is a Plugin.&rdquo; Its official repository has roughly 123,000 GitHub stars, making it one of the most popular agent harnesses in the ecosystem. The framework orchestrates multi-step agent loops: it plans, calls LLMs, invokes tools, spawns subagents, and retries failed steps. Each of those steps is a potential failure point, and without tracing you are effectively debugging a black box.</p>
<p>Agent loops are non-deterministic. A single user request can trigger dozens of LLM calls, tool invocations, and retries, and the failure often lives in the interaction between them rather than in any single call. Traditional logging gives you a flat stream of text with no causal structure. OpenTelemetry tracing gives you a hierarchical span tree that shows exactly which step produced which LLM call, which tool ran under which step, and which retries happened along the way.</p>
<p>For DeepSeek Harness specifically, tracing matters because of its plugin architecture. Because &ldquo;everything is a plugin,&rdquo; the harness exposes a public telemetry seam that any plugin can implement. That means you are not locked into a single vendor&rsquo;s exporter. You can swap the official OTLP-logs exporter for a community OTLP-traces plugin without forking the harness, and you can point the output at whichever backend your team already runs.</p>
<h2 id="understanding-the-telemetry-seam-deepseek-aidsh-session-telemetry">Understanding the telemetry seam (@deepseek-ai/dsh-session-telemetry)</h2>
<p>The key architectural fact is that DeepSeek Harness ships a public telemetry seam called <code>@deepseek-ai/dsh-session-telemetry</code>. This is the contract that plugins implement to receive session, agent-loop, LLM-stream, and tool-lifecycle events. The official repository also ships an OTLP-logs exporter that implements this seam.</p>
<p>Community plugins implement the same seam as alternative backends. Instead of exporting logs, they convert the events into OpenTelemetry GenAI traces and metrics. Because they implement the same interface, you can drop them in without modifying the harness source code.</p>
<p>There is one critical gotcha: the telemetry seam accepts exactly one backend per context. If you load a duplicate backend — for example, the official OTLP-logs exporter and a tracing plugin at the same time — the load throws an error. You must choose one backend per context, not stack them.</p>
<h2 id="choosing-an-opentelemetry-backend-jaeger-grafana-tempo-signoz-langfuse">Choosing an OpenTelemetry backend (Jaeger, Grafana Tempo, SigNoz, Langfuse)</h2>
<p>Because the plugins export standard OTLP/HTTP protobuf, you can send traces to any compatible backend. The table below compares the most common choices.</p>
<table>
  <thead>
      <tr>
          <th>Backend</th>
          <th>Best for</th>
          <th>OTLP support</th>
          <th>GenAI semconv</th>
          <th>Notes</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>Jaeger</td>
          <td>Lightweight local debugging</td>
          <td>Yes</td>
          <td>Partial</td>
          <td>Fast to spin up with Docker; great for single-node dev</td>
      </tr>
      <tr>
          <td>Grafana Tempo</td>
          <td>Grafana stack users</td>
          <td>Yes</td>
          <td>Yes</td>
          <td>Pairs with Grafana dashboards and Loki logs</td>
      </tr>
      <tr>
          <td>SigNoz</td>
          <td>Open-source APM alternative</td>
          <td>Yes</td>
          <td>Yes</td>
          <td>Full APM with metrics, traces, and logs in one</td>
      </tr>
      <tr>
          <td>Langfuse</td>
          <td>LLM product teams</td>
          <td>Yes</td>
          <td>Yes</td>
          <td>Native LLM features: feedback scores, sessions, prompts</td>
      </tr>
  </tbody>
</table>
<p>The GenAI semantic conventions (semconv) are what make these traces portable. They standardize LLM span attributes — model name, provider, token usage, prompt and completion — so the same trace renders meaningfully in Jaeger, Tempo, SigNoz, and Langfuse alike. If you already run one of these backends, you do not need to stand up a new one.</p>
<h2 id="installing-a-dsh-opentelemetry-tracing-plugin">Installing a dsh OpenTelemetry tracing plugin</h2>
<p>The most popular community options are <code>loongsuite/dsh-plugin</code>, <code>linyp/dsh-plugin-langfuse</code>, <code>CodePrometheus/dsh-observability</code>, and <code>TtTRz/dsh-langfuse</code>. All of them implement the telemetry seam and export OTLP traces. The table below summarizes their differences.</p>
<table>
  <thead>
      <tr>
          <th>Plugin</th>
          <th>Backend target</th>
          <th>Trace shape</th>
          <th>Metrics</th>
          <th>Notable config</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>loongsuite/dsh-plugin</td>
          <td>Any OTLP backend</td>
          <td>ENTRY -&gt; AGENT -&gt; STEP -&gt; LLM/TOOL</td>
          <td>gen_ai.client.operation.duration, gen_ai.client.token.usage</td>
          <td>Standalone, no sidecar or vendor dependency</td>
      </tr>
      <tr>
          <td>linyp/dsh-plugin-langfuse</td>
          <td>Langfuse</td>
          <td>One trace per turn, grouped by session</td>
          <td>No</td>
          <td>Reads LANGFUSE_HOST; feedback as Langfuse Scores</td>
      </tr>
      <tr>
          <td>CodePrometheus/dsh-observability</td>
          <td>Any OTLP backend</td>
          <td>Session span tree, model step to child span</td>
          <td>No</td>
          <td>DSH_OBSERVABILITY_MODE and DSH_OBSERVABILITY_OTLP_URL</td>
      </tr>
      <tr>
          <td>TtTRz/dsh-langfuse</td>
          <td>Langfuse</td>
          <td>One trace tree per session</td>
          <td>No</td>
          <td>Feedback scores and subagent lineage</td>
      </tr>
  </tbody>
</table>
<p>Before installing, verify your environment meets the requirements. The telemetry plugins require Node.js version 22.19.0 or higher and a DeepSeek Harness version of at least 0.1.0-rc.6 and below 0.2.0. If your harness is older or newer than that range, the plugin may not load.</p>
<p>Installation is typically done as a profile bundle. For example, the Langfuse plugin installs via a <code>cordis.patch.yml</code> profile bundle, and it reads the <code>LANGFUSE_HOST</code> environment variable rather than <code>LANGFUSE_BASE_URL</code>. Check each plugin&rsquo;s README for the exact install command, because the bundle mechanism differs slightly between plugins.</p>
<h2 id="configuring-otlp-export-and-privacy-modes-full--feedback_only--disabled">Configuring OTLP export and privacy modes (FULL / FEEDBACK_ONLY / DISABLED)</h2>
<p>Privacy is a first-class concern in these plugins. The design principle is that sharing stays off until a mode explicitly says otherwise, and configuring an endpoint is not consent to upload session content. You must opt in to each level of data sharing.</p>
<p>The <code>CodePrometheus/dsh-observability</code> plugin exposes three modes through the <code>DSH_OBSERVABILITY_MODE</code> environment variable:</p>
<ul>
<li><strong>FULL</strong> — exports the complete session content, including prompts, completions, and tool payloads. Use this only for backends you trust and for data you are allowed to share.</li>
<li><strong>FEEDBACK_ONLY</strong> — exports only feedback and scoring data, without the underlying session content. This is a middle ground for teams that want evaluation signals without shipping raw prompts.</li>
<li><strong>DISABLED</strong> — turns off sharing entirely. This is the safe default.</li>
</ul>
<p>You also configure the OTLP endpoint, typically through a variable such as <code>DSH_OBSERVABILITY_OTLP_URL</code> or the plugin&rsquo;s own endpoint setting. The endpoint points at your chosen backend&rsquo;s OTLP/HTTP receiver. Remember that setting the endpoint alone does not enable content sharing; the mode controls that.</p>
<h2 id="reading-the-genai-trace-tree-entry---agent---step---llmtool">Reading the GenAI trace tree (ENTRY -&gt; AGENT -&gt; STEP -&gt; LLM/TOOL)</h2>
<p>The trace shape is the most valuable part of the setup. The <code>loongsuite/dsh-plugin</code> exports a hierarchy of <code>ENTRY -&gt; AGENT -&gt; STEP -&gt; LLM/TOOL</code>. Here is what each level means:</p>
<ul>
<li><strong>ENTRY</strong> — the root span for a session or a top-level request.</li>
<li><strong>AGENT</strong> — the agent loop that orchestrates the work.</li>
<li><strong>STEP</strong> — a single planning or execution step within the loop.</li>
<li><strong>LLM/TOOL</strong> — the individual LLM call or tool invocation that ran under that step.</li>
</ul>
<p>The critical design detail is that each real LLM attempt gets its own LLM span. If the harness retries a failed LLM call, each retry appears as a separate span under the same step. This keeps retries and tool fallbacks visible under the same step, which is exactly what you need when debugging flaky agent loops. Instead of seeing one opaque &ldquo;LLM call failed&rdquo; message, you see the full sequence of attempts and their individual latencies and token usage.</p>
<p>Subagent sessions create their own trace and carry DeepSeek Harness parent-session and delegation attributes. This preserves fork and subagent lineage, so you can follow a request from the parent session down into each delegated subagent and back.</p>
<p>The plugins also export GenAI metrics. The <code>loongsuite/dsh-plugin</code> exports <code>gen_ai.client.operation.duration</code> and <code>gen_ai.client.token.usage</code>, giving you latency and token-cost telemetry alongside the traces.</p>
<h2 id="troubleshooting-common-issues-duplicate-backend-version-compatibility-restart-required">Troubleshooting common issues (duplicate backend, version compatibility, restart required)</h2>
<p>The most common problems fall into three buckets.</p>
<p><strong>Duplicate backend error.</strong> Because the telemetry seam accepts exactly one backend per context, loading the official OTLP-logs exporter and a tracing plugin together throws an error. Remove or disable the official exporter before loading a tracing plugin. If you need both logs and traces, choose one backend that handles both, or run them in separate contexts.</p>
<p><strong>Version incompatibility.</strong> The plugins require Node.js 22.19.0+ and DSH between 0.1.0-rc.6 and 0.2.0. If the plugin fails to load, check your Node and harness versions first. A harness outside the supported range will reject the plugin.</p>
<p><strong>Restart required.</strong> Telemetry backends are typically loaded at startup. After installing a plugin or changing a mode or endpoint variable, restart the harness so the new backend is picked up. A running session will not hot-reload the telemetry seam.</p>
<p><strong>No traces appearing.</strong> If your backend shows no traces, verify the OTLP endpoint is reachable, confirm the mode is not <code>DISABLED</code>, and check that the plugin actually loaded without a duplicate-backend error. Also confirm the backend&rsquo;s OTLP/HTTP receiver is enabled, since some backends disable it by default.</p>
<h2 id="faq">FAQ</h2>
<p><strong>What is OpenTelemetry tracing for DeepSeek Harness?</strong>
It is a way to export DeepSeek Harness agent sessions, LLM calls, and tool invocations as standard OTLP trace trees to backends like Jaeger, Grafana Tempo, SigNoz, or Langfuse, using community plugins that implement the official telemetry seam.</p>
<p><strong>Which DeepSeek Harness versions support OpenTelemetry tracing plugins?</strong>
The telemetry plugins require Node.js 22.19.0 or higher and a DeepSeek Harness version of at least 0.1.0-rc.6 and below 0.2.0.</p>
<p><strong>Can I use OpenTelemetry tracing with Langfuse?</strong>
Yes. Plugins like <code>linyp/dsh-plugin-langfuse</code> and <code>TtTRz/dsh-langfuse</code> export each session or turn as an OpenTelemetry trace to Langfuse&rsquo;s OTLP endpoint, including feedback scores and subagent lineage.</p>
<p><strong>Why do I get a duplicate backend error when installing a tracing plugin?</strong>
The <code>@deepseek-ai/dsh-session-telemetry</code> seam accepts exactly one backend per context. If you load the official OTLP-logs exporter and a tracing plugin together, the load throws an error. Remove the official exporter before loading a tracing plugin.</p>
<p><strong>How do privacy modes work in DeepSeek Harness tracing plugins?</strong>
Plugins like <code>CodePrometheus/dsh-observability</code> use modes such as FULL, FEEDBACK_ONLY, and DISABLED. Sharing stays off until a mode explicitly enables it, and configuring an endpoint is not consent to upload session content.</p>
]]></content:encoded></item><item><title>DeepSeek Harness Terminal UI: dsh-tianshu-tui Review (2026)</title><link>https://baeseokjae.github.io/posts/dsh-tianshu-tui-2026/</link><pubDate>Sat, 15 Aug 2026 16:01:21 +0000</pubDate><guid>https://baeseokjae.github.io/posts/dsh-tianshu-tui-2026/</guid><description>dsh-tianshu-tui turns DeepSeek Harness into a full terminal workspace. Review of features, install, prefix-cache savings, and how it compares to other dsh TUIs.</description><content:encoded><![CDATA[<p>The DeepSeek Harness terminal UI (dsh-tianshu-tui) is an official plugin that turns the DeepSeek Harness CLI into a full interactive terminal workspace, with live rendering, session restore, image support, and a TDD-driven evidence gate. It is a pure display layer that derives all agent state from the session event stream, so it adds no prompts, tools, or context of its own. This review covers what it does, how to install it, and how it compares to the other DeepSeek Harness TUIs.</p>
<h2 id="what-is-deepseek-harness-and-why-it-needs-a-tui">What is DeepSeek Harness and why it needs a TUI</h2>
<p>DeepSeek Harness (deepseek-ai/deepseek-harness) is the official open-source agent runtime from DeepSeek, and it is built around a simple but powerful philosophy: <strong>&ldquo;Everything is a Plugin.&rdquo;</strong> As of August 15, 2026, the repository holds 111,829 stars and 10,833 forks, making it one of the most-watched agent frameworks on GitHub. The harness is not a monolithic IDE; it is a modular runtime where prompts, tools, context providers, and even the user interface are all swappable plugins.</p>
<p>That modularity is exactly why a terminal UI matters. The stock <code>dsh</code> CLI is a capable but bare command-line interface. When you are running long agent sessions, reviewing tool calls, steering mid-turn, and inspecting reasoning traces, a plain prompt loop gets cramped. A TUI — a terminal user interface — gives you a full-screen workspace without leaving the terminal. It keeps the zero-chrome, scriptable, git-native workflow that terminal users prefer, while adding the visual density of an IDE.</p>
<p>The ecosystem has responded with at least five community TUI implementations. The most prominent is <strong>dsh-tianshu-tui</strong>, an official plugin by huiliyi37 that evolved from the Tianshu-Tui rendering core. It is the subject of this review.</p>
<h2 id="dsh-tianshu-tui-at-a-glance--the-plugin-that-turns-dsh-into-a-terminal-workspace">dsh-tianshu-tui at a glance — the plugin that turns dsh into a terminal workspace</h2>
<p>dsh-tianshu-tui is an npm package (<code>@huiliyi37/dsh-tianshu-tui</code>, Apache-2.0) that installs as a profile plugin for the official DeepSeek Harness CLI. It was created on August 13, 2026, and already has 158 stars and 6 forks, with the latest npm release at <code>0.1.2-rc.7</code> (August 15, 2026). The project co-evolved with the harness itself, accumulating 250+ commits between August 10 and August 13 on the August 9 baseline snapshot.</p>
<p>The key architectural decision is that the TUI is a <strong>pure display layer</strong>. It registers no prompts, no tools, and no context. Every piece of agent state — reasoning, tool calls, replies, subagent lifecycles — is derived from the session event stream. This means the TUI cannot interfere with the agent&rsquo;s behavior; it only renders what the harness already produces. That is a deliberate contrast to monolithic AI IDEs, where the interface and the agent logic are tightly coupled.</p>
<p>The result is a full in-terminal workspace with:</p>
<ul>
<li>Live rendering of the agent&rsquo;s activity</li>
<li>Session restore across restarts</li>
<li><code>/fork</code> and <code>/rewind</code> for branching and replaying sessions</li>
<li><code>/export</code> to Markdown</li>
<li><code>/steer</code> for mid-turn steering</li>
</ul>
<h2 id="installation-and-setup">Installation and setup</h2>
<p>Installing dsh-tianshu-tui requires three things: the official CLI, a recent Node.js, and pnpm.</p>
<p><strong>Requirements:</strong></p>
<table>
  <thead>
      <tr>
          <th>Requirement</th>
          <th>Version</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>Official CLI</td>
          <td><code>@deepseek-ai/dsh</code> 0.1.0-rc.6</td>
      </tr>
      <tr>
          <td>Node.js</td>
          <td><code>^22.19</code> or <code>&gt;=24</code></td>
      </tr>
      <tr>
          <td>Package manager</td>
          <td>pnpm on PATH</td>
      </tr>
  </tbody>
</table>
<p>The install command is a single line:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-bash" data-lang="bash"><span style="display:flex;"><span>npx -y @deepseek-ai/dsh plugin --profile tui add @huiliyi37/dsh-tianshu-tui
</span></span></code></pre></div><p>This adds the plugin to the <code>tui</code> profile. You then launch the TUI through that profile.</p>
<p><strong>Common pitfalls.</strong> The review notes two recurring install failures. The first is <code>ERR_FS_EISDIR</code>, which typically appears when the plugin path is misconfigured or a stale directory is being treated as a file. The second is a &ldquo;stale dsh&rdquo; problem, where an older CLI version is still on PATH and the plugin refuses to load. Both are resolved by pinning the official CLI to exactly <code>0.1.0-rc.6</code> and ensuring pnpm is reachable. Because the plugin co-evolves rapidly with the harness, keeping the CLI version in sync matters more than with a stable tool.</p>
<h2 id="core-features--session-workspace-images-input-and-reasoning">Core features — session workspace, images, input, and reasoning</h2>
<p>Once running, dsh-tianshu-tui provides a dense but navigable interface. The core features fall into four groups.</p>
<p><strong>Session workspace.</strong> The TUI supports live rendering, session restore, <code>/fork</code>, <code>/rewind</code>, and <code>/export</code> to Markdown. You can branch a session to try a different approach, rewind to an earlier checkpoint, and export the full transcript for sharing or documentation. <code>/steer</code> lets you redirect the agent mid-turn without killing the run.</p>
<p><strong>End-to-end images.</strong> The plugin handles images from clipboard to inline render. You can paste an image from the clipboard, and it renders inline using the kitty or iTerm2 graphics protocol. The standout feature is the <strong>vision bridge</strong>: when the main model cannot see images, the TUI auto-detects an auxiliary vision model and routes image understanding through it, with graceful degradation if none is available. This is a practical solution to the common problem of a text-only main model being asked to reason about a screenshot.</p>
<p><strong>Complete input surface.</strong> The input layer is grok-style: a slash menu for commands, <code>@</code>-path tab completion, bracketed paste support, optional vim keys, an external editor via Ctrl+E, and history search via Ctrl+F. For heavy terminal users, the vim keybindings and external editor integration make long sessions far more comfortable.</p>
<p><strong>Reasoning visualization.</strong> The think channel streams live, then folds into compact rows to keep the screen readable. Ctrl+O expands a reasoning block inline when you want to inspect the model&rsquo;s chain of thought. This is a meaningful improvement over a plain chat UI, where reasoning either floods the screen or is hidden entirely.</p>
<h2 id="harness-engineering-differentiators--tdd-evidence-gate-memory-code-intelligence">Harness-engineering differentiators — TDD evidence gate, memory, code intelligence</h2>
<p>Beyond rendering, dsh-tianshu-tui exposes several harness-engineering features that set it apart from a plain chat interface.</p>
<p><strong>TDD-driven workflow and evidence gate.</strong> The plugin supports a RED-first verification workflow: write a failing test, watch it fail, then implement until it passes. The evidence gate is an obligation state machine that tracks what must be verified before a task is considered done. If verification fails, the run is routed to failure handling rather than silently continuing. This is a real differentiator from chat UIs, which have no notion of &ldquo;prove it works.&rdquo;</p>
<p><strong>Memory and cross-session recall.</strong> The <code>/memory</code> command opens a project memory browser, and the underlying Tianshu-Tui core uses a Stigmergy (pheromone) self-decaying memory model. This shifts the tool from a stateless chat to a persistent agent partner that remembers context across sessions.</p>
<p><strong>Code intelligence and git tools.</strong> The plugin integrates code intelligence retrieval and git tooling, so you can inspect repository context and manage version control from within the TUI.</p>
<p><strong>Personalized harness integration.</strong> A <code>/doctor</code> command runs diagnostics, <code>/btw</code> spawns a background agent, and <code>/model</code> plus <code>/effort</code> let you hot-switch models and effort levels mid-session.</p>
<h2 id="the-prefix-cache-advantage--95-99-hit-rate-and-what-it-saves-you">The prefix-cache advantage — 95-99% hit rate and what it saves you</h2>
<p>One of the most compelling technical details is the prefix-cache engineering in the Tianshu-Tui rendering core. In long sessions, the core reports a <strong>steady-state prefix-cache hit rate of 95-99%</strong> on DeepSeek V4.</p>
<p>What does that mean in practice? Prefix caching means the model provider does not recompute the shared prefix of a conversation on every request. When the hit rate is high, the cost and latency of each turn drop dramatically, because only the new tokens need to be processed. In a long agent session where the conversation history grows large, a 95-99% hit rate is the difference between a responsive, cheap workflow and one that degrades into slow, expensive recomputation.</p>
<p>This is a hidden cost lever that most TUI reviews miss. The interface itself does not save tokens, but by keeping sessions long-lived and well-structured, it maximizes the prefix-cache benefit. For teams running DeepSeek V4 at scale, this is a real operational advantage.</p>
<h2 id="how-it-compares-to-other-deepseek-harness-tuis">How it compares to other DeepSeek Harness TUIs</h2>
<p>dsh-tianshu-tui is not the only TUI in the ecosystem. Here is how it stacks up against the main alternatives.</p>
<table>
  <thead>
      <tr>
          <th>TUI</th>
          <th>Language / stack</th>
          <th>Stars</th>
          <th>Key strength</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td><strong>dsh-tianshu-tui</strong></td>
          <td>TypeScript (Tianshu-Tui core)</td>
          <td>158</td>
          <td>Pure display layer, vision bridge, TDD evidence gate, 95-99% prefix-cache</td>
      </tr>
      <tr>
          <td><strong>Tianshu-Tui</strong></td>
          <td>TypeScript, Apache-2.0</td>
          <td>229</td>
          <td>Full standalone runtime, CVM + Stigmergy memory, 13,000+ tests</td>
      </tr>
      <tr>
          <td><strong>openma-ai/deepseek-harness-tui</strong></td>
          <td>Rust / ratatui, MIT</td>
          <td>25</td>
          <td>Up to 8 images per prompt, JSON-RPC runtime, token/cache metrics</td>
      </tr>
      <tr>
          <td><strong>gxinxing/deepseek-harness-tui</strong></td>
          <td>JavaScript (Ink)</td>
          <td>7</td>
          <td>Thin ~800-line UI, zero-chrome, OSC 11 theme detection</td>
      </tr>
      <tr>
          <td><strong>oh-dsh</strong></td>
          <td>TypeScript, MIT</td>
          <td>188</td>
          <td>Desktop + Web + TUI unified, plugin marketplace, Git Review</td>
      </tr>
  </tbody>
</table>
<p><strong>Tianshu-Tui</strong> is the rendering core that dsh-tianshu-tui evolved from. It is a full standalone terminal coding agent runtime with 229 stars, 13,000+ passing tests, and the Stigmergy memory model. If you want the complete runtime rather than a plugin for the official harness, this is the base.</p>
<p><strong>openma-ai/deepseek-harness-tui</strong> is the Rust/ratatui option. It is lighter on features but strong on image handling (up to 8 images per prompt) and exposes token and cache metrics. It runs either as a dsh profile plugin or directly against the SDK JSON-RPC runtime.</p>
<p><strong>gxinxing/deepseek-harness-tui</strong> is deliberately minimal — about 800 lines of Ink (React for terminals). It is a thin, readable UI that folds tool calls into cells and derives its theme from your terminal via an OSC 11 probe. It is the choice for users who want the least chrome possible.</p>
<p><strong>oh-dsh</strong> takes a different approach: it packages the DSH runtime into Desktop, Web, and TUI distributions with a shared plugin marketplace and a Git Review sidebar. If you want the same sessions across a desktop app, a browser, and a terminal, oh-dsh is the unified option.</p>
<h2 id="who-should-use-it-and-who-should-wait">Who should use it and who should wait</h2>
<p>dsh-tianshu-tui is best for developers who already live in the terminal and run DeepSeek Harness for real agent work — especially those doing TDD, long multi-turn sessions, or image-heavy tasks where the vision bridge matters. The prefix-cache benefit and the evidence gate make it a strong fit for teams that care about cost and verifiability.</p>
<p>You might wait if you prefer a minimal UI (gxinxing&rsquo;s Ink plugin), need a full standalone runtime (Tianshu-Tui), want multi-form Desktop/Web/TUI access (oh-dsh), or need the Rust/ratatui stack with heavy image staging (openma). Because the plugin is young and co-evolves rapidly with the harness, you should also be comfortable with frequent updates and the CLI version pin.</p>
<h2 id="verdict-and-final-thoughts">Verdict and final thoughts</h2>
<p>dsh-tianshu-tui is a well-engineered, feature-complete terminal UI for DeepSeek Harness. Its decision to be a pure display layer is the right one: it stays out of the agent&rsquo;s way while giving you a dense, navigable workspace. The vision bridge, TDD evidence gate, memory browser, and 95-99% prefix-cache hit rate are genuine differentiators, not cosmetic additions.</p>
<p>The main caveats are the young age of the project and the install friction around the CLI version pin. But for terminal-native DeepSeek Harness users, dsh-tianshu-tui is currently the most complete and polished option in the ecosystem. If you run dsh for serious agent work, it is worth installing today.</p>
<h2 id="faq">FAQ</h2>
<p><strong>What is the DeepSeek Harness terminal UI?</strong>
It is a TUI plugin (dsh-tianshu-tui) that turns the DeepSeek Harness CLI into a full interactive terminal workspace with live rendering, session restore, image support, and a TDD evidence gate.</p>
<p><strong>How do I install dsh-tianshu-tui?</strong>
Run <code>npx -y @deepseek-ai/dsh plugin --profile tui add @huiliyi37/dsh-tianshu-tui</code>. You need the official CLI <code>@deepseek-ai/dsh</code> 0.1.0-rc.6, Node.js <code>^22.19</code> or <code>&gt;=24</code>, and pnpm on PATH.</p>
<p><strong>Does dsh-tianshu-tui change how the agent behaves?</strong>
No. It is a pure display layer that derives all state from the session event stream and registers no prompts, tools, or context, so it cannot alter agent behavior.</p>
<p><strong>What is the vision bridge?</strong>
When the main model cannot see images, the TUI auto-detects an auxiliary vision model and routes image understanding through it, with graceful degradation if none is available.</p>
<p><strong>What is the prefix-cache hit rate and why does it matter?</strong>
The Tianshu-Tui core reports a 95-99% steady-state prefix-cache hit rate on DeepSeek V4 in long sessions, which cuts token cost and latency by avoiding recomputation of the shared conversation prefix.</p>
]]></content:encoded></item></channel></rss>