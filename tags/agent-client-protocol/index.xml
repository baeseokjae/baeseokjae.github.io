<?xml version="1.0" encoding="utf-8" standalone="yes"?><rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom" xmlns:content="http://purl.org/rss/1.0/modules/content/"><channel><title>Agent Client Protocol on RockB</title><link>https://baeseokjae.github.io/tags/agent-client-protocol/</link><description>Recent content in Agent Client Protocol on RockB</description><image><title>RockB</title><url>https://baeseokjae.github.io/images/og-default.png</url><link>https://baeseokjae.github.io/images/og-default.png</link></image><generator>Hugo</generator><language>en-us</language><lastBuildDate>Fri, 04 Sep 2026 01:01:32 +0000</lastBuildDate><atom:link href="https://baeseokjae.github.io/tags/agent-client-protocol/index.xml" rel="self" type="application/rss+xml"/><item><title>Agent Shell Emacs 2026: Vendor-Neutral Chat with AI Agents from Emacs</title><link>https://baeseokjae.github.io/posts/agent-shell-emacs-ai-agents-2026/</link><pubDate>Fri, 04 Sep 2026 01:01:32 +0000</pubDate><guid>https://baeseokjae.github.io/posts/agent-shell-emacs-ai-agents-2026/</guid><description>agent-shell turns Emacs into a vendor-neutral AI agent shell via ACP. Install from MELPA, connect Claude, Codex, Gemini, or Goose, and chat from one buffer.</description><content:encoded><![CDATA[<p>agent-shell is a native Emacs shell that lets you talk to any AI coding agent from a single buffer, powered by ACP (Agent Client Protocol) rather than any single vendor. It supports 18+ agents including Claude Agent, Codex, Gemini CLI, Goose, and Grok Build, and by September 2026 it has grown to roughly 1,824 GitHub stars. You install it from MELPA, configure authentication with environment variables, and switch agents without leaving Emacs.</p>
<h2 id="what-is-agent-shell-and-why-vendor-neutral-ai-agents-matter">What is agent-shell and why vendor-neutral AI agents matter</h2>
<p>agent-shell is an Emacs Lisp package that provides a native shell for interacting with LLM-powered coding agents. Unlike a terminal wrapper or a web chat, it is built on <code>comint-mode</code>, the same foundation as <code>M-x shell</code> and <code>M-x term</code>, so you never dance between character and line modes. You type a prompt, the agent streams its response, and the whole conversation lives in a normal Emacs buffer you can search, kill, and yank.</p>
<p>The reason this matters in 2026 is vendor neutrality. The AI agent landscape is fragmented: Claude Code, OpenAI Codex, Google Gemini CLI, Goose, Grok Build, Cursor, Qwen Code, and a dozen more all speak slightly different dialects. Historically, integrating each one meant writing bespoke glue code, fighting incompatible APIs, and locking yourself into whichever vendor you picked first. agent-shell sidesteps all of that by speaking one protocol — ACP — so the agent is a pluggable detail rather than the center of your workflow.</p>
<h2 id="understanding-acp-agent-client-protocol--the-lsp-moment-for-ai-agents">Understanding ACP (Agent Client Protocol) — the LSP moment for AI agents</h2>
<p>ACP, the Agent Client Protocol, standardizes communication between code editors and coding agents. It is developed between Zed and Google, and it is deliberately analogous to LSP, which standardized language-server integration a decade ago. Just as LSP let one editor work with any language server, ACP lets one editor — or one Emacs buffer — work with any coding agent.</p>
<p>The protocol solves three concrete problems:</p>
<ul>
<li><strong>Integration overhead</strong>: instead of N editors times M agents of bespoke glue, you write one ACP client and it works with every ACP-compliant agent.</li>
<li><strong>Limited compatibility</strong>: agents that only speak their own proprietary protocol become usable from any ACP client.</li>
<li><strong>Developer lock-in</strong>: you are no longer married to a single vendor&rsquo;s tooling; swap agents as easily as you swap language servers.</li>
</ul>
<p>Under the hood, local agents run as sub-processes communicating via JSON-RPC over stdio, while remote agents use HTTP or WebSocket (still in progress). ACP v1 is stable and v2 is in draft, with official libraries in Kotlin, Java, Python, Rust, and TypeScript. agent-shell is built on <code>acp.el</code>, an Emacs Lisp implementation of ACP developed between Zed and Google, so the whole stack is native to Emacs.</p>
<h2 id="installing-agent-shell-in-emacs-melpa-use-package-doom-emacs">Installing agent-shell in Emacs (MELPA, use-package, Doom Emacs)</h2>
<p>agent-shell and its dependency <code>acp.el</code> are both available on MELPA, so installation is a one-liner with <code>use-package</code>:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-elisp" data-lang="elisp"><span style="display:flex;"><span>(use-package agent-shell
</span></span><span style="display:flex;"><span>  :ensure <span style="color:#66d9ef">t</span>)
</span></span></code></pre></div><p>This pulls in <code>acp.el</code> and <code>shell-maker</code> automatically. If you use Doom Emacs, add <code>agent-shell</code> to your <code>packages.el</code> and run <code>doom sync</code>:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-elisp" data-lang="elisp"><span style="display:flex;"><span>(package! agent-shell)
</span></span></code></pre></div><p>After installation, start a session with <code>M-x agent-shell</code>. The first time you run it, agent-shell offers shell prompts as soon as possible so you can start typing during init rather than waiting for the agent to finish booting. If you prefer a specific agent to be preselected, set <code>agent-shell-preferred-agent-config</code> to skip the picker entirely.</p>
<h2 id="configuring-authentication-and-environment-variables">Configuring authentication and environment variables</h2>
<p>Most agents authenticate through environment variables rather than interactive login prompts. The exact variable depends on the agent, but the pattern is consistent: export the key in your shell environment before launching Emacs, and agent-shell passes it through to the agent sub-process.</p>
<p>For example, Claude Agent reads <code>ANTHROPIC_API_KEY</code>, OpenAI Codex reads <code>OPENAI_API_KEY</code>, and Google Gemini CLI reads <code>GOOGLE_API_KEY</code> (or uses <code>gcloud</code> application-default credentials). Goose no longer requires an OpenAI key by default, which makes it a convenient first agent to try. The safest approach is to add the relevant exports to your <code>~/.bashrc</code> or <code>~/.zshrc</code> and launch Emacs from a shell that has them set, so you never hard-code secrets into your Emacs config.</p>
<h2 id="connecting-your-first-agent-claude-agent-codex-gemini-cli-goose">Connecting your first agent (Claude Agent, Codex, Gemini CLI, Goose)</h2>
<p>Once authentication is configured, connecting an agent is a matter of selecting it from the picker or configuring it explicitly. agent-shell uses <code>acp-make-client</code> to define each agent, and the configuration is uniform across vendors. Here is a representative example adapted from the project&rsquo;s documentation:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-elisp" data-lang="elisp"><span style="display:flex;"><span>(require <span style="color:#e6db74">&#39;agent-shell</span>)
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>(setq agent-shell-preferred-agent-config
</span></span><span style="display:flex;"><span>      <span style="color:#f92672">&#39;</span>((:name <span style="color:#e6db74">&#34;Claude Agent&#34;</span>
</span></span><span style="display:flex;"><span>         :client (acp-make-client
</span></span><span style="display:flex;"><span>                  :command <span style="color:#e6db74">&#34;claude&#34;</span>
</span></span><span style="display:flex;"><span>                  :args <span style="color:#f92672">&#39;</span>(<span style="color:#e6db74">&#34;--print&#34;</span> <span style="color:#e6db74">&#34;--output-format&#34;</span> <span style="color:#e6db74">&#34;stream-json&#34;</span>)))))
</span></span></code></pre></div><p>The same shape works for Codex, Gemini CLI, and Goose — you change the command and arguments, not the integration layer. Because every agent speaks ACP, the Emacs-side code is identical; only the sub-process command differs. This is the practical payoff of vendor neutrality: learning one agent&rsquo;s configuration teaches you all of them.</p>
<h2 id="switching-between-18-agents-from-one-buffer">Switching between 18+ agents from one buffer</h2>
<p>agent-shell supports 18+ ACP-driven agents, including Claude Agent, Codex, Gemini CLI, Goose, Grok Build, Cursor, Kimi Code CLI, CodeBuddy, Kiro CLI, Qwen Code, Auggie, Mistral Vibe, Factory Droid, Pi, Oh My Pi, Opencode, and Antigravity. Recent releases have also added Oh My Pi (omp) and Grok Build (xAI), and moved Cursor onto its official Cursor CLI.</p>
<p>Switching between them is a first-class operation. You can start a new session with a different agent, or use <code>agent-shell-preferred-agent-config</code> to preselect one and skip the picker. Because the conversation format is standardized by ACP, you can run the same prompt against Claude, Codex, and Gemini in separate buffers and compare the results side by side — something that is painful with vendor-specific tools.</p>
<h2 id="chat-mode-activity-grouping-and-prompt-queueing-in-2026">Chat mode, activity grouping, and prompt queueing in 2026</h2>
<p>The 0.73 release marked a turning point: agent-shell entered the chat. Chat mode fuses comint with traditional chat-like labelling and is enabled by default. If you prefer the classic shell feel, disable it with <code>(setq agent-shell-chat-mode-enabled nil)</code>.</p>
<p>Two other 2026 features make long agent sessions manageable:</p>
<ul>
<li><strong>Activity grouping</strong>: tool calls and thinking are collapsed by default so the buffer stays readable. Expand them with <code>agent-shell-activity-group-expand-by-default</code>, or set it to <code>'latest</code> to expand only the most recent group.</li>
<li><strong>Prompt queueing</strong>: consolidated under <code>agent-shell-prompt-queue.el</code>, you can queue, resume, and remove prompts. This is invaluable when you want to fire several prompts at an agent and let it work through them in order.</li>
</ul>
<h2 id="composing-prompts-from-anywhere-and-tab-navigation">Composing prompts from anywhere and TAB navigation</h2>
<p><code>M-x agent-shell-prompt-compose</code> lets you craft a prompt from any buffer, not just the agent shell. This is a huge workflow win: you can be editing a file, select a region, and compose a prompt that references it without copying text around. <code>C-c C-c</code> sends the prompt fire-and-forget, while <code>C-u C-c C-c</code> queues another one.</p>
<p>Markdown rendering also got smarter. Lists get normalized padding and indentation, and TAB navigation works across source blocks, links, and images, with hints shown as you move. If you want custom rendering, <code>agent-shell-markdown-render-functions</code> lets you plug in your own renderers — for example, <code>agent-shell-math-renderer</code> renders LaTeX as SVG directly in the buffer.</p>
<h2 id="going-local-first-with-opencode-and-ollama">Going local-first with Opencode and Ollama</h2>
<p>One of the most compelling 2026 use cases is fully local AI. Because agent-shell is agent-agnostic, you can pair Opencode with a local Ollama model such as Qwen2.5:7b and run an entire agent workflow without sending a single prompt to a cloud API. This matters for privacy-sensitive work, air-gapped environments, and anyone who wants to avoid per-token costs.</p>
<p>The setup is the same as any other agent: configure Opencode as an ACP client, point it at your local Ollama endpoint, and start a session. The traffic stays on your machine, and you get the full agent-shell experience — chat mode, activity grouping, prompt queueing — over a local model. It is a reminder that vendor neutrality is not just about choosing between cloud vendors; it is about choosing whether to use a cloud at all.</p>
<h2 id="extending-agent-shell-with-the-companion-ecosystem">Extending agent-shell with the companion ecosystem</h2>
<p>agent-shell has grown a rich ecosystem of companion packages that extend it far beyond a bare shell:</p>
<ul>
<li><strong>emacs-skills</strong> — give agents reusable skills</li>
<li><strong>agent-shell-sidebar</strong> — a persistent sidebar for agent state</li>
<li><strong>agent-shell-bookmark</strong> — bookmark agent conversations</li>
<li><strong>agent-shell-workspace</strong> and <strong>agent-shell-manager</strong> — manage multiple workspaces and sessions</li>
<li><strong>agent-review</strong> — review agent output</li>
<li><strong>ob-agent-shell</strong> — run agents from Org Babel blocks</li>
<li><strong>agent-shell-tramp</strong> — use agents over TRAMP for remote files</li>
<li><strong>agent-shell-dashboard</strong> and <strong>agent-shell-hq</strong> — dashboards and higher-level orchestration</li>
</ul>
<p>There are also image utilities for clipboard paste (<code>pngpaste</code>, <code>wl-paste</code>, <code>xclip</code>) and per-platform screenshots, so you can feed images to multimodal agents directly from Emacs. The ecosystem is a strong signal that agent-shell is not a toy but a platform.</p>
<h2 id="troubleshooting-and-tips">Troubleshooting and tips</h2>
<ul>
<li><strong>Agent won&rsquo;t start</strong>: check that the agent binary is on your <code>PATH</code> and that the relevant API key is exported in the environment Emacs was launched from.</li>
<li><strong>No traffic visible</strong>: run <code>M-x agent-shell-view-traffic</code> to inspect the raw ACP JSON traffic between Emacs and the agent. This is the fastest way to see what is actually being sent.</li>
<li><strong>Testing without cost</strong>: agent-shell can save traffic to disk and replay it as a fake agent, giving you cheap, fast, deterministic tests without burning tokens.</li>
<li><strong>Too much chatter</strong>: enable activity grouping or set <code>agent-shell-activity-group-expand-by-default</code> to <code>'latest</code> to collapse tool calls and thinking.</li>
<li><strong>Theming</strong>: use <code>agent-shell-faces.el</code> to control faces and match your theme.</li>
</ul>
<h2 id="faq">FAQ</h2>
<p><strong>What is agent-shell for Emacs?</strong>
agent-shell is a native Emacs shell, built on comint-mode, for interacting with AI coding agents. It is powered by ACP (Agent Client Protocol) and supports 18+ agents from a single buffer.</p>
<p><strong>Is agent-shell vendor-neutral?</strong>
Yes. Because it speaks ACP rather than any single vendor&rsquo;s protocol, you can use Claude Agent, Codex, Gemini CLI, Goose, Grok Build, and many others interchangeably from the same Emacs interface.</p>
<p><strong>How do I install agent-shell?</strong>
Install it from MELPA with <code>(use-package agent-shell :ensure t)</code>. It depends on <code>acp.el</code> and <code>shell-maker</code>, which are pulled in automatically. Doom Emacs users add <code>(package! agent-shell)</code> and run <code>doom sync</code>.</p>
<p><strong>Which agents does agent-shell support?</strong>
It supports 18+ ACP-driven agents, including Claude Agent, Codex, Gemini CLI, Goose, Grok Build, Cursor, Qwen Code, Mistral Vibe, Opencode, and Antigravity, with new agents added regularly.</p>
<p><strong>Can I run agent-shell with a local model?</strong>
Yes. Pair Opencode with a local Ollama model such as Qwen2.5:7b to run a fully local, privacy-friendly agent workflow entirely from Emacs without cloud API calls.</p>
]]></content:encoded></item></channel></rss>