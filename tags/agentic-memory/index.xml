<?xml version="1.0" encoding="utf-8" standalone="yes"?><rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom" xmlns:content="http://purl.org/rss/1.0/modules/content/"><channel><title>Agentic Memory on RockB</title><link>https://baeseokjae.github.io/tags/agentic-memory/</link><description>Recent content in Agentic Memory on RockB</description><image><title>RockB</title><url>https://baeseokjae.github.io/images/og-default.png</url><link>https://baeseokjae.github.io/images/og-default.png</link></image><generator>Hugo</generator><language>en-us</language><lastBuildDate>Tue, 25 Aug 2026 04:02:52 +0000</lastBuildDate><atom:link href="https://baeseokjae.github.io/tags/agentic-memory/index.xml" rel="self" type="application/rss+xml"/><item><title>Universal Memory Protocol: A Shared Format for AI Agent Memory</title><link>https://baeseokjae.github.io/posts/universal-memory-protocol-a-shared-format-for-ai-agent-memory/</link><pubDate>Tue, 25 Aug 2026 04:02:52 +0000</pubDate><guid>https://baeseokjae.github.io/posts/universal-memory-protocol-a-shared-format-for-ai-agent-memory/</guid><description>A universal memory protocol is a shared, portable format for storing AI agent context so any tool can read and write it. Here is how it works, why it matters, and how to adopt it.</description><content:encoded><![CDATA[<p>A universal memory protocol is an open, standardized format for storing AI agent memory so that any agent or tool can read and write it, regardless of which vendor built it. Today most agent memory is trapped in proprietary silos: each tool stores context in its own schema, so switching agents means losing your history. A universal memory protocol is the missing &ldquo;Rosetta Stone&rdquo; that makes context portable, interoperable, and portable between tools.</p>
<h2 id="what-is-a-universal-memory-protocol">What Is a Universal Memory Protocol?</h2>
<p>A universal memory protocol is essentially a shared contract for how AI agents represent, store, and exchange their memory. It is the difference between a <strong>memory architecture</strong> and a <strong>memory format</strong>. An architecture describes how an agent organizes memory internally — for example, the core, archival, and recall layers popularized by Letta. A format describes the interoperable schema that lets data travel between systems. A universal memory protocol focuses on the format: a common representation of memory records, their metadata, their relationships, and their access permissions, so that data is not locked to one runtime.</p>
<p>Think of it like HTTP or SMTP for agent memory. Just as any web browser can talk to any web server because they both speak the same protocol, a universal memory protocol means any agent can hand its context to any other agent. The protocol defines how memory is written, how it is labeled, how it is queried, and — in the newest drafts — who is allowed to read it.</p>
<h2 id="why-agent-memory-is-stuck-in-silos-the-copy-paste-problem">Why Agent Memory Is Stuck in Silos (the copy-paste problem)</h2>
<p>The central problem a universal memory protocol solves is fragmentation. Today&rsquo;s AI agent ecosystem is a sprawling set of point solutions. One assistant stores your preferences in a local JSON blob. Another keeps them in a vector database. A third stores them in a graph. When you switch tools, none of that context carries over, so you are forced into a manual, error-prone copy-paste workflow to re-explain yourself to every new agent.</p>
<p>This &ldquo;copy-paste problem&rdquo; is more than an inconvenience; it is a structural blocker for the agent economy. A 2026 survey of the agent memory research space catalogued dozens of independent approaches — A-MEM&rsquo;s Zettelkasten-style knowledge networks, Letta&rsquo;s operating-system-like memory management, Graphiti&rsquo;s temporal knowledge graphs, and many more — all solving the same underlying problem in incompatible ways. When every vendor implements its own proprietary schema, interoperability between agents collapses. A universal memory protocol is the industry-grade fix: one schema, one contract, and portable context everywhere.</p>
<h2 id="the-pieces-of-a-shared-memory-format">The Pieces of a Shared Memory Format</h2>
<p>A practical universal memory format is not just a file. It needs several layers working together:</p>
<ul>
<li><strong>Schema</strong>: The core data model for memory records — events, facts, entities, and their attributes. A consistent schema is what makes cross-agent data possible.</li>
<li><strong>Namespaces</strong>: Logical separation so memory from one agent or user does not collide with another&rsquo;s, and so memory can be scoped per source.</li>
<li><strong>Vector / embedding layer</strong>: Dense representations that power semantic recall and retrieval.</li>
<li><strong>Graph layer</strong>: Structured relationships between memory, the memory of entities and temporal associations (as used by Graphiti-style systems).</li>
<li><strong>Permissioning</strong>: Metadata about who can read, write, and share each record — the layer that turns a format into a governed system.</li>
</ul>
<p>A mature protocol bundles these into one open specification rather than leaving each agent to reinvent its own. The key insight is that the format is universal, but the <em>architecture</em> — how you organize and rank memory inside a given agent — remains up to each system.</p>
<h2 id="existing-protocols-and-standards-you-should-know">Existing Protocols and Standards You Should Know</h2>
<p>The space is still early and fragmented, but several efforts are worth knowing:</p>
<table>
  <thead>
      <tr>
          <th>Protocol / System</th>
          <th>Type</th>
          <th>Focus</th>
          <th>Status</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>Open Agent Memory Protocol (OAMP)</td>
          <td>Open standard</td>
          <td>Cross-agent memory portability, governed memory</td>
          <td>v1.2 shipped; v1.3 (draft) adds granted-scope</td>
      </tr>
      <tr>
          <td>MCP memory</td>
          <td>Tool protocol</td>
          <td>Memory via the Model Context Protocol ecosystem</td>
          <td>Active</td>
      </tr>
      <tr>
          <td>A-MEM (arXiv 2502.12110)</td>
          <td>Research architecture</td>
          <td>Zettelkasten-style dynamic memory linking</td>
          <td>Published</td>
      </tr>
      <tr>
          <td>MemGPT / Letta</td>
          <td>Platform</td>
          <td>Stateful agents with self-editing memory</td>
          <td>Open source</td>
      </tr>
      <tr>
          <td>Graphiti</td>
          <td>Architecture</td>
          <td>Temporal knowledge graphs for relational memory</td>
          <td>Open source</td>
      </tr>
  </tbody>
</table>
<p>OAMP is arguably the most visible pure &ldquo;protocol&rdquo; play. Its 1.2 release shipped a concrete spec, and the 1.3 draft introduces &ldquo;governed memory&rdquo; — a direct response to the real-world hazard where a shared memory backend lets any agent the user has logged into read everything stored. Other independent efforts, including the Akashik Protocol, LedgerSync, TradeMemory Protocol, and WAMP (Web Agent Memory Protocol), launched in the same window, which tells you two things: demand for a cross-agent standard is real, but the field is still consolidating.</p>
<h2 id="how-a-universal-memory-protocol-actually-works-under-the-hood">How a Universal Memory Protocol Actually Works Under the Hood</h2>
<p>Under the hood, a universal memory protocol works like a well-governed shared data layer rather than a magic brain. When an agent writes a memory, it emits a structured record against the open schema: an identifier, a type, timestamps, the content, and optional embeddings for semantic retrieval. That record is written to a backing store — vector database, graph database, or both. When another agent needs that memory, it issues a query against the same interface and the protocol returns the matching records with their metadata and permissions.</p>
<p>The advantage of a protocol approach is that the actual storage engine can vary. Agent A can run on a vector store; Agent B can run on a graph. As long as both speak the same wire format, Agent B can read Agent A&rsquo;s context and continue the work. This is the same separation of concerns that made the web work: the transport and data contract are standardized, the internal implementation is free. It is also why research like A-MEM (Agentic Memory) demonstrates that memory that self-evolves as new memories integrate tends to outperform static memory baselines — dynamic, linked representations hold more context, and a shared format is what lets those rich representations travel.</p>
<h2 id="governance-privacy-and-scoped-memory-why-format-alone-is-not-enough">Governance, Privacy, and Scoped Memory (why format alone is not enough)</h2>
<p>Format solves interoperability, but it does not solve consent. The moment you have a shared memory layer, a new question arises: <strong>who can read what?</strong> In the current model, a shared backend that any agent you have logged into can often read everything stored — an open secret that motivated OAMP&rsquo;s governed-memory work. A mature universal memory protocol must therefore handle scope, consent, and audit, not just the storage schema.</p>
<p>The emerging answer is &ldquo;granted scope&rdquo;: memory records carry access permissions, and agents must be explicitly granted a view before they can read them. Governance moves from an all-or-nothing model to a fine-grained one. Combined with versioning and audit trails, this transforms a shared memory into a trust boundary — which is what makes cross-agent memory viable in production, and in regulated industries, across regulated industries. The format is the skeleton; governance is the security nervous system.</p>
<h2 id="how-to-adopt-a-portable-memory-in-your-own-agents">How to Adopt a Portable Memory in Your Own Agents</h2>
<p>You do not need to build a protocol from scratch to start gaining portability. The practical adoption path is incremental:</p>
<ol>
<li><strong>Pick a well-known schema.</strong> Start with a mature standard like OAMP or MCP-style memory blocks rather than inventing a proprietary one.</li>
<li><strong>Map your existing memory to it.</strong> Translate your current JSON blobs, vector records, and graph nodes into the protocol&rsquo;s record structure, preserving metadata and relationships.</li>
<li><strong>Export and import across tools.</strong> Once your memory is in a standard schema, you can export it from one tool and import it into another without manual reconstruction.</li>
<li><strong>Add scoping and audit from day one.</strong> Attach permissions and provenance metadata to every record so that when you do share memory, you share it safely.</li>
<li><strong>Test the handoff.</strong> Simulate switching between two agents and confirm context survives the move; that is the true test of portability.</li>
</ol>
<h2 id="the-road-ahead-and-open-challenges">The Road Ahead and Open Challenges</h2>
<p>The universal memory protocol is converging, but the space is still marked by fragmentation. Several standards launched recently — OAMP, Akashik, WAMP, and others — and the field has not yet settled on a single winner. The consolidation will be decided less by raw spec quality and more by adoption: which protocol gets used in the most real agents, has the cleanest governance story, and is the easiest to adopt.</p>
<p>The open challenges are clear. One is <strong>interoperability between protocols themselves</strong> — a standard for standards. Another is <strong>privacy-preserving shared memory</strong> — how to share context without exposing the full record. A third is <strong>performance at scale</strong>, since a shared, cross-agent memory layer will be queried far more heavily than any single-agent store. And the largest question remains <strong>governance</strong>: who owns your memory, how it is audited, and how the user keeps control when it moves between tools.</p>
<p>For developers, the strategic takeaway is straightforward: bet on an open, governed format rather than a proprietary silo. The tools will keep changing, but a universal memory protocol ensures your agent&rsquo;s context — and your user&rsquo;s trust — travels with you. As long as your data follows you, and not the tool, you will never again have to re-explain who you are to every new agent you try.</p>
<h2 id="faq">FAQ</h2>
<h3 id="what-is-a-universal-memory-protocol-in-ai">What is a universal memory protocol in AI?</h3>
<p>A universal memory protocol is a shared, open standard for how AI agents store, structure, and exchange their memory, so that different agents and tools can read each other&rsquo;s context without data being trapped in a single vendor&rsquo;s format.</p>
<h3 id="how-is-a-memory-format-different-from-a-memory-architecture">How is a memory format different from a memory architecture?</h3>
<p>A memory format is the interoperable data schema that lets information travel between systems; a memory architecture is how an individual agent organizes that data internally (for example core, archival, and recall layers). The format is what a universal protocol standardizes.</p>
<h3 id="what-is-the-open-agent-memory-protocol-oamp">What is the Open Agent Memory Protocol (OAMP)?</h3>
<p>OAMP is an open standard for cross-agent memory portability. Version 1.2 shipped a concrete specification, and the 1.3 draft adds &ldquo;governed&rdquo; memory with granted-scope access so that agents can only read the data they have explicit permission to.</p>
<h3 id="why-is-agent-memory-currently-trapped-in-silos">Why is agent memory currently trapped in silos?</h3>
<p>Most agents use proprietary, incompatible schemas — JSON, vector stores, or graph databases — with no common data contract. When you switch tools, none of that context carries over, forcing a manual copy-paste workflow and blocking true interoperability.</p>
<h3 id="how-can-i-make-my-agents-memory-portable">How can I make my agent&rsquo;s memory portable?</h3>
<p>Adopt a well-known schema (such as OAMP or MCP-style memory blocks), map your existing memory into that structure, add scoping and audit metadata, and then export and import across tools to verify the handoff works.</p>
]]></content:encoded></item></channel></rss>