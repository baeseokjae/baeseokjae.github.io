<?xml version="1.0" encoding="utf-8" standalone="yes"?><rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom" xmlns:content="http://purl.org/rss/1.0/modules/content/"><channel><title>Recursive Self-Improvement on RockB</title><link>https://baeseokjae.github.io/tags/recursive-self-improvement/</link><description>Recent content in Recursive Self-Improvement on RockB</description><image><title>RockB</title><url>https://baeseokjae.github.io/images/og-default.png</url><link>https://baeseokjae.github.io/images/og-default.png</link></image><generator>Hugo</generator><language>en-us</language><lastBuildDate>Fri, 28 Aug 2026 04:01:52 +0000</lastBuildDate><atom:link href="https://baeseokjae.github.io/tags/recursive-self-improvement/index.xml" rel="self" type="application/rss+xml"/><item><title>Self-Evolving Agent Harness: How Proteus Lets Agents Plug In, Evolve, and Measure</title><link>https://baeseokjae.github.io/posts/proteus-self-evolving-agent-harness-2026/</link><pubDate>Fri, 28 Aug 2026 04:01:52 +0000</pubDate><guid>https://baeseokjae.github.io/posts/proteus-self-evolving-agent-harness-2026/</guid><description>A self-evolving agent harness lets an agent improve its own prompts, skills, and tools. Proteus is an open-source framework to plug in, evolve, and measure that process.</description><content:encoded><![CDATA[<p>A self-evolving agent harness is the layer of prompts, memory, skills, tools, and control logic that lets an AI agent improve its own behavior over time. Proteus is an open-source, MIT-licensed research-preview framework that lets you plug in any agent harness, evolve it across episodes, and measure exactly what changed. Instead of chasing a single benchmark score, Proteus asks what a self-evolving harness actually does and whether an initial condition leaves a permanent mark.</p>
<h2 id="what-is-a-self-evolving-agent-harness-agent--model--harness">What Is a Self-Evolving Agent Harness? (Agent = Model + Harness)</h2>
<p>The phrase &ldquo;agent harness&rdquo; comes from a simple but powerful idea popularized by harness-engineering practitioners: a raw model is not an agent until a harness gives it state, tools, feedback loops, and constraints. As the widely cited definition puts it, &ldquo;a coding agent is the model plus everything you build around it.&rdquo; That everything includes system prompts, project memory files such as CLAUDE.md or AGENTS.md, skills, tools and MCP servers, the sandbox, orchestration, hooks, and observability.</p>
<p>A self-evolving agent harness takes that one step further. Instead of a human hand-tuning the harness, the agent itself proposes changes to its own harness, runs them, observes the results, and keeps or rolls back the changes. The harness becomes a learnable artifact rather than a fixed configuration. This is the frontier shift: agent self-improvement is moving from model weights to the harness that surrounds the model.</p>
<p>The core equation is simple: Agent = Model + Harness. A great model with a bad harness underperforms a decent model with a great harness. When the harness can evolve itself, the equation becomes a feedback loop rather than a static setup.</p>
<h2 id="why-self-improvement-is-moving-from-weights-to-the-harness">Why Self-Improvement Is Moving From Weights to the Harness</h2>
<p>For years, improving an AI agent meant fine-tuning model weights. That approach is expensive, requires training infrastructure, and is hard to apply to a specific agent&rsquo;s day-to-day behavior. The harness, by contrast, is cheap to change, immediately testable, and directly observable. A single prompt edit, a new skill, or a better tool can change an agent&rsquo;s behavior more than a costly weight update.</p>
<p>The academic field has caught up. A comprehensive survey of self-evolving AI agents frames the entire area as bridging static foundation-model capabilities with continuous lifelong adaptability. It abstracts the field into four feedback-loop components: System Inputs, Agent System, Environment, and Optimisers. The optimiser is the piece that decides how the agent changes itself, and increasingly that optimiser targets the harness rather than the weights.</p>
<p>Real-world results back this up. Cline reached 88.8% state-of-the-art on Terminal-Bench 2.1 through one-shot recursive self-improvement, at a run cost of $49.8 — less than a tenth of the $552 that Fable 5 spent to reach a lower score. The argument is that the bottleneck is not the models but the humans using them: weeks of human trace-reading work were compressed into a single prompt. When self-improvement targets the harness, the cost drops enough that individual developers can afford it.</p>
<h2 id="proteus-at-a-glance-plug-in-evolve-measure">Proteus at a Glance: Plug In, Evolve, Measure</h2>
<p>Proteus is an open-source research-preview framework whose tagline is &ldquo;Plug in. Evolve. Measure.&rdquo; It is MIT-licensed, requires Python 3.10 or later, and is currently at v0.3.0 with 57 GitHub stars and 7 forks as of late August 2026. It was created in mid-August 2026, so it is a young but active project.</p>
<p>The framework is deliberately harness-agnostic. You implement one small HarnessAdapter, and your agent — whether it is a minimal harness, an LLM harness, the DeepSeek Harness, Pi, Aki, or your own — plugs into the same framework, sandbox, and measurement instrument. This is a meaningful differentiator: most competing systems only evolve their own primitives, while Proteus is designed to evolve any harness you bring to it.</p>
<p>Proteus spans both goal and no-goal regimes. It supports visible or hidden evaluators, and it treats unpressured, no-goal evolution as a first-class mode. Most harness-evolution systems hard-code away the no-goal case, so this is a genuinely novel regime.</p>
<h2 id="how-proteus-works-the-observe--propose--act--reflect-episode-loop">How Proteus Works: The Observe → Propose → Act → Reflect Episode Loop</h2>
<p>Proteus organizes evolution into episodes, and each episode runs a four-phase loop: observe, propose, act, and reflect.</p>
<p>In the observe phase, the framework measures the current state of the harness and the agent&rsquo;s behavior. In the propose phase, the agent proposes a change to its own harness — a new skill, a modified prompt, a different action preference. In the act phase, the change is applied and the agent runs. In the reflect phase, the results are measured and the framework decides whether the change was an improvement.</p>
<p>Evolved harness files cross episode boundaries with snapshotting and rollback. If a change makes things worse, the framework can restore the previous state. This makes evolution safe and reversible rather than a one-way gamble.</p>
<p>The core objects in Proteus are the HarnessAdapter, the Surface, the Disposition (a removable action-preference perturbation), the GoalConfig, and the Sandbox (Local or Docker). The Disposition is particularly interesting: it is a removable perturbation to the agent&rsquo;s action preferences, which lets researchers test whether a specific initial condition leaves a permanent mark on the evolved harness.</p>
<h2 id="the-measurement-instrument-structural-distance-crystallization-and-behavioural-permutation-tests">The Measurement Instrument: Structural Distance, Crystallization, and Behavioural Permutation Tests</h2>
<p>Proteus&rsquo;s most distinctive feature is that it ships a measurement instrument, not just a score. Most competitors report only task pass-rates. Proteus measures three things:</p>
<ul>
<li>Structural distance per surface: how far the evolved harness has moved from its initial structure.</li>
<li>A crystallization and swap test: whether the harness has settled into a stable configuration, and whether swapping components back to their original state changes behavior.</li>
<li>Behavioural distance with a permutation test: whether the observed behavioral change is statistically significant or just noise.</li>
</ul>
<p>The demo run illustrates the power of this instrument. An installed action preference (the review_notes arm) shifted the mean units built from 3.5 (neutral) to 13.0, with a behavioural R of 3.075 and a p-value of 0.0150. That p-value means the change is statistically significant, not random.</p>
<p>Proteus&rsquo;s behavioural ruler also independently reproduces the headline dynamics of the research runs: arms separate at episode 1 with an R of 1.63 and converge by episode 30 with an R of 0.93. This is a strong validation signal — the measurement instrument reproduces known dynamics rather than inventing its own.</p>
<h2 id="harness-agnostic-onboarding-one-adapter-for-deepseek-harness-pi-aki-or-your-own-agent">Harness-Agnostic Onboarding: One Adapter for DeepSeek Harness, Pi, Aki, or Your Own Agent</h2>
<p>The single most practical feature of Proteus is its harness-agnostic design. You write one HarnessAdapter, and any agent can plug in. This matters because the ecosystem is fragmented: there is the DeepSeek Harness, Pi, Aki, and countless custom harnesses built by individual developers and teams.</p>
<p>The roadmap names Hermes Agent as the first target harness to onboard, followed by SWE-agent, OpenClaw, Codex CLI, OpenHands, OpenCode, and Goose. This is a concrete plan to make Proteus useful across the most popular agent harnesses in the ecosystem.</p>
<p>For a developer, this means you do not have to abandon your existing agent setup to experiment with self-evolution. You implement a small adapter, and your harness gains access to the same evolution loop, sandbox, and measurement instrument that every other harness uses.</p>
<h2 id="proteus-vs-the-field-evoharnessrl-skillopt-cline-and-the-self-evolving-survey">Proteus vs the Field: EvoHarnessRL, SkillOpt, Cline, and the Self-Evolving Survey</h2>
<p>To understand where Proteus fits, it helps to compare it against the other major approaches to self-evolving agents.</p>
<table>
  <thead>
      <tr>
          <th>System</th>
          <th>Approach</th>
          <th>Key Result</th>
          <th>Differentiator</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>Proteus</td>
          <td>Harness-agnostic evolution with measurement instrument</td>
          <td>Demo: action preference shifted units built from 3.5 to 13.0, p = 0.0150</td>
          <td>Structural distance, crystallization, behavioural permutation tests; no-goal mode</td>
      </tr>
      <tr>
          <td>EvoHarnessRL</td>
          <td>Learns harness policies (Belief, Progress, Experience state)</td>
          <td>96.9% success on ALFWorld with Qwen3-8B</td>
          <td>Treats external workspace and usage policy as learnable</td>
      </tr>
      <tr>
          <td>SkillOpt</td>
          <td>Controllable text-space optimizer for agent skills</td>
          <td>Best or tied on all 52 evaluated cells</td>
          <td>Separate optimizer model; bounded add/delete/replace edits</td>
      </tr>
      <tr>
          <td>Cline</td>
          <td>One-shot recursive self-improvement</td>
          <td>88.8% SOTA on Terminal-Bench 2.1 at $49.8</td>
          <td>Cost efficiency; ~1 billion tokens over 17 hours</td>
      </tr>
      <tr>
          <td>Self-Evolving Survey</td>
          <td>Academic framework</td>
          <td>Four-component abstraction of the field</td>
          <td>Canonical overview of the whole area</td>
      </tr>
  </tbody>
</table>
<p>EvoHarnessRL reaches 96.9% success on ALFWorld with a Qwen3-8B LLM by learning harness policies. It exposes Belief, Progress, and Experience as policy-facing harness state and uses supervised harness fine-tuning plus cost-aware GRPO. It reveals two dynamics: harness annealing, where training internalizes recurring harness-use patterns, and harness evolution, where progress updates and experience consolidation refine the harness.</p>
<p>SkillOpt, from Microsoft, is the first systematic controllable text-space optimizer for agent skills. A separate optimizer model turns scored rollouts into bounded add, delete, or replace edits, and an edit is accepted only when it strictly improves a held-out validation score. It is best or tied on all 52 evaluated cells across six benchmarks, seven target models, and three execution harnesses, and it has over 16,000 GitHub stars.</p>
<p>The key difference is that Proteus is a measurement-first, harness-agnostic framework, while EvoHarnessRL and SkillOpt optimize their own specific harnesses or skills. Proteus asks what a self-evolving harness does and whether initial conditions leave a permanent mark, rather than simply how to raise a benchmark score.</p>
<h2 id="safety-and-sandboxing-for-self-editing-agents">Safety and Sandboxing for Self-Editing Agents</h2>
<p>Self-evolving agents are a safety concern because they write and run their own code. A harness that can edit its own prompts, skills, and tools is a harness that can change its own behavior in ways a human did not anticipate.</p>
<p>Proteus addresses this with sandboxing. It runs real harnesses in a container whose filesystem holds the harness and nothing else. The DockerSandbox has no egress, meaning the agent cannot reach the outside network. This contains the blast radius of any self-editing behavior: the agent can only affect the isolated harness environment, not the host system or external services.</p>
<p>This is a pragmatic safety posture. It does not claim to solve the deep alignment problem of self-modifying agents, but it does make experimentation safe enough to run in practice. For researchers and developers, the ability to let an agent evolve its own harness inside a sealed container is what makes the whole field tractable.</p>
<h2 id="is-proteus-right-for-you-strengths-limits-and-the-roadmap">Is Proteus Right for You? Strengths, Limits, and the Roadmap</h2>
<p>Proteus is a research preview, so it is not yet a production tool. Its strengths are clear: it is open source, harness-agnostic, measurement-first, and it supports a genuinely novel no-goal evolution regime. If you want to understand whether your agent harness is actually improving, or whether a specific initial condition leaves a permanent mark, Proteus gives you the instrument to find out.</p>
<p>Its limits are equally clear. At v0.3.0 with 57 stars, it is young and the ecosystem around it is still forming. The roadmap names Hermes Agent as the first target harness to onboard, followed by SWE-agent, OpenClaw, Codex CLI, OpenHands, OpenCode, and Goose — but those adapters are not all built yet. If you use a harness that is not yet supported, you will need to write the adapter yourself.</p>
<p>The cost and efficiency story is compelling. Cline&rsquo;s $49.8 SOTA run shows that recursive self-improvement is now cheap enough for individual developers, not just well-funded labs. If you are already running an agent harness and want to see whether it can improve itself, Proteus is a low-cost, low-risk way to start measuring.</p>
<h2 id="faq">FAQ</h2>
<p><strong>What is a self-evolving agent harness?</strong>
A self-evolving agent harness is the layer of prompts, memory, skills, tools, and control logic that lets an AI agent improve its own behavior over time. It is the &ldquo;everything you build around the model&rdquo; that turns a raw model into an agent, made learnable rather than fixed.</p>
<p><strong>How is Proteus different from other self-evolving agent frameworks?</strong>
Proteus is harness-agnostic and measurement-first. Instead of optimizing its own primitives or chasing a single benchmark score, it lets you plug in any agent harness and measures structural distance, crystallization, and behavioural distance with a permutation test.</p>
<p><strong>Is Proteus free to use?</strong>
Yes. Proteus is open source under the MIT license and requires Python 3.10 or later. It is currently a v0.3.0 research preview with 57 GitHub stars as of late August 2026.</p>
<p><strong>Which agent harnesses does Proteus support?</strong>
Proteus is designed to be harness-agnostic. You implement one HarnessAdapter to plug in any agent, including the DeepSeek Harness, Pi, Aki, or your own. The roadmap names Hermes Agent as the first target, followed by SWE-agent, OpenClaw, Codex CLI, OpenHands, OpenCode, and Goose.</p>
<p><strong>Is it safe to let an agent evolve its own harness?</strong>
Proteus runs real harnesses in a container whose filesystem holds the harness and nothing else, with no network egress. This contains the blast radius of self-editing behavior, making experimentation safe enough to run in practice.</p>
]]></content:encoded></item></channel></rss>