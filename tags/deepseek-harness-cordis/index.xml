<?xml version="1.0" encoding="utf-8" standalone="yes"?><rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom" xmlns:content="http://purl.org/rss/1.0/modules/content/"><channel><title>Deepseek Harness Cordis on RockB</title><link>https://baeseokjae.github.io/tags/deepseek-harness-cordis/</link><description>Recent content in Deepseek Harness Cordis on RockB</description><image><title>RockB</title><url>https://baeseokjae.github.io/images/og-default.png</url><link>https://baeseokjae.github.io/images/og-default.png</link></image><generator>Hugo</generator><language>en-us</language><lastBuildDate>Sat, 05 Sep 2026 01:01:41 +0000</lastBuildDate><atom:link href="https://baeseokjae.github.io/tags/deepseek-harness-cordis/index.xml" rel="self" type="application/rss+xml"/><item><title>DeepSeek Harness Handbook 2026: The Source-Backed Guide to Agents, Plugins, and Runbooks</title><link>https://baeseokjae.github.io/posts/deepseek-harness-handbook-2026/</link><pubDate>Sat, 05 Sep 2026 01:01:41 +0000</pubDate><guid>https://baeseokjae.github.io/posts/deepseek-harness-handbook-2026/</guid><description>DeepSeek Harness (dsh) is an open-source, everything-is-a-plugin agent harness built on Cordis. This 2026 handbook covers agents, plugins, runbooks, and developer-preview pitfalls.</description><content:encoded><![CDATA[<p>DeepSeek Harness (dsh) is an open-source agent harness by DeepSeek AI, built on an everything-is-a-plugin architecture powered by Cordis, where the model adapter, tool registry, session log, and even the agent loop itself are replaceable plugins. You can launch it in seconds with <code>npx @deepseek-ai/dsh web</code>, connect a DeepSeek API key in Settings, and start delegating file edits, command runs, and multi-agent workflows. This 2026 handbook is a source-backed guide to its agents, plugins, and runbooks.</p>
<h2 id="what-is-deepseek-harness-dsh">What Is DeepSeek Harness (dsh)?</h2>
<p>DeepSeek Harness, tracked in the <code>deepseek-ai/deepseek-harness</code> repository, is an open-source agent harness that treats every part of the product as a plugin. According to the official architecture documentation, the model adapter, tool registry, session log, and the agent loop itself are each replaceable from configuration. The project is built on Cordis, a framework whose design is described in the arXiv paper &ldquo;A Programming Paradigm for Spatiotemporal Composability&rdquo; (arXiv:2608.25512).</p>
<p>The project is young and fast-moving. As of 2026-09-04, the repository had 212,253 GitHub stars and 24,916 forks, was created on 2026-08-13, and is MIT-licensed. It is explicitly in developer preview, meaning compatibility-breaking changes are expected. The official README describes dsh as an agent harness where plugins contribute services, typed events, and reversible effects to a shared context.</p>
<h3 id="the-everything-is-a-plugin-model">The Everything-Is-a-Plugin Model</h3>
<p>The defining architectural decision is that nothing is privileged. There is no core to patch. If you want to change how the agent loop behaves, how tools are registered, or how sessions are logged, you mount a plugin beside the others. This is a sharp departure from monolithic agent frameworks where customization means forking the core.</p>
<h3 id="cordis-as-the-foundation">Cordis as the Foundation</h3>
<p>Cordis provides the composability layer. Plugins contribute services, typed events, and reversible effects to a shared context. When a plugin unloads, its registrations unwind automatically. This reversibility is what makes the everything-is-a-plugin model safe to experiment with.</p>
<h2 id="why-config-over-code-matters">Why Config-Over-Code Matters</h2>
<p>The config-over-code philosophy is a core differentiator for dsh. A Hacker News discussion titled &ldquo;DeepSeek harness: what doors does config over code open?&rdquo; highlights the community&rsquo;s interest in this design. The key insight is that registrations are effects that unwind when a plugin unloads, and there is no privileged core to patch.</p>
<h3 id="reversible-registrations">Reversible Registrations</h3>
<p>Because plugin registrations are reversible, you can load and unload capabilities without leaving residue. This makes it practical to compose different tool sets, model adapters, and behaviors for different tasks without maintaining separate forks or branches.</p>
<h3 id="replaceable-subsystems">Replaceable Subsystems</h3>
<p>Every subsystem is a candidate for replacement. The model adapter determines which model you talk to. The tool registry decides which tools the agent can call. The session log records what happened. The agent loop defines how turns and steps are processed. Each of these can be swapped from configuration, which means the harness adapts to your workflow rather than the reverse.</p>
<h2 id="getting-started-in-5-minutes">Getting Started in 5 Minutes</h2>
<p>The fastest way to try DeepSeek Harness is the npx one-liner. Run <code>npx @deepseek-ai/dsh web</code> from your terminal. The Web UI defaults to <code>http://127.0.0.1:3080</code>, and you can pass <code>--no-open</code> to skip the automatic browser launch.</p>
<h3 id="configure-a-model">Configure a Model</h3>
<p>Once the Web UI is running, open Settings and navigate to Models. Add a DeepSeek API key there. The model route becomes usable immediately without restarting the harness, which is a practical convenience during setup.</p>
<h3 id="choose-a-workspace">Choose a Workspace</h3>
<p>Before the session composer becomes available, you must choose a workspace. The agent can read and edit files in that workspace, run commands, delegate work, and maintain a plan. Approval prompts appear under the active permission policy, so you stay in control of what the agent is allowed to do.</p>
<h2 id="understanding-profiles-and-bundles">Understanding Profiles and Bundles</h2>
<p>DeepSeek Harness ships with five profile templates: <code>web</code>, <code>headless</code>, <code>sdk</code>, <code>sdk-minimal</code>, and <code>acp</code>. The <code>dsh-base</code> profile is the shared first layer of <code>web</code>, <code>headless</code>, <code>sdk</code>, and <code>acp</code>. Profiles are layered using <code>cordis.patch.yml</code>, which lets you compose a configuration from base layers and targeted overrides.</p>
<table>
  <thead>
      <tr>
          <th>Profile</th>
          <th>Primary Use</th>
          <th>Notes</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>web</td>
          <td>Interactive Web UI</td>
          <td>Default for <code>npx @deepseek-ai/dsh web</code></td>
      </tr>
      <tr>
          <td>headless</td>
          <td>Scripted, no UI</td>
          <td>Good for automation and CI</td>
      </tr>
      <tr>
          <td>sdk</td>
          <td>Programmatic access</td>
          <td>Full SDK surface</td>
      </tr>
      <tr>
          <td>sdk-minimal</td>
          <td>Lightweight SDK</td>
          <td>Minimal footprint</td>
      </tr>
      <tr>
          <td>acp</td>
          <td>Agent Client Protocol</td>
          <td>For ACP-compatible clients</td>
      </tr>
      <tr>
          <td>dsh-base</td>
          <td>Shared foundation</td>
          <td>First layer of web/headless/sdk/acp</td>
      </tr>
  </tbody>
</table>
<h3 id="layering-with-cordispatchyml">Layering with cordis.patch.yml</h3>
<p>The <code>cordis.patch.yml</code> file is where you express your configuration as a series of patches over base profiles. This is the config-over-code model in action: you describe what you want, and the harness composes the final configuration from the layers you specify.</p>
<h2 id="agents-and-the-agent-loop">Agents and the Agent Loop</h2>
<p>The agent loop is the heart of dsh. It processes turns and steps, assembles the system prompt, classifies tools, and enforces the approval policy. Understanding this lifecycle helps you predict how the agent will behave in your workspace.</p>
<h3 id="turn-and-step-lifecycle">Turn and Step Lifecycle</h3>
<p>A session is composed of turns, and each turn is composed of steps. The agent reads the current state, decides on an action, and either executes a tool or produces output. The loop continues until the task is complete or the agent needs input.</p>
<h3 id="system-prompt-assembly">System-Prompt Assembly</h3>
<p>The system prompt is assembled from multiple sources, including skills and configuration. Skills are optional instructions (not session events) merged from a provider registry across host and per-scope layers. This means you can inject domain knowledge without hardcoding it into the core.</p>
<h3 id="tool-classification-and-approval">Tool Classification and Approval</h3>
<p>Shipped model-facing tools include <code>ask_user_question</code>, <code>run_code</code>, <code>exit_plan_mode</code>, <code>bash</code>, <code>job_*</code> tools, the skill tool, and the workflow tool. Tools are classified, and the approval policy determines which actions require human confirmation. This is how the harness balances autonomy with safety.</p>
<h2 id="plugins-the-everything-is-a-plugin-model">Plugins: The Everything-Is-a-Plugin Model</h2>
<p>Writing a plugin is the primary way to extend dsh. Because everything is a plugin, your custom capability mounts beside the built-in ones, and its registrations unwind when it unloads.</p>
<h3 id="the-config-catalog">The Config Catalog</h3>
<p>The config catalog defines what a plugin can configure. It is the declarative surface that the config-over-code model exposes. When you write a plugin, you describe its configurable options, and the harness handles the rest.</p>
<h3 id="the-tool-catalog">The Tool Catalog</h3>
<p>The tool catalog is where plugins register the tools the agent can call. The shipped tool catalog includes the model-facing tools listed above. Your plugin can add new tools that the agent discovers and uses.</p>
<h3 id="publishing-with-the-dsh-plugin-topic">Publishing with the dsh-plugin Topic</h3>
<p>To share a plugin, publish it and tag it with the <code>dsh-plugin</code> GitHub topic. This is how the community discovers new capabilities. The official README points to GitHub Discussions, Discord, and the <code>dsh-plugin</code> topic as the community channels.</p>
<h2 id="runbooks-and-workflows">Runbooks and Workflows</h2>
<p>The workflow seam is one of the most distinctive features of dsh. It runs a model-written orchestration script that starts subagents, using a <code>node:worker_threads</code> engine with one worker per run. This is a practical runbook pattern: the agent writes a script, and the harness executes it to coordinate multiple subagents.</p>
<h3 id="the-workflow-seam">The Workflow Seam</h3>
<p>The workflow tool lets the agent delegate to subagents. The orchestration script is written by the model, which means the coordination logic is generated dynamically based on the task. The <code>worker_threads</code> engine isolates each run in its own worker.</p>
<h3 id="a-practical-runbook-pattern">A Practical Runbook Pattern</h3>
<p>For a runbook, you describe the steps, and the agent writes an orchestration script that spawns subagents to execute them in parallel or sequence. This turns a static runbook into an executable workflow that adapts to the task.</p>
<h2 id="skills-agent-teams-and-advanced-subsystems">Skills, Agent Teams, and Advanced Subsystems</h2>
<p>Beyond the basics, dsh includes skills, experimental agent teams, and sandbox and permission presets.</p>
<h3 id="skills-registry">Skills Registry</h3>
<p>Skills are optional instructions merged from a provider registry across host and per-scope layers. They are not session events; they are instructions that shape how the agent behaves. This is how you inject reusable expertise.</p>
<h3 id="experimental-agent-teams">Experimental Agent Teams</h3>
<p>Agent Teams is an experimental domain with a durable mailbox. Each team task gets a <code>TeamTaskId</code> allocated as <code>task-&lt;n&gt;</code>, and members reach exactly one terminal phase: active or failed. This is a structured way to coordinate multiple agents on a shared objective.</p>
<h3 id="sandbox-and-permission-presets">Sandbox and Permission Presets</h3>
<p>Sandbox and permission presets control what the agent can do. The approval policy under the active permission preset determines which actions require confirmation. This is your safety boundary.</p>
<h2 id="the-2026-plugin-ecosystem">The 2026 Plugin Ecosystem</h2>
<p>The plugin economy around dsh is growing. Third-party registries such as <code>dshplugin.app</code> and <code>dsh-index.xlings.org</code> provide install commands, source-grounded capability analysis, security signals, compatibility evidence, and repository activity. The <code>dsh-plugin</code> GitHub topic is the canonical discovery surface.</p>
<table>
  <thead>
      <tr>
          <th>Registry</th>
          <th>What It Offers</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>dshplugin.app</td>
          <td>Install commands, capability analysis, security signals, compatibility evidence</td>
      </tr>
      <tr>
          <td>dsh-index.xlings.org</td>
          <td>Community index of plugins</td>
      </tr>
      <tr>
          <td>GitHub dsh-plugin topic</td>
          <td>Canonical discovery and tagging</td>
      </tr>
  </tbody>
</table>
<h3 id="security-signals">Security Signals</h3>
<p>Because plugins are code, security matters. The registries surface security signals so you can evaluate a plugin before installing it. Source-grounded capability analysis tells you what a plugin can actually do, and compatibility evidence tells you whether it works with your version of dsh.</p>
<h2 id="developer-preview-pitfalls-and-best-practices">Developer-Preview Pitfalls and Best Practices</h2>
<p>DeepSeek Harness is in developer preview, and compatibility-breaking changes are expected. This is the most important thing to plan for in 2026.</p>
<h3 id="version-pinning">Version Pinning</h3>
<p>Pin your dsh version. Because breaking changes are expected, a runbook that works today may not work after an upgrade. Pin the version in your project and upgrade deliberately, testing after each change.</p>
<h3 id="the-safety-notice">The Safety Notice</h3>
<p>The official README includes a safety notice. Treat the harness as experimental, and do not rely on it for production-critical automation without testing. The everything-is-a-plugin model is powerful, but it also means behavior can change.</p>
<h3 id="testing">Testing</h3>
<p>Test your plugins and runbooks against a pinned version before relying on them. Because registrations are reversible, you can experiment safely, but you should still verify behavior in a controlled environment.</p>
<h2 id="resources-and-community">Resources and Community</h2>
<p>The official documentation lives at <code>deepseek-harness.github.io/deepseek-harness/</code>, covering the user guide (Web UI, models, providers, Python SDK, schedule) and the developer guide (basic, framework, practice), plus a Cordis primer and tutorial. The source repository is <code>deepseek-ai/deepseek-harness</code> on GitHub.</p>
<p>Community channels include GitHub Discussions, Discord, and the <code>dsh-plugin</code> GitHub topic. The project is MIT-licensed, so you can read the source, contribute, and build on it freely.</p>
<h2 id="faq">FAQ</h2>
<h3 id="what-is-deepseek-harness-dsh-1">What is DeepSeek Harness (dsh)?</h3>
<p>DeepSeek Harness is an open-source agent harness by DeepSeek AI built on Cordis, where every part of the product — the model adapter, tool registry, session log, and agent loop — is a replaceable plugin. It is MIT-licensed and in developer preview.</p>
<h3 id="how-do-i-start-deepseek-harness">How do I start DeepSeek Harness?</h3>
<p>Run <code>npx @deepseek-ai/dsh web</code> from your terminal. The Web UI defaults to <code>http://127.0.0.1:3080</code>. Add a DeepSeek API key in Settings → Models, then choose a workspace before starting a session.</p>
<h3 id="what-does-everything-is-a-plugin-mean">What does &ldquo;everything-is-a-plugin&rdquo; mean?</h3>
<p>It means there is no privileged core to patch. Every subsystem, including the agent loop itself, is a plugin that can be replaced from configuration. Plugin registrations unwind automatically when a plugin unloads.</p>
<h3 id="what-is-the-workflow-seam-in-dsh">What is the workflow seam in dsh?</h3>
<p>The workflow seam runs a model-written orchestration script that starts subagents, using a <code>node:worker_threads</code> engine with one worker per run. It is a practical pattern for turning runbooks into executable multi-agent workflows.</p>
<h3 id="is-deepseek-harness-production-ready">Is DeepSeek Harness production-ready?</h3>
<p>No. It is in developer preview, and compatibility-breaking changes are expected. Pin your version, read the safety notice, and test your plugins and runbooks before relying on them in production.</p>
]]></content:encoded></item></channel></rss>