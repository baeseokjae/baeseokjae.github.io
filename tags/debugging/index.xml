<?xml version="1.0" encoding="utf-8" standalone="yes"?><rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom" xmlns:content="http://purl.org/rss/1.0/modules/content/"><channel><title>Debugging on RockB</title><link>https://baeseokjae.github.io/tags/debugging/</link><description>Recent content in Debugging on RockB</description><image><title>RockB</title><url>https://baeseokjae.github.io/images/og-default.png</url><link>https://baeseokjae.github.io/images/og-default.png</link></image><generator>Hugo</generator><language>en-us</language><lastBuildDate>Sat, 04 Jul 2026 12:00:00 +0000</lastBuildDate><atom:link href="https://baeseokjae.github.io/tags/debugging/index.xml" rel="self" type="application/rss+xml"/><item><title>Coding Agent Debug Logs Guide 2026: Claude Code, Codex, GitHub MCP, and Playwright MCP</title><link>https://baeseokjae.github.io/posts/coding-agent-debug-logs-guide-2026/</link><pubDate>Sat, 04 Jul 2026 12:00:00 +0000</pubDate><guid>https://baeseokjae.github.io/posts/coding-agent-debug-logs-guide-2026/</guid><description>A practical 2026 guide to debugging AI coding agents — Claude Code, Codex CLI, GitHub MCP Server, and Playwright MCP Server — with real commands, log locations, and troubleshooting patterns.</description><content:encoded><![CDATA[<p>If you&rsquo;ve been using AI coding agents daily in 2026, you&rsquo;ve hit the wall where Claude Code freezes mid-task, Codex CLI silently drops an MCP connection, or Playwright MCP opens a headed browser that nobody asked for. The tools are powerful, but when they break, the debugging experience varies wildly between them.</p>
<p>I&rsquo;ve spent the last six months running all four of these tools in production workflows — Claude Code for complex refactoring, Codex CLI for quick prototyping, GitHub MCP for PR automation, and Playwright MCP for browser testing. Here&rsquo;s what I&rsquo;ve learned about getting useful debug output out of each one.</p>
<h2 id="claude-code-the-most-debugging-tooling">Claude Code: The Most Debugging Tooling</h2>
<p>Claude Code has the richest debugging toolkit of any coding agent I&rsquo;ve used. It&rsquo;s not just about log files — there are built-in commands for almost every failure mode.</p>
<h3 id="the-doctor-command">The <code>/doctor</code> Command</h3>
<p>When something feels off — slow responses, MCP servers not loading, hooks not firing — start here. Run <code>/doctor</code> inside Claude Code or <code>claude doctor</code> from the shell. It runs an automated health check covering installation integrity, settings validity, MCP configuration, and context usage. I run this as my first step whenever I suspect a configuration issue rather than a transient API error.</p>
<h3 id="heap-dump-analysis-for-memory-issues">Heap Dump Analysis for Memory Issues</h3>
<p>Claude Code can consume surprising amounts of memory on large codebases. When you notice sluggishness or auto-compaction thrashing, run <code>/heapdump</code>. This writes a JavaScript heap snapshot to <code>~/Desktop/Claude\ Code.heapsnapshot</code> (or your home directory on Linux). Open it in Chrome DevTools → Memory → Load to see exactly what&rsquo;s eating memory. I&rsquo;ve caught runaway context buffers and bloated MCP response caches this way.</p>
<h3 id="safe-mode-and-compaction">Safe Mode and Compaction</h3>
<p>Two commands I use constantly:</p>
<ul>
<li><strong><code>claude --safe-mode</code></strong> — Starts Claude Code with all customizations disabled (plugins, MCP servers, hooks). If the problem disappears, you know one of your extensions is the culprit. I use this to bisect which MCP server is causing startup delays.</li>
<li><strong><code>/compact</code></strong> — Reduces context size. The real power is <code>/compact keep only the plan and the diff</code>, which strips everything except what you&rsquo;re actively working on. I run this before every major task switch.</li>
</ul>
<h3 id="session-recovery">Session Recovery</h3>
<p>Crashes happen. <code>claude --resume</code> in the same directory picks up your previous session after a restart. Combined with <code>/clear</code> to drop irrelevant conversation history, this makes long-running sessions manageable. For persistent issues, <code>/feedback</code> sends diagnostics directly to Anthropic.</p>
<h3 id="common-failure-patterns">Common Failure Patterns</h3>
<table>
  <thead>
      <tr>
          <th>Symptom</th>
          <th>First Thing to Try</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>High CPU / memory</td>
          <td><code>/heapdump</code> then <code>/compact</code></td>
      </tr>
      <tr>
          <td>MCP servers not loading</td>
          <td><code>claude --safe-mode</code></td>
      </tr>
      <tr>
          <td>Garbled terminal text</td>
          <td><code>/terminal-setup</code> or disable GPU acceleration</td>
      </tr>
      <tr>
          <td>Slow search on WSL</td>
          <td>Install ripgrep, set <code>USE_BUILTIN_RIPGREP=0</code></td>
      </tr>
      <tr>
          <td>529 / 429 errors</td>
          <td>Wait, then use <code>/compact</code> to reduce context</td>
      </tr>
      <tr>
          <td>Auto-compaction thrashing</td>
          <td>Read large files in chunks, use sub-agents</td>
      </tr>
  </tbody>
</table>
<p>For more on Claude Code&rsquo;s broader capabilities, check out the <a href="/posts/claude-code-artifacts-guide-2026-live-shareable-previews-from-ai-coding-sessions/">Claude Code Artifacts guide</a>.</p>
<h2 id="codex-cli-toml-config-and-plaintext-logs">Codex CLI: TOML Config and Plaintext Logs</h2>
<p>Codex CLI takes a different approach. Its debugging story centers on configuration files and log directories rather than interactive commands.</p>
<h3 id="log-directory-setup">Log Directory Setup</h3>
<p>Codex writes logs to <code>$CODEX_HOME/log</code> by default. Setting this explicitly also enables the opt-in plaintext TUI log (<code>codex-tui.log</code>), which is invaluable for seeing what Codex is doing in real time. I set <code>CODEX_HOME</code> in my shell profile and tail the log during long operations:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-bash" data-lang="bash"><span style="display:flex;"><span>export CODEX_HOME<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;</span>$HOME<span style="color:#e6db74">/.codex&#34;</span>
</span></span><span style="display:flex;"><span>tail -f <span style="color:#e6db74">&#34;</span>$CODEX_HOME<span style="color:#e6db74">/log/codex-tui.log&#34;</span>
</span></span></code></pre></div><h3 id="configuration-debugging">Configuration Debugging</h3>
<p>Codex uses <code>~/.codex/config.toml</code> (TOML format, not JSON or YAML). The key sections for debugging are:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-toml" data-lang="toml"><span style="display:flex;"><span>[<span style="color:#a6e22e">mcp_servers</span>.<span style="color:#a6e22e">my-server</span>]
</span></span><span style="display:flex;"><span><span style="color:#a6e22e">command</span> = <span style="color:#e6db74">&#34;npx&#34;</span>
</span></span><span style="display:flex;"><span><span style="color:#a6e22e">args</span> = [<span style="color:#e6db74">&#34;-y&#34;</span>, <span style="color:#e6db74">&#34;@modelcontextprotocol/server-filesystem&#34;</span>, <span style="color:#e6db74">&#34;/workspace&#34;</span>]
</span></span><span style="display:flex;"><span><span style="color:#a6e22e">bearer_token_env_var</span> = <span style="color:#e6db74">&#34;MY_SERVER_TOKEN&#34;</span>
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#75715e"># Force a specific auth method</span>
</span></span><span style="display:flex;"><span><span style="color:#a6e22e">forced_login_method</span> = <span style="color:#e6db74">&#34;chatgpt&#34;</span>  <span style="color:#75715e"># or &#34;api_key&#34;</span>
</span></span></code></pre></div><p>If MCP servers aren&rsquo;t loading, check the <code>[mcp_servers]</code> section first. The <code>bearer_token_env_var</code> field is particularly useful for HTTP-transport MCP servers — it sources the token from an environment variable rather than hardcoding it.</p>
<h3 id="auth-troubleshooting">Auth Troubleshooting</h3>
<p>Codex supports ChatGPT login (recommended) and API key auth. If you&rsquo;re getting auth failures:</p>
<ul>
<li><strong>MFA required</strong> for email/password login. Social login (Google, Microsoft, Apple) doesn&rsquo;t need MFA.</li>
<li><strong>Login caching</strong> stores credentials in a plaintext file. Tokens refresh automatically during use.</li>
<li><strong>Forced login method</strong> — set <code>forced_login_method</code> in config to lock to one auth path.</li>
</ul>
<h3 id="snapshot-shell-and-login-shell">Snapshot Shell and Login Shell</h3>
<p>Two config options that affect debugging:</p>
<ul>
<li><strong><code>snapshot_shell</code></strong> (on by default) — Snapshots the shell environment to speed up repeated commands. Turn it off if you&rsquo;re changing environment variables between commands and they&rsquo;re not being picked up.</li>
<li><strong><code>allow_login_shell</code></strong> — Uses login-shell semantics for shell-based tools. Enable this if your tools depend on profile files.</li>
</ul>
<h2 id="github-mcp-server-structured-error-types">GitHub MCP Server: Structured Error Types</h2>
<p>The GitHub MCP Server is unique among these four because it&rsquo;s a server, not a CLI agent. Its debugging story is about structured error handling rather than log files.</p>
<h3 id="error-types">Error Types</h3>
<p>The server defines two custom error types that bubble up through the MCP framework:</p>
<ul>
<li><strong><code>GitHubAPIError</code></strong> — For REST API errors. Contains <code>Message</code>, <code>Response</code> (the full <code>*github.Response</code> object), and <code>Err</code> fields.</li>
<li><strong><code>GitHubGraphQLError</code></strong> — For GraphQL API errors. Contains <code>Message</code> and <code>Err</code> fields.</li>
</ul>
<p>These errors are stored in context for middleware inspection. You can retrieve them with:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-go" data-lang="go"><span style="display:flex;"><span><span style="color:#a6e22e">errors</span>.<span style="color:#a6e22e">GetGitHubAPIErrors</span>(<span style="color:#a6e22e">ctx</span>)
</span></span><span style="display:flex;"><span><span style="color:#a6e22e">errors</span>.<span style="color:#a6e22e">GetGitHubGraphQLErrors</span>(<span style="color:#a6e22e">ctx</span>)
</span></span></code></pre></div><h3 id="design-philosophy">Design Philosophy</h3>
<p>The error handling follows a clear split:</p>
<ul>
<li><strong>User-actionable errors</strong> (auth failures, rate limits, 404s) → Failed tool calls that the agent can handle</li>
<li><strong>Developer errors</strong> (JSON marshaling, internal logic) → Go errors that bubble up through the MCP framework</li>
</ul>
<p>This means if you&rsquo;re building an agent that uses GitHub MCP, you should handle the first category in your agent&rsquo;s error recovery logic and let the second category crash to a log.</p>
<h3 id="authentication-methods">Authentication Methods</h3>
<p>The server supports three auth methods, and getting them wrong is the most common source of errors:</p>
<ol>
<li><strong>OAuth</strong> (browser-based, token stays in memory) — Best for interactive use</li>
<li><strong>Personal Access Token</strong> (via <code>GITHUB_PERSONAL_ACCESS_TOKEN</code> env var) — Best for CI/CD</li>
<li><strong>GitHub App</strong> — Required for GitHub Enterprise Server</li>
</ol>
<p>Minimum PAT scopes: <code>repo</code>, <code>read:packages</code>, <code>read:org</code>. Store in <code>.env</code> files, never commit.</p>
<h3 id="remote-vs-local">Remote vs Local</h3>
<p>The server can run remotely at <code>https://api.githubcopilot.com/mcp/</code> or locally via Docker (<code>ghcr.io/github/github-mcp-server</code>). For debugging, I prefer the local server with <code>--gh-host</code> pointing to my GHES instance — it gives me direct log output and I can restart it independently of the agent.</p>
<p>For a broader comparison of MCP capabilities across agents, see the <a href="/posts/ai-coding-agent-capability-matrix-2026/">AI Coding Agent Capability Matrix</a>.</p>
<h2 id="playwright-mcp-console-levels-and-browser-tracing">Playwright MCP: Console Levels and Browser Tracing</h2>
<p>Playwright MCP is the most configurable of the four when it comes to debug output. It&rsquo;s also the one most likely to need debugging, because browser automation is inherently flaky.</p>
<h3 id="console-level-filtering">Console Level Filtering</h3>
<p>The most important debug setting is the console level:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-bash" data-lang="bash"><span style="display:flex;"><span>npx @playwright/mcp --console-level debug
</span></span><span style="display:flex;"><span><span style="color:#75715e"># or</span>
</span></span><span style="display:flex;"><span>export PLAYWRIGHT_MCP_CONSOLE_LEVEL<span style="color:#f92672">=</span>debug
</span></span></code></pre></div><p>Levels cascade: <code>error</code> &lt; <code>warning</code> &lt; <code>info</code> &lt; <code>debug</code>. I run at <code>debug</code> during development and <code>warning</code> in production. The debug level shows every browser console message, network request, and page event — noisy but essential when a test is failing silently.</p>
<h3 id="output-modes">Output Modes</h3>
<p>Playwright MCP supports two output modes:</p>
<ul>
<li><strong><code>stdout</code></strong> (default) — Output goes to stdout. Good for local development.</li>
<li><strong><code>file</code></strong> — Output goes to <code>--output-dir</code>. Essential for CI/CD where you need to collect artifacts.</li>
</ul>
<p>Set <code>--output-max-size</code> to control when old output files get evicted. I use 50MB for CI runs.</p>
<h3 id="browser-tracing-and-step-debugging">Browser Tracing and Step Debugging</h3>
<p>Two features that save me hours:</p>
<ul>
<li><strong><code>browser_start_tracing</code> / <code>browser_stop_tracing</code></strong> — Records a Playwright trace that you can open in the Playwright Trace Viewer. This shows every network request, DOM mutation, and screenshot. I start tracing before any flaky test and stop it after.</li>
<li><strong><code>browser_pause</code> / <code>browser_resume</code></strong> — Pauses the browser at the current state. <code>browser_resume</code> with <code>step=true</code> pauses before the next action, letting you step through interactions one at a time.</li>
<li><strong><code>browser_set_debugger</code></strong> — Pauses at a specific <code>&lt;file&gt;:&lt;line&gt;</code> location. Think of it as a breakpoint for browser automation.</li>
</ul>
<h3 id="headless-vs-headed">Headless vs Headed</h3>
<p>By default, Playwright MCP runs <strong>headed</strong> (you see the browser window). This is great for debugging but annoying in CI. Use <code>--headless</code> or <code>PLAYWRIGHT_MCP_HEADLESS=true</code> for automated runs.</p>
<p>For containerized environments, you&rsquo;ll likely need <code>--no-sandbox</code>. And if you&rsquo;re testing against a local dev server with self-signed certs, <code>--ignore-https-errors</code> is your friend.</p>
<h3 id="session-persistence">Session Persistence</h3>
<p>Playwright MCP stores session data (cookies, localStorage) at a platform-specific location. Use <code>--user-data-dir</code> to override this, or <code>--isolated</code> to keep everything in memory (no disk writes). The <code>--save-session</code> flag saves the session into the output directory for later replay.</p>
<p>For more on browser-based agent workflows, see the <a href="/posts/github-copilot-browser-tools-guide-2026/">GitHub Copilot Browser Tools guide</a>.</p>
<h2 id="cross-tool-debugging-patterns">Cross-Tool Debugging Patterns</h2>
<p>After using all four tools, here are the patterns that apply across the board:</p>
<h3 id="1-environment-variables-are-your-first-debug-tool">1. Environment Variables Are Your First Debug Tool</h3>
<p>Every one of these tools respects environment variables for debug configuration. Before diving into config files or interactive commands, check what env vars are available:</p>
<ul>
<li>Claude Code: <code>CLAUDE_CODE_*</code> vars</li>
<li>Codex CLI: <code>CODEX_HOME</code>, <code>OPENAI_API_KEY</code></li>
<li>GitHub MCP: <code>GITHUB_PERSONAL_ACCESS_TOKEN</code>, <code>GITHUB_TOKEN</code></li>
<li>Playwright MCP: <code>PLAYWRIGHT_MCP_*</code> vars</li>
</ul>
<h3 id="2-mcp-connection-issues-are-the-most-common-failure">2. MCP Connection Issues Are the Most Common Failure</h3>
<p>All four tools support MCP, and MCP connection issues are the single most common source of debugging pain. The fix is almost always:</p>
<ul>
<li>Check that the MCP server process is actually running</li>
<li>Verify the transport (stdio vs HTTP) matches what the agent expects</li>
<li>Check environment variables are being forwarded to the MCP server process</li>
<li>For HTTP transport, verify the port and URL</li>
</ul>
<h3 id="3-session-persistence-is-not-a-given">3. Session Persistence Is Not a Given</h3>
<p>Claude Code and Codex CLI both support session resume after crashes. GitHub MCP and Playwright MCP don&rsquo;t — they&rsquo;re stateless by design. If you&rsquo;re doing long-running work, prefer Claude Code or Codex CLI, or build your own session management around the stateless tools.</p>
<h3 id="4-log-levels-matter">4. Log Levels Matter</h3>
<p>Claude Code has <code>/doctor</code>, Codex CLI has <code>codex-tui.log</code>, Playwright MCP has <code>--console-level</code>, and GitHub MCP has structured error types. Learn the debug entry point for each tool before you need it — reading docs during an outage is never fun.</p>
<h2 id="quick-reference-where-to-look-first">Quick Reference: Where to Look First</h2>
<table>
  <thead>
      <tr>
          <th>Tool</th>
          <th>First Debug Step</th>
          <th>Log Location</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>Claude Code</td>
          <td><code>/doctor</code> or <code>claude doctor</code></td>
          <td>Heap dump via <code>/heapdump</code></td>
      </tr>
      <tr>
          <td>Codex CLI</td>
          <td><code>tail -f $CODEX_HOME/log/codex-tui.log</code></td>
          <td><code>$CODEX_HOME/log/</code></td>
      </tr>
      <tr>
          <td>GitHub MCP</td>
          <td>Check <code>GITHUB_PERSONAL_ACCESS_TOKEN</code></td>
          <td>Structured errors in context</td>
      </tr>
      <tr>
          <td>Playwright MCP</td>
          <td><code>--console-level debug</code></td>
          <td><code>--output-dir</code> or stdout</td>
      </tr>
  </tbody>
</table>
<p>The reality is that AI coding agents in 2026 are still maturing. Each tool has its own debugging philosophy — Claude Code gives you interactive commands, Codex CLI gives you config files, GitHub MCP gives you structured errors, and Playwright MCP gives you browser-level tracing. None of them have a unified debug dashboard or a single log file that tells you everything.</p>
<p>But if you learn the debug entry point for each tool and keep a terminal tailing the relevant log, you&rsquo;ll cut your troubleshooting time by at least half. That&rsquo;s been my experience, and I&rsquo;ve been doing this full-time since early 2026.</p>
<h2 id="faq">FAQ</h2>
<h3 id="why-does-claude-code-freeze-or-slow-down-on-large-codebases">Why does Claude Code freeze or slow down on large codebases?</h3>
<p>Claude Code&rsquo;s context window fills up as your conversation grows. Run <code>/compact</code> to reduce context size, or <code>/compact keep only the plan and the diff</code> to strip everything except your active work. If memory usage is high, run <code>/heapdump</code> and inspect the heap snapshot in Chrome DevTools. For very large codebases, consider using sub-agents to parallelize work across smaller context windows.</p>
<h3 id="codex-cli-keeps-failing-to-connect-to-my-mcp-server-what-should-i-check-first">Codex CLI keeps failing to connect to my MCP server. What should I check first?</h3>
<p>Three things in order: (1) Verify the MCP server process is running — <code>ps aux | grep &lt;server-name&gt;</code>. (2) Check your <code>~/.codex/config.toml</code> <code>[mcp_servers]</code> section for typos in the command, args, or <code>bearer_token_env_var</code>. (3) If using HTTP transport, confirm the port matches and the URL is reachable. Codex&rsquo;s <code>codex-tui.log</code> (enabled by setting <code>CODEX_HOME</code> explicitly) will show connection errors in plain text.</p>
<h3 id="can-i-use-github-mcp-server-with-github-enterprise-server">Can I use GitHub MCP Server with GitHub Enterprise Server?</h3>
<p>Yes. Start the local Docker server with <code>--gh-host https://your-ghes-instance.com</code>. You&rsquo;ll need a GitHub App for authentication — PAT and OAuth won&rsquo;t work with GHES. The server also supports <code>ghe.com</code> subdomains with the same <code>--gh-host</code> flag. For the remote server at <code>api.githubcopilot.com/mcp/</code>, GHES is not supported.</p>
<h3 id="playwright-mcp-opens-a-browser-window-in-ci-how-do-i-make-it-headless">Playwright MCP opens a browser window in CI. How do I make it headless?</h3>
<p>Set <code>--headless</code> on the command line or <code>PLAYWRIGHT_MCP_HEADLESS=true</code> in your environment. By default Playwright MCP runs headed, which is useful for local debugging but breaks headless CI runners. You&rsquo;ll also want <code>--no-sandbox</code> in containerized environments and <code>--output-mode file</code> with <code>--output-dir</code> to collect artifacts.</p>
<h3 id="which-tool-has-the-best-session-recovery-after-a-crash">Which tool has the best session recovery after a crash?</h3>
<p>Claude Code and Codex CLI both support session resume — <code>claude --resume</code> and Codex&rsquo;s automatic token refresh, respectively. GitHub MCP and Playwright MCP are stateless by design and don&rsquo;t persist sessions. For long-running work where crashes are costly, Claude Code&rsquo;s <code>/compact</code> + <code>--resume</code> workflow is the most robust. For stateless tools, build your own checkpointing by saving state to disk at each step.</p>
]]></content:encoded></item></channel></rss>