<?xml version="1.0" encoding="utf-8" standalone="yes"?><rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom" xmlns:content="http://purl.org/rss/1.0/modules/content/"><channel><title>Debugging on RockB</title><link>https://baeseokjae.github.io/tags/debugging/</link><description>Recent content in Debugging on RockB</description><image><title>RockB</title><url>https://baeseokjae.github.io/images/og-default.png</url><link>https://baeseokjae.github.io/images/og-default.png</link></image><generator>Hugo</generator><language>en-us</language><lastBuildDate>Thu, 16 Jul 2026 21:05:12 +0000</lastBuildDate><atom:link href="https://baeseokjae.github.io/tags/debugging/index.xml" rel="self" type="application/rss+xml"/><item><title>MCP Snoop Review 2026: Wireshark for MCP — Transparent Proxy for Tool Call Debugging</title><link>https://baeseokjae.github.io/posts/mcpsnoop-wireshark-mcp-2026/</link><pubDate>Thu, 16 Jul 2026 21:05:12 +0000</pubDate><guid>https://baeseokjae.github.io/posts/mcpsnoop-wireshark-mcp-2026/</guid><description>mcpsnoop is a transparent MCP proxy that captures every JSON-RPC frame between client and server in real time, making it the Wireshark for MCP debugging.</description><content:encoded><![CDATA[<h2 id="what-is-mcpsnoop--wireshark-for-mcp">What is mcpsnoop? — Wireshark for MCP</h2>
<p>mcpsnoop is an open-source transparent proxy for the Model Context Protocol (MCP) that sits between your MCP client and server, capturing every JSON-RPC frame in real time. Built in Go with a Bubble Tea terminal UI, it gives developers the same kind of deep packet inspection for MCP traffic that Wireshark provides for network packets. Launched in late June 2026, mcpsnoop reached 270 GitHub stars in just three weeks and is rapidly becoming the go-to tool for MCP debugging.</p>
<h2 id="the-problem--why-mcp-inspector-falls-short">The Problem — Why MCP Inspector Falls Short</h2>
<p>The official MCP Inspector from MCPJam is the most popular MCP debugging tool with 2,068 stars, but it has a fundamental limitation: it connects to your MCP server as its own client. This means it never sees the actual traffic between your real client and server. When a tool silently isn&rsquo;t called, capabilities don&rsquo;t line up, or a call just hangs, MCP Inspector can&rsquo;t help you see what your actual client is sending or receiving.</p>
<p>This is the equivalent of debugging a network issue by running a separate ping test instead of looking at the actual packets on the wire. You need a tool that sits in the real data path — and that is exactly what mcpsnoop provides.</p>
<h2 id="how-mcpsnoop-works--transparent-proxy-architecture">How mcpsnoop Works — Transparent Proxy Architecture</h2>
<p>mcpsnoop uses a dual-role architecture that separates the concerns of traffic capture and session inspection. The system has two components: a <strong>shim</strong> that forwards frames and copies them to a hub, and a <strong>hub</strong> that runs the TUI and stores session data.</p>
<h3 id="stdio-shim-mode-mcpsnoop--">Stdio Shim Mode (mcpsnoop &ndash; <!-- raw HTML omitted -->)</h3>
<p>In stdio mode, you wrap your MCP server command with mcpsnoop:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-bash" data-lang="bash"><span style="display:flex;"><span>mcpsnoop -- npx @anthropic-ai/claude-code-server
</span></span></code></pre></div><p>The shim intercepts every stdin/stdout message between the client and server, forwards them transparently, and copies each JSON-RPC frame to the hub for live inspection. The client and server never know mcpsnoop is there — it is a completely transparent proxy.</p>
<h3 id="http-reverse-proxy-mode-mcpsnoop-http">HTTP Reverse Proxy Mode (mcpsnoop http)</h3>
<p>For HTTP-based MCP servers, mcpsnoop provides a reverse proxy mode:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-bash" data-lang="bash"><span style="display:flex;"><span>mcpsnoop http --target http://localhost:8080
</span></span></code></pre></div><p>This starts an HTTP server that forwards all requests to the target MCP server while capturing every request and response for inspection. This mode is ideal for debugging MCP servers that communicate over HTTP/SSE.</p>
<h3 id="dual-role-design-shim--hub--tui">Dual-Role Design: Shim + Hub + TUI</h3>
<p>The shim runs as a lightweight stdio forwarder that copies frames to the hub over a local connection. The hub aggregates frames from one or more shims, stores them in a session store, and feeds them to the Bubble Tea TUI for real-time display. This separation means you can run the shim on a remote machine and the hub locally, or run multiple shims feeding into a single hub for cross-session analysis.</p>
<h2 id="key-features-deep-dive">Key Features Deep Dive</h2>
<h3 id="live-tui-with-real-time-frame-inspection">Live TUI with Real-Time Frame Inspection</h3>
<p>The terminal UI is built with the Bubble Tea framework and provides a real-time view of every MCP frame flowing through the proxy. Each frame is displayed with its direction (client→server or server→client), method name, ID, status, and timing. You can scroll through the live stream, select individual frames to see their full JSON-RPC payload, and watch the conversation unfold in real time.</p>
<h3 id="filtering-system-tool-method-id-dir-kind-status">Filtering System (tool:, method:, id:, dir:, kind:, status:)</h3>
<p>mcpsnoop&rsquo;s filtering system is one of its most powerful features. You can filter the live frame stream using keybindings that support:</p>
<ul>
<li><strong>tool:</strong> — Filter by tool name (e.g., <code>tool:search_files</code>)</li>
<li><strong>method:</strong> — Filter by JSON-RPC method (e.g., <code>method:tools/call</code>)</li>
<li><strong>id:</strong> — Filter by request ID</li>
<li><strong>dir:</strong> — Filter by direction (<code>dir:in</code> for server responses, <code>dir:out</code> for client requests)</li>
<li><strong>kind:</strong> — Filter by frame kind (request, response, notification, error)</li>
<li><strong>status:</strong> — Filter by status (success, error, pending)</li>
</ul>
<p>This makes it trivial to isolate a single tool call among hundreds of frames in a busy session.</p>
<h3 id="session-export-json-html-text-otlp">Session Export (JSON, HTML, Text, OTLP)</h3>
<p>mcpsnoop supports exporting captured sessions in multiple formats:</p>
<ul>
<li><strong>JSON</strong> — Full structured export for programmatic analysis</li>
<li><strong>HTML</strong> — Interactive HTML report for sharing with team members who don&rsquo;t have mcpsnoop installed</li>
<li><strong>Text</strong> — Plain text log for quick review or inclusion in bug reports</li>
<li><strong>OTLP (OpenTelemetry)</strong> — Export to OpenTelemetry-compatible backends like Jaeger, Grafana Tempo, or Datadog for integration with existing observability infrastructure</li>
</ul>
<p>The OTLP export is particularly valuable for teams that already use distributed tracing — it turns MCP tool calls into spans that appear alongside your application traces.</p>
<h3 id="ci-check-mode--gate-builds-on-mcp-errors">CI Check Mode — Gate Builds on MCP Errors</h3>
<p>One of mcpsnoop&rsquo;s most unique features is its CI check mode. You can run mcpsnoop in a CI pipeline and it will:</p>
<ul>
<li>Fail the build if any MCP errors occur</li>
<li>Flag invalid JSON-RPC frames</li>
<li>Warn on slow calls exceeding a configurable threshold</li>
<li>Detect pending calls that never received a response</li>
<li>Report warnings for suspicious patterns</li>
</ul>
<p>This turns MCP debugging from a reactive firefighting exercise into a proactive quality gate. If your MCP server starts returning errors after a deployment, your CI pipeline catches it before it reaches production.</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-bash" data-lang="bash"><span style="display:flex;"><span>mcpsnoop --check -- npx @anthropic-ai/claude-code-server
</span></span></code></pre></div><h3 id="remote-ssh-tunnel--debug-remote-servers">Remote SSH Tunnel — Debug Remote Servers</h3>
<p>mcpsnoop supports SSH tunneling for debugging MCP servers running on remote machines. You can start a shim on a remote server and connect to it from your local machine:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-bash" data-lang="bash"><span style="display:flex;"><span>mcpsnoop --ssh user@remote-server -- npx @anthropic-ai/claude-code-server
</span></span></code></pre></div><p>The TUI runs locally while the shim runs on the remote machine, giving you the same real-time inspection experience for remote MCP traffic.</p>
<h3 id="secret-redaction--safe-trace-sharing">Secret Redaction — Safe Trace Sharing</h3>
<p>When sharing MCP session traces for debugging or collaboration, you often need to redact sensitive information like API keys, tokens, or personal data. mcpsnoop provides three levels of secret redaction:</p>
<ul>
<li><strong>Key-based</strong> — Redact values for specific JSON keys</li>
<li><strong>JSONPath-based</strong> — Redact values matching a JSONPath expression</li>
<li><strong>Regex-based</strong> — Redact values matching a regular expression pattern</li>
</ul>
<p>Redaction rules can be configured in the <code>.mcpsnoop.toml</code> config file, making them reusable across sessions.</p>
<h3 id="call-replay--re-run-captured-tool-calls">Call Replay — Re-run Captured Tool Calls</h3>
<p>mcpsnoop can replay captured tool calls against a server. This is invaluable for regression testing — if a tool call worked in one session but fails in another, you can replay the exact same request to isolate whether the issue is in the client, server, or network.</p>
<h2 id="mcpsnoop-vs-competitors">mcpsnoop vs. Competitors</h2>
<p>The MCP debugging ecosystem has grown rapidly, with at least six dedicated tools as of mid-2026. Here is how mcpsnoop compares:</p>
<table>
	<thead>
			<tr>
					<th>Feature</th>
					<th>mcpsnoop</th>
					<th>MCP Inspector</th>
					<th>reloaderoo</th>
					<th>mcp-interceptor</th>
					<th>mcpobservatory</th>
			</tr>
	</thead>
	<tbody>
			<tr>
					<td><strong>Stars</strong></td>
					<td>270★</td>
					<td>2,068★</td>
					<td>125★</td>
					<td>7★</td>
					<td>4★</td>
			</tr>
			<tr>
					<td><strong>Language</strong></td>
					<td>Go</td>
					<td>TypeScript</td>
					<td>TypeScript</td>
					<td>TypeScript</td>
					<td>Rust</td>
			</tr>
			<tr>
					<td><strong>License</strong></td>
					<td>MIT</td>
					<td>MIT</td>
					<td>MIT</td>
					<td>MIT</td>
					<td>Apache-2.0</td>
			</tr>
			<tr>
					<td><strong>Transparent proxy</strong></td>
					<td>✅ Yes</td>
					<td>❌ No (own client)</td>
					<td>✅ Yes</td>
					<td>✅ Yes</td>
					<td>✅ Yes</td>
			</tr>
			<tr>
					<td><strong>Live TUI</strong></td>
					<td>✅ Bubble Tea</td>
					<td>✅ Web UI</td>
					<td>❌ CLI only</td>
					<td>❌ CLI only</td>
					<td>✅ Timeline</td>
			</tr>
			<tr>
					<td><strong>CI check mode</strong></td>
					<td>✅ Yes</td>
					<td>❌ No</td>
					<td>❌ No</td>
					<td>❌ No</td>
					<td>❌ No</td>
			</tr>
			<tr>
					<td><strong>OTLP export</strong></td>
					<td>✅ Yes</td>
					<td>❌ No</td>
					<td>❌ No</td>
					<td>❌ No</td>
					<td>❌ No</td>
			</tr>
			<tr>
					<td><strong>SSH tunnel</strong></td>
					<td>✅ Yes</td>
					<td>❌ No</td>
					<td>❌ No</td>
					<td>❌ No</td>
					<td>❌ No</td>
			</tr>
			<tr>
					<td><strong>Secret redaction</strong></td>
					<td>✅ Yes</td>
					<td>❌ No</td>
					<td>❌ No</td>
					<td>❌ No</td>
					<td>❌ No</td>
			</tr>
			<tr>
					<td><strong>Call replay</strong></td>
					<td>✅ Yes</td>
					<td>❌ No</td>
					<td>❌ No</td>
					<td>❌ No</td>
					<td>✅ Yes</td>
			</tr>
			<tr>
					<td><strong>Session export</strong></td>
					<td>JSON, HTML, Text, OTLP</td>
					<td>Limited</td>
					<td>JSON</td>
					<td>JSON</td>
					<td>JSON</td>
			</tr>
			<tr>
					<td><strong>Single binary</strong></td>
					<td>✅ Yes</td>
					<td>❌ No (Node.js)</td>
					<td>❌ No (Node.js)</td>
					<td>❌ No (Node.js)</td>
					<td>✅ Yes</td>
			</tr>
			<tr>
					<td><strong>Created</strong></td>
					<td>June 2026</td>
					<td>May 2025</td>
					<td>June 2025</td>
					<td>June 2025</td>
					<td>April 2026</td>
			</tr>
	</tbody>
</table>
<h3 id="vs-mcp-inspector-2068">vs. MCP Inspector (2,068★)</h3>
<p>MCP Inspector is the most established tool with the largest community, but it serves a different purpose. It is an evaluation and testing platform that connects to MCP servers as its own client — great for testing a server in isolation, but useless for debugging real client-server interactions. mcpsnoop is the better choice when you need to debug actual production traffic.</p>
<h3 id="vs-reloaderoo-125">vs. reloaderoo (125★)</h3>
<p>reloaderoo is a capable debugging proxy with a CLI focus. It lacks the live TUI, CI check mode, OTLP export, and SSH tunnel features that mcpsnoop offers. reloaderoo is more mature (created June 2025) but has not kept pace with mcpsnoop&rsquo;s rapid feature development.</p>
<h3 id="vs-mcp-interceptor-7">vs. mcp-interceptor (7★)</h3>
<p>mcp-interceptor is a lightweight proxy that focuses on request interception via a proxy URL. It is simple and gets the job done for basic debugging, but lacks the depth of features that mcpsnoop provides for serious MCP observability work.</p>
<h3 id="vs-mcpobservatory-4">vs. mcpobservatory (4★)</h3>
<p>mcpobservatory is the closest competitor to mcpsnoop in terms of positioning — a Rust-based drop-in proxy with timeline, replay, and diff capabilities. It is local-first with no telemetry and ships as a single binary. However, with only 4 stars and a much smaller community, it has not gained the same traction as mcpsnoop.</p>
<h2 id="getting-started--quick-setup-guide">Getting Started — Quick Setup Guide</h2>
<p>Getting started with mcpsnoop takes less than a minute:</p>
<ol>
<li>
<p><strong>Download the binary</strong> from the <a href="https://github.com/kerlenton/mcpsnoop/releases">GitHub releases page</a> — it is a single Go binary with no runtime dependencies.</p>
</li>
<li>
<p><strong>Wrap your MCP server:</strong></p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-bash" data-lang="bash"><span style="display:flex;"><span>mcpsnoop -- npx @anthropic-ai/claude-code-server
</span></span></code></pre></div></li>
<li>
<p><strong>Start inspecting:</strong> The TUI opens automatically, showing live frames as they flow through the proxy.</p>
</li>
<li>
<p><strong>Filter and explore:</strong> Use the keybindings to filter by tool, method, direction, or status. Select a frame to see its full JSON-RPC payload.</p>
</li>
<li>
<p><strong>Export when needed:</strong> Press the export key to save the session as JSON, HTML, or text.</p>
</li>
</ol>
<p>For HTTP servers:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-bash" data-lang="bash"><span style="display:flex;"><span>mcpsnoop http --target http://localhost:8080 --port <span style="color:#ae81ff">9090</span>
</span></span></code></pre></div><p>For CI pipelines:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-bash" data-lang="bash"><span style="display:flex;"><span>mcpsnoop --check --threshold <span style="color:#ae81ff">5000</span> -- npx @anthropic-ai/claude-code-server
</span></span></code></pre></div><p>Configuration can be persisted in <code>.mcpsnoop.toml</code>:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-toml" data-lang="toml"><span style="display:flex;"><span>[<span style="color:#a6e22e">redact</span>]
</span></span><span style="display:flex;"><span><span style="color:#a6e22e">keys</span> = [<span style="color:#e6db74">&#34;apiKey&#34;</span>, <span style="color:#e6db74">&#34;token&#34;</span>, <span style="color:#e6db74">&#34;password&#34;</span>]
</span></span><span style="display:flex;"><span><span style="color:#a6e22e">jsonpath</span> = [<span style="color:#e6db74">&#34;$.credentials.*&#34;</span>]
</span></span><span style="display:flex;"><span><span style="color:#a6e22e">regex</span> = [<span style="color:#e6db74">&#34;sk-[a-zA-Z0-9]+&#34;</span>]
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>[<span style="color:#a6e22e">check</span>]
</span></span><span style="display:flex;"><span><span style="color:#a6e22e">error_threshold</span> = <span style="color:#ae81ff">0</span>
</span></span><span style="display:flex;"><span><span style="color:#a6e22e">slow_call_ms</span> = <span style="color:#ae81ff">5000</span>
</span></span><span style="display:flex;"><span><span style="color:#a6e22e">warn_pending</span> = <span style="color:#66d9ef">true</span>
</span></span></code></pre></div><h2 id="real-world-use-cases">Real-World Use Cases</h2>
<h3 id="debugging-silent-tool-call-failures">Debugging Silent Tool Call Failures</h3>
<p>The most common MCP debugging scenario: a tool is supposed to be called but never executes, or returns an unexpected result. With mcpsnoop, you can see exactly what the client sent and what the server responded with. If the server returned an error that the client silently ignored, you will see it in the frame stream. If the client never sent the request, you will see that too.</p>
<h3 id="cicd-pipeline-quality-gates-for-mcp">CI/CD Pipeline Quality Gates for MCP</h3>
<p>Teams deploying MCP servers can use mcpsnoop&rsquo;s CI check mode as a quality gate. Before deploying a new version of an MCP server, run the test suite through mcpsnoop with <code>--check</code>. If the new version introduces errors, slow responses, or invalid frames, the build fails. This catches regressions before they reach production.</p>
<h3 id="post-mortem-analysis-of-agent-runs">Post-Mortem Analysis of Agent Runs</h3>
<p>When an AI agent produces unexpected results, mcpsnoop&rsquo;s session export lets you save the full MCP conversation for post-mortem analysis. Export to HTML for a shareable report, or export to OTLP to see MCP tool calls alongside application traces in your observability platform.</p>
<h3 id="remote-server-debugging-via-ssh">Remote Server Debugging via SSH</h3>
<p>If your MCP server runs on a remote machine or in a container, mcpsnoop&rsquo;s SSH tunnel mode lets you debug it from your local machine. The shim runs on the remote server, the TUI runs locally, and you see frames in real time as if the server were running locally.</p>
<h2 id="limitations-and-considerations">Limitations and Considerations</h2>
<p>mcpsnoop is still very young — it reached v0.9.0 within 18 days of its first release. While the rapid iteration is impressive, it means the tool is not yet stable. Users should expect breaking changes between versions.</p>
<p>The tool currently supports stdio and HTTP modes, but does not support WebSocket-based MCP transports. If your MCP server uses WebSockets, you will need to use a different tool or wait for future releases.</p>
<p>The Bubble Tea TUI, while functional and fast, is not as polished as a web-based UI. Users accustomed to graphical debugging tools may find the terminal interface less intuitive at first.</p>
<p>mcpsnoop has 270 stars and a small community. While the developer is actively maintaining the project, long-term sustainability is not guaranteed. Teams adopting mcpsnoop should have a fallback debugging strategy.</p>
<h2 id="verdict--is-mcpsnoop-the-wireshark-for-mcp">Verdict — Is mcpsnoop the Wireshark for MCP?</h2>
<p>Yes, the comparison is fair. mcpsnoop does for MCP traffic what Wireshark does for network packets — it sits transparently in the data path, captures every frame in real time, and provides powerful filtering and inspection tools. No other MCP debugging tool offers the same combination of transparent proxy architecture, live TUI, CI check mode, OTLP export, SSH tunneling, and secret redaction.</p>
<p>For developers building MCP servers or clients, mcpsnoop is an essential addition to your debugging toolkit. It fills a gap that the official MCP Inspector cannot address — real client-server traffic inspection. The rapid development pace (9 releases in 18 days) and growing community suggest mcpsnoop is here to stay.</p>
<p>If you are doing any serious MCP development in 2026, you need mcpsnoop in your toolbox.</p>
<h2 id="faq">FAQ</h2>
<h3 id="what-is-mcpsnoop-and-how-does-it-differ-from-mcp-inspector">What is mcpsnoop and how does it differ from MCP Inspector?</h3>
<p>mcpsnoop is a transparent proxy that sits between your MCP client and server, capturing real traffic as it flows. MCP Inspector connects to your server as its own client and never sees actual client-server interactions. mcpsnoop is for debugging real production traffic; MCP Inspector is for testing servers in isolation.</p>
<h3 id="can-mcpsnoop-be-used-in-cicd-pipelines">Can mcpsnoop be used in CI/CD pipelines?</h3>
<p>Yes, mcpsnoop has a dedicated CI check mode (<code>--check</code>) that fails the build on MCP errors, invalid frames, slow calls, and pending calls that never received a response. This makes it a proactive quality gate for MCP server deployments.</p>
<h3 id="does-mcpsnoop-support-http-based-mcp-servers">Does mcpsnoop support HTTP-based MCP servers?</h3>
<p>Yes, mcpsnoop supports HTTP reverse proxy mode (<code>mcpsnoop http --target &lt;url&gt;</code>) for HTTP/SSE-based MCP servers. It also supports stdio mode for command-line MCP servers.</p>
<h3 id="can-i-share-mcpsnoop-session-traces-with-my-team">Can I share mcpsnoop session traces with my team?</h3>
<p>Yes, mcpsnoop supports session export to JSON, HTML, and text formats. The HTML export creates an interactive report that anyone can view in a browser without installing mcpsnoop. You can also export to OTLP for integration with OpenTelemetry-compatible observability platforms.</p>
<h3 id="is-mcpsnoop-safe-to-use-with-production-api-keys-and-secrets">Is mcpsnoop safe to use with production API keys and secrets?</h3>
<p>Yes, mcpsnoop includes built-in secret redaction with three levels: key-based, JSONPath-based, and regex-based value scrubbing. You can configure redaction rules in <code>.mcpsnoop.toml</code> to automatically redact sensitive information before saving or sharing session traces.</p>
]]></content:encoded></item><item><title>Coding Agent Debug Logs Guide 2026: Claude Code, Codex, GitHub MCP, and Playwright MCP</title><link>https://baeseokjae.github.io/posts/coding-agent-debug-logs-guide-2026/</link><pubDate>Sat, 04 Jul 2026 04:00:00 +0000</pubDate><guid>https://baeseokjae.github.io/posts/coding-agent-debug-logs-guide-2026/</guid><description>Complete guide to debugging AI coding agents in 2026: Claude Code /doctor and heapdump, Codex CLI log_dir and TOML config, GitHub MCP error types, and Playwright MCP tracing tools.</description><content:encoded><![CDATA[<p>AI coding agents ship code faster than ever, but when they break, the debugging experience is nothing like a traditional stack trace. You don&rsquo;t get a line number and a segfault — you get a silent hang, a garbled terminal, a 529 from the API, or an MCP server that just won&rsquo;t connect. After spending the last year running Claude Code, Codex CLI, GitHub MCP Server, and Playwright MCP in production pipelines, I&rsquo;ve collected the debug patterns that actually work. Here&rsquo;s the field guide I wish I&rsquo;d had.</p>
<h2 id="claude-code-the-most-debuggable-agent-if-you-know-where-to-look">Claude Code: The Most Debuggable Agent, If You Know Where to Look</h2>
<p>Claude Code has the richest debug tooling of any coding agent in 2026 — I covered its overall capabilities in the <a href="/posts/ai-coding-agent-capability-matrix-2026/">AI Coding Agent Capability Matrix 2026</a> — but most of it is hidden behind slash commands you&rsquo;d never discover unless someone told you.</p>
<h3 id="the-doctor-command--your-first-stop">The /doctor Command — Your First Stop</h3>
<p>Before you dig into logs, run <code>/doctor</code> inside Claude Code or <code>claude doctor</code> from the shell. It runs an automated health check covering installation integrity, settings validity, MCP server connectivity, and context usage statistics. I&rsquo;ve seen it catch a stale Node.js version that was silently breaking MCP tool calls — something that would have taken me an hour to trace manually.</p>
<p>When Claude Code feels sluggish or behaves oddly, <code>/doctor</code> is the fastest triage tool. It reports back in under five seconds and flags issues with severity levels.</p>
<h3 id="heapdump-for-memory-mysteries">/heapdump for Memory Mysteries</h3>
<p>If Claude Code is eating RAM and you don&rsquo;t know why, run <code>/heapdump</code>. It writes a JavaScript heap snapshot to <code>~/Desktop/</code> (or your home directory on Linux) as a <code>.heapsnapshot</code> file. Open that in Chrome DevTools → Memory → Load, and you can inspect object allocations, retained sizes, and closure chains. I used this to discover a plugin that was holding a reference to the entire file tree in memory — 1.2 GB of retained objects from a 50 MB project.</p>
<h3 id="safe-mode-and-compaction">Safe Mode and Compaction</h3>
<p>When performance degrades mid-session, two commands save the day:</p>
<ul>
<li><strong><code>claude --safe-mode</code></strong> — launches Claude Code with zero customizations: no plugins, no MCP servers, no hooks. If the problem disappears, one of your customizations is the culprit. Binary search your MCP servers from there.</li>
<li><strong><code>/compact</code></strong> — reduces context size by summarizing older conversation turns. I run <code>/compact keep only the plan and the diff</code> every 30-40 turns in a long session. Without it, auto-compaction kicks in and starts thrashing, which actually makes things worse.</li>
</ul>
<h3 id="session-recovery">Session Recovery</h3>
<p>Crashes happen. <code>claude --resume</code> in the same directory picks up your previous session. If the session file is corrupted, <code>/clear</code> starts fresh without losing your CLAUDE.md project config. And <code>/feedback</code> sends debug logs directly to Anthropic — use it when you hit something that feels like a real bug. For sharing session outputs, check out the <a href="/posts/claude-code-artifacts-guide-2026-live-shareable-previews-from-ai-coding-sessions/">Claude Code Artifacts guide</a> for live previews.</p>
<h3 id="common-claude-code-issues-ive-hit">Common Claude Code Issues I&rsquo;ve Hit</h3>
<table>
	<thead>
			<tr>
					<th>Symptom</th>
					<th>Likely Cause</th>
					<th>Fix</th>
			</tr>
	</thead>
	<tbody>
			<tr>
					<td>High CPU, fan spinning</td>
					<td>Context too large</td>
					<td><code>/compact</code> or <code>/clear</code>, restart</td>
			</tr>
			<tr>
					<td>Garbled terminal text</td>
					<td>GPU acceleration in editor</td>
					<td><code>terminal.integrated.gpuAcceleration: off</code></td>
			</tr>
			<tr>
					<td>Slow search on WSL</td>
					<td>Missing ripgrep</td>
					<td>Install ripgrep, set <code>USE_BUILTIN_RIPGREP=0</code></td>
			</tr>
			<tr>
					<td>529 Overloaded</td>
					<td>API capacity</td>
					<td>Wait 30s, retry. Use off-peak hours</td>
			</tr>
			<tr>
					<td>MCP servers not loading</td>
					<td>Config syntax error</td>
					<td><code>claude --safe-mode</code> to isolate</td>
			</tr>
	</tbody>
</table>
<h2 id="codex-cli-toml-config-and-the-hidden-tui-log">Codex CLI: TOML Config and the Hidden TUI Log</h2>
<p>Codex CLI takes a different approach. Where Claude Code has slash commands, Codex has a configuration file and a log directory.</p>
<h3 id="finding-the-logs">Finding the Logs</h3>
<p>Codex writes logs to <code>$CODEX_HOME/log</code> by default. Setting <code>CODEX_HOME</code> explicitly also enables the opt-in plaintext TUI log (<code>codex-tui.log</code>), which is invaluable for debugging startup issues. If Codex CLI fails to launch or crashes immediately, check that file first — it captures what happened before the TUI even started.</p>
<p>The config lives at <code>~/.codex/config.toml</code> in TOML format. Here&rsquo;s a debug-oriented config I use:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-toml" data-lang="toml"><span style="display:flex;"><span>[<span style="color:#a6e22e">logging</span>]
</span></span><span style="display:flex;"><span><span style="color:#a6e22e">log_dir</span> = <span style="color:#e6db74">&#34;/home/me/.codex/logs&#34;</span>
</span></span><span style="display:flex;"><span><span style="color:#a6e22e">level</span> = <span style="color:#e6db74">&#34;debug&#34;</span>
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>[<span style="color:#a6e22e">mcp_servers</span>.<span style="color:#a6e22e">github</span>]
</span></span><span style="display:flex;"><span><span style="color:#a6e22e">command</span> = <span style="color:#e6db74">&#34;npx&#34;</span>
</span></span><span style="display:flex;"><span><span style="color:#a6e22e">args</span> = [<span style="color:#e6db74">&#34;-y&#34;</span>, <span style="color:#e6db74">&#34;@github/github-mcp-server&#34;</span>]
</span></span><span style="display:flex;"><span><span style="color:#a6e22e">env</span> = { <span style="color:#a6e22e">GITHUB_PERSONAL_ACCESS_TOKEN</span> = <span style="color:#e6db74">&#34;${GITHUB_TOKEN}&#34;</span> }
</span></span></code></pre></div><h3 id="authentication-debugging">Authentication Debugging</h3>
<p>Codex CLI caches login details in a plaintext file at <code>~/.codex/auth.json</code>. Tokens refresh automatically during use, but if you&rsquo;re getting auth errors, check:</p>
<ol>
<li><strong>MFA requirements</strong> — Email/password login requires MFA. Social login (Google, Microsoft, Apple) doesn&rsquo;t.</li>
<li><strong>Forced login method</strong> — Set <code>forced_login_method = &quot;chatgpt&quot;</code> or <code>&quot;api_key&quot;</code> in config.toml to enforce one auth path.</li>
<li><strong>Bearer token env var</strong> — For MCP HTTP servers, use <code>bearer_token_env_var</code> in the MCP server config to source tokens from environment variables rather than hardcoding.</li>
</ol>
<h3 id="mcp-oauth-gotcha">MCP OAuth Gotcha</h3>
<p>Codex CLI supports MCP OAuth with an optional fixed port for the callback server. If you&rsquo;re running in a devbox or behind ingress, set the base callback URL override. Without it, the OAuth redirect URL defaults to localhost and breaks in proxied environments.</p>
<h2 id="github-mcp-server-structured-error-types">GitHub MCP Server: Structured Error Types</h2>
<p>The GitHub MCP Server handles errors differently from the coding agents — it uses typed error objects that bubble through the MCP framework.</p>
<h3 id="error-types-youll-encounter">Error Types You&rsquo;ll Encounter</h3>
<p>The server defines two custom error types:</p>
<ul>
<li><strong><code>GitHubAPIError</code></strong> — for REST API errors. Contains <code>Message</code>, <code>Response</code> (the full <code>*github.Response</code> object), and <code>Err</code> fields. When a tool call fails with a 404 or 403, this is what you get.</li>
<li><strong><code>GitHubGraphQLError</code></strong> — for GraphQL API errors. Contains <code>Message</code> and <code>Err</code> fields. GraphQL errors are trickier because the HTTP status is 200 even when the query fails — you have to inspect the error body.</li>
</ul>
<p>Both types are stored in Go context via <code>errors.ContextWithGitHubErrors(ctx)</code> and retrieved with <code>errors.GetGitHubAPIErrors(ctx)</code> / <code>errors.GetGitHubGraphQLErrors(ctx)</code>. If you&rsquo;re building a custom MCP host, this middleware pattern lets you inspect all errors from a session in one place.</p>
<h3 id="authentication-options">Authentication Options</h3>
<p>The GitHub MCP Server supports three auth methods:</p>
<ol>
<li><strong>OAuth (browser-based)</strong> — Token lives in memory only. Best for local development.</li>
<li><strong>Personal Access Token</strong> — Set <code>GITHUB_PERSONAL_ACCESS_TOKEN</code> env var. Minimum scopes: <code>repo</code>, <code>read:packages</code>, <code>read:org</code>. Store in <code>.env</code>, never commit.</li>
<li><strong>GitHub App</strong> — For GitHub Enterprise Server (GHES/ghe.com). Use <code>--gh-host https://your-subdomain.ghe.com</code>.</li>
</ol>
<p>For enterprise setups, the <code>--gh-host</code> flag is required with the <code>https://</code> prefix. I&rsquo;ve seen teams waste hours debugging &ldquo;not found&rdquo; errors only to realize the host flag was missing.</p>
<h3 id="insiders-mode">Insiders Mode</h3>
<p>Add <code>/insiders</code> to the URL path or set the <code>X-MCP-Insiders</code> header for early access to new features. Useful for testing, but don&rsquo;t rely on it in production — insiders features can change without notice.</p>
<h2 id="playwright-mcp-browser-level-debugging">Playwright MCP: Browser-Level Debugging</h2>
<p>Playwright MCP is unique among these tools because it controls a real browser — similar to how <a href="/posts/github-copilot-browser-tools-guide-2026/">GitHub Copilot&rsquo;s browser tools</a> work in VS Code, but with more granular control. Its debug features are built around browser instrumentation rather than log files.</p>
<h3 id="console-level-filtering">Console Level Filtering</h3>
<p>Set the console output level with <code>--console-level &lt;level&gt;</code> or <code>PLAYWRIGHT_MCP_CONSOLE_LEVEL</code> env var. Values: <code>error</code>, <code>warning</code>, <code>info</code>, <code>debug</code>. Each level includes all more severe levels. I keep mine at <code>info</code> during development and bump to <code>debug</code> when a page interaction silently fails.</p>
<h3 id="output-modes">Output Modes</h3>
<p><code>--output-mode &lt;mode&gt;</code> controls where Playwright MCP writes its output. Two options:</p>
<ul>
<li><strong><code>stdout</code></strong> (default) — Output goes to standard out. Good for local development.</li>
<li><strong><code>file</code></strong> — Writes to <code>--output-dir &lt;path&gt;</code>. Essential for CI/CD pipelines where stdout is ephemeral.</li>
</ul>
<p>Set <code>--output-max-size &lt;bytes&gt;</code> to control when old output files get evicted. I use 50 MB for long-running test suites.</p>
<h3 id="browser-tracing-and-step-debugging">Browser Tracing and Step Debugging</h3>
<p>This is where Playwright MCP shines:</p>
<ul>
<li><strong><code>browser_start_tracing</code> / <code>browser_stop_tracing</code></strong> — Records a full browser trace (network requests, console logs, DOM mutations). Open the trace file in <code>https://trace.playwright.dev</code> for a frame-by-frame replay of what the browser did.</li>
<li><strong><code>browser_pause</code> / <code>browser_resume</code></strong> — Pauses and resumes execution. <code>browser_resume</code> with <code>step=true</code> pauses before the next action, giving you frame-by-frame control.</li>
<li><strong><code>browser_set_debugger</code></strong> — Pauses at a specific <code>&lt;file&gt;:&lt;line&gt;</code> location. Think of it as a breakpoint for browser automation.</li>
</ul>
<p>I use tracing when a Playwright MCP test passes locally but fails in CI. The trace replay shows exactly where the page state diverged — usually a timing issue or a missing network response.</p>
<h3 id="headless-vs-headed">Headless vs. Headed</h3>
<p>Playwright MCP runs <strong>headed by default</strong> (you see the browser window). For CI or headless servers, pass <code>--headless</code>. In containerized environments, you may also need <code>--no-sandbox</code> and <code>--ignore-https-errors</code> depending on your setup.</p>
<h3 id="persistent-profiles">Persistent Profiles</h3>
<p>Session data (cookies, localStorage, IndexedDB) is stored at a platform-specific location by default. Override with <code>--user-data-dir</code> to share sessions across runs. For security-sensitive tasks, use <code>--isolated</code> to keep the profile in memory only — nothing touches disk.</p>
<h2 id="cross-tool-debug-patterns">Cross-Tool Debug Patterns</h2>
<p>After debugging all four tools in anger, here are the patterns that apply across the board:</p>
<h3 id="1-environment-variables-are-your-first-log">1. Environment Variables Are Your First Log</h3>
<p>Every tool in this guide respects environment variables for debug configuration. Before you open a log file, check that your env vars are set correctly. A typo in <code>PLAYWRIGHT_MCP_CONSOLE_LEVEL</code> or a missing <code>GITHUB_PERSONAL_ACCESS_TOKEN</code> will fail silently.</p>
<h3 id="2-mcp-server-health-check">2. MCP Server Health Check</h3>
<p>When an MCP server won&rsquo;t connect, the problem is almost always one of three things:</p>
<ul>
<li><strong>Port conflict</strong> — Another process is using the port. <code>lsof -i :&lt;port&gt;</code> to check.</li>
<li><strong>Auth mismatch</strong> — The token or OAuth session expired. Re-authenticate.</li>
<li><strong>Path issue</strong> — The <code>command</code> in your MCP config isn&rsquo;t on the agent&rsquo;s PATH. Use absolute paths in MCP server configs.</li>
</ul>
<h3 id="3-session-persistence-is-fragile">3. Session Persistence Is Fragile</h3>
<p>Both Claude Code (<code>--resume</code>) and Codex CLI (auth cache) support session persistence, but neither is bulletproof. If you&rsquo;re running long-lived agent sessions, build checkpointing into your workflow — commit frequently, save intermediate results, and don&rsquo;t trust session files as your only state.</p>
<h2 id="which-debug-approach-fits-your-workflow">Which Debug Approach Fits Your Workflow?</h2>
<ul>
<li><strong>Claude Code</strong> — Best debug tooling overall. Use <code>/doctor</code> for triage, <code>/heapdump</code> for memory issues, and <code>/compact</code> for context management. If you&rsquo;re running Claude Code daily, learn these commands.</li>
<li><strong>Codex CLI</strong> — Best for config-driven debugging. The TOML config and log directory give you fine-grained control. Use when you need to debug MCP server connections or auth flows.</li>
<li><strong>GitHub MCP Server</strong> — Best for API-level debugging. The typed error objects and context middleware make it the most engineer-friendly MCP server to troubleshoot. Use when you&rsquo;re building custom MCP hosts.</li>
<li><strong>Playwright MCP</strong> — Best for browser-level debugging. Tracing and step debugging are unmatched for diagnosing flaky browser automation. Use when your tests pass locally but fail in CI.</li>
</ul>
<p>The reality is that most teams will use all four. Claude Code or Codex CLI as the primary coding agent, GitHub MCP for repository operations, and Playwright MCP for browser testing. Knowing the debug patterns for each one — before they break — is what separates a smooth workflow from a frustrating afternoon of staring at a silent terminal.</p>
<h2 id="faq">FAQ</h2>
<h3 id="how-do-i-enable-verbose-logging-in-claude-code">How do I enable verbose logging in Claude Code?</h3>
<p>Claude Code doesn&rsquo;t have a traditional verbose log flag. Instead, use <code>/doctor</code> for a health check, <code>/compact</code> to inspect context usage, and <code>claude --safe-mode</code> to isolate whether a plugin or MCP server is causing issues. For memory profiling, <code>/heapdump</code> writes a Chrome-compatible heap snapshot. If you need raw output, run <code>claude --verbose</code> from the shell — it prints more detail about tool calls and API requests.</p>
<h3 id="codex-cli-logs-are-empty--whats-wrong">Codex CLI logs are empty — what&rsquo;s wrong?</h3>
<p>If <code>$CODEX_HOME/log</code> is empty, you probably haven&rsquo;t set <code>CODEX_HOME</code> explicitly. Codex only writes the plaintext TUI log (<code>codex-tui.log</code>) when <code>CODEX_HOME</code> is set as an environment variable. Without it, logs go to a default location that may not be obvious. Set <code>CODEX_HOME=/home/you/.codex</code> in your shell profile, then restart Codex. Also check that your <code>config.toml</code> has a <code>[logging]</code> section with <code>level = &quot;debug&quot;</code>.</p>
<h3 id="why-does-my-github-mcp-server-return-404-for-repos-i-can-access">Why does my GitHub MCP server return 404 for repos I can access?</h3>
<p>This is almost always an authentication issue, not a missing repo. If you&rsquo;re using a Personal Access Token, verify it has the <code>repo</code> scope. If you&rsquo;re using OAuth, the token may have expired — re-authenticate. For GitHub Enterprise Server, the <code>--gh-host</code> flag must include the <code>https://</code> prefix (e.g., <code>--gh-host https://git.yourcompany.com</code>). Without it, the server defaults to github.com and can&rsquo;t find your enterprise repos.</p>
<h3 id="playwright-mcp-tests-pass-locally-but-fail-in-ci--how-do-i-debug-this">Playwright MCP tests pass locally but fail in CI — how do I debug this?</h3>
<p>Use browser tracing. Add <code>browser_start_tracing</code> before the failing interaction and <code>browser_stop_tracing</code> after it. Open the trace file in <a href="https://trace.playwright.dev">trace.playwright.dev</a> for a frame-by-frame replay. The most common CI-specific issues are: headed mode not available (pass <code>--headless</code>), sandbox restrictions (pass <code>--no-sandbox</code> in containers), and missing environment variables like <code>PLAYWRIGHT_MCP_CONSOLE_LEVEL</code>. Also check that <code>--output-mode file</code> is set so logs persist after the CI job ends.</p>
<h3 id="can-i-use-claude-code-and-codex-cli-together-in-the-same-project">Can I use Claude Code and Codex CLI together in the same project?</h3>
<p>Yes, but with caveats. Both tools respect <code>CLAUDE.md</code> / project config files, but they use different formats — Claude Code uses Markdown-based CLAUDE.md, while Codex uses TOML-based <code>config.toml</code> and <code>requirements.toml</code>. They won&rsquo;t interfere with each other, but you&rsquo;ll need to maintain two config files. I run Claude Code for architecture and planning work, then switch to Codex CLI for tasks where I want the ChatGPT subscription&rsquo;s model access. The MCP server configs are shared — both tools read the same MCP server binaries, so you only configure GitHub MCP or Playwright MCP once.</p>
]]></content:encoded></item></channel></rss>