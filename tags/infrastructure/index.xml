<?xml version="1.0" encoding="utf-8" standalone="yes"?><rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom" xmlns:content="http://purl.org/rss/1.0/modules/content/"><channel><title>Infrastructure on RockB</title><link>https://baeseokjae.github.io/tags/infrastructure/</link><description>Recent content in Infrastructure on RockB</description><image><title>RockB</title><url>https://baeseokjae.github.io/images/og-default.png</url><link>https://baeseokjae.github.io/images/og-default.png</link></image><generator>Hugo</generator><language>en-us</language><lastBuildDate>Thu, 09 Jul 2026 12:00:00 +0000</lastBuildDate><atom:link href="https://baeseokjae.github.io/tags/infrastructure/index.xml" rel="self" type="application/rss+xml"/><item><title>Northflank vs Blaxel vs Modal AI Sandbox: 2026 Agent Infrastructure Compared</title><link>https://baeseokjae.github.io/posts/northflank-vs-blaxel-vs-modal-ai-sandboxes-2026/</link><pubDate>Thu, 09 Jul 2026 12:00:00 +0000</pubDate><guid>https://baeseokjae.github.io/posts/northflank-vs-blaxel-vs-modal-ai-sandboxes-2026/</guid><description>Northflank, Blaxel, and Modal compared for AI agent sandboxes, BYOC, persistence, GPUs, networking, and pricing.</description><content:encoded><![CDATA[<p>If I had to choose quickly: Northflank is the enterprise and BYOC pick, Blaxel is the agent-native persistent sandbox pick, and Modal is the Python-first serverless compute and GPU pick. The right answer depends less on &ldquo;can it run code?&rdquo; and more on where state, network access, compliance, and cost boundaries live.</p>
<h2 id="what-does-ai-agent-sandbox-infrastructure-mean-in-2026">What does AI agent sandbox infrastructure mean in 2026?</h2>
<p>An AI agent sandbox used to mean a short-lived container where an LLM could run a Python snippet, maybe install a package, and return stdout. That is still useful, but it is no longer enough for serious agent products.</p>
<p>When building coding agents, data-analysis agents, or internal automation agents, I ran into the same pattern repeatedly: the sandbox is not a sidecar utility. It becomes part of the product&rsquo;s reliability model. It needs a filesystem, process lifecycle controls, environment secrets, network policy, logs, resource limits, maybe GPUs, and a story for what happens when a user comes back ten minutes later. That is the same lifecycle pressure behind background coding systems like the <a href="/posts/github-copilot-coding-agent-guide-2026/">GitHub Copilot coding agent</a>.</p>
<p>That is why the Northflank vs Blaxel vs Modal AI sandbox comparison is more interesting than a simple cold-start table. These platforms overlap, but they are not trying to be the same product.</p>
<p>Northflank is closer to a full application platform with microVM sandboxes, databases, CI/CD, observability, GPU options, and bring-your-own-cloud deployment. Blaxel is focused on AI agent infrastructure, especially persistent virtual-machine-like environments that can hibernate and resume quickly. Modal is a Python-first serverless compute platform with Sandboxes for isolated code execution and a very strong GPU execution story.</p>
<p>The useful way to compare them is by workload:</p>
<table>
  <thead>
      <tr>
          <th>Workload</th>
          <th style="text-align: right">Best fit</th>
          <th>Why</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>Regulated internal agent platform</td>
          <td style="text-align: right">Northflank</td>
          <td>BYOC, private cloud deployment, auditability, and broader platform primitives</td>
      </tr>
      <tr>
          <td>Persistent coding agent session</td>
          <td style="text-align: right">Blaxel</td>
          <td>Standby environments, filesystem/process state, and fast resume</td>
      </tr>
      <tr>
          <td>Python code interpreter at scale</td>
          <td style="text-align: right">Modal</td>
          <td>Simple Python SDK, second-based billing, and mature serverless ergonomics</td>
      </tr>
      <tr>
          <td>GPU-heavy batch or inference jobs</td>
          <td style="text-align: right">Modal</td>
          <td>Broad GPU SKUs and clear per-second GPU pricing</td>
      </tr>
      <tr>
          <td>Customer-VPC execution boundary</td>
          <td style="text-align: right">Northflank</td>
          <td>Can run sandboxes in the customer&rsquo;s cloud or infrastructure</td>
      </tr>
      <tr>
          <td>Agent product with built-in sandbox APIs</td>
          <td style="text-align: right">Blaxel</td>
          <td>Sandboxes are treated as a first-class agent runtime</td>
      </tr>
  </tbody>
</table>
<h2 id="what-is-the-quick-verdict-on-northflank-vs-blaxel-vs-modal">What is the quick verdict on Northflank vs Blaxel vs Modal?</h2>
<p>I&rsquo;ve found that the cleanest decision rule is this:</p>
<p><strong>Choose Northflank</strong> when the sandbox is part of a larger production platform. If you need sandboxes next to services, databases, jobs, GPUs, logs, RBAC, audit controls, and customer-cloud deployment, Northflank fits the enterprise infrastructure buyer better than a narrow code-execution product.</p>
<p><strong>Choose Blaxel</strong> when the agent session itself is the product. Blaxel&rsquo;s pitch is strong for long-running or frequently resumed agents because its sandboxes are instant-launching virtual machines with file system and process APIs, a built-in MCP server, standby behavior, and memory state preservation.</p>
<p><strong>Choose Modal</strong> when your team already thinks in Python functions, jobs, queues, and GPU tasks. Modal Sandboxes are useful for untrusted or LLM-generated code, repository test runs, and arbitrary dependency containers, but Modal&rsquo;s center of gravity is still serverless compute rather than full-stack enterprise deployment.</p>
<p>There are trade-offs. Northflank gives more infrastructure control, but that means you need to think like a platform team. Blaxel gives agent-native lifecycle features, but its managed model and preview-stage network controls may matter in compliance reviews. Modal makes compute and GPU work very ergonomic, but managed-only deployment and sandbox networking constraints can shape what you can build.</p>
<h2 id="how-do-northflank-blaxel-and-modal-compare-across-isolation-persistence-networking-gpus-byoc-and-pricing">How do Northflank, Blaxel, and Modal compare across isolation, persistence, networking, GPUs, BYOC, and pricing?</h2>
<p>Here is the comparison I would want before choosing a platform for a real AI agent product:</p>
<table>
  <thead>
      <tr>
          <th>Category</th>
          <th>Northflank</th>
          <th>Blaxel</th>
          <th>Modal</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>Main positioning</td>
          <td>Full-stack AI sandbox and app platform</td>
          <td>Agent-native sandbox and runtime platform</td>
          <td>Python-first serverless compute with Sandboxes</td>
      </tr>
      <tr>
          <td>Isolation model</td>
          <td>MicroVM-backed containers with VM-level isolation</td>
          <td>Instant-launching virtual machines for agent code execution</td>
          <td>Secure containers by default; VM Sandboxes in beta</td>
      </tr>
      <tr>
          <td>Persistence model</td>
          <td>Platform services, volumes, deployable workloads, customer-cloud environments</td>
          <td>Standby environments preserve memory, running processes, and filesystem</td>
          <td>Sandboxes, snapshots, volumes, and function/job model</td>
      </tr>
      <tr>
          <td>Resume behavior</td>
          <td>Docs emphasize under 1 second sandbox boot</td>
          <td>Under 25 ms resume from standby</td>
          <td>Workload-dependent serverless startup and snapshot patterns</td>
      </tr>
      <tr>
          <td>BYOC</td>
          <td>Yes, across major clouds, on-prem, bare metal options</td>
          <td>Managed platform model</td>
          <td>Managed platform model</td>
      </tr>
      <tr>
          <td>GPUs</td>
          <td>Published GPU pricing including L4, A100, H100, RTX PRO 6000</td>
          <td>Agent/runtime focus; pricing brief emphasizes CPU and storage</td>
          <td>Broad published GPU task rates including T4, L4, A10, L40S, A100, H100, H200, B200, B300</td>
      </tr>
      <tr>
          <td>Networking controls</td>
          <td>Enterprise and customer-cloud network boundary options</td>
          <td>Domain filtering in public preview via proxy behavior</td>
          <td>Outbound block, CIDR allowlist, beta domain allowlist</td>
      </tr>
      <tr>
          <td>Pricing unit</td>
          <td>vCPU-hour, GB-hour, GPU-hour</td>
          <td>GB RAM-second, snapshot GB-month, image GB-month</td>
          <td>Core-second, GiB-second, GPU-second</td>
      </tr>
      <tr>
          <td>Best buyer</td>
          <td>Platform, DevOps, security, enterprise engineering</td>
          <td>Agent product engineering</td>
          <td>ML, data, Python infrastructure teams</td>
      </tr>
  </tbody>
</table>
<p>In practice, the table tells you where to start the proof of concept. It does not replace testing your own workload. A 30-second code interpreter, a two-hour SWE agent session, and a GPU batch job stress completely different parts of the platform.</p>
<h2 id="where-does-northflank-fit-best">Where does Northflank fit best?</h2>
<p>Northflank is strongest when the AI sandbox is not an isolated feature but part of a production platform. Its docs describe sandboxes as microVM-backed containers with VM-level isolation and container performance, designed for untrusted code, LLM-generated code, AI agents, and CI/CD workloads. The docs also describe sub-second boot behavior for those sandboxes.</p>
<p>The bigger point is deployment model. Northflank can run microVM-isolated sandboxes in the customer&rsquo;s cloud. That matters more than it sounds. In many enterprise conversations, the question is not &ldquo;does this provider have SOC 2?&rdquo; The question is &ldquo;does customer code, data, or secrets have to leave our infrastructure boundary?&rdquo;</p>
<p>Northflank&rsquo;s BYOC and BYOK requirements give this a concrete shape. The research brief lists 1 node minimum, 12 vCPUs per cluster, and 24 GB memory per cluster for BYOC. BYOK lists 3 nodes minimum, 12 vCPUs, and 24 GB memory. Those are not hobby-project numbers, but they are practical for a platform team deploying internal agent infrastructure.</p>
<p>A Northflank-style architecture for an enterprise agent platform looks like this:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-text" data-lang="text"><span style="display:flex;"><span>User request
</span></span><span style="display:flex;"><span>  -&gt; Agent API service
</span></span><span style="display:flex;"><span>  -&gt; Policy and identity layer
</span></span><span style="display:flex;"><span>  -&gt; Northflank sandbox in customer cloud
</span></span><span style="display:flex;"><span>  -&gt; Private package registry, internal APIs, vector database
</span></span><span style="display:flex;"><span>  -&gt; Logs, audit events, artifacts, and storage
</span></span></code></pre></div><p>This is the model I would choose for a company building internal code agents that need access to private Git repositories, ticketing systems, staging APIs, and databases. Keeping execution near private infrastructure reduces awkward exceptions in security reviews.</p>
<p>The trade-off is operational surface area. If all you need is a hosted code interpreter for a SaaS app, Northflank can be more platform than you want to think about on day one. But if your roadmap already includes private networking, deploy previews, databases, jobs, GPU workloads, and compliance reviews, that platform depth stops being overhead and starts being the reason to choose it. For a broader view of how agent workflows connect to enterprise tools, see the <a href="/posts/openai-codex-plugins-guide-2026/">OpenAI Codex plugins guide</a>.</p>
<p>Northflank&rsquo;s published pricing also makes cost modeling familiar to infrastructure teams: CPU at $0.01667 per vCPU-hour and memory at $0.00833 per GB-hour. Published GPU examples include L4 24 GB at $0.80/hour, A100 40 GB at $1.42/hour, A100 80 GB at $1.76/hour, H100 80 GB at $2.74/hour, and RTX PRO 6000 96 GB at $3.00/hour.</p>
<h2 id="where-does-blaxel-fit-best">Where does Blaxel fit best?</h2>
<p>Blaxel is the most agent-native option in this comparison. Its docs describe sandboxes as instant-launching virtual machines for agent code execution with filesystem APIs, process APIs, and a built-in MCP server for agents. That language matters because it matches how modern agent products are actually built.</p>
<p>A coding agent does not just run <code>python main.py</code> and exit. It clones repositories, installs dependencies, starts dev servers, runs tests, edits files, and may need to come back to the same process tree later. If the agent spends most of its life waiting for the user, another tool, or a model response, idle economics and resume behavior become first-class product features.</p>
<p>Blaxel states that sandboxes resume from standby in under 25 milliseconds, scale to zero after a few seconds of inactivity, and maintain memory state including running processes and the filesystem. That is the core reason to evaluate it. For persistent agent sessions, &ldquo;fast resume with state&rdquo; can be more important than a raw cold-start benchmark.</p>
<p>The shape of a Blaxel workload is usually:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-text" data-lang="text"><span style="display:flex;"><span>Create sandbox for agent session
</span></span><span style="display:flex;"><span>  -&gt; Agent installs dependencies and opens files
</span></span><span style="display:flex;"><span>  -&gt; Sandbox goes to standby during idle time
</span></span><span style="display:flex;"><span>  -&gt; User or model resumes work
</span></span><span style="display:flex;"><span>  -&gt; Running processes and filesystem are still available
</span></span><span style="display:flex;"><span>  -&gt; Snapshot or persist artifacts as needed
</span></span></code></pre></div><p>Pricing reinforces that positioning. Blaxel prices active sandbox CPU at $0.0000115 per GB RAM-second, snapshot storage at $0.20 per GB-month, images at $0.045 per GB-month, and batch jobs at $0.000006 per GB RAM-second. It also describes persistent environments that idle to zero, resume in about 25 ms, and scale to 50,000+ concurrent machines.</p>
<p>The caution is network enforcement. Blaxel&rsquo;s domain filtering is in public preview, and the docs say it relies on tools and libraries respecting standard proxy environment variables, with routing-level enforcement planned later. That can be acceptable for many agent products, especially early ones. For strict enterprise containment, I would treat that as a security design question, not a checkbox.</p>
<p>Blaxel is the platform I would test first when the main product experience is &ldquo;give every user or agent a living workspace.&rdquo; It is less obviously the default when the requirement is &ldquo;run this inside my VPC with my audit chain and my existing Kubernetes-adjacent operations model.&rdquo;</p>
<h2 id="where-does-modal-fit-best">Where does Modal fit best?</h2>
<p>Modal is excellent when the team wants to express infrastructure as Python. Its Sandboxes are described as secure containers for executing untrusted user or agent code, including LLM-generated code, isolated untrusted code, repository test runs, and containers with arbitrary dependencies.</p>
<p>I have seen Modal-style platforms work best when the agent system is compute-heavy but not necessarily enterprise-network-heavy. For example: run user-submitted Python notebooks, evaluate generated code, process datasets, fan out batch jobs, or call GPUs for model work.</p>
<p>A Modal-oriented agent execution path might look like this:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-python" data-lang="python"><span style="display:flex;"><span><span style="color:#75715e"># Sketch only: the important idea is Python-owned infrastructure.</span>
</span></span><span style="display:flex;"><span><span style="color:#f92672">import</span> modal
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>app <span style="color:#f92672">=</span> modal<span style="color:#f92672">.</span>App(<span style="color:#e6db74">&#34;agent-code-runner&#34;</span>)
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>image <span style="color:#f92672">=</span> (
</span></span><span style="display:flex;"><span>    modal<span style="color:#f92672">.</span>Image<span style="color:#f92672">.</span>debian_slim()
</span></span><span style="display:flex;"><span>    <span style="color:#f92672">.</span>pip_install(<span style="color:#e6db74">&#34;pytest&#34;</span>, <span style="color:#e6db74">&#34;numpy&#34;</span>, <span style="color:#e6db74">&#34;pandas&#34;</span>)
</span></span><span style="display:flex;"><span>)
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#a6e22e">@app.function</span>(image<span style="color:#f92672">=</span>image, timeout<span style="color:#f92672">=</span><span style="color:#ae81ff">300</span>)
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">def</span> <span style="color:#a6e22e">run_agent_check</span>(repo_url: str, test_command: str) <span style="color:#f92672">-&gt;</span> str:
</span></span><span style="display:flex;"><span>    <span style="color:#75715e"># Clone, install, run, collect output, return a compact result.</span>
</span></span><span style="display:flex;"><span>    <span style="color:#f92672">...</span>
</span></span></code></pre></div><p>That model is productive because the boundary between app code and infra code is small. Python engineers can define images, functions, resource requirements, timeouts, and GPU usage without building a separate platform team workflow first.</p>
<p>Modal&rsquo;s pricing is also easy to reason about for bursty jobs. Standard compute is listed at $0.0000131 per physical core-second and $0.00000222 per GiB-second memory. Modal Sandbox + Notebooks pricing lists CPU at $0.00003942 per core-second and memory at $0.00000672 per GiB-second.</p>
<p>The GPU menu is a major strength. Modal lists GPU task rates including T4 at $0.000164/sec, L4 at $0.000222/sec, A10 at $0.000306/sec, L40S at $0.000542/sec, A100 40 GB at $0.000583/sec, A100 80 GB at $0.000694/sec, H100 at $0.001097/sec, H200 at $0.001261/sec, B200 at $0.001736/sec, and B300 at $0.001972/sec.</p>
<p>Networking is more constrained by default than some teams expect. Modal networking docs say default sandboxes cannot accept incoming network connections or access Modal resources, while outbound public IP access is allowed by default. Controls include full outbound block, CIDR allowlist, and a beta domain allowlist. Modal VM Sandboxes are also in beta and run on a full virtual machine rather than gVisor, which helps workloads that need a real Linux kernel or Docker-like behavior inside the sandbox.</p>
<p>That makes Modal a strong fit for GPU jobs, Python data workflows, and controlled untrusted-code execution. I would be more careful if the product requirement is long-lived browser/dev-server previews, customer-cloud deployment, or private enterprise network integration.</p>
<h2 id="how-should-you-think-about-isolation-and-security">How should you think about isolation and security?</h2>
<p>Do not reduce sandbox security to a single word like &ldquo;container&rdquo; or &ldquo;VM.&rdquo; The useful question is: what failure are you trying to contain?</p>
<p>For AI agents, common failure modes include secret exfiltration, malicious package installs, supply-chain attacks, container escapes, filesystem corruption, network pivoting, and accidental data leakage. Different runtime boundaries answer different parts of that threat model.</p>
<p>Northflank emphasizes microVM-backed containers, which is attractive when you want VM-level isolation with container-like performance. Blaxel describes virtual machines for agent code execution, which maps naturally to persistent workspaces. Modal&rsquo;s default sandboxing uses secure containers, and its VM Sandboxes beta gives each sandbox a real Linux kernel instead of gVisor for workloads that need that behavior.</p>
<p>Network control is just as important as runtime isolation. A sandbox that cannot break out of the kernel boundary can still leak secrets if it can call arbitrary public endpoints. In practice, I ask these questions before trusting an agent sandbox:</p>
<table>
  <thead>
      <tr>
          <th>Security question</th>
          <th>Why it matters</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>Can outbound network access be disabled completely?</td>
          <td>Stops broad exfiltration and surprise dependency calls</td>
      </tr>
      <tr>
          <td>Can access be limited by CIDR or domain?</td>
          <td>Supports package mirrors, private APIs, and allowlisted SaaS endpoints</td>
      </tr>
      <tr>
          <td>Where is domain filtering enforced?</td>
          <td>Proxy-level controls are different from routing-level enforcement</td>
      </tr>
      <tr>
          <td>Are secrets injected only when required?</td>
          <td>Reduces blast radius when generated code behaves badly</td>
      </tr>
      <tr>
          <td>Can the platform run inside my cloud boundary?</td>
          <td>Changes vendor-processing and data-residency review</td>
      </tr>
      <tr>
          <td>Are logs and audit events available?</td>
          <td>Makes incident response and compliance evidence possible</td>
      </tr>
  </tbody>
</table>
<p>Modal has explicit outbound block, CIDR allowlist, and beta domain allowlist controls. Blaxel has domain filtering in public preview but currently depends on proxy environment variable behavior. Northflank&rsquo;s enterprise angle is different: if the sandbox runs in your cloud or infrastructure boundary, private networking and compliance posture can be designed around your existing controls.</p>
<p>If your sandbox exposes tools through MCP, authentication belongs in the same threat model. I covered the remote-server side of that problem in the <a href="/posts/mcp-oauth-authentication-guide-2026/">MCP OAuth 2.1 authentication guide</a>, and the short version is that sandbox isolation does not replace authorization.</p>
<h2 id="how-should-you-compare-lifecycle-and-persistence">How should you compare lifecycle and persistence?</h2>
<p>Cold start is the easiest benchmark to quote and one of the easiest to overvalue. I care more about lifecycle fit.</p>
<p>For a code interpreter, a clean ephemeral sandbox is often a feature. You want each run isolated, short-lived, and cheap. Modal is strong here because the Python SDK and resource model are straightforward, and second-based billing maps well to bursty execution.</p>
<p>For a coding agent, persistence matters more. Installing a large dependency tree, running a dev server, indexing a repository, and preserving process state can dominate user experience. Blaxel&rsquo;s standby and memory preservation model directly targets this. A sub-25 ms resume claim is meaningful when the same agent workspace wakes many times during a session.</p>
<p>For enterprise internal agents, persistence is only one piece. You may need the sandbox to sit beside internal services, use private registries, write artifacts to approved storage, and preserve audit history. Northflank&rsquo;s broader platform model is useful here because the sandbox is not isolated from the rest of the deployment architecture.</p>
<p>I would test lifecycle with a script like this instead of a synthetic hello-world:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-bash" data-lang="bash"><span style="display:flex;"><span>git clone &lt;private-or-large-repo&gt;
</span></span><span style="display:flex;"><span>cd repo
</span></span><span style="display:flex;"><span>npm ci <span style="color:#f92672">||</span> pnpm install <span style="color:#f92672">||</span> pip install -r requirements.txt
</span></span><span style="display:flex;"><span>start dev server in background
</span></span><span style="display:flex;"><span>run unit tests
</span></span><span style="display:flex;"><span>pause or idle the sandbox
</span></span><span style="display:flex;"><span>resume after <span style="color:#ae81ff">10</span> minutes
</span></span><span style="display:flex;"><span>verify process state, filesystem state, logs, and network policy
</span></span></code></pre></div><p>That test reveals what matters: package install time, cache behavior, filesystem durability, process survival, resume semantics, log continuity, and billing during idle time.</p>
<h2 id="how-should-you-compare-pricing">How should you compare pricing?</h2>
<p>Pricing comparisons between Northflank, Blaxel, and Modal get misleading fast because the billing units are different.</p>
<p>Northflank publishes vCPU-hour, GB-hour, and GPU-hour style pricing. Blaxel prices active CPU by GB RAM-second and separately prices snapshots and images. Modal prices core-seconds, GiB-seconds, Sandbox + Notebooks usage, and GPU-seconds.</p>
<p>The mistake is to compare one CPU number and call it done. For AI agent infrastructure, I would model at least five cost buckets:</p>
<table>
  <thead>
      <tr>
          <th>Cost bucket</th>
          <th>Questions to ask</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>Active runtime</td>
          <td>How much CPU and memory does the sandbox consume while working?</td>
      </tr>
      <tr>
          <td>Idle or standby time</td>
          <td>Is idle billed, scaled to zero, or charged as reserved capacity?</td>
      </tr>
      <tr>
          <td>Persistence</td>
          <td>Are snapshots, volumes, images, or storage billed separately?</td>
      </tr>
      <tr>
          <td>GPU usage</td>
          <td>Are GPUs per-second, per-hour, shared, reserved, or bundled?</td>
      </tr>
      <tr>
          <td>Concurrency</td>
          <td>What happens at 200, 2,000, or 50,000 concurrent sandboxes?</td>
      </tr>
  </tbody>
</table>
<p>For example, a support-agent code interpreter that runs for 20 seconds and disappears should optimize active runtime and cold start. A coding-agent workspace that idles for 95 percent of its life should optimize standby behavior and snapshot cost. A model-evaluation pipeline should optimize GPU availability, GPU price, queueing, and batch orchestration.</p>
<p>This is why Blaxel&rsquo;s scale-to-zero and resume story is economically important, not just technically interesting. It is also why Modal&rsquo;s per-second GPU rates are useful for ML teams. Northflank&rsquo;s pricing will feel more natural to teams that already forecast infrastructure by vCPU-hour, memory, and GPU-hour across environments.</p>
<h2 id="what-matters-for-enterprise-readiness">What matters for enterprise readiness?</h2>
<p>Enterprise readiness is where Northflank separates itself most clearly. The research brief frames Northflank around SOC 2 Type 2, RBAC, audit logging, and self-serve BYOC across AWS, GCP, Azure, Oracle, CoreWeave, Civo, on-prem, and bare metal. Those details matter because enterprise sandbox reviews are usually about control boundaries, not product demos.</p>
<p>For a managed-only sandbox provider, security and legal teams may classify the vendor as an additional processor for source code, customer data, credentials, or generated artifacts. That may be fine, but it adds review work. BYOC can change the conversation because execution can stay in the customer&rsquo;s infrastructure boundary.</p>
<p>Blaxel and Modal still have strong reasons to exist in enterprise stacks. Blaxel is compelling when the product team needs agent workspaces that feel alive. Modal is compelling when the ML or data team needs Python-first compute and GPU capacity without running the underlying platform.</p>
<p>The question I would ask in procurement is simple: &ldquo;Who owns the blast radius?&rdquo; If the sandbox can reach private repositories, private APIs, internal databases, package registries, or customer files, the deployment model becomes a security feature.</p>
<h2 id="which-platform-should-you-choose">Which platform should you choose?</h2>
<p>Choose <strong>Northflank</strong> if your sandbox is part of a larger platform architecture. It is the best fit when you need BYOC, private networking, microVM-backed workloads, GPUs, databases, jobs, deploy previews, RBAC, auditability, and infrastructure control. I would start here for regulated internal tools, enterprise AI agent platforms, and teams that need to deploy close to customer data.</p>
<p>Choose <strong>Blaxel</strong> if your main product is a persistent agent workspace. Its standby model, process and filesystem preservation, MCP-oriented agent APIs, and fast resume are directly aligned with coding agents and long-running agent sessions. I would start here for agent products where user experience depends on waking the same environment again and again.</p>
<p>Choose <strong>Modal</strong> if your team is Python-heavy and compute-heavy. Modal is especially attractive for code execution, data workflows, batch jobs, and GPU workloads where per-second resource pricing and Python-defined infrastructure keep the system small. I would start here for code interpreters, notebook-like workloads, evaluation pipelines, and ML-heavy agent backends.</p>
<p>If you are still unsure, build the same unpleasant proof of concept on all three:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-text" data-lang="text"><span style="display:flex;"><span>1. Run generated code with untrusted dependencies.
</span></span><span style="display:flex;"><span>2. Clone a large repository and run its tests.
</span></span><span style="display:flex;"><span>3. Start a dev server and preview it.
</span></span><span style="display:flex;"><span>4. Idle the environment for 10 minutes, then resume.
</span></span><span style="display:flex;"><span>5. Restrict outbound access to only approved endpoints.
</span></span><span style="display:flex;"><span>6. Run one GPU-backed task.
</span></span><span style="display:flex;"><span>7. Export logs, artifacts, and audit evidence.
</span></span><span style="display:flex;"><span>8. Model cost at 200 concurrent sessions.
</span></span></code></pre></div><p>That test will usually make the decision obvious. You will learn whether your bottleneck is isolation, lifecycle, GPU access, network policy, enterprise deployment, or cost at concurrency.</p>
<h2 id="faq-northflank-vs-blaxel-vs-modal-ai-sandboxes">FAQ: Northflank vs Blaxel vs Modal AI sandboxes</h2>
<h3 id="is-northflank-better-than-blaxel-for-ai-agent-sandboxes">Is Northflank better than Blaxel for AI agent sandboxes?</h3>
<p>Northflank is better when you need enterprise deployment control, BYOC, private infrastructure boundaries, and a broader platform around the sandbox. Blaxel is better when the agent workspace lifecycle is the core product experience and fast standby/resume behavior matters more than customer-cloud deployment.</p>
<h3 id="is-blaxel-better-than-modal-for-persistent-ai-agents">Is Blaxel better than Modal for persistent AI agents?</h3>
<p>For persistent agent workspaces, Blaxel is usually the more direct fit because its sandboxes are designed around memory state, running processes, filesystem persistence, standby, and fast resume. Modal can run sandboxed code well, but its strongest advantage is Python-first serverless compute and GPU jobs.</p>
<h3 id="is-modal-a-good-choice-for-untrusted-ai-generated-code">Is Modal a good choice for untrusted AI-generated code?</h3>
<p>Yes, Modal Sandboxes are designed for secure execution of untrusted user or agent code, including LLM-generated code and repository test runs. The fit is strongest when the workload is short-lived, Python-oriented, batch-like, or GPU-heavy. Review networking limits and VM Sandbox beta status if your workload needs inbound servers or Docker-like behavior.</p>
<h3 id="which-platform-has-the-best-gpu-story">Which platform has the best GPU story?</h3>
<p>Modal has the clearest GPU-first story in this comparison because it publishes broad per-second GPU task rates across T4, L4, A10, L40S, A100, H100, H200, B200, and B300. Northflank also publishes GPU-hour pricing, including L4, A100, H100, and RTX PRO 6000 options. Blaxel&rsquo;s strongest documented angle is persistent agent sandboxes rather than GPU breadth.</p>
<h3 id="what-is-the-biggest-mistake-when-comparing-ai-sandbox-platforms">What is the biggest mistake when comparing AI sandbox platforms?</h3>
<p>The biggest mistake is comparing only cold starts or CPU price. For real agent systems, you need to compare isolation, outbound network control, filesystem and process persistence, snapshot costs, idle behavior, GPU access, auditability, BYOC, and concurrency. The best platform is the one whose lifecycle matches your workload.</p>
]]></content:encoded></item><item><title>Multi-Model LLM Routing Guide 2026: Cut AI Costs 85% with Smart Routing</title><link>https://baeseokjae.github.io/posts/multi-model-llm-routing-guide-2026/</link><pubDate>Thu, 30 Apr 2026 00:11:22 +0000</pubDate><guid>https://baeseokjae.github.io/posts/multi-model-llm-routing-guide-2026/</guid><description>Smart LLM routing cuts AI costs up to 85% by directing queries to cheaper models without sacrificing quality. Here&amp;#39;s the complete 2026 guide.</description><content:encoded><![CDATA[<p>Multi-model LLM routing is a strategy that directs each AI query to the most cost-efficient model capable of handling it — instead of routing everything to the most expensive one. In production systems, smart routing reduces LLM API costs by 57–85% while maintaining 95%+ of the quality you&rsquo;d get from premium models alone.</p>
<h2 id="why-llm-routing-is-now-essential-the-84b-problem">Why LLM Routing Is Now Essential (The $8.4B Problem)</h2>
<p>Enterprise LLM API spending exploded from $3.5B in late 2024 to $8.4B by mid-2025 — a 2.4x increase in roughly six months. The core driver: most teams discovered that &ldquo;use GPT-4 for everything&rdquo; is expensive and unnecessary. There&rsquo;s a 300x price gap between the cheapest and most expensive models today — simple queries cost around $0.10 per million tokens, while complex coding or reasoning tasks can cost $30 per million tokens. Sending a &ldquo;what are your store hours?&rdquo; customer support query to Claude 3.5 Sonnet when Claude 3.5 Haiku would answer it identically is money left on the table at scale. By 2026, 37% of enterprises run five or more LLMs in production, and the teams that thrive are the ones who&rsquo;ve built routing logic that treats the model pool as a tiered resource rather than a single endpoint. In February 2026, 5% of all LLM call spans reported errors — 60% caused by rate limits — and smart routing directly reduces those failures by distributing load across providers. The question in 2026 isn&rsquo;t whether to route; it&rsquo;s how to route well.</p>
<h2 id="how-smart-routing-achieves-85-cost-savings-the-mechanics">How Smart Routing Achieves 85% Cost Savings (The Mechanics)</h2>
<p>LLM routing saves money by exploiting one insight: most queries are easy, but teams pay as if all queries are hard. UC Berkeley&rsquo;s LMSYS lab built RouteLLM to prove this empirically. Their open-source framework, trained on Chatbot Arena preference data, achieved 85% cost reduction on the MT Bench benchmark and 45% on MMLU — while maintaining 95% of GPT-4&rsquo;s response quality. The mechanism is simple: a lightweight classifier evaluates each incoming query before it reaches an expensive model. Easy queries (factual lookups, simple formatting, short summaries) route to fast, cheap models like Claude 3.5 Haiku, GPT-4o mini, or Llama 3 70B. Only queries that genuinely require frontier-model reasoning hit GPT-4o or Claude 3.5 Sonnet. In real deployments, one customer support platform cut monthly LLM spend from $42,000 to $18,000 — a 57% reduction — simply by routing factual or templated queries to Haiku and escalating ambiguous or complaint-heavy tickets to Sonnet. Combined with response caching, the same approach can achieve 47–80% cost reduction depending on query repetition rates.</p>
<h2 id="5-core-llm-routing-strategies-explained">5 Core LLM Routing Strategies Explained</h2>
<p>LLM routing strategies fall along a spectrum from zero-code rule matching to trained ML classifiers, and the right choice depends on your query distribution, engineering capacity, and latency budget. The five strategies are: rule-based routing (explicit keyword or length conditions), complexity-based routing (scoring queries on estimated difficulty), semantic or intent-based routing (vector embeddings that match queries to categories), cascading routing (try cheap first, escalate on quality failure), and learned ML-based routing (a trained classifier like RouteLLM). In production systems, these strategies are layered — rule-based filters catch obvious easy queries immediately, while an ML classifier handles the ambiguous middle tier. A customer support platform with 400K monthly tickets typically finds that 60–70% of volume can be routed by simple rules alone, achieving most of the savings before any ML complexity is introduced. Teams new to routing should start with rule-based and complexity-based strategies before investing in ML classifiers, as the marginal savings beyond the simpler approaches often don&rsquo;t justify the labeling and training overhead until you&rsquo;re processing millions of queries per month.</p>
<h3 id="rule-based-routing-fastest-zero-overhead">Rule-Based Routing (Fastest, Zero Overhead)</h3>
<p>Rule-based routing is the simplest form: you define explicit conditions that map query attributes to model tiers. Examples include: if the query is under 50 tokens and contains no code blocks, route to the cheap model; if the query mentions &ldquo;debug,&rdquo; &ldquo;error,&rdquo; or &ldquo;stack trace,&rdquo; route to the premium model. Implementation overhead is near-zero — a few <code>if</code> statements or a YAML config. Latency impact is sub-millisecond. The tradeoff is brittleness: rules don&rsquo;t generalize, require manual maintenance, and fail on queries that don&rsquo;t match any condition cleanly. Rule-based routing is best as a quick win or as a safety layer on top of more sophisticated strategies. It works well when your query distribution is stable and your cheap-vs-expensive split is clearly defined by obvious syntactic signals like query length, presence of code, or specific keywords.</p>
<h3 id="complexity-based-routing-highest-roi">Complexity-Based Routing (Highest ROI)</h3>
<p>Complexity-based routing scores each query on estimated difficulty before dispatching it. Signals include token count, vocabulary complexity, presence of multi-step reasoning indicators (&ldquo;compare,&rdquo; &ldquo;explain the tradeoffs,&rdquo; &ldquo;given that X, what follows&rdquo;), and syntactic depth. The classifier assigns a complexity score; queries below a threshold hit cheaper models, above it hit premium models. This is the strategy with the highest ROI because it generalizes well across query types without requiring explicit rules for every domain. RouteLLM uses a variant of this approach trained on human preference data — its classifier learned what &ldquo;hard&rdquo; means from millions of Chatbot Arena comparisons, not hand-crafted heuristics. Teams implementing complexity scoring without a pretrained model can use feature-engineered signals (token count, reading level score, number of clauses) with a lightweight logistic regression or gradient boosted tree. The savings at 70–85% are the highest achievable without sacrificing meaningful quality.</p>
<h3 id="semantic--intent-based-routing">Semantic / Intent-Based Routing</h3>
<p>Semantic routing uses vector embeddings to match incoming queries against intent clusters in real time. You precompute embeddings for representative examples of each query category — &ldquo;customer support,&rdquo; &ldquo;code generation,&rdquo; &ldquo;document summarization,&rdquo; &ldquo;creative writing&rdquo; — and at inference time compute the similarity between the new query embedding and each cluster centroid. The nearest cluster determines the model. This approach handles nuanced intent distinctions that rule-based systems miss entirely. A query like &ldquo;help me clean up this Python function&rdquo; and &ldquo;write a Python function from scratch&rdquo; look similar by keyword but have different complexity profiles — semantic routing distinguishes them. The overhead is 5–15 milliseconds for embedding computation, which is negligible relative to LLM inference latency. It requires an embedding model (a lightweight one like <code>text-embedding-3-small</code> works well) and initial cluster setup, but no labeled training data. Intent-based routing is ideal for products with multiple distinct query domains mapping to different model needs.</p>
<h3 id="cascading-routing-quality-safety-net">Cascading Routing (Quality Safety Net)</h3>
<p>Cascading routing tries the cheapest model first and escalates to a more capable one only if the response fails a quality check. The check can be an LLM-as-judge prompt (&ldquo;does this answer the question completely?&rdquo;), a confidence score from the model itself, a regex for required format, or a simple length threshold. If the cheap model passes, the response goes to the user. If it fails, the request cascades to the next tier. This is the right pattern for latency-tolerant applications where correctness matters more than speed — internal tooling, batch processing, overnight report generation. For synchronous user-facing products, the double-inference latency may be unacceptable. The savings are lower than complexity-based routing because some queries always cascade, but the quality floor is higher since you&rsquo;ve explicitly verified the output. Cascading routing is often combined with rule-based routing as a two-layer system: obvious easy queries route directly, ambiguous ones cascade.</p>
<h3 id="learned--ml-based-routing-routellm-approach">Learned / ML-Based Routing (RouteLLM Approach)</h3>
<p>ML-based routing trains a model specifically to predict whether a given query needs a strong model or can be answered by a weak one. RouteLLM&rsquo;s four trained routers use Chatbot Arena preference data as supervision signal — if humans rated Model A&rsquo;s response better than Model B&rsquo;s on query X, the router learns that query X required Model A&rsquo;s capabilities. This data-driven approach generalizes across domains without explicit feature engineering. The routers RouteLLM released include a matrix factorization router, a BERT-based classifier, a Causal LLM router, and a similarity-based router. Performance varies by benchmark: on MT Bench, the BERT classifier achieves the 85% cost savings figure; on MMLU, the matrix factorization router performs best. Teams can fine-tune these on their own query logs to further adapt to their specific distribution. The implementation overhead is higher than rule-based approaches but the ROI is correspondingly higher — this is the right choice for large-scale deployments where 1% quality degradation translates to real user impact.</p>
<h2 id="best-llm-routing-tools-in-2026-comparison-table">Best LLM Routing Tools in 2026: Comparison Table</h2>
<p>The five leading LLM routing tools in 2026 each occupy a distinct position on the build-vs-buy and latency-vs-features tradeoff spectrum. RouteLLM from UC Berkeley is the open-source ML routing framework that proved 85% cost savings at academic rigor. LiteLLM is the self-hosted Python proxy that unifies 100+ providers behind a single OpenAI-compatible API, with routing logic defined in YAML. Bifrost is the Rust-based gateway that adds only 11 microseconds of overhead at 5,000 RPS — the right choice when routing latency itself is unacceptable. OpenRouter is the managed service giving access to 623+ models via one API key for teams who don&rsquo;t want any infrastructure. Portkey is the enterprise-grade managed gateway with built-in compliance, semantic caching, and spend monitoring. The comparison table below summarizes the key dimensions for choosing between them; detailed breakdowns follow for each tool.</p>
<table>
  <thead>
      <tr>
          <th>Tool</th>
          <th>Type</th>
          <th>Latency Overhead</th>
          <th>Models</th>
          <th>Best For</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>RouteLLM</td>
          <td>Open Source</td>
          <td>~10ms (classifier)</td>
          <td>Any 2</td>
          <td>Cost optimization research</td>
      </tr>
      <tr>
          <td>LiteLLM</td>
          <td>Self-Hosted</td>
          <td>~500ms at 500 RPS</td>
          <td>100+</td>
          <td>Multi-provider unified API</td>
      </tr>
      <tr>
          <td>Bifrost</td>
          <td>Self-Hosted (Rust)</td>
          <td>11 microseconds</td>
          <td>50+</td>
          <td>Ultra-high throughput</td>
      </tr>
      <tr>
          <td>OpenRouter</td>
          <td>Managed</td>
          <td>~20ms</td>
          <td>623+</td>
          <td>Fast multi-model access</td>
      </tr>
      <tr>
          <td>Portkey</td>
          <td>Managed</td>
          <td>~15ms</td>
          <td>250+</td>
          <td>Enterprise compliance</td>
      </tr>
  </tbody>
</table>
<h3 id="routellm-open-source-berkeley">RouteLLM (Open Source, Berkeley)</h3>
<p>RouteLLM is UC Berkeley LMSYS&rsquo;s open-source framework for cost-efficient LLM routing, released with four pretrained routers trained on Chatbot Arena preference data. It achieves 85% cost reduction on MT Bench while retaining 95% of GPT-4 performance. The setup requires a Python environment and access to your target models via API — the router itself is a lightweight classifier that adds around 10ms overhead. RouteLLM supports a configurable cost threshold: you set what fraction of queries should go to the strong model, and the router calibrates to meet it. It is the right tool for teams who want academic-grade, validated routing logic without paying for a managed service. GitHub: <code>lm-sys/RouteLLM</code>. The main limitation is that it&rsquo;s designed for binary routing (weak vs. strong) rather than multi-tier routing across five or more models, and the pretrained routers may need fine-tuning on domain-specific query distributions.</p>
<h3 id="litellm-self-hosted-100-providers">LiteLLM (Self-Hosted, 100+ Providers)</h3>
<p>LiteLLM is a Python-based proxy that unifies 100+ LLM providers behind a single OpenAI-compatible API. Routing logic lives in a YAML config: you define model lists, routing strategies (round-robin, least-busy, cost-based), and fallback chains. When the primary provider rate-limits or errors, LiteLLM automatically falls back to the next in the chain. Cost-based routing in LiteLLM ranks models by token price and routes to the cheapest model that hasn&rsquo;t hit its rate limit. The Python-based architecture creates a throughput ceiling around 500 RPS before you need horizontal scaling — at high traffic, consider Bifrost or a Rust-based alternative. LiteLLM is best for teams who want self-hosted routing without building it from scratch, value the broad provider coverage, and aren&rsquo;t yet at the scale where the Python bottleneck matters.</p>
<h3 id="bifrost-high-performance-rust-based">Bifrost (High Performance, Rust-Based)</h3>
<p>Bifrost is a Rust-based LLM gateway that adds only 11 microseconds of overhead at 5,000 requests per second — essentially zero latency impact on user-facing applications. It supports 50+ providers, provides OpenAI-compatible endpoints, and handles authentication, rate limiting, and routing in a single binary. The 11μs overhead figure (compared to LiteLLM&rsquo;s Python bottleneck at 500 RPS) makes Bifrost the right choice for latency-sensitive products where routing overhead itself would be perceptible. Bifrost&rsquo;s routing capabilities are less ML-sophisticated than RouteLLM — it focuses on rule-based, cost-aware, and load-balancing strategies rather than learned classifiers. For teams at 1,000+ RPS who need routing primarily for cost arbitrage and provider redundancy rather than quality optimization, Bifrost is the best available option in 2026.</p>
<h3 id="openrouter-managed-623-models">OpenRouter (Managed, 623+ Models)</h3>
<p>OpenRouter is a managed routing service that provides access to 623+ models across all major providers through a single API key. You pay per-token at competitive rates, and OpenRouter handles provider failover, load balancing, and model selection automatically. The &ldquo;auto&rdquo; routing endpoint uses OpenRouter&rsquo;s internal routing logic to select the cheapest model capable of handling your request quality tier. For teams who don&rsquo;t want to self-host any routing infrastructure, OpenRouter is the fastest path to multi-model access — setup takes under 10 minutes if you already have OpenAI-compatible client code. The tradeoff is less control over routing decisions compared to self-hosted options, and your query logs pass through OpenRouter&rsquo;s infrastructure (important consideration for privacy-sensitive applications).</p>
<h3 id="portkey-enterprise-grade">Portkey (Enterprise Grade)</h3>
<p>Portkey is a managed LLM gateway designed for enterprise compliance requirements — SOC 2 Type II, HIPAA, GDPR, and audit logging out of the box. It supports 250+ providers, provides semantic caching that can reduce costs by 40-60% beyond routing alone, and includes a UI for monitoring spend by model, team, and query type. Portkey&rsquo;s routing adds approximately 15ms overhead and supports virtual API keys that abstract provider credentials from application code. For enterprises with compliance requirements or teams that need centralized cost visibility across multiple product teams, Portkey solves problems that self-hosted tools require custom engineering to address. The managed pricing model (per-request fees on top of provider costs) makes it less economical at very high volumes compared to self-hosted Bifrost or LiteLLM.</p>
<h2 id="real-world-case-study-42k-to-18k-monthly">Real-World Case Study: $42K to $18K Monthly</h2>
<p>A SaaS customer support platform handling 400,000+ tickets per month was routing all queries to Claude 3.5 Sonnet, spending $42,000 per month in LLM API costs. An analysis of query distribution showed that 68% of tickets were simple category — password resets, order status requests, store hours, return policy explanations — queries where Haiku and Sonnet produced indistinguishable responses in blind evaluation. The remaining 32% involved complaint escalations, account investigations, billing disputes, and multi-step troubleshooting where Sonnet&rsquo;s reasoning demonstrably outperformed Haiku. The team built a complexity classifier in one sprint: a rule-based layer that caught obvious simple queries (under 40 tokens, no account-specific data required, matched one of 15 template patterns), followed by a lightweight BERT model trained on 2,000 labeled examples from their own ticket history for the ambiguous middle tier. Routing result: 68% of volume hit Haiku at $0.25/M input tokens, 32% hit Sonnet at $3.00/M input tokens. Monthly cost dropped from $42,000 to $18,000 — a 57% reduction — with no measurable change in customer satisfaction scores over the subsequent 90-day monitoring period. Total engineering investment: one sprint (2 engineers, 10 days), plus $800/month for a vector database to support the BERT classifier. Payback period: 8 days.</p>
<h2 id="implementation-guide-building-your-first-llm-router">Implementation Guide: Building Your First LLM Router</h2>
<p>Building a minimum viable LLM router takes less than a day of engineering time. The following approach uses Python and LiteLLM, targeting the most common use case: routing between a cheap model and an expensive model based on query complexity.</p>
<p><strong>Step 1: Install LiteLLM and define your model tiers</strong></p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-bash" data-lang="bash"><span style="display:flex;"><span>pip install litellm
</span></span></code></pre></div><div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-python" data-lang="python"><span style="display:flex;"><span><span style="color:#f92672">import</span> litellm
</span></span><span style="display:flex;"><span><span style="color:#f92672">from</span> litellm <span style="color:#f92672">import</span> Router
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>model_list <span style="color:#f92672">=</span> [
</span></span><span style="display:flex;"><span>    {<span style="color:#e6db74">&#34;model_name&#34;</span>: <span style="color:#e6db74">&#34;cheap-tier&#34;</span>, <span style="color:#e6db74">&#34;litellm_params&#34;</span>: {<span style="color:#e6db74">&#34;model&#34;</span>: <span style="color:#e6db74">&#34;claude-haiku-4-5&#34;</span>, <span style="color:#e6db74">&#34;api_key&#34;</span>: <span style="color:#e6db74">&#34;your-key&#34;</span>}},
</span></span><span style="display:flex;"><span>    {<span style="color:#e6db74">&#34;model_name&#34;</span>: <span style="color:#e6db74">&#34;premium-tier&#34;</span>, <span style="color:#e6db74">&#34;litellm_params&#34;</span>: {<span style="color:#e6db74">&#34;model&#34;</span>: <span style="color:#e6db74">&#34;claude-sonnet-4-6&#34;</span>, <span style="color:#e6db74">&#34;api_key&#34;</span>: <span style="color:#e6db74">&#34;your-key&#34;</span>}},
</span></span><span style="display:flex;"><span>]
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>router <span style="color:#f92672">=</span> Router(model_list<span style="color:#f92672">=</span>model_list)
</span></span></code></pre></div><p><strong>Step 2: Build a complexity scorer</strong></p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-python" data-lang="python"><span style="display:flex;"><span><span style="color:#66d9ef">def</span> <span style="color:#a6e22e">complexity_score</span>(prompt: str) <span style="color:#f92672">-&gt;</span> float:
</span></span><span style="display:flex;"><span>    <span style="color:#e6db74">&#34;&#34;&#34;Return 0.0 (trivial) to 1.0 (complex). Simple heuristic version.&#34;&#34;&#34;</span>
</span></span><span style="display:flex;"><span>    tokens <span style="color:#f92672">=</span> len(prompt<span style="color:#f92672">.</span>split())
</span></span><span style="display:flex;"><span>    has_code <span style="color:#f92672">=</span> any(kw <span style="color:#f92672">in</span> prompt <span style="color:#66d9ef">for</span> kw <span style="color:#f92672">in</span> [<span style="color:#e6db74">&#34;def &#34;</span>, <span style="color:#e6db74">&#34;class &#34;</span>, <span style="color:#e6db74">&#34;import &#34;</span>, <span style="color:#e6db74">&#34;```&#34;</span>, <span style="color:#e6db74">&#34;error&#34;</span>, <span style="color:#e6db74">&#34;traceback&#34;</span>])
</span></span><span style="display:flex;"><span>    has_reasoning <span style="color:#f92672">=</span> any(kw <span style="color:#f92672">in</span> prompt<span style="color:#f92672">.</span>lower() <span style="color:#66d9ef">for</span> kw <span style="color:#f92672">in</span> [<span style="color:#e6db74">&#34;compare&#34;</span>, <span style="color:#e6db74">&#34;explain why&#34;</span>, <span style="color:#e6db74">&#34;analyze&#34;</span>, <span style="color:#e6db74">&#34;tradeoffs&#34;</span>, <span style="color:#e6db74">&#34;design&#34;</span>])
</span></span><span style="display:flex;"><span>    
</span></span><span style="display:flex;"><span>    score <span style="color:#f92672">=</span> min(tokens <span style="color:#f92672">/</span> <span style="color:#ae81ff">200</span>, <span style="color:#ae81ff">0.5</span>)  <span style="color:#75715e"># token length contributes up to 0.5</span>
</span></span><span style="display:flex;"><span>    <span style="color:#66d9ef">if</span> has_code: score <span style="color:#f92672">+=</span> <span style="color:#ae81ff">0.3</span>
</span></span><span style="display:flex;"><span>    <span style="color:#66d9ef">if</span> has_reasoning: score <span style="color:#f92672">+=</span> <span style="color:#ae81ff">0.25</span>
</span></span><span style="display:flex;"><span>    <span style="color:#66d9ef">return</span> min(score, <span style="color:#ae81ff">1.0</span>)
</span></span></code></pre></div><p><strong>Step 3: Route based on score</strong></p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-python" data-lang="python"><span style="display:flex;"><span><span style="color:#66d9ef">def</span> <span style="color:#a6e22e">routed_completion</span>(prompt: str, threshold: float <span style="color:#f92672">=</span> <span style="color:#ae81ff">0.4</span>) <span style="color:#f92672">-&gt;</span> str:
</span></span><span style="display:flex;"><span>    score <span style="color:#f92672">=</span> complexity_score(prompt)
</span></span><span style="display:flex;"><span>    model <span style="color:#f92672">=</span> <span style="color:#e6db74">&#34;premium-tier&#34;</span> <span style="color:#66d9ef">if</span> score <span style="color:#f92672">&gt;=</span> threshold <span style="color:#66d9ef">else</span> <span style="color:#e6db74">&#34;cheap-tier&#34;</span>
</span></span><span style="display:flex;"><span>    
</span></span><span style="display:flex;"><span>    response <span style="color:#f92672">=</span> router<span style="color:#f92672">.</span>completion(
</span></span><span style="display:flex;"><span>        model<span style="color:#f92672">=</span>model,
</span></span><span style="display:flex;"><span>        messages<span style="color:#f92672">=</span>[{<span style="color:#e6db74">&#34;role&#34;</span>: <span style="color:#e6db74">&#34;user&#34;</span>, <span style="color:#e6db74">&#34;content&#34;</span>: prompt}]
</span></span><span style="display:flex;"><span>    )
</span></span><span style="display:flex;"><span>    <span style="color:#66d9ef">return</span> response<span style="color:#f92672">.</span>choices[<span style="color:#ae81ff">0</span>]<span style="color:#f92672">.</span>message<span style="color:#f92672">.</span>content
</span></span></code></pre></div><p><strong>Step 4: Add logging to calibrate your threshold</strong></p>
<p>Log each query&rsquo;s complexity score, which model was used, and user feedback signals (thumbs up/down, follow-up questions, corrections). After 1,000 queries, plot the distribution. If cheap-tier queries are generating follow-up corrections at a high rate, lower the threshold. If premium-tier usage exceeds 30% of volume and no corrections occur, raise the threshold. This feedback loop turns your heuristic router into a continuously improving system without requiring labeled training data.</p>
<h2 id="common-routing-anti-patterns-to-avoid">Common Routing Anti-Patterns to Avoid</h2>
<p>Understanding what not to do is as important as knowing the right strategies. These are the mistakes teams make in their first routing implementation — each one has bitten at least one production system.</p>
<p><strong>Anti-pattern 1: Routing everything to the cheap model until complaints arrive.</strong> This destroys user trust and is harder to recover from than spending the money. Routing should optimize cost within a quality constraint, not minimize cost unconditionally. Start with a conservative threshold (route only clearly trivial queries to cheap models) and loosen it as you gather data.</p>
<p><strong>Anti-pattern 2: Ignoring latency in routing decisions.</strong> A cascading router that tries cheap first and escalates adds double the latency for failed queries. In synchronous user-facing products with a 2-second latency expectation, a failed cheap query + escalation can easily hit 5–8 seconds. Measure p95 latency under cascade conditions, not just happy-path latency.</p>
<p><strong>Anti-pattern 3: Single-provider dependency.</strong> Routing between GPT-4o mini and GPT-4o both run on OpenAI&rsquo;s infrastructure. A single API outage eliminates your entire routing strategy. Multi-model routing should also mean multi-provider routing: include at least one Anthropic and one Google model in your fallback chain to survive provider-level incidents.</p>
<p><strong>Anti-pattern 4: Treating routing as a one-time setup.</strong> Model capabilities and pricing change frequently — GPT-4o mini in Q1 2026 is significantly more capable than it was at launch. A model that was &ldquo;cheap tier&rdquo; six months ago may now be capable enough to handle your &ldquo;premium tier&rdquo; queries at a fraction of the cost. Re-evaluate your routing tiers quarterly.</p>
<p><strong>Anti-pattern 5: Not measuring routing decision quality.</strong> Running routing without logging which model handled which query makes it impossible to audit quality problems. Always log: query hash, model used, complexity score, response latency, and any downstream quality signals. This data is the foundation for every optimization iteration.</p>
<h2 id="advanced-routing-consensus-models-and-2026-trends">Advanced Routing: Consensus Models and 2026 Trends</h2>
<p>The most interesting routing development in 2026 is consensus routing — sending the same prompt to multiple cheap models simultaneously and aggregating their responses to match the quality of a single expensive model. In early experiments, three simultaneous GPT-4o mini calls cost about the same as one GPT-4o call, but the majority-vote output achieves comparable quality on reasoning benchmarks. This pattern trades latency (the three calls run in parallel, so latency equals the slowest of the three) for cost efficiency without any routing classification overhead. RouterEval, a benchmark from ArXiv (2503.10657), tested 8,500+ LLM router configurations and found that capable routers can actually outperform the best single model in the pool — something consensus routing achieves by combining diverse model perspectives. Other 2026 trends include cloud-native routing from AWS (Bedrock&rsquo;s intelligent routing feature) and NVIDIA NIM microservices with built-in load-aware routing. These lower the operational barrier for teams already in those ecosystems. The long-term direction is routing that adapts in real time based on live model performance data — if Claude 3.5 Sonnet&rsquo;s error rate spikes, the router automatically shifts load to GPT-4o — rather than static threshold configurations updated manually.</p>
<h2 id="faq">FAQ</h2>
<p><strong>What is multi-model LLM routing?</strong>
Multi-model LLM routing is the practice of directing each AI query to the most appropriate and cost-efficient model from a pool of options, based on signals like query complexity, intent, token count, or real-time model availability. Instead of sending all traffic to a single expensive model, a router evaluates each request and dispatches it to the cheapest model likely to produce acceptable quality — reducing costs while maintaining user experience.</p>
<p><strong>How much can LLM routing realistically save?</strong>
Savings depend heavily on your query distribution. If 60–70% of your queries are simple and repetitive (common in customer support, document Q&amp;A, and data extraction), you can realistically achieve 50–80% cost reduction. UC Berkeley&rsquo;s RouteLLM demonstrated 85% savings on the MT Bench benchmark with only 5% quality degradation. A simpler rule-based approach typically achieves 40–60% savings with near-zero engineering overhead. Combined with semantic caching, total cost reduction of 70–85% is achievable in production.</p>
<p><strong>Which LLM routing tool should I start with?</strong>
For most teams: start with LiteLLM for multi-provider routing with minimal setup. If you need zero-latency overhead at high RPS, use Bifrost. If you want pre-trained ML-based routing without building a classifier, use RouteLLM. If you need enterprise compliance (SOC 2, HIPAA) and centralized spend monitoring, use Portkey. If you just want access to 600+ models via one API key with no infrastructure, use OpenRouter.</p>
<p><strong>Does LLM routing hurt response quality?</strong>
Well-implemented routing does not meaningfully hurt quality. The key is calibrating your routing threshold conservatively at first — route only queries you&rsquo;re confident are easy, and monitor quality signals before expanding scope. RouteLLM&rsquo;s Berkeley research shows 95% quality retention at 85% cost reduction. In practice, a well-calibrated system achieves 97–99% quality parity for routed queries because the classifier learns to be cautious at borderline cases.</p>
<p><strong>How do I handle routing failures or model outages?</strong>
Build provider redundancy into your routing layer from day one. Use a fallback chain: if the primary model returns an error or rate-limit response, automatically retry with an equivalent model from a different provider. LiteLLM and Portkey both support configurable fallback chains. For mission-critical applications, maintain at least one provider from three separate ecosystems (OpenAI, Anthropic, Google) in your model pool so any single provider outage doesn&rsquo;t take down your routing strategy.</p>
]]></content:encoded></item><item><title>AI Agent Deployment Infrastructure 2026: Ampere.sh, E2B, Northflank, and Modal Compared</title><link>https://baeseokjae.github.io/posts/ai-agent-deployment-infrastructure-guide-2026/</link><pubDate>Mon, 13 Apr 2026 12:00:00 +0000</pubDate><guid>https://baeseokjae.github.io/posts/ai-agent-deployment-infrastructure-guide-2026/</guid><description>A practical 2026 comparison of Ampere.sh, E2B, Northflank, and Modal for deploying AI agents.</description><content:encoded><![CDATA[<p>AI agent deployment infrastructure in 2026 is not one category. Ampere.sh, E2B, Northflank, and Modal solve different problems: managed agent hosting, secure code execution, full-stack production infrastructure, and Python/GPU serverless compute. The right shortlist depends on what your agent actually does after the model call returns.</p>
<p>I&rsquo;ve found that teams get into trouble when they compare these platforms as if they were four interchangeable &ldquo;agent sandbox&rdquo; vendors. They are not. A personal OpenClaw agent that needs to stay online in a managed product has little in common with a coding agent that needs Firecracker isolation for 20 concurrent Python sessions. A regulated enterprise deploying agents near private data has different constraints again. And if your agent is mostly a Python inference pipeline wrapped in tool calls, Modal may be closer to the center of gravity than a dedicated sandbox API.</p>
<p>This guide treats <strong>AI agent deployment infrastructure 2026</strong> as an architecture decision. I will compare Ampere.sh, E2B, Northflank, and Modal across runtime model, isolation, persistence, BYOC, GPU fit, developer experience, and pricing behavior. For adjacent production concerns, I would pair this with my notes on <a href="/posts/multi-model-fallback-architecture-guide/">multi-model fallback architecture</a> and <a href="/posts/ai-agent-identity-framework-guide/">AI agent identity frameworks</a>, because deployment is only one part of making agents survive production traffic.</p>
<h2 id="which-platform-should-you-shortlist-first">Which platform should you shortlist first?</h2>
<p>Use this quick version before reading the deeper comparison:</p>
<table>
  <thead>
      <tr>
          <th>Your primary problem</th>
          <th>Best first shortlist</th>
          <th>Why</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>You want hosted OpenClaw without running servers</td>
          <td>Ampere.sh</td>
          <td>It is a managed OpenClaw hosting product, not a generic sandbox API</td>
      </tr>
      <tr>
          <td>You need isolated code execution for agent-generated Python or JavaScript</td>
          <td>E2B</td>
          <td>It is purpose-built for agent code execution and uses Firecracker microVMs</td>
      </tr>
      <tr>
          <td>You need persistent services, jobs, databases, BYOC, and governance</td>
          <td>Northflank</td>
          <td>It is closer to a full application platform with sandbox support</td>
      </tr>
      <tr>
          <td>You need Python-native serverless compute, GPUs, inference jobs, and sandboxes</td>
          <td>Modal</td>
          <td>It combines serverless Python/GPU workflows with secure Sandboxes</td>
      </tr>
  </tbody>
</table>
<p>In practice, I would start with E2B for a coding-agent prototype, Northflank for an agent that is part of a larger production system, Modal for ML-heavy workloads, and Ampere.sh when the requirement is &ldquo;run my OpenClaw agent continuously without making me operate infrastructure.&rdquo;</p>
<h2 id="what-should-you-evaluate-before-choosing-agent-infrastructure">What should you evaluate before choosing agent infrastructure?</h2>
<p>The evaluation criteria that matter most for agents are not the same as a normal web app checklist. Agents create long-running, stateful, and sometimes untrusted execution paths. They also blur the line between application hosting, job orchestration, sandboxing, model routing, and data access.</p>
<p>I would evaluate these seven dimensions:</p>
<table>
  <thead>
      <tr>
          <th>Dimension</th>
          <th>Why it matters for agents</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>Runtime model</td>
          <td>Determines whether you are hosting an agent, executing generated code, running services, or invoking serverless functions</td>
      </tr>
      <tr>
          <td>Isolation</td>
          <td>Controls blast radius when an agent executes code or touches user-provided files</td>
      </tr>
      <tr>
          <td>Session duration</td>
          <td>Affects overnight tasks, async workers, human-in-the-loop delays, and background research agents</td>
      </tr>
      <tr>
          <td>Persistence</td>
          <td>Determines whether state lives in a filesystem, snapshot, database, volume, or managed product layer</td>
      </tr>
      <tr>
          <td>BYOC/private networking</td>
          <td>Matters when agents need private data, VPC access, compliance controls, or regional deployment</td>
      </tr>
      <tr>
          <td>GPU support</td>
          <td>Becomes central when the agent owns inference, embeddings, video, image, or batch ML work</td>
      </tr>
      <tr>
          <td>Pricing model</td>
          <td>Short bursty sessions, always-on agents, and GPU jobs produce very different bills</td>
      </tr>
  </tbody>
</table>
<p>When building agent systems, I ran into a simple pattern: the platform that feels cheapest in a local prototype is not always cheapest after you add concurrency, idle time, secrets, retries, logs, and data egress. The spreadsheet needs to model behavior, not just list monthly plan prices.</p>
<h2 id="is-amperesh-a-sandbox-provider-or-managed-agent-hosting">Is Ampere.sh a sandbox provider or managed agent hosting?</h2>
<p>Ampere.sh should be evaluated as <strong>managed OpenClaw hosting</strong>. Its public positioning is about one-click OpenClaw deployment, managed hosting, messaging channels, credits, and avoiding server setup. That is useful, but it is a different purchase than a low-level sandbox API.</p>
<p>If a user asks, &ldquo;How do I keep my OpenClaw agent online without maintaining a VPS?&rdquo;, Ampere.sh belongs in the conversation. Its marketing says deployment can happen in about 60 seconds, users can bring their own API keys with zero markup, use Ampere credits, and export data. Ampere comparison content has also listed a free OpenClaw path with 5,000 credits, 2 vCPU, and 2GB RAM, plus paid tiers such as Pro at $39/month, Ultra at $79/month, and Unlimited at $299/month. Pricing pages can change, so I would re-check before buying.</p>
<p>Where I would be careful is treating Ampere.sh as a general-purpose secure code execution platform. The research brief shows much thinner low-level infrastructure documentation than E2B, Northflank, or Modal. I would not pick it because I need arbitrary multi-tenant sandboxes, BYOC deployment, GPU scheduling, or a deep platform control plane.</p>
<p>Ampere.sh is strongest when the desired abstraction is the agent product itself:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-text" data-lang="text"><span style="display:flex;"><span>User wants OpenClaw online
</span></span><span style="display:flex;"><span>        -&gt; Ampere.sh manages hosting and product setup
</span></span><span style="display:flex;"><span>        -&gt; user configures model keys, credits, and channels
</span></span><span style="display:flex;"><span>        -&gt; operational surface stays intentionally small
</span></span></code></pre></div><p>That trade-off is honest. You get less infrastructure flexibility because the point is to avoid infrastructure.</p>
<h2 id="when-should-you-choose-e2b">When should you choose E2B?</h2>
<p>E2B is the cleanest fit when the core problem is <strong>secure code execution for AI agents</strong>. Its homepage and pricing material position each sandbox as Firecracker-powered, and the practical developer path is SDK-first: create a sandbox, run code, move files in and out, terminate the session.</p>
<p>For a coding agent, data-analysis agent, spreadsheet agent, or notebook-like assistant, this is often exactly what you want. The agent writes Python or JavaScript. The platform runs it in an isolated environment. Your app receives stdout, files, and results. You do not have to build a per-user container scheduler before you know whether the product works.</p>
<p>The limits matter. E2B pricing lists the Hobby tier as free plus usage, with a one-time $100 usage credit, 1-hour sandbox session length, and 20 concurrent sandboxes. The Pro tier is listed at $150/month plus usage, with 24-hour session length, 100 concurrent sandboxes, and purchasable extra concurrency up to 1,100. Those are clear, useful constraints. They also shape architecture.</p>
<p>For example, a 20-minute data-analysis run fits naturally:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-ts" data-lang="ts"><span style="display:flex;"><span><span style="color:#66d9ef">import</span> { <span style="color:#a6e22e">Sandbox</span> } <span style="color:#66d9ef">from</span> <span style="color:#e6db74">&#34;e2b&#34;</span>;
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">const</span> <span style="color:#a6e22e">sandbox</span> <span style="color:#f92672">=</span> <span style="color:#66d9ef">await</span> <span style="color:#a6e22e">Sandbox</span>.<span style="color:#a6e22e">create</span>();
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">await</span> <span style="color:#a6e22e">sandbox</span>.<span style="color:#a6e22e">files</span>.<span style="color:#a6e22e">write</span>(<span style="color:#e6db74">&#34;analysis.py&#34;</span>, <span style="color:#a6e22e">userGeneratedPython</span>);
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">const</span> <span style="color:#a6e22e">result</span> <span style="color:#f92672">=</span> <span style="color:#66d9ef">await</span> <span style="color:#a6e22e">sandbox</span>.<span style="color:#a6e22e">commands</span>.<span style="color:#a6e22e">run</span>(<span style="color:#e6db74">&#34;python analysis.py&#34;</span>);
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">await</span> <span style="color:#a6e22e">sandbox</span>.<span style="color:#a6e22e">kill</span>();
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#a6e22e">console</span>.<span style="color:#a6e22e">log</span>(<span style="color:#a6e22e">result</span>.<span style="color:#a6e22e">stdout</span>);
</span></span></code></pre></div><p>But an autonomous agent that waits six hours for a user approval, sleeps overnight, resumes work, and expects local filesystem state to still be there needs more planning. You can externalize state to your database and rehydrate a fresh sandbox. You can design every execution as a bounded job. Or you can choose infrastructure that is built around persistent services.</p>
<p>I like E2B when the sandbox is the product boundary. I would be more cautious when the agent becomes a long-lived distributed system.</p>
<h2 id="when-does-northflank-justify-the-extra-platform-surface">When does Northflank justify the extra platform surface?</h2>
<p>Northflank is the most complete option in this comparison when the agent is part of a broader production system. It brings services, jobs, databases, CI/CD, observability, BYOC clusters, and sandboxed workloads into one control plane.</p>
<p>The official Northflank docs describe sandboxes as microVM-backed containers with VM-level isolation and container-like performance, with boot times under 1 second. Its BYOC docs describe sandbox deployment into your cloud with microVM isolation enabled by default for workloads on microVM-enabled node pools. That matters for teams with private data, VPC requirements, procurement constraints, or data residency rules.</p>
<p>This is the platform I would consider when the architecture looks like this:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-text" data-lang="text"><span style="display:flex;"><span>Agent API service
</span></span><span style="display:flex;"><span>  -&gt; task queue
</span></span><span style="display:flex;"><span>  -&gt; sandbox workers
</span></span><span style="display:flex;"><span>  -&gt; Postgres / Redis / object storage
</span></span><span style="display:flex;"><span>  -&gt; private APIs in a customer VPC
</span></span><span style="display:flex;"><span>  -&gt; CI/CD, logs, metrics, secrets, and rollout controls
</span></span></code></pre></div><p>That is not a toy sandbox problem anymore. It is production application infrastructure with an agent-specific execution risk inside it.</p>
<p>Northflank&rsquo;s Developer Sandbox free plan allows 2 services, 2 jobs, 1 addon, and up to 1 BYOC cluster, but the docs warn that it should not be used for production applications. That warning is useful. A free plan can validate shape and workflow; it should not be mistaken for a production operating model.</p>
<p>The trade-off is setup cost. If all you need is &ldquo;run this snippet in a temporary sandbox,&rdquo; Northflank may feel heavier than E2B. If your agent needs persistent workloads, private networking, databases, and deployment governance, that extra surface stops being overhead and starts being the point.</p>
<h2 id="when-is-modal-better-than-a-dedicated-sandbox-provider">When is Modal better than a dedicated sandbox provider?</h2>
<p>Modal is strongest when agent infrastructure and ML compute infrastructure are the same problem. It is a serverless Python/GPU cloud first, with Sandboxes for secure execution of untrusted user or agent code. Its docs describe Sandboxes as secure containers, and Modal also documents gVisor-based sandboxing with optional VM runtime material in its broader sandbox documentation.</p>
<p>For Python-heavy teams, this can be a better fit than a dedicated sandbox-only provider. You can run inference, batch jobs, scheduled functions, web endpoints, notebooks, custom containers, and sandboxed code in the same ecosystem. Modal&rsquo;s Sandboxes support long-running processes, detach/terminate workflows, snapshots, and warm sandbox pools. Snapshots are especially useful when startup latency is dominated by package installation or environment setup.</p>
<p>The cost model deserves attention. Modal Sandbox resources are billed by the second based on whichever is higher: requested resources or actual usage. That is a good match for bursty compute and GPU-heavy jobs when you size resources carefully. It can be surprising if you over-request CPU, memory, or GPU for sandboxes that spend a lot of time idle.</p>
<p>A Modal-shaped agent workload often looks like this:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-python" data-lang="python"><span style="display:flex;"><span><span style="color:#f92672">import</span> modal
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>app <span style="color:#f92672">=</span> modal<span style="color:#f92672">.</span>App(<span style="color:#e6db74">&#34;agent-worker&#34;</span>)
</span></span><span style="display:flex;"><span>image <span style="color:#f92672">=</span> modal<span style="color:#f92672">.</span>Image<span style="color:#f92672">.</span>debian_slim()<span style="color:#f92672">.</span>pip_install(<span style="color:#e6db74">&#34;transformers&#34;</span>, <span style="color:#e6db74">&#34;torch&#34;</span>)
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#a6e22e">@app.function</span>(gpu<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;A10G&#34;</span>, image<span style="color:#f92672">=</span>image, timeout<span style="color:#f92672">=</span><span style="color:#ae81ff">900</span>)
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">def</span> <span style="color:#a6e22e">run_inference_job</span>(payload: dict) <span style="color:#f92672">-&gt;</span> dict:
</span></span><span style="display:flex;"><span>    <span style="color:#75715e"># Agent orchestration can call this for expensive model-side work.</span>
</span></span><span style="display:flex;"><span>    <span style="color:#66d9ef">return</span> {<span style="color:#e6db74">&#34;status&#34;</span>: <span style="color:#e6db74">&#34;processed&#34;</span>, <span style="color:#e6db74">&#34;items&#34;</span>: len(payload[<span style="color:#e6db74">&#34;items&#34;</span>])}
</span></span></code></pre></div><p>I would pick Modal when the agent is mostly a Python production workload that occasionally needs sandboxing, not when I want the simplest vendor-neutral sandbox API. Modal gives you a lot of power, but it also assumes your team is comfortable living in its Python-first deployment model.</p>
<h2 id="how-do-amperesh-e2b-northflank-and-modal-compare-side-by-side">How do Ampere.sh, E2B, Northflank, and Modal compare side by side?</h2>
<table>
  <thead>
      <tr>
          <th>Dimension</th>
          <th>Ampere.sh</th>
          <th>E2B</th>
          <th>Northflank</th>
          <th>Modal</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>Primary abstraction</td>
          <td>Hosted OpenClaw agent instance</td>
          <td>Sandbox for executing agent-generated code</td>
          <td>Projects, services, jobs, databases, BYOC clusters, sandboxes</td>
          <td>Python functions, apps, containers, GPUs, sandboxes</td>
      </tr>
      <tr>
          <td>Best fit</td>
          <td>Managed always-on OpenClaw</td>
          <td>Short-to-medium secure code execution</td>
          <td>Production platform for agent workloads</td>
          <td>Python/GPU-heavy agent systems</td>
      </tr>
      <tr>
          <td>Isolation model</td>
          <td>Product-level hosted isolation; comparison content mentions per-user containers</td>
          <td>Firecracker microVMs</td>
          <td>MicroVM-backed containers</td>
          <td>Secure containers, gVisor-based sandboxing, optional VM runtime docs</td>
      </tr>
      <tr>
          <td>Session model</td>
          <td>Always-on managed hosting</td>
          <td>Hobby up to 1 hour; Pro up to 24 hours</td>
          <td>Persistent services and sandbox workloads</td>
          <td>Long-running sandbox processes, detach/terminate, snapshots</td>
      </tr>
      <tr>
          <td>BYOC/private infra</td>
          <td>Self-host OpenClaw separately; Ampere.sh is managed hosting</td>
          <td>Enterprise/self-host/BYOC positioning, not core Hobby/Pro path</td>
          <td>First-class BYOC cluster deployment</td>
          <td>Managed Modal cloud, not positioned as BYOC</td>
      </tr>
      <tr>
          <td>GPU fit</td>
          <td>Not the core value proposition</td>
          <td>Not the strongest GPU deployment story</td>
          <td>Supports broader GPU workload positioning</td>
          <td>Core strength for serverless GPU and ML workloads</td>
      </tr>
      <tr>
          <td>Pricing signal</td>
          <td>Free and paid OpenClaw plans in marketing content</td>
          <td>Free Hobby plus usage; Pro $150/month plus usage</td>
          <td>Free Developer Sandbox, production paid/cloud costs</td>
          <td>Per-second billing by requested or actual resources</td>
      </tr>
  </tbody>
</table>
<p>The main takeaway is that the names occupy different layers. Ampere.sh is closest to productized agent hosting. E2B is closest to a sandbox API. Northflank is closest to a production platform. Modal is closest to serverless Python and GPU infrastructure with sandboxing included.</p>
<h2 id="how-should-session-duration-change-your-architecture">How should session duration change your architecture?</h2>
<p>Session duration is not a pricing footnote. It changes how you design state.</p>
<p>With a 1-hour sandbox, you should assume the sandbox is disposable. Persist task state outside the sandbox. Store artifacts in object storage. Keep conversation and tool state in your database. Make retries idempotent.</p>
<p>With a 24-hour sandbox, you can support longer coding runs, extended data processing, or human review windows, but I would still design the state model as recoverable. A long session is not a backup strategy.</p>
<p>With persistent services, you can keep workers, queues, volumes, and databases alive as first-class infrastructure. That makes sense for background agents that run continuously or coordinate many subtasks.</p>
<p>With snapshots and warm pools, you optimize startup latency rather than pretending a sandbox should live forever. Modal&rsquo;s snapshot model is a good example: if environment creation is expensive, snapshot the initialized state and branch from it.</p>
<p>For more on testing whether these systems behave reliably after failures, see my <a href="/posts/open-source-agent-eval-harness-comparison-2026/">open source agent eval harness comparison</a>. A deployment platform does not remove the need to test recovery paths.</p>
<h2 id="how-should-teams-model-cost">How should teams model cost?</h2>
<p>I would model four scenarios instead of asking for a single cheapest platform:</p>
<table>
  <thead>
      <tr>
          <th>Scenario</th>
          <th>Cost driver</th>
          <th>Likely fit</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>Solo OpenClaw agent</td>
          <td>Monthly hosting plan, credits, API keys</td>
          <td>Ampere.sh</td>
      </tr>
      <tr>
          <td>100 concurrent short coding sessions</td>
          <td>Sandbox concurrency, CPU/memory seconds, session length</td>
          <td>E2B</td>
      </tr>
      <tr>
          <td>Overnight autonomous workers</td>
          <td>Idle time, persistence, external state, background services</td>
          <td>Northflank or carefully designed E2B/Modal jobs</td>
      </tr>
      <tr>
          <td>GPU inference and batch ML</td>
          <td>GPU seconds, cold starts, snapshots, utilization</td>
          <td>Modal</td>
      </tr>
  </tbody>
</table>
<p>The hidden cost is usually not the published monthly plan. It is engineer time spent building missing operational pieces. E2B can save time when all you need is isolated code execution. Northflank can save time when you would otherwise stitch together services, jobs, databases, networking, and observability yourself. Modal can save time when your team would otherwise build a GPU job platform around Python. Ampere.sh can save time when the whole point is to avoid owning OpenClaw hosting.</p>
<h2 id="what-security-guarantees-matter-for-agent-code-execution">What security guarantees matter for agent code execution?</h2>
<p>For agents, I care about five controls:</p>
<ol>
<li>Strong isolation between tenants, users, and executions.</li>
<li>Resource limits for CPU, memory, disk, process count, runtime, and network.</li>
<li>Secrets handling that prevents prompt-injected code from reading unrelated credentials.</li>
<li>Egress controls and audit logs for calls to external systems.</li>
<li>A recovery model for failed, compromised, or runaway executions.</li>
</ol>
<p>Firecracker microVMs, microVM-backed containers, gVisor, and VM runtimes all exist because plain shared-process execution is not enough for untrusted code. The exact implementation matters less than whether the platform gives you the control surface your risk model needs.</p>
<p>For example, an internal analytics assistant executing trusted SQL templates has a different threat model from a public coding agent that runs arbitrary npm packages uploaded by users. The second system needs stricter sandboxing, tighter egress policy, and more aggressive artifact scanning.</p>
<h2 id="what-migration-paths-are-realistic">What migration paths are realistic?</h2>
<p>From local OpenClaw, Ampere.sh is the most direct managed-hosting move. You trade control for speed and less maintenance.</p>
<p>From a raw VPS running agent code in Docker, E2B is a good step if the pain is secure per-request execution. Northflank is a better step if the pain is the whole production platform: deployments, services, databases, secrets, jobs, and private networking.</p>
<p>From notebook-driven ML scripts, Modal is often the cleanest migration because the unit of work already looks like Python functions, images, jobs, and GPU-backed execution.</p>
<p>From an E2B prototype to production, I would first ask whether the sandbox remains the central abstraction. If yes, stay and harden around it. If no, and the agent now needs persistent services, private networks, and platform governance, evaluate Northflank or a similar application platform.</p>
<h2 id="which-platform-should-each-team-profile-choose">Which platform should each team profile choose?</h2>
<p>Solo builders should start with the smallest abstraction that proves the product. For hosted OpenClaw, that is Ampere.sh. For code execution, that is E2B. Avoid building a platform before you know whether users care.</p>
<p>Startups building coding agents should usually prototype on E2B, then revisit session duration, concurrency, and state persistence once real usage appears. Do not optimize for enterprise BYOC before the product has stable execution patterns.</p>
<p>Regulated teams should look hard at Northflank because BYOC, private infrastructure, and operational control become requirements early. The extra platform surface is easier to justify when procurement and security review are unavoidable.</p>
<p>ML-heavy AI teams should evaluate Modal first when GPUs, Python jobs, model serving, and batch processing dominate the workload. If the sandbox is only one tool inside a broader ML compute system, Modal&rsquo;s integrated model can be cleaner.</p>
<p>Teams that just want an always-on managed OpenClaw agent should not overcomplicate the decision. Ampere.sh exists for that use case.</p>
<h2 id="faq">FAQ</h2>
<h3 id="is-amperesh-a-sandbox-provider">Is Ampere.sh a sandbox provider?</h3>
<p>Not in the same sense as E2B or Modal Sandboxes. Ampere.sh is better understood as managed OpenClaw hosting. Use it when you want an OpenClaw agent running without server setup. Do not evaluate it as a general-purpose sandbox API unless its current docs explicitly cover your sandbox requirements.</p>
<h3 id="when-should-i-choose-e2b-over-northflank">When should I choose E2B over Northflank?</h3>
<p>Choose E2B when secure code execution is the central problem and your sessions fit its runtime and concurrency model. Choose Northflank when the agent is part of a larger production system that needs persistent services, jobs, databases, BYOC, observability, and deployment governance.</p>
<h3 id="when-is-modal-better-than-e2b">When is Modal better than E2B?</h3>
<p>Modal is better when your agent workload is tightly coupled to Python, GPU inference, batch jobs, or ML infrastructure. E2B is usually simpler when you just need agent-generated code execution through a focused sandbox API.</p>
<h3 id="do-long-running-agents-need-persistent-sandboxes">Do long-running agents need persistent sandboxes?</h3>
<p>Not always. Long-running agents need persistent state, but that state can live in a database, queue, object store, snapshot, volume, or managed platform. A persistent sandbox is useful, but relying on a single live sandbox as the only state store is fragile.</p>
<h3 id="what-is-the-safest-default-for-untrusted-agent-code">What is the safest default for untrusted agent code?</h3>
<p>Use a platform with strong isolation, explicit resource limits, careful secrets handling, and auditable network behavior. Firecracker microVMs, microVM-backed containers, gVisor-based sandboxes, and VM runtimes are all attempts to reduce blast radius. Match the platform to the risk of the code you execute.</p>
]]></content:encoded></item></channel></rss>