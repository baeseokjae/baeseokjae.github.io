<?xml version="1.0" encoding="utf-8" standalone="yes"?><rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom" xmlns:content="http://purl.org/rss/1.0/modules/content/"><channel><title>GraphRAG on RockB</title><link>https://baeseokjae.github.io/tags/graphrag/</link><description>Recent content in GraphRAG on RockB</description><image><title>RockB</title><url>https://baeseokjae.github.io/images/og-default.png</url><link>https://baeseokjae.github.io/images/og-default.png</link></image><generator>Hugo</generator><language>en-us</language><lastBuildDate>Mon, 20 Jul 2026 21:01:54 +0000</lastBuildDate><atom:link href="https://baeseokjae.github.io/tags/graphrag/index.xml" rel="self" type="application/rss+xml"/><item><title>Graphiti: LLM-Powered Temporal Knowledge Graphs for AI Agents</title><link>https://baeseokjae.github.io/posts/graphiti-temporal-knowledge-graphs-2026/</link><pubDate>Mon, 20 Jul 2026 21:01:54 +0000</pubDate><guid>https://baeseokjae.github.io/posts/graphiti-temporal-knowledge-graphs-2026/</guid><description>Graphiti is an open-source temporal knowledge graph framework by Zep that gives AI agents persistent, time-aware memory with 94.8% DMR accuracy and sub-100ms search.</description><content:encoded><![CDATA[<p>Graphiti is an open-source temporal knowledge graph framework by Zep that gives AI agents persistent, time-aware memory. Unlike static retrieval-augmented generation (RAG) systems that treat all information as equally current, Graphiti tracks when facts are valid, automatically invalidates outdated information, and enables agents to reason across time. With 29,000 GitHub stars, Apache-2.0 licensing, and benchmark scores of 94.8% on the DMR evaluation, it has become the leading open-source solution for agent memory infrastructure.</p>
<h2 id="what-is-graphiti-an-open-source-temporal-context-graph-framework">What Is Graphiti? An Open-Source Temporal Context Graph Framework</h2>
<p>Graphiti is a Python-based, open-source framework designed specifically for building temporal knowledge graphs that serve as long-term memory for AI agents. Developed and maintained by Zep, Graphiti addresses a fundamental gap in the AI agent ecosystem: the inability of most systems to remember what changed and when.</p>
<p>At its core, Graphiti represents information as a graph of entities and relationships, where every fact carries a temporal validity window. When an agent learns something new, Graphiti adds it to the graph. When a fact becomes outdated — for example, a person changes jobs or a company relocates — Graphiti automatically invalidates the old fact and records the new one, preserving the full history.</p>
<p>The framework supports four graph database backends: Neo4j, FalkorDB, Amazon Neptune, and Kuzu. It integrates with major LLM providers including OpenAI, Anthropic, Gemini, and Groq, and offers a REST API built on FastAPI for easy deployment. Graphiti also ships with an MCP (Model Context Protocol) server, enabling direct integration with Claude, Cursor, and other MCP-compatible AI tools.</p>
<h2 id="why-temporal-knowledge-graphs-matter-for-ai-agents">Why Temporal Knowledge Graphs Matter for AI Agents</h2>
<p>Traditional RAG systems retrieve information from a static vector store. They work well for answering questions about fixed knowledge — documentation, reference materials, or historical facts. But they fail when information changes over time. A RAG system cannot distinguish between &ldquo;the CEO of Acme Corp is Jane Doe&rdquo; (current) and &ldquo;the CEO of Acme Corp was John Smith&rdquo; (outdated). Both statements exist as vectors in the embedding space, and the system has no mechanism to prefer one over the other.</p>
<p>This is a critical limitation for AI agents that operate in dynamic environments. Enterprise AI assistants need to track changing project statuses, evolving customer relationships, shifting organizational structures, and time-sensitive compliance data. Without temporal awareness, agents either hallucinate outdated information or require expensive full-context-window approaches that do not scale.</p>
<p>Graphiti solves this with three core capabilities:</p>
<ul>
<li><strong>Temporal fact management</strong>: Every fact has a validity window with a start and end date. Queries automatically filter for facts that were true at the relevant time.</li>
<li><strong>Automatic fact invalidation</strong>: When new information contradicts existing facts, Graphiti marks the old facts as expired and adds the new ones, maintaining a complete audit trail.</li>
<li><strong>Cross-session reasoning</strong>: Agents can synthesize information from multiple conversations, understanding how facts evolved over time.</li>
</ul>
<p>The result is an agent memory system that behaves more like human memory — it knows not just what you told it, but when you told it, and whether that information is still relevant.</p>
<h2 id="key-features-deep-dive">Key Features Deep Dive</h2>
<h3 id="temporal-fact-management">Temporal Fact Management</h3>
<p>Every piece of information in Graphiti is stored as a triple — subject, relationship, object — with temporal metadata. A fact like &ldquo;Acme Corp acquired StartupX&rdquo; is stored with a <code>valid_at</code> timestamp. If Acme later divests StartupX, the original fact is marked as expired and the new fact is recorded. Queries can target specific time windows or ask &ldquo;what was true at the time of the last conversation.&rdquo;</p>
<p>This temporal layer is what separates Graphiti from every static knowledge graph solution. It enables agents to answer questions like &ldquo;What did the customer say about pricing in our last three meetings?&rdquo; or &ldquo;Has the project timeline changed since last week?&rdquo; — queries that are impossible for traditional RAG systems.</p>
<h3 id="episodes-and-provenance">Episodes and Provenance</h3>
<p>Graphiti organizes information into episodes — discrete units of interaction or observation. Each episode captures a complete snapshot of what the agent learned during a specific session or event. Every fact in the graph is linked back to the episode where it was first recorded, providing full provenance.</p>
<p>This provenance chain is invaluable for debugging and auditability. When an agent makes a decision based on a fact, developers can trace that fact back to its source episode, understand the context in which it was learned, and verify its accuracy. In regulated industries like finance and healthcare, this audit trail is not just useful — it is often a compliance requirement.</p>
<h3 id="prescribed-and-learned-ontology">Prescribed and Learned Ontology</h3>
<p>Graphiti supports two modes of ontology management. In <strong>prescribed ontology</strong> mode, developers define the entity types and relationship schemas upfront, ensuring strict consistency. In <strong>learned ontology</strong> mode, Graphiti uses LLMs to dynamically discover entity types and relationships from unstructured data, adapting the graph schema as new information arrives.</p>
<p>This dual-mode approach is powerful because it accommodates both structured enterprise environments where schemas must be controlled and exploratory AI applications where the data landscape is unknown at design time. Many production deployments use a hybrid approach: a prescribed core ontology for critical entities with learned extensions for ad-hoc information.</p>
<h3 id="incremental-graph-construction">Incremental Graph Construction</h3>
<p>Rather than rebuilding the entire graph on each update, Graphiti performs incremental graph construction. When new information arrives, the system identifies which parts of the graph are affected, updates only those nodes and edges, and propagates changes through connected relationships. This approach keeps update latency low and enables real-time ingestion of streaming data.</p>
<p>The incremental construction algorithm uses LLM calls to extract entities and relationships from new text, then merges them into the existing graph structure. The system handles entity resolution — recognizing that &ldquo;Acme Corp&rdquo; and &ldquo;Acme Corporation&rdquo; refer to the same entity — and relationship deduplication automatically.</p>
<h3 id="hybrid-retrieval-semantic--bm25--graph-traversal">Hybrid Retrieval: Semantic + BM25 + Graph Traversal</h3>
<p>Graphiti&rsquo;s retrieval pipeline combines three complementary approaches:</p>
<ol>
<li><strong>Semantic search</strong>: Embedding-based similarity search over entity descriptions and fact text, capturing semantic meaning.</li>
<li><strong>BM25 keyword search</strong>: Traditional term-frequency-based retrieval for exact matches and technical terminology.</li>
<li><strong>Graph traversal</strong>: Following relationships through the graph to discover connected facts that may not be semantically similar but are contextually relevant.</li>
</ol>
<p>This hybrid approach consistently outperforms any single retrieval method. The graph traversal component is particularly powerful for agent memory because it enables serendipitous discovery — finding relevant information through relationship chains rather than keyword or semantic similarity alone. Search results return in under 100 milliseconds, with the primary latency contribution coming from embedding API calls.</p>
<h2 id="graphiti-vs-graphrag-head-to-head-comparison">Graphiti vs GraphRAG: Head-to-Head Comparison</h2>
<p>The most common question developers ask is how Graphiti compares to Microsoft&rsquo;s GraphRAG. While both use graph structures to enhance LLM retrieval, they serve fundamentally different purposes.</p>
<table>
	<thead>
			<tr>
					<th>Feature</th>
					<th>Graphiti</th>
					<th>GraphRAG</th>
			</tr>
	</thead>
	<tbody>
			<tr>
					<td><strong>Primary purpose</strong></td>
					<td>Agent memory with temporal awareness</td>
					<td>Document-level Q&amp;A over static corpora</td>
			</tr>
			<tr>
					<td><strong>Temporal awareness</strong></td>
					<td>Full temporal fact management with validity windows and automatic invalidation</td>
					<td>None — treats all information as static</td>
			</tr>
			<tr>
					<td><strong>Graph construction</strong></td>
					<td>Incremental, real-time updates</td>
					<td>Batch processing over entire document sets</td>
			</tr>
			<tr>
					<td><strong>Retrieval method</strong></td>
					<td>Hybrid: semantic + BM25 + graph traversal</td>
					<td>Community-based summarization + semantic search</td>
			</tr>
			<tr>
					<td><strong>Update frequency</strong></td>
					<td>Continuous, per-interaction</td>
					<td>Requires full re-indexing for new documents</td>
			</tr>
			<tr>
					<td><strong>Ontology</strong></td>
					<td>Prescribed and learned (dual-mode)</td>
					<td>Automatically derived from data</td>
			</tr>
			<tr>
					<td><strong>Provenance</strong></td>
					<td>Episode-linked with full audit trail</td>
					<td>Document-level citations</td>
			</tr>
			<tr>
					<td><strong>Best for</strong></td>
					<td>AI agents, chatbots, personal assistants, dynamic enterprise data</td>
					<td>Document analysis, research, large-scale knowledge management</td>
			</tr>
			<tr>
					<td><strong>Deployment</strong></td>
					<td>Docker Compose, REST API, MCP server</td>
					<td>Python library, CLI tool</td>
			</tr>
			<tr>
					<td><strong>Database backends</strong></td>
					<td>Neo4j, FalkorDB, Amazon Neptune, Kuzu</td>
					<td>Microsoft Fabric, Azure AI Search</td>
			</tr>
			<tr>
					<td><strong>License</strong></td>
					<td>Apache-2.0 (open source)</td>
					<td>MIT (open source)</td>
			</tr>
			<tr>
					<td><strong>GitHub stars</strong></td>
					<td>~29,000</td>
					<td>~22,000</td>
			</tr>
	</tbody>
</table>
<p>The key takeaway: <strong>Graphiti and GraphRAG are complementary, not competitive.</strong> GraphRAG excels at extracting insights from large static document collections — think legal document review, academic literature analysis, or enterprise knowledge base mining. Graphiti excels at maintaining up-to-date, temporally aware memory for interactive AI agents — think customer support bots that remember past interactions, personal assistants that track changing preferences, or enterprise agents that monitor evolving project statuses.</p>
<p>If your use case involves information that changes over time and requires an agent to remember what happened when, Graphiti is the right choice. If you need to analyze a large, static corpus and generate structured insights, GraphRAG is the better fit.</p>
<h2 id="benchmarks-and-performance">Benchmarks and Performance</h2>
<p>Graphiti&rsquo;s performance has been rigorously evaluated in Zep&rsquo;s peer-reviewed paper, &ldquo;A Temporal Knowledge Graph Architecture for Agent Memory&rdquo; (arXiv:2501.13956). The results demonstrate state-of-the-art performance across multiple benchmarks.</p>
<h3 id="dmr-benchmark-948-accuracy">DMR Benchmark: 94.8% Accuracy</h3>
<p>The Dialog Memory Reasoning (DMR) benchmark evaluates how well agent memory systems can recall and reason about information from past conversations. Graphiti achieves 94.8% accuracy, outperforming MemGPT (Letta AI) at 93.4%. This 1.4 percentage point improvement represents a meaningful reduction in memory-related errors for production AI agents.</p>
<h3 id="longmemeval-up-to-185-improvement">LongMemEval: Up to 18.5% Improvement</h3>
<p>LongMemEval tests an agent&rsquo;s ability to perform complex temporal reasoning and synthesize information across multiple sessions. Graphiti demonstrates up to 18.5% accuracy improvement over baseline approaches on enterprise-specific tasks. This benchmark is particularly relevant for real-world deployments because it simulates the cross-session reasoning that enterprise AI assistants must perform daily.</p>
<h3 id="response-latency-90-reduction">Response Latency: 90% Reduction</h3>
<p>Compared to full-context-window baselines — where the entire conversation history is fed into the LLM prompt — Graphiti achieves a 90% reduction in response latency. Instead of processing thousands of tokens of history for every query, Graphiti retrieves only the relevant facts from the knowledge graph, dramatically reducing both cost and response time.</p>
<h3 id="search-performance-sub-100ms">Search Performance: Sub-100ms</h3>
<p>Graphiti&rsquo;s hybrid retrieval pipeline returns search results in under 100 milliseconds. The primary latency contributor is the embedding API call for semantic search; the graph traversal and BM25 components add negligible overhead. This makes Graphiti suitable for real-time agent interactions where users expect sub-second responses.</p>
<h2 id="getting-started-with-graphiti">Getting Started with Graphiti</h2>
<h3 id="installation">Installation</h3>
<p>Graphiti is available via pip and works with Python 3.10 or later:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-bash" data-lang="bash"><span style="display:flex;"><span>pip install graphiti-core
</span></span></code></pre></div><h3 id="supported-graph-backends">Supported Graph Backends</h3>
<p>Graphiti supports four graph database backends, giving you flexibility based on your infrastructure and scale requirements:</p>
<ul>
<li><strong>Neo4j</strong>: The most popular graph database, ideal for production deployments with rich querying needs</li>
<li><strong>FalkorDB</strong>: High-performance graph database optimized for low-latency operations</li>
<li><strong>Amazon Neptune</strong>: Fully managed graph database service on AWS, suitable for enterprise cloud deployments</li>
<li><strong>Kuzu</strong>: Embedded graph database for development and small-scale deployments</li>
</ul>
<h3 id="quickstart-example">Quickstart Example</h3>
<p>The fastest way to get started is with Docker Compose using Neo4j:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-bash" data-lang="bash"><span style="display:flex;"><span><span style="color:#75715e"># Clone the repository</span>
</span></span><span style="display:flex;"><span>git clone https://github.com/getzep/graphiti.git
</span></span><span style="display:flex;"><span>cd graphiti
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#75715e"># Start with Docker Compose</span>
</span></span><span style="display:flex;"><span>docker compose up -d
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#75715e"># Install Python dependencies</span>
</span></span><span style="display:flex;"><span>pip install graphiti-core
</span></span></code></pre></div><p>Once running, you can create a Graphiti client and start adding facts:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-python" data-lang="python"><span style="display:flex;"><span><span style="color:#f92672">from</span> graphiti <span style="color:#f92672">import</span> Graphiti
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>client <span style="color:#f92672">=</span> Graphiti(
</span></span><span style="display:flex;"><span>    uri<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;bolt://localhost:7687&#34;</span>,
</span></span><span style="display:flex;"><span>    user<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;neo4j&#34;</span>,
</span></span><span style="display:flex;"><span>    password<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;password&#34;</span>
</span></span><span style="display:flex;"><span>)
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#75715e"># Add a fact with temporal context</span>
</span></span><span style="display:flex;"><span>client<span style="color:#f92672">.</span>add_fact(
</span></span><span style="display:flex;"><span>    subject<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;Acme Corp&#34;</span>,
</span></span><span style="display:flex;"><span>    relationship<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;acquired&#34;</span>,
</span></span><span style="display:flex;"><span>    obj<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;StartupX&#34;</span>,
</span></span><span style="display:flex;"><span>    valid_at<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;2026-01-15&#34;</span>
</span></span><span style="display:flex;"><span>)
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#75715e"># Query current facts</span>
</span></span><span style="display:flex;"><span>results <span style="color:#f92672">=</span> client<span style="color:#f92672">.</span>query(<span style="color:#e6db74">&#34;What companies did Acme Corp acquire?&#34;</span>)
</span></span></code></pre></div><h2 id="mcp-server-integration">MCP Server Integration</h2>
<p>One of Graphiti&rsquo;s most powerful features is its MCP (Model Context Protocol) server. The MCP server acts as a bridge between AI agents and the temporal knowledge graph, enabling persistent memory across sessions.</p>
<h3 id="how-it-works">How It Works</h3>
<p>The MCP server exposes Graphiti&rsquo;s capabilities as a set of tools that MCP-compatible clients can call. When an AI agent like Claude or Cursor needs to remember information from a previous interaction, it queries the MCP server instead of relying on its limited context window.</p>
<p>The server provides tools for:</p>
<ul>
<li><strong>Adding facts</strong>: Storing new information with temporal context</li>
<li><strong>Querying facts</strong>: Retrieving relevant information based on current context</li>
<li><strong>Updating facts</strong>: Invalidating outdated information and recording changes</li>
<li><strong>Searching across episodes</strong>: Finding information from specific time periods or conversations</li>
</ul>
<h3 id="supported-clients">Supported Clients</h3>
<p>Graphiti&rsquo;s MCP server works with any MCP-compatible client, including:</p>
<ul>
<li><strong>Claude Desktop</strong>: Claude can maintain persistent memory across conversations, remembering user preferences, project details, and ongoing tasks</li>
<li><strong>Cursor</strong>: The AI-powered code editor can remember codebase context, coding conventions, and project history across sessions</li>
<li><strong>Custom MCP clients</strong>: Any application implementing the MCP protocol can leverage Graphiti for persistent memory</li>
</ul>
<p>This integration transforms AI agents from stateless query responders into systems that build and maintain context over time, much like a human collaborator who remembers the history of a project.</p>
<h2 id="production-deployment">Production Deployment</h2>
<h3 id="docker-compose-setup">Docker Compose Setup</h3>
<p>For production deployments, Graphiti provides a Docker Compose configuration that includes the Graphiti service, your chosen graph database, and supporting infrastructure:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-yaml" data-lang="yaml"><span style="display:flex;"><span><span style="color:#f92672">version</span>: <span style="color:#e6db74">&#39;3.8&#39;</span>
</span></span><span style="display:flex;"><span><span style="color:#f92672">services</span>:
</span></span><span style="display:flex;"><span>  <span style="color:#f92672">graphiti</span>:
</span></span><span style="display:flex;"><span>    <span style="color:#f92672">image</span>: <span style="color:#ae81ff">zep/graphiti:latest</span>
</span></span><span style="display:flex;"><span>    <span style="color:#f92672">ports</span>:
</span></span><span style="display:flex;"><span>      - <span style="color:#e6db74">&#34;8080:8080&#34;</span>
</span></span><span style="display:flex;"><span>    <span style="color:#f92672">environment</span>:
</span></span><span style="display:flex;"><span>      - <span style="color:#ae81ff">GRAPH_DATABASE_URL=bolt://neo4j:7687</span>
</span></span><span style="display:flex;"><span>      - <span style="color:#ae81ff">LLM_PROVIDER=openai</span>
</span></span><span style="display:flex;"><span>      - <span style="color:#ae81ff">OPENAI_API_KEY=${OPENAI_API_KEY}</span>
</span></span><span style="display:flex;"><span>    <span style="color:#f92672">depends_on</span>:
</span></span><span style="display:flex;"><span>      - <span style="color:#ae81ff">neo4j</span>
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>  <span style="color:#f92672">neo4j</span>:
</span></span><span style="display:flex;"><span>    <span style="color:#f92672">image</span>: <span style="color:#ae81ff">neo4j:5</span>
</span></span><span style="display:flex;"><span>    <span style="color:#f92672">environment</span>:
</span></span><span style="display:flex;"><span>      - <span style="color:#ae81ff">NEO4J_AUTH=neo4j/password</span>
</span></span><span style="display:flex;"><span>    <span style="color:#f92672">volumes</span>:
</span></span><span style="display:flex;"><span>      - <span style="color:#ae81ff">neo4j_data:/data</span>
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#f92672">volumes</span>:
</span></span><span style="display:flex;"><span>  <span style="color:#f92672">neo4j_data</span>:
</span></span></code></pre></div><h3 id="rest-api">REST API</h3>
<p>Graphiti&rsquo;s FastAPI-based REST API provides endpoints for all core operations:</p>
<ul>
<li><code>POST /facts</code> — Add a new fact with temporal metadata</li>
<li><code>GET /facts/{id}</code> — Retrieve a specific fact with its history</li>
<li><code>POST /search</code> — Hybrid search across the knowledge graph</li>
<li><code>POST /episodes</code> — Create a new episode</li>
<li><code>GET /episodes/{id}</code> — Retrieve episode details with provenance</li>
</ul>
<h3 id="scalability-considerations">Scalability Considerations</h3>
<p>Graphiti is designed for horizontal scalability. The graph database backends handle data distribution, while the Graphiti service layer is stateless and can be scaled independently. For high-throughput deployments, consider:</p>
<ul>
<li>Using Amazon Neptune or a clustered Neo4j deployment for the graph backend</li>
<li>Scaling the Graphiti REST API behind a load balancer</li>
<li>Caching frequent queries at the application layer</li>
<li>Monitoring embedding API latency, which is typically the bottleneck</li>
</ul>
<h2 id="community-and-ecosystem">Community and Ecosystem</h2>
<p>Graphiti has built a substantial open-source community since its launch in August 2024:</p>
<ul>
<li><strong>29,000+ GitHub stars</strong> and 2,900+ forks, reflecting strong developer adoption</li>
<li><strong>Apache-2.0 license</strong>, enabling unrestricted commercial use</li>
<li><strong>Active Discord community</strong> for support, feature discussions, and contributions</li>
<li><strong>Regular releases</strong> with new features, performance improvements, and bug fixes</li>
<li><strong>Comprehensive documentation</strong> at help.getzep.com/graphiti with installation guides, quickstart examples, and API references</li>
</ul>
<p>The project&rsquo;s growth trajectory — from a Show HN launch to 29K stars in under two years — signals that temporal knowledge graphs are not a niche technology but a core infrastructure need for the AI agent ecosystem.</p>
<h2 id="the-future-of-agent-memory">The Future of Agent Memory</h2>
<p>Graphiti represents a fundamental shift in how AI agents handle memory. The trajectory is clear: agents are moving from stateless, session-bound interactions to persistent, context-aware systems that build understanding over time.</p>
<p>Several trends point to Graphiti&rsquo;s growing importance:</p>
<ul>
<li><strong>Multi-agent systems</strong>: As organizations deploy fleets of AI agents, shared temporal knowledge graphs will enable agents to coordinate, share context, and avoid duplicating work</li>
<li><strong>Regulatory compliance</strong>: Industries subject to data retention and audit requirements will need temporal fact management to demonstrate what an agent knew and when</li>
<li><strong>Personal AI assistants</strong>: Consumer-facing AI assistants require persistent memory to be genuinely useful — remembering user preferences, past conversations, and evolving needs</li>
<li><strong>Enterprise knowledge management</strong>: Organizations are recognizing that static knowledge bases are insufficient for dynamic business environments</li>
</ul>
<p>Graphiti&rsquo;s open-source foundation, strong benchmark performance, and growing ecosystem position it as the leading infrastructure layer for this transition. As LLMs continue to improve, the bottleneck for agent capabilities will increasingly be memory and context management — precisely the problem Graphiti was built to solve.</p>
<h2 id="frequently-asked-questions">Frequently Asked Questions</h2>
<h3 id="what-makes-graphiti-different-from-a-regular-vector-database">What makes Graphiti different from a regular vector database?</h3>
<p>Vector databases store embeddings and retrieve semantically similar content, but they have no concept of time or fact validity. Graphiti stores structured facts with temporal metadata, enabling queries like &ldquo;what was true at this point in time&rdquo; and automatically invalidating outdated information. Vector databases are a component of Graphiti&rsquo;s hybrid retrieval pipeline, not a replacement for it.</p>
<h3 id="can-i-use-graphiti-without-neo4j">Can I use Graphiti without Neo4j?</h3>
<p>Yes. Graphiti supports four graph database backends: Neo4j, FalkorDB, Amazon Neptune, and Kuzu. Kuzu is an embedded graph database that requires no separate server, making it ideal for development and testing. FalkorDB offers lower latency for high-throughput scenarios, and Amazon Neptune provides a fully managed cloud experience.</p>
<h3 id="does-graphiti-work-with-any-llm-provider">Does Graphiti work with any LLM provider?</h3>
<p>Graphiti integrates with OpenAI, Anthropic (Claude), Google Gemini, and Groq for entity extraction, relationship discovery, and query understanding. The framework&rsquo;s LLM abstraction layer makes it straightforward to add custom providers. The graph operations themselves — storage, retrieval, traversal — are LLM-agnostic and run entirely on the graph database backend.</p>
<h3 id="how-does-graphiti-handle-fact-conflicts">How does Graphiti handle fact conflicts?</h3>
<p>When new information contradicts existing facts, Graphiti does not overwrite the old data. Instead, it marks the existing fact as expired by setting its validity end date, then adds the new fact with a fresh validity window starting from the current time. This preserves the complete history and enables temporal queries that can ask &ldquo;what did we believe at any point in time.&rdquo;</p>
<h3 id="is-graphiti-suitable-for-production-use">Is Graphiti suitable for production use?</h3>
<p>Yes. Graphiti powers Zep&rsquo;s production agent memory infrastructure and has been deployed in enterprise environments since 2024. It offers Docker Compose deployment, a FastAPI REST API, support for production-grade graph databases like Neo4j and Amazon Neptune, and horizontal scalability. The Apache-2.0 license allows unrestricted commercial use, and the active community provides ongoing support and development.</p>
]]></content:encoded></item></channel></rss>