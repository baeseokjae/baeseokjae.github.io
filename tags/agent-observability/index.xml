<?xml version="1.0" encoding="utf-8" standalone="yes"?><rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom" xmlns:content="http://purl.org/rss/1.0/modules/content/"><channel><title>Agent Observability on RockB</title><link>https://baeseokjae.github.io/tags/agent-observability/</link><description>Recent content in Agent Observability on RockB</description><image><title>RockB</title><url>https://baeseokjae.github.io/images/og-default.png</url><link>https://baeseokjae.github.io/images/og-default.png</link></image><generator>Hugo</generator><language>en-us</language><lastBuildDate>Fri, 04 Sep 2026 07:02:15 +0000</lastBuildDate><atom:link href="https://baeseokjae.github.io/tags/agent-observability/index.xml" rel="self" type="application/rss+xml"/><item><title>Rungraph 2026 Review: Ask Your Agent What Happened in a Run with Interactive Graphs</title><link>https://baeseokjae.github.io/posts/rungraph-agent-run-visualization-2026/</link><pubDate>Fri, 04 Sep 2026 07:02:15 +0000</pubDate><guid>https://baeseokjae.github.io/posts/rungraph-agent-run-visualization-2026/</guid><description>Rungraph converts existing Claude Code, Codex, Hermes, opencode, and Cursor transcripts into interactive graphs and answers MCP queries about what happened in a run — no hooks or telemetry.</description><content:encoded><![CDATA[<p>Rungraph is an open-source, zero-instrumentation tool that reads the agent session transcripts already on your disk and renders them as interactive graphs. With a single <code>npx rungraph</code> command, it visualizes the orchestrator, subagents, and tool calls of any Claude Code, Codex, Hermes Agent, opencode, or Cursor run — then lets you ask, in plain language through an MCP server, &ldquo;what happened?&rdquo; and lights up the exact nodes the answer refers to.</p>
<h2 id="what-is-rungraph-turning-agent-transcripts-into-interactive-graphs">What Is Rungraph? Turning Agent Transcripts into Interactive Graphs</h2>
<p>Rungraph is a JavaScript, open-source agent-run visualizer released on Hacker News in August 2026 by FayzanMalik. It sits on top of the session logs your coding agents already write to disk and converts them into a graph that you can explore interactively. The core idea is deliberately different from tracing and observability frameworks: there are no hooks, no wrappers, and no telemetry to install. Everything rungraph needs — the orchestrator turns, subagent spawns, tool calls, and human interventions — is already recorded in the transcripts your agents produce.</p>
<p>The tool&rsquo;s positioning is captured in its tagline: &ldquo;Ask your agent what happened in a run.&rdquo; Instead of reading thousands of lines of silent terminal output, you load a run into rungraph&rsquo;s dashboard, and the work becomes a visual, interrogable artifact.</p>
<p>Rungraph currently works across five agents: Claude Code, Codex, Hermes Agent, opencode, and Cursor. It handles all five from a single command and requires no per-agent setup. This vendor-neutral design is a direct answer to the fragmented terminal-agent landscape of 2026, where Goose, Claude Code, opencode, and Pi coexist and each writes a different, unversioned session format to disk.</p>
<h2 id="how-rungraph-works-retroactive-zero-instrumentation-fully-local">How Rungraph Works: Retroactive, Zero-Instrumentation, Fully Local</h2>
<p>The remarkable thing about rungraph is that it works <strong>retroactively</strong>. Because it reads transcripts already stored on disk, it can visualize runs that happened weeks before you ever installed the tool. There is no requirement to have &ldquo;started recording&rdquo; in advance.</p>
<ul>
<li><strong>No hooks:</strong> Rungraph does not modify your agents or their runtime.</li>
<li><strong>No wrappers:</strong> You do not wrap your commands or change how you work.</li>
<li><strong>No telemetry:</strong> The tool makes no network requests — it is fully local. Your run data never leaves your machine.</li>
</ul>
<p>To use it, you run a single command: <code>npx rungraph</code>. It scans the session stores of the supported agents, parses their transcripts, and opens an interactive dashboard. Under the hood, rungraph reads each agent&rsquo;s session format — Claude Code, Codex, Hermes, opencode, and Cursor — from disk. Some of those formats require Node version 22.13 or later: Hermes, opencode, and Cursor runs use rungraph&rsquo;s built-in SQLite reader, which needs that Node version.</p>
<p>One honest caveat is &ldquo;coverage.&rdquo; Coding-agent transcript formats are undocumented and unversioned, so rungraph cannot interpret every record it encounters. The tool reports how much of a run it could read (for example, &ldquo;read 95% of this run&rdquo;) and names the records it could not interpret. This transparency is what makes the tool trustworthy for a debugging workflow — you know when you are looking at a partial picture.</p>
<h2 id="the-graph-orchestrator-subagents-tools-and-human-interventions-as-nodes">The Graph: Orchestrator, Subagents, Tools, and Human Interventions as Nodes</h2>
<p>The centerpiece of rungraph is the graph itself. At its most basic, it is a node-and-edge view of an agent run:</p>
<ul>
<li><strong>Nodes</strong> represent the orchestrator, subagents, and individual tool calls.</li>
<li><strong>Edges</strong> represent spawn and return relationships — a subagent is spawned by the orchestrator, returns a result, and the path continues.</li>
<li><strong>Human interventions</strong> are marked on the path: permission denials, interrupts, and other human actions are visible rather than buried in logs.</li>
</ul>
<p>This structure makes the shape of a run legible at a glance. You can immediately see when the orchestrator delegatated a task to a subagent, how many nested spawns occurred, and where a human stepped in to deny a permission or interrupt the flow.</p>
<p>Because a single large agent run can exceed 20,000 tokens if fully serialized, rungraph is designed around a &ldquo;narrow then pull&rdquo; query model. Rather than dumping the entire graph into your context, you focus on the nodes you care about and pull details on demand. This keeps both the dashboard and any connected LLM context efficient.</p>
<h2 id="ask-your-agent-what-happened--the-mcp-layer-and-focus_nodes-deep-links">Ask Your Agent What Happened — the MCP Layer and focus_nodes Deep Links</h2>
<p>The most distinctive feature of rungraph is its MCP (Model Context Protocol) server. It gives you a set of tools — <code>list_runs</code>, <code>get_graph</code>, <code>find_nodes</code>, <code>get_detail</code>, and <code>focus_nodes</code> — that let you interrogate a run conversationally.</p>
<p>The workflow is: you open rungraph&rsquo;s dashboard alongside your AI assistant, then ask the assistant a question like &ldquo;what did the subagent that edited the config file actually do?&rdquo; The MCP server resolves the answer, and <code>focus_nodes</code> highlights the exact nodes on the graph that the answer references. Instead of reading a text response and mentally mapping it onto the run, you see the relevant portion of the graph light up in front of you.</p>
<ul>
<li><strong>list_runs:</strong> enumerate the runs rungraph has parsed.</li>
<li><strong>get_graph:</strong> retrieve the structure of a run.</li>
<li><strong>find_nodes:</strong> locate specific nodes by query.</li>
<li><strong>get_detail:</strong> pull full detail for a narrow set of nodes.</li>
<li><strong>focus_nodes:</strong> highlight nodes in the open dashboard — the visual answer.</li>
</ul>
<p>This &ldquo;ask your agent what happened&rdquo; model turns a silent, finished transcript into something you can have a conversation with. It is particularly valuable for audits, code reviews, and debugging, where the question is often the hardest part: &ldquo;which tool call touched this file?&rdquo;</p>
<h2 id="opinionated-signals-retry-storms-unresolved-errors-interventions-outliers">Opinionated Signals: Retry Storms, Unresolved Errors, Interventions, Outliers</h2>
<p>Most log viewers render every event with the same visual weight, forcing you to scan for what matters. Rungraph takes a different, opinionated approach via a <strong>signal strip</strong> that flags specific patterns on the run:</p>
<ul>
<li><strong>Retry storms:</strong> Repeated retries of the same failing call are flagged, revealing where the agent struggled.</li>
<li><strong>Unresolved errors:</strong> Errors that were never resolved are surfaced so you know the run ended in a problematic state.</li>
<li><strong>Interventions:</strong> Human actions (permission denials, course corrections) are marked.</li>
<li><strong>Outliers:</strong> Unusually long or unusual nodes stand out.</li>
<li><strong>Course changes:</strong> Points where the agent changed direction are visible.</li>
</ul>
<p>On a clean run, the signal strip sits at zero height — nothing flashes, nothing vies for your attention. It only activates when there is something worth seeing. This is what the brief calls &ldquo;opinionated observability&rdquo;: rendering not everything equal, but the things that matter louder. For a developer reviewing whether a run went smoothly, this is the difference between staring at a wall of text and immediately knowing where to look.</p>
<h2 id="replay-minimap-and-sharing-runs-as-vendor-neutral-bundles">Replay, Minimap, and Sharing Runs as Vendor-Neutral Bundles</h2>
<p>Two more interactive features round out the dashboard: live growth and replay.</p>
<p>Because rungraph reads transcripts, a run that is happening <strong>right now</strong> grows live as the agent works. You can watch the graph extend in real time rather than only after the run completes. A replay bar then lets you scrub back and forth through the run, inspecting the state of the graph at any point in time.</p>
<p>These graphs are built to be shared. Rungraph exports run bundles in a <code>.rungraph</code> format — a vendor-neutral package you can hand to a teammate, attach to a PR, or archive. An example from the project&rsquo;s README shows a bundle containing 2 runs, 143 nodes, 12 prompts, and 24 files touched, serialized to a 412KB file. Because a graph plus focused node deep links is a far more reviewable artifact than a pasted log, rungraph positions agent runs as <em>collaborative</em> artifacts in code review and team debugging.</p>
<h2 id="security-by-design-secret-redaction-in-export">Security by Design: Secret Redaction in Export</h2>
<p>Transcripts are a security hazard that is easy to overlook. When an agent runs a tool like a file reader, it records the file contents verbatim — so an agent that merely opens <code>.env</code> to read its keys has captured those keys in its session log. Any tool that re-shares or exports a run must handle this responsibly.</p>
<p>Rungraph addresses it in two ways on export:</p>
<ul>
<li><strong>Secret redaction:</strong> Shared <code>.rungraph</code> bundles redact likely secrets automatically.</li>
<li><strong>High-confidence blocking:</strong> Export blocks on high-confidence secrets, including AWS keys and GitHub, Slack, and API tokens.</li>
</ul>
<p>This is a meaningful security posture for a tool whose whole value proposition is sharing runs with other people. Because the data stays local until you explicitly export, and because export is aggressively redacted, rungraph avoids becoming a channel for leaking credential material that transcripts routinely capture.</p>
<h2 id="rungraph-vs-the-agent-observability-landscape">Rungraph vs. the Agent-Observability Landscape</h2>
<p>Rungraph does not exist in a vacuum — the agent-observability space is crowded and moving fast in 2026. Here is how it stacks up against the main alternatives:</p>
<table>
  <thead>
      <tr>
          <th>Tool / Approach</th>
          <th>Model</th>
          <th>Instrumentation</th>
          <th>Data locality</th>
          <th>Best at</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td><strong>Rungraph</strong></td>
          <td>Graph-native visualization + MCP query</td>
          <td>None (reads on-disk transcripts)</td>
          <td>Fully local</td>
          <td>Retroactive, shareable, graph exploration</td>
      </tr>
      <tr>
          <td><strong>Agentlore</strong></td>
          <td>Searchable team log over agent sessions</td>
          <td>Reads session data</td>
          <td>Local per team</td>
          <td>Team/communal search &amp; indexing</td>
      </tr>
      <tr>
          <td><strong>LangChain SmithDB</strong></td>
          <td>Purpose-built observability data layer</td>
          <td>Central tracing</td>
          <td>Cloud / hosted</td>
          <td>Enterprise LLM trace analytics</td>
      </tr>
      <tr>
          <td><strong>Grafana agent observability</strong></td>
          <td>Standard-dashboard ingestion</td>
          <td>Telemetry to Grafana</td>
          <td>Aggregated / hosted</td>
          <td>Folding agent runs into existing dashboards</td>
      </tr>
      <tr>
          <td><strong>Log-file grepping</strong></td>
          <td>Manual</td>
          <td>None</td>
          <td>Local</td>
          <td>Ad-hoc inspection of a single run</td>
      </tr>
  </tbody>
</table>
<p>The key contrast is <strong>instrumentation philosophy</strong>. SmithDB and Grafana&rsquo;s approach assume you build tracing into the pipeline and funnel event data into a central system. Agentlore positions run visibility as a team-collaboration problem focused on searchability. Rungraph, by contrast, is zero-instrumentation and local-first: it performs retroactive visualization of transcripts you already have, with graph structure plus MCP-driven interrogation as the interface. Where SmithDB answers enterprise &ldquo;trace everything&rdquo; needs, rungraph answers the individual developer&rsquo;s &ldquo;what actually happened here?&rdquo; need.</p>
<p>The Grafana adapter for Hermes Agent is a useful data point: it shows the observability ecosystem racing to ingest agent-run data into standard dashboards. Rungraph&rsquo;s differentiator is that it needs none of that infrastructure — there is nothing to ingest because the data is already on your disk.</p>
<h2 id="who-rungraph-is-for-limitations-and-2026-context">Who Rungraph Is For, Limitations, and 2026 Context</h2>
<p>Rungraph is best suited to developers who already work with terminal and CLI coding agents and want to understand, audit, or share what their agents did. It is especially relevant in 2026, when terminal agents have gone fully mainstream — comparisons of Goose, Claude Code, opencode, and Pi are common — and where the coexistence of multiple agents fragments run and session formats. A vendor-neutral graph that reads them all is a practical answer to that fragmentation.</p>
<p>The tool&rsquo;s main limitations are honest ones:</p>
<ul>
<li><strong>Coverage is not 100%.</strong> Because transcript formats are undocumented, rungraph may fail to interpret some records. It reports a coverage percentage and names what it could not read.</li>
<li><strong>Node requirements.</strong> Hermes, opencode, and Cursor use rungraph&rsquo;s SQLite reader, which requires Node 22.13+.</li>
<li><strong>Local-first by design.</strong> If you need centralized, enterprise-wide tracing across a fleet of agents, rungraph is not that; SmithDB or Grafana ingestion is.</li>
<li><strong>Young project.</strong> At research time it had 24 stars and 2 forks — useful and capable, but not battle-tested across a large community yet.</li>
</ul>
<p>For the Hermes Agent user specifically, rungraph has native support — a nice alignment for anyone running Hermes-managed pipelines who also wants to see how individual runs unfolded.</p>
<h2 id="verdict--is-rungraph-worth-adding-to-your-agent-workflow">Verdict — Is Rungraph Worth Adding to Your Agent Workflow?</h2>
<p>Rungraph solves a real, growing problem in a clever way. If you use Claude Code, Codex, Hermes Agent, opencode, or Cursor, and you have ever wished you could actually <em>see</em> what your agent did — or explain it to a teammate without pasting a wall of log text — rungraph is worth a try. The zero-instrumentation, retroactive design means there is no cost to adopting it: you can point it at runs you already have and get value immediately.</p>
<p>The MCP &ldquo;ask your agent what happened&rdquo; workflow, the opinionated signal strip, and the shareable <code>.rungraph</code> bundles make it genuinely useful for debugging and code review, not merely a pretty visualization. Be mindful of its coverage limitations and the Node 22.13 requirement, and treat exported bundles with the same caution you would any transcript (rungraph redacts, but transcripts are sensitive by default).</p>
<p><strong>Bottom line:</strong> For individual developers and small teams who want to understand and share agent runs without rearchitecting their observability stack, rungraph is a strong, low-friction addition to the toolkit in 2026.</p>
<h2 id="faq">FAQ</h2>
<h3 id="what-is-a-rungraph-agent-run">What is a rungraph agent run?</h3>
<p>A rungraph agent run is a visualization of one coding-agent session loaded from the transcript already stored on disk. Rungraph renders the orchestrator, subagents, and tool calls as nodes with spawn/return edges, marks human interventions, and lets you replay and query the run. It supports Claude Code, Codex, Hermes Agent, opencode, and Cursor with one <code>npx rungraph</code> command and no hooks or telemetry.</p>
<h3 id="how-do-i-ask-my-agent-what-happened-in-a-run">How do I ask my agent what happened in a run?</h3>
<p>Rungraph ships an MCP server with tools like <code>list_runs</code>, <code>get_graph</code>, <code>find_nodes</code>, <code>get_detail</code>, and <code>focus_nodes</code>. You connect it to your AI assistant, ask a question about the run in plain language, and <code>focus_nodes</code> highlights the exact nodes the answer references in the open dashboard — turning a silent transcript into a question-answerable, visual artifact.</p>
<h3 id="is-rungraph-really-zero-instrumentation">Is rungraph really zero-instrumentation?</h3>
<p>Yes. Rungraph reads the session transcripts your agents already write to disk; it does not install hooks, wrap your commands, or send telemetry. It works retroactively on runs that happened before you installed it, and it makes no network requests — the tool is fully local.</p>
<h3 id="what-agent-observability-tools-compare-with-rungraph">What agent observability tools compare with rungraph?</h3>
<p>The main alternatives are Agentlore (a searchable team log over agent sessions), LangChain SmithDB (a purpose-built enterprise tracing data layer), and Grafana agent observability for Hermes (ingesting run data into standard dashboards). Rungraph differs by being zero-instrumentation, local-first, and graph-native, with MCP-driven interrogation rather than centralized tracing.</p>
<h3 id="how-does-rungraph-handle-secrets-when-exporting-runs">How does rungraph handle secrets when exporting runs?</h3>
<p>Agent transcripts capture file contents verbatim, so merely opening <code>.env</code> can log its keys. On export, rungraph redacts likely secrets and blocks on high-confidence ones such as AWS keys and GitHub, Slack, or API tokens, so shared <code>.rungraph</code> bundles do not leak credential material.</p>
]]></content:encoded></item><item><title>TrueForge: The Open-Source Agent Harness for Reproducible Agent Runs</title><link>https://baeseokjae.github.io/posts/trueforge-open-source-agent-harness/</link><pubDate>Tue, 01 Sep 2026 22:00:57 +0000</pubDate><guid>https://baeseokjae.github.io/posts/trueforge-open-source-agent-harness/</guid><description>TrueForge is an open-source agent harness that runs the full agent execution loop — model calls, MCP tools, sandboxing, and approvals — for reproducible agent runs at lower cost.</description><content:encoded><![CDATA[<p>An open source agent harness is the runtime layer that turns a raw LLM into a working agent, handling the execution loop, tool calls, sandboxing, and context management so runs are reproducible. TrueForge is a TypeScript, MIT-licensed harness that delivers this in one process with SQLite locally or Postgres and Redis when hosted, claiming the same accuracy as Claude Managed Agents and deepagents at lower cost.</p>
<h2 id="what-is-an-agent-harness-and-why-does-it-matter">What Is an Agent Harness and Why Does It Matter?</h2>
<p>Building an agent is easy; running one well is hard. The gap between a prompt that calls a model and a system that reliably completes multi-step tasks is exactly what an agent harness fills. A harness is the software layer that owns the agent execution loop: it decides when to call the model, how to route tool invocations, how to manage the growing context window, and how to keep the whole run safe and auditable.</p>
<p>Without a harness, every team reinvents the same plumbing. You hand-roll retry logic, tool schemas, session persistence, and sandboxing, and each implementation drifts slightly from the last. That drift is the enemy of reproducibility — the same prompt can produce different behavior depending on which ad-hoc loop happened to run it. A harness standardizes that loop so that a given input, toolset, and configuration yields a consistent, traceable run.</p>
<p>The category has matured quickly. In 2026 the agent harness has become a distinct open-source product category, with projects like qm (682 Hacker News points), Munder Difflin (311 points), and Gambit (91 points) all launching recently. TrueForge sits squarely in this space, but with a specific thesis: reproducibility as the primary design goal rather than an afterthought.</p>
<h2 id="trueforge-at-a-glance--the-open-source-runtime-layer">TrueForge at a Glance — The Open-Source Runtime Layer</h2>
<p>TrueForge is the open-source agent harness and runtime layer from TrueFoundry that turns an LLM into a working agent. As of September 1, 2026, the repository holds roughly 5,038 GitHub stars and 348 forks, is written in TypeScript, and is released under the MIT license. The repo was created on July 23, 2026, and last updated on September 1, 2026 — roughly six weeks old and clearly active, with a Trendshift badge marking it as a trending repository.</p>
<p>The project is exposed three ways: a chat UI for interactive use, an HTTP API with a TypeScript SDK for programmatic control, and an embeddable UI SDK for dropping agent interfaces into your own applications. This three-pronged surface means the same harness powers a personal chat session, a backend service, and a product feature without reimplementation.</p>
<p>The runtime requires Node.js 22.14 or newer. As of August 27, 2026, the latest releases are 0.2.0-rc.0 for the core and 0.3.0-rc.0 for the UI — release candidates, which signals an actively iterating project rather than a finished, frozen one.</p>
<h2 id="core-capabilities-model-calls-mcp-tools-skills-and-sandboxing">Core Capabilities: Model Calls, MCP Tools, Skills, and Sandboxing</h2>
<p>The harness runs the full agent execution loop, and its capabilities map directly to the hard parts of agent engineering.</p>
<p><strong>Model calls.</strong> The harness owns the model invocation layer, including retries, structured outputs, and the routing of requests to the configured provider. This is the part most teams get wrong first, because naive loops fail silently on transient errors and malformed responses.</p>
<p><strong>MCP tools.</strong> TrueForge integrates with the Model Context Protocol, letting agents call external tools through a standardized interface. MCP is becoming the lingua franca of agent tooling, and a harness that treats it as a first-class citizen avoids the lock-in of bespoke tool adapters.</p>
<p><strong>Skills.</strong> Beyond raw tools, the harness supports skills — reusable, packaged capabilities that an agent can load on demand. This is a context-engineering feature: instead of stuffing every possible capability into the prompt, the agent pulls in only the skills it needs for the current task.</p>
<p><strong>Sandboxing.</strong> Tool execution is isolated so that a misbehaving or malicious tool call cannot damage the host. TrueForge treats the sandbox as a tool itself, which is a deliberate architectural choice we will examine shortly.</p>
<p><strong>Approvals and session state.</strong> The harness tracks session state across turns and can pause for human approval before risky actions. This combination of statefulness and human-in-the-loop gating is what makes long-running, multi-step agents safe enough to trust with real work.</p>
<h2 id="reproducibility-first--benchmarks-vs-claude-managed-agents-and-deepagents">Reproducibility First — Benchmarks vs Claude Managed Agents and deepagents</h2>
<p>The headline claim in the TrueForge README is that its benchmarks show the same accuracy as Claude Managed Agents and deepagents at lower cost. Reproducibility here means more than &ldquo;the same answer twice&rdquo; — it means a run that can be replayed, audited, and compared against other runs with confidence.</p>
<p>The cost angle is worth unpacking. Agent runs are expensive not because of the model calls alone but because of the waste: redundant tool calls, context bloat, and failed retries. A harness that manages context aggressively and defers tool loading until needed can cut token spend substantially while holding accuracy flat. TrueForge&rsquo;s positioning is that you do not have to trade quality for cost — the harness gives you both.</p>
<p>It is important to treat vendor benchmarks with appropriate skepticism and to validate them against your own workloads. But the direction of the claim matters: the project is competing on efficiency and reproducibility, not on raw model capability, which is the right axis for a harness to compete on.</p>
<h2 id="local-vs-hosted-mode-from-personal-use-to-teams">Local vs Hosted Mode: From Personal Use to Teams</h2>
<p>TrueForge supports two deployment modes that scale from a single developer to a full team.</p>
<p><strong>Local mode</strong> runs as one process backed by SQLite. This is the fastest path to a working agent: no infrastructure, no orchestration, just a single binary and a database file. It is ideal for personal use, prototyping, and local development where you want the full harness without the operational overhead.</p>
<p><strong>Hosted mode</strong> swaps SQLite for Postgres and Redis and adds Docker Compose, Helm, and Railway deployment options. This mode is built for teams: shared session state, concurrent agents, and the durability and concurrency that a production workload demands. The migration path from local to hosted is the same harness with a different backing store, so you are not rewriting your agent logic when you outgrow your laptop.</p>
<p>This local-to-hosted gradient is a practical strength. Many agent frameworks force you to choose an architecture up front; TrueForge lets you start small and scale the infrastructure without changing the agent code.</p>
<h2 id="the-harness-vs-sandbox-architecture-debate">The Harness-vs-Sandbox Architecture Debate</h2>
<p>A central architectural debate in harness design is whether the harness belongs inside or outside the sandbox. A widely discussed argument, surfaced on Hacker News with 182 points, contends that the agent harness belongs outside the sandbox. The reasoning is that the harness holds secrets, session state, and control logic — things you do not want exposed to the code the agent is executing.</p>
<p>TrueForge&rsquo;s design follows the &ldquo;sandbox as a tool&rdquo; model: the sandbox is one of the tools the agent can invoke, and secrets stay in the harness rather than leaking into the sandboxed environment. This separation means that even if a tool call goes rogue inside the sandbox, it cannot reach the harness&rsquo;s credentials or the broader host.</p>
<p>This is a meaningful design decision for anyone evaluating a harness. If the harness and sandbox are fused, a compromise of the sandboxed code can compromise the whole agent. If they are separated, you get a clean trust boundary. For teams running agents that touch real systems, that boundary is often the difference between a safe experiment and a security incident.</p>
<h2 id="human-checkpoints-and-context-engineering-for-reliable-runs">Human Checkpoints and Context Engineering for Reliable Runs</h2>
<p>Reliability is not just about the model — it is about the loop around it. TrueForge builds human checkpoints into the execution flow: tool approval gates, ask-user-questions interactions, and Generative UI that lets the agent present rich, interactive prompts rather than raw text. These checkpoints are a reproducibility feature as much as a safety one, because they let a human steer a run before it commits to an expensive or irreversible action.</p>
<p>Context engineering is the other half of reliable runs. TrueForge supports subagents, which let the main agent delegate focused work and keep the primary context lean. It supports deferred tool loading, so tool schemas are not all resident in the context window at once. It has a Code Mode for code-heavy tasks, and compaction to summarize and shrink the context when it grows too large.</p>
<p>These features attack the two biggest failure modes of long agent runs: context overflow and attention dilution. By keeping the context window focused and compacting it when necessary, the harness keeps the agent&rsquo;s reasoning sharp over many turns — which is precisely what reproducibility requires.</p>
<h2 id="how-trueforge-compares-to-other-open-source-agent-harnesses">How TrueForge Compares to Other Open-Source Agent Harnesses</h2>
<p>The open-source agent harness space is crowded, and TrueForge&rsquo;s differentiators are worth mapping against its peers.</p>
<table>
  <thead>
      <tr>
          <th>Harness</th>
          <th>Focus</th>
          <th>Key Differentiator</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>TrueForge</td>
          <td>Reproducible agent runs</td>
          <td>Same accuracy as Claude Managed Agents / deepagents at lower cost; local-to-hosted scaling</td>
      </tr>
      <tr>
          <td>qm</td>
          <td>Multiplayer agent harness</td>
          <td>Collaboration and multiplayer workflows for teams</td>
      </tr>
      <tr>
          <td>Gambit</td>
          <td>Reliable AI agents</td>
          <td>Emphasis on reliability in agent construction</td>
      </tr>
      <tr>
          <td>Munder Difflin</td>
          <td>Agent harness</td>
          <td>Recent HN launch (311 points) in the same category</td>
      </tr>
  </tbody>
</table>
<p>The table shows a category that is fragmenting by emphasis. qm leans into collaboration, Gambit into reliability, and TrueForge into reproducibility and cost efficiency. If your priority is a harness that scales from a single SQLite-backed process to a Postgres-backed team deployment while keeping runs auditable and cheap, TrueForge&rsquo;s positioning is the most direct match.</p>
<h2 id="getting-started-quickstart-sdk-and-embeddable-ui">Getting Started: Quickstart, SDK, and Embeddable UI</h2>
<p>Getting started with TrueForge follows the standard open-source pattern. You install the package, point it at a model provider, and run the harness in local mode to get a working agent in minutes. The HTTP API and TypeScript SDK give you programmatic control, and the embeddable UI SDK lets you surface the agent inside your own product.</p>
<p>The practical path is: start in local mode with SQLite to validate your agent logic, then move to hosted mode with Postgres and Redis when you need shared state and concurrency. Because the agent code does not change between modes, the migration is about infrastructure, not rework.</p>
<h2 id="verdict--is-trueforge-right-for-your-agent-workflows">Verdict — Is TrueForge Right for Your Agent Workflows?</h2>
<p>TrueForge is a strong choice if your agent work needs reproducibility, cost control, and a clean path from personal use to team deployment. Its &ldquo;sandbox as a tool&rdquo; architecture addresses a real security concern, its context-engineering features attack the dominant failure modes of long runs, and its benchmark claims position it as an efficiency leader in the category.</p>
<p>It is a young project — roughly six weeks old and shipping release candidates — so you should expect rapid change and validate the benchmark claims against your own workloads. But for teams that want an open-source agent harness that treats reproducibility as a first-class goal rather than an afterthought, TrueForge is worth a serious look.</p>
<h2 id="faq">FAQ</h2>
<p><strong>What is an open source agent harness?</strong>
An open source agent harness is a runtime layer that turns a raw LLM into a working agent by owning the execution loop — model calls, tool routing, sandboxing, context management, and session state — so that agent runs are reproducible and auditable.</p>
<p><strong>Is TrueForge free to use?</strong>
Yes. TrueForge is released under the MIT license, so it is free to use, modify, and embed in your own projects, including commercial ones.</p>
<p><strong>What are the system requirements for TrueForge?</strong>
TrueForge requires Node.js 22.14 or newer. In local mode it runs as a single process backed by SQLite; hosted mode uses Postgres and Redis with Docker Compose, Helm, or Railway deployment options.</p>
<p><strong>How does TrueForge compare to Claude Managed Agents and deepagents?</strong>
According to TrueForge&rsquo;s benchmarks, it achieves the same accuracy as Claude Managed Agents and deepagents at lower cost, primarily through aggressive context management and deferred tool loading that reduce token waste.</p>
<p><strong>Does TrueForge support human approval of agent actions?</strong>
Yes. TrueForge includes human checkpoints such as tool approval gates, ask-user-questions interactions, and Generative UI, letting a human steer a run before it commits to expensive or irreversible actions.</p>
]]></content:encoded></item><item><title>Agent Trajectory ETL and Compliance: Building a Local-First Observability Router</title><link>https://baeseokjae.github.io/posts/trajpack-agent-trajectory-etl/</link><pubDate>Sat, 22 Aug 2026 07:01:22 +0000</pubDate><guid>https://baeseokjae.github.io/posts/trajpack-agent-trajectory-etl/</guid><description>A local-first agent trajectory ETL and compliance router captures, transforms, and routes LLM agent traces to audit, observability, and warehouse stores.</description><content:encoded><![CDATA[<p>An agent trajectory ETL and compliance router is a pipeline that extracts raw LLM agent traces, transforms them into structured, portable telemetry, and routes each event to the right destination — an audit log, an observability backend, or a data warehouse — based on regulatory and operational rules. It keeps sensitive trajectory data local-first, captures only what is needed, and produces tamper-proof audit trails that satisfy frameworks such as the EU AI Act Article 12 and the NIST AI Risk Management Framework. This guide explains why agent trajectories outgrow general-purpose databases, how to model them, and how to build a compliant, vendor-neutral pipeline.</p>
<h2 id="why-agent-trajectories-need-a-dedicated-etl-pipeline">Why Agent Trajectories Need a Dedicated ETL Pipeline</h2>
<p>Modern AI agents do not behave like traditional software. A single task can fan out into hundreds of nested spans, mix text, images, and tool calls, and keep spans open for hours while an agent waits on a human or a slow external service. LangChain&rsquo;s SmithDB team observed that agent traces have &ldquo;outgrown traditional observability stores&rdquo; precisely because of this shape: hundreds of nested spans, multi-modal content, and spans that stay open for hours create data volumes and query patterns that general-purpose databases were never designed to handle.</p>
<p>The problem is not just volume. It is structure. A trajectory is a directed graph of decisions, tool invocations, model calls, and intermediate reasoning — not a flat log line. When you try to store that in a conventional relational or time-series database, you either flatten away the relationships that matter or you pay a heavy cost to reconstruct the tree on every read. A dedicated ETL pipeline exists to solve this mismatch before data ever reaches a store.</p>
<p>There is also a compliance dimension. The nondeterministic behavior of LLM agents defies the static auditing approaches that historically underpinned software assurance, as the AgentTrace research on arXiv (2602.10133) argues. You cannot prove what an agent did by reading its source code, because the same prompt can produce different actions on different runs. The only reliable evidence is the recorded trajectory itself. That makes extraction and transformation of trajectory data a first-class engineering concern, not an afterthought.</p>
<h2 id="the-three-surface-telemetry-model-operational-cognitive-contextual">The Three-Surface Telemetry Model (Operational, Cognitive, Contextual)</h2>
<p>Before you build an ETL pipeline, you need a schema that captures everything an auditor or an engineer might need to reconstruct an agent&rsquo;s behavior. The AgentTrace framework proposes a useful model: instrument agents across three surfaces.</p>
<ul>
<li><strong>Operational telemetry</strong> records what the system did mechanically: which tools were called, with what arguments, what latency each call incurred, what errors occurred, and what resources were consumed.</li>
<li><strong>Cognitive telemetry</strong> captures the model&rsquo;s reasoning and decisions: the prompts sent, the completions returned, the chain-of-thought or reasoning traces, and the choices the agent made at each branch.</li>
<li><strong>Contextual telemetry</strong> captures the environment around the run: the user identity, the session, the input context, the retrieved documents, and the system state that shaped the agent&rsquo;s behavior.</li>
</ul>
<p>Why does this three-way split matter for an ETL pipeline? Because the three surfaces have different retention, sensitivity, and access requirements. Operational telemetry is low-sensitivity and high-volume — you want it in your observability backend for months. Cognitive telemetry is high-sensitivity and often contains proprietary reasoning — you may want it encrypted, access-controlled, and retained only as long as regulation requires. Contextual telemetry can contain personal data, which triggers data-protection obligations. By keeping these surfaces distinct in your schema, you can route and retain each one independently.</p>
<h2 id="local-first-capture-keeping-sensitive-trajectory-data-on-prem">Local-First Capture: Keeping Sensitive Trajectory Data On-Prem</h2>
<p>The &ldquo;local-first&rdquo; principle is the defining design choice of a trajectory ETL. Instead of streaming every event to a cloud backend the moment it happens, you capture trajectories on the device or in the on-prem environment where the agent runs, and you sync only what is needed. The traced.run project articulates the motivation directly: &ldquo;less data over the wire, interface matches what&rsquo;s actually used.&rdquo;</p>
<p>Local-first capture has three concrete benefits.</p>
<ol>
<li><strong>Privacy and sovereignty.</strong> Sensitive agent data — customer conversations, internal documents, proprietary reasoning — never leaves your boundary unless you explicitly route it. This is essential for regulated industries and for enterprises that cannot send data to third-party clouds.</li>
<li><strong>Resilience.</strong> If the network drops or the observability backend is down, the agent keeps running and the trajectory keeps recording locally. You lose nothing when connectivity returns.</li>
<li><strong>Cost control.</strong> You only pay to transmit and store the events that actually matter, not the full firehose of every token and every intermediate step.</li>
</ol>
<p>The trade-off is that you need a local buffer, a sync protocol, and a way to reconcile local and remote state. In practice this means a write-ahead log on disk, a queue that can survive process restarts, and a sync mechanism that knows which events have been acknowledged by the destination.</p>
<h2 id="building-the-etl-on-opentelemetry-genai-semantic-conventions">Building the ETL on OpenTelemetry GenAI Semantic Conventions</h2>
<p>The single most important decision you can make is to build your trajectory ETL on OpenTelemetry (OTel) and its GenAI semantic conventions. SigNoz and the broader observability community argue that OpenTelemetry should be the standard for LLM observability, and the GenAI semantic conventions standardize how LLM and agent spans, events, and metrics are emitted. OpenLIT, Langtrace, Traceloop, and Helicone all build OTel-native LLM observability, which means a pipeline that emits OTel-compliant spans can feed any of them without rewriting instrumentation.</p>
<p>Why does this matter for a compliance router specifically? Because vendor neutrality is a compliance property. If your audit trail is locked into a proprietary format, you cannot easily produce it for a regulator, migrate it to a new provider, or prove its integrity across systems. OTel gives you a portable, open, well-documented format that any tool can consume.</p>
<p>In practice, your ETL&rsquo;s extraction stage should emit OTel spans and events using the GenAI semantic conventions for model calls, tool calls, and agent decisions. The transformation stage then enriches those spans with the three-surface classification, adds routing metadata, and normalizes timestamps and identifiers. Because the source format is standard, your transformation logic is reusable and your downstream stores are interchangeable.</p>
<h2 id="the-compliance-router-routing-events-to-audit-observability-and-warehouse-stores">The Compliance Router: Routing Events to Audit, Observability, and Warehouse Stores</h2>
<p>The &ldquo;router&rdquo; in a trajectory ETL is the component that decides where each event goes. It is a policy engine that evaluates every transformed event against a set of rules and dispatches it to the appropriate destination. A typical configuration has three destination classes.</p>
<table>
  <thead>
      <tr>
          <th>Destination</th>
          <th>Purpose</th>
          <th>Typical events</th>
          <th>Retention</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>Audit log</td>
          <td>Regulatory evidence, tamper-proof</td>
          <td>Cognitive + contextual, decisions, tool calls</td>
          <td>Long, immutable</td>
      </tr>
      <tr>
          <td>Observability backend</td>
          <td>Debugging, performance, alerting</td>
          <td>Operational telemetry, latency, errors</td>
          <td>Medium, queryable</td>
      </tr>
      <tr>
          <td>Data warehouse</td>
          <td>Analytics, training, reporting</td>
          <td>Aggregated trajectories, metrics</td>
          <td>Long, analytical</td>
      </tr>
  </tbody>
</table>
<p>The routing rules encode your compliance posture. For example, an event containing personal data might be routed to the audit log with encryption and access controls, while the same event&rsquo;s operational metadata is routed to the observability backend without the sensitive payload. An event tied to a high-risk decision under the EU AI Act might be flagged for long-term immutable retention, while routine tool calls are retained for only 30 days.</p>
<p>The router should be deterministic and testable. You want to be able to prove, for a given event and a given rule set, exactly which destination it went to and why. That is what makes the router itself auditable — a compliance system that cannot explain its own routing decisions is not much better than no system at all.</p>
<h2 id="tamper-proof-audit-trails-for-eu-ai-act-article-12-and-nist-ai-rmf">Tamper-Proof Audit Trails for EU AI Act Article 12 and NIST AI RMF</h2>
<p>The compliance driver for trajectory ETL is increasingly regulatory. The EU AI Act Article 12 requires high-risk AI systems to automatically record logs (event logs) that enable traceability of system operation, with an August 2026 compliance deadline for many obligations. The NIST AI Risk Management Framework similarly emphasizes documentation, transparency, and accountability throughout the AI lifecycle.</p>
<p>A tamper-proof audit trail is the technical answer to these requirements. It means that once a trajectory event is committed to the audit log, it cannot be silently altered or deleted. Practical techniques include:</p>
<ul>
<li><strong>Append-only storage.</strong> The audit log only ever grows; there is no update or delete path.</li>
<li><strong>Hash chaining.</strong> Each event&rsquo;s hash includes the previous event&rsquo;s hash, so any modification breaks the chain and is immediately detectable.</li>
<li><strong>Cryptographic signing.</strong> Events are signed with a key held outside the agent&rsquo;s runtime, so even a compromised agent cannot forge audit entries.</li>
<li><strong>Access separation.</strong> The audit log is written by the router but readable only through a separate, controlled interface.</li>
</ul>
<p>The three-surface model maps cleanly onto these requirements. For EU AI Act Article 12, you need to record enough of the cognitive and contextual surfaces to reconstruct what the system did and why. For NIST AI RMF, you need the operational surface to demonstrate that risk controls were applied and monitored. A trajectory ETL that captures all three surfaces and routes them to a tamper-proof audit store gives you the evidence base both frameworks demand.</p>
<h2 id="choosing-storage-purpose-built-trace-stores-vs-general-purpose-databases">Choosing Storage: Purpose-Built Trace Stores vs. General-Purpose Databases</h2>
<p>Once your ETL has extracted and transformed trajectory data, where does it live? The research is clear that general-purpose databases struggle with agent trace workloads. LangChain&rsquo;s SmithDB reports P50 latencies of 92ms for trace tree loads and 400ms for full-text search on agent observability workloads — performance that comes from a purpose-built data layer designed for the shape of agent traces.</p>
<table>
  <thead>
      <tr>
          <th>Consideration</th>
          <th>Purpose-built trace store</th>
          <th>General-purpose database</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>Trace tree reconstruction</td>
          <td>Native, fast</td>
          <td>Requires joins or denormalization</td>
      </tr>
      <tr>
          <td>Multi-modal content</td>
          <td>First-class</td>
          <td>Awkward, needs blobs</td>
      </tr>
      <tr>
          <td>Long-open spans</td>
          <td>Handled well</td>
          <td>Time-series assumptions break</td>
      </tr>
      <tr>
          <td>Full-text search</td>
          <td>Optimized</td>
          <td>Often bolted on</td>
      </tr>
      <tr>
          <td>Vendor lock-in</td>
          <td>Higher</td>
          <td>Lower, but schema burden is yours</td>
      </tr>
  </tbody>
</table>
<p>The pragmatic answer is a hybrid. Use a purpose-built trace store for the observability and debugging workload where interactive trace-tree exploration matters. Use a general-purpose warehouse for the analytical and compliance workload where you need SQL, joins, and long-term retention. The ETL router is what makes this hybrid possible: it sends the same trajectory to both destinations in the appropriate shape, so you never have to choose one store for everything.</p>
<h2 id="a-reference-trajpack-architecture-and-implementation-walkthrough">A Reference TrajPack Architecture and Implementation Walkthrough</h2>
<p>Putting it together, a reference TrajPack architecture has five stages.</p>
<ol>
<li><strong>Instrumentation.</strong> The agent runtime emits OTel GenAI spans for every model call, tool call, and decision, tagged with the three-surface classification.</li>
<li><strong>Local capture.</strong> A local write-ahead log buffers all spans on-device or on-prem, surviving restarts and network outages.</li>
<li><strong>Extraction.</strong> A collector reads the local log and normalizes spans into a canonical trajectory event format.</li>
<li><strong>Transformation.</strong> The transformer enriches each event with routing metadata, classifies sensitivity, and computes hash-chain integrity values.</li>
<li><strong>Routing.</strong> The compliance router evaluates policy rules and dispatches each event to the audit log, observability backend, or warehouse.</li>
</ol>
<p>A minimal implementation might look like this: an OTel SDK in the agent emits spans; a local OTel collector with a file exporter writes them to a JSONL buffer; a small Python service reads the buffer, classifies each span by surface, and applies routing rules; and the router writes signed, hash-chained records to an append-only audit store while forwarding operational spans to an OTel-native backend. The whole thing runs on-prem, with only the observability stream leaving the boundary.</p>
<h2 id="common-pitfalls-and-how-to-avoid-them">Common Pitfalls and How to Avoid Them</h2>
<p>Building a trajectory ETL is full of subtle failure modes. Here are the most common ones and how to avoid them.</p>
<ul>
<li><strong>Flattening the trajectory.</strong> If you store spans as flat log lines, you lose the parent-child relationships that make a trajectory meaningful. Preserve the tree structure in your schema.</li>
<li><strong>Sending everything to the cloud.</strong> This defeats local-first capture and can violate data-protection rules. Route selectively.</li>
<li><strong>Skipping the three-surface split.</strong> If you treat all telemetry the same, you cannot apply different retention and access controls to sensitive reasoning versus operational metrics.</li>
<li><strong>Ignoring vendor lock-in.</strong> A proprietary trace format makes your audit trail hard to produce and migrate. Standardize on OTel.</li>
<li><strong>Making the router opaque.</strong> If you cannot explain why an event went to a given store, the router itself is a compliance risk. Log routing decisions.</li>
<li><strong>Forgetting tamper-evidence.</strong> An audit log you can edit is not an audit log. Use append-only storage and hash chaining.</li>
</ul>
<h2 id="conclusion-from-observability-to-accountability">Conclusion: From Observability to Accountability</h2>
<p>Agent observability is no longer just about debugging — it is about accountability. As agents move from demos to production, enterprises and regulators increasingly require proof that an agent is trustworthy, and that proof lives in the trajectory. A local-first agent trajectory ETL and compliance router gives you that proof: it captures the full operational, cognitive, and contextual picture, keeps sensitive data on-prem, standardizes on OpenTelemetry, and routes every event to the right store with a tamper-proof audit trail. By separating extraction and transformation from storage, you avoid lock-in and build a pipeline that can satisfy the EU AI Act Article 12, the NIST AI RMF, and the next regulation after them.</p>
<h2 id="faq">FAQ</h2>
<p><strong>What is an agent trajectory ETL?</strong>
An agent trajectory ETL is a pipeline that extracts raw LLM agent traces, transforms them into structured portable telemetry, and routes each event to the appropriate destination such as an audit log, observability backend, or data warehouse.</p>
<p><strong>Why do agent traces need a dedicated pipeline instead of a normal database?</strong>
Agent traces contain hundreds of nested spans, multi-modal content, and spans that stay open for hours, creating data volumes and query patterns that general-purpose databases were never designed to handle.</p>
<p><strong>What does &ldquo;local-first&rdquo; mean for agent trajectory data?</strong>
Local-first means capturing trajectories on-device or on-prem and syncing only what is needed, keeping sensitive data within your boundary, improving resilience, and reducing transmission and storage costs.</p>
<p><strong>How does a compliance router work?</strong>
A compliance router is a policy engine that evaluates every transformed trajectory event against routing rules and dispatches it to the right store — audit log, observability backend, or warehouse — based on sensitivity, regulatory requirements, and retention policy.</p>
<p><strong>How do I satisfy EU AI Act Article 12 logging requirements?</strong>
Record the operational, cognitive, and contextual surfaces of agent behavior, route them to an append-only, hash-chained, cryptographically signed audit store, and keep the routing decisions themselves auditable and explainable.</p>
]]></content:encoded></item><item><title>Oodle.ai Agent Trace Pricing: $10 per Million Traces, Explained</title><link>https://baeseokjae.github.io/posts/oodle/</link><pubDate>Tue, 18 Aug 2026 13:01:59 +0000</pubDate><guid>https://baeseokjae.github.io/posts/oodle/</guid><description>Oodle.ai charges $10 per million agent traces with no sampling and sub-second query latency — 8x cheaper than Langfuse. Here&amp;#39;s how it works and who it fits.</description><content:encoded><![CDATA[<p>Oodle.ai prices agent trace observability at $10 per million spans, with no sampling, sub-second p99 query latency, and 100% of traces analyzed. That is roughly 8x cheaper than Langfuse&rsquo;s base tier ($80 per million units) and far below the per-seat-plus-storage model LangSmith uses. The company processed 120 million agent traces in the last month, and its founder reports Langfuse was 6x more expensive for their own observability workload. This review explains how Oodle achieves that price, whether it is genuinely cheap, and who should adopt it.</p>
<h2 id="what-is-oodleai-and-why-does-10-per-million-agent-traces-matter">What Is Oodle.ai and Why Does $10 per Million Agent Traces Matter?</h2>
<p>Oodle.ai is an LLM agent observability platform built on a custom columnar storage engine. Unlike traditional APM tools that sample a small fraction of traces to control cost, Oodle retains every agent execution with zero sampling. The headline price of $10 per million spans is significant because agent traces are dramatically larger than traditional traces — system prompts alone can exceed 200KB — which makes full-fidelity storage historically expensive.</p>
<p>The pricing matters for a simple reason: production AI agents generate enormous volumes of trace data. A single agent that runs thousands of tasks per day can produce millions of spans per month. At Langfuse&rsquo;s $80-per-million base rate, that becomes a serious line item. At Oodle&rsquo;s $10-per-million rate, full-fidelity observability becomes affordable enough to run on every trace rather than a sampled subset.</p>
<p>Oodle&rsquo;s positioning is that you should not have to choose between cost and completeness. The platform detects silent failures that sampling misses: aborted tasks, infinite loops, frustrated users, budget burns, and duplicate prompts. Its case study with Fello, an AI teammate platform, shows 3M+ agent traces captured per day with zero sampling.</p>
<h2 id="how-does-oodle-achieve-10million-architecture-and-storage-engine">How Does Oodle Achieve $10/Million? Architecture and Storage Engine</h2>
<p>The cost-efficiency story starts with the storage engine. Oodle built a custom columnar database on object storage (S3) for each observability signal — logs, metrics, and traces. Data is stored in a proprietary parquet-like columnar file format, and queries run via AWS Lambda, so compute only spins up when a query is issued.</p>
<p>This architecture delivers three claimed advantages:</p>
<ul>
<li><strong>3-5x more cost efficient than traditional observability</strong> platforms</li>
<li><strong>10x more cost efficient than other agent observability</strong> solutions</li>
<li><strong>Sub-second p99 query latency</strong> even on large trace volumes</li>
</ul>
<p>The engineering team documented several optimizations that made this possible. They use out-of-band dictionary storage to compress repeated strings, lazy row group loading that cut peak memory from 5.3GB to 1.3GB, and a decode-only dictionary reader. They also fixed a persistent Lambda memory leak. Together these changes reduced a common 7-day query from 60 seconds to under 2 seconds.</p>
<p>The key insight is that agent traces are not like traditional traces. Traditional observability deals with small tags and short spans. Agent traces carry huge payloads — full system prompts, tool outputs, and reasoning chains. A storage engine designed for small tags cannot handle this efficiently. Oodle&rsquo;s columnar format is purpose-built for these large, repetitive payloads, which is why it can store them at roughly $1 per million observations in the storage layer.</p>
<h2 id="the-no-sampling-philosophy-why-random-sampling-fails-for-agents">The No-Sampling Philosophy: Why Random Sampling Fails for Agents</h2>
<p>Oodle&rsquo;s core thesis is that sampling evals creates random blind spots. If you evaluate only 1% of your traces, you miss the rare edge cases and hallucinations that actually break production agents. A random 1% sample is statistically unlikely to catch a failure that occurs in 0.5% of runs — the exact failures that matter most.</p>
<p>The problem is that evaluating every trace with an expensive LLM judge is cost-prohibitive. This is the tension Oodle&rsquo;s architecture resolves. The answer is not to sample randomly, but to judge all observations using cheap deterministic layers first, then reserve expensive LLM judges for the small subset that actually has problems.</p>
<p>This &ldquo;no sampling&rdquo; approach matters for reliability. Silent failures — tasks that abort, loops that never terminate, users who get frustrated, budgets that burn through — are precisely the events that a 1% sample will miss. By analyzing 100% of traces, Oodle surfaces these failures reliably.</p>
<h2 id="chained-evaluation-cheap-filters-before-expensive-llm-judges">Chained Evaluation: Cheap Filters Before Expensive LLM Judges</h2>
<p>Oodle&rsquo;s evaluation pipeline uses a chained approach. Cheap deterministic layers run on every trace, and expensive LLM judges run only on the 5-10% of traces that the cheap layers flag as problematic.</p>
<p>The deterministic layers include:</p>
<ul>
<li><strong>VADER sentiment analysis</strong> (free, deterministic) on every observation from the second turn onward, to detect user frustration</li>
<li><strong>Code evaluators</strong> that check for valid JSON, correct tool selection, token limits, and hallucinated URLs — all running on every trace for free</li>
<li><strong>Anomaly detection</strong> on latency, token counts, and other signals</li>
</ul>
<p>Only after these cheap filters identify a problem does an LLM judge run. This means the expensive LLM-as-a-judge cost is applied to roughly 5-10% of traces instead of a random 1% sample — and crucially, it is applied to the traces that actually have issues, not a random subset.</p>
<p>This is the mechanism that makes $10 per million spans viable with healthy margins. The deterministic-first approach means the marginal cost of processing each additional trace is tiny, because most traces never reach the expensive LLM stage.</p>
<h2 id="oodle-vs-langfuse-vs-langsmith-agent-trace-pricing-compared">Oodle vs Langfuse vs LangSmith: Agent Trace Pricing Compared</h2>
<p>To understand whether $10 per million is cheap, it helps to compare against the two dominant competitors.</p>
<table>
  <thead>
      <tr>
          <th>Platform</th>
          <th>Pricing Model</th>
          <th>Effective Cost per Million</th>
          <th>Notes</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td><strong>Oodle.ai</strong></td>
          <td>$10 per million spans</td>
          <td><strong>$10</strong></td>
          <td>No sampling, sub-second p99, 100% analyzed</td>
      </tr>
      <tr>
          <td><strong>Langfuse</strong></td>
          <td>$8 per 100k units (base tier)</td>
          <td><strong>$80</strong></td>
          <td>8x Oodle; tiers drop to $6/100k at 50M+</td>
      </tr>
      <tr>
          <td><strong>LangSmith</strong></td>
          <td>Per-seat + LSU storage units</td>
          <td><strong>$1.00 per LSU</strong> + $39/seat</td>
          <td>14-day base retention; usage-based on top</td>
      </tr>
  </tbody>
</table>
<p>Langfuse&rsquo;s pricing is usage-based: 0-100k units free, then $8 per 100k up to 1M, $7 per 100k up to 10M, $6.5 per 100k up to 50M, and $6 per 100k beyond. At the base tier that is $80 per million — 8x Oodle&rsquo;s price. Even at Langfuse&rsquo;s highest volume tier ($60 per million), Oodle is still 6x cheaper. Oodle&rsquo;s founder states Langfuse was 6x more expensive for their own agent observability workload.</p>
<p>LangSmith uses a different model: per-seat plans (Developer $0/seat with 5k base traces/month, Plus $39/seat with 10k base traces/month, Enterprise custom) plus pay-as-you-go for LangChain Compute Units ($1.50) and LangChain Storage Units ($1.00). Base traces have 14-day retention. For high-volume teams, the per-seat cost plus storage units can add up quickly.</p>
<p>The comparison table makes the pricing gap clear. For a team generating 10 million traces per month, Oodle would cost $100, while Langfuse would cost $800 at base tier. That is a meaningful difference for any production AI operation.</p>
<h2 id="is-10million-actually-cheap-market-context-and-alternatives">Is $10/Million Actually Cheap? Market Context and Alternatives</h2>
<p>The pricing is contested. On the Show HN thread for Oodle (July 14, 2026, 31 points), reactions were mixed. One commenter said &ldquo;Dang that&rsquo;s expensive,&rdquo; while another reported paying just $0.75 per million traces through a vendor — a dramatically cheaper alternative.</p>
<p>This is an important caveat. $10 per million is cheap relative to Langfuse and LangSmith, but it is not the cheapest option on the market. Teams that only need basic trace storage without the full evaluation pipeline may find cheaper vendors. The $0.75-per-million figure suggests there is a low-cost segment that Oodle does not compete in.</p>
<p>However, price per million is not the only metric. Oodle&rsquo;s value proposition includes the built-in evaluation pipeline, no-sampling reliability, and sub-second query latency. A cheaper vendor at $0.75 per million may not offer deterministic-first evaluation, silent-failure detection, or the same query performance. The right comparison depends on what you need beyond raw storage.</p>
<p>For high-volume production agents, the total cost of ownership matters more than the per-trace price. If full-fidelity tracing catches one expensive production incident that sampling would have missed, the observability cost is trivially justified.</p>
<h2 id="who-should-use-oodle-for-agent-observability-and-who-shouldnt">Who Should Use Oodle for Agent Observability (and Who Shouldn&rsquo;t)</h2>
<p>Oodle is a strong fit for teams with high-volume production agents where full-fidelity tracing matters. If you run thousands of agent tasks per day, need to detect silent failures reliably, and want sub-second query latency across millions of traces, Oodle&rsquo;s model is compelling. The Fello case study — 3M+ traces per day with zero sampling — is exactly the profile that benefits.</p>
<p>Oodle is also a good fit for teams that want to avoid the complexity of building their own columnar storage engine. The engineering effort documented in Oodle&rsquo;s blog — custom file formats, Lambda memory fixes, dictionary compression — is not something most teams want to replicate in-house.</p>
<p>Oodle may not be the right choice for:</p>
<ul>
<li><strong>Low-volume teams</strong> generating only a few thousand traces per month, where the $10-per-million rate is irrelevant and a free tier (like Langfuse&rsquo;s 0-100k free) is more attractive</li>
<li><strong>Teams that only need basic trace storage</strong> without evaluation, who can find cheaper vendors (the $0.75-per-million market segment)</li>
<li><strong>Teams already deeply invested in LangSmith&rsquo;s ecosystem</strong>, where the per-seat model and LangChain integration may outweigh the per-trace cost difference</li>
</ul>
<p>The decision hinges on volume and evaluation needs. High volume plus a need for reliable failure detection favors Oodle. Low volume or minimal evaluation needs favor cheaper or free alternatives.</p>
<h2 id="verdict-is-oodles-10million-agent-trace-pricing-worth-it">Verdict: Is Oodle&rsquo;s $10/Million Agent Trace Pricing Worth It?</h2>
<p>Oodle.ai&rsquo;s $10 per million agent traces is a genuine price disruption in agent observability. It is 8x cheaper than Langfuse&rsquo;s base tier and far more cost-effective than LangSmith&rsquo;s per-seat-plus-storage model for high-volume workloads. The no-sampling philosophy, backed by a deterministic-first evaluation pipeline, addresses a real reliability gap: random sampling misses the rare failures that break production agents.</p>
<p>The architecture — custom columnar storage on S3 with serverless Lambda compute — is the reason the price is sustainable. By running cheap deterministic filters on every trace and reserving expensive LLM judges for the 5-10% that actually have problems, Oodle keeps marginal costs low while analyzing 100% of traces.</p>
<p>The main caveat is that $10 per million is not the cheapest option on the market. HN commenters cited $0.75-per-million alternatives. But for teams that need full-fidelity tracing, silent-failure detection, and sub-second query performance at production scale, Oodle offers a compelling price-performance ratio that the incumbents do not match.</p>
<p>For high-volume production agent teams, Oodle&rsquo;s $10-per-million pricing is worth it — and it may be the most cost-effective way to get reliable, full-fidelity agent observability today.</p>
<h2 id="faq">FAQ</h2>
<h3 id="what-does-oodleai-charge-for-agent-traces">What does Oodle.ai charge for agent traces?</h3>
<p>Oodle.ai charges $10 per million agent trace spans, with no sampling and 100% of traces analyzed. The platform processed 120 million agent traces in the last month.</p>
<h3 id="how-does-oodles-pricing-compare-to-langfuse">How does Oodle&rsquo;s pricing compare to Langfuse?</h3>
<p>Oodle&rsquo;s $10 per million is roughly 8x cheaper than Langfuse&rsquo;s base tier of $8 per 100k units ($80 per million). Even at Langfuse&rsquo;s highest volume tier ($60 per million), Oodle is 6x cheaper.</p>
<h3 id="what-is-the-no-sampling-approach-in-oodle">What is the &ldquo;no sampling&rdquo; approach in Oodle?</h3>
<p>Oodle analyzes 100% of agent traces instead of a random sample. It uses cheap deterministic filters (VADER sentiment, code evaluators, anomaly detection) on every trace, then runs expensive LLM judges only on the 5-10% of traces that actually have problems.</p>
<h3 id="is-10-per-million-agent-traces-actually-cheap">Is $10 per million agent traces actually cheap?</h3>
<p>It is cheap relative to Langfuse and LangSmith, but not the cheapest on the market. Some HN commenters reported paying $0.75 per million through other vendors. The value depends on whether you need the full evaluation pipeline and no-sampling reliability.</p>
<h3 id="who-should-use-oodle-for-agent-observability">Who should use Oodle for agent observability?</h3>
<p>High-volume production agent teams that need full-fidelity tracing, silent-failure detection, and sub-second query latency. Low-volume teams or those needing only basic storage may find cheaper or free alternatives more suitable.</p>
]]></content:encoded></item><item><title>What Breaks an AI Agent After 50 Clean Demos: Production Reliability Guide (2026)</title><link>https://baeseokjae.github.io/posts/ai-agent-50-clean-demos-break-2026/</link><pubDate>Tue, 14 Jul 2026 12:00:00 +0000</pubDate><guid>https://baeseokjae.github.io/posts/ai-agent-50-clean-demos-break-2026/</guid><description>Why AI agents that pass 50 demos fail in production — the 7 failure modes, the $47K prompt injection case study, and how to build production-grade reliability with chaos engineering, 12-factor architecture, and multi-trial testing.</description><content:encoded><![CDATA[<p>You demo an AI agent to your team. Fifty runs, zero failures. Everyone&rsquo;s impressed. You deploy to production. Within a week, it&rsquo;s hallucinating tool calls, getting stuck in loops, and your Slack is full of &ldquo;the agent did something weird&rdquo; messages.</p>
<p>I&rsquo;ve been there. Multiple times. And I&rsquo;ve spent the last year digging into why this happens and what actually works to fix it.</p>
<p>The short answer: <strong>your agent isn&rsquo;t broken — your testing methodology is.</strong> Single-digit demos and pass/fail judgments hide a massive variance problem that only emerges under statistical scrutiny. Gartner predicts over 40% of AI agent projects will fail by 2027, and in January 2026, a prompt injection in a customer support agent processed a $47,000 fraudulent refund. These aren&rsquo;t edge cases — they&rsquo;re systematic failures that most teams aren&rsquo;t testing for.</p>
<p>This guide covers the seven failure modes that kill production agents, the architectural patterns that survive them, and the testing infrastructure you need before you can trust an agent in production.</p>
<h2 id="the-50-demo-illusion">The 50-Demo Illusion</h2>
<p>Here&rsquo;s a number that should scare you: LLMs show up to <strong>72% variance across runs even at temperature=0</strong>. This isn&rsquo;t a bug in one model — it&rsquo;s a fundamental property of how transformer-based models sample tokens. The <a href="/posts/llm-benchmark-variance-2026/">LLM benchmark variance problem</a> is well-documented, and it applies doubly to agentic workflows where a single token change in a tool selection can cascade into a completely different execution path.</p>
<p>The <a href="https://github.com/alepot55/agentrial">agentrial</a> project ran agents 100 times on the same task and found that pass rates dropped to <strong>60-80%</strong> despite 90%+ accuracy in single-run demos. That&rsquo;s not a 10% degradation — it&rsquo;s a 20-40 percentage point drop that only appears when you run enough trials to get statistically meaningful data.</p>
<p>In practice, this means:</p>
<ul>
<li><strong>A single &ldquo;pass&rdquo; tells you nothing.</strong> You need at least 30-50 runs to establish a baseline pass rate.</li>
<li><strong>Temperature=0 is not deterministic.</strong> It reduces variance but doesn&rsquo;t eliminate it. The model still samples from a probability distribution — it just picks the most likely token every time. But &ldquo;most likely&rdquo; changes based on floating-point precision, prompt formatting, and context window state.</li>
<li><strong>Variance compounds across steps.</strong> A 5-step agent where each step has 90% reliability has a theoretical pass rate of 0.9⁵ = 59%. Run that 100 times and you&rsquo;ll see failures in 40% of trials.</li>
</ul>
<h2 id="the-7-failure-modes-every-production-agent-faces">The 7 Failure Modes Every Production Agent Faces</h2>
<p>Most teams test hallucination and prompt injection. Almost no one systematically tests cascade failures, context limit drift, or data integration drift before shipping. Here are the seven failure modes I&rsquo;ve seen kill production agents in practice.</p>
<h3 id="1-hallucination-under-unexpected-inputs">1. Hallucination Under Unexpected Inputs</h3>
<p>This is the classic failure mode, but it&rsquo;s more nuanced than &ldquo;the model makes stuff up.&rdquo; In production, hallucination manifests as the agent confidently asserting facts that don&rsquo;t exist in its context window — not because the model is broken, but because it&rsquo;s operating outside its training distribution.</p>
<p>I&rsquo;ve watched an agent that correctly processed 47 out of 50 records, then at record 48 started returning fabricated data because the relevant instructions had scrolled out of the active context window. The model didn&rsquo;t tell me it was confused. It just started making things up with the same confidence it had for the first 47 records.</p>
<h3 id="2-edge-case-collapse-nulls-unicode-concurrency">2. Edge Case Collapse (Nulls, Unicode, Concurrency)</h3>
<p>Production data is messy. Null fields, unicode homoglyphs, concurrent requests, and malformed inputs that your test suite never generated will find their way into your agent&rsquo;s context window. I&rsquo;ve seen agents crash on a single null field in a JSON response — not because the tool returned an error, but because the model couldn&rsquo;t decide what to do with <code>null</code> and entered an infinite reasoning loop.</p>
<p>Unicode homoglyphs are particularly nasty. An attacker can replace a Latin &lsquo;a&rsquo; with a Cyrillic &lsquo;а&rsquo; (U+0430) — visually identical, semantically different. Your input validation passes, but the agent interprets the string differently. This is one of the 18 prompt injection attack vectors that bypassed PromptGuard with 100% confidence in a 2026 audit.</p>
<h3 id="3-prompt-injection-and-adversarial-inputs">3. Prompt Injection and Adversarial Inputs</h3>
<p>The January 2026 incident is the one everyone references now: a customer support agent received a carefully crafted message that included hidden instructions embedded in what appeared to be a legitimate support request. The agent processed a $47,000 fraudulent refund before anyone caught it.</p>
<p>The techniques are evolving fast. Attackers now use:</p>
<ul>
<li><strong>Unicode homoglyphs</strong> — visually identical characters with different byte representations</li>
<li><strong>Base64-encoded instructions</strong> — the agent decodes and follows them</li>
<li><strong>ROT13 and other simple ciphers</strong> — the agent &ldquo;figures out&rdquo; the hidden message</li>
<li><strong>Non-English languages</strong> — instructions in a language the agent handles but the monitoring system doesn&rsquo;t</li>
<li><strong>Multi-turn fragmentation</strong> — spreading the injection across multiple conversation turns to avoid detection</li>
</ul>
<p>The <a href="/posts/secure-ai-agents-least-privilege-2026/">secure AI agents guide</a> covers defense-in-depth approaches, but the reality is: no single guardrail catches everything. In a 2026 audit, 12 out of 18 prompt injection attack vectors bypassed PromptGuard with 100% confidence. You need layered defenses, not a silver bullet.</p>
<h3 id="4-context-limit-surprises">4. Context Limit Surprises</h3>
<p>This is the most insidious failure mode because it&rsquo;s invisible until it hits. An agent that works perfectly with a 10-turn conversation will start hallucinating at turn 50 because the context window is full of accumulated tool outputs, error messages, and intermediate results.</p>
<p>The model doesn&rsquo;t tell you it&rsquo;s confused. It just starts making things up. I&rsquo;ve seen agents that correctly processed 47 out of 50 records, then at record 48 started returning fabricated data because the relevant instructions had scrolled out of the active context window.</p>
<p>The fix is the <strong>stateless reducer pattern</strong> — instead of dumping everything into the context window, maintain a compact state representation and only inject what the agent needs for the current step. This is the same pattern Redux popularized for frontend state management, and it works just as well for agents.</p>
<h3 id="5-cascade-failures-in-multi-step-workflows">5. Cascade Failures in Multi-Step Workflows</h3>
<p>Every step in an agent&rsquo;s execution chain adds failure surface area. A 10-step agent where each step has 95% reliability has a 40% chance of completing without error. But the degradation isn&rsquo;t linear — I&rsquo;ve seen agents that handle steps 1-4 perfectly, then at step 5 the context window is cluttered with intermediate results, the model loses track of what it was doing, and it starts repeating steps or skipping critical operations.</p>
<p>This is especially bad in the &ldquo;here&rsquo;s your prompt, here&rsquo;s a bag of tools, loop until you hit the goal&rdquo; pattern that many agent frameworks default to. The <a href="/posts/open-source-agent-eval-harness-comparison-2026/">open-source agent eval harnesses</a> I&rsquo;ve tested show this pattern consistently: agents with Directed Graph (DAG) orchestrators like Airflow or Prefect significantly outperform monolithic loop-based agents on multi-step tasks, because the orchestration layer handles state management and retry logic instead of leaving it to the LLM.</p>
<h3 id="6-data-integration-drift">6. Data Integration Drift</h3>
<p>Your agent depends on external data sources — APIs, databases, file systems. These sources change over time. An API returns a new field, a database schema changes, a file format gets updated. The agent, trained on the old format, starts making incorrect assumptions.</p>
<p>I&rsquo;ve seen this manifest as an agent that reliably parsed CSV exports for months, then suddenly started failing because the export tool added a BOM header. The agent didn&rsquo;t fail gracefully — it silently misparsed the data and made decisions based on the wrong columns. Data integration drift is almost never tested because it requires maintaining test fixtures that evolve with the production data sources.</p>
<h3 id="7-authorization-confusion-in-multi-tenant-systems">7. Authorization Confusion in Multi-Tenant Systems</h3>
<p>When an agent operates across multiple tenants or user contexts, it can accidentally apply one user&rsquo;s permissions to another&rsquo;s data. This is especially dangerous in customer support agents that handle multiple accounts — the agent might read data from account A while operating under account B&rsquo;s authorization context.</p>
<p>The fix is <strong>least-privilege tool access</strong>: each tool call should carry its own authorization context, not inherit the agent&rsquo;s global permissions. The <a href="/posts/secure-ai-agents-least-privilege-2026/">secure AI agents guide</a> covers this in detail, but the short version is: treat every tool call as an independent authorization boundary.</p>
<h2 id="epistemic-distortion-the-meta-failure-mode">Epistemic Distortion: The Meta-Failure Mode</h2>
<p>Beyond the seven specific failure modes, there&rsquo;s a meta-pattern that researchers have documented across 1,400+ controlled experiments: <strong>epistemic distortion</strong>. This is the agent&rsquo;s tendency to silently drop conflicting instructions, apply wrong evidence standards, or selectively ignore parts of its system prompt.</p>
<p>In practice, epistemic distortion looks like:</p>
<ul>
<li>The agent follows the most recent instruction even when earlier instructions explicitly contradict it</li>
<li>The agent applies different reasoning standards to different types of inputs (treating user input as more authoritative than system prompts)</li>
<li>The agent &ldquo;forgets&rdquo; constraints that were established earlier in the conversation</li>
</ul>
<p>This isn&rsquo;t a bug — it&rsquo;s a consequence of how attention mechanisms work. The model weights all tokens in its context window, and newer or more salient tokens can override earlier instructions. The fix is structural: don&rsquo;t rely on the model to remember constraints. Encode them in the tool schemas, the orchestration layer, and the validation pipeline.</p>
<h2 id="the-12-factor-agent-architecture">The 12-Factor Agent Architecture</h2>
<p>The <a href="https://github.com/humanlayer/12-factor-agents">12-Factor Agents</a> methodology (which hit 475+ points on Hacker News for good reason) provides a solid foundation. Here are the principles I&rsquo;ve found most impactful in practice.</p>
<h3 id="own-your-prompts-and-context-window">Own Your Prompts and Context Window</h3>
<p>Don&rsquo;t let the framework manage your prompts. You need explicit control over:</p>
<ul>
<li><strong>System prompt versioning</strong> — every change is a new version, tracked in git</li>
<li><strong>Context window budgeting</strong> — know exactly how many tokens each component uses</li>
<li><strong>Prompt injection surface</strong> — user input should never be concatenated into system prompts</li>
</ul>
<p>I use a simple pattern: each agent step gets a fixed token budget. The system prompt gets 30%, the current state gets 20%, tool definitions get 30%, and user input gets 20%. If something exceeds its budget, it gets truncated — not the model&rsquo;s job to figure out what&rsquo;s important.</p>
<h3 id="tools-as-structured-outputs">Tools as Structured Outputs</h3>
<p>Treat tool definitions as JSON schemas with strict validation, not as natural language descriptions. This means:</p>
<ul>
<li>Every tool parameter has a type, description, and required/optional flag</li>
<li>Tool calls are validated against the schema before execution</li>
<li>Validation failures return structured error messages, not &ldquo;something went wrong&rdquo;</li>
</ul>
<p>This pattern, combined with the <a href="/posts/llm-structured-output-guide-2026/">LLM structured output</a> capabilities available in 2026, means you can catch tool selection errors before they cause damage. The agent might still pick the wrong tool, but it won&rsquo;t call it with invalid parameters.</p>
<h3 id="small-focused-agents-over-monolithic-loops">Small, Focused Agents over Monolithic Loops</h3>
<p>A single agent with 20 tools and a &ldquo;keep going until done&rdquo; instruction is a recipe for unpredictable behavior. Instead, decompose your workflow into small, focused agents:</p>
<ul>
<li><strong>One agent per task type</strong> — each with 3-5 tools max</li>
<li><strong>DAG orchestration</strong> — use an orchestrator to sequence agents, not the LLM</li>
<li><strong>Explicit handoffs</strong> — each agent produces a structured output that the next agent consumes</li>
</ul>
<p>I&rsquo;ve found that agents with 3-5 tools have significantly lower tool selection drift than agents with 10+ tools. The model has fewer options to confuse, and the tool descriptions can be more specific.</p>
<h3 id="stateless-reducer-pattern">Stateless Reducer Pattern</h3>
<p>Instead of passing the entire conversation history to every agent call, maintain a compact state object:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-json" data-lang="json"><span style="display:flex;"><span>{
</span></span><span style="display:flex;"><span>  <span style="color:#f92672">&#34;step&#34;</span>: <span style="color:#ae81ff">4</span>,
</span></span><span style="display:flex;"><span>  <span style="color:#f92672">&#34;completed_tasks&#34;</span>: [<span style="color:#e6db74">&#34;search&#34;</span>, <span style="color:#e6db74">&#34;validate&#34;</span>, <span style="color:#e6db74">&#34;transform&#34;</span>],
</span></span><span style="display:flex;"><span>  <span style="color:#f92672">&#34;current_input&#34;</span>: <span style="color:#e6db74">&#34;output_from_step_3.json&#34;</span>,
</span></span><span style="display:flex;"><span>  <span style="color:#f92672">&#34;errors&#34;</span>: [],
</span></span><span style="display:flex;"><span>  <span style="color:#f92672">&#34;accumulated_results&#34;</span>: [<span style="color:#e6db74">&#34;result_1&#34;</span>, <span style="color:#e6db74">&#34;result_2&#34;</span>]
</span></span><span style="display:flex;"><span>}
</span></span></code></pre></div><p>Each agent step reads the current state, produces an action, and returns a new state. The full conversation history is stored in an audit log for debugging, but the agent only sees what it needs for the current step. This dramatically reduces context window pressure and eliminates the &ldquo;forgot what it was doing&rdquo; failure mode.</p>
<h2 id="testing-strategies-that-actually-work">Testing Strategies That Actually Work</h2>
<p>If you take one thing from this guide, let it be this: <strong>stop testing agents like you test deterministic software.</strong> Agents are stochastic systems, and they need stochastic testing methodologies.</p>
<h3 id="multi-trial-testing-with-confidence-intervals">Multi-Trial Testing with Confidence Intervals</h3>
<p>Run every test scenario at least 30 times. Calculate:</p>
<ul>
<li><strong>Pass rate</strong> with Wilson confidence intervals (not raw percentages)</li>
<li><strong>Cost variance</strong> — how much does each run cost in tokens?</li>
<li><strong>Latency variance</strong> — how long does each run take?</li>
</ul>
<p>The <a href="https://github.com/alepot55/agentrial">agentrial</a> framework does this out of the box. You define a task, run it 100 times, and get back pass rates with confidence intervals, cost distributions, and latency distributions. Without this, you&rsquo;re making decisions based on anecdotes.</p>
<h3 id="chaos-engineering-for-agents-flakestorm-approach">Chaos Engineering for Agents (Flakestorm Approach)</h3>
<p><a href="https://github.com/flakestorm">Flakestorm</a> and similar tools apply chaos engineering principles to agent systems. The approach is simple: inject failures at the tool call level and observe how the agent recovers. Testing a LangChain agent with this approach revealed a <strong>95% failure rate on adversarial inputs</strong> — not because the LLM was bad, but because the agent had no recovery mechanisms.</p>
<p>The chaos engineering scenarios I&rsquo;ve found most valuable:</p>
<ul>
<li><strong>Inject API latency</strong> — test timeout handling. Most agents crash when a tool call takes 30 seconds instead of 2.</li>
<li><strong>Return malformed tool outputs</strong> — test error recovery. Can the agent retry, or does it enter an infinite loop?</li>
<li><strong>Drop intermediate results</strong> — test state persistence. Does the agent resume or start over?</li>
<li><strong>Rotate model versions mid-workflow</strong> — test compatibility. Does the agent handle a different model&rsquo;s output format?</li>
<li><strong>Adversarial inputs</strong> — test prompt injection resistance. Can the agent distinguish instructions from data?</li>
</ul>
<p>I&rsquo;ve found that most agents fail catastrophically under these conditions — not because the LLM is bad, but because the surrounding infrastructure doesn&rsquo;t handle failures gracefully. A well-designed agent should degrade, not crash.</p>
<h3 id="cascade-failure-testing">Cascade Failure Testing</h3>
<p>This is the one almost no one tests. To test cascade failures:</p>
<ol>
<li>Inject a bad intermediate result at step N</li>
<li>Observe whether the agent detects the error or propagates it</li>
<li>Measure how many steps the error propagates before detection</li>
<li>Test recovery: can the agent roll back to a known-good state?</li>
</ol>
<p>The Fisher exact test (used by agentrial) can identify which specific step has statistically significant failure rates. This is the difference between &ldquo;the agent fails sometimes&rdquo; and &ldquo;step 4 of the tool selection phase has a 30% failure rate when the input contains more than 5 items.&rdquo;</p>
<h3 id="context-window-monitoring">Context Window Monitoring</h3>
<p>Monitor context window utilization as a first-class metric. When utilization exceeds 70%, the risk of context limit surprises increases exponentially. Set up alerts for:</p>
<ul>
<li><strong>Token count per turn</strong> — sudden increases indicate context window bloat</li>
<li><strong>Instruction proximity</strong> — how far back are the original instructions?</li>
<li><strong>Tool output size</strong> — are intermediate results consuming too much context?</li>
</ul>
<h2 id="infrastructure-for-resilient-agents">Infrastructure for Resilient Agents</h2>
<p>The testing patterns above tell you where your agent breaks. The infrastructure patterns below keep it running anyway.</p>
<h3 id="durable-execution-and-state-persistence">Durable Execution and State Persistence</h3>
<p><a href="https://github.com/orra-dev/orra">Orra</a> and similar tools provide durable execution for AI agent workflows. The key idea: if an agent crashes mid-step, it should resume from where it left off, not start over. This requires:</p>
<ul>
<li><strong>State persistence</strong> — every intermediate result is saved to durable storage</li>
<li><strong>Idempotent tool calls</strong> — running the same tool with the same input twice produces the same result</li>
<li><strong>Automatic retry with backoff</strong> — transient failures don&rsquo;t kill the workflow</li>
</ul>
<p>The <a href="/posts/openai-agents-sdk-temporal-integration-2026/">OpenAI Agents SDK Temporal integration</a> is a good example of this pattern in practice — Temporal provides the durable execution layer while the agent SDK handles the LLM interactions.</p>
<h3 id="pre-validated-execution-plans">Pre-Validated Execution Plans</h3>
<p>Before an agent executes anything, validate the entire plan against your constraints:</p>
<ul>
<li>Are all required tools available?</li>
<li>Do the tool parameters match the schemas?</li>
<li>Is the estimated cost within budget?</li>
<li>Are there any circular dependencies in the plan?</li>
</ul>
<p>This catches the &ldquo;agent decided to do something unexpected&rdquo; failure mode before it costs you money or corrupts data. Orra&rsquo;s pre-validated execution plans are a reference implementation of this pattern.</p>
<h3 id="automatic-health-monitoring-and-audit-logs">Automatic Health Monitoring and Audit Logs</h3>
<p>You need observability at two levels:</p>
<ol>
<li><strong>Agent-level</strong> — what did the agent decide, what tools did it call, what was the outcome?</li>
<li><strong>System-level</strong> — what&rsquo;s the pass rate trend, cost trend, latency trend?</li>
</ol>
<p>The <a href="/posts/llm-observability-tools-comparison-2026/">LLM observability tools</a> (LangSmith, Langfuse, Helicone, Arize) all support agent tracing now. The key metric I track is <strong>pass rate over a rolling 24-hour window</strong> — if it drops below 80%, I want to know immediately, not when a user complains.</p>
<h2 id="building-a-production-reliability-checklist">Building a Production Reliability Checklist</h2>
<p>Before you deploy an agent to production, run through this checklist:</p>
<p><strong>Testing (pre-deployment)</strong></p>
<ul>
<li><input disabled="" type="checkbox"> Multi-trial testing: 100 runs per scenario with confidence intervals</li>
<li><input disabled="" type="checkbox"> Chaos engineering: injected failures at tool call level</li>
<li><input disabled="" type="checkbox"> Adversarial eval: prompt injection attack vectors tested</li>
<li><input disabled="" type="checkbox"> Cascade failure testing: bad intermediate results injected</li>
<li><input disabled="" type="checkbox"> Edge case coverage: nulls, unicode, concurrent requests</li>
</ul>
<p><strong>Architecture</strong></p>
<ul>
<li><input disabled="" type="checkbox"> Stateless reducer pattern (not full conversation history in context)</li>
<li><input disabled="" type="checkbox"> Small, focused agents (3-5 tools per agent)</li>
<li><input disabled="" type="checkbox"> DAG orchestration (not monolithic loops)</li>
<li><input disabled="" type="checkbox"> Structured tool definitions with JSON schema validation</li>
<li><input disabled="" type="checkbox"> Pre-validated execution plans</li>
</ul>
<p><strong>Infrastructure</strong></p>
<ul>
<li><input disabled="" type="checkbox"> Durable execution with state persistence</li>
<li><input disabled="" type="checkbox"> Action deduplication (idempotent tool calls)</li>
<li><input disabled="" type="checkbox"> Human-in-the-loop approval gates for destructive operations</li>
<li><input disabled="" type="checkbox"> Least-privilege tool access with per-call authorization</li>
</ul>
<p><strong>Monitoring</strong></p>
<ul>
<li><input disabled="" type="checkbox"> Rolling 24-hour pass rate with alerts</li>
<li><input disabled="" type="checkbox"> Context window utilization tracking</li>
<li><input disabled="" type="checkbox"> Cost anomaly detection</li>
<li><input disabled="" type="checkbox"> Tool call distribution monitoring</li>
<li><input disabled="" type="checkbox"> Audit log with full conversation history</li>
</ul>
<h2 id="from-demo-to-production-confidence">From Demo to Production Confidence</h2>
<p>The 40%+ failure rate for AI agent projects isn&rsquo;t because agents are fundamentally unreliable. It&rsquo;s because most teams treat agents like deterministic software and are surprised when stochastic behavior emerges.</p>
<p>The path to production confidence is straightforward:</p>
<ol>
<li><strong>Test statistically</strong> — run 100 trials, not 5 demos</li>
<li><strong>Architect for failure</strong> — DAG orchestrators, stateless reducers, durable execution</li>
<li><strong>Monitor continuously</strong> — rolling pass rates, cost trends, latency distributions</li>
<li><strong>Gate destructively</strong> — human approval for high-risk operations</li>
<li><strong>Test adversarially</strong> — prompt injection, chaos engineering, cascade failures</li>
</ol>
<p>I&rsquo;ve been running production agents for over a year now, and the ones that survive are the ones built with these patterns from day one. The ones that fail are the ones that looked perfect in demos and fell apart under real load.</p>
<p>Your agent isn&rsquo;t broken. Your testing methodology is. Fix that first, and the rest follows.</p>
]]></content:encoded></item><item><title>What Breaks an AI Agent After 50 Clean Demos: Production Reliability Guide (2026)</title><link>https://baeseokjae.github.io/posts/what-breaks-an-ai-agent-after-50-clean-demos-production-reliability-guide/</link><pubDate>Tue, 14 Jul 2026 12:00:00 +0000</pubDate><guid>https://baeseokjae.github.io/posts/what-breaks-an-ai-agent-after-50-clean-demos-production-reliability-guide/</guid><description>Why AI agents that pass 50 demos fail in production — the 7 failure modes, the $47K prompt injection case study, and how to build production-grade reliability with chaos engineering, 12-factor architecture, and multi-trial testing.</description><content:encoded><![CDATA[<p>You demo an AI agent to your team. Fifty runs, zero failures. Everyone&rsquo;s impressed. You deploy to production. Within a week, it&rsquo;s hallucinating tool calls, getting stuck in loops, and your Slack is full of &ldquo;the agent did something weird&rdquo; messages.</p>
<p>I&rsquo;ve been there. Multiple times. And I&rsquo;ve spent the last year digging into why this happens and what actually works to fix it.</p>
<p>The short answer: <strong>your agent isn&rsquo;t broken — your testing methodology is.</strong> Single-digit demos and pass/fail judgments hide a massive variance problem that only emerges under statistical scrutiny. Gartner predicts over 40% of AI agent projects will fail by 2027, and in January 2026, a prompt injection in a customer support agent processed a $47,000 fraudulent refund. These aren&rsquo;t edge cases — they&rsquo;re systematic failures that most teams aren&rsquo;t testing for.</p>
<p>This guide covers the seven failure modes that kill production agents, the architectural patterns that survive them, and the testing infrastructure you need before you can trust an agent in production.</p>
<h2 id="the-50-demo-illusion">The 50-Demo Illusion</h2>
<p>Here&rsquo;s a number that should scare you: LLMs show up to <strong>72% variance across runs even at temperature=0</strong>. This isn&rsquo;t a bug in one model — it&rsquo;s a fundamental property of how transformer-based models sample tokens. The <a href="/posts/llm-benchmark-variance-2026/">LLM benchmark variance problem</a> is well-documented, and it applies doubly to agentic workflows where a single token change in a tool selection can cascade into a completely different execution path.</p>
<p>The <a href="https://github.com/alepot55/agentrial">agentrial</a> project ran agents 100 times on the same task and found that pass rates dropped to <strong>60-80%</strong> despite 90%+ accuracy in single-run demos. That&rsquo;s not a 10% degradation — it&rsquo;s a 20-40 percentage point drop that only appears when you run enough trials to get statistically meaningful data.</p>
<p>In practice, this means:</p>
<ul>
<li><strong>A single &ldquo;pass&rdquo; tells you nothing.</strong> You need at least 30-50 runs to establish a baseline pass rate.</li>
<li><strong>Temperature=0 is not deterministic.</strong> It reduces variance but doesn&rsquo;t eliminate it. The model still samples from a probability distribution — it just picks the most likely token every time. But &ldquo;most likely&rdquo; changes based on floating-point precision, prompt formatting, and context window state.</li>
<li><strong>Variance compounds across steps.</strong> A 5-step agent where each step has 90% reliability has a theoretical pass rate of 0.9⁵ = 59%. Run that 100 times and you&rsquo;ll see failures in 40% of trials.</li>
</ul>
<h2 id="the-7-failure-modes-every-production-agent-faces">The 7 Failure Modes Every Production Agent Faces</h2>
<p>Most teams test hallucination and prompt injection. Almost no one systematically tests cascade failures, context limit drift, or data integration drift before shipping. Here are the seven failure modes I&rsquo;ve seen kill production agents in practice.</p>
<h3 id="1-hallucination-under-unexpected-inputs">1. Hallucination Under Unexpected Inputs</h3>
<p>This is the classic failure mode, but it&rsquo;s more nuanced than &ldquo;the model makes stuff up.&rdquo; In production, hallucination manifests as the agent confidently asserting facts that don&rsquo;t exist in its context window — not because the model is broken, but because it&rsquo;s operating outside its training distribution.</p>
<p>I&rsquo;ve watched an agent that correctly processed 47 out of 50 records, then at record 48 started returning fabricated data because the relevant instructions had scrolled out of the active context window. The model didn&rsquo;t tell me it was confused. It just started making things up with the same confidence it had for the first 47 records.</p>
<h3 id="2-edge-case-collapse-nulls-unicode-concurrency">2. Edge Case Collapse (Nulls, Unicode, Concurrency)</h3>
<p>Production data is messy. Null fields, unicode homoglyphs, concurrent requests, and malformed inputs that your test suite never generated will find their way into your agent&rsquo;s context window. I&rsquo;ve seen agents crash on a single null field in a JSON response — not because the tool returned an error, but because the model couldn&rsquo;t decide what to do with <code>null</code> and entered an infinite reasoning loop.</p>
<p>Unicode homoglyphs are particularly nasty. An attacker can replace a Latin &lsquo;a&rsquo; with a Cyrillic &lsquo;а&rsquo; (U+0430) — visually identical, semantically different. Your input validation passes, but the agent interprets the string differently. This is one of the 18 prompt injection attack vectors that bypassed PromptGuard with 100% confidence in a 2026 audit.</p>
<h3 id="3-prompt-injection-and-adversarial-inputs">3. Prompt Injection and Adversarial Inputs</h3>
<p>The January 2026 incident is the one everyone references now: a customer support agent received a carefully crafted message that included hidden instructions embedded in what appeared to be a legitimate support request. The agent processed a $47,000 fraudulent refund before anyone caught it.</p>
<p>The techniques are evolving fast. Attackers now use:</p>
<ul>
<li><strong>Unicode homoglyphs</strong> — visually identical characters with different byte representations</li>
<li><strong>Base64-encoded instructions</strong> — the agent decodes and follows them</li>
<li><strong>ROT13 and other simple ciphers</strong> — the agent &ldquo;figures out&rdquo; the hidden message</li>
<li><strong>Non-English languages</strong> — instructions in a language the agent handles but the monitoring system doesn&rsquo;t</li>
<li><strong>Multi-turn fragmentation</strong> — spreading the injection across multiple conversation turns to avoid detection</li>
</ul>
<p>The <a href="/posts/secure-ai-agents-least-privilege-2026/">secure AI agents guide</a> covers defense-in-depth approaches, but the reality is: no single guardrail catches everything. In a 2026 audit, 12 out of 18 prompt injection attack vectors bypassed PromptGuard with 100% confidence. You need layered defenses, not a silver bullet.</p>
<h3 id="4-context-limit-surprises">4. Context Limit Surprises</h3>
<p>This is the most insidious failure mode because it&rsquo;s invisible until it hits. An agent that works perfectly with a 10-turn conversation will start hallucinating at turn 50 because the context window is full of accumulated tool outputs, error messages, and intermediate results.</p>
<p>The model doesn&rsquo;t tell you it&rsquo;s confused. It just starts making things up. I&rsquo;ve seen agents that correctly processed 47 out of 50 records, then at record 48 started returning fabricated data because the relevant instructions had scrolled out of the active context window.</p>
<p>The fix is the <strong>stateless reducer pattern</strong> — instead of dumping everything into the context window, maintain a compact state representation and only inject what the agent needs for the current step. This is the same pattern Redux popularized for frontend state management, and it works just as well for agents.</p>
<h3 id="5-cascade-failures-in-multi-step-workflows">5. Cascade Failures in Multi-Step Workflows</h3>
<p>Every step in an agent&rsquo;s execution chain adds failure surface area. A 10-step agent where each step has 95% reliability has a 40% chance of completing without error. But the degradation isn&rsquo;t linear — I&rsquo;ve seen agents that handle steps 1-4 perfectly, then at step 5 the context window is cluttered with intermediate results, the model loses track of what it was doing, and it starts repeating steps or skipping critical operations.</p>
<p>This is especially bad in the &ldquo;here&rsquo;s your prompt, here&rsquo;s a bag of tools, loop until you hit the goal&rdquo; pattern that many agent frameworks default to. The <a href="/posts/open-source-agent-eval-harness-comparison-2026/">open-source agent eval harnesses</a> I&rsquo;ve tested show this pattern consistently: agents with Directed Graph (DAG) orchestrators like Airflow or Prefect significantly outperform monolithic loop-based agents on multi-step tasks, because the orchestration layer handles state management and retry logic instead of leaving it to the LLM.</p>
<h3 id="6-data-integration-drift">6. Data Integration Drift</h3>
<p>Your agent depends on external data sources — APIs, databases, file systems. These sources change over time. An API returns a new field, a database schema changes, a file format gets updated. The agent, trained on the old format, starts making incorrect assumptions.</p>
<p>I&rsquo;ve seen this manifest as an agent that reliably parsed CSV exports for months, then suddenly started failing because the export tool added a BOM header. The agent didn&rsquo;t fail gracefully — it silently misparsed the data and made decisions based on the wrong columns. Data integration drift is almost never tested because it requires maintaining test fixtures that evolve with the production data sources.</p>
<h3 id="7-authorization-confusion-in-multi-tenant-systems">7. Authorization Confusion in Multi-Tenant Systems</h3>
<p>When an agent operates across multiple tenants or user contexts, it can accidentally apply one user&rsquo;s permissions to another&rsquo;s data. This is especially dangerous in customer support agents that handle multiple accounts — the agent might read data from account A while operating under account B&rsquo;s authorization context.</p>
<p>The fix is <strong>least-privilege tool access</strong>: each tool call should carry its own authorization context, not inherit the agent&rsquo;s global permissions. The <a href="/posts/secure-ai-agents-least-privilege-2026/">secure AI agents guide</a> covers this in detail, but the short version is: treat every tool call as an independent authorization boundary.</p>
<h2 id="epistemic-distortion-the-meta-failure-mode">Epistemic Distortion: The Meta-Failure Mode</h2>
<p>Beyond the seven specific failure modes, there&rsquo;s a meta-pattern that researchers have documented across 1,400+ controlled experiments: <strong>epistemic distortion</strong>. This is the agent&rsquo;s tendency to silently drop conflicting instructions, apply wrong evidence standards, or selectively ignore parts of its system prompt.</p>
<p>In practice, epistemic distortion looks like:</p>
<ul>
<li>The agent follows the most recent instruction even when earlier instructions explicitly contradict it</li>
<li>The agent applies different reasoning standards to different types of inputs (treating user input as more authoritative than system prompts)</li>
<li>The agent &ldquo;forgets&rdquo; constraints that were established earlier in the conversation</li>
</ul>
<p>This isn&rsquo;t a bug — it&rsquo;s a consequence of how attention mechanisms work. The model weights all tokens in its context window, and newer or more salient tokens can override earlier instructions. The fix is structural: don&rsquo;t rely on the model to remember constraints. Encode them in the tool schemas, the orchestration layer, and the validation pipeline.</p>
<h2 id="the-12-factor-agent-architecture">The 12-Factor Agent Architecture</h2>
<p>The <a href="https://github.com/humanlayer/12-factor-agents">12-Factor Agents</a> methodology (which hit 475+ points on Hacker News for good reason) provides a solid foundation. Here are the principles I&rsquo;ve found most impactful in practice.</p>
<h3 id="own-your-prompts-and-context-window">Own Your Prompts and Context Window</h3>
<p>Don&rsquo;t let the framework manage your prompts. You need explicit control over:</p>
<ul>
<li><strong>System prompt versioning</strong> — every change is a new version, tracked in git</li>
<li><strong>Context window budgeting</strong> — know exactly how many tokens each component uses</li>
<li><strong>Prompt injection surface</strong> — user input should never be concatenated into system prompts</li>
</ul>
<p>I use a simple pattern: each agent step gets a fixed token budget. The system prompt gets 30%, the current state gets 20%, tool definitions get 30%, and user input gets 20%. If something exceeds its budget, it gets truncated — not the model&rsquo;s job to figure out what&rsquo;s important.</p>
<h3 id="tools-as-structured-outputs">Tools as Structured Outputs</h3>
<p>Treat tool definitions as JSON schemas with strict validation, not as natural language descriptions. This means:</p>
<ul>
<li>Every tool parameter has a type, description, and required/optional flag</li>
<li>Tool calls are validated against the schema before execution</li>
<li>Validation failures return structured error messages, not &ldquo;something went wrong&rdquo;</li>
</ul>
<p>This pattern, combined with the <a href="/posts/llm-structured-output-guide-2026/">LLM structured output</a> capabilities available in 2026, means you can catch tool selection errors before they cause damage. The agent might still pick the wrong tool, but it won&rsquo;t call it with invalid parameters.</p>
<h3 id="small-focused-agents-over-monolithic-loops">Small, Focused Agents over Monolithic Loops</h3>
<p>A single agent with 20 tools and a &ldquo;keep going until done&rdquo; instruction is a recipe for unpredictable behavior. Instead, decompose your workflow into small, focused agents:</p>
<ul>
<li><strong>One agent per task type</strong> — each with 3-5 tools max</li>
<li><strong>DAG orchestration</strong> — use an orchestrator to sequence agents, not the LLM</li>
<li><strong>Explicit handoffs</strong> — each agent produces a structured output that the next agent consumes</li>
</ul>
<p>I&rsquo;ve found that agents with 3-5 tools have significantly lower tool selection drift than agents with 10+ tools. The model has fewer options to confuse, and the tool descriptions can be more specific.</p>
<h3 id="stateless-reducer-pattern">Stateless Reducer Pattern</h3>
<p>Instead of passing the entire conversation history to every agent call, maintain a compact state object:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-json" data-lang="json"><span style="display:flex;"><span>{
</span></span><span style="display:flex;"><span>  <span style="color:#f92672">&#34;step&#34;</span>: <span style="color:#ae81ff">4</span>,
</span></span><span style="display:flex;"><span>  <span style="color:#f92672">&#34;completed_tasks&#34;</span>: [<span style="color:#e6db74">&#34;search&#34;</span>, <span style="color:#e6db74">&#34;validate&#34;</span>, <span style="color:#e6db74">&#34;transform&#34;</span>],
</span></span><span style="display:flex;"><span>  <span style="color:#f92672">&#34;current_input&#34;</span>: <span style="color:#e6db74">&#34;output_from_step_3.json&#34;</span>,
</span></span><span style="display:flex;"><span>  <span style="color:#f92672">&#34;errors&#34;</span>: [],
</span></span><span style="display:flex;"><span>  <span style="color:#f92672">&#34;accumulated_results&#34;</span>: [<span style="color:#e6db74">&#34;result_1&#34;</span>, <span style="color:#e6db74">&#34;result_2&#34;</span>]
</span></span><span style="display:flex;"><span>}
</span></span></code></pre></div><p>Each agent step reads the current state, produces an action, and returns a new state. The full conversation history is stored in an audit log for debugging, but the agent only sees what it needs for the current step. This dramatically reduces context window pressure and eliminates the &ldquo;forgot what it was doing&rdquo; failure mode.</p>
<h2 id="testing-strategies-that-actually-work">Testing Strategies That Actually Work</h2>
<p>If you take one thing from this guide, let it be this: <strong>stop testing agents like you test deterministic software.</strong> Agents are stochastic systems, and they need stochastic testing methodologies.</p>
<h3 id="multi-trial-testing-with-confidence-intervals">Multi-Trial Testing with Confidence Intervals</h3>
<p>Run every test scenario at least 30 times. Calculate:</p>
<ul>
<li><strong>Pass rate</strong> with Wilson confidence intervals (not raw percentages)</li>
<li><strong>Cost variance</strong> — how much does each run cost in tokens?</li>
<li><strong>Latency variance</strong> — how long does each run take?</li>
</ul>
<p>The <a href="https://github.com/alepot55/agentrial">agentrial</a> framework does this out of the box. You define a task, run it 100 times, and get back pass rates with confidence intervals, cost distributions, and latency distributions. Without this, you&rsquo;re making decisions based on anecdotes.</p>
<h3 id="chaos-engineering-for-agents-flakestorm-approach">Chaos Engineering for Agents (Flakestorm Approach)</h3>
<p><a href="https://github.com/flakestorm">Flakestorm</a> and similar tools apply chaos engineering principles to agent systems. The approach is simple: inject failures at the tool call level and observe how the agent recovers. Testing a LangChain agent with this approach revealed a <strong>95% failure rate on adversarial inputs</strong> — not because the LLM was bad, but because the agent had no recovery mechanisms.</p>
<p>The chaos engineering scenarios I&rsquo;ve found most valuable:</p>
<ul>
<li><strong>Inject API latency</strong> — test timeout handling. Most agents crash when a tool call takes 30 seconds instead of 2.</li>
<li><strong>Return malformed tool outputs</strong> — test error recovery. Can the agent retry, or does it enter an infinite loop?</li>
<li><strong>Drop intermediate results</strong> — test state persistence. Does the agent resume or start over?</li>
<li><strong>Rotate model versions mid-workflow</strong> — test compatibility. Does the agent handle a different model&rsquo;s output format?</li>
<li><strong>Adversarial inputs</strong> — test prompt injection resistance. Can the agent distinguish instructions from data?</li>
</ul>
<p>I&rsquo;ve found that most agents fail catastrophically under these conditions — not because the LLM is bad, but because the surrounding infrastructure doesn&rsquo;t handle failures gracefully. A well-designed agent should degrade, not crash.</p>
<h3 id="cascade-failure-testing">Cascade Failure Testing</h3>
<p>This is the one almost no one tests. To test cascade failures:</p>
<ol>
<li>Inject a bad intermediate result at step N</li>
<li>Observe whether the agent detects the error or propagates it</li>
<li>Measure how many steps the error propagates before detection</li>
<li>Test recovery: can the agent roll back to a known-good state?</li>
</ol>
<p>The Fisher exact test (used by agentrial) can identify which specific step has statistically significant failure rates. This is the difference between &ldquo;the agent fails sometimes&rdquo; and &ldquo;step 4 of the tool selection phase has a 30% failure rate when the input contains more than 5 items.&rdquo;</p>
<h3 id="context-window-monitoring">Context Window Monitoring</h3>
<p>Monitor context window utilization as a first-class metric. When utilization exceeds 70%, the risk of context limit surprises increases exponentially. Set up alerts for:</p>
<ul>
<li><strong>Token count per turn</strong> — sudden increases indicate context window bloat</li>
<li><strong>Instruction proximity</strong> — how far back are the original instructions?</li>
<li><strong>Tool output size</strong> — are intermediate results consuming too much context?</li>
</ul>
<h2 id="infrastructure-for-resilient-agents">Infrastructure for Resilient Agents</h2>
<p>The testing patterns above tell you where your agent breaks. The infrastructure patterns below keep it running anyway.</p>
<h3 id="durable-execution-and-state-persistence">Durable Execution and State Persistence</h3>
<p><a href="https://github.com/orra-dev/orra">Orra</a> and similar tools provide durable execution for AI agent workflows. The key idea: if an agent crashes mid-step, it should resume from where it left off, not start over. This requires:</p>
<ul>
<li><strong>State persistence</strong> — every intermediate result is saved to durable storage</li>
<li><strong>Idempotent tool calls</strong> — running the same tool with the same input twice produces the same result</li>
<li><strong>Automatic retry with backoff</strong> — transient failures don&rsquo;t kill the workflow</li>
</ul>
<p>The <a href="/posts/openai-agents-sdk-temporal-integration-2026/">OpenAI Agents SDK Temporal integration</a> is a good example of this pattern in practice — Temporal provides the durable execution layer while the agent SDK handles the LLM interactions.</p>
<h3 id="pre-validated-execution-plans">Pre-Validated Execution Plans</h3>
<p>Before an agent executes anything, validate the entire plan against your constraints:</p>
<ul>
<li>Are all required tools available?</li>
<li>Do the tool parameters match the schemas?</li>
<li>Is the estimated cost within budget?</li>
<li>Are there any circular dependencies in the plan?</li>
</ul>
<p>This catches the &ldquo;agent decided to do something unexpected&rdquo; failure mode before it costs you money or corrupts data. Orra&rsquo;s pre-validated execution plans are a reference implementation of this pattern.</p>
<h3 id="automatic-health-monitoring-and-audit-logs">Automatic Health Monitoring and Audit Logs</h3>
<p>You need observability at two levels:</p>
<ol>
<li><strong>Agent-level</strong> — what did the agent decide, what tools did it call, what was the outcome?</li>
<li><strong>System-level</strong> — what&rsquo;s the pass rate trend, cost trend, latency trend?</li>
</ol>
<p>The <a href="/posts/llm-observability-tools-comparison-2026/">LLM observability tools</a> (LangSmith, Langfuse, Helicone, Arize) all support agent tracing now. The key metric I track is <strong>pass rate over a rolling 24-hour window</strong> — if it drops below 80%, I want to know immediately, not when a user complains.</p>
<h2 id="building-a-production-reliability-checklist">Building a Production Reliability Checklist</h2>
<p>Before you deploy an agent to production, run through this checklist:</p>
<p><strong>Testing (pre-deployment)</strong></p>
<ul>
<li><input disabled="" type="checkbox"> Multi-trial testing: 100 runs per scenario with confidence intervals</li>
<li><input disabled="" type="checkbox"> Chaos engineering: injected failures at tool call level</li>
<li><input disabled="" type="checkbox"> Adversarial eval: prompt injection attack vectors tested</li>
<li><input disabled="" type="checkbox"> Cascade failure testing: bad intermediate results injected</li>
<li><input disabled="" type="checkbox"> Edge case coverage: nulls, unicode, concurrent requests</li>
</ul>
<p><strong>Architecture</strong></p>
<ul>
<li><input disabled="" type="checkbox"> Stateless reducer pattern (not full conversation history in context)</li>
<li><input disabled="" type="checkbox"> Small, focused agents (3-5 tools per agent)</li>
<li><input disabled="" type="checkbox"> DAG orchestration (not monolithic loops)</li>
<li><input disabled="" type="checkbox"> Structured tool definitions with JSON schema validation</li>
<li><input disabled="" type="checkbox"> Pre-validated execution plans</li>
</ul>
<p><strong>Infrastructure</strong></p>
<ul>
<li><input disabled="" type="checkbox"> Durable execution with state persistence</li>
<li><input disabled="" type="checkbox"> Action deduplication (idempotent tool calls)</li>
<li><input disabled="" type="checkbox"> Human-in-the-loop approval gates for destructive operations</li>
<li><input disabled="" type="checkbox"> Least-privilege tool access with per-call authorization</li>
</ul>
<p><strong>Monitoring</strong></p>
<ul>
<li><input disabled="" type="checkbox"> Rolling 24-hour pass rate with alerts</li>
<li><input disabled="" type="checkbox"> Context window utilization tracking</li>
<li><input disabled="" type="checkbox"> Cost anomaly detection</li>
<li><input disabled="" type="checkbox"> Tool call distribution monitoring</li>
<li><input disabled="" type="checkbox"> Audit log with full conversation history</li>
</ul>
<h2 id="from-demo-to-production-confidence">From Demo to Production Confidence</h2>
<p>The 40%+ failure rate for AI agent projects isn&rsquo;t because agents are fundamentally unreliable. It&rsquo;s because most teams treat agents like deterministic software and are surprised when stochastic behavior emerges.</p>
<p>The path to production confidence is straightforward:</p>
<ol>
<li><strong>Test statistically</strong> — run 100 trials, not 5 demos</li>
<li><strong>Architect for failure</strong> — DAG orchestrators, stateless reducers, durable execution</li>
<li><strong>Monitor continuously</strong> — rolling pass rates, cost trends, latency distributions</li>
<li><strong>Gate destructively</strong> — human approval for high-risk operations</li>
<li><strong>Test adversarially</strong> — prompt injection, chaos engineering, cascade failures</li>
</ol>
<p>I&rsquo;ve been running production agents for over a year now, and the ones that survive are the ones built with these patterns from day one. The ones that fail are the ones that looked perfect in demos and fell apart under real load.</p>
<p>Your agent isn&rsquo;t broken. Your testing methodology is. Fix that first, and the rest follows.</p>
]]></content:encoded></item></channel></rss>