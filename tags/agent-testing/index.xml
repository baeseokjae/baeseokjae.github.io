<?xml version="1.0" encoding="utf-8" standalone="yes"?><rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom" xmlns:content="http://purl.org/rss/1.0/modules/content/"><channel><title>Agent Testing on RockB</title><link>https://baeseokjae.github.io/tags/agent-testing/</link><description>Recent content in Agent Testing on RockB</description><image><title>RockB</title><url>https://baeseokjae.github.io/images/og-default.png</url><link>https://baeseokjae.github.io/images/og-default.png</link></image><generator>Hugo</generator><language>en-us</language><lastBuildDate>Fri, 17 Jul 2026 13:06:50 +0000</lastBuildDate><atom:link href="https://baeseokjae.github.io/tags/agent-testing/index.xml" rel="self" type="application/rss+xml"/><item><title>ASSERT Agent Evaluation Harness 2026 Review: Requirement-Driven Testing for AI Agents</title><link>https://baeseokjae.github.io/posts/assert-agent-evaluation-harness-2026/</link><pubDate>Fri, 17 Jul 2026 13:06:50 +0000</pubDate><guid>https://baeseokjae.github.io/posts/assert-agent-evaluation-harness-2026/</guid><description>ASSERT is Microsoft&amp;#39;s open-source, requirement-driven evaluation harness that turns agent policies into executable tests across any framework.</description><content:encoded><![CDATA[<p>ASSERT (Adaptive Spec-driven Scoring for Evaluation and Regression Testing) is an open-source, requirement-driven evaluation harness for AI agents and LLM applications, announced at Microsoft Build 2026. Built by Microsoft&rsquo;s Responsible AI organization and released under the MIT license, ASSERT transforms natural-language agent policies into executable test suites through a four-stage pipeline — systematize, test set, inference, and judge — making it the first framework to bridge the gap between product requirements and automated test coverage for AI agents.</p>
<h2 id="what-is-assert--adaptive-spec-driven-scoring-for-evaluation-and-regression-testing">What Is ASSERT? — Adaptive Spec-driven Scoring for Evaluation and Regression Testing</h2>
<p>ASSERT is a Python-based evaluation framework (compatible with Python 3.11 through 3.13) that lets development teams define what their AI agents should and should not do in natural language, then automatically generates test scenarios and scores agent behavior against those requirements. Unlike traditional LLM benchmarks such as MMLU or HumanEval that test general knowledge, ASSERT evaluates agents against your specific policies — the rules, constraints, and behavioral expectations that define acceptable agent behavior in your particular use case.</p>
<p>The project was created on April 30, 2026, and as of July 17, 2026, has accumulated 196 stars and 27 forks on GitHub. It is built on the research methodology described in Agarwal et al. (2026) &ldquo;AI-Assisted Systematization for Evaluating GenAI Systems,&rdquo; published by Microsoft Research. The framework is framework-agnostic, supporting major agent orchestration tools including LangChain, CrewAI, OpenAI Agents SDK, DSPy, LlamaIndex, and AutoGen, and connects to over 100 model endpoints through its LiteLLM integration.</p>
<h2 id="the-open-trust-stack-assert-acs-and-openinference">The Open Trust Stack: ASSERT, ACS, and OpenInference</h2>
<p>ASSERT does not exist in isolation. It is one component of Microsoft&rsquo;s broader <strong>Open Trust Stack</strong>, a coordinated set of open-source tools announced at Build 2026 that together provide end-to-end governance for AI agents. The stack consists of three complementary layers:</p>
<table>
	<thead>
			<tr>
					<th>Component</th>
					<th>Role</th>
					<th>Stage</th>
			</tr>
	</thead>
	<tbody>
			<tr>
					<td><strong>ASSERT</strong></td>
					<td>Pre-deployment evaluation</td>
					<td>Before release</td>
			</tr>
			<tr>
					<td><strong>ACS (Agent Control Specification)</strong></td>
					<td>Runtime safety enforcement</td>
					<td>During execution</td>
			</tr>
			<tr>
					<td><strong>OpenInference</strong></td>
					<td>Observability and trace capture</td>
					<td>Throughout lifecycle</td>
			</tr>
	</tbody>
</table>
<p>ASSERT evaluates whether an agent meets policy requirements before it reaches production. ACS, the Agent Control Specification, defines eight control points in the agent lifecycle — from tool invocation to memory access — where runtime policies can be enforced deterministically. OpenInference, built on the OpenTelemetry standard, provides the observability layer that both ASSERT and ACS rely on for trace-grounded decision-making. Together, they form a complete governance loop: evaluate with ASSERT, deploy with ACS controls, monitor with OpenInference, and re-evaluate when policies change.</p>
<h2 id="how-assert-works-the-4-stage-pipeline">How ASSERT Works: The 4-Stage Pipeline</h2>
<p>ASSERT&rsquo;s architecture is organized around four distinct pipeline stages, each producing artifacts that can be cached and reused independently. This design makes the framework practical for CI/CD integration, where changing only the inference target (e.g., testing a new model version) reuses the systematization and test set stages without recomputation.</p>
<h3 id="stage-1--systematize-from-policy-to-taxonomy">Stage 1 — Systematize: From Policy to Taxonomy</h3>
<p>The systematize stage takes natural-language policy documents — your agent&rsquo;s behavioral requirements, safety rules, and domain constraints — and produces a structured taxonomy of evaluation dimensions. This is where the &ldquo;AI-assisted systematization&rdquo; research methodology comes into play: instead of manually enumerating every possible test scenario, ASSERT uses an LLM to help decompose high-level policies into granular, testable dimensions. The output is a JSON taxonomy file that defines what aspects of agent behavior need to be measured.</p>
<h3 id="stage-2--test-set-generating-scenarios">Stage 2 — Test Set: Generating Scenarios</h3>
<p>Using the taxonomy from Stage 1, the test set stage generates concrete test scenarios. Each scenario is a structured prompt or instruction that exercises a specific policy dimension. For example, if your policy states &ldquo;the agent must not execute financial transactions without explicit user confirmation,&rdquo; the test set stage generates scenarios where the agent is asked to perform financial actions under varying levels of user consent. These scenarios are stored as JSONL artifacts, making them inspectable, version-controllable, and reusable across evaluation runs.</p>
<h3 id="stage-3--inference-running-the-agent">Stage 3 — Inference: Running the Agent</h3>
<p>The inference stage executes the agent against each test scenario from Stage 2. ASSERT is framework-agnostic at this stage — it provides a standardized interface that wraps any supported agent framework. The agent&rsquo;s responses, tool calls, reasoning traces, and intermediate steps are all captured. Crucially, ASSERT integrates with OpenInference to capture OpenTelemetry traces during execution, providing a complete record of the agent&rsquo;s decision-making process for later judgment.</p>
<h3 id="stage-4--judge-scoring-with-llm-verdicts">Stage 4 — Judge: Scoring with LLM Verdicts</h3>
<p>The judge stage evaluates the agent&rsquo;s performance against the original policy requirements. Unlike simple pass/fail checks, ASSERT uses LLM-based judges that examine the full trace — including intermediate reasoning, tool calls, and final outputs — to determine whether the agent complied with each policy dimension. The judgment is grounded in the actual execution trace captured via OpenTelemetry, which distinguishes ASSERT from black-box evaluation tools that only see final outputs. Results are scored and stored as local JSON artifacts, and the bundled local viewer enables side-by-side comparison of different runs.</p>
<h2 id="key-features-and-differentiators">Key Features and Differentiators</h2>
<h3 id="spec-driven-coverage-vs-generic-benchmarks">Spec-Driven Coverage vs Generic Benchmarks</h3>
<p>The most significant differentiator of ASSERT is its requirement-driven approach. Traditional LLM benchmarks measure general capabilities — how well does the model answer factual questions, solve coding problems, or reason about common-sense scenarios. ASSERT measures compliance with your specific policies. This shift from generic capability testing to requirement-driven evaluation is critical for production AI agents, where the question is not &ldquo;is this agent smart?&rdquo; but &ldquo;does this agent behave correctly in my specific domain?&rdquo;</p>
<h3 id="framework-agnostic-architecture">Framework-Agnostic Architecture</h3>
<p>ASSERT does not lock you into a specific agent framework or cloud provider. It supports LangChain, CrewAI, OpenAI Agents SDK, DSPy, LlamaIndex, and AutoGen out of the box, and its modular architecture makes it straightforward to add support for additional frameworks. This framework-agnostic design is a deliberate choice — Microsoft has positioned ASSERT as an industry-wide tool, not a vendor lock-in play. As confirmed by community analysis, ASSERT is not tied to Azure or Microsoft Foundry.</p>
<h3 id="trace-grounded-judgment-via-opentelemetry">Trace-Grounded Judgment via OpenTelemetry</h3>
<p>ASSERT&rsquo;s integration with OpenInference and OpenTelemetry enables trace-grounded judgment. Instead of evaluating only the final output of an agent, ASSERT examines the full execution trace — every tool call, every reasoning step, every intermediate decision. This is particularly important for multi-step agent tasks where the final answer may be correct but the path taken to reach it violates policy. OpenInference auto-instruments over 33 agent frameworks in just two lines of code, making trace capture nearly effortless.</p>
<h3 id="local-first-artifacts-and-viewer">Local-First Artifacts and Viewer</h3>
<p>All ASSERT artifacts — taxonomies, test scenarios, inference outputs, and judgment scores — are stored as local JSON and JSONL files. This local-first approach means teams can version-control their evaluation artifacts, reproduce results, and audit the evaluation process without depending on external services. The bundled local viewer provides a web-based interface for comparing runs side by side, making it easy to track regressions across agent versions.</p>
<h2 id="assert-vs-alternatives-vally-msbench-bloom-petri">ASSERT vs Alternatives: Vally, MSBench, Bloom, Petri</h2>
<p>The AI agent evaluation landscape includes several other tools, each with different strengths. Here is how ASSERT compares:</p>
<table>
	<thead>
			<tr>
					<th>Tool</th>
					<th>Approach</th>
					<th>Best For</th>
					<th>Key Limitation</th>
			</tr>
	</thead>
	<tbody>
			<tr>
					<td><strong>ASSERT</strong></td>
					<td>Requirement-driven, 4-stage pipeline</td>
					<td>Policy compliance testing, CI integration</td>
					<td>Newer ecosystem, smaller community</td>
			</tr>
			<tr>
					<td><strong>Vally</strong></td>
					<td>Local-first, deterministic graders</td>
					<td>Fast iteration, reproducible scoring</td>
					<td>Less suited for complex multi-step scenarios</td>
			</tr>
			<tr>
					<td><strong>MSBench</strong></td>
					<td>At-scale regression testing</td>
					<td>Large-scale model regression suites</td>
					<td>Requires Microsoft infrastructure</td>
			</tr>
			<tr>
					<td><strong>Bloom</strong></td>
					<td>Behavioral simulation</td>
					<td>Testing edge cases and adversarial inputs</td>
					<td>Less structured policy mapping</td>
			</tr>
			<tr>
					<td><strong>Petri</strong></td>
					<td>Petri-net based workflow testing</td>
					<td>Complex multi-agent orchestration</td>
					<td>Steeper learning curve</td>
			</tr>
	</tbody>
</table>
<p>ASSERT fills a unique niche: it is the only framework that starts from natural-language policies and automatically generates a structured test suite. Vally excels at fast, deterministic scoring but lacks ASSERT&rsquo;s scenario generation capability. MSBench is designed for large-scale regression but is more tightly coupled to Microsoft&rsquo;s ecosystem. For teams that need to translate product requirements into test coverage without writing hundreds of manual test cases, ASSERT offers the most direct path.</p>
<h2 id="ecosystem-and-partner-integrations">Ecosystem and Partner Integrations</h2>
<p>ASSERT launched with a broad set of ecosystem partners, reflecting Microsoft&rsquo;s strategy of building an open industry standard rather than a proprietary tool. Launch partners include:</p>
<ul>
<li><strong>CrewAI</strong> — Multi-agent orchestration framework</li>
<li><strong>Arize AI</strong> — ML observability and monitoring</li>
<li><strong>LiteLLM</strong> — Unified model endpoint interface (100+ models)</li>
<li><strong>Pipecat</strong> — Voice agent framework</li>
<li><strong>Pydantic</strong> — Data validation and schema management</li>
<li><strong>HoneyHive</strong> — LLM evaluation and tracing</li>
</ul>
<p>The integration with LiteLLM is particularly important — it means ASSERT can evaluate agents running on any of 100+ model endpoints without custom adapter code. The OpenInference integration, which auto-instruments 33+ agent frameworks, ensures that trace capture works across the entire agent ecosystem with minimal configuration.</p>
<h2 id="getting-started-with-assert">Getting Started with ASSERT</h2>
<p>To start using ASSERT, install the package via pip:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-bash" data-lang="bash"><span style="display:flex;"><span>pip install assert-ai
</span></span></code></pre></div><p>The package requires Python 3.11 or later. The basic workflow involves:</p>
<ol>
<li><strong>Define your policies</strong> — Write natural-language policy documents describing what your agent should and should not do.</li>
<li><strong>Run the systematize stage</strong> — <code>assert systematize --policies policies.md</code> to generate a taxonomy.</li>
<li><strong>Generate test scenarios</strong> — <code>assert test-set --taxonomy taxonomy.json</code> to produce test cases.</li>
<li><strong>Run inference</strong> — <code>assert infer --test-set scenarios.jsonl --agent your-agent-config.yaml</code> to execute tests.</li>
<li><strong>Judge results</strong> — <code>assert judge --inference-results results.jsonl --taxonomy taxonomy.json</code> to score performance.</li>
<li><strong>View results</strong> — Launch the local viewer with <code>assert view</code> to compare runs.</li>
</ol>
<p>The artifact caching system means that if you only change the agent configuration (e.g., switching from GPT-4 to Claude 4), stages 1 and 2 are skipped automatically, and only inference and judgment are re-executed.</p>
<h2 id="limitations-and-considerations">Limitations and Considerations</h2>
<p>While ASSERT represents a significant advance in agent evaluation, it has several limitations worth considering:</p>
<p><strong>New and rapidly evolving.</strong> The project was created in April 2026 and is still in its early stages. With 196 stars and 27 forks, the community is small compared to established tools. Teams adopting ASSERT should expect API changes and should pin their version.</p>
<p><strong>LLM-dependent systematization.</strong> The AI-assisted systematization stage relies on an LLM to decompose policies into taxonomies. This introduces a meta-evaluation question: how do you verify that the systematization itself is correct? The quality of your test suite depends on the quality of the LLM used in Stage 1.</p>
<p><strong>No built-in runtime enforcement.</strong> ASSERT is an evaluation tool, not a runtime guard. It tells you whether your agent passes policy checks, but it cannot prevent policy violations during live operation. This is where ACS (Agent Control Specification) fills the gap — but ACS is a separate tool that requires its own integration.</p>
<p><strong>Trace dependency.</strong> The trace-grounded judgment feature depends on OpenInference and OpenTelemetry instrumentation. If your agent framework is not among the 33+ auto-instrumented frameworks, or if you disable tracing for performance reasons, the judge stage loses visibility into intermediate steps.</p>
<h2 id="conclusion-is-assert-ready-for-production">Conclusion: Is ASSERT Ready for Production?</h2>
<p>ASSERT is ready for teams that are building production AI agents and need a structured, repeatable way to verify policy compliance. The framework&rsquo;s requirement-driven approach addresses a genuine gap in the AI agent development lifecycle — the disconnect between product requirements and test coverage. The 4-stage pipeline with artifact caching is well-designed for CI/CD integration, and the framework-agnostic architecture means it can be adopted incrementally alongside existing tools.</p>
<p>For teams already using LangChain, CrewAI, or OpenAI Agents SDK, ASSERT provides immediate value as a policy compliance layer. The combination of ASSERT for pre-deployment evaluation and ACS for runtime enforcement creates a governance loop that is currently unmatched in the open-source ecosystem. However, teams should be prepared for the framework&rsquo;s early-stage maturity — expect API changes, contribute feedback, and pin versions in production pipelines.</p>
<p>ASSERT is not a replacement for general capability benchmarks or for runtime monitoring tools. But as a requirement-driven evaluation harness that turns natural-language policies into executable tests, it fills a critical role that no other open-source tool currently addresses. For AI engineering teams that take agent governance seriously, ASSERT deserves a place in the evaluation stack.</p>
<h2 id="frequently-asked-questions">Frequently Asked Questions</h2>
<p><strong>What is ASSERT in the context of AI agent evaluation?</strong>
ASSERT (Adaptive Spec-driven Scoring for Evaluation and Regression Testing) is an open-source framework by Microsoft that converts natural-language agent policies into executable test suites through a four-stage pipeline: systematize, test set, inference, and judge.</p>
<p><strong>How does ASSERT differ from traditional LLM benchmarks like MMLU?</strong>
Traditional benchmarks measure general knowledge and reasoning capabilities, while ASSERT evaluates agents against your specific policies and requirements. It tests whether an agent behaves correctly in your domain, not whether it answers general knowledge questions correctly.</p>
<p><strong>Is ASSERT tied to Microsoft Azure or Microsoft Foundry?</strong>
No. ASSERT is framework-agnostic and not tied to any cloud provider. It supports multiple agent frameworks including LangChain, CrewAI, OpenAI Agents SDK, DSPy, LlamaIndex, and AutoGen, and works with over 100 model endpoints through LiteLLM.</p>
<p><strong>What is the relationship between ASSERT and ACS?</strong>
ASSERT handles pre-deployment evaluation — checking whether an agent meets policy requirements before release. ACS (Agent Control Specification) handles runtime enforcement — preventing policy violations during live operation. Together they form a complete governance loop.</p>
<p><strong>How do I get started with ASSERT?</strong>
Install the package with <code>pip install assert-ai</code> (requires Python 3.11+), define your policies in natural language, then run the four pipeline stages: systematize, test-set, infer, and judge. Results are stored as local JSON artifacts viewable with the bundled local viewer.</p>
]]></content:encoded></item><item><title>What Breaks an AI Agent After 50 Clean Demos: Production Reliability Guide (2026)</title><link>https://baeseokjae.github.io/posts/ai-agent-50-clean-demos-break-2026/</link><pubDate>Tue, 14 Jul 2026 12:00:00 +0000</pubDate><guid>https://baeseokjae.github.io/posts/ai-agent-50-clean-demos-break-2026/</guid><description>Why AI agents that pass 50 demos fail in production — the 7 failure modes, the $47K prompt injection case study, and how to build production-grade reliability with chaos engineering, 12-factor architecture, and multi-trial testing.</description><content:encoded><![CDATA[<p>You demo an AI agent to your team. Fifty runs, zero failures. Everyone&rsquo;s impressed. You deploy to production. Within a week, it&rsquo;s hallucinating tool calls, getting stuck in loops, and your Slack is full of &ldquo;the agent did something weird&rdquo; messages.</p>
<p>I&rsquo;ve been there. Multiple times. And I&rsquo;ve spent the last year digging into why this happens and what actually works to fix it.</p>
<p>The short answer: <strong>your agent isn&rsquo;t broken — your testing methodology is.</strong> Single-digit demos and pass/fail judgments hide a massive variance problem that only emerges under statistical scrutiny. Gartner predicts over 40% of AI agent projects will fail by 2027, and in January 2026, a prompt injection in a customer support agent processed a $47,000 fraudulent refund. These aren&rsquo;t edge cases — they&rsquo;re systematic failures that most teams aren&rsquo;t testing for.</p>
<p>This guide covers the seven failure modes that kill production agents, the architectural patterns that survive them, and the testing infrastructure you need before you can trust an agent in production.</p>
<h2 id="the-50-demo-illusion">The 50-Demo Illusion</h2>
<p>Here&rsquo;s a number that should scare you: LLMs show up to <strong>72% variance across runs even at temperature=0</strong>. This isn&rsquo;t a bug in one model — it&rsquo;s a fundamental property of how transformer-based models sample tokens. The <a href="/posts/llm-benchmark-variance-2026/">LLM benchmark variance problem</a> is well-documented, and it applies doubly to agentic workflows where a single token change in a tool selection can cascade into a completely different execution path.</p>
<p>The <a href="https://github.com/alepot55/agentrial">agentrial</a> project ran agents 100 times on the same task and found that pass rates dropped to <strong>60-80%</strong> despite 90%+ accuracy in single-run demos. That&rsquo;s not a 10% degradation — it&rsquo;s a 20-40 percentage point drop that only appears when you run enough trials to get statistically meaningful data.</p>
<p>In practice, this means:</p>
<ul>
<li><strong>A single &ldquo;pass&rdquo; tells you nothing.</strong> You need at least 30-50 runs to establish a baseline pass rate.</li>
<li><strong>Temperature=0 is not deterministic.</strong> It reduces variance but doesn&rsquo;t eliminate it. The model still samples from a probability distribution — it just picks the most likely token every time. But &ldquo;most likely&rdquo; changes based on floating-point precision, prompt formatting, and context window state.</li>
<li><strong>Variance compounds across steps.</strong> A 5-step agent where each step has 90% reliability has a theoretical pass rate of 0.9⁵ = 59%. Run that 100 times and you&rsquo;ll see failures in 40% of trials.</li>
</ul>
<h2 id="the-7-failure-modes-every-production-agent-faces">The 7 Failure Modes Every Production Agent Faces</h2>
<p>Most teams test hallucination and prompt injection. Almost no one systematically tests cascade failures, context limit drift, or data integration drift before shipping. Here are the seven failure modes I&rsquo;ve seen kill production agents in practice.</p>
<h3 id="1-hallucination-under-unexpected-inputs">1. Hallucination Under Unexpected Inputs</h3>
<p>This is the classic failure mode, but it&rsquo;s more nuanced than &ldquo;the model makes stuff up.&rdquo; In production, hallucination manifests as the agent confidently asserting facts that don&rsquo;t exist in its context window — not because the model is broken, but because it&rsquo;s operating outside its training distribution.</p>
<p>I&rsquo;ve watched an agent that correctly processed 47 out of 50 records, then at record 48 started returning fabricated data because the relevant instructions had scrolled out of the active context window. The model didn&rsquo;t tell me it was confused. It just started making things up with the same confidence it had for the first 47 records.</p>
<h3 id="2-edge-case-collapse-nulls-unicode-concurrency">2. Edge Case Collapse (Nulls, Unicode, Concurrency)</h3>
<p>Production data is messy. Null fields, unicode homoglyphs, concurrent requests, and malformed inputs that your test suite never generated will find their way into your agent&rsquo;s context window. I&rsquo;ve seen agents crash on a single null field in a JSON response — not because the tool returned an error, but because the model couldn&rsquo;t decide what to do with <code>null</code> and entered an infinite reasoning loop.</p>
<p>Unicode homoglyphs are particularly nasty. An attacker can replace a Latin &lsquo;a&rsquo; with a Cyrillic &lsquo;а&rsquo; (U+0430) — visually identical, semantically different. Your input validation passes, but the agent interprets the string differently. This is one of the 18 prompt injection attack vectors that bypassed PromptGuard with 100% confidence in a 2026 audit.</p>
<h3 id="3-prompt-injection-and-adversarial-inputs">3. Prompt Injection and Adversarial Inputs</h3>
<p>The January 2026 incident is the one everyone references now: a customer support agent received a carefully crafted message that included hidden instructions embedded in what appeared to be a legitimate support request. The agent processed a $47,000 fraudulent refund before anyone caught it.</p>
<p>The techniques are evolving fast. Attackers now use:</p>
<ul>
<li><strong>Unicode homoglyphs</strong> — visually identical characters with different byte representations</li>
<li><strong>Base64-encoded instructions</strong> — the agent decodes and follows them</li>
<li><strong>ROT13 and other simple ciphers</strong> — the agent &ldquo;figures out&rdquo; the hidden message</li>
<li><strong>Non-English languages</strong> — instructions in a language the agent handles but the monitoring system doesn&rsquo;t</li>
<li><strong>Multi-turn fragmentation</strong> — spreading the injection across multiple conversation turns to avoid detection</li>
</ul>
<p>The <a href="/posts/secure-ai-agents-least-privilege-2026/">secure AI agents guide</a> covers defense-in-depth approaches, but the reality is: no single guardrail catches everything. In a 2026 audit, 12 out of 18 prompt injection attack vectors bypassed PromptGuard with 100% confidence. You need layered defenses, not a silver bullet.</p>
<h3 id="4-context-limit-surprises">4. Context Limit Surprises</h3>
<p>This is the most insidious failure mode because it&rsquo;s invisible until it hits. An agent that works perfectly with a 10-turn conversation will start hallucinating at turn 50 because the context window is full of accumulated tool outputs, error messages, and intermediate results.</p>
<p>The model doesn&rsquo;t tell you it&rsquo;s confused. It just starts making things up. I&rsquo;ve seen agents that correctly processed 47 out of 50 records, then at record 48 started returning fabricated data because the relevant instructions had scrolled out of the active context window.</p>
<p>The fix is the <strong>stateless reducer pattern</strong> — instead of dumping everything into the context window, maintain a compact state representation and only inject what the agent needs for the current step. This is the same pattern Redux popularized for frontend state management, and it works just as well for agents.</p>
<h3 id="5-cascade-failures-in-multi-step-workflows">5. Cascade Failures in Multi-Step Workflows</h3>
<p>Every step in an agent&rsquo;s execution chain adds failure surface area. A 10-step agent where each step has 95% reliability has a 40% chance of completing without error. But the degradation isn&rsquo;t linear — I&rsquo;ve seen agents that handle steps 1-4 perfectly, then at step 5 the context window is cluttered with intermediate results, the model loses track of what it was doing, and it starts repeating steps or skipping critical operations.</p>
<p>This is especially bad in the &ldquo;here&rsquo;s your prompt, here&rsquo;s a bag of tools, loop until you hit the goal&rdquo; pattern that many agent frameworks default to. The <a href="/posts/open-source-agent-eval-harness-comparison-2026/">open-source agent eval harnesses</a> I&rsquo;ve tested show this pattern consistently: agents with Directed Graph (DAG) orchestrators like Airflow or Prefect significantly outperform monolithic loop-based agents on multi-step tasks, because the orchestration layer handles state management and retry logic instead of leaving it to the LLM.</p>
<h3 id="6-data-integration-drift">6. Data Integration Drift</h3>
<p>Your agent depends on external data sources — APIs, databases, file systems. These sources change over time. An API returns a new field, a database schema changes, a file format gets updated. The agent, trained on the old format, starts making incorrect assumptions.</p>
<p>I&rsquo;ve seen this manifest as an agent that reliably parsed CSV exports for months, then suddenly started failing because the export tool added a BOM header. The agent didn&rsquo;t fail gracefully — it silently misparsed the data and made decisions based on the wrong columns. Data integration drift is almost never tested because it requires maintaining test fixtures that evolve with the production data sources.</p>
<h3 id="7-authorization-confusion-in-multi-tenant-systems">7. Authorization Confusion in Multi-Tenant Systems</h3>
<p>When an agent operates across multiple tenants or user contexts, it can accidentally apply one user&rsquo;s permissions to another&rsquo;s data. This is especially dangerous in customer support agents that handle multiple accounts — the agent might read data from account A while operating under account B&rsquo;s authorization context.</p>
<p>The fix is <strong>least-privilege tool access</strong>: each tool call should carry its own authorization context, not inherit the agent&rsquo;s global permissions. The <a href="/posts/secure-ai-agents-least-privilege-2026/">secure AI agents guide</a> covers this in detail, but the short version is: treat every tool call as an independent authorization boundary.</p>
<h2 id="epistemic-distortion-the-meta-failure-mode">Epistemic Distortion: The Meta-Failure Mode</h2>
<p>Beyond the seven specific failure modes, there&rsquo;s a meta-pattern that researchers have documented across 1,400+ controlled experiments: <strong>epistemic distortion</strong>. This is the agent&rsquo;s tendency to silently drop conflicting instructions, apply wrong evidence standards, or selectively ignore parts of its system prompt.</p>
<p>In practice, epistemic distortion looks like:</p>
<ul>
<li>The agent follows the most recent instruction even when earlier instructions explicitly contradict it</li>
<li>The agent applies different reasoning standards to different types of inputs (treating user input as more authoritative than system prompts)</li>
<li>The agent &ldquo;forgets&rdquo; constraints that were established earlier in the conversation</li>
</ul>
<p>This isn&rsquo;t a bug — it&rsquo;s a consequence of how attention mechanisms work. The model weights all tokens in its context window, and newer or more salient tokens can override earlier instructions. The fix is structural: don&rsquo;t rely on the model to remember constraints. Encode them in the tool schemas, the orchestration layer, and the validation pipeline.</p>
<h2 id="the-12-factor-agent-architecture">The 12-Factor Agent Architecture</h2>
<p>The <a href="https://github.com/humanlayer/12-factor-agents">12-Factor Agents</a> methodology (which hit 475+ points on Hacker News for good reason) provides a solid foundation. Here are the principles I&rsquo;ve found most impactful in practice.</p>
<h3 id="own-your-prompts-and-context-window">Own Your Prompts and Context Window</h3>
<p>Don&rsquo;t let the framework manage your prompts. You need explicit control over:</p>
<ul>
<li><strong>System prompt versioning</strong> — every change is a new version, tracked in git</li>
<li><strong>Context window budgeting</strong> — know exactly how many tokens each component uses</li>
<li><strong>Prompt injection surface</strong> — user input should never be concatenated into system prompts</li>
</ul>
<p>I use a simple pattern: each agent step gets a fixed token budget. The system prompt gets 30%, the current state gets 20%, tool definitions get 30%, and user input gets 20%. If something exceeds its budget, it gets truncated — not the model&rsquo;s job to figure out what&rsquo;s important.</p>
<h3 id="tools-as-structured-outputs">Tools as Structured Outputs</h3>
<p>Treat tool definitions as JSON schemas with strict validation, not as natural language descriptions. This means:</p>
<ul>
<li>Every tool parameter has a type, description, and required/optional flag</li>
<li>Tool calls are validated against the schema before execution</li>
<li>Validation failures return structured error messages, not &ldquo;something went wrong&rdquo;</li>
</ul>
<p>This pattern, combined with the <a href="/posts/llm-structured-output-guide-2026/">LLM structured output</a> capabilities available in 2026, means you can catch tool selection errors before they cause damage. The agent might still pick the wrong tool, but it won&rsquo;t call it with invalid parameters.</p>
<h3 id="small-focused-agents-over-monolithic-loops">Small, Focused Agents over Monolithic Loops</h3>
<p>A single agent with 20 tools and a &ldquo;keep going until done&rdquo; instruction is a recipe for unpredictable behavior. Instead, decompose your workflow into small, focused agents:</p>
<ul>
<li><strong>One agent per task type</strong> — each with 3-5 tools max</li>
<li><strong>DAG orchestration</strong> — use an orchestrator to sequence agents, not the LLM</li>
<li><strong>Explicit handoffs</strong> — each agent produces a structured output that the next agent consumes</li>
</ul>
<p>I&rsquo;ve found that agents with 3-5 tools have significantly lower tool selection drift than agents with 10+ tools. The model has fewer options to confuse, and the tool descriptions can be more specific.</p>
<h3 id="stateless-reducer-pattern">Stateless Reducer Pattern</h3>
<p>Instead of passing the entire conversation history to every agent call, maintain a compact state object:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-json" data-lang="json"><span style="display:flex;"><span>{
</span></span><span style="display:flex;"><span>  <span style="color:#f92672">&#34;step&#34;</span>: <span style="color:#ae81ff">4</span>,
</span></span><span style="display:flex;"><span>  <span style="color:#f92672">&#34;completed_tasks&#34;</span>: [<span style="color:#e6db74">&#34;search&#34;</span>, <span style="color:#e6db74">&#34;validate&#34;</span>, <span style="color:#e6db74">&#34;transform&#34;</span>],
</span></span><span style="display:flex;"><span>  <span style="color:#f92672">&#34;current_input&#34;</span>: <span style="color:#e6db74">&#34;output_from_step_3.json&#34;</span>,
</span></span><span style="display:flex;"><span>  <span style="color:#f92672">&#34;errors&#34;</span>: [],
</span></span><span style="display:flex;"><span>  <span style="color:#f92672">&#34;accumulated_results&#34;</span>: [<span style="color:#e6db74">&#34;result_1&#34;</span>, <span style="color:#e6db74">&#34;result_2&#34;</span>]
</span></span><span style="display:flex;"><span>}
</span></span></code></pre></div><p>Each agent step reads the current state, produces an action, and returns a new state. The full conversation history is stored in an audit log for debugging, but the agent only sees what it needs for the current step. This dramatically reduces context window pressure and eliminates the &ldquo;forgot what it was doing&rdquo; failure mode.</p>
<h2 id="testing-strategies-that-actually-work">Testing Strategies That Actually Work</h2>
<p>If you take one thing from this guide, let it be this: <strong>stop testing agents like you test deterministic software.</strong> Agents are stochastic systems, and they need stochastic testing methodologies.</p>
<h3 id="multi-trial-testing-with-confidence-intervals">Multi-Trial Testing with Confidence Intervals</h3>
<p>Run every test scenario at least 30 times. Calculate:</p>
<ul>
<li><strong>Pass rate</strong> with Wilson confidence intervals (not raw percentages)</li>
<li><strong>Cost variance</strong> — how much does each run cost in tokens?</li>
<li><strong>Latency variance</strong> — how long does each run take?</li>
</ul>
<p>The <a href="https://github.com/alepot55/agentrial">agentrial</a> framework does this out of the box. You define a task, run it 100 times, and get back pass rates with confidence intervals, cost distributions, and latency distributions. Without this, you&rsquo;re making decisions based on anecdotes.</p>
<h3 id="chaos-engineering-for-agents-flakestorm-approach">Chaos Engineering for Agents (Flakestorm Approach)</h3>
<p><a href="https://github.com/flakestorm">Flakestorm</a> and similar tools apply chaos engineering principles to agent systems. The approach is simple: inject failures at the tool call level and observe how the agent recovers. Testing a LangChain agent with this approach revealed a <strong>95% failure rate on adversarial inputs</strong> — not because the LLM was bad, but because the agent had no recovery mechanisms.</p>
<p>The chaos engineering scenarios I&rsquo;ve found most valuable:</p>
<ul>
<li><strong>Inject API latency</strong> — test timeout handling. Most agents crash when a tool call takes 30 seconds instead of 2.</li>
<li><strong>Return malformed tool outputs</strong> — test error recovery. Can the agent retry, or does it enter an infinite loop?</li>
<li><strong>Drop intermediate results</strong> — test state persistence. Does the agent resume or start over?</li>
<li><strong>Rotate model versions mid-workflow</strong> — test compatibility. Does the agent handle a different model&rsquo;s output format?</li>
<li><strong>Adversarial inputs</strong> — test prompt injection resistance. Can the agent distinguish instructions from data?</li>
</ul>
<p>I&rsquo;ve found that most agents fail catastrophically under these conditions — not because the LLM is bad, but because the surrounding infrastructure doesn&rsquo;t handle failures gracefully. A well-designed agent should degrade, not crash.</p>
<h3 id="cascade-failure-testing">Cascade Failure Testing</h3>
<p>This is the one almost no one tests. To test cascade failures:</p>
<ol>
<li>Inject a bad intermediate result at step N</li>
<li>Observe whether the agent detects the error or propagates it</li>
<li>Measure how many steps the error propagates before detection</li>
<li>Test recovery: can the agent roll back to a known-good state?</li>
</ol>
<p>The Fisher exact test (used by agentrial) can identify which specific step has statistically significant failure rates. This is the difference between &ldquo;the agent fails sometimes&rdquo; and &ldquo;step 4 of the tool selection phase has a 30% failure rate when the input contains more than 5 items.&rdquo;</p>
<h3 id="context-window-monitoring">Context Window Monitoring</h3>
<p>Monitor context window utilization as a first-class metric. When utilization exceeds 70%, the risk of context limit surprises increases exponentially. Set up alerts for:</p>
<ul>
<li><strong>Token count per turn</strong> — sudden increases indicate context window bloat</li>
<li><strong>Instruction proximity</strong> — how far back are the original instructions?</li>
<li><strong>Tool output size</strong> — are intermediate results consuming too much context?</li>
</ul>
<h2 id="infrastructure-for-resilient-agents">Infrastructure for Resilient Agents</h2>
<p>The testing patterns above tell you where your agent breaks. The infrastructure patterns below keep it running anyway.</p>
<h3 id="durable-execution-and-state-persistence">Durable Execution and State Persistence</h3>
<p><a href="https://github.com/orra-dev/orra">Orra</a> and similar tools provide durable execution for AI agent workflows. The key idea: if an agent crashes mid-step, it should resume from where it left off, not start over. This requires:</p>
<ul>
<li><strong>State persistence</strong> — every intermediate result is saved to durable storage</li>
<li><strong>Idempotent tool calls</strong> — running the same tool with the same input twice produces the same result</li>
<li><strong>Automatic retry with backoff</strong> — transient failures don&rsquo;t kill the workflow</li>
</ul>
<p>The <a href="/posts/openai-agents-sdk-temporal-integration-2026/">OpenAI Agents SDK Temporal integration</a> is a good example of this pattern in practice — Temporal provides the durable execution layer while the agent SDK handles the LLM interactions.</p>
<h3 id="pre-validated-execution-plans">Pre-Validated Execution Plans</h3>
<p>Before an agent executes anything, validate the entire plan against your constraints:</p>
<ul>
<li>Are all required tools available?</li>
<li>Do the tool parameters match the schemas?</li>
<li>Is the estimated cost within budget?</li>
<li>Are there any circular dependencies in the plan?</li>
</ul>
<p>This catches the &ldquo;agent decided to do something unexpected&rdquo; failure mode before it costs you money or corrupts data. Orra&rsquo;s pre-validated execution plans are a reference implementation of this pattern.</p>
<h3 id="automatic-health-monitoring-and-audit-logs">Automatic Health Monitoring and Audit Logs</h3>
<p>You need observability at two levels:</p>
<ol>
<li><strong>Agent-level</strong> — what did the agent decide, what tools did it call, what was the outcome?</li>
<li><strong>System-level</strong> — what&rsquo;s the pass rate trend, cost trend, latency trend?</li>
</ol>
<p>The <a href="/posts/llm-observability-tools-comparison-2026/">LLM observability tools</a> (LangSmith, Langfuse, Helicone, Arize) all support agent tracing now. The key metric I track is <strong>pass rate over a rolling 24-hour window</strong> — if it drops below 80%, I want to know immediately, not when a user complains.</p>
<h2 id="building-a-production-reliability-checklist">Building a Production Reliability Checklist</h2>
<p>Before you deploy an agent to production, run through this checklist:</p>
<p><strong>Testing (pre-deployment)</strong></p>
<ul>
<li><input disabled="" type="checkbox"> Multi-trial testing: 100 runs per scenario with confidence intervals</li>
<li><input disabled="" type="checkbox"> Chaos engineering: injected failures at tool call level</li>
<li><input disabled="" type="checkbox"> Adversarial eval: prompt injection attack vectors tested</li>
<li><input disabled="" type="checkbox"> Cascade failure testing: bad intermediate results injected</li>
<li><input disabled="" type="checkbox"> Edge case coverage: nulls, unicode, concurrent requests</li>
</ul>
<p><strong>Architecture</strong></p>
<ul>
<li><input disabled="" type="checkbox"> Stateless reducer pattern (not full conversation history in context)</li>
<li><input disabled="" type="checkbox"> Small, focused agents (3-5 tools per agent)</li>
<li><input disabled="" type="checkbox"> DAG orchestration (not monolithic loops)</li>
<li><input disabled="" type="checkbox"> Structured tool definitions with JSON schema validation</li>
<li><input disabled="" type="checkbox"> Pre-validated execution plans</li>
</ul>
<p><strong>Infrastructure</strong></p>
<ul>
<li><input disabled="" type="checkbox"> Durable execution with state persistence</li>
<li><input disabled="" type="checkbox"> Action deduplication (idempotent tool calls)</li>
<li><input disabled="" type="checkbox"> Human-in-the-loop approval gates for destructive operations</li>
<li><input disabled="" type="checkbox"> Least-privilege tool access with per-call authorization</li>
</ul>
<p><strong>Monitoring</strong></p>
<ul>
<li><input disabled="" type="checkbox"> Rolling 24-hour pass rate with alerts</li>
<li><input disabled="" type="checkbox"> Context window utilization tracking</li>
<li><input disabled="" type="checkbox"> Cost anomaly detection</li>
<li><input disabled="" type="checkbox"> Tool call distribution monitoring</li>
<li><input disabled="" type="checkbox"> Audit log with full conversation history</li>
</ul>
<h2 id="from-demo-to-production-confidence">From Demo to Production Confidence</h2>
<p>The 40%+ failure rate for AI agent projects isn&rsquo;t because agents are fundamentally unreliable. It&rsquo;s because most teams treat agents like deterministic software and are surprised when stochastic behavior emerges.</p>
<p>The path to production confidence is straightforward:</p>
<ol>
<li><strong>Test statistically</strong> — run 100 trials, not 5 demos</li>
<li><strong>Architect for failure</strong> — DAG orchestrators, stateless reducers, durable execution</li>
<li><strong>Monitor continuously</strong> — rolling pass rates, cost trends, latency distributions</li>
<li><strong>Gate destructively</strong> — human approval for high-risk operations</li>
<li><strong>Test adversarially</strong> — prompt injection, chaos engineering, cascade failures</li>
</ol>
<p>I&rsquo;ve been running production agents for over a year now, and the ones that survive are the ones built with these patterns from day one. The ones that fail are the ones that looked perfect in demos and fell apart under real load.</p>
<p>Your agent isn&rsquo;t broken. Your testing methodology is. Fix that first, and the rest follows.</p>
]]></content:encoded></item><item><title>What Breaks an AI Agent After 50 Clean Demos: Production Reliability Guide (2026)</title><link>https://baeseokjae.github.io/posts/what-breaks-an-ai-agent-after-50-clean-demos-production-reliability-guide/</link><pubDate>Tue, 14 Jul 2026 12:00:00 +0000</pubDate><guid>https://baeseokjae.github.io/posts/what-breaks-an-ai-agent-after-50-clean-demos-production-reliability-guide/</guid><description>Why AI agents that pass 50 demos fail in production — the 7 failure modes, the $47K prompt injection case study, and how to build production-grade reliability with chaos engineering, 12-factor architecture, and multi-trial testing.</description><content:encoded><![CDATA[<p>You demo an AI agent to your team. Fifty runs, zero failures. Everyone&rsquo;s impressed. You deploy to production. Within a week, it&rsquo;s hallucinating tool calls, getting stuck in loops, and your Slack is full of &ldquo;the agent did something weird&rdquo; messages.</p>
<p>I&rsquo;ve been there. Multiple times. And I&rsquo;ve spent the last year digging into why this happens and what actually works to fix it.</p>
<p>The short answer: <strong>your agent isn&rsquo;t broken — your testing methodology is.</strong> Single-digit demos and pass/fail judgments hide a massive variance problem that only emerges under statistical scrutiny. Gartner predicts over 40% of AI agent projects will fail by 2027, and in January 2026, a prompt injection in a customer support agent processed a $47,000 fraudulent refund. These aren&rsquo;t edge cases — they&rsquo;re systematic failures that most teams aren&rsquo;t testing for.</p>
<p>This guide covers the seven failure modes that kill production agents, the architectural patterns that survive them, and the testing infrastructure you need before you can trust an agent in production.</p>
<h2 id="the-50-demo-illusion">The 50-Demo Illusion</h2>
<p>Here&rsquo;s a number that should scare you: LLMs show up to <strong>72% variance across runs even at temperature=0</strong>. This isn&rsquo;t a bug in one model — it&rsquo;s a fundamental property of how transformer-based models sample tokens. The <a href="/posts/llm-benchmark-variance-2026/">LLM benchmark variance problem</a> is well-documented, and it applies doubly to agentic workflows where a single token change in a tool selection can cascade into a completely different execution path.</p>
<p>The <a href="https://github.com/alepot55/agentrial">agentrial</a> project ran agents 100 times on the same task and found that pass rates dropped to <strong>60-80%</strong> despite 90%+ accuracy in single-run demos. That&rsquo;s not a 10% degradation — it&rsquo;s a 20-40 percentage point drop that only appears when you run enough trials to get statistically meaningful data.</p>
<p>In practice, this means:</p>
<ul>
<li><strong>A single &ldquo;pass&rdquo; tells you nothing.</strong> You need at least 30-50 runs to establish a baseline pass rate.</li>
<li><strong>Temperature=0 is not deterministic.</strong> It reduces variance but doesn&rsquo;t eliminate it. The model still samples from a probability distribution — it just picks the most likely token every time. But &ldquo;most likely&rdquo; changes based on floating-point precision, prompt formatting, and context window state.</li>
<li><strong>Variance compounds across steps.</strong> A 5-step agent where each step has 90% reliability has a theoretical pass rate of 0.9⁵ = 59%. Run that 100 times and you&rsquo;ll see failures in 40% of trials.</li>
</ul>
<h2 id="the-7-failure-modes-every-production-agent-faces">The 7 Failure Modes Every Production Agent Faces</h2>
<p>Most teams test hallucination and prompt injection. Almost no one systematically tests cascade failures, context limit drift, or data integration drift before shipping. Here are the seven failure modes I&rsquo;ve seen kill production agents in practice.</p>
<h3 id="1-hallucination-under-unexpected-inputs">1. Hallucination Under Unexpected Inputs</h3>
<p>This is the classic failure mode, but it&rsquo;s more nuanced than &ldquo;the model makes stuff up.&rdquo; In production, hallucination manifests as the agent confidently asserting facts that don&rsquo;t exist in its context window — not because the model is broken, but because it&rsquo;s operating outside its training distribution.</p>
<p>I&rsquo;ve watched an agent that correctly processed 47 out of 50 records, then at record 48 started returning fabricated data because the relevant instructions had scrolled out of the active context window. The model didn&rsquo;t tell me it was confused. It just started making things up with the same confidence it had for the first 47 records.</p>
<h3 id="2-edge-case-collapse-nulls-unicode-concurrency">2. Edge Case Collapse (Nulls, Unicode, Concurrency)</h3>
<p>Production data is messy. Null fields, unicode homoglyphs, concurrent requests, and malformed inputs that your test suite never generated will find their way into your agent&rsquo;s context window. I&rsquo;ve seen agents crash on a single null field in a JSON response — not because the tool returned an error, but because the model couldn&rsquo;t decide what to do with <code>null</code> and entered an infinite reasoning loop.</p>
<p>Unicode homoglyphs are particularly nasty. An attacker can replace a Latin &lsquo;a&rsquo; with a Cyrillic &lsquo;а&rsquo; (U+0430) — visually identical, semantically different. Your input validation passes, but the agent interprets the string differently. This is one of the 18 prompt injection attack vectors that bypassed PromptGuard with 100% confidence in a 2026 audit.</p>
<h3 id="3-prompt-injection-and-adversarial-inputs">3. Prompt Injection and Adversarial Inputs</h3>
<p>The January 2026 incident is the one everyone references now: a customer support agent received a carefully crafted message that included hidden instructions embedded in what appeared to be a legitimate support request. The agent processed a $47,000 fraudulent refund before anyone caught it.</p>
<p>The techniques are evolving fast. Attackers now use:</p>
<ul>
<li><strong>Unicode homoglyphs</strong> — visually identical characters with different byte representations</li>
<li><strong>Base64-encoded instructions</strong> — the agent decodes and follows them</li>
<li><strong>ROT13 and other simple ciphers</strong> — the agent &ldquo;figures out&rdquo; the hidden message</li>
<li><strong>Non-English languages</strong> — instructions in a language the agent handles but the monitoring system doesn&rsquo;t</li>
<li><strong>Multi-turn fragmentation</strong> — spreading the injection across multiple conversation turns to avoid detection</li>
</ul>
<p>The <a href="/posts/secure-ai-agents-least-privilege-2026/">secure AI agents guide</a> covers defense-in-depth approaches, but the reality is: no single guardrail catches everything. In a 2026 audit, 12 out of 18 prompt injection attack vectors bypassed PromptGuard with 100% confidence. You need layered defenses, not a silver bullet.</p>
<h3 id="4-context-limit-surprises">4. Context Limit Surprises</h3>
<p>This is the most insidious failure mode because it&rsquo;s invisible until it hits. An agent that works perfectly with a 10-turn conversation will start hallucinating at turn 50 because the context window is full of accumulated tool outputs, error messages, and intermediate results.</p>
<p>The model doesn&rsquo;t tell you it&rsquo;s confused. It just starts making things up. I&rsquo;ve seen agents that correctly processed 47 out of 50 records, then at record 48 started returning fabricated data because the relevant instructions had scrolled out of the active context window.</p>
<p>The fix is the <strong>stateless reducer pattern</strong> — instead of dumping everything into the context window, maintain a compact state representation and only inject what the agent needs for the current step. This is the same pattern Redux popularized for frontend state management, and it works just as well for agents.</p>
<h3 id="5-cascade-failures-in-multi-step-workflows">5. Cascade Failures in Multi-Step Workflows</h3>
<p>Every step in an agent&rsquo;s execution chain adds failure surface area. A 10-step agent where each step has 95% reliability has a 40% chance of completing without error. But the degradation isn&rsquo;t linear — I&rsquo;ve seen agents that handle steps 1-4 perfectly, then at step 5 the context window is cluttered with intermediate results, the model loses track of what it was doing, and it starts repeating steps or skipping critical operations.</p>
<p>This is especially bad in the &ldquo;here&rsquo;s your prompt, here&rsquo;s a bag of tools, loop until you hit the goal&rdquo; pattern that many agent frameworks default to. The <a href="/posts/open-source-agent-eval-harness-comparison-2026/">open-source agent eval harnesses</a> I&rsquo;ve tested show this pattern consistently: agents with Directed Graph (DAG) orchestrators like Airflow or Prefect significantly outperform monolithic loop-based agents on multi-step tasks, because the orchestration layer handles state management and retry logic instead of leaving it to the LLM.</p>
<h3 id="6-data-integration-drift">6. Data Integration Drift</h3>
<p>Your agent depends on external data sources — APIs, databases, file systems. These sources change over time. An API returns a new field, a database schema changes, a file format gets updated. The agent, trained on the old format, starts making incorrect assumptions.</p>
<p>I&rsquo;ve seen this manifest as an agent that reliably parsed CSV exports for months, then suddenly started failing because the export tool added a BOM header. The agent didn&rsquo;t fail gracefully — it silently misparsed the data and made decisions based on the wrong columns. Data integration drift is almost never tested because it requires maintaining test fixtures that evolve with the production data sources.</p>
<h3 id="7-authorization-confusion-in-multi-tenant-systems">7. Authorization Confusion in Multi-Tenant Systems</h3>
<p>When an agent operates across multiple tenants or user contexts, it can accidentally apply one user&rsquo;s permissions to another&rsquo;s data. This is especially dangerous in customer support agents that handle multiple accounts — the agent might read data from account A while operating under account B&rsquo;s authorization context.</p>
<p>The fix is <strong>least-privilege tool access</strong>: each tool call should carry its own authorization context, not inherit the agent&rsquo;s global permissions. The <a href="/posts/secure-ai-agents-least-privilege-2026/">secure AI agents guide</a> covers this in detail, but the short version is: treat every tool call as an independent authorization boundary.</p>
<h2 id="epistemic-distortion-the-meta-failure-mode">Epistemic Distortion: The Meta-Failure Mode</h2>
<p>Beyond the seven specific failure modes, there&rsquo;s a meta-pattern that researchers have documented across 1,400+ controlled experiments: <strong>epistemic distortion</strong>. This is the agent&rsquo;s tendency to silently drop conflicting instructions, apply wrong evidence standards, or selectively ignore parts of its system prompt.</p>
<p>In practice, epistemic distortion looks like:</p>
<ul>
<li>The agent follows the most recent instruction even when earlier instructions explicitly contradict it</li>
<li>The agent applies different reasoning standards to different types of inputs (treating user input as more authoritative than system prompts)</li>
<li>The agent &ldquo;forgets&rdquo; constraints that were established earlier in the conversation</li>
</ul>
<p>This isn&rsquo;t a bug — it&rsquo;s a consequence of how attention mechanisms work. The model weights all tokens in its context window, and newer or more salient tokens can override earlier instructions. The fix is structural: don&rsquo;t rely on the model to remember constraints. Encode them in the tool schemas, the orchestration layer, and the validation pipeline.</p>
<h2 id="the-12-factor-agent-architecture">The 12-Factor Agent Architecture</h2>
<p>The <a href="https://github.com/humanlayer/12-factor-agents">12-Factor Agents</a> methodology (which hit 475+ points on Hacker News for good reason) provides a solid foundation. Here are the principles I&rsquo;ve found most impactful in practice.</p>
<h3 id="own-your-prompts-and-context-window">Own Your Prompts and Context Window</h3>
<p>Don&rsquo;t let the framework manage your prompts. You need explicit control over:</p>
<ul>
<li><strong>System prompt versioning</strong> — every change is a new version, tracked in git</li>
<li><strong>Context window budgeting</strong> — know exactly how many tokens each component uses</li>
<li><strong>Prompt injection surface</strong> — user input should never be concatenated into system prompts</li>
</ul>
<p>I use a simple pattern: each agent step gets a fixed token budget. The system prompt gets 30%, the current state gets 20%, tool definitions get 30%, and user input gets 20%. If something exceeds its budget, it gets truncated — not the model&rsquo;s job to figure out what&rsquo;s important.</p>
<h3 id="tools-as-structured-outputs">Tools as Structured Outputs</h3>
<p>Treat tool definitions as JSON schemas with strict validation, not as natural language descriptions. This means:</p>
<ul>
<li>Every tool parameter has a type, description, and required/optional flag</li>
<li>Tool calls are validated against the schema before execution</li>
<li>Validation failures return structured error messages, not &ldquo;something went wrong&rdquo;</li>
</ul>
<p>This pattern, combined with the <a href="/posts/llm-structured-output-guide-2026/">LLM structured output</a> capabilities available in 2026, means you can catch tool selection errors before they cause damage. The agent might still pick the wrong tool, but it won&rsquo;t call it with invalid parameters.</p>
<h3 id="small-focused-agents-over-monolithic-loops">Small, Focused Agents over Monolithic Loops</h3>
<p>A single agent with 20 tools and a &ldquo;keep going until done&rdquo; instruction is a recipe for unpredictable behavior. Instead, decompose your workflow into small, focused agents:</p>
<ul>
<li><strong>One agent per task type</strong> — each with 3-5 tools max</li>
<li><strong>DAG orchestration</strong> — use an orchestrator to sequence agents, not the LLM</li>
<li><strong>Explicit handoffs</strong> — each agent produces a structured output that the next agent consumes</li>
</ul>
<p>I&rsquo;ve found that agents with 3-5 tools have significantly lower tool selection drift than agents with 10+ tools. The model has fewer options to confuse, and the tool descriptions can be more specific.</p>
<h3 id="stateless-reducer-pattern">Stateless Reducer Pattern</h3>
<p>Instead of passing the entire conversation history to every agent call, maintain a compact state object:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-json" data-lang="json"><span style="display:flex;"><span>{
</span></span><span style="display:flex;"><span>  <span style="color:#f92672">&#34;step&#34;</span>: <span style="color:#ae81ff">4</span>,
</span></span><span style="display:flex;"><span>  <span style="color:#f92672">&#34;completed_tasks&#34;</span>: [<span style="color:#e6db74">&#34;search&#34;</span>, <span style="color:#e6db74">&#34;validate&#34;</span>, <span style="color:#e6db74">&#34;transform&#34;</span>],
</span></span><span style="display:flex;"><span>  <span style="color:#f92672">&#34;current_input&#34;</span>: <span style="color:#e6db74">&#34;output_from_step_3.json&#34;</span>,
</span></span><span style="display:flex;"><span>  <span style="color:#f92672">&#34;errors&#34;</span>: [],
</span></span><span style="display:flex;"><span>  <span style="color:#f92672">&#34;accumulated_results&#34;</span>: [<span style="color:#e6db74">&#34;result_1&#34;</span>, <span style="color:#e6db74">&#34;result_2&#34;</span>]
</span></span><span style="display:flex;"><span>}
</span></span></code></pre></div><p>Each agent step reads the current state, produces an action, and returns a new state. The full conversation history is stored in an audit log for debugging, but the agent only sees what it needs for the current step. This dramatically reduces context window pressure and eliminates the &ldquo;forgot what it was doing&rdquo; failure mode.</p>
<h2 id="testing-strategies-that-actually-work">Testing Strategies That Actually Work</h2>
<p>If you take one thing from this guide, let it be this: <strong>stop testing agents like you test deterministic software.</strong> Agents are stochastic systems, and they need stochastic testing methodologies.</p>
<h3 id="multi-trial-testing-with-confidence-intervals">Multi-Trial Testing with Confidence Intervals</h3>
<p>Run every test scenario at least 30 times. Calculate:</p>
<ul>
<li><strong>Pass rate</strong> with Wilson confidence intervals (not raw percentages)</li>
<li><strong>Cost variance</strong> — how much does each run cost in tokens?</li>
<li><strong>Latency variance</strong> — how long does each run take?</li>
</ul>
<p>The <a href="https://github.com/alepot55/agentrial">agentrial</a> framework does this out of the box. You define a task, run it 100 times, and get back pass rates with confidence intervals, cost distributions, and latency distributions. Without this, you&rsquo;re making decisions based on anecdotes.</p>
<h3 id="chaos-engineering-for-agents-flakestorm-approach">Chaos Engineering for Agents (Flakestorm Approach)</h3>
<p><a href="https://github.com/flakestorm">Flakestorm</a> and similar tools apply chaos engineering principles to agent systems. The approach is simple: inject failures at the tool call level and observe how the agent recovers. Testing a LangChain agent with this approach revealed a <strong>95% failure rate on adversarial inputs</strong> — not because the LLM was bad, but because the agent had no recovery mechanisms.</p>
<p>The chaos engineering scenarios I&rsquo;ve found most valuable:</p>
<ul>
<li><strong>Inject API latency</strong> — test timeout handling. Most agents crash when a tool call takes 30 seconds instead of 2.</li>
<li><strong>Return malformed tool outputs</strong> — test error recovery. Can the agent retry, or does it enter an infinite loop?</li>
<li><strong>Drop intermediate results</strong> — test state persistence. Does the agent resume or start over?</li>
<li><strong>Rotate model versions mid-workflow</strong> — test compatibility. Does the agent handle a different model&rsquo;s output format?</li>
<li><strong>Adversarial inputs</strong> — test prompt injection resistance. Can the agent distinguish instructions from data?</li>
</ul>
<p>I&rsquo;ve found that most agents fail catastrophically under these conditions — not because the LLM is bad, but because the surrounding infrastructure doesn&rsquo;t handle failures gracefully. A well-designed agent should degrade, not crash.</p>
<h3 id="cascade-failure-testing">Cascade Failure Testing</h3>
<p>This is the one almost no one tests. To test cascade failures:</p>
<ol>
<li>Inject a bad intermediate result at step N</li>
<li>Observe whether the agent detects the error or propagates it</li>
<li>Measure how many steps the error propagates before detection</li>
<li>Test recovery: can the agent roll back to a known-good state?</li>
</ol>
<p>The Fisher exact test (used by agentrial) can identify which specific step has statistically significant failure rates. This is the difference between &ldquo;the agent fails sometimes&rdquo; and &ldquo;step 4 of the tool selection phase has a 30% failure rate when the input contains more than 5 items.&rdquo;</p>
<h3 id="context-window-monitoring">Context Window Monitoring</h3>
<p>Monitor context window utilization as a first-class metric. When utilization exceeds 70%, the risk of context limit surprises increases exponentially. Set up alerts for:</p>
<ul>
<li><strong>Token count per turn</strong> — sudden increases indicate context window bloat</li>
<li><strong>Instruction proximity</strong> — how far back are the original instructions?</li>
<li><strong>Tool output size</strong> — are intermediate results consuming too much context?</li>
</ul>
<h2 id="infrastructure-for-resilient-agents">Infrastructure for Resilient Agents</h2>
<p>The testing patterns above tell you where your agent breaks. The infrastructure patterns below keep it running anyway.</p>
<h3 id="durable-execution-and-state-persistence">Durable Execution and State Persistence</h3>
<p><a href="https://github.com/orra-dev/orra">Orra</a> and similar tools provide durable execution for AI agent workflows. The key idea: if an agent crashes mid-step, it should resume from where it left off, not start over. This requires:</p>
<ul>
<li><strong>State persistence</strong> — every intermediate result is saved to durable storage</li>
<li><strong>Idempotent tool calls</strong> — running the same tool with the same input twice produces the same result</li>
<li><strong>Automatic retry with backoff</strong> — transient failures don&rsquo;t kill the workflow</li>
</ul>
<p>The <a href="/posts/openai-agents-sdk-temporal-integration-2026/">OpenAI Agents SDK Temporal integration</a> is a good example of this pattern in practice — Temporal provides the durable execution layer while the agent SDK handles the LLM interactions.</p>
<h3 id="pre-validated-execution-plans">Pre-Validated Execution Plans</h3>
<p>Before an agent executes anything, validate the entire plan against your constraints:</p>
<ul>
<li>Are all required tools available?</li>
<li>Do the tool parameters match the schemas?</li>
<li>Is the estimated cost within budget?</li>
<li>Are there any circular dependencies in the plan?</li>
</ul>
<p>This catches the &ldquo;agent decided to do something unexpected&rdquo; failure mode before it costs you money or corrupts data. Orra&rsquo;s pre-validated execution plans are a reference implementation of this pattern.</p>
<h3 id="automatic-health-monitoring-and-audit-logs">Automatic Health Monitoring and Audit Logs</h3>
<p>You need observability at two levels:</p>
<ol>
<li><strong>Agent-level</strong> — what did the agent decide, what tools did it call, what was the outcome?</li>
<li><strong>System-level</strong> — what&rsquo;s the pass rate trend, cost trend, latency trend?</li>
</ol>
<p>The <a href="/posts/llm-observability-tools-comparison-2026/">LLM observability tools</a> (LangSmith, Langfuse, Helicone, Arize) all support agent tracing now. The key metric I track is <strong>pass rate over a rolling 24-hour window</strong> — if it drops below 80%, I want to know immediately, not when a user complains.</p>
<h2 id="building-a-production-reliability-checklist">Building a Production Reliability Checklist</h2>
<p>Before you deploy an agent to production, run through this checklist:</p>
<p><strong>Testing (pre-deployment)</strong></p>
<ul>
<li><input disabled="" type="checkbox"> Multi-trial testing: 100 runs per scenario with confidence intervals</li>
<li><input disabled="" type="checkbox"> Chaos engineering: injected failures at tool call level</li>
<li><input disabled="" type="checkbox"> Adversarial eval: prompt injection attack vectors tested</li>
<li><input disabled="" type="checkbox"> Cascade failure testing: bad intermediate results injected</li>
<li><input disabled="" type="checkbox"> Edge case coverage: nulls, unicode, concurrent requests</li>
</ul>
<p><strong>Architecture</strong></p>
<ul>
<li><input disabled="" type="checkbox"> Stateless reducer pattern (not full conversation history in context)</li>
<li><input disabled="" type="checkbox"> Small, focused agents (3-5 tools per agent)</li>
<li><input disabled="" type="checkbox"> DAG orchestration (not monolithic loops)</li>
<li><input disabled="" type="checkbox"> Structured tool definitions with JSON schema validation</li>
<li><input disabled="" type="checkbox"> Pre-validated execution plans</li>
</ul>
<p><strong>Infrastructure</strong></p>
<ul>
<li><input disabled="" type="checkbox"> Durable execution with state persistence</li>
<li><input disabled="" type="checkbox"> Action deduplication (idempotent tool calls)</li>
<li><input disabled="" type="checkbox"> Human-in-the-loop approval gates for destructive operations</li>
<li><input disabled="" type="checkbox"> Least-privilege tool access with per-call authorization</li>
</ul>
<p><strong>Monitoring</strong></p>
<ul>
<li><input disabled="" type="checkbox"> Rolling 24-hour pass rate with alerts</li>
<li><input disabled="" type="checkbox"> Context window utilization tracking</li>
<li><input disabled="" type="checkbox"> Cost anomaly detection</li>
<li><input disabled="" type="checkbox"> Tool call distribution monitoring</li>
<li><input disabled="" type="checkbox"> Audit log with full conversation history</li>
</ul>
<h2 id="from-demo-to-production-confidence">From Demo to Production Confidence</h2>
<p>The 40%+ failure rate for AI agent projects isn&rsquo;t because agents are fundamentally unreliable. It&rsquo;s because most teams treat agents like deterministic software and are surprised when stochastic behavior emerges.</p>
<p>The path to production confidence is straightforward:</p>
<ol>
<li><strong>Test statistically</strong> — run 100 trials, not 5 demos</li>
<li><strong>Architect for failure</strong> — DAG orchestrators, stateless reducers, durable execution</li>
<li><strong>Monitor continuously</strong> — rolling pass rates, cost trends, latency distributions</li>
<li><strong>Gate destructively</strong> — human approval for high-risk operations</li>
<li><strong>Test adversarially</strong> — prompt injection, chaos engineering, cascade failures</li>
</ol>
<p>I&rsquo;ve been running production agents for over a year now, and the ones that survive are the ones built with these patterns from day one. The ones that fail are the ones that looked perfect in demos and fell apart under real load.</p>
<p>Your agent isn&rsquo;t broken. Your testing methodology is. Fix that first, and the rest follows.</p>
]]></content:encoded></item><item><title>AWS Agent-EvalKit: Open-Source AI Agent Evaluation for Developers — Tutorial &amp; Deep Dive</title><link>https://baeseokjae.github.io/posts/aws-agent-evalkit-developer-tutorial-2026/</link><pubDate>Sat, 20 Jun 2026 12:00:00 +0000</pubDate><guid>https://baeseokjae.github.io/posts/aws-agent-evalkit-developer-tutorial-2026/</guid><description>A hands-on tutorial for AWS Agent-EvalKit (released June 11, 2026): install, run the 6-phase eval workflow, combine code-based and LLM-judge scoring, an...</description><content:encoded><![CDATA[<p>AWS Agent-EvalKit is an open-source toolkit (Apache 2.0, released June 11, 2026) that runs AI agent evaluation directly inside your coding assistant via slash commands. Instead of treating agent evaluation as a post-deployment activity, it brings a six-phase workflow — Plan, Data, Trace, Run Agent, Eval, Report — into Claude Code, Kiro CLI, or Kilo Code, combining code-based evaluators with LLM-as-judge scoring through Amazon Bedrock. I&rsquo;ve been running evaluations against AI agents for the last two years, and the pattern I kept seeing was: teams either buy a managed eval platform or cobble together Python scripts and a prompt template. Agent-EvalKit splits the difference — it&rsquo;s a CLI that reads your agent source code, generates test cases, instruments tracing, runs the trials, and recommends fixes with file-level accuracy. In this tutorial, I&rsquo;ll walk through installing it, running your first evaluation, and the real-world case study where it caught a hallucination problem that output-level testing missed entirely.</p>
<h2 id="what-agent-evalkit-actually-does">What Agent-EvalKit Actually Does</h2>
<p>Agent-EvalKit is not another eval framework you import as a library. It&rsquo;s an AI assistant that operates through your existing coding assistant. You install it once with <code>uv tool install</code>, initialize a project, then issue slash commands like <code>/evalkit.plan</code> and <code>/evalkit.eval</code> in your Claude Code or Kiro CLI session. The assistant reads your agent&rsquo;s source code from disk, designs an evaluation strategy, generates test cases, adds OpenTelemetry tracing to instrument your agent, runs it against the test cases, scores the traces, and writes a report with specific code-level fix recommendations.</p>
<p>The key architectural decision is that the evaluation pipeline lives in your dev environment, not in a separate platform. This means it reads your actual agent code, runs against real tool endpoints (with the safety caveat that you should use staging credentials), and produces recommendations that reference specific lines in your codebase. The trade-off vs. a managed platform like AgentCore Evaluations: you get deeper code awareness and lower setup friction, but you don&rsquo;t get the managed dataset versioning, cross-team dashboards, or release gating that a platform provides.</p>
<h2 id="installation">Installation</h2>
<p>Agent-EvalKit requires Python 3.11+, <code>uv</code>, Git, and a supported AI coding assistant. I&rsquo;m using Claude Code 0.3.14 and it works cleanly.</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-bash" data-lang="bash"><span style="display:flex;"><span>uv tool install evalkit --from git+https://github.com/awslabs/Agent-EvalKit.git
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#75715e"># Verify</span>
</span></span><span style="display:flex;"><span>evalkit check
</span></span></code></pre></div><p>This installs the <code>evalkit</code> CLI globally. The actual eval workflow runs through your coding assistant&rsquo;s slash commands, but <code>evalkit init</code> scaffolds the project structure.</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-bash" data-lang="bash"><span style="display:flex;"><span>evalkit init my-search-agent-eval
</span></span><span style="display:flex;"><span>cd my-search-agent-eval
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#75715e"># Copy your agent source into the eval project</span>
</span></span><span style="display:flex;"><span>cp -r /path/to/your/search-agent .
</span></span></code></pre></div><p>The project structure created by <code>evalkit init</code> includes a <code>commands/</code> directory where the slash-command handlers live, a <code>templates/</code> directory for evaluation test case templates, and a <code>tracing/</code> directory with OpenTelemetry instrumentation helpers.</p>
<h2 id="the-six-phase-workflow">The Six-Phase Workflow</h2>
<p>Once the project is initialized, you open the <code>my-search-agent-eval</code> directory with your coding assistant and start the workflow.</p>
<h3 id="phase-1-plan-evalkitplan">Phase 1: Plan (<code>/evalkit.plan</code>)</h3>
<p>This is the only phase that requires user input. You tell the assistant what kind of agent you&rsquo;re evaluating and what you care about.</p>
<pre tabindex="0"><code>/evalkit.plan Evaluate my search agent at ./search-agent for faithfulness and tool accuracy
</code></pre><p>The assistant reads every Python file in <code>./search-agent</code>, identifies the tool-calling patterns, the LLM provider configuration, and the response formatting logic. It returns an evaluation strategy document that specifies:</p>
<ul>
<li>Which metrics to compute (faithfulness, tool-selection accuracy, response quality)</li>
<li>What evaluator style to use for each (LLM-as-judge for faithfulness, code-based for tool accuracy)</li>
<li>How many test cases to generate and what categories they should cover</li>
</ul>
<p>I found the plan output thorough but verbose — it runs the full agent code through the assistant&rsquo;s context window, which costs 8,000–15,000 tokens per <code>plan</code> invocation depending on agent size. You can constrain it with a focused description if your agent is large.</p>
<h3 id="phase-2-data-evalkitdata">Phase 2: Data (<code>/evalkit.data</code>)</h3>
<pre tabindex="0"><code>/evalkit.data Focus on edge cases: empty search results, malformed queries, timeout scenarios
</code></pre><p>The assistant generates test cases with ground-truth annotations and writes them to <code>eval/test_cases.json</code>. Each test case includes:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-json" data-lang="json"><span style="display:flex;"><span>{
</span></span><span style="display:flex;"><span>  <span style="color:#f92672">&#34;id&#34;</span>: <span style="color:#e6db74">&#34;tc_003&#34;</span>,
</span></span><span style="display:flex;"><span>  <span style="color:#f92672">&#34;input&#34;</span>: <span style="color:#e6db74">&#34;search for non-existent product &#39;XYZ-999&#39;&#34;</span>,
</span></span><span style="display:flex;"><span>  <span style="color:#f92672">&#34;ground_truth&#34;</span>: {
</span></span><span style="display:flex;"><span>    <span style="color:#f92672">&#34;expected_tool&#34;</span>: <span style="color:#e6db74">&#34;search_products&#34;</span>,
</span></span><span style="display:flex;"><span>    <span style="color:#f92672">&#34;expected_params&#34;</span>: {<span style="color:#f92672">&#34;query&#34;</span>: <span style="color:#e6db74">&#34;XYZ-999&#34;</span>},
</span></span><span style="display:flex;"><span>    <span style="color:#f92672">&#34;expected_response_contains&#34;</span>: [<span style="color:#e6db74">&#34;no results found&#34;</span>, <span style="color:#e6db74">&#34;try different search terms&#34;</span>],
</span></span><span style="display:flex;"><span>    <span style="color:#f92672">&#34;faithfulness_check&#34;</span>: <span style="color:#e6db74">&#34;response must not fabricate product details&#34;</span>
</span></span><span style="display:flex;"><span>  },
</span></span><span style="display:flex;"><span>  <span style="color:#f92672">&#34;category&#34;</span>: <span style="color:#e6db74">&#34;empty_results&#34;</span>
</span></span><span style="display:flex;"><span>}
</span></span></code></pre></div><p>The ground truth structure matters because it enables both code-based checks (did the agent call the right tool with the right params?) and LLM-judge checks (is the response faithful to the tool output?).</p>
<h3 id="phase-3-trace-evalkittrace">Phase 3: Trace (<code>/evalkit.trace</code>)</h3>
<pre tabindex="0"><code>/evalkit.trace
</code></pre><p>This phase instruments your agent with OpenTelemetry tracing. The assistant reads your agent code and inserts tracer spans around each tool call. For a Python agent using the Strands Agents SDK, the instrumentation wraps the <code>agent.run()</code> call with span context that captures:</p>
<ul>
<li>Tool name and input parameters</li>
<li>Timestamps and duration</li>
<li>Tool output (truncated to configurable max chars)</li>
<li>Error states and retry attempts</li>
</ul>
<p>The tracing is optional — you can run the eval against raw agent outputs if you already have logging in place. But without traces, you can only score the final response, not the tool-call path, which is where Agent-EvalKit&rsquo;s best value lives.</p>
<h3 id="phase-4-run-agent-evalkitrun_agent">Phase 4: Run Agent (<code>/evalkit.run_agent</code>)</h3>
<pre tabindex="0"><code>/evalkit.run_agent
</code></pre><p>The assistant executes your agent against each test case, collects the traces, and writes the results to <code>eval/traces/</code>. Each trace file captures the full execution path:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-json" data-lang="json"><span style="display:flex;"><span>{
</span></span><span style="display:flex;"><span>  <span style="color:#f92672">&#34;test_case_id&#34;</span>: <span style="color:#e6db74">&#34;tc_003&#34;</span>,
</span></span><span style="display:flex;"><span>  <span style="color:#f92672">&#34;agent_response&#34;</span>: <span style="color:#e6db74">&#34;I couldn&#39;t find any products matching &#39;XYZ-999&#39;. Please try different search terms or check the product catalog.&#34;</span>,
</span></span><span style="display:flex;"><span>  <span style="color:#f92672">&#34;tools_called&#34;</span>: [
</span></span><span style="display:flex;"><span>    {
</span></span><span style="display:flex;"><span>      <span style="color:#f92672">&#34;tool&#34;</span>: <span style="color:#e6db74">&#34;search_products&#34;</span>,
</span></span><span style="display:flex;"><span>      <span style="color:#f92672">&#34;params&#34;</span>: {<span style="color:#f92672">&#34;query&#34;</span>: <span style="color:#e6db74">&#34;XYZ-999&#34;</span>},
</span></span><span style="display:flex;"><span>      <span style="color:#f92672">&#34;result&#34;</span>: {<span style="color:#f92672">&#34;products&#34;</span>: [], <span style="color:#f92672">&#34;total_count&#34;</span>: <span style="color:#ae81ff">0</span>, <span style="color:#f92672">&#34;suggestions&#34;</span>: []}
</span></span><span style="display:flex;"><span>    }
</span></span><span style="display:flex;"><span>  ],
</span></span><span style="display:flex;"><span>  <span style="color:#f92672">&#34;latency_ms&#34;</span>: <span style="color:#ae81ff">2340</span>,
</span></span><span style="display:flex;"><span>  <span style="color:#f92672">&#34;token_usage&#34;</span>: {<span style="color:#f92672">&#34;input&#34;</span>: <span style="color:#ae81ff">1450</span>, <span style="color:#f92672">&#34;output&#34;</span>: <span style="color:#ae81ff">320</span>}
</span></span><span style="display:flex;"><span>}
</span></span></code></pre></div><h3 id="phase-5-eval-evalkiteval">Phase 5: Eval (<code>/evalkit.eval</code>)</h3>
<pre tabindex="0"><code>/evalkit.eval
</code></pre><p>This is where the actual scoring happens. The assistant writes evaluation code that reads the traces and computes metrics. For the travel agent case study AWS published, the eval pipeline used two evaluators in parallel:</p>
<p><strong>Code-based evaluator</strong> — checks tool-selection correctness deterministically. Did the agent call the expected tool? Were the parameters correct? This runs fast (milliseconds per trace) and produces binary pass/fail scores.</p>
<p><strong>LLM-as-judge evaluator</strong> — scores response faithfulness on a 0–100 scale. The LLM judge (Amazon Nova Pro through Bedrock in the published case study) receives the tool trace alongside the agent&rsquo;s response and answers: &ldquo;Does the response faithfully reflect what the tools returned, without adding information not present in the tool output?&rdquo;</p>
<p>The assistant writes the evaluation code at <code>eval/evaluator.py</code> so you can review and customize it. I found the generated evaluator templates reasonable but simplistic — the faithfulness judge prompt needed tuning for my domain-specific vocabulary.</p>
<h3 id="phase-6-report-evalkitreport">Phase 6: Report (<code>/evalkit.report</code>)</h3>
<pre tabindex="0"><code>/evalkit.report
</code></pre><p>The final output is a markdown report covering:</p>
<ul>
<li>Aggregate scores per metric</li>
<li>Per-test-case breakdown with pass/fail details</li>
<li>A ranked list of improvement recommendations referencing specific code locations</li>
<li>Expected impact estimate for each recommendation</li>
</ul>
<h2 id="the-travel-agent-case-study-why-output-level-testing-lies-to-you">The Travel Agent Case Study: Why Output-Level Testing Lies to You</h2>
<p>The AWS Machine Learning Blog published Agent-EvalKit using a travel research agent built with Strands Agents SDK and Amazon Bedrock. The agent&rsquo;s job: given a destination and preferences, research flights, hotels, and attractions, then write a travel brief.</p>
<p>The standard eval — ask the agent 20 questions, have a human rate the answers — gave it a response quality score of 83.9%. That looks solid. But Agent-EvalKit&rsquo;s trace-level evaluation told a different story. The faithfulness score was 32.3%.</p>
<p>Here&rsquo;s what happened. When the agent&rsquo;s <code>search_hotels</code> tool returned empty results (no vacancies matching the criteria), the agent didn&rsquo;t say &ldquo;no hotels found.&rdquo; It fabricated hotel names, prices, and descriptions that sounded plausible but came entirely from the model&rsquo;s training data. The final response was well-structured and actionable — every human rater gave it high marks because the hallucinations matched the destination&rsquo;s real hotel landscape. The agent looked competent while being completely wrong.</p>
<p>The trace caught it because the eval compared the tool output (empty list) against the agent response (hallucinated hotel names). An output-only eval never sees the tool output.</p>
<p>After applying Agent-EvalKit&rsquo;s report recommendations — adding an explicit &ldquo;disclose empty results&rdquo; instruction to the system prompt and a post-processing check that flags responses containing information not present in tool outputs — the faithfulness score went from 32.3% to 78.1% in one iteration.</p>
<p>This pattern is why I now run trace-level evaluation on every agent I ship. If you only check final outputs, you&rsquo;re measuring presentation quality, not factual reliability.</p>
<h2 id="cicd-integration">CI/CD Integration</h2>
<p>Agent-EvalKit generates standalone evaluation code at <code>eval/</code> that you can run outside the coding assistant. After the initial <code>evalkit init</code> and a full workflow run to set up the pipeline, subsequent runs work as a shell command:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-bash" data-lang="bash"><span style="display:flex;"><span><span style="color:#75715e"># Run the evaluation pipeline without the slash-command assistant</span>
</span></span><span style="display:flex;"><span>python eval/run_pipeline.py --config eval/config.yaml --output eval/report.json
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#75715e"># Check against threshold</span>
</span></span><span style="display:flex;"><span>python -c <span style="color:#e6db74">&#34;
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">import json
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">r = json.load(open(&#39;eval/report.json&#39;))
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">assert r[&#39;faithfulness&#39;] &gt; 0.70, f&#39;Faithfulness {r[\&#34;faithfulness\&#34;]} below threshold&#39;
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">assert r[&#39;tool_accuracy&#39;] &gt; 0.85, f&#39;Tool accuracy {r[\&#34;tool_accuracy\&#34;]} below threshold&#39;
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">&#34;</span>
</span></span></code></pre></div><p>This runs in any CI runner that has Python and network access to Bedrock. For a full CI/CD setup with gating, cost controls, and shadow deployment, see the <a href="/posts/agent-ci-cd-eval-pipeline-integration-guide-2026/">Agent CI/CD Eval Pipeline Integration Guide</a>. The interaction between Agent-EvalKit&rsquo;s trace-level scoring and broader CI gates (regression detection, cost budgets, canary rollouts) is where it gets production-relevant.</p>
<h2 id="honest-trade-offs">Honest Trade-offs</h2>
<p>After running Agent-EvalKit against three different agents over the past week, here are the limitations you&rsquo;ll hit:</p>
<p><strong>No cross-run versioning.</strong> Each <code>evalkit init</code> creates a fresh project. There&rsquo;s no built-in way to compare scores across agent versions or track regression over time. You have to build that yourself by archiving <code>eval/report.json</code> files and comparing them externally. The <a href="/posts/open-source-agent-eval-harness-comparison-2026/">Open Source Agent Eval Harness Comparison</a> covers tools that handle versioning natively if that&rsquo;s a priority.</p>
<p><strong>LLM-judge cost adds up.</strong> Each eval run pays for both the agent under test (API calls to its tools + LLM) and the judge LLM (Amazon Bedrock inference for faithfulness scoring). On the travel agent eval with 50 test cases, the judge cost was roughly $0.80 per run. At 20 CI runs per day, that&rsquo;s $16/day just for the judge — before the agent&rsquo;s own inference cost.</p>
<p><strong>Narrow framework support for tracing.</strong> The auto-instrumentation currently generates OpenTelemetry traces for Strands Agents SDK, LangGraph, and CrewAI. If your agent uses a custom framework (many production agents do), you need to write the OTel instrumentation yourself. The generated trace templates at <code>tracing/</code> are a decent starting point but not plug-and-play.</p>
<p><strong>Single-project scope.</strong> Agent-EvalKit is designed for evaluating one agent at a time in one project directory. It doesn&rsquo;t help with cross-agent comparison, A/B testing of prompt variants, or multi-agent system evaluation. For those use cases, tools like MASEval (covered in the eval harness comparison) are a better fit.</p>
<h2 id="when-to-use-agent-evalkit-vs-when-to-skip">When to Use Agent-EvalKit vs. When to Skip</h2>
<p>Use Agent-EvalKit when: you&rsquo;re actively developing an agent and want to catch trace-level failures before they hit production; you need code-level fix recommendations, not just pass/fail scores; you&rsquo;re already in a Claude Code, Kiro CLI, or Kilo Code workflow and want evaluation to feel like an extension of your coding session.</p>
<p>Skip it when: your agent does no tool calling (pure chat — a manual checklist is sufficient); you need managed dataset versioning and regression dashboards across a team; you&rsquo;re evaluating multi-agent systems where coordination topology matters more than individual agent traces.</p>
<p>The toolkit is fresh (v0.1.2 at time of writing) and the community is small — 25 GitHub stars, 5 forks. The Apache 2.0 license and AWS backing suggest it will grow, but today you should expect to customize the generated eval code and trace instrumentation rather than use everything out of the box. For most teams building production agents in 2026, that&rsquo;s still a net positive: the 80% of scaffolding that Agent-EvalKit generates is the boring, error-prone part of setting up trace-level evaluation, and the 20% you customize is the part that makes your agent&rsquo;s specific failure modes visible.</p>
]]></content:encoded></item><item><title>LangWatch Review 2026: LLM and Agent Application Monitoring Platform</title><link>https://baeseokjae.github.io/posts/langwatch-review-2026/</link><pubDate>Sat, 16 May 2026 00:06:27 +0000</pubDate><guid>https://baeseokjae.github.io/posts/langwatch-review-2026/</guid><description>LangWatch 2026 review: features, pricing, and competitor analysis for the open-source LLM monitoring, agent simulation, and evaluation platform.</description><content:encoded><![CDATA[<p>LangWatch is an open-source monitoring, evaluation, and optimization platform for LLM applications and AI agents. It provides tracing, real-time evaluation, agent simulation, and prompt management in a single unified system — with cloud plans starting at €59/month and self-hosting completely free with no feature gates.</p>
<h2 id="what-is-langwatch-the-llm-observability-platform-explained">What Is LangWatch? (The LLM Observability Platform Explained)</h2>
<p>LangWatch is an open-source LLMOps platform that combines production monitoring, automated evaluation, agent simulation testing, and prompt optimization in a single unified system. Founded to address the fragmented tooling problem facing AI teams — where developers typically need 3–5 separate tools for tracing, evals, prompt management, and cost control — LangWatch consolidates all these workflows under one roof. As of 2026, the platform has surpassed 3,000 GitHub stars and supports 10+ LLM providers including OpenAI, Azure, AWS Bedrock, Google Gemini, Deepseek, Groq, MistralAI, VertexAI, and LiteLLM. The platform is built natively on OpenTelemetry, meaning enterprise teams can integrate with existing observability stacks without vendor lock-in. The LLM observability market it operates in is expanding fast: from $1.97 billion in 2025, it&rsquo;s projected to hit $2.69 billion in 2026 at a 36.3% CAGR, and $9.26 billion by 2030. LangWatch positions itself as the platform for developers who want production-grade AI monitoring without stitching together half a dozen point solutions.</p>
<p>In March 2026, LangWatch open-sourced its agent evaluation layer, including end-to-end tracing and simulation capabilities — a move that meaningfully raised the bar for what &ldquo;free and open-source&rdquo; means in the LLMOps space.</p>
<h2 id="langwatch-core-features--what-you-actually-get">LangWatch Core Features — What You Actually Get</h2>
<p>LangWatch delivers five primary capability areas that distinguish it from narrower monitoring tools: LLM tracing with OpenTelemetry-native instrumentation, real-time and offline automated evaluations, multi-turn agent simulation via the Scenario framework, version-controlled prompt management with DSPy optimization, and an AI Gateway for cost governance. The platform&rsquo;s core thesis is that observability, evaluation, and optimization belong in the same feedback loop rather than siloed across separate systems — a philosophy that drives its unified data model where every trace, eval result, and prompt change shares the same underlying timeline. Unlike tools that solve one problem well (Langfuse for tracing, a separate service for evals), LangWatch&rsquo;s integration means that when you change a prompt and run an eval, the trace correlation is automatic. For a team running 50,000 LLM calls per day, that correlation turns debugging from a multi-hour grep-through-logs exercise into a 2-minute dashboard filter. This section breaks down each feature area with specific implementation details so you know what you&rsquo;re actually building on.</p>
<h3 id="llm-tracing--observability-opentelemetry-native">LLM Tracing &amp; Observability (OpenTelemetry-Native)</h3>
<p>LangWatch&rsquo;s tracing layer is built directly on OpenTelemetry, which means it emits standard OTLP spans that integrate with any OTel-compatible backend — Datadog, Grafana, Jaeger, or your own collector. Unlike proprietary SDKs that create tight coupling, this architecture lets enterprise teams slot LangWatch into existing observability pipelines without forklift migrations. Each LLM call is captured as a span with latency, token counts, cost per request, model version, and the full prompt/response payload. For multi-step agent workflows, LangWatch assembles these spans into trace trees that show exactly which tool calls, LLM calls, and retrieval steps happened in which order. Teams at the 99th percentile latency can instantly see whether slowness originates from the LLM provider, a slow vector search, or downstream API calls. The Python and TypeScript SDKs require roughly 5 lines of instrumentation code, and LangChain/LlamaIndex auto-instrumentation is supported out of the box.</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-python" data-lang="python"><span style="display:flex;"><span><span style="color:#f92672">import</span> langwatch
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>langwatch<span style="color:#f92672">.</span>setup()
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#a6e22e">@langwatch.trace</span>()
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">def</span> <span style="color:#a6e22e">run_agent</span>(user_input: str):
</span></span><span style="display:flex;"><span>    <span style="color:#75715e"># Your existing agent code here</span>
</span></span><span style="display:flex;"><span>    response <span style="color:#f92672">=</span> your_llm_call(user_input)
</span></span><span style="display:flex;"><span>    <span style="color:#66d9ef">return</span> response
</span></span></code></pre></div><h3 id="real-time-and-offline-evaluations">Real-Time and Offline Evaluations</h3>
<p>LangWatch&rsquo;s evaluation system runs both in real-time (production traffic) and offline (batch runs against datasets). The platform ships with 20+ built-in evaluators covering hallucination detection, toxicity, faithfulness, answer relevance, and custom LLM-as-judge configurations. Real-time evaluators run as async side-effects — they don&rsquo;t add latency to the production path but flag problematic outputs within seconds for review. Offline evals let teams run systematic regression tests against historical traces or synthetic datasets before deploying prompt changes. The no-code evaluator builder allows product managers and QA engineers to define custom quality criteria using natural language, without writing Python. This bridges a common organizational gap where developers build the system but non-technical stakeholders own the quality bar. Teams can set automated alerts when eval scores drop below thresholds, making LangWatch function as a quality gating system in CI/CD pipelines.</p>
<h3 id="agent-simulation-testing-scenario-framework">Agent Simulation Testing (Scenario Framework)</h3>
<p>LangWatch&rsquo;s Scenario framework is its most differentiated feature in 2026 — a purpose-built system for simulating multi-turn agent conversations before they hit production. Unlike unit tests that mock LLM responses, Scenario runs real LLM calls against a configurable simulated user and evaluates the full conversation arc for task completion, safety adherence, and edge-case handling. You define a scenario with an agent goal, a simulated user persona, and pass/fail criteria; LangWatch then runs the conversation repeatedly to measure success rates across different inputs. This is especially valuable for customer-facing agents where a single off-rail response can damage user trust. The framework integrates with pytest, so it slots into existing CI workflows. In March 2026, LangWatch open-sourced the full Scenario framework, making it available to the entire community at no cost.</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-python" data-lang="python"><span style="display:flex;"><span><span style="color:#f92672">import</span> scenario
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#a6e22e">@scenario.test</span>()
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">async</span> <span style="color:#66d9ef">def</span> <span style="color:#a6e22e">test_support_agent</span>():
</span></span><span style="display:flex;"><span>    result <span style="color:#f92672">=</span> <span style="color:#66d9ef">await</span> scenario<span style="color:#f92672">.</span>run(
</span></span><span style="display:flex;"><span>        agent<span style="color:#f92672">=</span>your_support_agent,
</span></span><span style="display:flex;"><span>        user_goal<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;Get a refund for a broken product&#34;</span>,
</span></span><span style="display:flex;"><span>        criteria<span style="color:#f92672">=</span>[<span style="color:#e6db74">&#34;Agent escalates to human within 3 turns if unresolved&#34;</span>]
</span></span><span style="display:flex;"><span>    )
</span></span><span style="display:flex;"><span>    <span style="color:#66d9ef">assert</span> result<span style="color:#f92672">.</span>success
</span></span></code></pre></div><h3 id="prompt-management--dspy-optimization">Prompt Management &amp; DSPy Optimization</h3>
<p>LangWatch includes a version-controlled prompt registry with diff tracking, rollback, and A/B testing built in. Every prompt change is linked to trace data, so you can directly correlate a prompt edit with downstream eval score changes. The platform&rsquo;s DSPy integration — specifically MIPROv2 — turns prompt optimization from manual iteration into a measurable automated process. You define input/output examples and a metric, and DSPy systematically searches the prompt space to find configurations that maximize your score. LangWatch surfaces the optimization runs as first-class experiments with before/after comparisons. For teams spending hours on prompt crafting, this pipeline typically cuts iteration cycles from days to hours while producing prompts that are quantifiably better than human-authored baselines.</p>
<h3 id="ai-gateway--cost-control--governance">AI Gateway — Cost Control &amp; Governance</h3>
<p>LangWatch&rsquo;s AI Gateway sits as a proxy layer between your application and LLM providers, enabling centralized cost controls, rate limiting, PII masking, and provider failover. Teams can define spend caps per project or user, route traffic between providers based on cost/latency tradeoffs, and automatically redact sensitive data before it reaches the LLM API. The gateway logs every request through the same tracing pipeline, so cost analysis and trace analysis use the same data model. For enterprises running multiple AI products, the gateway provides the governance layer that finance and compliance teams require before approving production AI deployments.</p>
<h2 id="langwatch-pricing--free-tier-cloud-and-self-hosted">LangWatch Pricing — Free Tier, Cloud, and Self-Hosted</h2>
<p>LangWatch uses an unusually generous open-source model: the self-hosted version is fully free with no feature limits, meaning enterprises with the infrastructure capability get the full platform at zero license cost. This stands in contrast to competitors like LangSmith and Arize Phoenix, which gate advanced features behind paid tiers even for self-hosted deployments. The cloud-managed offering starts at €59/month (approximately $64/month), which covers hosted infrastructure, managed updates, and support SLAs. There is also a free cloud tier for individual developers and small projects. Enterprise contracts with SSO, dedicated support, and SLA guarantees are available by negotiation. The pricing model has strategic implications: teams evaluating LangWatch for enterprise deployment can run a full-featured production pilot on self-hosted infrastructure before committing to a cloud contract, eliminating the typical &ldquo;limited trial&rdquo; friction of SaaS-only tools.</p>
<table>
	<thead>
			<tr>
					<th>Tier</th>
					<th>Price</th>
					<th>Traces/month</th>
					<th>Features</th>
			</tr>
	</thead>
	<tbody>
			<tr>
					<td>Free Cloud</td>
					<td>€0</td>
					<td>Limited</td>
					<td>Core tracing, basic evals</td>
			</tr>
			<tr>
					<td>Pro Cloud</td>
					<td>€59/month</td>
					<td>Generous</td>
					<td>Full eval suite, prompt mgmt, gateway</td>
			</tr>
			<tr>
					<td>Enterprise Cloud</td>
					<td>Custom</td>
					<td>Unlimited</td>
					<td>SSO, SLA, dedicated support</td>
			</tr>
			<tr>
					<td>Self-Hosted OSS</td>
					<td>Free</td>
					<td>Unlimited</td>
					<td>All features, no limits</td>
			</tr>
	</tbody>
</table>
<h2 id="langwatch-vs-competitors-head-to-head-comparison">LangWatch vs Competitors: Head-to-Head Comparison</h2>
<p>LangWatch competes in a crowded LLMOps market against Langfuse (20k GitHub stars, the most widely deployed open-source option), Arize Phoenix (8.1k stars, strong ML observability heritage), and LangSmith (LangChain&rsquo;s proprietary platform). LangWatch&rsquo;s combination of open-source licensing, unified feature set, and the unique Scenario agent simulation framework creates a differentiated position — but it enters these comparisons with a smaller community (3k stars) and less mature ecosystem than the incumbents. The LLM observability market growing at 36.3% CAGR means all these platforms are gaining adoption simultaneously, so &ldquo;community size today&rdquo; is a less stable signal than it would be in a mature market. The right decision framework: identify your primary bottleneck — is it production visibility, evaluation automation, agent testing, cost governance, or compliance? Each tool leads on different dimensions. Understanding where LangWatch wins and loses is essential for making the right platform choice given your team&rsquo;s size, technical sophistication, and budget constraints. Vendor lock-in is a real concern: LangSmith&rsquo;s lack of self-hosting and proprietary trace format means switching costs compound over time, whereas LangWatch&rsquo;s OpenTelemetry-native approach keeps your data in a portable format from day one.</p>
<h3 id="langwatch-vs-langfuse">LangWatch vs Langfuse</h3>
<p>Langfuse leads the open-source LLMOps category by GitHub stars (20k vs LangWatch&rsquo;s 3k) and has a broader community and integration ecosystem. Langfuse excels at pure observability — its tracing UI is mature, well-documented, and widely used. However, LangWatch pulls ahead on evaluation depth (more built-in evaluators, real-time eval pipelines), agent simulation (Scenario has no direct Langfuse equivalent), and the DSPy optimization integration. Teams whose primary need is production tracing and cost visibility will find Langfuse satisfactory. Teams building multi-turn agent products that need systematic simulation testing should look seriously at LangWatch. On pricing, both offer self-hosted free tiers with cloud paid plans at comparable price points.</p>
<h3 id="langwatch-vs-arize-phoenix">LangWatch vs Arize Phoenix</h3>
<p>Arize Phoenix (8.1k GitHub stars) is strong on ML model monitoring heritage — it grew from traditional ML observability into LLM use cases and carries robust drift detection and data quality analysis from that lineage. Phoenix&rsquo;s strength is dataset-centric analysis and offline evaluation workflows. LangWatch is stronger on production real-time monitoring, the agent Scenario testing framework, and the integrated AI gateway. For teams with both traditional ML models and LLM applications in production, Phoenix&rsquo;s broader ML coverage may justify the choice. Pure LLM teams will find LangWatch&rsquo;s feature set more directly aligned to their workflow.</p>
<h3 id="langwatch-vs-langsmith">LangWatch vs LangSmith</h3>
<p>LangSmith (by LangChain) integrates tightly with the LangChain framework and benefits from the large LangChain developer community. If your stack is heavily LangChain-based, LangSmith&rsquo;s native integration reduces instrumentation friction. LangSmith&rsquo;s pricing is SaaS-only with a developer tier and paid plans; there is no self-hosted free option. LangWatch supports LangChain auto-instrumentation and is framework-agnostic, making it viable for teams using LlamaIndex, custom agents, or bare OpenAI SDK calls. For organizations sensitive to vendor lock-in or needing self-hosted deployment for compliance, LangWatch is the stronger choice.</p>
<table>
	<thead>
			<tr>
					<th>Feature</th>
					<th>LangWatch</th>
					<th>Langfuse</th>
					<th>Arize Phoenix</th>
					<th>LangSmith</th>
			</tr>
	</thead>
	<tbody>
			<tr>
					<td>Open Source</td>
					<td>✅ Full</td>
					<td>✅ Full</td>
					<td>✅ Full</td>
					<td>❌ Closed</td>
			</tr>
			<tr>
					<td>Self-hosted free</td>
					<td>✅ All features</td>
					<td>✅ All features</td>
					<td>✅ Limited</td>
					<td>❌</td>
			</tr>
			<tr>
					<td>Agent Simulation</td>
					<td>✅ Scenario</td>
					<td>❌</td>
					<td>❌</td>
					<td>❌</td>
			</tr>
			<tr>
					<td>Real-time Evals</td>
					<td>✅</td>
					<td>✅</td>
					<td>❌</td>
					<td>✅</td>
			</tr>
			<tr>
					<td>DSPy Optimization</td>
					<td>✅ Native</td>
					<td>❌</td>
					<td>❌</td>
					<td>❌</td>
			</tr>
			<tr>
					<td>AI Gateway</td>
					<td>✅</td>
					<td>❌</td>
					<td>❌</td>
					<td>❌</td>
			</tr>
			<tr>
					<td>GitHub Stars</td>
					<td>3k+</td>
					<td>20k</td>
					<td>8.1k</td>
					<td>N/A</td>
			</tr>
			<tr>
					<td>Cloud Pricing</td>
					<td>€59/mo</td>
					<td>~$59/mo</td>
					<td>Custom</td>
					<td>$39/mo</td>
			</tr>
			<tr>
					<td>OpenTelemetry</td>
					<td>✅ Native</td>
					<td>✅</td>
					<td>✅</td>
					<td>❌</td>
			</tr>
			<tr>
					<td>LangChain Support</td>
					<td>✅</td>
					<td>✅</td>
					<td>✅</td>
					<td>✅ Native</td>
			</tr>
	</tbody>
</table>
<h2 id="who-should-use-langwatch-ideal-use-cases">Who Should Use LangWatch? (Ideal Use Cases)</h2>
<p>LangWatch is best suited for teams building production-grade LLM applications who need more than basic logging but don&rsquo;t want to stitch together five separate tools. The platform&rsquo;s sweet spot is AI engineering teams of 2–15 developers working on customer-facing products where quality and cost control matter at scale. Specifically, LangWatch delivers the most value in three scenarios: (1) Teams building multi-turn AI agents for customer service, coding assistance, or workflow automation — where the Scenario simulation framework provides a systematic way to validate agent behavior before shipping; (2) Organizations in regulated industries (fintech, healthcare, legal) that need self-hosted deployment for data privacy compliance, since the full-featured OSS version eliminates cloud data residency concerns; and (3) AI product teams with non-technical stakeholders involved in quality decisions, where the no-code evaluation builder lets product managers define and monitor quality criteria without developer intervention. Startups building their first LLM product are well-served by the free tier for prototyping, with a clear upgrade path as traffic grows.</p>
<p><strong>Ideal users:</strong></p>
<ul>
<li>AI engineers building production chatbots, coding assistants, or RAG pipelines</li>
<li>Teams requiring GDPR/HIPAA-compliant self-hosted LLM monitoring</li>
<li>Product teams that need cross-functional quality review workflows</li>
<li>Companies optimizing LLM costs at scale (10k+ requests/day)</li>
</ul>
<p><strong>Less ideal for:</strong></p>
<ul>
<li>Teams using traditional ML models only (no LLM component)</li>
<li>Solo developers needing only basic API call logging</li>
<li>Orgs heavily locked into LangChain who can tolerate LangSmith&rsquo;s lack of self-hosting</li>
</ul>
<h2 id="langwatch-pros-and-cons--honest-assessment">LangWatch Pros and Cons — Honest Assessment</h2>
<p>LangWatch&rsquo;s strongest cards are its self-hosted free tier with zero feature gates, the Scenario agent simulation framework (genuinely unique in the market), and the OpenTelemetry-native architecture that avoids vendor lock-in. The DSPy optimization integration is a differentiator for teams serious about systematic prompt engineering. The pricing model is unusually fair — self-hosting costs only infrastructure, not licenses — which matters for enterprises doing cost-benefit analysis against SaaS alternatives. However, LangWatch&rsquo;s 3k GitHub stars vs Langfuse&rsquo;s 20k reflects a smaller community, fewer third-party integrations, and less battle-tested documentation. Teams hitting edge cases in instrumentation will find more Stack Overflow answers and community solutions for Langfuse. The platform is also earlier-stage — the Scenario framework launched in 2026 and while technically impressive, its tooling will mature with more production usage. The UI is functional but less polished than Langfuse&rsquo;s at this stage.</p>
<p><strong>Pros:</strong></p>
<ul>
<li>Fully-featured self-hosted version at zero license cost</li>
<li>Scenario framework for multi-turn agent simulation testing</li>
<li>OpenTelemetry-native (no vendor lock-in)</li>
<li>20+ built-in evaluators including real-time hallucination detection</li>
<li>AI Gateway with cost controls and PII masking built in</li>
<li>DSPy integration for automated prompt optimization</li>
<li>No-code eval builder for non-technical stakeholders</li>
</ul>
<p><strong>Cons:</strong></p>
<ul>
<li>Smaller community than Langfuse (3k vs 20k stars)</li>
<li>Scenario framework is new (launched 2026) — less mature</li>
<li>UI less polished than some competitors</li>
<li>Fewer pre-built integrations in the marketplace</li>
<li>Documentation gaps in advanced configuration</li>
</ul>
<h2 id="how-to-get-started-with-langwatch-quick-setup-guide">How to Get Started with LangWatch (Quick Setup Guide)</h2>
<p>Getting started with LangWatch takes under 15 minutes for a basic tracing setup. The cloud free tier requires only an account creation at langwatch.ai, while self-hosted deployment uses Docker Compose for local setup or Helm charts for Kubernetes. The Python SDK is pip-installable and adds 5 lines of code to instrument your first LLM application. For production deployments, the recommended path is to start with cloud free tier to validate the platform&rsquo;s fit, then migrate to self-hosted if data residency requirements emerge.</p>
<p><strong>Cloud quick start:</strong></p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-bash" data-lang="bash"><span style="display:flex;"><span>pip install langwatch
</span></span></code></pre></div><div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-python" data-lang="python"><span style="display:flex;"><span><span style="color:#f92672">import</span> langwatch
</span></span><span style="display:flex;"><span><span style="color:#f92672">import</span> openai
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>langwatch<span style="color:#f92672">.</span>setup()  <span style="color:#75715e"># Reads LANGWATCH_API_KEY from env</span>
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#a6e22e">@langwatch.trace</span>()
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">def</span> <span style="color:#a6e22e">chat</span>(user_message: str) <span style="color:#f92672">-&gt;</span> str:
</span></span><span style="display:flex;"><span>    client <span style="color:#f92672">=</span> openai<span style="color:#f92672">.</span>OpenAI()
</span></span><span style="display:flex;"><span>    response <span style="color:#f92672">=</span> client<span style="color:#f92672">.</span>chat<span style="color:#f92672">.</span>completions<span style="color:#f92672">.</span>create(
</span></span><span style="display:flex;"><span>        model<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;gpt-4o&#34;</span>,
</span></span><span style="display:flex;"><span>        messages<span style="color:#f92672">=</span>[{<span style="color:#e6db74">&#34;role&#34;</span>: <span style="color:#e6db74">&#34;user&#34;</span>, <span style="color:#e6db74">&#34;content&#34;</span>: user_message}]
</span></span><span style="display:flex;"><span>    )
</span></span><span style="display:flex;"><span>    <span style="color:#66d9ef">return</span> response<span style="color:#f92672">.</span>choices[<span style="color:#ae81ff">0</span>]<span style="color:#f92672">.</span>message<span style="color:#f92672">.</span>content
</span></span></code></pre></div><p><strong>Self-hosted Docker Compose:</strong></p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-bash" data-lang="bash"><span style="display:flex;"><span>git clone https://github.com/langwatch/langwatch
</span></span><span style="display:flex;"><span>cd langwatch
</span></span><span style="display:flex;"><span>cp .env.example .env
</span></span><span style="display:flex;"><span>docker compose up -d
</span></span></code></pre></div><p>After setup, the LangWatch dashboard shows incoming traces within seconds. The evaluation pipeline requires one additional configuration step — selecting which evaluators to run and setting alert thresholds — which the UI guides through a wizard. For LangChain or LlamaIndex users, auto-instrumentation means zero code changes beyond the <code>langwatch.setup()</code> call.</p>
<h2 id="verdict--is-langwatch-worth-it-in-2026">Verdict — Is LangWatch Worth It in 2026?</h2>
<p>LangWatch is worth serious evaluation for any team running LLM applications in production, and it&rsquo;s the strongest choice for teams building AI agents who need systematic simulation testing before deployment. The self-hosted free tier with no feature limits removes the usual &ldquo;premium features require payment&rdquo; barrier that makes evaluating LLMOps tools frustrating. If your organization has infrastructure to self-host, you can run LangWatch at full capability with zero software cost — a meaningful advantage over SaaS-only competitors billing $500–$2,000/month at scale. The Scenario simulation framework is the feature that puts LangWatch ahead of the field for agent developers specifically: there is no comparable open-source tool for systematically testing multi-turn agent conversations before production. The tradeoffs are real — smaller community, newer feature surface, less polished UX — but for a team prioritizing agent testing and compliance-safe self-hosting, these are acceptable costs. For teams whose primary need is mature tracing with a large community, Langfuse remains the safer choice. For teams building the next generation of AI agents, LangWatch&rsquo;s 2026 feature set is purpose-built for the job.</p>
<p><strong>Bottom line:</strong> Start with the free cloud tier for a 2-week pilot. If agent simulation testing and self-hosted deployment matter to your roadmap, migrate to self-hosted or upgrade to Pro. The platform earns its keep at both price points.</p>
<hr>
<h2 id="faq">FAQ</h2>
<p>These are the most common questions developers ask when evaluating LangWatch for production LLM and agent application monitoring. The answers cover pricing, competitive positioning, provider support, compliance, and the Scenario testing framework. LangWatch&rsquo;s documentation at langwatch.ai covers setup in depth; these answers focus on the evaluation and decision questions that documentation typically doesn&rsquo;t address. Key takeaway before diving in: LangWatch&rsquo;s self-hosted free tier with full feature parity is the platform&rsquo;s most frequently misunderstood advantage — most developers assume &ldquo;open source&rdquo; means &ldquo;limited version&rdquo; until they discover there are no feature gates on the OSS build. That one fact changes the cost calculus for most teams compared to SaaS-only alternatives charging $500–$2,000/month at production scale. With the LLM observability market projected to reach $9.26 billion by 2030 and Gartner projecting 50% of GenAI deployments will include observability investment by 2028, the platform you choose today will likely remain embedded in your stack for years.</p>
<h3 id="is-langwatch-free-to-use">Is LangWatch free to use?</h3>
<p>Yes. LangWatch offers a free cloud tier for individual developers and a fully-featured self-hosted open-source version with no license cost and no feature gates. The cloud Pro plan starts at €59/month for teams needing managed infrastructure. Enterprise contracts with dedicated SLAs are negotiated separately. Self-hosted deployment costs only your infrastructure — compute and storage — making it effectively free at low-to-moderate scale.</p>
<h3 id="how-does-langwatch-compare-to-langfuse">How does LangWatch compare to Langfuse?</h3>
<p>LangWatch and Langfuse are both open-source LLMOps platforms with similar pricing. Langfuse leads on community size (20k GitHub stars vs 3k) and integration maturity. LangWatch leads on evaluation depth, the Scenario agent simulation framework (Langfuse has no equivalent), the AI Gateway feature, and DSPy optimization integration. Teams doing pure tracing and cost monitoring should consider Langfuse; teams building multi-turn agents should evaluate LangWatch&rsquo;s Scenario capabilities directly.</p>
<h3 id="what-llm-providers-does-langwatch-support">What LLM providers does LangWatch support?</h3>
<p>LangWatch supports 10+ LLM providers out of the box: OpenAI, Azure OpenAI, AWS Bedrock, Google Gemini, Google VertexAI, Deepseek, Groq, MistralAI, LiteLLM, and others via its OpenTelemetry-native SDK. The AI Gateway layer supports provider failover and load balancing across providers. New providers can be added through the OpenTelemetry instrumentation layer without waiting for first-party SDK support.</p>
<h3 id="can-langwatch-be-self-hosted-for-gdpr-compliance">Can LangWatch be self-hosted for GDPR compliance?</h3>
<p>Yes. The LangWatch self-hosted version runs entirely within your infrastructure — LLM traces, evaluation results, and prompt data never leave your environment. This makes it suitable for GDPR, HIPAA, and other data residency compliance requirements. The Docker Compose and Helm chart deployment options support air-gapped environments. The self-hosted version includes all features of the paid cloud tier at no license cost.</p>
<h3 id="what-is-the-langwatch-scenario-framework">What is the LangWatch Scenario framework?</h3>
<p>Scenario is LangWatch&rsquo;s agent simulation testing framework, open-sourced in March 2026. It enables developers to define multi-turn conversation tests with a simulated user persona, pass/fail criteria, and automated success measurement — running real LLM calls rather than mocked responses. This lets teams systematically test AI agent behavior across edge cases before production deployment. Scenario integrates with pytest for CI/CD inclusion and is the only open-source tool of its kind for agent conversation simulation.</p>
]]></content:encoded></item></channel></rss>