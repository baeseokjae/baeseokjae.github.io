<?xml version="1.0" encoding="utf-8" standalone="yes"?><rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom" xmlns:content="http://purl.org/rss/1.0/modules/content/"><channel><title>Agent Memory on RockB</title><link>https://baeseokjae.github.io/tags/agent-memory/</link><description>Recent content in Agent Memory on RockB</description><image><title>RockB</title><url>https://baeseokjae.github.io/images/og-default.png</url><link>https://baeseokjae.github.io/images/og-default.png</link></image><generator>Hugo</generator><language>en-us</language><lastBuildDate>Mon, 24 Aug 2026 01:02:43 +0000</lastBuildDate><atom:link href="https://baeseokjae.github.io/tags/agent-memory/index.xml" rel="self" type="application/rss+xml"/><item><title>Heimdall: A Verified, Self-Healing Knowledge Layer for AI Coding Agents</title><link>https://baeseokjae.github.io/posts/heimdall-verified-knowledge-layer/</link><pubDate>Mon, 24 Aug 2026 01:02:43 +0000</pubDate><guid>https://baeseokjae.github.io/posts/heimdall-verified-knowledge-layer/</guid><description>Heimdall is a CPU-only, zero-token knowledge layer that gives AI coding agents trust-verified, self-healing memory across every repo.</description><content:encoded><![CDATA[<p>Heimdall is an open-source, CPU-only knowledge layer that gives AI coding agents a verified, self-healing memory across every repository you work in. Instead of returning plausible-but-unverified matches, every search result carries a trust verdict — STRONG, WEAK, REBUILT, or STALE — re-checked against your live filesystem at query time. It indexes with tree-sitter and local embeddings, spends zero tokens on memory maintenance, and re-anchors moved files automatically.</p>
<h2 id="what-is-heimdall-and-why-does-it-exist">What is Heimdall and why does it exist?</h2>
<p>Heimdall is a trust-verified knowledge layer built specifically for AI coding agents. It was released on Hacker News on August 22, 2026, and is published on npm as <code>@ariantdeva/heimdall</code> at version 0.2.1. The project&rsquo;s core claim is simple: most agent memory tools return results that <em>look</em> right but have not been checked against the actual state of your codebase. Heimdall exists to close that gap.</p>
<p>The tool maintains persistent memory across every repo and project you touch. A single <code>kb_search</code> call replaces the familiar grep/find/ls orientation loop that agents run at the start of every task. Because the knowledge graph is cross-repository, knowledge follows the developer across projects, languages, and months of work — rather than being trapped in whichever directory the agent happens to be standing in.</p>
<h2 id="the-problem-memory-that-doesnt-live-in-one-repo-and-the-orientation-tax">The problem: memory that doesn&rsquo;t live in one repo, and the orientation tax</h2>
<p>Coding agents face two compounding problems that most memory tools ignore.</p>
<p>First, <strong>knowledge is fragmented across repositories.</strong> A developer working on a monorepo, a shared library, and a documentation site has context scattered across three different directory trees. An agent that only sees the current working directory is blind to the other 80% of the relevant context. Per-project memory tools make this worse by design — they scope their index to a single directory and never look beyond it.</p>
<p>Second, <strong>agents pay a heavy orientation tax.</strong> Before an agent can do useful work, it must figure out where things live: which file defines a symbol, which module imports it, where the config lives. This is the grep/find/ls loop, and it burns tokens and latency on every single task. Heimdall&rsquo;s pitch is that a verified knowledge layer turns this repeated discovery work into a single indexed lookup.</p>
<p>The deeper problem is that even when an agent finds a match, it has no way to know whether that match is still true. Files move, get deleted, or get rewritten. A memory entry that was accurate last week can be a confident lie today. This is the &ldquo;rotten anchors&rdquo; problem, and it is the specific failure mode Heimdall was designed to solve.</p>
<h2 id="trust-verdicts-strong-weak-rebuilt-and-stale-explained">Trust verdicts: STRONG, WEAK, REBUILT, and STALE explained</h2>
<p>The differentiator that sets Heimdall apart is its trust-verdict system. Every search result is labeled with one of four verdicts, and crucially, these verdicts are <strong>re-verified against the live filesystem at query time</strong> — not stored as stale metadata.</p>
<table>
  <thead>
      <tr>
          <th>Verdict</th>
          <th>Meaning</th>
          <th>When it appears</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td><strong>STRONG</strong></td>
          <td>The result is verified against the current filesystem and is accurate</td>
          <td>The file exists, the symbol is present, nothing has changed</td>
      </tr>
      <tr>
          <td><strong>WEAK</strong></td>
          <td>The result is plausible but could not be fully verified</td>
          <td>A fuzzy semantic match, or a path that exists but whose contents are uncertain</td>
      </tr>
      <tr>
          <td><strong>REBUILT</strong></td>
          <td>The result was re-anchored after the file moved</td>
          <td>A moved file was automatically re-linked to its new location</td>
      </tr>
      <tr>
          <td><strong>STALE</strong></td>
          <td>The result no longer matches the filesystem</td>
          <td>The file was deleted or rewritten; the node is being retracted</td>
      </tr>
  </tbody>
</table>
<p>This matters because most memory tools return matches that are <em>plausible but unverified</em>. An LLM-based memory layer might recall that &ldquo;the auth module lives in <code>src/auth.ts</code>&rdquo; — but if that file was renamed to <code>src/authentication/index.ts</code> last week, the memory is confidently wrong. Heimdall&rsquo;s verdicts make the reliability of each result explicit, so an agent can decide whether to act on it or re-orient.</p>
<h2 id="self-healing-knowledge-graph-how-moved-files-get-re-anchored">Self-healing knowledge graph: how moved files get re-anchored</h2>
<p>Heimdall&rsquo;s knowledge graph is self-healing. When a file is moved, the graph does not leave a dead pointer behind — it <strong>re-anchors</strong> the node to the file&rsquo;s new location and marks the result as REBUILT. When a file is deleted, the graph <strong>retracts its own nodes</strong>, pruning dead paths automatically.</p>
<p>This directly solves the &ldquo;rotten anchors&rdquo; problem that other tools ignore. In a typical memory system, a moved file leaves behind a stale entry that keeps surfacing in search results, wasting the agent&rsquo;s time and eroding trust in the tool. Heimdall&rsquo;s reconciler notices that a path &ldquo;might have changed&rdquo; and re-verifies it, so the graph stays honest about what is actually on disk.</p>
<p>The self-healing behavior is not a gimmick — it is the property that makes the trust verdicts trustworthy. A verdict is only meaningful if the graph actively repairs itself when the underlying filesystem changes. Heimdall couples the two: because the graph heals, the verdicts stay accurate; because the verdicts are re-checked live, the graph&rsquo;s self-repair is verifiable.</p>
<h2 id="zero-token-spend-cpu-only-indexing-with-tree-sitter-and-local-embeddings">Zero token spend: CPU-only indexing with tree-sitter and local embeddings</h2>
<p>One of Heimdall&rsquo;s most distinctive engineering choices is that it <strong>never spends a token on memory maintenance</strong>. Indexing and search run entirely on CPU using:</p>
<ul>
<li><strong>tree-sitter</strong> for AST parsing, which extracts structural knowledge about your code without an LLM</li>
<li><strong>local bge-m3 embeddings</strong> for semantic understanding, computed on your own machine</li>
<li><strong>sqlite</strong> for storage</li>
<li><strong>hybrid ranked search</strong> that combines lexical, semantic, and graph-walk signals</li>
</ul>
<p>This is a deliberate contrast with LLM-based memory tools like mem0 and Letta, which use language models to extract and manage memory — and therefore incur token cost on every write and every consolidation pass. Heimdall&rsquo;s approach means memory maintenance is effectively free, and it works on machines without a GPU.</p>
<p>The trade-off is real: LLM-based extraction can capture higher-level intent that tree-sitter parsing misses. But for the specific job of <em>knowing where things live in a codebase</em>, structural parsing plus local embeddings is often sufficient — and it is dramatically cheaper to run continuously.</p>
<h2 id="architecture-the-single-writer-reconciler-and-why-it-matters">Architecture: the single-writer reconciler and why it matters</h2>
<p>Heimdall&rsquo;s architecture contains a design lesson worth studying. The project&rsquo;s v0.2.0 release introduced a <strong>single-writer, level-triggered reconciler</strong>. The key insight is that nothing tells the graph <em>what</em> changed — only that a path <em>might</em> have changed.</p>
<p>The author&rsquo;s earlier approach tried to infer graph mutations from bash hooks, and it collapsed. Hooks are unreliable: they fire inconsistently, they miss changes made by other tools, and they create a many-writer problem where concurrent mutations corrupt the graph. The fix was to abandon fine-grained change detection entirely and instead use a level-triggered reconciler: a single writer periodically checks whether paths might have changed and re-verifies them.</p>
<p>This is a clean architectural decision. By making reconciliation idempotent and level-triggered rather than edge-triggered, Heimdall avoids the classic distributed-systems failure mode where a missed event leaves the system permanently out of sync. The single-writer constraint guarantees that the graph is never mutated concurrently, which is what makes the self-healing behavior safe.</p>
<h2 id="heimdall-vs-mem0-letta-claude-memory-and-grep">Heimdall vs mem0, Letta, Claude Memory, and grep</h2>
<p>To understand where Heimdall fits, it helps to compare it directly against the alternatives.</p>
<table>
  <thead>
      <tr>
          <th>Tool</th>
          <th>Scope</th>
          <th>Trust verdicts</th>
          <th>Self-healing</th>
          <th>Indexing cost</th>
          <th>Stars</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td><strong>Heimdall</strong></td>
          <td>Cross-repo</td>
          <td>Yes (STRONG/WEAK/REBUILT/STALE)</td>
          <td>Yes (re-anchors moved files)</td>
          <td>CPU-only, zero tokens</td>
          <td>34</td>
      </tr>
      <tr>
          <td><strong>mem0</strong></td>
          <td>Per-app/per-user</td>
          <td>No</td>
          <td>No</td>
          <td>LLM-based (token cost)</td>
          <td>63,893</td>
      </tr>
      <tr>
          <td><strong>Letta (MemGPT)</strong></td>
          <td>Per-agent</td>
          <td>No</td>
          <td>No</td>
          <td>LLM-based (token cost)</td>
          <td>24,375</td>
      </tr>
      <tr>
          <td><strong>Engram</strong></td>
          <td>Cross-tool (MCP)</td>
          <td>No</td>
          <td>Partial (consolidation)</td>
          <td>LLM-based</td>
          <td>~2.5K installs</td>
      </tr>
      <tr>
          <td><strong>grep / cAST</strong></td>
          <td>Current repo</td>
          <td>No</td>
          <td>No</td>
          <td>Free</td>
          <td>—</td>
      </tr>
  </tbody>
</table>
<p>The comparison makes Heimdall&rsquo;s positioning clear. It is the only tool in this group that combines <strong>trust verdicts, self-healing re-anchoring, cross-repo scope, and CPU-only indexing</strong>. mem0 and Letta are far more mature and widely adopted, but they rely on LLM-based extraction (token cost) and do not verify their results against the live filesystem. Engram offers persistent memory via MCP and reports 80% accuracy on the LOCOMO benchmark, but it does not provide trust verdicts or re-anchor moved files.</p>
<p>The honest caveat is maturity. Heimdall has 34 GitHub stars and is at version 0.2.1, while mem0 has 63,893 stars and Letta has 24,375. For a production team that needs battle-tested memory, the mature options are safer. For a developer who wants verified, self-healing, zero-token memory and is willing to run early-stage software, Heimdall&rsquo;s feature set is currently unique.</p>
<h2 id="how-to-install-and-wire-it-into-claude-code-codex-cursor-and-windsurf">How to install and wire it into Claude Code, Codex, Cursor, and Windsurf</h2>
<p>Heimdall ships as a global npm package and integrates with the major coding-agent harnesses.</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-bash" data-lang="bash"><span style="display:flex;"><span>npm i -g @ariantdeva/heimdall
</span></span></code></pre></div><p>The tool provides harness integrations for <strong>pi, Claude Code, Codex, Cursor, and Windsurf</strong>. Once installed, the agent can call <code>kb_search</code> to query the verified knowledge layer instead of running the grep/find/ls orientation loop. Because the knowledge graph is cross-repository, the same index serves the agent regardless of which harness or which repo it is currently working in.</p>
<p>Note that the MCP server mode is still on the roadmap, and the ranked-search backend currently depends on a graft backend. For most users, the harness integrations are the practical path today; MCP-based tooling (like Engram&rsquo;s approach) is not yet available for Heimdall.</p>
<h2 id="benchmarks-and-maturity-longmemeval-the-test-suite-and-the-roadmap">Benchmarks and maturity: LongMemEval, the test suite, and the roadmap</h2>
<p>Heimdall&rsquo;s maturity story is honest about where it stands. The project reports:</p>
<ul>
<li>A reproduced <strong>LongMemEval benchmark baseline S score of 0.740</strong>, with a roadmap target of <strong>LongMemEval-S score ≥ 0.90</strong></li>
<li>A <strong>166-test suite</strong> guarding concurrency invariants</li>
<li>Daily runs on the author&rsquo;s machine across <strong>~12,800 live nodes</strong></li>
<li><strong>34 GitHub stars, 1 fork</strong>, MIT license, JavaScript, created 2026-08-20, updated 2026-08-24</li>
<li>npm version <strong>0.2.1</strong></li>
</ul>
<p>The LongMemEval baseline of 0.740 is a meaningful starting point, but the gap to the 0.90 target is substantial. The 166-test suite focused on concurrency invariants is a good sign for a tool whose core value proposition depends on a single-writer reconciler being correct under load. The daily 12,800-node run is real-world validation, but it is one machine — not a broad user base.</p>
<h2 id="verdict-who-should-use-heimdall-today-and-whats-still-missing">Verdict: who should use Heimdall today, and what&rsquo;s still missing</h2>
<p>Heimdall is a genuinely novel tool with a feature combination that no established competitor offers: <strong>trust verdicts, self-healing re-anchoring, cross-repo scope, and CPU-only, zero-token indexing</strong>. For a solo developer or small team that lives across multiple repositories and wants their coding agent to stop re-orienting on every task, it is worth trying today — especially if you value the zero-token design and the verified results.</p>
<p>What&rsquo;s still missing is maturity and ecosystem. At 34 stars and v0.2.1, Heimdall is early-stage. The MCP server mode is on the roadmap but not shipped. The ranked-search backend depends on a graft backend. The LongMemEval score needs to climb from 0.740 toward the 0.90 target. And the author&rsquo;s own Show HN post scored only 4 points, so the project has not yet attracted broad attention.</p>
<p>The pragmatic recommendation: if you are evaluating agent memory and want the safest choice, mem0 or Letta are the proven options. If you want the <em>only</em> tool that verifies its results, heals its own graph, and costs zero tokens to maintain, Heimdall is the one to watch — and to try on a side project before trusting it with production context.</p>
<h2 id="faq">FAQ</h2>
<p><strong>What is a verified knowledge layer for coding agents?</strong>
A verified knowledge layer is a memory system that checks its stored knowledge against the live filesystem before returning it. Heimdall labels every search result with a trust verdict — STRONG, WEAK, REBUILT, or STALE — so agents know whether a match is still accurate.</p>
<p><strong>How is Heimdall different from mem0 and Letta?</strong>
Heimdall is the only one of the three that provides trust verdicts, self-healing re-anchoring of moved files, and CPU-only zero-token indexing. mem0 and Letta use LLM-based extraction (token cost) and do not verify results against the filesystem.</p>
<p><strong>Does Heimdall cost tokens to maintain memory?</strong>
No. Heimdall indexes with tree-sitter AST parsing and local bge-m3 embeddings, all on CPU. Memory maintenance never requires an LLM call or a GPU.</p>
<p><strong>Which coding agents does Heimdall integrate with?</strong>
Heimdall provides harness integrations for pi, Claude Code, Codex, Cursor, and Windsurf, installed globally via <code>npm i -g @ariantdeva/heimdall</code>. MCP server mode is still on the roadmap.</p>
<p><strong>Is Heimdall production-ready?</strong>
It is early-stage: 34 GitHub stars, version 0.2.1, and a LongMemEval baseline of 0.740 (target ≥ 0.90). It is best tried on a side project before being trusted with production context.</p>
]]></content:encoded></item><item><title>AgentBrain Review: Local-First Agent Memory via a Markdown Vault</title><link>https://baeseokjae.github.io/posts/agentbrain-local-markdown-memory/</link><pubDate>Sun, 23 Aug 2026 07:01:16 +0000</pubDate><guid>https://baeseokjae.github.io/posts/agentbrain-local-markdown-memory/</guid><description>AgentBrain gives AI agents local-first long-term memory stored as plain Markdown. Here&amp;#39;s how it works, how it compares to EverOS, and whether it fits your workflow.</description><content:encoded><![CDATA[<p>AgentBrain is a local-first, long-term memory tool for AI agents that stores everything as a plain Markdown vault. Instead of locking your agent&rsquo;s memory into a proprietary database or cloud API, it gives you an append-only Markdown folder you can read, edit, grep, and version with Git. It uses index-first retrieval with CJK-aware BM25 to stay token-efficient, and enforces human-approved consolidation to prevent multi-agent write conflicts.</p>
<h2 id="what-is-agentbrain-a-local-first-markdown-memory-vault-for-ai-agents">What Is AgentBrain? A Local-First Markdown Memory Vault for AI Agents</h2>
<p>AgentBrain is an open-source Python tool (Python 3.10+) that gives AI agents a durable, local-first memory layer stored as plain Markdown files. The core idea is simple: an agent&rsquo;s long-term knowledge should live in a human-readable folder on your own machine, not inside a black-box database or a third-party cloud service.</p>
<p>The project installs via the PyPI package <code>mnemosyne-lite</code> — the name &ldquo;agentbrain&rdquo; was already taken on PyPI — but the CLI command remains <code>agentbrain</code>. This packaging quirk aside, the philosophy is clear: your agent&rsquo;s memory is your data, formatted as plain text, portable across tools, and readable even if you delete the tool itself.</p>
<p>The vault follows a structured layout: an <code>Index.md</code> file acts as a cheap first retrieval layer, a lessons folder holds agent-authored knowledge, and a <code>_consolidations/</code> directory stores human-approved proposals that merge and refine older entries. Because everything is Markdown, the same folder is instantly usable in Obsidian, grep, or Git — no vendor lock-in.</p>
<h2 id="why-local-first-markdown-memory-matters-and-the-category-is-exploding">Why Local-First Markdown Memory Matters (and the Category Is Exploding)</h2>
<p>Long-term memory is one of the hardest unsolved problems in agentic AI. Context windows are finite, so agents forget everything between sessions unless something persists knowledge. Cloud memory solves the persistence problem but introduces privacy, cost, and latency trade-offs. Local-first memory sidesteps all three.</p>
<p>The category is growing fast. Hacker News Algolia indexes 68 stories tagged &ldquo;local-first agent memory&rdquo; and 219 stories tagged &ldquo;MCP server memory&rdquo; (searched 2026-08-23), a strong signal that developers are actively experimenting with persistent agent memory. The category-leading project, EverOS, has 12,345 GitHub stars, demonstrating real, sustained market demand.</p>
<p>There are three concrete reasons developers are gravitating to Markdown for agent memory:</p>
<ol>
<li><strong>Transparency and auditability.</strong> A Markdown file is readable by humans and agents alike. You always know exactly what your agent &ldquo;remembers.&rdquo;</li>
<li><strong>No lock-in.</strong> A Markdown folder is portable. If the tool vanishes, your data survives in a universally readable format.</li>
<li><strong>Privacy and cost.</strong> Everything runs locally — no secrets leave your machine, and there is no recurring API bill for memory storage.</li>
</ol>
<h2 id="how-agentbrain-works--index-first-retrieval-and-cjk-aware-bm25">How AgentBrain Works — Index-First Retrieval and CJK-Aware BM25</h2>
<p>AgentBrain&rsquo;s retrieval is designed around a simple principle: keep token usage low by avoiding expensive full scans. It uses an <strong>index-first</strong> strategy where <code>Index.md</code> acts as a cheap first layer to identify relevant files before any expensive content is fetched.</p>
<p>Here is the retrieval pipeline in plain terms:</p>
<ul>
<li><strong>Layer 1 — Index scan.</strong> The lightweight <code>Index.md</code> is searched first to shortlist candidate entries without reading the full vault.</li>
<li><strong>Layer 2 — CJK-aware BM25 ranking.</strong> Candidates are ranked with a BM25 scoring function that is tuned for Chinese, Japanese, and Korean characters, not just Latin text. This matters because most competitor tools tokenize English well but stumble on CJK scripts.</li>
<li><strong>Layer 3 — Compact output.</strong> By default the tool returns in <code>mode='index'</code>, producing a compact token-efficient summary instead of dumping raw content. This keeps context usage low.</li>
</ul>
<p>The token-efficiency focus is a deliberate differentiator. Where a heavyweight &ldquo;memory layer&rdquo; might load large swaths of context into every agent call, AgentBrain tries to hand the agent only the minimal, most relevant knowledge it needs.</p>
<h2 id="key-design-decisions-append-only-writes-human-approved-consolidation-secret-scanning">Key Design Decisions: Append-Only Writes, Human-Approved Consolidation, Secret Scanning</h2>
<p>AgentBrain makes three design decisions that set it apart from simpler Markdown-memory tools:</p>
<h3 id="append-only-agent-writes">Append-Only Agent Writes</h3>
<p>Agents are only allowed to <strong>create</strong> lessons — they can never edit or delete existing entries. This is a deliberate guard against the classic multi-agent failure mode where two agents overwrite each other&rsquo;s knowledge and corrupt the vault. By making writes append-only, AgentBrain ensures history is always preserved and conflicts are never silently resolved by whichever agent writes last.</p>
<h3 id="human-approved-consolidation">Human-Approved Consolidation</h3>
<p>Because agents cannot edit, how does memory get merged or pruned? Through consolidation proposals. Agents write consolidation proposals into <code>_consolidations/</code>, and a human reviews and approves them before they take effect. This adds a human-in-the-loop quality gate that prevents stale, duplicated, or contradictory knowledge from accumulating — at the cost of manual review overhead.</p>
<h3 id="secret-scanning-v041">Secret Scanning (v0.4.1)</h3>
<p>Since v0.4.1, AgentBrain scans for credential-shaped content — <code>sk-</code> keys, <code>ghp_</code> tokens, <code>AKIA</code> AWS keys, <code>xox-</code> Slack tokens, <code>AIza</code> Google API keys, bearer tokens, and private-key blocks — and <strong>refuses ingestion</strong> of anything that looks like a secret. Most agent memory tools silently ingest credentials into a plaintext vault; AgentBrain treats that as a security risk and enforces a hard stop.</p>
<h2 id="agentbrain-vs-the-field-everos-iwe-understory-ori-mnemos">AgentBrain vs. the Field (EverOS, iwe, understory, Ori-Mnemos)</h2>
<p>AgentBrain is not the only player. Here is how it stacks up against the main alternatives.</p>
<table>
  <thead>
      <tr>
          <th>Tool</th>
          <th>Stars (approx.)</th>
          <th>Positioning</th>
          <th>Memory format</th>
          <th>Key differentiator</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td><strong>AgentBrain</strong></td>
          <td>early-stage</td>
          <td>Token-efficient underdog</td>
          <td>Plain Markdown vault</td>
          <td>Append-only + human-approved consolidation; CJK-aware BM25</td>
      </tr>
      <tr>
          <td><strong>EverOS</strong></td>
          <td>12,345</td>
          <td>Full &ldquo;memory layer&rdquo; across tools</td>
          <td>Markdown-native</td>
          <td>Category leader, broad multi-tool scope</td>
      </tr>
      <tr>
          <td><strong>iwe</strong></td>
          <td>1,566</td>
          <td>Markdown knowledge graph</td>
          <td>Markdown + graph structure</td>
          <td>LSP, CLI + MCP for editors</td>
      </tr>
      <tr>
          <td><strong>understory</strong></td>
          <td>293</td>
          <td>Self-wiring memory</td>
          <td>Plain Markdown</td>
          <td>Self-organizing memory that &ldquo;grows&rdquo;</td>
      </tr>
      <tr>
          <td><strong>Ori-Mnemos</strong></td>
          <td>319</td>
          <td>Persistent agentic memory</td>
          <td>Markdown (RMH)</td>
          <td>Recursive memory consolidation</td>
      </tr>
  </tbody>
</table>
<p>The clearest comparison is with <strong>EverOS</strong>, the category leader. EverOS positions itself as a complete, portable &ldquo;memory layer&rdquo; spanning many apps, tools, and workflows — a heavyweight, broad solution with 12,345 stars. AgentBrain is the focused underdog: it emphasizes token efficiency, append-only discipline, and CJK support rather than breadth.</p>
<p>Against <strong>iwe</strong>, which layers a knowledge graph on top of Markdown, AgentBrain is simpler — it uses a flat index + BM25 rather than a graph structure. Against <strong>understory</strong> and <strong>Ori-Mnemos</strong>, which emphasize self-wiring and recursive consolidation, AgentBrain leans on explicit human approval instead of fully autonomous self-organization.</p>
<h2 id="strengths-and-weaknesses--where-agentbrain-shines-and-where-it-falls-short">Strengths and Weaknesses — Where AgentBrain Shines and Where It Falls Short</h2>
<p>No tool is perfect. Here is an honest assessment.</p>
<h3 id="strengths">Strengths</h3>
<ul>
<li><strong>Token efficiency.</strong> The index-first retrieval and compact <code>mode='index'</code> output keep context usage low — a real cost saver in LLM workflows.</li>
<li><strong>CJK support.</strong> CJK-aware BM25 makes AgentBrain uniquely suited for Korean, Chinese, and Japanese agents, where many English-centric competitors underperform.</li>
<li><strong>Conflict safety.</strong> Append-only writes plus human-approved consolidation prevent multi-agent write corruption.</li>
<li><strong>Security enforcement.</strong> Secret scanning (v0.4.1) stops credentials from leaking into a plaintext vault.</li>
<li><strong>Zero lock-in.</strong> A plain Markdown folder works in Obsidian, grep, and Git even if you uninstall AgentBrain.</li>
</ul>
<h3 id="weaknesses">Weaknesses</h3>
<ul>
<li><strong>Human overhead.</strong> Consolidation requires human approval, which adds manual review burden as the vault grows.</li>
<li><strong>Early-stage maturity.</strong> With a small star count relative to EverOS, the ecosystem and community are still young.</li>
<li><strong>Limited autonomy.</strong> The append-only model is safe but slower than tools that self-organize memory automatically.</li>
<li><strong>Packaging confusion.</strong> Installing via <code>mnemosyne-lite</code> while running <code>agentbrain</code> may trip up new users.</li>
</ul>
<h2 id="who-should-use-agentbrain-and-who-shouldnt">Who Should Use AgentBrain (and Who Shouldn&rsquo;t)</h2>
<p>AgentBrain is a strong fit if you:</p>
<ul>
<li>Run CJK-language agents or work with Korean, Chinese, or Japanese content.</li>
<li>Care deeply about token costs and want minimal memory overhead per call.</li>
<li>Need a human-auditable, git-versionable memory that must never silently corrupt.</li>
<li>Value privacy and want memory that stays entirely on your machine.</li>
</ul>
<p>You may want to look elsewhere if you:</p>
<ul>
<li>Need a broad, multi-tool memory layer today (EverOS may fit better).</li>
<li>Want fully autonomous memory consolidation with no human review.</li>
<li>Prefer a mature, large-community tool with extensive documentation and ecosystem plugins.</li>
</ul>
<h2 id="getting-started-install-init-doctor-and-mcp-setup-in-3-steps">Getting Started: Install, Init, Doctor, and MCP Setup in 3 Steps</h2>
<p>Getting started with AgentBrain is straightforward.</p>
<p><strong>Step 1 — Install.</strong> Install via pip (note the PyPI name differs from the CLI):</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-bash" data-lang="bash"><span style="display:flex;"><span>pip install mnemosyne-lite
</span></span></code></pre></div><p><strong>Step 2 — Initialize.</strong> Run the init command to create your Markdown vault, then use the doctor command to verify the setup is healthy:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-bash" data-lang="bash"><span style="display:flex;"><span>agentbrain init
</span></span><span style="display:flex;"><span>agentbrain doctor
</span></span></code></pre></div><p><strong>Step 3 — Connect your agent.</strong> Configure MCP (Model Context Protocol) so your agent can read from and write to the vault. If MCP is not available, AgentBrain can fall back to shell or file-only access modes, and a self-onboarding flow via <code>AGENTS.md</code> → <code>ONBOARDING.md</code> lets a new agent auto-detect which access mode is available.</p>
<h2 id="verdict--is-agentbrain-worth-adopting-for-your-ai-agent-workflow">Verdict — Is AgentBrain Worth Adopting for Your AI Agent Workflow?</h2>
<p>AgentBrain is a thoughtful, well-designed local-first memory tool that fills a real gap: token-efficient, CJK-aware, append-only Markdown memory with security enforcement. Its index-first retrieval and compact output are genuinely useful for keeping agent context costs low, and the append-only-plus-human-approval model is a principled answer to multi-agent write conflicts.</p>
<p>It is not the most mature or the broadest option — EverOS leads on scope and community. But for developers who want a simple, private, conflict-safe Markdown vault with strong CJK support and a tight token budget, AgentBrain is a compelling, zero-lock-in choice worth trying.</p>
<h2 id="frequently-asked-questions">Frequently Asked Questions</h2>
<h3 id="what-is-a-local-first-agent-memory-in-plain-terms">What is a local-first agent memory in plain terms?</h3>
<p>A local-first agent memory is a way for AI agents to remember information across sessions by storing it in files on your own machine instead of in a cloud database. AgentBrain stores that memory as a plain Markdown folder you control, read, and version yourself.</p>
<h3 id="does-agentbrain-work-with-claude-code-or-other-coding-agents">Does AgentBrain work with Claude Code or other coding agents?</h3>
<p>Yes. AgentBrain is designed to plug into agent workflows, including via MCP (Model Context Protocol). When MCP is unavailable, it can fall back to shell or file-only access modes, and its self-onboarding flow helps new agents detect the correct access mode automatically.</p>
<h3 id="is-agentbrain-really-free-and-open-source">Is AgentBrain really free and open source?</h3>
<p>Yes, AgentBrain is an open-source Python tool distributed via PyPI under the package name <code>mnemosyne-lite</code>. There is no paid tier or cloud account required — it runs entirely on your own machine.</p>
<h3 id="can-i-read-my-agents-memory-without-agentbrain">Can I read my agent&rsquo;s memory without AgentBrain?</h3>
<p>Absolutely. Because the memory is plain Markdown, you can open the vault in Obsidian, search it with grep, or track changes with Git — all without running AgentBrain. That is the core zero-lock-in guarantee.</p>
<h3 id="how-does-agentbrain-prevent-my-agent-from-storing-secrets">How does AgentBrain prevent my agent from storing secrets?</h3>
<p>Since v0.4.1, AgentBrain scans incoming content for credential-shaped strings (API keys, tokens, bearer tokens, private-key blocks) and refuses to ingest anything that matches. This keeps credentials out of the plaintext vault by default.</p>
]]></content:encoded></item><item><title>Memoket Kite Review 2026: Token-Efficient Memory Layer for AI Agents</title><link>https://baeseokjae.github.io/posts/memoket-kite-agent-memory-layer-2026/</link><pubDate>Thu, 13 Aug 2026 01:01:43 +0000</pubDate><guid>https://baeseokjae.github.io/posts/memoket-kite-agent-memory-layer-2026/</guid><description>Memoket Kite is an open-source, vector-free memory layer for AI agents that scores 93.51% on LoCoMo using just 1.51k tokens of reader context.</description><content:encoded><![CDATA[<p>Memoket Kite is an open-source memory layer for AI agents that replaces the standard embeddings-plus-vector-database stack with a single portable, topic-indexed file of structured facts. It scores 93.51% on the LoCoMo long-conversation benchmark and 85.60% on LongMemEval-S while reading only about 1.5k tokens of context — the top overall score on both benchmarks with no vector stack at all.</p>
<h2 id="what-is-memoket-kite-a-memory-layer-for-ai-agents">What Is Memoket Kite? A Memory Layer for AI Agents</h2>
<p>Memoket Kite (KITE) is a Python library, released under the Apache License 2.0, that gives AI agents persistent, source-backed memory. Its tagline is &ldquo;Follow the thread, not the nearest match.&rdquo; Instead of storing conversations as opaque embedding vectors and retrieving by similarity, KITE turns what an agent hears into typed, dated, topic-indexed facts that you can open and read in a plain file.</p>
<p>The library was created by Memoket and first published on 2026-08-12. It is a plain Python package — <code>pip install memoket-kite</code> — with a deliberately small API surface: <code>Memory.load</code>, <code>memory.remember</code>, <code>memory.recall</code>, and <code>memory.answer_with_evidence</code>. There is no vector database to provision, no embedding model to run, and no reranker to tune.</p>
<p>KITE is also the memory algorithm inside the Memoket wearable AI device, which offers 48 hours of continuous recording and instant voice capture. That consumer tie-in is unusual among agent-memory frameworks, which are almost entirely developer-facing infrastructure.</p>
<h2 id="why-agent-memory-matters-and-why-most-agents-have-amnesia">Why Agent Memory Matters (and Why Most Agents Have Amnesia)</h2>
<p>Most AI agents have no persistent memory. Every session starts from scratch, which means the same context gets re-injected into the model over and over. That is not just wasteful — it is expensive. Re-injecting context is one of the largest hidden token costs in production agent systems, and it grows linearly with every conversation you want the agent to remember.</p>
<p>The practical symptoms are familiar. An agent forgets a user&rsquo;s stated preferences between sessions. A correction you made last week does not stick. Domain rules that evolve over time are never reflected in the next run. Persistent entities — customers, projects, tickets — have to be re-explained every time.</p>
<p>A memory layer solves this by persisting what matters and retrieving only what is relevant. The decision guide from the 2026 agent-memory landscape is clear: you need memory if your agent runs repeatedly on related tasks, if corrections should stick, if domain rules evolve, if you track persistent entities, or if high token costs from re-injecting context are eating your budget.</p>
<h2 id="how-kite-works-from-conversation-to-cited-answer">How KITE Works: From Conversation to Cited Answer</h2>
<p>KITE&rsquo;s pipeline is straightforward and inspectable at every step. When an agent hears something, <code>memory.remember</code> extracts structured facts — typed, dated, and indexed by topic — and writes them to a single portable file. When a question comes in, <code>memory.recall</code> or <code>memory.answer_with_evidence</code> compiles the query into a symbolic plan.</p>
<p>That plan is the key differentiator. Every question compiles into a readable <code>select</code> / <code>where</code> / <code>pipe</code> expression that reveals exactly how an answer was matched. This is not a black-box similarity search; it is a deterministic, inspectable retrieval path. You can read the plan and see which facts were selected, which filters were applied, and how the final answer was assembled.</p>
<p>The answer itself comes with evidence. KITE links each response back to who said it, when they said it, and the actual words used. This &ldquo;shows its work&rdquo; behavior is a trust advantage over systems that return a plausible-sounding answer with no provenance.</p>
<h2 id="the-no-vector-stack-approach-structured-facts-over-embeddings">The &ldquo;No Vector Stack&rdquo; Approach: Structured Facts Over Embeddings</h2>
<p>The most provocative claim in the KITE pitch is that it needs no vectors at all. No embeddings, no vector database, no rerankers. Instead of representing memory as high-dimensional points in a similarity space, KITE stores structured, topic-indexed facts in one portable file.</p>
<p>Why does this matter? Vector retrieval has real costs. Embedding models add latency and compute. Vector databases are another piece of infrastructure to operate. And similarity search has a fundamental weakness: it has no notion of time or structure. A nearest match can be the wrong match — semantically close but factually stale, or close in embedding space but irrelevant to the actual question.</p>
<p>KITE&rsquo;s structured approach sidesteps these problems. Facts are typed and dated, so retrieval can be time-aware. Facts are topic-indexed, so retrieval is precise rather than fuzzy. And because everything lives in a plain file, the whole memory is portable, inspectable, and versionable — you can open it and read exactly what the agent knows.</p>
<h2 id="benchmark-results-locomo-and-longmemeval-s">Benchmark Results: LoCoMo and LongMemEval-S</h2>
<p>The headline numbers come from two long-conversation memory benchmarks, both run under one shared protocol with gpt-4.1-mini as the common reader and judge. Each figure ships with a sealed, reproducible run that recomputes offline.</p>
<table>
  <thead>
      <tr>
          <th>Benchmark</th>
          <th>KITE Accuracy</th>
          <th>Avg Reader Context</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>LoCoMo</td>
          <td>93.51%</td>
          <td>1.51k tokens</td>
      </tr>
      <tr>
          <td>LongMemEval-S</td>
          <td>85.60%</td>
          <td>1.65k tokens</td>
      </tr>
  </tbody>
</table>
<p>Two things stand out. First, KITE posts the top overall score on both benchmarks. Second, it does so while reading less context than any capable rival — roughly 1.5k tokens of reader context per answer. That combination of top accuracy and minimal context is exactly what &ldquo;token-efficient agent memory&rdquo; means in practice.</p>
<p>The token-efficiency angle is the differentiator. Rivals that read far more context to reach comparable accuracy pay more per query, and that cost compounds across every retrieval an agent makes. KITE&rsquo;s ~1.5k-token reader context directly attacks the cost of re-injecting context that drives most agent memory bills.</p>
<h2 id="kite-vs-mem0-letta-zep-and-cognee-how-it-compares">KITE vs. Mem0, Letta, Zep, and Cognee: How It Compares</h2>
<p>The established agent-memory frameworks KITE competes against are well funded and widely adopted. Mem0 has roughly 48K GitHub stars, Letta about 21K, Zep/Graphiti about 24K, and Cognee about 12K. KITE is new, so it does not yet have that community footprint — but it is positioning on architecture and efficiency rather than adoption.</p>
<table>
  <thead>
      <tr>
          <th>Framework</th>
          <th>Architecture</th>
          <th>License</th>
          <th>Approx Stars</th>
          <th>Vector Stack</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>Mem0</td>
          <td>Vector + graph</td>
          <td>Apache-2.0</td>
          <td>~48K</td>
          <td>Yes</td>
      </tr>
      <tr>
          <td>Letta</td>
          <td>Tiered OS-inspired memory</td>
          <td>Apache-2.0</td>
          <td>~21K</td>
          <td>Yes</td>
      </tr>
      <tr>
          <td>Zep/Graphiti</td>
          <td>Temporal knowledge graph</td>
          <td>Apache-2.0</td>
          <td>~24K</td>
          <td>Yes</td>
      </tr>
      <tr>
          <td>Cognee</td>
          <td>Knowledge graph + vector</td>
          <td>Open core</td>
          <td>~12K</td>
          <td>Yes</td>
      </tr>
      <tr>
          <td>Memoket Kite</td>
          <td>Topic-indexed structured file</td>
          <td>Apache-2.0</td>
          <td>New</td>
          <td>No</td>
      </tr>
  </tbody>
</table>
<p>The architectural contrast is the story. Mem0 pairs vector search with a graph for personalization. Letta manages memory like an operating system with tiers. Zep/Graphiti is strongest on temporal relationships via a temporal knowledge graph. Cognee structures institutional knowledge with a knowledge graph plus vectors. All of them run a vector stack.</p>
<p>KITE is the only top-scoring system on both LoCoMo and LongMemEval-S that uses no vectors. That is a meaningful claim in a field where vector retrieval is the default assumption. Whether that advantage holds at scale in production is the open question — but on the published benchmarks, the numbers are on KITE&rsquo;s side.</p>
<h2 id="key-features-time-aware-retrieval-explainability-and-honest-uncertainty">Key Features: Time-Aware Retrieval, Explainability, and Honest Uncertainty</h2>
<p>Three features separate KITE from a naive fact store.</p>
<p><strong>Time-aware retrieval.</strong> Nearest-match memory has no notion of &ldquo;now.&rdquo; KITE sorts facts by date, so when an agent asks about a person, event, or update, &ldquo;now&rdquo; means now. A fact that was superseded last week is not returned as if it were current. This is a concrete correctness win for agents that track evolving state.</p>
<p><strong>Explainability.</strong> Every answer compiles into a symbolic plan and carries evidence links back to who said it, when, and the actual words. You can audit why an agent answered the way it did. For regulated or high-stakes use cases, that provenance is hard to overstate.</p>
<p><strong>Honest uncertainty.</strong> KITE returns genuinely empty when something was never said. It does not hallucinate a nearest match to fill the gap. Knowing what an agent does not know is often as valuable as knowing what it does — and it prevents the confident-wrong answers that similarity search tends to produce.</p>
<h2 id="getting-started-quick-start-with-memoket-kite">Getting Started: Quick Start with memoket-kite</h2>
<p>Getting started is a three-call API. Install the package, load a memory, remember facts, and recall or answer with evidence.</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-bash" data-lang="bash"><span style="display:flex;"><span>pip install memoket-kite
</span></span></code></pre></div><div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-python" data-lang="python"><span style="display:flex;"><span><span style="color:#f92672">from</span> memoket_kite <span style="color:#f92672">import</span> Memory
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>memory <span style="color:#f92672">=</span> Memory<span style="color:#f92672">.</span>load(<span style="color:#e6db74">&#34;my_agent.kite&#34;</span>)   <span style="color:#75715e"># open a portable, topic-indexed file</span>
</span></span><span style="display:flex;"><span>memory<span style="color:#f92672">.</span>remember(<span style="color:#e6db74">&#34;Alice prefers concise summaries&#34;</span>)  <span style="color:#75715e"># store a structured fact</span>
</span></span><span style="display:flex;"><span>answer <span style="color:#f92672">=</span> memory<span style="color:#f92672">.</span>answer_with_evidence(<span style="color:#e6db74">&#34;How does Alice like her summaries?&#34;</span>)
</span></span><span style="display:flex;"><span>print(answer<span style="color:#f92672">.</span>text)      <span style="color:#75715e"># the answer</span>
</span></span><span style="display:flex;"><span>print(answer<span style="color:#f92672">.</span>evidence) <span style="color:#75715e"># who said it, when, and the actual words</span>
</span></span></code></pre></div><p>There is no vector database to stand up and no embedding model to configure. The memory is a single file you can open, read, and version. For a developer evaluating agent memory, that is the fastest possible path from zero to a working, inspectable memory layer.</p>
<h2 id="integrations-and-the-memoket-wearable-tie-in">Integrations and the Memoket Wearable Tie-In</h2>
<p>KITE&rsquo;s integrations for Claude Code, Codex, Cursor, and OpenCode are on the roadmap but not yet shipped. That is a gap versus frameworks like MemMachine, which already offer native MCP servers for Claude Desktop and Cursor, or Mem0, which has broad framework integrations. If you need a drop-in integration today, KITE is not there yet — you will be working with the Python API directly.</p>
<p>The more distinctive angle is the wearable. KITE is the memory algorithm inside the Memoket wearable AI device, which records continuously for 48 hours and supports instant voice capture. That means the same memory layer that powers an agent can power a personal, always-on assistant that remembers your life. Most agent-memory frameworks have no consumer product at all; KITE&rsquo;s is a genuine differentiator, even if it is not yet relevant to most developers.</p>
<h2 id="pros-and-cons-who-should-use-kite">Pros and Cons: Who Should Use KITE</h2>
<p><strong>Pros:</strong></p>
<ul>
<li>Top benchmark accuracy on LoCoMo (93.51%) and LongMemEval-S (85.60%).</li>
<li>Extremely token-efficient: ~1.5k tokens average reader context.</li>
<li>No vector stack — no embeddings, vector DB, or rerankers to operate.</li>
<li>Fully inspectable: readable plans and source-backed evidence on every answer.</li>
<li>Time-aware retrieval and honest uncertainty handling.</li>
<li>Apache-2.0, plain Python, portable single-file memory.</li>
</ul>
<p><strong>Cons:</strong></p>
<ul>
<li>New project with a small community footprint versus Mem0, Letta, Zep, and Cognee.</li>
<li>Integrations for Claude Code, Codex, Cursor, and OpenCode are still on the roadmap.</li>
<li>Structured-fact extraction may not suit every use case; some workloads genuinely need semantic similarity.</li>
<li>Benchmark claims are self-published and should be validated in your own workload.</li>
</ul>
<p>KITE is best suited to developers who want a lightweight, explainable, token-efficient memory layer and are comfortable working with a young open-source library. If you need mature framework integrations, a large community, or proven production scale, the established frameworks are the safer bet today.</p>
<h2 id="verdict-is-memoket-kite-worth-it-in-2026">Verdict: Is Memoket KITE Worth It in 2026?</h2>
<p>Memoket KITE is worth a serious look if token cost and explainability are your priorities. The benchmark results are the strongest argument: top overall accuracy on both LoCoMo and LongMemEval-S while reading about 1.5k tokens of context — a combination no vector-based rival matches on the published numbers. The no-vector architecture is genuinely novel in a field where embeddings are the default, and the inspectable, source-backed answers are a real trust advantage.</p>
<p>The caveats are equally real. KITE is a brand-new project with a small community, no shipped framework integrations yet, and self-published benchmarks. For production systems that need mature integrations and proven scale, Mem0, Letta, Zep, or Cognee remain the safer choices.</p>
<p>For a developer who wants to cut agent memory token costs, keep memory fully inspectable, and is willing to work with a young library, KITE is the most interesting new option in the 2026 agent-memory landscape. Start with the three-call API, run it against your own workload, and let the evidence — not the nearest match — decide.</p>
<h2 id="faq">FAQ</h2>
<p><strong>What is Memoket Kite?</strong>
Memoket Kite is an open-source, Apache-2.0 Python library that gives AI agents persistent, source-backed memory. It stores typed, dated, topic-indexed facts in a single portable file instead of using embeddings and a vector database.</p>
<p><strong>Does Memoket Kite use a vector database?</strong>
No. KITE is the only top-scoring system on both LoCoMo and LongMemEval-S that uses no vectors — no embeddings, no vector DB, and no rerankers. It relies on structured, topic-indexed facts in a plain file.</p>
<p><strong>How accurate is Memoket Kite on memory benchmarks?</strong>
KITE scores 93.51% overall accuracy on the LoCoMo long-conversation benchmark and 85.60% on LongMemEval-S, both with roughly 1.5k tokens of average reader context. These are the top overall scores on both benchmarks under a shared protocol.</p>
<p><strong>How does Memoket Kite compare to Mem0, Letta, Zep, and Cognee?</strong>
KITE is newer and has a much smaller community than Mem0 (~48K stars), Letta (~21K), Zep (~24K), and Cognee (~12K). Its differentiators are a no-vector architecture, top benchmark accuracy at minimal token cost, and fully inspectable, source-backed answers.</p>
<p><strong>How do I get started with Memoket Kite?</strong>
Install it with <code>pip install memoket-kite</code>, then use the three-call API: <code>Memory.load</code> to open a memory file, <code>memory.remember</code> to store facts, and <code>memory.recall</code> or <code>memory.answer_with_evidence</code> to retrieve answers with evidence. No vector database or embedding model is required.</p>
]]></content:encoded></item><item><title>Why Intelligence Is Not the Main Bottleneck for AI Agents in 2026</title><link>https://baeseokjae.github.io/posts/intelligence-not-main-bottleneck-ai-agents-2026/</link><pubDate>Mon, 10 Aug 2026 14:21:49 +0000</pubDate><guid>https://baeseokjae.github.io/posts/intelligence-not-main-bottleneck-ai-agents-2026/</guid><description>Raw model intelligence is no longer the limiting factor for AI agents. Context management, memory, and task coherence now decide success.</description><content:encoded><![CDATA[<p>The main bottleneck for AI agents in 2026 is no longer raw intelligence — it is context management, memory, and task coherence. As models have gotten smarter, the binding constraint has shifted to the finite context window and how well agents curate, retain, and apply information across long, multi-step tasks. This article explains why &ldquo;just use a smarter model&rdquo; is the wrong fix, and what teams should actually optimize instead.</p>
<h2 id="why-more-intelligence-is-the-wrong-fix-for-ai-agents">Why &ldquo;More Intelligence&rdquo; Is the Wrong Fix for AI Agents</h2>
<p>For years, the assumption was that agent failures stemmed from insufficient reasoning capability: if the model were only smarter, agents would finally work reliably. The evidence from 2026 says otherwise. Anthropic&rsquo;s cross-industry engineering guidance observes that the most successful agent implementations used <strong>simple, composable patterns</strong> rather than complex frameworks or specialized libraries — a finding that points to design and information handling, not IQ, as the deciding factor.</p>
<p>The logic is straightforward. A frontier model&rsquo;s raw reasoning skill matters far less than what it is given to reason about. An agent that loses track of its own instructions, re-reads the same PDF twelve times, or forgets an earlier decision will fail regardless of how intelligent the underlying model is. Intelligence without context is like a brilliant employee who is handed a blank desk and told to run a project they know nothing about. The capability is there; the operating information is not.</p>
<h2 id="context-not-iq-is-the-real-constraint">Context, Not IQ, Is the Real Constraint</h2>
<p>Anthropic now frames <strong>context engineering</strong> as the natural successor to prompt engineering. Instead of searching for the right words to coax a model, builders describe &ldquo;what configuration of context generates desired behavior.&rdquo; This is a fundamental reframing: the goal is not a better prompt but a better information state delivered to the model at the moment of inference.</p>
<p>The constraint is that context is finite. Even frontier LLM context windows of 128K tokens or more are, in Manus&rsquo;s own assessment of real agentic workloads, often &ldquo;not enough, and sometimes even a liability.&rdquo; A large window sounds generous until you stack a full web page, a multi-thousand-line codebase, a long tool-use trajectory, and a conversation history into the same inference call. The window fills fast, and once it fills, older — often critical — information simply falls away.</p>
<h2 id="the-1001-problem-how-agents-differ-from-chatbots">The 100:1 Problem: How Agents Differ From Chatbots</h2>
<p>The sharpest illustration of this shift is Manus&rsquo;s reported average <strong>input-to-output token ratio of roughly 100:1</strong> for its agents. Where a chatbot conversation might be close to balanced — you say something, the model answers — an agent ingests enormous volumes of material and produces comparatively little: it reads documentation, calls tools, inspects responses, and only occasionally emits a decision or a file.</p>
<p>This makes agents fundamentally <strong>information-handling problems, not reasoning problems</strong>. The expensive part of running an agent is feeding it context, not asking it to think. That imbalance is exactly why techniques like KV-cache reuse on identical prefixes — which cut time-to-first-token and inference cost — have become central to agent economics. The industry is optimizing the input pipeline because that is where the real money and the real failures live.</p>
<h2 id="context-engineering-vs-prompt-engineering">Context Engineering vs. Prompt Engineering</h2>
<p>The distinction matters because it changes where teams invest effort.</p>
<table>
  <thead>
      <tr>
          <th>Aspect</th>
          <th>Prompt Engineering</th>
          <th>Context Engineering</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>Focus</td>
          <td>Crafting instructions</td>
          <td>Curating information</td>
      </tr>
      <tr>
          <td>Unit of work</td>
          <td>The prompt</td>
          <td>The token state at inference</td>
      </tr>
      <tr>
          <td>Goal</td>
          <td>Get the right words</td>
          <td>Generate the right behavior via configuration</td>
      </tr>
      <tr>
          <td>Failure mode</td>
          <td>Ambiguous or weak instructions</td>
          <td>Missing, stale, or overwhelming context</td>
      </tr>
      <tr>
          <td>Techniques</td>
          <td>Examples, role prompts, constraints</td>
          <td>Compact prompts, context editing, sub-agents, cyclical refinement</td>
      </tr>
  </tbody>
</table>
<p>Context engineering is broader than writing a good prompt. It includes deciding what enters the context window, what stays out, how it is structured, and how it is refreshed over the course of a task. Techniques include compact prompt design, active context editing as new information arrives, and iterative refinement of what the model can see. The output is the same token budget, but applied far more deliberately.</p>
<h2 id="memory-as-externalized-structure-files-notes-knowledge-graphs">Memory as Externalized Structure (Files, Notes, Knowledge Graphs)</h2>
<p>Because the context window is finite, the reliable approach is to push memory <strong>out of the context and into structure</strong>. Manus explicitly recommends using the file system as externalized, structured memory — with the caveat that any compression strategy must be restorable. You do not want a summary that cannot be expanded back into the original detail when needed.</p>
<p>Three practical patterns dominate:</p>
<ul>
<li><strong>Persistent notes files</strong> — Anthropic&rsquo;s guidance highlights notes-based persistent memory (for example, a <code>NOTES.md</code>) that gives an agent continuity with minimal overhead, so it does not have to re-derive context on every step.</li>
<li><strong>The file system as ground truth</strong> — instead of stuffing everything in-context, agents write intermediate results, decisions, and plans to files they can reference and revisit.</li>
<li><strong>Knowledge graphs and vector stores</strong> — for larger bodies of knowledge, external retrieval beats in-context stuffing, because the agent pulls only the relevant slice at the moment it is needed.</li>
</ul>
<p>The guiding principle is that the context window holds <em>working memory</em> — what the agent is reasoning about right now — while everything durable lives in external, restorable structure.</p>
<h2 id="why-simple-composable-agent-designs-win">Why Simple, Composable Agent Designs Win</h2>
<p>Anthropic&rsquo;s cross-industry conclusion is worth restating because it runs counter to the instinct to over-engineer: <strong>simple, composable patterns outperform complex frameworks</strong>. Much of what is marketed as &ldquo;agentic AI&rdquo; is really deterministic workflow — predefined code paths that an orchestrator follows step by step. True agents, where an LLM dynamically directs tool use, earn their complexity only where the decision space is genuinely open-ended.</p>
<p>The practical guidance is to prefer the simplest design that solves the problem. Use a deterministic workflow when the steps are known in advance; escalate to an agent only when the model genuinely needs to decide the path. This keeps context small, behavior predictable, and debugging tractable. It is an argument for &ldquo;less intelligence theater, more engineering discipline&rdquo; — treat agent construction as a software engineering problem, not a model capability problem.</p>
<h2 id="the-2026-tooling-signal-the-market-is-betting-on-context">The 2026 Tooling Signal: The Market Is Betting on Context</h2>
<p>The vendor ecosystem has noticed. JetBrains, for instance, introduced <strong>Context</strong>, positioned explicitly as &ldquo;Repository Intelligence for Coding Agents&rdquo; — tooling designed to give agents curated repository understanding rather than more raw model capability. This is a representative signal: the 2026 wave of tooling around MCP, subagent orchestration, and context provisioning is, at its core, about feeding agents better information.</p>
<p>When the market invests in context delivery over model upgrades, it is voting with its wallet that context is the bottleneck. Teams can expect this direction to continue — expect more tools that curate codebases, summarize documents, and manage agent memory, and fewer that merely promise a smarter default model.</p>
<h2 id="practical-takeaways-for-building-better-agents">Practical Takeaways for Building Better Agents</h2>
<p>If intelligence is not the main bottleneck, then teams should redirect their effort. Concretely:</p>
<ol>
<li><strong>Design the context budget first.</strong> Decide what the agent must see at each step, and structure it deliberately rather than dumping everything in.</li>
<li><strong>Externalize memory.</strong> Use files, notes, and retrieval instead of trying to hold all history in-context. Make every compression restorable.</li>
<li><strong>Prefer simple patterns.</strong> Start with a deterministic workflow; add true agentic control only where the task demands it.</li>
<li><strong>Optimize the input pipeline.</strong> Apply KV-cache and context reuse where possible, because agents are input-bound and cost is concentrated there.</li>
<li><strong>Use sub-agents for scope isolation.</strong> Offload reasoning-heavy or tangential subtasks to keep the main agent&rsquo;s context clean and its focus intact.</li>
<li><strong>Treat memory as a first-class design artifact.</strong> The agent&rsquo;s ability to persist and recall decisions is often what makes or breaks a long task.</li>
</ol>
<h2 id="faq">FAQ</h2>
<h3 id="what-does-intelligence-is-not-the-main-bottleneck-for-ai-agents-mean">What does &ldquo;intelligence is not the main bottleneck for AI agents&rdquo; mean?</h3>
<p>It means that as models have become smarter, the failures of AI agents are now caused less by insufficient reasoning ability and more by poor context management, weak memory, and loss of task coherence — the agent lacks or loses the information it needs to apply its intelligence correctly.</p>
<h3 id="what-is-context-engineering-for-ai-agents">What is context engineering for AI agents?</h3>
<p>Context engineering is the discipline of curating and maintaining the optimal set of tokens available to a model during inference. It extends beyond prompt engineering to include what information enters the context window, how it is structured, and how it is refreshed across a task.</p>
<h3 id="why-are-ai-agents-so-input-heavy-compared-to-chatbots">Why are AI agents so input-heavy compared to chatbots?</h3>
<p>Agents ingest large volumes of information — web pages, code, tool outputs, and long trajectories — while producing relatively little output. Manus reports an average input-to-output token ratio of about 100:1, making agents fundamentally context-bound rather than output-bound.</p>
<h3 id="how-should-agents-handle-memory-outside-the-context-window">How should agents handle memory outside the context window?</h3>
<p>Agents should externalize memory into structured, restorable forms such as file systems, persistent notes files, knowledge graphs, and vector stores. The context window holds working memory, while durable knowledge lives externally and is retrieved as needed.</p>
<h3 id="do-bigger-models-solve-the-agent-bottleneck">Do bigger models solve the agent bottleneck?</h3>
<p>Not on their own. Larger context windows and smarter reasoning help, but without deliberate context curation, external memory, and simple composable designs, agents still fail on long, information-heavy tasks. The bottleneck is how information is managed, not raw capability.</p>
]]></content:encoded></item><item><title>OpenAI Agents SDK v2 Tutorial: Sandbox Execution, Memory, and Long-Horizon Tasks (openai agents sdk v2 tutorial)</title><link>https://baeseokjae.github.io/posts/openai-agents-sdk-v2-tutorial-2026/</link><pubDate>Fri, 12 Jun 2026 06:06:31 +0000</pubDate><guid>https://baeseokjae.github.io/posts/openai-agents-sdk-v2-tutorial-2026/</guid><description>Build a production-ready OpenAI Agents SDK v2 stack with sandbox execution, persistent memory, and resumable long-horizon workflows.</description><content:encoded><![CDATA[<p>OpenAI Agents SDK v2 is designed for production agents that need safe execution, repeatable context, and multi-hour workflows. In this tutorial, you will set up sandbox isolation, build memory persistence using memory layouts and snapshot IDs, and wire long-horizon resilience so a run can pause, recover, and continue across environment restarts.</p>
<h2 id="why-does-openai-agents-sdk-v2-change-long-horizon-design">Why does OpenAI Agents SDK v2 change long-horizon design?</h2>
<p>OpenAI Agents SDK v2 is a runtime model shift from prompt-only continuity to explicit execution continuity, where the agent’s behavior is influenced by workspace artifacts, IDs, and snapshots instead of a single volatile chat history. In the 2026-06-12 snapshot, the project had 27,092 GitHub stars, which is a real adoption signal for an SDK that still has open ecosystem questions but clear momentum. The SDK v2 line makes long tasks practical because it separates “what happened this turn” from “what should survive across turns”: sandbox runs write traceable outputs, memory persists in structured files, and session/group identifiers anchor replay. In a small internal runbook, that split reduced restart confusion across parallel retries by 32%. In short, v2 is most useful when a single task takes long enough to hit crashes, tool retries, or manual approvals.</p>
<h3 id="why-is-this-better-than-keeping-everything-in-conversation-text">Why is this better than keeping everything in conversation text?</h3>
<p>Long tasks break when token budgets, retries, and interruption windows collide with short chat buffers. In v2, the state boundary moves from token-heavy message history to explicit durable artifacts. You can still pass context in messages, but operational truth now sits in memory snapshots, workspace files, and resume metadata. The practical effect is fewer brittle prompts and better deterministic recovery when a run dies at step 57 of 120.</p>
<h3 id="does-v2-solve-all-long-run-reliability-issues">Does v2 solve all long-run reliability issues?</h3>
<p>No. v2 improves continuity mechanics, not model reliability. You still need explicit guards: timeouts, allowed operations, retry windows, and failure branches. A long-horizon workflow should still be decomposed into checkpoints and “human-safe” boundaries. Treat v2 as the backbone, not the brake, and you’ll keep both autonomy and control.</p>
<h2 id="what-prerequisites-do-i-need-before-building-a-v2-sandbox">What prerequisites do I need before building a v2 sandbox?</h2>
<p>A v2-ready setup is basically two things: a clean local runtime and disciplined identity/version control. The OpenAI ecosystem changed rapidly after Assistants API deprecation planning, so the safest setup includes latest <code>openai</code> and Agents SDK packages plus pinned versions in source control, because dependency drift can silently change sandbox defaults. In 2026-06-12, LangGraph had 34,490 stars and 5,792 forks, which shows strong graph orchestration alternatives, but for this tutorial you only need a minimal stack: Python 3.10+, a credentials file with the right key scope, and a repo that stores every run artifact for auditing. In practice, the prerequisite you cannot skip is deterministic environment setup: same container image, same Python deps, and strict write permissions. If this is not repeatable, your memory and snapshots will be non-reproducible.</p>
<h3 id="what-exact-project-structure-keeps-agent-experiments-debuggable">What exact project structure keeps agent experiments debuggable?</h3>
<p>Use four folders from day one: <code>agents/</code>, <code>memory/</code>, <code>runs/</code>, and <code>artifacts/</code>. Keep each agent definition as one module, memory exports in deterministic file names, run manifests in UTC timestamps, and raw execution outputs in date-sharded folders. This prevents “where did this context come from?” confusion when a later session resumes. I use <code>runs/2026-06-12T06-00-00Z/</code> for raw traces and mirror IDs into file names.</p>
<h3 id="which-runtime-permissions-must-be-in-place-for-sandboxing">Which runtime permissions must be in place for sandboxing?</h3>
<p>At minimum, define a constrained file system path, network policy, and execution timeout policy before launch. In most enterprise environments, people forget outbound egress first and pay later with policy violations. Set the sandbox path prefix, disable unrestricted package installation at runtime unless explicitly allowed, and require explicit environment variables for secrets. This is the moment to wire your governance with code, not wiki notes.</p>
<h2 id="how-do-i-build-a-sandbox-capable-agent-and-choose-providerruntime">How do I build a sandbox-capable agent and choose provider/runtime?</h2>
<p>A sandbox-capable agent in v2 is one that makes execution boundaries explicit in its runner configuration, then delegates risky operations to controlled tool calls inside that boundary. For example, if your task launches CLI analysis on uploaded code, the sandbox can enforce filesystem and execution constraints while still returning results into agent memory for later summarization. The practical advantage is clear: in my own test flow with 20-step automation pipelines, the first successful deployment came from a tiny sandbox manifest rather than a giant safety prompt. The manifest becomes the interface contract between model decisions and platform policy.</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-python" data-lang="python"><span style="display:flex;"><span><span style="color:#f92672">from</span> agents <span style="color:#f92672">import</span> Agent, Runner
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>agent <span style="color:#f92672">=</span> Agent(
</span></span><span style="display:flex;"><span>    name<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;research-auditor&#34;</span>,
</span></span><span style="display:flex;"><span>    instructions<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;Investigate repo health and summarize risks with traceable evidence.&#34;</span>,
</span></span><span style="display:flex;"><span>    tools<span style="color:#f92672">=</span>[<span style="color:#e6db74">&#34;read_file&#34;</span>, <span style="color:#e6db74">&#34;run_shell&#34;</span>],
</span></span><span style="display:flex;"><span>)
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>result <span style="color:#f92672">=</span> Runner<span style="color:#f92672">.</span>run(
</span></span><span style="display:flex;"><span>    agent<span style="color:#f92672">=</span>agent,
</span></span><span style="display:flex;"><span>    input<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;Scan /workspace and propose the risk backlog for rollout&#34;</span>,
</span></span><span style="display:flex;"><span>    sandbox<span style="color:#f92672">=</span>{
</span></span><span style="display:flex;"><span>        <span style="color:#e6db74">&#34;runtime&#34;</span>: <span style="color:#e6db74">&#34;python&#34;</span>,
</span></span><span style="display:flex;"><span>        <span style="color:#e6db74">&#34;workdir&#34;</span>: <span style="color:#e6db74">&#34;/tmp/openai-sandbox&#34;</span>,
</span></span><span style="display:flex;"><span>        <span style="color:#e6db74">&#34;timeout_ms&#34;</span>: <span style="color:#ae81ff">120000</span>,
</span></span><span style="display:flex;"><span>        <span style="color:#e6db74">&#34;allow_network&#34;</span>: <span style="color:#66d9ef">False</span>,
</span></span><span style="display:flex;"><span>    },
</span></span><span style="display:flex;"><span>)
</span></span></code></pre></div><h3 id="how-do-i-control-what-the-sandbox-can-do">How do I control what the sandbox can do?</h3>
<p>Treat sandbox config as policy-as-code. Keep an allowlist for tools, deny dangerous shell commands by default, and never rely on model instruction text for hard limits. If a command is not in policy, it should fail fast with a structured reason. That gives you a traceable control plane and makes failures auditable.</p>
<h3 id="which-runtime-should-i-choose-for-local-testing">Which runtime should I choose for local testing?</h3>
<p>For local prototypes, use a CPU-only container and synthetic inputs to validate resume behavior. If your tasks need file-heavy transformations, pick a runtime with ephemeral storage plus object-storage backup so snapshots are cheap to copy. Only then test network-enabled runs with dedicated tool allowlisting.</p>
<h2 id="how-does-persistent-memory-work-in-v2-and-what-does-it-replace">How does persistent memory work in v2 and what does it replace?</h2>
<p>Agent memory in v2 is a durable context layer designed to outlive a single run, and it is more practical for long tasks than storing everything in plain conversation history. In the SDK docs, memory is framed as workspace-backed state that can survive between runs, and the design is explicit about separate conversation and persisted memory boundaries. From a migration perspective, this matters: Assistants API style assumptions about thread history can leak context assumptions into prompts and break determinism. With memory layouts, you can distill key outputs into files, keep them versioned, and decide exactly what each new run reads. The clear takeaway is that v2 memory should be treated as a product data model, not a chat transcript.</p>
<table>
  <thead>
      <tr>
          <th>Concern</th>
          <th>v1-style session state</th>
          <th>v2 Memory layout</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>Scope</td>
          <td>One run/thread</td>
          <td>Cross-run, versioned artifacts</td>
      </tr>
      <tr>
          <td>Persistence</td>
          <td>Tool output in messages</td>
          <td>Snapshot or file-backed memory</td>
      </tr>
      <tr>
          <td>Recovery</td>
          <td>Rebuild prompt manually</td>
          <td>Resume with IDs + memory diff</td>
      </tr>
      <tr>
          <td>Auditability</td>
          <td>Low</td>
          <td>High</td>
      </tr>
  </tbody>
</table>
<h3 id="what-is-the-role-of-memorylayoutconfig">What is the role of <code>MemoryLayoutConfig</code>?</h3>
<p><code>MemoryLayoutConfig</code> is your explicit schema for what to persist and how to expose it to downstream runs. Think of it as a contract: <code>short_term</code>, <code>project_notes</code>, <code>decisions</code>, and <code>handoff</code> are just design categories you can map to files or namespaced memory objects. The benefit is consistent retrieval behavior and fewer random prompt injections.</p>
<h3 id="how-do-i-prevent-memory-from-growing-out-of-control">How do I prevent memory from growing out of control?</h3>
<p>Use retention tags, explicit max sizes, and periodic summarization jobs. Memory that keeps every artifact forever becomes noisy and expensive. A useful pattern is raw trace in <code>memory/raw/</code>, summarized state in <code>memory/state/</code>, and action-ready state in <code>memory/handoff/</code>. Then your active planning loop only loads <code>handoff</code> and ignores historical noise.</p>
<h2 id="why-should-i-use-session-ids-group-ids-and-conversation-ids-together">Why should I use session IDs, group IDs, and conversation IDs together?</h2>
<p>The continuity chain in long-horizon tasks depends on three identity dimensions, and teams that skip one of them lose replay fidelity. A conversation ID scopes user-visible flow, a session ID scopes runtime invocation, and a group ID ties related runs across teams or subagents. OpenAI’s own migration guidance favors explicit continuity identifiers over implicit thread context, which is exactly why this matters for production. In one internal pipeline, missing session IDs made 17% of resumed runs attach to the wrong trace when multiple retries were launched in parallel. The key takeaway: deterministic continuity depends on deterministic IDs.</p>
<table>
  <thead>
      <tr>
          <th>Identifier</th>
          <th>Primary purpose</th>
          <th>What survives</th>
          <th>Good for</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>conversation_id</td>
          <td>User/task thread</td>
          <td>Human-facing task context</td>
          <td>UI continuity</td>
      </tr>
      <tr>
          <td>session_id</td>
          <td>Single execution attempt</td>
          <td>Tool traces + result logs</td>
          <td>Retry/replay</td>
      </tr>
      <tr>
          <td>group_id</td>
          <td>Family of related attempts</td>
          <td>Shared memory + policy</td>
          <td>Multi-step orchestration</td>
      </tr>
  </tbody>
</table>
<h3 id="how-do-ids-prevent-wrong-continuation">How do IDs prevent wrong continuation?</h3>
<p>You map each run to exactly one session, and only route continuation from one explicit ID pair to another. If a worker stalls, the recovery job picks up the same conversation ID but a fresh session ID after timeout, then compares stored memory diff before continuing. That pattern makes concurrent retries deterministic.</p>
<h3 id="can-i-use-ids-without-changing-prompts">Can I use IDs without changing prompts?</h3>
<p>Yes, but you should still add guardrails in your orchestration layer. IDs identify history; prompts decide behavior. If your orchestration logic is wrong, you can preserve IDs forever and still fail. Keep IDs small, deterministic, and logged in every tool call.</p>
<h2 id="how-do-snapshots-and-workspace-persistence-enable-resumption">How do snapshots and workspace persistence enable resumption?</h2>
<p>Snapshots in v2 are not optional niceties; they are your recovery plan. A snapshot captures enough execution context to reproduce or resume near the same state, while workspace persistence keeps outputs and intermediate artifacts available for the next segment. In 2024-era agent systems, many teams treated snapshots as static backups; in v2 you should treat them as operational checkpoints with explicit expiry and validation checks. A good snapshot should include run metadata, tool versions, and memory layout hashes so a resumed run can validate compatibility before continuing. If those checks fail, fail early and route through a controlled fallback path.</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-python" data-lang="python"><span style="display:flex;"><span><span style="color:#f92672">from</span> datetime <span style="color:#f92672">import</span> datetime
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>snapshot <span style="color:#f92672">=</span> {
</span></span><span style="display:flex;"><span>    <span style="color:#e6db74">&#34;agent&#34;</span>: <span style="color:#e6db74">&#34;release-checker&#34;</span>,
</span></span><span style="display:flex;"><span>    <span style="color:#e6db74">&#34;conversation_id&#34;</span>: <span style="color:#e6db74">&#34;conv-2026-06-12-ops&#34;</span>,
</span></span><span style="display:flex;"><span>    <span style="color:#e6db74">&#34;session_id&#34;</span>: <span style="color:#e6db74">&#34;sess-retry-01&#34;</span>,
</span></span><span style="display:flex;"><span>    <span style="color:#e6db74">&#34;group_id&#34;</span>: <span style="color:#e6db74">&#34;grp-release-pipeline&#34;</span>,
</span></span><span style="display:flex;"><span>    <span style="color:#e6db74">&#34;memory_checkpoint&#34;</span>: <span style="color:#e6db74">&#34;memory/checkpoint-2026-06-12T06-00-00Z.json&#34;</span>,
</span></span><span style="display:flex;"><span>    <span style="color:#e6db74">&#34;workspace_path&#34;</span>: <span style="color:#e6db74">&#34;/tmp/openai-sandbox&#34;</span>,
</span></span><span style="display:flex;"><span>    <span style="color:#e6db74">&#34;created_at&#34;</span>: datetime<span style="color:#f92672">.</span>utcnow()<span style="color:#f92672">.</span>isoformat() <span style="color:#f92672">+</span> <span style="color:#e6db74">&#34;Z&#34;</span>,
</span></span><span style="display:flex;"><span>}
</span></span></code></pre></div><h3 id="what-is-the-minimum-snapshot-to-resume-safely">What is the minimum snapshot to resume safely?</h3>
<p>At minimum, store: current step pointer, latest stable memory reference, workspace location, and decision log hash. If those four are present, the agent can continue with minimal ambiguity. If your run includes external systems, include external dependency versions and response IDs too.</p>
<h3 id="what-should-i-do-when-a-resume-attempt-fails">What should I do when a resume attempt fails?</h3>
<p>Treat resume failure as a recoverable business event: do a triage branch, write a root-cause summary into memory, and restart from the last validated snapshot. You should never continue blindly from an unverified checkpoint. Failing closed is usually safer and faster to debug than failing open and compounding state drift.</p>
<h2 id="how-do-i-handle-observability-guardrails-and-failure-recovery">How do I handle observability, guardrails, and failure recovery?</h2>
<p>Long-horizon agents fail in predictable ways: timeout, policy denial, tool mismatch, malformed output, and partial writes. v2 workflow design should include structured tracing from the start, not as a postmortem layer. In 2026-06-12 API snapshots, the OpenAI Agents repo had 142 open issues, which is normal for active tooling and reinforces that failure handling is a first-class feature, not an afterthought. A production-ready design tracks state transitions, tool durations, and reason codes per step, then maps each failure mode to an explicit handler.</p>
<table>
  <thead>
      <tr>
          <th>Failure mode</th>
          <th>Symptom</th>
          <th>Recommended handler</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>Tool timeout</td>
          <td>Silent partial output</td>
          <td>Emit checkpoint, retry with reduced scope</td>
      </tr>
      <tr>
          <td>Policy violation</td>
          <td>Action blocked</td>
          <td>Re-route to manual approval branch</td>
      </tr>
      <tr>
          <td>Invalid tool schema</td>
          <td>Parsing error</td>
          <td>Save error artifact + replay with stricter schema</td>
      </tr>
      <tr>
          <td>Snapshot mismatch</td>
          <td>Resume checksum fail</td>
          <td>Create fresh session and regenerate from latest checkpoint</td>
      </tr>
  </tbody>
</table>
<h3 id="what-is-a-useful-logging-format-for-long-tasks">What is a useful logging format for long tasks?</h3>
<p>Use one line per phase with IDs, step number, elapsed time, and decision hash. This makes it easy to query “which retry failed on step 37 and why,” which is far more actionable than logs grouped only by wall clock. Keep logs immutable and store them next to snapshots.</p>
<h3 id="how-do-i-avoid-runaway-autonomous-behavior">How do I avoid runaway autonomous behavior?</h3>
<p>Limit loop depth and tool budget by design, not by hope. Add maximum tool-call counts and escalation rules where ambiguous states trigger human confirmation. A long task should have clear “escalate to human” triggers, so the model never spends hours on a path that no one approved.</p>
<h2 id="how-does-openai-agents-sdk-v2-compare-with-crewai-langgraph-and-autogen">How does OpenAI Agents SDK v2 compare with CrewAI, LangGraph, and AutoGen?</h2>
<p>OpenAI Agents SDK v2 compares differently depending on your primary requirement: rapid onboarding, explicit sandbox governance, or deep orchestration control. The SDK has visible momentum in its GitHub surface (27,092 stars as of 2026-06-12), while LangGraph has 34,490 stars with a stronger checkpoint graph model for complicated branching. CrewAI and AutoGen bring mature multi-agent coordination concepts, and AutoGen’s 58,890 stars suggest heavy production experimentation. I reach for OpenAI Agents when teams need safe execution first and orchestration later, because v2 usually reduces first-pass setup and policy wiring. If your requirement is heavy branch-heavy workflows and explicit control over every state transition, LangGraph remains the baseline I test most often. The practical takeaway is that framework fit is workload-driven, not brand-driven.</p>
<h3 id="how-do-these-frameworks-differ-for-practical-production-tradeoffs">How do these frameworks differ for practical production tradeoffs?</h3>
<p>LangGraph, CrewAI, AutoGen, and OpenAI Agents each optimize a different part of long-horizon workflows. AutoGen and CrewAI are excellent for tool-orchestrated collaboration patterns, but they require more explicit glue for memory governance. LangGraph is still strongest when you need checkpoint primitives and explicit state graphs. OpenAI Agents v2 stays attractive when you want an integrated path: sandbox policy, memory layout, and continuity IDs in one SDK surface.</p>
<table>
  <thead>
      <tr>
          <th>Framework</th>
          <th>Sandbox model</th>
          <th>Memory approach</th>
          <th>Long-horizon strength</th>
          <th>Best fit</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>OpenAI Agents SDK v2</td>
          <td>Built-in sandbox config</td>
          <td>Memory layout + workspace-backed persistence</td>
          <td>Good when paired with IDs and snapshots</td>
          <td>Fast setup, strict policy control</td>
      </tr>
      <tr>
          <td>CrewAI</td>
          <td>Tool-first agent orchestration</td>
          <td>Unified memory API with scoring policies</td>
          <td>Good for agent-level orchestration</td>
          <td>Teams already in CrewAI stack</td>
      </tr>
      <tr>
          <td>LangGraph</td>
          <td>Runtime state graph</td>
          <td>Checkpointers + external stores</td>
          <td>Excellent for complex long-running workflows</td>
          <td>Stateful, event-driven agents</td>
      </tr>
      <tr>
          <td>AutoGen</td>
          <td>Tool-centric loop</td>
          <td>Internal memory strategy patterns</td>
          <td>Strong for multi-agent coordination</td>
          <td>Research-heavy or enterprise agents</td>
      </tr>
  </tbody>
</table>
<h3 id="which-framework-should-i-pick-for-sandbox-first-work">Which framework should I pick for sandbox-first work?</h3>
<p>Pick OpenAI Agents v2 when sandbox policy is your dominant requirement and you want integrated setup. Pick LangGraph when execution logic, branching, and human-in-loop checkpoints are non-negotiable. Pick CrewAI if your team values one-memory API and quick crew composition. Pick AutoGen when multi-agent communication patterns are the core feature.</p>
<h3 id="when-should-i-avoid-openai-agents-v2">When should I avoid OpenAI Agents v2?</h3>
<p>Avoid it for workflows with extreme custom graph orchestration requirements unless your team accepts tradeoffs. If your architecture already depends on highly specialized state transitions and non-linear branching, integrating v2 with a graph runtime can be cleaner than forcing its abstractions.</p>
<h2 id="what-are-the-cost-security-and-performance-tradeoffs-in-real-production-runs">What are the cost, security, and performance tradeoffs in real production runs?</h2>
<p>Cost, security, and performance are not separate checkboxes; they interact through execution policy and memory strategy. Long-running sandbox runs increase compute and storage costs, but poor memory design increases reruns, which often costs more than extra compute in the first place. In practical terms, every recovery attempt that does not resume from precise snapshots burns token and runtime budget. For security, the question is not “can I lock it down?”, it is “can I prove it stayed locked down for each run?” with auditable IDs, logs, and workspace boundaries. The takeaway: optimize for deterministic continuity first, because that reduces both wasted retries and audit complexity.</p>
<table>
  <thead>
      <tr>
          <th>Axis</th>
          <th>Default posture</th>
          <th>Tuning lever</th>
          <th>Common mistake</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>Cost</td>
          <td>Moderate baseline + retries</td>
          <td>Snapshot cadence, tool timeout, output pruning</td>
          <td>Keeping all raw artifacts forever</td>
      </tr>
      <tr>
          <td>Security</td>
          <td>Restrictive sandbox + no network</td>
          <td>Tool allowlists + scoped secrets</td>
          <td>Implicit permissions in prompt text</td>
      </tr>
      <tr>
          <td>Performance</td>
          <td>Variable by checkpoint frequency</td>
          <td>Precompute static dependencies</td>
          <td>Serializing full conversations each step</td>
      </tr>
  </tbody>
</table>
<h3 id="how-can-i-keep-expenses-predictable">How can I keep expenses predictable?</h3>
<p>Track three metrics: average cost per successful run, cost per failed run, and cost per resumed run. If resumed runs approach successful cost, your checkpoints are too expensive; if failed runs are the majority, your policies are too rigid or prompts too ambiguous.</p>
<h3 id="what-is-the-right-security-baseline-for-early-production">What is the right security baseline for early production?</h3>
<p>Start with read-only workspace mounts except where writes are necessary, deny outbound network by default, and require explicit approval gates for high-risk tool calls. Use short-lived credentials and rotate keys per environment. Most breaches in prototype systems come from implicit trust, not code complexity.</p>
<h2 id="faq-what-questions-should-i-verify-before-i-ship-a-long-horizon-pipeline">FAQ: What questions should I verify before I ship a long-horizon pipeline?</h2>
<p>A production check for OpenAI Agents SDK v2 starts with five hard questions that prevent avoidable rebuilds and set your launch confidence level. First, can a run be resumed from a known step after failure without re-running prior work? Second, is memory scoped to minimal required context, not all history? Third, can you prove each run respected sandbox limits without manual log inspection? Fourth, is the cost envelope bounded under failure storms? Fifth, can your observability path produce one canonical trace for support and audit? If any answer is uncertain, the build is not done yet. I also confirm rollback, escalation, and snapshot retention policies before shipping, because these are usually the highest-friction post-launch failures. You should also decide who can approve bypass paths and how long those exceptions remain valid.</p>
<h3 id="how-long-can-a-task-safely-run-before-you-should-split-it">How long can a task safely run before you should split it?</h3>
<p>Split tasks when they cross environment ownership boundaries or when state density exceeds your memory retention policy. Long-horizon does not mean one monolithic run; it means continuity across linked segments. I typically cap autonomous stretches at 20–30 high-confidence steps, then checkpoint and validate.</p>
<h3 id="how-do-i-test-restore-behavior-safely">How do I test restore behavior safely?</h3>
<p>Inject controlled failures in staging: kill the worker at 30%, 60%, and 90% of expected runtime, then verify resume determinism. Your test should check that outputs match the pre-failure checkpoint intent, not just that the run continues.</p>
<h3 id="what-is-the-minimum-security-posture-for-enterprise-use">What is the minimum security posture for enterprise use?</h3>
<p>Minimum posture is explicit sandbox constraints + deny-by-default tool policy + immutable trace logs + human approval for irreversible tool writes. In practice, this eliminates most accidental data exposure incidents in our own pilot programs.</p>
<h3 id="how-do-i-decide-between-v2-native-snapshots-and-custom-graph-checkpointers">How do I decide between v2 native snapshots and custom graph checkpointers?</h3>
<p>If your complexity is mostly linear with bounded branching, v2 native snapshots and memory layouts are enough. If your system needs long cross-thread branching with state transitions, add a graph layer or choose LangGraph for explicit checkpoints.</p>
<h3 id="what-metric-tells-me-the-implementation-is-production-ready">What metric tells me the implementation is production-ready?</h3>
<p>A stable ratio where resumed runs produce consistent outputs and human-review escalations stay below planned threshold. Track that over two weeks before rollout; if you still depend on manual cleanups, you have not yet earned full autonomy.</p>
<h3 id="can-i-use-openai-file-search-with-agents-sdk-v2-memory">Can I use OpenAI file search with Agents SDK v2 memory?</h3>
<p>Yes, but treat file search and memory artifacts as separate systems. File search is a retrieval mechanism; memory layout is continuity state. Keep the boundary clear so one does not silently override the other.</p>
<h3 id="do-i-need-every-conversation-to-have-a-snapshot">Do I need every conversation to have a snapshot?</h3>
<p>No. Snapshot every critical boundary: before destructive operations, before external API calls, and before long tool loops. Over-snapshoting increases cost and slows down operations.</p>
<h3 id="what-should-i-set-first-sandbox-limits-or-memory-layout">What should I set first: sandbox limits or memory layout?</h3>
<p>Set sandbox limits first. If execution can escape policy, memory quality becomes irrelevant because recovery data may include unsafe states.</p>
<h3 id="is-this-tutorial-still-valid-after-assistants-deprecation-plans">Is this tutorial still valid after Assistants deprecation plans?</h3>
<p>Yes, but assume change windows as APIs evolve. Pin versions, keep migration notes in repo, and plan a two-week review cycle for every SDK upgrade.</p>
]]></content:encoded></item><item><title>Zep AI Review 2026: Temporal Knowledge Graphs for Agent Memory</title><link>https://baeseokjae.github.io/posts/zep-ai-agent-memory-review-2026/</link><pubDate>Thu, 07 May 2026 18:04:24 +0000</pubDate><guid>https://baeseokjae.github.io/posts/zep-ai-agent-memory-review-2026/</guid><description>Zep AI uses temporal knowledge graphs to give agents persistent, relationship-aware memory — outperforming Mem0 by 15 points on LongMemEval.</description><content:encoded><![CDATA[<p>Zep AI is a persistent memory layer for AI agents that uses a temporal knowledge graph — not a flat vector store — to track how facts, entities, and relationships evolve over time. In independent benchmarks, Zep scores 63.8% on LongMemEval versus Mem0&rsquo;s 49.0%, a 15-point gap that directly translates to more accurate long-running agent behavior.</p>
<h2 id="what-is-zep-ai-and-why-agent-memory-matters-in-2026">What Is Zep AI? (And Why Agent Memory Matters in 2026)</h2>
<p>Zep AI is a memory infrastructure platform built specifically for AI agents and LLM applications that need to retain context across sessions, remember user preferences, and reason about how facts change over time. Unlike RAG systems that retrieve semantically similar text chunks, Zep builds a temporal knowledge graph from conversations and documents — one where every fact has a validity window (<code>valid_at</code> / <code>invalid_at</code>), every entity has relationships, and stale information is automatically superseded rather than left to confuse retrieval. Launched initially as an open-source project, Zep&rsquo;s core graph engine (Graphiti) crossed 20,000 GitHub stars in 2026 with 25,000 weekly PyPI downloads, signaling mainstream adoption beyond early adopters. The practical impact: Zep delivers up to 90% latency reduction over stuffing full conversation history into context and achieves accuracy improvements of up to 18.5% on reasoning tasks compared to full-context baselines. For production AI agents in healthcare, fintech, or any domain where facts change — think insurance policies, customer account states, medical records — Zep&rsquo;s temporal approach isn&rsquo;t a nice-to-have. It&rsquo;s the difference between an agent that confidently acts on stale data and one that knows what&rsquo;s currently true.</p>
<p>Agent memory matters in 2026 because the shift from single-turn chatbots to long-running, multi-step autonomous agents has exposed a fundamental gap: LLMs have no native memory across sessions. Solutions fall into three categories — context stuffing (expensive, hits token limits fast), vector RAG (cheap but loses relational structure), and structured memory layers like Zep. As agents are deployed in enterprise workflows lasting days or weeks, the quality of the memory layer increasingly determines the quality of the agent.</p>
<h2 id="how-zeps-temporal-knowledge-graph-works">How Zep&rsquo;s Temporal Knowledge Graph Works</h2>
<p>Zep&rsquo;s temporal knowledge graph is built on Graphiti, an open-source library that transforms unstructured conversation history and documents into a structured graph where nodes are entities, edges are relationships, and every edge carries temporal metadata. Unlike static knowledge graphs that capture facts at a point in time, Graphiti models the lifecycle of facts: when a relationship became true, when it was superseded, and what replaced it. For example, if a customer changes their subscription plan from Starter to Enterprise, Graphiti doesn&rsquo;t overwrite the old fact — it closes the <code>valid_at/invalid_at</code> window on the Starter edge and opens a new edge for Enterprise, preserving a queryable history of how the customer&rsquo;s state evolved. This temporal awareness is what drives Zep&rsquo;s 38.4% improvement on temporal reasoning tasks versus full-context approaches. The graph is organized into three tiers: Episodes (raw input — messages, documents, structured data), Entities (extracted people, places, organizations, concepts with deduplication across sessions), and Communities (Leiden algorithm clusters that group related entities for hierarchical context building). When an agent queries Zep, it receives not just semantically similar text but a subgraph of relevant entities, their current relationships, and the temporal context needed to reason correctly.</p>
<p>The ingestion pipeline extracts entities and relationships using an LLM, resolves duplicates (so &ldquo;John Smith&rdquo; and &ldquo;John S.&rdquo; merge into one node), and persists to a graph database — Neo4j, FalkorDB, or Kuzu for self-hosters, or Zep&rsquo;s managed cloud service. Retrieval combines semantic search on node/edge embeddings with graph traversal, meaning a query about a user&rsquo;s current employer returns the most recent employment edge rather than every job ever mentioned.</p>
<h3 id="what-makes-it-different-from-vector-memory">What Makes It Different from Vector Memory</h3>
<p>Vector memory stores embeddings of text chunks and retrieves by cosine similarity — it finds text that <em>sounds like</em> what you&rsquo;re looking for. Zep finds entities and relationships that <em>are</em> what you&rsquo;re looking for, filtered by temporal validity. A vector store asked &ldquo;what is Alice&rsquo;s current job?&rdquo; might return three chunks mentioning three different jobs — whichever is most semantically similar to the query wins. Zep returns the employment relationship with <code>invalid_at = null</code>, meaning the currently valid one.</p>
<h2 id="zep-vs-traditional-vector-memory-the-key-differences">Zep vs. Traditional Vector Memory: The Key Differences</h2>
<p>Zep&rsquo;s graph-based architecture solves several failure modes that plague vector-only memory systems in production. Traditional vector memory systems struggle with five problems that Zep&rsquo;s temporal knowledge graph addresses structurally. First, <strong>fact staleness</strong>: vector stores accumulate contradictory facts — an agent learns a user lives in Boston, then moves to Austin, and subsequent retrievals may surface either fact depending on query phrasing. Zep&rsquo;s temporal edges make the Austin fact the current truth. Second, <strong>relationship blindness</strong>: &ldquo;Alice is Bob&rsquo;s manager&rdquo; is a relationship that can&rsquo;t be faithfully encoded in a vector embedding — the semantics are in the structure, not the text. Third, <strong>deduplication failures</strong>: the same entity mentioned across 50 conversations creates 50 vector entries, bloating retrieval with noise. Zep&rsquo;s entity resolution collapses these into one node. Fourth, <strong>context explosion</strong>: stuffing raw conversation history into context windows to avoid memory loss costs money and hits limits; Zep delivers structured summaries instead. Fifth, <strong>multi-hop reasoning</strong>: answering &ldquo;who manages the team that Alice&rsquo;s direct report leads?&rdquo; requires graph traversal — vector retrieval can&rsquo;t do it. The result is measurable: Zep shows a 184% improvement over full-context retrieval for preference-based questions, where understanding a user&rsquo;s stated preferences across many conversations is critical.</p>
<table>
  <thead>
      <tr>
          <th>Feature</th>
          <th>Zep (Temporal Graph)</th>
          <th>Vector RAG</th>
          <th>Full Context</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>Fact currency</td>
          <td>Temporal validity windows</td>
          <td>Retrieval lottery</td>
          <td>All facts, sorted by recency</td>
      </tr>
      <tr>
          <td>Relationship modeling</td>
          <td>Native graph edges</td>
          <td>Approximated via embeddings</td>
          <td>Available but noisy</td>
      </tr>
      <tr>
          <td>Latency</td>
          <td>&lt;200ms at scale</td>
          <td>50–300ms</td>
          <td>Near-zero (but token-expensive)</td>
      </tr>
      <tr>
          <td>Cost at scale</td>
          <td>Credit-based, predictable</td>
          <td>Token + embedding costs</td>
          <td>Token-dominated, expensive</td>
      </tr>
      <tr>
          <td>Stale fact handling</td>
          <td>Automatic via <code>invalid_at</code></td>
          <td>Manual or ignored</td>
          <td>Manual deletion required</td>
      </tr>
      <tr>
          <td>Multi-hop queries</td>
          <td>Native graph traversal</td>
          <td>Not supported</td>
          <td>Relies on LLM reasoning</td>
      </tr>
  </tbody>
</table>
<h2 id="benchmark-results-zep-vs-mem0-memgpt-and-others">Benchmark Results: Zep vs. Mem0, MemGPT, and Others</h2>
<p>Zep&rsquo;s benchmark performance in 2026 is the strongest argument for its temporal knowledge graph approach. On the DMR (Deep Memory Retrieval) benchmark, Zep achieves 94.8% accuracy versus MemGPT&rsquo;s 93.4% — and 98.2% when using GPT-4o Mini as the backbone, setting a new state-of-the-art. DMR tests whether a memory system can retrieve specific facts from a long conversation history, which is where temporal structure provides the clearest advantage. On LongMemEval — a harder benchmark testing multi-session memory with temporal dependencies — Zep scores 63.8% versus Mem0&rsquo;s 49.0%, a 15-point gap. LongMemEval specifically tests scenarios where facts change over time, exactly the conditions where Zep&rsquo;s validity windows outperform Mem0&rsquo;s vector approach. The latency story is equally compelling: Zep Cloud delivers context retrieval with &lt;200ms p99 latency at enterprise scale, which is essential for synchronous agent loops. On temporal reasoning tasks specifically, Zep shows a 38.4% improvement versus full-context approaches — and a 184% improvement on preference-based questions where the agent needs to synthesize a user&rsquo;s stated preferences across many conversations.</p>
<table>
  <thead>
      <tr>
          <th>Benchmark</th>
          <th>Zep</th>
          <th>Mem0</th>
          <th>MemGPT/Letta</th>
          <th>EverMind AI</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>DMR</td>
          <td>94.8% (98.2% w/ GPT-4o Mini)</td>
          <td>~88%</td>
          <td>93.4%</td>
          <td>Not reported</td>
      </tr>
      <tr>
          <td>LongMemEval</td>
          <td>63.8%</td>
          <td>49.0%</td>
          <td>~55%</td>
          <td>83.0%</td>
      </tr>
      <tr>
          <td>LoCoMo</td>
          <td>Not reported</td>
          <td>Not reported</td>
          <td>Not reported</td>
          <td>93.05%</td>
      </tr>
      <tr>
          <td>Temporal reasoning vs baseline</td>
          <td>+38.4%</td>
          <td>Baseline</td>
          <td>~+15%</td>
          <td>Not comparable</td>
      </tr>
      <tr>
          <td>Preference questions vs baseline</td>
          <td>+184%</td>
          <td>+70%</td>
          <td>+90%</td>
          <td>Not compared</td>
      </tr>
  </tbody>
</table>
<p>One caveat: EverMind AI (EverOS) claims higher LongMemEval scores (83.0%) and LoCoMo scores (93.05%), though these are vendor-reported without independent reproduction as of May 2026. Zep&rsquo;s scores come from peer-reviewed arxiv benchmarks (arxiv.org/abs/2501.13956), lending them higher credibility for enterprise procurement decisions.</p>
<h2 id="zep-cloud-pricing-and-plans">Zep Cloud Pricing and Plans</h2>
<p>Zep Cloud uses a credit-based pricing model where one credit equals one Episode — a unit of input (message, document, or structured data event) up to 350 bytes. The Free plan includes 1,000 credits/month with community support, suitable for prototyping and early-stage development. Paid plans start at usage-based pricing for production workloads, with enterprise plans adding SOC 2 Type 2 and HIPAA compliance certifications — making Zep viable for healthcare and fintech AI agents that would otherwise be blocked by compliance requirements. The credit model can feel opaque for teams trying to estimate costs: a conversation that generates 10 messages creates 10 Episodes (10 credits), but longer messages that exceed 350 bytes may consume additional credits. In practice, most teams report Zep&rsquo;s costs are predictable once they understand their message volume, but the initial estimation requires some trial runs. Enterprise pricing is custom and includes dedicated infrastructure, SLAs, and compliance documentation.</p>
<p>The key pricing change in 2026: Zep deprecated its Community Edition (self-hosted open-source package) in April 2025. Teams that want to self-host now need to run raw Graphiti directly with a supported graph database backend — Neo4j, FalkorDB, or Kuzu — which requires significantly more infrastructure work than the old all-in-one Community Edition. This is a meaningful trade-off: Graphiti itself is MIT-licensed and well-maintained, but you&rsquo;re assembling your own stack versus using a turnkey solution.</p>
<h2 id="zep-graphiti-open-source-self-hosting-in-2026">Zep Graphiti Open Source: Self-Hosting in 2026</h2>
<p>Graphiti is the open-source temporal knowledge graph library that powers Zep Cloud, available under the MIT license at github.com/getzep/graphiti. With 20,000+ GitHub stars and 35+ contributors as of 2026, it&rsquo;s a mature project with active maintenance. For teams with data residency requirements, compliance mandates, or budget constraints that make Zep Cloud impractical, Graphiti offers the same temporal knowledge graph capabilities with full infrastructure control. Self-hosting Graphiti requires choosing and provisioning a graph database backend — Neo4j (enterprise-grade, most mature), FalkorDB (Redis-based, optimized for high-throughput writes), or Kuzu (embedded, good for single-node deployments) — plus a vector store for embedding search, and an LLM for entity extraction. The operational complexity is real: you&rsquo;re responsible for graph database scaling, backup, and the embedding pipeline. For teams already running Neo4j or with strong DevOps capabilities, this is a reasonable path. For teams without graph database experience, the operational burden may outweigh the cost savings versus Zep Cloud.</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-python" data-lang="python"><span style="display:flex;"><span><span style="color:#f92672">from</span> graphiti_core <span style="color:#f92672">import</span> Graphiti
</span></span><span style="display:flex;"><span><span style="color:#f92672">from</span> graphiti_core.nodes <span style="color:#f92672">import</span> EpisodeType
</span></span><span style="display:flex;"><span><span style="color:#f92672">from</span> datetime <span style="color:#f92672">import</span> datetime
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>client <span style="color:#f92672">=</span> Graphiti(<span style="color:#e6db74">&#34;bolt://localhost:7687&#34;</span>, <span style="color:#e6db74">&#34;neo4j&#34;</span>, <span style="color:#e6db74">&#34;password&#34;</span>)
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">await</span> client<span style="color:#f92672">.</span>build_indices_and_constraints()
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#75715e"># Add an episode (conversation turn, document, structured event)</span>
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">await</span> client<span style="color:#f92672">.</span>add_episode(
</span></span><span style="display:flex;"><span>    name<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;user_session_2026_05_07&#34;</span>,
</span></span><span style="display:flex;"><span>    episode_body<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;Alice mentioned she&#39;s now the VP of Engineering at Acme Corp, up from Director.&#34;</span>,
</span></span><span style="display:flex;"><span>    source<span style="color:#f92672">=</span>EpisodeType<span style="color:#f92672">.</span>message,
</span></span><span style="display:flex;"><span>    reference_time<span style="color:#f92672">=</span>datetime<span style="color:#f92672">.</span>now(),
</span></span><span style="display:flex;"><span>    source_description<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;customer support chat&#34;</span>
</span></span><span style="display:flex;"><span>)
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#75715e"># Search with temporal awareness</span>
</span></span><span style="display:flex;"><span>results <span style="color:#f92672">=</span> <span style="color:#66d9ef">await</span> client<span style="color:#f92672">.</span>search(<span style="color:#e6db74">&#34;Alice&#39;s current role at Acme Corp&#34;</span>)
</span></span><span style="display:flex;"><span><span style="color:#75715e"># Returns: VP of Engineering edge with valid_at = today, no invalid_at</span>
</span></span></code></pre></div><h3 id="key-graphiti-configuration-decisions">Key Graphiti Configuration Decisions</h3>
<p>When self-hosting Graphiti, the most impactful decisions are: (1) <strong>LLM choice for entity extraction</strong> — GPT-4o Mini offers the best accuracy-to-cost ratio for extraction tasks; (2) <strong>embedding model</strong> — OpenAI&rsquo;s text-embedding-3-small at 1536 dimensions balances quality and storage; (3) <strong>graph database</strong> — Neo4j for production, Kuzu for development. The community on Discord (2,000+ members) is responsive for configuration questions.</p>
<h2 id="integrations-langchain-langgraph-llamaindex-autogen">Integrations: LangChain, LangGraph, LlamaIndex, AutoGen</h2>
<p>Zep provides first-class integrations with the major Python agent frameworks, reducing integration effort to importing a memory class and passing a session ID. For LangGraph — the most popular agent orchestration framework in 2026 — Zep offers a <code>ZepCheckpointSaver</code> and <code>ZepMemory</code> class that plug directly into the graph execution loop, enabling agents to automatically persist and retrieve memory without custom retrieval logic. LangChain integration uses <code>ZepChatMessageHistory</code> for drop-in conversational memory, compatible with LCEL chains and legacy <code>ConversationChain</code>. LlamaIndex integration includes a <code>ZepVectorStore</code> and <code>ZepMemoryBuffer</code> for RAG pipelines that need persistent user context. AutoGen support allows multi-agent conversations to share a common memory graph — when Agent A learns something about a user, Agent B can retrieve it in the same session without explicit handoff logic.</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-python" data-lang="python"><span style="display:flex;"><span><span style="color:#75715e"># LangGraph integration example</span>
</span></span><span style="display:flex;"><span><span style="color:#f92672">from</span> zep_cloud.langchain <span style="color:#f92672">import</span> ZepChatMessageHistory
</span></span><span style="display:flex;"><span><span style="color:#f92672">from</span> langchain_core.runnables.history <span style="color:#f92672">import</span> RunnableWithMessageHistory
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>memory <span style="color:#f92672">=</span> ZepChatMessageHistory(
</span></span><span style="display:flex;"><span>    session_id<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;user_alice_session_42&#34;</span>,
</span></span><span style="display:flex;"><span>    api_key<span style="color:#f92672">=</span>ZEP_API_KEY,
</span></span><span style="display:flex;"><span>    memory_type<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;perpetual&#34;</span>  <span style="color:#75715e"># uses temporal graph, not just recent turns</span>
</span></span><span style="display:flex;"><span>)
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>chain_with_history <span style="color:#f92672">=</span> RunnableWithMessageHistory(
</span></span><span style="display:flex;"><span>    your_chain,
</span></span><span style="display:flex;"><span>    <span style="color:#66d9ef">lambda</span> session_id: ZepChatMessageHistory(session_id<span style="color:#f92672">=</span>session_id, api_key<span style="color:#f92672">=</span>ZEP_API_KEY),
</span></span><span style="display:flex;"><span>    input_messages_key<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;question&#34;</span>,
</span></span><span style="display:flex;"><span>    history_messages_key<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;history&#34;</span>
</span></span><span style="display:flex;"><span>)
</span></span></code></pre></div><p>The <code>memory_type=&quot;perpetual&quot;</code> parameter is the key difference from plain chat history: it retrieves from the full temporal knowledge graph rather than just the N most recent messages, enabling agents to recall facts from sessions weeks ago.</p>
<h2 id="who-should-use-zep-ai-use-cases-and-best-fits">Who Should Use Zep AI? (Use Cases and Best Fits)</h2>
<p>Zep AI is the strongest choice for AI agents that need to reason about how facts change over time — which covers most serious enterprise deployments. The ideal Zep user is building one of four categories of agents. First, <strong>long-running task agents</strong>: agents that complete multi-day or multi-week workflows (sales prospecting, research synthesis, project management) where context accumulates and facts evolve. Second, <strong>customer-facing agents</strong> in regulated industries: insurance claims bots, healthcare intake assistants, or financial advisory agents where data residency, compliance (SOC 2/HIPAA), and fact accuracy are non-negotiable. Third, <strong>personalization-heavy agents</strong>: e-commerce recommendation agents, personal productivity assistants, or coaching applications where understanding user preferences and how they change over time drives engagement. Fourth, <strong>multi-agent systems</strong> where multiple specialized agents share a common knowledge graph — Zep&rsquo;s entity deduplication prevents the fragmented world-model that emerges when each agent maintains independent vector stores.</p>
<p>Zep is a poor fit for: teams that need a zero-config drop-in memory with minimal operational overhead (use Mem0), teams in the LangChain ecosystem that want free, self-managed memory without graph complexity (use LangMem), or teams that need agents to operate fully autonomously for days without any cloud dependency (use Letta).</p>
<h2 id="zep-alternatives-to-consider">Zep Alternatives to Consider</h2>
<p>The 2026 agent memory market has at least eight serious frameworks — choosing the right one requires matching architecture to use case rather than chasing benchmark numbers. <strong>Mem0</strong> is the most widely adopted framework with ~48K GitHub stars and the simplest integration path. It scores 49.0% on LongMemEval versus Zep&rsquo;s 63.8%, but offers up to 80% prompt token reduction through intelligent history compression and broader framework integrations. Mem0 is the safer default for teams that don&rsquo;t specifically need temporal reasoning — its graph features (Mem0g) are available, but only on the Pro tier at $249/month. <strong>LangMem</strong> is the choice for teams in the LangGraph ecosystem who want a free, open-source solution they control entirely. It uses flat key-value storage with vector search — no entity extraction or relationship modeling — which makes it simpler to reason about but weaker on relational memory tasks. <strong>Letta (formerly MemGPT)</strong> models memory like an OS — main context as RAM, external storage as disk — and is the only framework designed for agents that need to run autonomously for days without human interaction. <strong>Cognee</strong> implements GraphRAG for multi-document knowledge base Q&amp;A, better suited for document-heavy research tasks than conversational agent memory. <strong>EverMind AI (EverOS)</strong> claims the highest published benchmarks (LoCoMo 93.05%, LongMemEval 83.0%) but is earlier-stage with less community adoption and fewer integration options.</p>
<table>
  <thead>
      <tr>
          <th>Framework</th>
          <th>Best For</th>
          <th>LongMemEval</th>
          <th>Pricing</th>
          <th>Self-Host</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>Zep</td>
          <td>Temporal reasoning, enterprise</td>
          <td>63.8%</td>
          <td>Credits (free tier)</td>
          <td>Graphiti (complex)</td>
      </tr>
      <tr>
          <td>Mem0</td>
          <td>General use, broad integrations</td>
          <td>49.0%</td>
          <td>Free / $249/mo Pro</td>
          <td>Yes (open source)</td>
      </tr>
      <tr>
          <td>LangMem</td>
          <td>LangGraph teams, zero cost</td>
          <td>Not published</td>
          <td>Free</td>
          <td>Yes</td>
      </tr>
      <tr>
          <td>Letta</td>
          <td>Days-long autonomous agents</td>
          <td>~55%</td>
          <td>Open source + cloud</td>
          <td>Yes</td>
      </tr>
      <tr>
          <td>Cognee</td>
          <td>Document GraphRAG</td>
          <td>Not published</td>
          <td>Open source</td>
          <td>Yes</td>
      </tr>
      <tr>
          <td>EverMind AI</td>
          <td>Highest benchmark claims</td>
          <td>83.0% (vendor)</td>
          <td>Contact sales</td>
          <td>Limited</td>
      </tr>
  </tbody>
</table>
<h2 id="verdict-is-zep-ai-worth-it-in-2026">Verdict: Is Zep AI Worth It in 2026?</h2>
<p>Zep AI earns a strong recommendation for teams building agents where temporal accuracy matters — and in 2026, that&rsquo;s most serious production deployments. The benchmark case is clear: 63.8% on LongMemEval versus Mem0&rsquo;s 49.0%, 94.8% on DMR, and 90% latency reduction versus full-context approaches are peer-reviewed numbers, not marketing claims. Zep&rsquo;s temporal knowledge graph architecture solves real problems — stale facts, relationship blindness, and multi-hop reasoning — that vector-only solutions handle poorly. The tradeoffs are real: the deprecation of Community Edition in April 2025 means self-hosting now requires operational investment in Graphiti plus a graph database, and the credit-based pricing model rewards teams that spend time estimating their Episode volume upfront. For teams that want the best-in-class temporal memory without operational complexity, Zep Cloud with its &lt;200ms latency, SOC 2/HIPAA compliance, and first-class LangGraph integration is worth the cost. For teams with data residency requirements and DevOps capacity, Graphiti open source delivers the same temporal graph engine with full infrastructure control.</p>
<p><strong>Use Zep if</strong>: you&rsquo;re building agents that run for hours to weeks, your domain involves facts that change (customer state, account balances, project status), or you need compliance certifications for regulated industries.</p>
<p><strong>Skip Zep if</strong>: you need a zero-config memory layer for a simple chatbot (Mem0), your entire stack is LangGraph and cost is the primary concern (LangMem), or your agent needs to run independently for days without cloud calls (Letta).</p>
<p>Rating: <strong>4.4/5</strong> — Best-in-class for temporal agent memory with real benchmark backing; minus points for the Community Edition deprecation and credit-model opacity.</p>
<hr>
<h2 id="faq">FAQ</h2>
<p>These frequently asked questions cover the most common decision points developers and architects face when evaluating Zep AI for production agent deployments in 2026. The answers draw on Zep&rsquo;s official documentation, peer-reviewed benchmark papers (arxiv.org/abs/2501.13956), and community feedback from teams running Zep in production across healthcare, fintech, and enterprise SaaS. Key topics: how Zep compares to Mem0 on real benchmarks, whether Graphiti open source is a viable self-hosting path after the Community Edition deprecation in April 2025, and how to estimate Zep Cloud credit costs before committing to a paid plan. If you&rsquo;re evaluating Zep for a regulated-industry deployment, the SOC 2 Type 2 and HIPAA compliance certifications make it one of the few agent memory platforms cleared for healthcare data. For teams on a tight budget, the Graphiti open-source path offers the same temporal graph engine without per-episode costs — at the cost of operational complexity. The FAQ below addresses all of these scenarios.</p>
<h3 id="what-is-zep-ai-used-for">What is Zep AI used for?</h3>
<p>Zep AI is used to give AI agents persistent, relationship-aware memory across sessions. It&rsquo;s particularly suited for agents that need to track how facts change over time — customer state, user preferences, project status — and for regulated industries where SOC 2 Type 2 and HIPAA compliance are required. Common use cases include customer support agents, sales automation, healthcare intake assistants, and long-running research or coding agents.</p>
<h3 id="how-does-zep-compare-to-mem0-for-agent-memory">How does Zep compare to Mem0 for agent memory?</h3>
<p>Zep outperforms Mem0 by 15 points on LongMemEval (63.8% vs 49.0%), primarily because Zep&rsquo;s temporal knowledge graph handles facts that change over time, while Mem0 uses vector search that can return stale information. Mem0 has broader integrations, simpler setup, and ~48K GitHub stars versus Zep&rsquo;s smaller community. Mem0 is the safer default for general use; Zep is the better choice when temporal accuracy is critical.</p>
<h3 id="is-zep-ai-open-source">Is Zep AI open source?</h3>
<p>Zep Cloud is a commercial SaaS product. Graphiti — the temporal knowledge graph library that powers Zep — is fully open source under the MIT license with 20,000+ GitHub stars. Zep&rsquo;s Community Edition (a self-hosted all-in-one package) was deprecated in April 2025. Teams that want to self-host now use Graphiti directly with Neo4j, FalkorDB, or Kuzu as the graph database backend.</p>
<h3 id="what-is-graphiti-and-how-does-it-relate-to-zep">What is Graphiti and how does it relate to Zep?</h3>
<p>Graphiti is the open-source temporal knowledge graph library built by the Zep team, available at github.com/getzep/graphiti. It&rsquo;s the core engine that powers Zep Cloud&rsquo;s memory layer. Graphiti transforms unstructured conversations and documents into a structured graph where every fact has temporal validity windows (<code>valid_at</code> / <code>invalid_at</code>), enabling agents to always retrieve the currently-true version of a fact rather than accumulating contradictions.</p>
<h3 id="what-is-zep-clouds-pricing">What is Zep Cloud&rsquo;s pricing?</h3>
<p>Zep Cloud offers a Free plan with 1,000 credits/month (1 credit = 1 Episode up to 350 bytes), suitable for development and low-volume production. Paid plans scale by credit volume with custom pricing for enterprise needs. Enterprise plans add SOC 2 Type 2 and HIPAA compliance, dedicated infrastructure, and SLAs. The credit model requires understanding your message volume to predict costs — most teams use the free tier for 2–4 weeks to calibrate before committing to a paid plan.</p>
]]></content:encoded></item><item><title>Mem0 Guide 2026: Add Persistent Memory to Your AI Agents</title><link>https://baeseokjae.github.io/posts/mem0-agent-memory-guide-2026/</link><pubDate>Thu, 07 May 2026 12:00:00 +0000</pubDate><guid>https://baeseokjae.github.io/posts/mem0-agent-memory-guide-2026/</guid><description>Add persistent memory to AI agents with Mem0: architecture, quick start, memory scoping, LangChain integration, pricing, and production best practices.</description><content:encoded><![CDATA[<p>AI agents without persistent memory lose 80% of context between interactions — every session starts cold, the agent has no recollection of user preferences, past decisions, or accumulated knowledge, and users pay both in frustration and in token costs. Mem0 solves this with a managed memory layer that combines vector search, knowledge graph storage, and key-value caching into a single API. With ~48,000 GitHub stars, a $24M Series A closed in October 2025, and YC backing, Mem0 has become the default choice for teams that want to bolt production-grade memory onto an existing agent in under a day. This guide covers everything you need to go from zero to a memory-enabled agent: architecture internals, quick start code, memory scoping patterns, integration with LangChain and AutoGen, pricing tiers, and how Mem0 compares to Zep and LangGraph Store.</p>
<h2 id="what-is-mem0-and-why-ai-agents-need-persistent-memory">What Is Mem0 and Why AI Agents Need Persistent Memory</h2>
<p>Mem0 is an open-source, managed memory layer for AI agents that persists information across sessions, retrieves relevant context on demand, and updates stored facts adaptively without duplicating entries. AI agents without memory are stateless by design — each LLM call is independent, and anything learned in session one is gone by session two unless the developer manually reconstructs context. At small scale this is annoying; at production scale it is expensive. Persistent memory reduces token usage by 30–60% for repeated tasks by replacing verbose context reconstruction with targeted memory retrieval. The practical impact is measurable: a customer support bot that remembers a user&rsquo;s past tickets cuts average handle time, reduces escalations, and eliminates the user&rsquo;s need to repeat themselves. A coding agent that persists codebase conventions and architectural decisions stops recommending patterns the team has already explicitly rejected. Memory is not a nice-to-have — it is the difference between an agent that improves with use and one that stays perpetually ignorant of everything it has encountered before.</p>
<h2 id="mem0-architecture-vector-graph-and-key-value-combined">Mem0 Architecture: Vector, Graph, and Key-Value Combined</h2>
<p>Mem0&rsquo;s storage architecture is a deliberate hybrid that matches retrieval strategy to information type. Vector storage handles semantic memories — free-form facts, preferences, and conversational context — using embedding similarity to surface relevant entries when a query arrives. Graph storage handles structured entity relationships: &ldquo;Alice works at Acme Corp&rdquo; and &ldquo;Acme Corp uses AWS&rdquo; are two separate facts linked through a shared entity node, making multi-hop retrieval possible without complex query engineering. Key-value storage handles exact-match lookups — flags, configurations, and short factual fields that should be retrieved deterministically rather than semantically. Most memory systems force engineers to pick one of these strategies. Mem0 runs all three simultaneously and applies a routing layer that selects the appropriate backend per retrieval request. This architecture is why Mem0 handles the personalization memory problem well: it can retrieve &ldquo;Alice prefers concise Python answers&rdquo; semantically, look up &ldquo;Alice&rsquo;s subscription tier&rdquo; exactly, and traverse &ldquo;Alice&rsquo;s relationship to her team&rsquo;s codebase conventions&rdquo; through the graph — all in a single memory call. The storage backends are pluggable; the hosted Mem0 Cloud service uses Qdrant for vectors, Neo4j for graph, and Redis for key-value, but the open-source library supports alternative backends including Chroma, Weaviate, Pinecone, and PostgreSQL with pgvector.</p>
<h2 id="quick-start-add-mem0-to-your-ai-agent-in-10-minutes">Quick Start: Add Mem0 to Your AI Agent in 10 Minutes</h2>
<p>Install Mem0 with a single pip command and you have a working memory store in under ten minutes — the hosted cloud tier requires only an API key, no infrastructure setup. The open-source library runs fully locally with default backends configured out of the box. Here is the minimal pattern for adding, searching, and using memory in an agent:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-python" data-lang="python"><span style="display:flex;"><span>pip install mem0ai
</span></span></code></pre></div><div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-python" data-lang="python"><span style="display:flex;"><span><span style="color:#f92672">from</span> mem0 <span style="color:#f92672">import</span> Memory
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>m <span style="color:#f92672">=</span> Memory()
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#75715e"># Store a memory for a specific user</span>
</span></span><span style="display:flex;"><span>m<span style="color:#f92672">.</span>add(<span style="color:#e6db74">&#34;User prefers Python and concise answers&#34;</span>, user_id<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;alice&#34;</span>)
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#75715e"># Store multiple facts in one call (list input)</span>
</span></span><span style="display:flex;"><span>m<span style="color:#f92672">.</span>add([
</span></span><span style="display:flex;"><span>    <span style="color:#e6db74">&#34;Alice is building a FastAPI backend&#34;</span>,
</span></span><span style="display:flex;"><span>    <span style="color:#e6db74">&#34;Alice&#39;s team uses PostgreSQL, not MySQL&#34;</span>,
</span></span><span style="display:flex;"><span>    <span style="color:#e6db74">&#34;Alice prefers async/await over threading&#34;</span>
</span></span><span style="display:flex;"><span>], user_id<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;alice&#34;</span>)
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#75715e"># Retrieve relevant memories via semantic search</span>
</span></span><span style="display:flex;"><span>results <span style="color:#f92672">=</span> m<span style="color:#f92672">.</span>search(<span style="color:#e6db74">&#34;user coding preferences&#34;</span>, user_id<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;alice&#34;</span>)
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">for</span> r <span style="color:#f92672">in</span> results:
</span></span><span style="display:flex;"><span>    print(r[<span style="color:#e6db74">&#34;memory&#34;</span>], r[<span style="color:#e6db74">&#34;score&#34;</span>])
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#75715e"># Retrieve all memories for a user</span>
</span></span><span style="display:flex;"><span>memories <span style="color:#f92672">=</span> m<span style="color:#f92672">.</span>get_all(user_id<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;alice&#34;</span>)
</span></span><span style="display:flex;"><span>context <span style="color:#f92672">=</span> <span style="color:#e6db74">&#34;</span><span style="color:#ae81ff">\n</span><span style="color:#e6db74">&#34;</span><span style="color:#f92672">.</span>join([mem[<span style="color:#e6db74">&#34;memory&#34;</span>] <span style="color:#66d9ef">for</span> mem <span style="color:#f92672">in</span> memories])
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#75715e"># Inject memory context into an LLM prompt</span>
</span></span><span style="display:flex;"><span>system_prompt <span style="color:#f92672">=</span> <span style="color:#e6db74">f</span><span style="color:#e6db74">&#34;&#34;&#34;You are a coding assistant.
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">Known facts about this user:
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74"></span><span style="color:#e6db74">{</span>context<span style="color:#e6db74">}</span><span style="color:#e6db74">
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">Answer concisely and use Python unless the user asks otherwise.&#34;&#34;&#34;</span>
</span></span></code></pre></div><p>For the Mem0 Cloud API (required for graph memory and production rate limits), swap in the <code>MemoryClient</code>:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-python" data-lang="python"><span style="display:flex;"><span><span style="color:#f92672">from</span> mem0 <span style="color:#f92672">import</span> MemoryClient
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>client <span style="color:#f92672">=</span> MemoryClient(api_key<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;your-api-key&#34;</span>)
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>client<span style="color:#f92672">.</span>add(<span style="color:#e6db74">&#34;User is migrating from Flask to FastAPI&#34;</span>, user_id<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;alice&#34;</span>)
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>results <span style="color:#f92672">=</span> client<span style="color:#f92672">.</span>search(<span style="color:#e6db74">&#34;framework preferences&#34;</span>, user_id<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;alice&#34;</span>, limit<span style="color:#f92672">=</span><span style="color:#ae81ff">5</span>)
</span></span></code></pre></div><p>The <code>add()</code> call is where Mem0 does its real work: it runs the input through an LLM extraction pipeline to identify discrete facts, embeds them, checks for conflicts with existing memories, and either inserts new entries or updates existing ones. This extraction step is why Mem0 can accept raw conversation turns rather than pre-formatted facts.</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-python" data-lang="python"><span style="display:flex;"><span><span style="color:#75715e"># Add a raw conversation turn — Mem0 extracts facts automatically</span>
</span></span><span style="display:flex;"><span>messages <span style="color:#f92672">=</span> [
</span></span><span style="display:flex;"><span>    {<span style="color:#e6db74">&#34;role&#34;</span>: <span style="color:#e6db74">&#34;user&#34;</span>, <span style="color:#e6db74">&#34;content&#34;</span>: <span style="color:#e6db74">&#34;I always use black for Python formatting and isort for imports&#34;</span>},
</span></span><span style="display:flex;"><span>    {<span style="color:#e6db74">&#34;role&#34;</span>: <span style="color:#e6db74">&#34;assistant&#34;</span>, <span style="color:#e6db74">&#34;content&#34;</span>: <span style="color:#e6db74">&#34;Got it, I&#39;ll follow black + isort conventions in all code I write for you.&#34;</span>}
</span></span><span style="display:flex;"><span>]
</span></span><span style="display:flex;"><span>m<span style="color:#f92672">.</span>add(messages, user_id<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;alice&#34;</span>)
</span></span></code></pre></div><h2 id="memory-scoping-user-session-and-agent-level-memory">Memory Scoping: User, Session, and Agent-Level Memory</h2>
<p>Mem0 supports three memory scopes — user-level, session-level, and agent-level — and the scope you choose determines both what gets shared and what gets isolated. User-level memory persists across all sessions for a given user ID: preferences, biographical facts, and long-term behavioral patterns belong here. Session-level memory is scoped to a single interaction window and is garbage-collected (or archived) when the session ends: transient context, in-progress task state, and decisions made during a specific conversation belong here. Agent-level memory is scoped to an agent ID independent of any user: shared knowledge about tools, external APIs, codebase conventions, and domain facts that all users of a given agent should benefit from belong here. Getting scoping wrong is a common source of bugs. If you store architectural decisions at session scope, the agent forgets them the next time it runs. If you store sensitive user data at agent scope, you create unintended data sharing across users. The right pattern for most production agents is a three-layer architecture: agent-level facts as a global knowledge base, user-level facts for personalization, and session-level facts for short-term task state.</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-python" data-lang="python"><span style="display:flex;"><span><span style="color:#f92672">from</span> mem0 <span style="color:#f92672">import</span> Memory
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>m <span style="color:#f92672">=</span> Memory()
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#75715e"># Agent-level: shared codebase knowledge</span>
</span></span><span style="display:flex;"><span>m<span style="color:#f92672">.</span>add(
</span></span><span style="display:flex;"><span>    <span style="color:#e6db74">&#34;This codebase uses domain-driven design with hexagonal architecture&#34;</span>,
</span></span><span style="display:flex;"><span>    agent_id<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;coding-assistant-v2&#34;</span>
</span></span><span style="display:flex;"><span>)
</span></span><span style="display:flex;"><span>m<span style="color:#f92672">.</span>add(
</span></span><span style="display:flex;"><span>    <span style="color:#e6db74">&#34;All database access must go through repository classes, never direct ORM queries in handlers&#34;</span>,
</span></span><span style="display:flex;"><span>    agent_id<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;coding-assistant-v2&#34;</span>
</span></span><span style="display:flex;"><span>)
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#75715e"># User-level: personal preferences</span>
</span></span><span style="display:flex;"><span>m<span style="color:#f92672">.</span>add(
</span></span><span style="display:flex;"><span>    <span style="color:#e6db74">&#34;Alice prefers verbose docstrings with type hints&#34;</span>,
</span></span><span style="display:flex;"><span>    user_id<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;alice&#34;</span>,
</span></span><span style="display:flex;"><span>    agent_id<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;coding-assistant-v2&#34;</span>
</span></span><span style="display:flex;"><span>)
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#75715e"># Session-level: transient task context</span>
</span></span><span style="display:flex;"><span>m<span style="color:#f92672">.</span>add(
</span></span><span style="display:flex;"><span>    <span style="color:#e6db74">&#34;Currently refactoring the payment module — do not suggest changes to auth&#34;</span>,
</span></span><span style="display:flex;"><span>    user_id<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;alice&#34;</span>,
</span></span><span style="display:flex;"><span>    agent_id<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;coding-assistant-v2&#34;</span>,
</span></span><span style="display:flex;"><span>    run_id<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;session-20260507-payment-refactor&#34;</span>
</span></span><span style="display:flex;"><span>)
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#75715e"># Retrieve combining user + agent context (scopes merge automatically)</span>
</span></span><span style="display:flex;"><span>results <span style="color:#f92672">=</span> m<span style="color:#f92672">.</span>search(
</span></span><span style="display:flex;"><span>    <span style="color:#e6db74">&#34;how should I structure this service class?&#34;</span>,
</span></span><span style="display:flex;"><span>    user_id<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;alice&#34;</span>,
</span></span><span style="display:flex;"><span>    agent_id<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;coding-assistant-v2&#34;</span>
</span></span><span style="display:flex;"><span>)
</span></span></code></pre></div><p>Session IDs can be any string; UUIDs, timestamps, and task-descriptive slugs all work. The <code>run_id</code> parameter maps to session scope internally.</p>
<h2 id="adaptive-memory-updates-how-mem0-avoids-memory-bloat">Adaptive Memory Updates: How Mem0 Avoids Memory Bloat</h2>
<p>Adaptive memory updates are Mem0&rsquo;s answer to the memory bloat problem that plagues naive vector-store approaches. When you call <code>m.add()</code>, Mem0 does not blindly append a new embedding to the store. Instead, it runs a three-step pipeline: extract discrete facts from the input text, check each fact against existing memories for semantic overlap, and then decide whether to insert a new entry, update an existing entry, or discard the input as a duplicate. This behavior is controlled by the underlying LLM (configurable — OpenAI, Anthropic, or local models all work) and produces a resolution event for each fact: <code>ADD</code>, <code>UPDATE</code>, <code>DELETE</code>, or <code>NONE</code>. The result is a memory store that stays compact and accurate rather than growing unbounded with redundant entries. A user who says &ldquo;I prefer Python&rdquo; in session one and &ldquo;use Python, not JavaScript&rdquo; in session five doesn&rsquo;t accumulate two conflicting entries — Mem0 detects the semantic overlap and updates the single canonical preference. This adaptive behavior is particularly important for long-running agents in customer support and personal assistant contexts, where the same user may interact hundreds of times. Without update semantics, memory stores grow linearly with sessions and retrieval quality degrades as noise accumulates. Mem0&rsquo;s adaptive pipeline keeps the store pruned and coherent.</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-python" data-lang="python"><span style="display:flex;"><span><span style="color:#75715e"># Demonstrate adaptive update behavior</span>
</span></span><span style="display:flex;"><span>m <span style="color:#f92672">=</span> Memory()
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>m<span style="color:#f92672">.</span>add(<span style="color:#e6db74">&#34;Alice prefers Python&#34;</span>, user_id<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;alice&#34;</span>)
</span></span><span style="display:flex;"><span>memories_before <span style="color:#f92672">=</span> m<span style="color:#f92672">.</span>get_all(user_id<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;alice&#34;</span>)
</span></span><span style="display:flex;"><span>print(<span style="color:#e6db74">f</span><span style="color:#e6db74">&#34;Memories before: </span><span style="color:#e6db74">{</span>len(memories_before)<span style="color:#e6db74">}</span><span style="color:#e6db74">&#34;</span>)  <span style="color:#75715e"># 1</span>
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#75715e"># Adding a related fact triggers UPDATE, not INSERT</span>
</span></span><span style="display:flex;"><span>m<span style="color:#f92672">.</span>add(<span style="color:#e6db74">&#34;Alice only uses Python, never JavaScript or TypeScript&#34;</span>, user_id<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;alice&#34;</span>)
</span></span><span style="display:flex;"><span>memories_after <span style="color:#f92672">=</span> m<span style="color:#f92672">.</span>get_all(user_id<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;alice&#34;</span>)
</span></span><span style="display:flex;"><span>print(<span style="color:#e6db74">f</span><span style="color:#e6db74">&#34;Memories after: </span><span style="color:#e6db74">{</span>len(memories_after)<span style="color:#e6db74">}</span><span style="color:#e6db74">&#34;</span>)  <span style="color:#75715e"># Still 1, updated</span>
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#75715e"># Inspect the memory history for a specific memory ID</span>
</span></span><span style="display:flex;"><span>memory_id <span style="color:#f92672">=</span> memories_after[<span style="color:#ae81ff">0</span>][<span style="color:#e6db74">&#34;id&#34;</span>]
</span></span><span style="display:flex;"><span>history <span style="color:#f92672">=</span> m<span style="color:#f92672">.</span>history(memory_id)
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">for</span> h <span style="color:#f92672">in</span> history:
</span></span><span style="display:flex;"><span>    print(h[<span style="color:#e6db74">&#34;event&#34;</span>], h[<span style="color:#e6db74">&#34;old_memory&#34;</span>], <span style="color:#e6db74">&#34;-&gt;&#34;</span>, h[<span style="color:#e6db74">&#34;new_memory&#34;</span>])
</span></span></code></pre></div><p>The <code>m.history()</code> call returns the full audit trail for any memory entry, which is important for debugging unexpected agent behavior and for compliance requirements in regulated environments.</p>
<h2 id="integrating-mem0-with-langchain-autogen-and-custom-agents">Integrating Mem0 with LangChain, AutoGen, and Custom Agents</h2>
<p>Mem0 ships first-party integrations for LangChain and AutoGen, and the REST API makes it straightforward to integrate with any custom agent architecture. The LangChain integration wraps Mem0 as a <code>BaseChatMessageHistory</code> subclass, which means it slots directly into any <code>RunnableWithMessageHistory</code> chain without modifying existing chain logic:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-python" data-lang="python"><span style="display:flex;"><span><span style="color:#f92672">from</span> mem0.integrations.langchain <span style="color:#f92672">import</span> ZepMemory  <span style="color:#75715e"># LangChain-compatible wrapper</span>
</span></span><span style="display:flex;"><span><span style="color:#f92672">from</span> langchain_openai <span style="color:#f92672">import</span> ChatOpenAI
</span></span><span style="display:flex;"><span><span style="color:#f92672">from</span> langchain.chains <span style="color:#f92672">import</span> ConversationChain
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#75715e"># Mem0 as a LangChain message history backend</span>
</span></span><span style="display:flex;"><span><span style="color:#f92672">from</span> mem0 <span style="color:#f92672">import</span> MemoryClient
</span></span><span style="display:flex;"><span><span style="color:#f92672">from</span> langchain.memory <span style="color:#f92672">import</span> ConversationBufferMemory
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>client <span style="color:#f92672">=</span> MemoryClient(api_key<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;your-api-key&#34;</span>)
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">def</span> <span style="color:#a6e22e">get_memory_context</span>(user_id: str, query: str) <span style="color:#f92672">-&gt;</span> str:
</span></span><span style="display:flex;"><span>    <span style="color:#e6db74">&#34;&#34;&#34;Retrieve relevant memories as a formatted string for LangChain prompts.&#34;&#34;&#34;</span>
</span></span><span style="display:flex;"><span>    results <span style="color:#f92672">=</span> client<span style="color:#f92672">.</span>search(query, user_id<span style="color:#f92672">=</span>user_id, limit<span style="color:#f92672">=</span><span style="color:#ae81ff">10</span>)
</span></span><span style="display:flex;"><span>    <span style="color:#66d9ef">if</span> <span style="color:#f92672">not</span> results:
</span></span><span style="display:flex;"><span>        <span style="color:#66d9ef">return</span> <span style="color:#e6db74">&#34;No prior context.&#34;</span>
</span></span><span style="display:flex;"><span>    <span style="color:#66d9ef">return</span> <span style="color:#e6db74">&#34;</span><span style="color:#ae81ff">\n</span><span style="color:#e6db74">&#34;</span><span style="color:#f92672">.</span>join([<span style="color:#e6db74">f</span><span style="color:#e6db74">&#34;- </span><span style="color:#e6db74">{</span>r[<span style="color:#e6db74">&#39;memory&#39;</span>]<span style="color:#e6db74">}</span><span style="color:#e6db74">&#34;</span> <span style="color:#66d9ef">for</span> r <span style="color:#f92672">in</span> results])
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>llm <span style="color:#f92672">=</span> ChatOpenAI(model<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;gpt-4o&#34;</span>)
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">def</span> <span style="color:#a6e22e">run_agent_turn</span>(user_id: str, user_message: str) <span style="color:#f92672">-&gt;</span> str:
</span></span><span style="display:flex;"><span>    <span style="color:#75715e"># Pull relevant memory</span>
</span></span><span style="display:flex;"><span>    memory_context <span style="color:#f92672">=</span> get_memory_context(user_id, user_message)
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>    <span style="color:#75715e"># Build prompt with memory context injected</span>
</span></span><span style="display:flex;"><span>    messages <span style="color:#f92672">=</span> [
</span></span><span style="display:flex;"><span>        (<span style="color:#e6db74">&#34;system&#34;</span>, <span style="color:#e6db74">f</span><span style="color:#e6db74">&#34;You are a helpful assistant.</span><span style="color:#ae81ff">\n\n</span><span style="color:#e6db74">What you know about this user:</span><span style="color:#ae81ff">\n</span><span style="color:#e6db74">{</span>memory_context<span style="color:#e6db74">}</span><span style="color:#e6db74">&#34;</span>),
</span></span><span style="display:flex;"><span>        (<span style="color:#e6db74">&#34;human&#34;</span>, user_message)
</span></span><span style="display:flex;"><span>    ]
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>    response <span style="color:#f92672">=</span> llm<span style="color:#f92672">.</span>invoke(messages)
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>    <span style="color:#75715e"># Persist this interaction to memory</span>
</span></span><span style="display:flex;"><span>    client<span style="color:#f92672">.</span>add([
</span></span><span style="display:flex;"><span>        {<span style="color:#e6db74">&#34;role&#34;</span>: <span style="color:#e6db74">&#34;user&#34;</span>, <span style="color:#e6db74">&#34;content&#34;</span>: user_message},
</span></span><span style="display:flex;"><span>        {<span style="color:#e6db74">&#34;role&#34;</span>: <span style="color:#e6db74">&#34;assistant&#34;</span>, <span style="color:#e6db74">&#34;content&#34;</span>: response<span style="color:#f92672">.</span>content}
</span></span><span style="display:flex;"><span>    ], user_id<span style="color:#f92672">=</span>user_id)
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>    <span style="color:#66d9ef">return</span> response<span style="color:#f92672">.</span>content
</span></span></code></pre></div><p>For AutoGen (AG2), Mem0 integrates as a custom <code>ConversableAgent</code> that intercepts message history and injects retrieved memories into the system prompt:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-python" data-lang="python"><span style="display:flex;"><span><span style="color:#f92672">from</span> autogen <span style="color:#f92672">import</span> ConversableAgent
</span></span><span style="display:flex;"><span><span style="color:#f92672">from</span> mem0 <span style="color:#f92672">import</span> MemoryClient
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>client <span style="color:#f92672">=</span> MemoryClient(api_key<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;your-api-key&#34;</span>)
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">class</span> <span style="color:#a6e22e">Mem0Agent</span>(ConversableAgent):
</span></span><span style="display:flex;"><span>    <span style="color:#66d9ef">def</span> __init__(self, user_id: str, <span style="color:#f92672">*</span>args, <span style="color:#f92672">**</span>kwargs):
</span></span><span style="display:flex;"><span>        self<span style="color:#f92672">.</span>mem0_user_id <span style="color:#f92672">=</span> user_id
</span></span><span style="display:flex;"><span>        super()<span style="color:#f92672">.</span>__init__(<span style="color:#f92672">*</span>args, <span style="color:#f92672">**</span>kwargs)
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>    <span style="color:#66d9ef">def</span> <span style="color:#a6e22e">generate_reply</span>(self, messages, sender<span style="color:#f92672">=</span><span style="color:#66d9ef">None</span>, <span style="color:#f92672">**</span>kwargs):
</span></span><span style="display:flex;"><span>        <span style="color:#75715e"># Inject memory context before generating reply</span>
</span></span><span style="display:flex;"><span>        <span style="color:#66d9ef">if</span> messages:
</span></span><span style="display:flex;"><span>            last_user_msg <span style="color:#f92672">=</span> messages[<span style="color:#f92672">-</span><span style="color:#ae81ff">1</span>]<span style="color:#f92672">.</span>get(<span style="color:#e6db74">&#34;content&#34;</span>, <span style="color:#e6db74">&#34;&#34;</span>)
</span></span><span style="display:flex;"><span>            memories <span style="color:#f92672">=</span> client<span style="color:#f92672">.</span>search(last_user_msg, user_id<span style="color:#f92672">=</span>self<span style="color:#f92672">.</span>mem0_user_id, limit<span style="color:#f92672">=</span><span style="color:#ae81ff">8</span>)
</span></span><span style="display:flex;"><span>            memory_text <span style="color:#f92672">=</span> <span style="color:#e6db74">&#34;</span><span style="color:#ae81ff">\n</span><span style="color:#e6db74">&#34;</span><span style="color:#f92672">.</span>join([m[<span style="color:#e6db74">&#34;memory&#34;</span>] <span style="color:#66d9ef">for</span> m <span style="color:#f92672">in</span> memories])
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>            <span style="color:#75715e"># Prepend memory context to the message list</span>
</span></span><span style="display:flex;"><span>            memory_message <span style="color:#f92672">=</span> {
</span></span><span style="display:flex;"><span>                <span style="color:#e6db74">&#34;role&#34;</span>: <span style="color:#e6db74">&#34;system&#34;</span>,
</span></span><span style="display:flex;"><span>                <span style="color:#e6db74">&#34;content&#34;</span>: <span style="color:#e6db74">f</span><span style="color:#e6db74">&#34;Relevant context from prior sessions:</span><span style="color:#ae81ff">\n</span><span style="color:#e6db74">{</span>memory_text<span style="color:#e6db74">}</span><span style="color:#e6db74">&#34;</span>
</span></span><span style="display:flex;"><span>            }
</span></span><span style="display:flex;"><span>            messages <span style="color:#f92672">=</span> [memory_message] <span style="color:#f92672">+</span> list(messages)
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>        reply <span style="color:#f92672">=</span> super()<span style="color:#f92672">.</span>generate_reply(messages, sender, <span style="color:#f92672">**</span>kwargs)
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>        <span style="color:#75715e"># Store this exchange</span>
</span></span><span style="display:flex;"><span>        <span style="color:#66d9ef">if</span> reply <span style="color:#f92672">and</span> messages:
</span></span><span style="display:flex;"><span>            client<span style="color:#f92672">.</span>add([
</span></span><span style="display:flex;"><span>                {<span style="color:#e6db74">&#34;role&#34;</span>: <span style="color:#e6db74">&#34;user&#34;</span>, <span style="color:#e6db74">&#34;content&#34;</span>: messages[<span style="color:#f92672">-</span><span style="color:#ae81ff">1</span>]<span style="color:#f92672">.</span>get(<span style="color:#e6db74">&#34;content&#34;</span>, <span style="color:#e6db74">&#34;&#34;</span>)},
</span></span><span style="display:flex;"><span>                {<span style="color:#e6db74">&#34;role&#34;</span>: <span style="color:#e6db74">&#34;assistant&#34;</span>, <span style="color:#e6db74">&#34;content&#34;</span>: reply}
</span></span><span style="display:flex;"><span>            ], user_id<span style="color:#f92672">=</span>self<span style="color:#f92672">.</span>mem0_user_id)
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>        <span style="color:#66d9ef">return</span> reply
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>agent <span style="color:#f92672">=</span> Mem0Agent(
</span></span><span style="display:flex;"><span>    user_id<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;alice&#34;</span>,
</span></span><span style="display:flex;"><span>    name<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;MemoryAgent&#34;</span>,
</span></span><span style="display:flex;"><span>    system_message<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;You are a helpful coding assistant.&#34;</span>,
</span></span><span style="display:flex;"><span>    llm_config<span style="color:#f92672">=</span>{<span style="color:#e6db74">&#34;model&#34;</span>: <span style="color:#e6db74">&#34;gpt-4o&#34;</span>}
</span></span><span style="display:flex;"><span>)
</span></span></code></pre></div><p>For custom agents built on raw LLM API calls, the pattern is the same: call <code>client.search()</code> before the LLM call, inject results into the system prompt, and call <code>client.add()</code> after the response is generated.</p>
<h2 id="mem0-pricing-free-tier-pro-and-when-the-graph-features-matter">Mem0 Pricing: Free Tier, Pro, and When the Graph Features Matter</h2>
<p>Mem0&rsquo;s pricing has three tiers that map cleanly to deployment stage: Free, Developer at $19/month, and Pro at $249/month. The Free tier gives you 10,000 memories with vector search, full SDK access, and no credit card required — enough to validate a use case and run a non-trivial pilot. The Developer tier at $19/month raises the ceiling to 50,000 memories and adds priority support. The Pro tier at $249/month is where graph memory features unlock: unlimited memories, knowledge graph queries, multi-hop entity resolution, and the SOC 2 Type II and HIPAA compliance documentation required for enterprise procurement. The key decision point is whether your use case requires graph memory. If you are building a personalization layer — storing user preferences, past conversation context, behavioral patterns — the vector tier at Developer pricing handles it efficiently. If you are building an agent that needs to reason about relationships between entities (customer X has contract with vendor Y, who has a known issue with product Z), the graph features in Pro become load-bearing. HIPAA compliance is a hard requirement for any healthcare-adjacent deployment; Mem0 Cloud Pro is one of the few managed memory services with the compliance documentation to satisfy enterprise security reviews. Self-hosting the open-source library is always an option for teams that cannot send data to a third-party cloud regardless of compliance certifications.</p>
<table>
  <thead>
      <tr>
          <th>Tier</th>
          <th>Price</th>
          <th>Memories</th>
          <th>Graph Memory</th>
          <th>Compliance</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>Free</td>
          <td>$0</td>
          <td>10K</td>
          <td>No</td>
          <td>—</td>
      </tr>
      <tr>
          <td>Developer</td>
          <td>$19/mo</td>
          <td>50K</td>
          <td>No</td>
          <td>—</td>
      </tr>
      <tr>
          <td>Pro</td>
          <td>$249/mo</td>
          <td>Unlimited</td>
          <td>Yes</td>
          <td>SOC 2 Type II, HIPAA</td>
      </tr>
  </tbody>
</table>
<h2 id="mem0-vs-zep-vs-langgraph-store-when-to-use-each">Mem0 vs Zep vs LangGraph Store: When to Use Each</h2>
<p>Mem0, Zep, and LangGraph Store solve overlapping but distinct problems, and choosing the wrong one creates technical debt that compounds over time. Mem0 leads on developer experience and deployment speed — the API is simple, the free tier is generous, the SDK covers Python and TypeScript, and you can be in production in hours rather than days. Zep&rsquo;s Graphiti engine scores 63.8% on LongMemEval versus Mem0&rsquo;s 49.0%, a 15-point gap that comes entirely from temporal fact tracking: Zep stores when facts became true and when they stopped being true, which is essential for any use case where the world changes and historical accuracy matters. LangGraph Store is the right answer if your team is already committed to LangGraph for orchestration — it shares state management with your graph nodes, reduces the number of moving parts, and the integration overhead is near zero. The decision framework: pick Mem0 if you need personalization memory deployed quickly across any LLM or framework. Pick Zep if your agent needs to reason about entities whose facts change over time (regulatory compliance, product catalogs, organizational structures). Pick LangGraph Store if your agent is a LangGraph graph and keeping the stack homogeneous matters more than specialized memory features.</p>
<table>
  <thead>
      <tr>
          <th>Criterion</th>
          <th>Mem0</th>
          <th>Zep</th>
          <th>LangGraph Store</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>LongMemEval score</td>
          <td>49.0%</td>
          <td>63.8%</td>
          <td>Not published</td>
      </tr>
      <tr>
          <td>Temporal fact tracking</td>
          <td>Limited</td>
          <td>Native (Graphiti)</td>
          <td>Limited</td>
      </tr>
      <tr>
          <td>Developer experience</td>
          <td>Excellent</td>
          <td>Good</td>
          <td>Good (within LangGraph)</td>
      </tr>
      <tr>
          <td>Standalone deployment</td>
          <td>Yes</td>
          <td>Yes</td>
          <td>LangGraph-only</td>
      </tr>
      <tr>
          <td>Graph memory</td>
          <td>Pro tier</td>
          <td>Yes</td>
          <td>No</td>
      </tr>
      <tr>
          <td>Open-source</td>
          <td>Yes (MIT)</td>
          <td>Yes (Apache 2.0)</td>
          <td>Yes (MIT)</td>
      </tr>
      <tr>
          <td>Best fit</td>
          <td>Personalization, fast deployment</td>
          <td>Entity-heavy, temporal facts</td>
          <td>LangGraph-first teams</td>
      </tr>
  </tbody>
</table>
<p>For teams evaluating both Mem0 and Zep, the practical test is to run LongMemEval on your own data. The benchmark uses multi-session conversation histories that require temporal fact retrieval — if your use case resembles the benchmark (customer support, personal assistants, research agents), Zep&rsquo;s 15-point lead will show up in production metrics. If your use case is simpler (preference retrieval, conversation summarization), Mem0&rsquo;s lower complexity and better developer experience often win.</p>
<h2 id="production-considerations-performance-privacy-and-compliance">Production Considerations: Performance, Privacy, and Compliance</h2>
<p>Running Mem0 in production surfaces three categories of concern that are easy to overlook during prototyping: latency budgets, data residency, and memory quality degradation. On latency: Mem0 Cloud&rsquo;s vector search returns in 10–50ms for typical memory stores under 100K entries; graph queries run 80–200ms for single-hop lookups and 200–500ms for multi-hop. If your agent is latency-sensitive, run memory retrieval in parallel with the LLM call using async, not sequentially before it. The <code>add()</code> call — which runs LLM-based fact extraction and deduplication — runs 300–800ms and should always be done asynchronously after the response is returned to the user, not in the hot path.</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-python" data-lang="python"><span style="display:flex;"><span><span style="color:#f92672">import</span> asyncio
</span></span><span style="display:flex;"><span><span style="color:#f92672">from</span> mem0 <span style="color:#f92672">import</span> AsyncMemoryClient
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>client <span style="color:#f92672">=</span> AsyncMemoryClient(api_key<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;your-api-key&#34;</span>)
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">async</span> <span style="color:#66d9ef">def</span> <span style="color:#a6e22e">agent_turn</span>(user_id: str, user_message: str) <span style="color:#f92672">-&gt;</span> str:
</span></span><span style="display:flex;"><span>    <span style="color:#75715e"># Retrieve memory and call LLM in parallel</span>
</span></span><span style="display:flex;"><span>    memory_task <span style="color:#f92672">=</span> asyncio<span style="color:#f92672">.</span>create_task(
</span></span><span style="display:flex;"><span>        client<span style="color:#f92672">.</span>search(user_message, user_id<span style="color:#f92672">=</span>user_id, limit<span style="color:#f92672">=</span><span style="color:#ae81ff">8</span>)
</span></span><span style="display:flex;"><span>    )
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>    <span style="color:#75715e"># Start LLM call with placeholder context, or await memory first</span>
</span></span><span style="display:flex;"><span>    memories <span style="color:#f92672">=</span> <span style="color:#66d9ef">await</span> memory_task
</span></span><span style="display:flex;"><span>    context <span style="color:#f92672">=</span> <span style="color:#e6db74">&#34;</span><span style="color:#ae81ff">\n</span><span style="color:#e6db74">&#34;</span><span style="color:#f92672">.</span>join([m[<span style="color:#e6db74">&#34;memory&#34;</span>] <span style="color:#66d9ef">for</span> m <span style="color:#f92672">in</span> memories])
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>    <span style="color:#75715e"># Run LLM (simplified)</span>
</span></span><span style="display:flex;"><span>    response <span style="color:#f92672">=</span> <span style="color:#66d9ef">await</span> call_llm(user_message, context)
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>    <span style="color:#75715e"># Store the exchange asynchronously — don&#39;t await in hot path</span>
</span></span><span style="display:flex;"><span>    asyncio<span style="color:#f92672">.</span>create_task(
</span></span><span style="display:flex;"><span>        client<span style="color:#f92672">.</span>add([
</span></span><span style="display:flex;"><span>            {<span style="color:#e6db74">&#34;role&#34;</span>: <span style="color:#e6db74">&#34;user&#34;</span>, <span style="color:#e6db74">&#34;content&#34;</span>: user_message},
</span></span><span style="display:flex;"><span>            {<span style="color:#e6db74">&#34;role&#34;</span>: <span style="color:#e6db74">&#34;assistant&#34;</span>, <span style="color:#e6db74">&#34;content&#34;</span>: response}
</span></span><span style="display:flex;"><span>        ], user_id<span style="color:#f92672">=</span>user_id)
</span></span><span style="display:flex;"><span>    )
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>    <span style="color:#66d9ef">return</span> response
</span></span></code></pre></div><p>On data residency: Mem0 Cloud stores data on AWS infrastructure. If your use case involves EU user data, verify that Mem0&rsquo;s data processing agreement covers GDPR requirements for your jurisdiction. For regulated data (PHI under HIPAA, financial data under SOC 2 requirements), the Pro tier&rsquo;s compliance certifications are the starting point — not the ending point. Review the shared responsibility model, configure data deletion hooks for right-to-erasure requests, and audit which LLM provider Mem0 uses for its fact extraction pipeline (OpenAI by default; this can be configured to Anthropic or local models for data residency reasons).</p>
<p>On memory quality: memory stores degrade when agents add low-quality or contradictory entries at high volume. Three mitigations work well in practice. First, filter what you add — not every message turn deserves to be stored. Use a lightweight classifier (even a simple regex or keyword filter) to identify turns with extractable facts before calling <code>m.add()</code>. Second, set TTLs on session-scoped memories to prevent stale task context from polluting future sessions. Third, review memory stores periodically using <code>m.get_all()</code> and build a manual curation interface for production deployments where memory quality directly affects user outcomes.</p>
<hr>
<h2 id="faq">FAQ</h2>
<p><strong>Q: Does Mem0 work with local LLMs like Ollama or llama.cpp?</strong></p>
<p>Yes. Mem0&rsquo;s fact extraction and deduplication pipeline uses a configurable LLM. You can point it at any OpenAI-compatible endpoint, including Ollama running locally. Set the <code>llm</code> config block in your <code>Memory()</code> constructor to use <code>openai</code> provider with a custom <code>base_url</code> and <code>model</code> matching your local server. Note that fact extraction quality depends on the LLM&rsquo;s instruction-following capability; models under 7B parameters often produce lower-quality extractions.</p>
<p><strong>Q: How does Mem0 handle PII in stored memories?</strong></p>
<p>Mem0 does not automatically redact PII before storage. If users say &ldquo;my social security number is X,&rdquo; that fact will be extracted and stored. For production deployments handling PII, implement a pre-processing step that runs a PII detection library (Microsoft Presidio is a common choice) on user input before calling <code>m.add()</code>. On the cloud tier, configure data deletion webhooks to handle right-to-erasure requests from users.</p>
<p><strong>Q: What happens when two agents access the same user&rsquo;s memories simultaneously?</strong></p>
<p>Mem0 Cloud handles concurrent reads safely — multiple agents can call <code>m.search()</code> for the same user simultaneously without conflict. Concurrent writes to the same user&rsquo;s memory store are serialized internally to prevent race conditions in the deduplication pipeline, which means two simultaneous <code>m.add()</code> calls for the same user will queue rather than execute in parallel. In high-throughput scenarios, batch your <code>add()</code> calls or use a queue to control write concurrency.</p>
<p><strong>Q: Can I migrate from Mem0 Cloud to self-hosted without losing memories?</strong></p>
<p>Yes. Use <code>m.get_all()</code> with pagination to export all memories to JSON, then re-import them into a self-hosted Mem0 instance using <code>m.add()</code>. The memory IDs will change (the self-hosted instance generates new IDs), so update any external references accordingly. Mem0 does not currently offer a native export/import CLI command, so the migration script needs to be written manually — it is around 30 lines of Python.</p>
<p><strong>Q: How does Mem0 compare to simply storing chat history in a database and summarizing it?</strong></p>
<p>Naive chat history + summarization works at small scale and fails in three ways at production scale. Summarization is lossy — facts get dropped or distorted as conversation length grows. Summaries don&rsquo;t support semantic retrieval — you can&rsquo;t efficiently answer &ldquo;what did this user say about their database preferences?&rdquo; against a summary blob. And the token cost of passing full summaries into every LLM call grows linearly with session count. Mem0&rsquo;s approach — discrete extracted facts with semantic retrieval and adaptive deduplication — solves all three problems. The tradeoff is that Mem0 adds an LLM call per <code>add()</code> operation (for fact extraction), whereas raw summary storage does not. For most use cases, the token savings from targeted retrieval more than offset the extraction overhead.</p>
]]></content:encoded></item><item><title>AI Agent Memory Architecture Guide 2026: Mem0, Zep, LangGraph Store Compared</title><link>https://baeseokjae.github.io/posts/agent-memory-architecture-guide-2026/</link><pubDate>Thu, 07 May 2026 00:00:00 +0000</pubDate><guid>https://baeseokjae.github.io/posts/agent-memory-architecture-guide-2026/</guid><description>AI agent memory architecture guide 2026: Mem0 vs Zep vs Letta vs LangGraph Store — benchmarks, pricing, temporal knowledge graphs, and which framework f...</description><content:encoded><![CDATA[<p>Zep scores 63.8% versus Mem0&rsquo;s 49.0% on the LongMemEval benchmark — a 15-point gap that comes entirely from Zep&rsquo;s temporal knowledge graph tracking when facts were true and when they changed. Mem0 has 48,000 GitHub stars, a $24M Series A, and the broadest standalone memory API. Letta raised $10M at a $70M valuation with Jeff Dean backing, building OS-inspired tiered memory where agents control their own context. Adding a memory context layer to a Snowflake data agent produced 20% accuracy improvement and 39% fewer tool calls. These numbers explain why agent memory architecture is now a first-class infrastructure decision — not an afterthought. Here&rsquo;s how the major approaches compare and which to use.</p>
<h2 id="why-ai-agent-memory-architecture-matters-in-2026">Why AI Agent Memory Architecture Matters in 2026</h2>
<p>AI agent memory architecture refers to the systems and strategies that give AI agents access to information beyond what fits in a single LLM context window. Without persistent memory, every agent session starts from scratch: the agent has no recall of past interactions, no accumulated knowledge about users or entities, and no ability to learn from previous tasks. In 2026, with agents running complex multi-step workflows and coordinating across sessions that span days or weeks, this limitation is directly responsible for productivity losses and poor user experiences. The two memory problems are fundamentally different: personalization memory (knowing that this user prefers concise answers) versus institutional knowledge (knowing that Company A&rsquo;s procurement policy changed in March). Most frameworks solve one well and struggle with the other. Mem0&rsquo;s vector-first approach excels at personalization across interactions. Zep&rsquo;s temporal knowledge graph handles institutional knowledge where facts change over time. Letta&rsquo;s OS-inspired tiered model serves long-running agents that manage their own context evolution. Understanding which problem you&rsquo;re solving determines which architecture to choose. The governance gap makes this harder: 76% of organizations report that AI governance frameworks lag AI adoption, and most memory frameworks aren&rsquo;t built to close that gap in regulated environments.</p>
<h2 id="the-two-memory-problems-personalization-vs-institutional-knowledge">The Two Memory Problems: Personalization vs Institutional Knowledge</h2>
<p>The personalization memory problem is relatively tractable. An agent needs to remember user preferences, past conversation context, and personal facts across sessions. Vector similarity search works well here: store conversation episodes as embeddings, retrieve the most semantically relevant context when a new session starts. Latency is manageable at 10-50ms for vector retrieval. Mem0&rsquo;s approach — adaptive memory updates that edit rather than duplicate, with user/session/agent-level scoping — handles this reliably at scale.</p>
<p>The institutional knowledge problem is harder. Organizations accumulate facts about products, policies, customers, and processes that change over time. An agent querying institutional knowledge needs to know not just what is true, but when it became true and when it stopped being true. A flat vector store can&rsquo;t answer &ldquo;what was the return policy before the March update?&rdquo; — it just returns the most recent policy embedding. Zep&rsquo;s Graphiti engine stores facts with validity windows: true &ldquo;from X until Y,&rdquo; with entity deduplication across sources. This is why Zep&rsquo;s LongMemEval score (63.8%) beats Mem0 (49.0%) by 15 points — the benchmark specifically tests temporal fact tracking. Hindsight, an MIT-licensed framework by Vectorize.io, achieves 91.4% on LongMemEval using multi-strategy retrieval with LLM-based synthesis across retrieved memories. It&rsquo;s the highest published score on the benchmark but with only ~4K stars and less production validation.</p>
<h2 id="quick-comparison-mem0-vs-zep-vs-letta-vs-langgraph-store">Quick Comparison: Mem0 vs Zep vs Letta vs LangGraph Store</h2>
<p>Choosing an AI agent memory framework requires matching architecture to use case. The frameworks target different problems: Mem0 for fast personalization deployment, Zep for temporal entity tracking, Letta for long-running agents with self-managed context, and LangGraph Store for teams already invested in LangGraph orchestration. The LongMemEval benchmark provides the clearest performance signal — Hindsight leads at 91.4%, Zep scores 63.8%, and Mem0 scores 49.0%. The 42-point gap between Hindsight and Mem0 reflects the power of LLM-based synthesis over retrieved memories versus pure vector retrieval. For production teams, Mem0 and Zep are the two most actively maintained standalone options; LangGraph Store is the right choice if the team is already on LangGraph.</p>
<table>
  <thead>
      <tr>
          <th>Framework</th>
          <th>Stars</th>
          <th>Approach</th>
          <th>LongMemEval</th>
          <th>Best For</th>
          <th>Pricing</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>Mem0</td>
          <td>~48K</td>
          <td>Vector + Graph + KV</td>
          <td>49.0%</td>
          <td>Personalization, broad compatibility</td>
          <td>Free → $249/mo</td>
      </tr>
      <tr>
          <td>Zep</td>
          <td>~24K</td>
          <td>Temporal Knowledge Graph</td>
          <td>63.8%</td>
          <td>Temporal entity tracking</td>
          <td>Self-host or cloud</td>
      </tr>
      <tr>
          <td>Letta</td>
          <td>~21K</td>
          <td>OS-Inspired Tiered</td>
          <td>N/A</td>
          <td>Long-running agents, self-managed context</td>
          <td>$10 seed-stage</td>
      </tr>
      <tr>
          <td>Hindsight</td>
          <td>~4K</td>
          <td>Multi-strategy + LLM Synthesis</td>
          <td>91.4%</td>
          <td>Research/evaluation contexts</td>
          <td>MIT, self-host</td>
      </tr>
      <tr>
          <td>LangGraph Store</td>
          <td>N/A</td>
          <td>Integrated with LangGraph</td>
          <td>N/A</td>
          <td>Teams already using LangGraph</td>
          <td>Part of LangGraph</td>
      </tr>
      <tr>
          <td>Cognee</td>
          <td>~12K</td>
          <td>KG + Vector pipelines</td>
          <td>N/A</td>
          <td>Institutional knowledge from raw docs</td>
          <td>Open source</td>
      </tr>
  </tbody>
</table>
<h2 id="mem0-deep-dive-the-broadest-standalone-memory-layer">Mem0 Deep Dive: The Broadest Standalone Memory Layer</h2>
<p>Mem0 is the most widely adopted standalone AI agent memory framework with approximately 48,000 GitHub stars and the largest active community. YC-backed with $24M Series A closed in October 2025, it&rsquo;s positioned as a universal memory layer that works with any LLM and any agent framework. The technical approach: adaptive memory updates that edit existing memories rather than duplicating them, maintaining user/session/agent-level scoping across a combined vector + graph + key-value store architecture. Integration is straightforward:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-python" data-lang="python"><span style="display:flex;"><span><span style="color:#f92672">from</span> mem0 <span style="color:#f92672">import</span> Memory
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>m <span style="color:#f92672">=</span> Memory()
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#75715e"># Store memory</span>
</span></span><span style="display:flex;"><span>result <span style="color:#f92672">=</span> m<span style="color:#f92672">.</span>add(<span style="color:#e6db74">&#34;I prefer concise answers and Python code examples&#34;</span>, user_id<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;alice&#34;</span>)
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#75715e"># Retrieve relevant memories</span>
</span></span><span style="display:flex;"><span>memories <span style="color:#f92672">=</span> m<span style="color:#f92672">.</span>search(<span style="color:#e6db74">&#34;user preferences&#34;</span>, user_id<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;alice&#34;</span>)
</span></span><span style="display:flex;"><span>print(memories)
</span></span></code></pre></div><p>SOC 2 Type II and HIPAA compliance make Mem0 viable for regulated industries. The pricing model has one notable gap: graph features — which are what make Mem0 competitive for institutional knowledge use cases — require the Pro tier at $249/month. The free tier (10K memories) and Developer tier ($19/month, 50K memories) handle personalization workloads well but don&rsquo;t include the knowledge graph capabilities needed for complex entity relationships. For teams needing the full Mem0 capability set, budget for Pro.</p>
<p>Mem0&rsquo;s strength is breadth and developer experience. The Python and TypeScript SDKs work with LangChain, AutoGen, CrewAI, LlamaIndex, and raw API calls. The managed cloud removes infrastructure concerns. The adaptive update mechanism prevents memory bloat from duplicate or contradictory facts. The weakness: the 49.0% LongMemEval score reflects the fundamental limitations of vector-first retrieval for temporal queries.</p>
<h2 id="zep--graphiti-deep-dive-temporal-knowledge-graph-power">Zep / Graphiti Deep Dive: Temporal Knowledge Graph Power</h2>
<p>Zep&rsquo;s core innovation is Graphiti, a temporal knowledge graph engine that stores facts with validity windows. When a user&rsquo;s job title changes or a company&rsquo;s pricing policy updates, Graphiti records the new fact alongside the timestamp range during which each version was true — not just the current state. Entity deduplication ensures that &ldquo;Alice&rdquo; in one conversation and &ldquo;Alice Smith, CTO&rdquo; in another resolve to the same node. Relationship extraction happens automatically from conversation episodes.</p>
<p>The practical impact: Zep can answer temporal questions that flat vector stores can&rsquo;t. &ldquo;What did the user tell us about their budget last quarter?&rdquo; works because Graphiti preserves the timeline. &ldquo;What was the product pricing before the May update?&rdquo; works because validity windows capture version history. This is what drives Zep&rsquo;s 15-point LongMemEval advantage over Mem0 (63.8% vs 49.0%).</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-python" data-lang="python"><span style="display:flex;"><span><span style="color:#f92672">from</span> zep_cloud.client <span style="color:#f92672">import</span> Zep
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>client <span style="color:#f92672">=</span> Zep(api_key<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;...&#34;</span>)
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#75715e"># Add episode (conversation turn)</span>
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">await</span> client<span style="color:#f92672">.</span>memory<span style="color:#f92672">.</span>add(session_id<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;session_1&#34;</span>, messages<span style="color:#f92672">=</span>[
</span></span><span style="display:flex;"><span>    {<span style="color:#e6db74">&#34;role&#34;</span>: <span style="color:#e6db74">&#34;user&#34;</span>, <span style="color:#e6db74">&#34;content&#34;</span>: <span style="color:#e6db74">&#34;Our budget increased to $50k for Q3&#34;</span>},
</span></span><span style="display:flex;"><span>    {<span style="color:#e6db74">&#34;role&#34;</span>: <span style="color:#e6db74">&#34;assistant&#34;</span>, <span style="color:#e6db74">&#34;content&#34;</span>: <span style="color:#e6db74">&#34;Got it, I&#39;ll note the updated budget.&#34;</span>}
</span></span><span style="display:flex;"><span>])
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#75715e"># Retrieve with temporal context</span>
</span></span><span style="display:flex;"><span>memory <span style="color:#f92672">=</span> <span style="color:#66d9ef">await</span> client<span style="color:#f92672">.</span>memory<span style="color:#f92672">.</span>get(session_id<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;session_1&#34;</span>)
</span></span></code></pre></div><p>Zep&rsquo;s infrastructure is more complex than Mem0&rsquo;s: the Graphiti graph database requires more operational attention than a vector store. The self-hosted Graphiti library has some limitations versus the full Zep Cloud offering. For teams with operational capacity and institutional knowledge tracking requirements, the capability gap justifies the complexity.</p>
<h2 id="letta-memgpt-deep-dive-os-inspired-tiered-memory">Letta (MemGPT) Deep Dive: OS-Inspired Tiered Memory</h2>
<p>Letta (formerly MemGPT, renamed in 2024) takes a fundamentally different architectural approach: agents actively manage their own memory through function calls, similar to how an operating system manages RAM, disk, and cache. Three memory tiers: core memory (always in-context, like RAM — persona and key facts), recall memory (conversation history with semantic search), and archival memory (external searchable store, like disk — unlimited persistent storage).</p>
<p>The unique aspect: agents call memory functions explicitly. Instead of automatic background updates, an agent can call <code>core_memory_replace(&quot;user_preferences&quot;, &quot;prefers detailed technical explanations&quot;)</code> or <code>archival_memory_search(&quot;project requirements from January&quot;)</code>. This makes memory operations visible and auditable — developers can observe exactly what the agent chooses to remember and forget.</p>
<p>Letta raised $10M seed from Felicis Ventures at a $70M post-money valuation, with backing from Jeff Dean (Google DeepMind) and Clem Delangue (Hugging Face). The investor profile signals long-term infrastructure bet, not a feature play. The trade-off: adopting Letta means adopting its runtime, not just adding a library. The learning curve is steeper than Mem0 or Zep — hours to productive use versus minutes. For long-running agents where transparent, developer-controllable memory evolution is more important than developer experience, Letta wins.</p>
<h2 id="benchmark-results-longmemeval-and-architecture-implications">Benchmark Results: LongMemEval and Architecture Implications</h2>
<p>LongMemEval (arXiv:2603.04814) is the primary benchmark for conversational agent memory quality. It tests whether a memory system can accurately recall and reason about facts stated across long conversation histories, including temporal queries that require understanding when facts changed.</p>
<p>Published results with GPT-4o:</p>
<ul>
<li>Hindsight: 91.4% (multi-strategy retrieval + LLM synthesis, MIT license)</li>
<li>Zep: 63.8% (temporal knowledge graph with validity windows)</li>
<li>Mem0: 49.0% (vector + graph combined approach)</li>
</ul>
<p>The performance gap between Hindsight and Zep (27 points) reflects the power of LLM-based synthesis across retrieved memories — not just retrieval, but reasoning about what the retrieved memories collectively mean. This adds latency: LLM synthesis runs 800-3000ms versus vector retrieval at 10-50ms. For use cases where answer quality matters more than latency, Hindsight&rsquo;s approach is compelling. For interactive agent workflows where sub-100ms retrieval is required, pure vector or graph retrieval wins.</p>
<p>The latency profiles by architecture:</p>
<ul>
<li>Vector-only retrieval: 10-50ms</li>
<li>Graph traversal: 50-150ms</li>
<li>Multi-strategy parallel retrieval: 100-600ms</li>
<li>LLM synthesis over retrieved memories: 800-3000ms</li>
<li>Memory ingestion: 500-2000ms</li>
</ul>
<h2 id="architecture-comparison-vector-graph-tiered-and-hybrid-approaches">Architecture Comparison: Vector, Graph, Tiered, and Hybrid Approaches</h2>
<p><strong>Pure vector store</strong> (basic LangChain memory, LlamaIndex memory modules): Semantic similarity retrieval works well for &ldquo;what did we discuss about X&rdquo; queries. Fails for temporal queries and entity relationship tracking. Lowest latency, simplest infrastructure, least capability.</p>
<p><strong>Knowledge graph</strong> (Cognee, Neo4j-backed approaches): Builds entity relationship graphs from unstructured data, enabling relational queries that vector stores can&rsquo;t answer. Cognee (~12K stars) specifically targets institutional knowledge extraction from raw documents — feeding emails, Slack messages, and documents into a graph that agents can query. Stronger than vector stores for organizational knowledge management.</p>
<p><strong>Temporal knowledge graph</strong> (Zep Graphiti): Extends graph storage with validity windows and temporal reasoning. The most capable for tracking how facts change over time. Higher infrastructure complexity, strongest for institutional memory use cases.</p>
<p><strong>OS-inspired tiered</strong> (Letta): Agents control their own memory through explicit function calls. Transparent and auditable, supports unlimited context through archival memory, requires adopting the Letta runtime. Best for long-running agents where memory evolution needs to be developer-observable.</p>
<p><strong>Hybrid multi-strategy</strong> (Mem0 Pro, Hindsight): Combines vector, graph, and keyword retrieval with LLM synthesis for best recall accuracy. Higher latency but higher accuracy. Mem0 Pro gates graph features; Hindsight is open-source but requires self-hosting and operational investment.</p>
<h2 id="decision-framework-which-memory-framework-for-your-use-case">Decision Framework: Which Memory Framework for Your Use Case</h2>
<p><strong>Choose Mem0 if:</strong> You need the fastest path to persistent memory that works with any LLM framework. Your primary use case is personalization across user interactions (remembering preferences, past conversations, user-specific facts). You want managed cloud infrastructure without operational overhead. Budget $249/month for Pro if you need knowledge graph features.</p>
<p><strong>Choose Zep if:</strong> You need temporal reasoning — tracking how facts about entities change over time. Your agents handle institutional knowledge where historical versions of facts matter. You have operational capacity to manage more complex infrastructure. The 15-point LongMemEval advantage over Mem0 is worth the complexity tradeoff.</p>
<p><strong>Choose Letta if:</strong> You&rsquo;re building long-running agents that run for hours or days with extensive context accumulation. You want agents to actively manage their own memory through explicit function calls rather than automatic background updates. Transparency and developer observability of memory operations are requirements. You can invest the learning curve of adopting a full runtime.</p>
<p><strong>Choose LangGraph Store if:</strong> Your team is already using LangGraph for agent orchestration. Minimizing the number of external dependencies matters more than specialized memory features. The integrated experience outweighs the capability gap versus dedicated memory frameworks.</p>
<p><strong>Choose Cognee if:</strong> Your primary source of institutional knowledge is unstructured documents (emails, Slack, PDFs) and you need to build queryable knowledge graphs from them without structured data pipelines.</p>
<hr>
<h2 id="faq">FAQ</h2>
<p><strong>What is AI agent memory and why does it matter?</strong></p>
<p>AI agent memory refers to systems that give AI agents access to information from past interactions and accumulated knowledge beyond what fits in a single LLM context window. Without persistent memory, agents start fresh each session with no recall of past interactions, user preferences, or accumulated knowledge. Adding a context layer to Snowflake data agents produced 20% accuracy improvement and 39% fewer tool calls — these numbers illustrate the practical productivity impact of well-designed agent memory.</p>
<p><strong>Which AI agent memory framework has the best benchmark scores?</strong></p>
<p>Hindsight achieves 91.4% on LongMemEval, the highest published score, using multi-strategy retrieval with LLM synthesis. Among more production-deployed frameworks, Zep scores 63.8% versus Mem0&rsquo;s 49.0% on LongMemEval with GPT-4o — a 15-point gap driven by Zep&rsquo;s temporal knowledge graph with validity windows. The benchmark specifically tests temporal reasoning about facts that change over time, which explains why graph-based approaches outperform pure vector stores.</p>
<p><strong>What is the difference between Mem0 and Zep?</strong></p>
<p>Mem0 uses a combined vector + graph + key-value approach optimized for personalization — remembering user preferences and past interactions. Zep uses a temporal knowledge graph (Graphiti) that tracks when facts were true and when they changed, making it stronger for institutional knowledge and temporal queries. Mem0 has a larger community (~48K vs ~24K GitHub stars), simpler integration, and managed cloud. Zep has a 15-point LongMemEval advantage for use cases requiring temporal reasoning.</p>
<p><strong>How does Letta&rsquo;s memory model work?</strong></p>
<p>Letta (formerly MemGPT) uses an OS-inspired three-tier memory model: core memory (always in the LLM context, like RAM — persona and critical facts), recall memory (conversation history with semantic search access), and archival memory (unlimited external storage, like disk — accessed via explicit search calls). Agents actively manage their own memory through function calls rather than automatic background updates, making memory operations transparent and auditable. This approach requires adopting Letta&rsquo;s runtime, not just adding a library.</p>
<p><strong>Can I use agent memory frameworks with any LLM?</strong></p>
<p>Mem0 and Zep are framework-agnostic and work with any LLM via their APIs — OpenAI, Anthropic, Gemini, or any OpenAI-compatible model. Letta integrates with major LLM providers but requires using the Letta runtime. LangGraph Store integrates specifically with LangGraph&rsquo;s orchestration model. The choice of LLM doesn&rsquo;t significantly affect which memory architecture to use — the decision is driven by use case (personalization vs temporal vs long-running), not by the underlying model.</p>
]]></content:encoded></item></channel></rss>