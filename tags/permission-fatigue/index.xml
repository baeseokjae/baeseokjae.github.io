<?xml version="1.0" encoding="utf-8" standalone="yes"?><rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom" xmlns:content="http://purl.org/rss/1.0/modules/content/"><channel><title>Permission Fatigue on RockB</title><link>https://baeseokjae.github.io/tags/permission-fatigue/</link><description>Recent content in Permission Fatigue on RockB</description><image><title>RockB</title><url>https://baeseokjae.github.io/images/og-default.png</url><link>https://baeseokjae.github.io/images/og-default.png</link></image><generator>Hugo</generator><language>en-us</language><lastBuildDate>Thu, 03 Sep 2026 16:04:33 +0000</lastBuildDate><atom:link href="https://baeseokjae.github.io/tags/permission-fatigue/index.xml" rel="self" type="application/rss+xml"/><item><title>AI Agent Human Oversight: Why Humans Missed 1 in 3 Threats Approving Commands</title><link>https://baeseokjae.github.io/posts/humans-missed-threats-ai-agent-approval-2026/</link><pubDate>Thu, 03 Sep 2026 16:04:33 +0000</pubDate><guid>https://baeseokjae.github.io/posts/humans-missed-threats-ai-agent-approval-2026/</guid><description>A 40,000-session study found humans missed 1 in 3 threats approving AI agent commands. Here&amp;#39;s why human-in-the-loop review fails and how to fix it.</description><content:encoded><![CDATA[<p>When a human reviews an AI agent&rsquo;s commands before they run, they miss roughly one in three threats. That is the headline finding of a study of more than 40,000 sessions and 409,000 individual approve-or-deny decisions, where players reviewed an AI coding agent&rsquo;s shell commands under time pressure and achieved a mean accuracy of just 66.3%. The result is not a verdict on human competence — it is a verdict on a design pattern. Sustained vigilance is something humans are measurably bad at, and any security model that depends on it as the sole safeguard is built to fail. The fix is not better humans; it is better systems that layer sandboxing, scoped credentials, and surgical gating on top of human review.</p>
<h2 id="the-study-40000-sessions-of-human-in-the-loop-approval">The Study: 40,000 Sessions of Human-in-the-Loop Approval</h2>
<p>The data comes from a browser game built by Alex Wauters and hosted at scalex.dev (llmgame.scalex.dev). Players are placed in the role of a human reviewer watching an AI coding agent execute shell commands. For each command, they must decide whether to approve or deny it before it runs — the same decision a developer makes dozens of times a day when an agent like Claude Code, Codex, or Cursor asks for permission to run a command.</p>
<p>Across more than 40,000 sessions, the game collected 409,000 individual approve/deny decisions. The commands ranged from obviously destructive operations like <code>rm -rf /</code> to subtle, camouflaged attacks hidden inside familiar npm scripts. Roughly 34% of the commands in the game were genuinely malicious — a threat density far above what any real developer encounters, which matters for how the results should be read.</p>
<p>Because players knew they were being tested and the threat density was artificially high, the 66.3% accuracy figure is best understood as an optimistic ceiling, not a realistic floor. In real work, where threats are rarer and attention is divided across many tasks, performance would almost certainly be worse, not better.</p>
<h2 id="the-headline-numbers-1-in-3-threats-missed">The Headline Numbers: 1 in 3 Threats Missed</h2>
<p>The central statistic is stark: players missed roughly one in three threats. Across the full dataset, mean accuracy was 66.3%, meaning that for every three malicious commands a reviewer faced, one slipped through.</p>
<p>The deeper numbers are more revealing:</p>
<ul>
<li><strong>32.9% of sessions ended net-negative</strong> — the player approved threats and blocked safe commands more often than they made correct calls, meaning their review actively made the system worse than no review at all.</li>
<li><strong>35.2% of players caught every threat</strong>, but only <strong>20.8%</strong> did so while blocking at most one in five safe commands. In other words, most people who caught everything did so by being so restrictive that the agent could barely function.</li>
<li><strong>7% of players approved every single prompt</strong> — the equivalent of running the agent with <code>--dangerously-skip-permissions</code> permanently enabled.</li>
</ul>
<p>These numbers describe a dilemma at the heart of human-in-the-loop approval. A reviewer can be thorough, or they can be usable, but the study suggests very few people manage both. The 7% who approved everything and the 32.9% who ended net-negative are the two failure modes of the same broken pattern.</p>
<h2 id="not-all-misses-are-equal-threat-categories-and-miss-rates">Not All Misses Are Equal: Threat Categories and Miss Rates</h2>
<p>The study broke down miss rates by threat category, and the differences are dramatic. Humans are excellent at spotting threats that look destructive, and poor at spotting threats that look like normal work.</p>
<table>
  <thead>
      <tr>
          <th>Threat category</th>
          <th>Example</th>
          <th>Miss rate</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>Obviously destructive</td>
          <td><code>rm -rf /</code>, <code>chmod -R 777 /</code></td>
          <td>11.7%</td>
      </tr>
      <tr>
          <td>Persistent mutation</td>
          <td>crontab injection, git config hijack</td>
          <td>23.8%</td>
      </tr>
      <tr>
          <td>Exfiltration / code execution</td>
          <td>curl to unknown APIs, typosquatted packages</td>
          <td>33.4%</td>
      </tr>
      <tr>
          <td>Scope violations</td>
          <td><code>cat ~/.aws/credentials</code>, <code>cat ~/.kube/config</code></td>
          <td>35.0%</td>
      </tr>
  </tbody>
</table>
<p>The pattern is clear: the more a command looks like a normal read or script invocation, the more likely a human is to approve it. Credentials exfiltration — reading <code>~/.aws/credentials</code> or <code>~/.kube/config</code> — was missed roughly three times more often than destructive commands. A command that looks like a routine file read triggers none of the alarm bells that <code>rm -rf /</code> does, even when the file it reads is the key to your entire cloud account.</p>
<h2 id="the-npm-run-blind-spot-familiarity-is-the-attack-surface">The npm Run Blind Spot: Familiarity Is the Attack Surface</h2>
<p>The single most-missed command in the entire dataset was <code>npm run analyze</code>, approved 64.7% of the time — even though the history log clearly showed an exfiltration payload. The command name is familiar, it looks like a normal build step, and that familiarity is precisely what makes it dangerous.</p>
<p>The study pooled three camouflaged npm-script attacks (<code>analyze</code>, <code>setup</code>, <code>deploy</code>) and found they were missed 52.5% of the time, versus 28.4% for other exfiltration attacks. Familiarity roughly doubled the attack success rate. An attacker who hides malicious code inside a script named <code>npm run setup</code> is exploiting a human heuristic: &ldquo;this looks like normal work, so it must be safe.&rdquo;</p>
<p>This is the clearest single lesson in the dataset. When you review an agent&rsquo;s command, you are almost certainly reading the command name, not the script it invokes. <code>npm run X</code> always looks safe because it always looks the same — and that uniformity is the attack surface.</p>
<h2 id="permission-fatigue-is-real-not-just-a-feeling">Permission Fatigue Is Real, Not Just a Feeling</h2>
<p>The study found that miss rates climb toward the end of sessions. The more approval prompts a reviewer has already processed, the less vigilance they have available for the next one. This is permission fatigue, and it is a measured effect, not an anecdote.</p>
<p>Every added approval prompt taxes the attention available for the next decision. A developer who has approved forty commands in a row is measurably more likely to approve the forty-first without reading it carefully. This is the same cognitive phenomenon behind alert fatigue in security operations centers — when every alert demands attention, attention becomes a scarce resource that gets rationed, and the rationing happens exactly where it is most dangerous.</p>
<p>The implication is uncomfortable: the more you rely on human approval as your security boundary, the weaker that boundary becomes over the course of a session. The system degrades precisely as the agent does more work.</p>
<h2 id="the-other-side-over-blocking-benign-commands">The Other Side: Over-Blocking Benign Commands</h2>
<p>The dilemma cuts both ways. The same reviewers who missed threats also over-blocked safe commands at alarming rates:</p>
<ul>
<li><code>npm config set registry</code> was blocked 59% of the time — a completely benign command.</li>
<li><code>rm -rf dist/</code> was blocked 45% of the time — a routine build cleanup.</li>
<li><code>kill $(lsof -t -i:3000)</code> was blocked 43% of the time — a standard way to free a port.</li>
<li><code>cat ~/.zshrc</code> was the most contested command, approved only 45.9% of the time, because whether it is safe depends entirely on whether secrets live in the shell profile.</li>
</ul>
<p>Over-blocking is not harmless caution. When a reviewer blocks benign commands that the agent needs to do its job, the workflow becomes unusable, and the developer&rsquo;s natural response is to bypass the friction entirely — by running the agent in full-access mode with permissions skipped. The study&rsquo;s 7% who approved everything are the endpoint of this pressure. Over-blocking pushes developers toward the very bypass that makes the 1-in-3 miss rate catastrophic.</p>
<h2 id="why-human-review-alone-is-not-enough">Why Human Review Alone Is Not Enough</h2>
<p>The 1-in-3 miss rate is a failure of the design pattern, not of the humans. Humans are bad at sustained vigilance by design — it is a well-documented cognitive limitation, not a personal failing. Any security model that depends on a human maintaining perfect attention across hundreds of decisions is structurally unsound.</p>
<p>The real-world record confirms this. The Claude Memory Heist demonstrated exfiltration via <code>web_fetch</code> with no approval prompt at all — the agent exfiltrated data without ever asking. The Codex <code>$HOME</code> deletion incident showed a full-access mode bypassing sandboxing entirely. In both cases, the failure was not a human making a bad call; it was a system that had no defense in depth beyond the human, or that let the human&rsquo;s fatigue be routed around entirely.</p>
<p>Anthropic&rsquo;s own internal study of 1,053 testers reached a similar conclusion. The &ldquo;ask the user for everything&rdquo; approach had an 86.4% miss rate — worse than the game&rsquo;s 66.3%, because in real work the prompts are more numerous and more routine. A classifier-based Auto mode, by contrast, had a miss rate of roughly 11%. The data is unambiguous: for routine commands, automated gating beats human review by a wide margin.</p>
<h2 id="defense-in-depth-sandboxing-scoped-credentials-and-surgical-gating">Defense in Depth: Sandboxing, Scoped Credentials, and Surgical Gating</h2>
<p>The answer is not to remove humans from the loop — it is to stop treating them as the only line of defense. Defense in depth layers multiple independent controls so that no single failure is catastrophic:</p>
<ul>
<li><strong>Sandboxed execution.</strong> Run the agent in an isolated environment with restricted filesystem and network access. Even if a malicious command is approved, it cannot reach your production data or your real credentials.</li>
<li><strong>Scoped, least-privilege credentials.</strong> Give the agent credentials that can only do what the task requires. A command that reads <code>~/.aws/credentials</code> is only dangerous if those credentials are worth stealing — scoped credentials make exfiltration far less valuable.</li>
<li><strong>Surgical gating, not everywhere.</strong> Gate the commands that matter — destructive operations, network calls, credential access — and let routine commands run. This reduces the number of prompts, which directly reduces permission fatigue.</li>
<li><strong>Automated threat detection.</strong> Use pattern matching and classifiers to flag dangerous commands before they reach the human, so the human only reviews the genuinely ambiguous cases.</li>
<li><strong>Rate limiting on destructive actions.</strong> Cap impactful actions per time window (for example, a maximum of 10 destructive operations per five minutes) to contain the blast radius of a single bad approval.</li>
<li><strong>Log everything with context, and audit regularly.</strong> You cannot learn from a miss you cannot see. Full logging with surrounding context turns individual mistakes into systemic improvements.</li>
</ul>
<p>The principle of least privilege is the foundation. Run the agent as a non-root user with restricted filesystem and network access, and the worst case of any single approval is bounded.</p>
<h2 id="practical-habits-for-teams-using-ai-coding-agents">Practical Habits for Teams Using AI Coding Agents</h2>
<p>For teams deploying AI coding agents today, the study translates into concrete habits:</p>
<ol>
<li><strong>Read the script, not the command name.</strong> The npm run blind spot is the clearest number in the dataset. When an agent proposes <code>npm run X</code>, look at what the script actually does before approving.</li>
<li><strong>Pair human review with sandboxing and scoped credentials.</strong> Human review is a layer, not the whole stack. Assume a miss will happen and design so that a miss is survivable.</li>
<li><strong>Gate surgically, not everywhere.</strong> Reduce the number of approval prompts so that the ones that remain get real attention. Every prompt you remove is vigilance you preserve for the next one.</li>
<li><strong>Prefer automated gating for routine commands.</strong> The Anthropic data — 11% miss for Auto mode versus 86.4% for ask-everything — is the strongest argument for letting classifiers handle the routine and reserving human judgment for the ambiguous.</li>
<li><strong>Use pre-approval whitelists and a break-glass kill switch.</strong> Whitelist known-safe operations so they never prompt, and have a hard stop that can halt the agent immediately if something goes wrong.</li>
</ol>
<h2 id="the-bottom-line-better-systems-not-better-humans">The Bottom Line: Better Systems, Not Better Humans</h2>
<p>The 1-in-3 miss rate is not a reason to abandon AI agents — it is a reason to stop building security on the assumption that a human will catch everything. Human-in-the-loop approval is a valuable layer, but it is a layer, not a foundation. The teams that deploy agents safely will be the ones that combine human review with sandboxing, scoped credentials, automated threat detection, and surgical gating — so that when a human misses a threat, the system catches it anyway.</p>
<h2 id="faq">FAQ</h2>
<p><strong>Q: What does &ldquo;1 in 3 threats missed&rdquo; actually mean?</strong>
A: In a study of 40,000+ sessions and 409,000 approve/deny decisions, human reviewers approved roughly one in three malicious AI agent commands. Mean accuracy was 66.3%, meaning about a third of threats slipped through.</p>
<p><strong>Q: Why do humans miss AI agent threats?</strong>
A: Mostly because of permission fatigue and familiarity bias. Miss rates rise over a session as attention depletes, and commands that look like normal work — especially familiar npm scripts — are approved without scrutiny.</p>
<p><strong>Q: Is human-in-the-loop approval a reliable security boundary?</strong>
A: No. The study shows it is unreliable as a sole safeguard. 32.9% of sessions ended net-negative, and Anthropic found an 86.4% miss rate for &ldquo;ask for everything&rdquo; versus ~11% for classifier-based Auto mode.</p>
<p><strong>Q: What is the npm run blind spot?</strong>
A: Camouflaged npm-script attacks were missed 52.5% of the time versus 28.4% for other exfiltration attacks. <code>npm run analyze</code> was the single most-missed command (64.7% approved) because familiar command names look safe.</p>
<p><strong>Q: How should teams protect against AI agent threats?</strong>
A: Use defense in depth: sandboxed execution, scoped least-privilege credentials, automated threat detection, surgical gating, rate limiting on destructive actions, and full logging. Human review should be one layer, not the whole stack.</p>
]]></content:encoded></item></channel></rss>