<?xml version="1.0" encoding="utf-8" standalone="yes"?><rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom" xmlns:content="http://purl.org/rss/1.0/modules/content/"><channel><title>SKILL.md on RockB</title><link>https://baeseokjae.github.io/tags/skill.md/</link><description>Recent content in SKILL.md on RockB</description><image><title>RockB</title><url>https://baeseokjae.github.io/images/og-default.png</url><link>https://baeseokjae.github.io/images/og-default.png</link></image><generator>Hugo</generator><language>en-us</language><lastBuildDate>Thu, 27 Aug 2026 22:01:25 +0000</lastBuildDate><atom:link href="https://baeseokjae.github.io/tags/skill.md/index.xml" rel="self" type="application/rss+xml"/><item><title>Project Orrery: A Portable Codex Project Documentation Skill for Traceable Docs</title><link>https://baeseokjae.github.io/posts/project-orrery-codex-skill-2026/</link><pubDate>Thu, 27 Aug 2026 22:01:25 +0000</pubDate><guid>https://baeseokjae.github.io/posts/project-orrery-codex-skill-2026/</guid><description>Project Orrery is a portable Codex skill that turns goals into traceable YAML plans and archived execution reports, so agent work stays auditable.</description><content:encoded><![CDATA[<p>A codex project documentation skill is a portable, SKILL.md-based capability that lets an AI coding agent plan work as traceable YAML contracts and record every execution step, so the reasoning behind a project survives as an auditable archive instead of vanishing into a chat log. Project Orrery implements exactly this: it turns high-level goals into executable, engineered workflows whose state lives in a <code>.agent-work/</code> directory with <code>plans/</code>, <code>reports/</code>, and <code>completed/</code> subdirectories. Because the skill is built on the open agent skills standard, the same skill works in Codex CLI, Claude Code, Cursor, and Gemini CLI.</p>
<h2 id="what-is-project-orrery-and-why-traceability-matters">What Is Project Orrery and Why Traceability Matters</h2>
<p>Project Orrery is a workflow planning and orchestration CLI for AI agents. Where a typical agent session is a black box — you see the final diff but not the reasoning, the rejected alternatives, or the acceptance criteria that shaped each step — Orrery makes the process itself a first-class artifact. It converts a high-level goal into a structured plan, executes that plan through one or more agents, and archives the entire lifecycle for later review.</p>
<p>Traceability matters because AI agents increasingly run autonomously. When a human is not watching every keystroke, you need a durable record of <em>what</em> was done, <em>why</em> it was done that way, and <em>whether</em> it met the stated criteria. Orrery&rsquo;s <code>.agent-work/</code> archive turns every plan and every execution report into an auditable record. That is the difference between trusting an agent and verifying an agent.</p>
<p>The project is built around modular &ldquo;Skills&rdquo; — Discovery, Refine-Plan, and Simulate-Plan — that install into global agent config directories such as <code>~/.claude/skills</code>. The orchestrator, <code>orrery exec</code>, loads a plan, resolves its dependencies, invokes the appropriate agents, and manages the lifecycle with reporting and archiving. Advanced workflows include plan refinement, devcontainer isolation, a review loop, parallel execution, background mode, and completion hooks.</p>
<h2 id="the-open-agent-skills-standard-why-skillmd-is-portable">The Open Agent Skills Standard: Why SKILL.md Is Portable</h2>
<p>The reason a codex project documentation skill can be portable is the open agent skills standard. Agent Skills is a lightweight, open format for extending AI agent capabilities: a skill is simply a folder containing a <code>SKILL.md</code> file with name and description metadata, plus optional <code>scripts/</code>, <code>references/</code>, <code>assets/</code>, and <code>agents/</code> subdirectories. Because the format is open and markdown-based, any agent that can load markdown skills can consume it.</p>
<p>This portability is the core of the &ldquo;portable&rdquo; claim. A skill written once for Codex CLI also works in Claude Code, Cursor, GitHub Copilot, OpenCode, Gemini CLI, and Pi. The ecosystem has embraced the convention: the <code>agent-skills</code> collection by addyosi, a set of production-grade engineering skills for AI coding agents, has roughly 90,000 GitHub stars, and OpenAI Codex CLI itself has about 119,000 stars, making it one of the most popular terminal coding agents.</p>
<p>The standard also defines how skills are discovered. In Codex, the initial skills list uses at most 2% of the model&rsquo;s context window, or 8,000 characters when the context window is unknown. This keeps the skill menu cheap to load while still allowing the full <code>SKILL.md</code> to be pulled in when a skill is actually selected — a mechanism called progressive disclosure.</p>
<h2 id="how-orrery-turns-goals-into-traceable-plans">How Orrery Turns Goals into Traceable Plans</h2>
<p>Orrery&rsquo;s central abstraction is the plan. A plan is a YAML file that defines the &ldquo;contract&rdquo; for a piece of work: its dependencies, its acceptance criteria, and the context needed per step. This contract mindset is what makes agent work reviewable <em>before</em> it runs autonomously.</p>
<p>A typical plan encodes:</p>
<ul>
<li><strong>Dependencies</strong> — which steps must complete before others begin, enabling parallel execution where safe.</li>
<li><strong>Acceptance criteria</strong> — explicit, testable conditions that define when a step is genuinely done, not merely attempted.</li>
<li><strong>Context per step</strong> — the information each agent needs, so a step is self-contained and reproducible.</li>
</ul>
<p>Once a plan is written, <code>orrery exec</code> loads it, resolves the dependency graph, and dispatches work to agents. Execution state is stored in <code>.agent-work/</code> with <code>plans/</code>, <code>reports/</code>, and <code>completed/</code> subdirectories. When a plan finishes, its report and plan move into the <code>completed/</code> archive, giving you a full, chronological record of every workflow the project has run.</p>
<h2 id="step-by-step-building-a-portable-codex-skill-for-project-docs">Step-by-Step: Building a Portable Codex Skill for Project Docs</h2>
<p>Here is how to build your own portable codex project documentation skill, modeled on Orrery&rsquo;s approach.</p>
<p><strong>Step 1: Create the skill directory.</strong> A skill is a folder. Create <code>~/.codex/skills/project-docs/</code> (or the equivalent global config directory for your agent) and add a <code>SKILL.md</code> file.</p>
<p><strong>Step 2: Write the frontmatter.</strong> The <code>SKILL.md</code> opens with YAML frontmatter containing a <code>name</code> and <code>description</code>. The description is what the agent reads to decide whether to load the skill, so make it specific: &ldquo;Plan project work as traceable YAML contracts and archive execution reports.&rdquo;</p>
<p><strong>Step 3: Define the plan schema.</strong> Decide what a plan looks like. At minimum, include <code>goal</code>, <code>steps</code>, <code>dependencies</code>, and <code>acceptance_criteria</code>. Keep it YAML so it is human-readable and diffable.</p>
<p><strong>Step 4: Add a plan template.</strong> Include a <code>templates/plan.yaml</code> that the agent can copy. A good template makes the skill usable without the agent inventing structure on the fly.</p>
<p><strong>Step 5: Specify the archive layout.</strong> Tell the skill to write plans and reports into a <code>.agent-work/</code> directory with <code>plans/</code>, <code>reports/</code>, and <code>completed/</code> subdirectories, mirroring Orrery&rsquo;s traceability model.</p>
<p><strong>Step 6: Add a verification step.</strong> Instruct the agent to check acceptance criteria before marking a step done, and to write a short report per step. This is what turns a chat session into an auditable record.</p>
<p><strong>Step 7: Test portability.</strong> Copy the same skill folder into <code>~/.claude/skills/</code> and <code>~/.cursor/skills/</code> and confirm the agent loads it. If it works in all three, your skill is genuinely portable.</p>
<h2 id="progressive-disclosure-keeping-your-skill-light-on-context">Progressive Disclosure: Keeping Your Skill Light on Context</h2>
<p>One of the smartest design decisions in the agent skills standard is progressive disclosure. The agent does not load your entire skill into context at startup. Instead, it reads only the name and description — a tiny footprint that costs at most 2% of the context window, or 8,000 characters. Only when the agent decides the skill is relevant does it load the full <code>SKILL.md</code> and any referenced files.</p>
<p>This matters for a project documentation skill because the full instructions can be long. By keeping the description crisp and pushing the detailed plan schema, templates, and examples into referenced files, you keep the skill discoverable without bloating every session. Large skill sets may even omit skills with a warning if the combined list exceeds the budget, so a tight description is not just nice-to-have — it is what keeps your skill from being dropped entirely.</p>
<h2 id="making-documentation-a-byproduct-not-extra-work">Making Documentation a Byproduct, Not Extra Work</h2>
<p>The most common reason project documentation fails is that it is treated as a separate, after-the-fact task. By the time the code is written, the reasoning is gone. Orrery and related skills invert this: they capture reasoning <em>during</em> the conversation, as a byproduct of working with the agent, rather than writing docs afterward.</p>
<p>This is the philosophy behind &ldquo;Keep the Why,&rdquo; a repo-native convention and agent skill that preserves the reasoning behind a codebase as a byproduct of working with your agent. It offers four modes — continuous capture, retrospective recovery, knowledge-transfer interview, and maintenance — and ships <code>context/</code> updates in the same commit as the code change, so they are versioned and reviewed the same way. Its tagline captures the distinction: &ldquo;Keep a Changelog records what changed. Keep the Why preserves why it changed.&rdquo;</p>
<p>For a codex project documentation skill, the practical takeaway is to make documentation part of the plan contract. Each step&rsquo;s report <em>is</em> the documentation. You are not adding a documentation phase; you are making the execution phase produce documentation automatically.</p>
<h2 id="running-autonomously-without-losing-control">Running Autonomously Without Losing Control</h2>
<p>Autonomy is only safe when you can bound its blast radius. Orrery supports devcontainer isolation and isolated branches so agents can run unattended without risking the main branch. A review loop lets a human or a second agent check work before it merges, and completion hooks fire when a workflow finishes.</p>
<p>The combination of isolated execution and a traceable archive is what makes unattended agent work defensible. If something goes wrong, you can inspect the plan, the reports, and the acceptance criteria to understand exactly what happened and why. That is the safety net that turns &ldquo;the agent did it&rdquo; from a liability into an auditable, reviewable process.</p>
<h2 id="comparing-orrery-keep-the-why-and-living-docs">Comparing Orrery, Keep the Why, and Living Docs</h2>
<table>
  <thead>
      <tr>
          <th>Tool</th>
          <th>Core idea</th>
          <th>Format</th>
          <th>Best for</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td><strong>Project Orrery</strong></td>
          <td>Goal-to-plan orchestration with archived execution</td>
          <td>YAML plans + SKILL.md</td>
          <td>Traceable, multi-step agent workflows</td>
      </tr>
      <tr>
          <td><strong>Keep the Why</strong></td>
          <td>Preserve reasoning as a byproduct of work</td>
          <td>SKILL.md + <code>context/</code></td>
          <td>Explaining <em>why</em> code changed</td>
      </tr>
      <tr>
          <td><strong>Living Docs</strong></td>
          <td>Docs-as-code kept in sync with code</td>
          <td>SKILL.md + Rust CLI</td>
          <td>Governing doc structure and invariants</td>
      </tr>
      <tr>
          <td><strong>AGENTS.md</strong></td>
          <td>A README for agents</td>
          <td>Markdown</td>
          <td>Project context and instructions</td>
      </tr>
  </tbody>
</table>
<p>Each tool solves a different slice of the documentation problem. Orrery focuses on the <em>process</em> — turning goals into traceable, executable plans. Keep the Why focuses on the <em>reasoning</em> — preserving the decisions and rejected alternatives that code alone cannot explain. Living Docs focuses on the <em>structure</em> — governing how docs are organized with ADRs, BDRs, PRDs, a constitution, glossary, and living Mermaid diagrams, enforced by five governance invariants: docs-first, one home per fact, and cross-reference instead of copying. AGENTS.md, meanwhile, is the simplest layer: a dedicated, predictable place to provide context and instructions to help AI coding agents work on a project.</p>
<h2 id="best-practices-for-traceable-project-documentation">Best Practices for Traceable Project Documentation</h2>
<p>Drawing on Orrery and the wider agent-skills ecosystem, here are the practices that make project documentation genuinely traceable:</p>
<ul>
<li><strong>Make the plan the contract.</strong> Write acceptance criteria before execution, not after. A plan you can review is a plan you can trust.</li>
<li><strong>Archive everything.</strong> Keep plans, reports, and completed workflows in a versioned directory so the history is inspectable.</li>
<li><strong>Capture the why, not just the what.</strong> Record rejected alternatives and workarounds alongside the chosen approach.</li>
<li><strong>Ship docs with code.</strong> Version documentation in the same commit as the change it describes, so the two cannot drift apart.</li>
<li><strong>Keep skills portable.</strong> Build on the open SKILL.md standard so your skill works across Codex, Claude Code, Cursor, and Gemini CLI.</li>
<li><strong>Respect the context budget.</strong> Keep skill descriptions tight so progressive disclosure keeps your skill discoverable.</li>
<li><strong>Isolate autonomous runs.</strong> Use devcontainers and isolated branches so unattended agents cannot damage the main branch.</li>
</ul>
<h2 id="conclusion">Conclusion</h2>
<p>A codex project documentation skill built on the open agent skills standard gives you the best of both worlds: the autonomy of an AI coding agent and the auditability of a well-run engineering process. Project Orrery demonstrates the pattern — turn goals into YAML plan contracts, execute them through agents, and archive every plan and report in a <code>.agent-work/</code> directory for full traceability. Because the skill is portable, the same capability works in Codex CLI, Claude Code, Cursor, and Gemini CLI. The result is documentation that is a byproduct of the work rather than an afterthought, and agent autonomy that is safe because it is verifiable.</p>
<h2 id="faq">FAQ</h2>
<p><strong>What is a codex project documentation skill?</strong>
A codex project documentation skill is a portable, SKILL.md-based capability that lets an AI coding agent plan work as traceable YAML contracts and record every execution step, producing an auditable archive of the reasoning behind a project.</p>
<p><strong>How does Project Orrery make agent work traceable?</strong>
Orrery stores execution state in a <code>.agent-work/</code> directory with <code>plans/</code>, <code>reports/</code>, and <code>completed/</code> subdirectories, so every plan and execution report becomes an auditable record of what was done and why.</p>
<p><strong>Is a SKILL.md skill portable across different AI agents?</strong>
Yes. SKILL.md is an open cross-agent format, so a skill written for Codex CLI also works in Claude Code, Cursor, GitHub Copilot, OpenCode, Gemini CLI, and Pi.</p>
<p><strong>What is progressive disclosure in agent skills?</strong>
Progressive disclosure means the agent loads only a skill&rsquo;s name and description at startup — at most 2% of the context window or 8,000 characters — and loads the full SKILL.md only when the skill is selected.</p>
<p><strong>How is Orrery different from AGENTS.md?</strong>
AGENTS.md is a simple markdown file that provides project context and instructions to coding agents, while Orrery is a workflow orchestration CLI that turns goals into executable, traceable YAML plans with archived execution reports.</p>
]]></content:encoded></item></channel></rss>