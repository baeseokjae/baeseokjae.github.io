<?xml version="1.0" encoding="utf-8" standalone="yes"?><rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom" xmlns:content="http://purl.org/rss/1.0/modules/content/"><channel><title>Agent-Monitoring on RockB</title><link>https://baeseokjae.github.io/tags/agent-monitoring/</link><description>Recent content in Agent-Monitoring on RockB</description><image><title>RockB</title><url>https://baeseokjae.github.io/images/og-default.png</url><link>https://baeseokjae.github.io/images/og-default.png</link></image><generator>Hugo</generator><language>en-us</language><lastBuildDate>Fri, 04 Sep 2026 07:02:15 +0000</lastBuildDate><atom:link href="https://baeseokjae.github.io/tags/agent-monitoring/index.xml" rel="self" type="application/rss+xml"/><item><title>Rungraph 2026 Review: Ask Your Agent What Happened in a Run with Interactive Graphs</title><link>https://baeseokjae.github.io/posts/rungraph-agent-run-visualization-2026/</link><pubDate>Fri, 04 Sep 2026 07:02:15 +0000</pubDate><guid>https://baeseokjae.github.io/posts/rungraph-agent-run-visualization-2026/</guid><description>Rungraph converts existing Claude Code, Codex, Hermes, opencode, and Cursor transcripts into interactive graphs and answers MCP queries about what happened in a run — no hooks or telemetry.</description><content:encoded><![CDATA[<p>Rungraph is an open-source, zero-instrumentation tool that reads the agent session transcripts already on your disk and renders them as interactive graphs. With a single <code>npx rungraph</code> command, it visualizes the orchestrator, subagents, and tool calls of any Claude Code, Codex, Hermes Agent, opencode, or Cursor run — then lets you ask, in plain language through an MCP server, &ldquo;what happened?&rdquo; and lights up the exact nodes the answer refers to.</p>
<h2 id="what-is-rungraph-turning-agent-transcripts-into-interactive-graphs">What Is Rungraph? Turning Agent Transcripts into Interactive Graphs</h2>
<p>Rungraph is a JavaScript, open-source agent-run visualizer released on Hacker News in August 2026 by FayzanMalik. It sits on top of the session logs your coding agents already write to disk and converts them into a graph that you can explore interactively. The core idea is deliberately different from tracing and observability frameworks: there are no hooks, no wrappers, and no telemetry to install. Everything rungraph needs — the orchestrator turns, subagent spawns, tool calls, and human interventions — is already recorded in the transcripts your agents produce.</p>
<p>The tool&rsquo;s positioning is captured in its tagline: &ldquo;Ask your agent what happened in a run.&rdquo; Instead of reading thousands of lines of silent terminal output, you load a run into rungraph&rsquo;s dashboard, and the work becomes a visual, interrogable artifact.</p>
<p>Rungraph currently works across five agents: Claude Code, Codex, Hermes Agent, opencode, and Cursor. It handles all five from a single command and requires no per-agent setup. This vendor-neutral design is a direct answer to the fragmented terminal-agent landscape of 2026, where Goose, Claude Code, opencode, and Pi coexist and each writes a different, unversioned session format to disk.</p>
<h2 id="how-rungraph-works-retroactive-zero-instrumentation-fully-local">How Rungraph Works: Retroactive, Zero-Instrumentation, Fully Local</h2>
<p>The remarkable thing about rungraph is that it works <strong>retroactively</strong>. Because it reads transcripts already stored on disk, it can visualize runs that happened weeks before you ever installed the tool. There is no requirement to have &ldquo;started recording&rdquo; in advance.</p>
<ul>
<li><strong>No hooks:</strong> Rungraph does not modify your agents or their runtime.</li>
<li><strong>No wrappers:</strong> You do not wrap your commands or change how you work.</li>
<li><strong>No telemetry:</strong> The tool makes no network requests — it is fully local. Your run data never leaves your machine.</li>
</ul>
<p>To use it, you run a single command: <code>npx rungraph</code>. It scans the session stores of the supported agents, parses their transcripts, and opens an interactive dashboard. Under the hood, rungraph reads each agent&rsquo;s session format — Claude Code, Codex, Hermes, opencode, and Cursor — from disk. Some of those formats require Node version 22.13 or later: Hermes, opencode, and Cursor runs use rungraph&rsquo;s built-in SQLite reader, which needs that Node version.</p>
<p>One honest caveat is &ldquo;coverage.&rdquo; Coding-agent transcript formats are undocumented and unversioned, so rungraph cannot interpret every record it encounters. The tool reports how much of a run it could read (for example, &ldquo;read 95% of this run&rdquo;) and names the records it could not interpret. This transparency is what makes the tool trustworthy for a debugging workflow — you know when you are looking at a partial picture.</p>
<h2 id="the-graph-orchestrator-subagents-tools-and-human-interventions-as-nodes">The Graph: Orchestrator, Subagents, Tools, and Human Interventions as Nodes</h2>
<p>The centerpiece of rungraph is the graph itself. At its most basic, it is a node-and-edge view of an agent run:</p>
<ul>
<li><strong>Nodes</strong> represent the orchestrator, subagents, and individual tool calls.</li>
<li><strong>Edges</strong> represent spawn and return relationships — a subagent is spawned by the orchestrator, returns a result, and the path continues.</li>
<li><strong>Human interventions</strong> are marked on the path: permission denials, interrupts, and other human actions are visible rather than buried in logs.</li>
</ul>
<p>This structure makes the shape of a run legible at a glance. You can immediately see when the orchestrator delegatated a task to a subagent, how many nested spawns occurred, and where a human stepped in to deny a permission or interrupt the flow.</p>
<p>Because a single large agent run can exceed 20,000 tokens if fully serialized, rungraph is designed around a &ldquo;narrow then pull&rdquo; query model. Rather than dumping the entire graph into your context, you focus on the nodes you care about and pull details on demand. This keeps both the dashboard and any connected LLM context efficient.</p>
<h2 id="ask-your-agent-what-happened--the-mcp-layer-and-focus_nodes-deep-links">Ask Your Agent What Happened — the MCP Layer and focus_nodes Deep Links</h2>
<p>The most distinctive feature of rungraph is its MCP (Model Context Protocol) server. It gives you a set of tools — <code>list_runs</code>, <code>get_graph</code>, <code>find_nodes</code>, <code>get_detail</code>, and <code>focus_nodes</code> — that let you interrogate a run conversationally.</p>
<p>The workflow is: you open rungraph&rsquo;s dashboard alongside your AI assistant, then ask the assistant a question like &ldquo;what did the subagent that edited the config file actually do?&rdquo; The MCP server resolves the answer, and <code>focus_nodes</code> highlights the exact nodes on the graph that the answer references. Instead of reading a text response and mentally mapping it onto the run, you see the relevant portion of the graph light up in front of you.</p>
<ul>
<li><strong>list_runs:</strong> enumerate the runs rungraph has parsed.</li>
<li><strong>get_graph:</strong> retrieve the structure of a run.</li>
<li><strong>find_nodes:</strong> locate specific nodes by query.</li>
<li><strong>get_detail:</strong> pull full detail for a narrow set of nodes.</li>
<li><strong>focus_nodes:</strong> highlight nodes in the open dashboard — the visual answer.</li>
</ul>
<p>This &ldquo;ask your agent what happened&rdquo; model turns a silent, finished transcript into something you can have a conversation with. It is particularly valuable for audits, code reviews, and debugging, where the question is often the hardest part: &ldquo;which tool call touched this file?&rdquo;</p>
<h2 id="opinionated-signals-retry-storms-unresolved-errors-interventions-outliers">Opinionated Signals: Retry Storms, Unresolved Errors, Interventions, Outliers</h2>
<p>Most log viewers render every event with the same visual weight, forcing you to scan for what matters. Rungraph takes a different, opinionated approach via a <strong>signal strip</strong> that flags specific patterns on the run:</p>
<ul>
<li><strong>Retry storms:</strong> Repeated retries of the same failing call are flagged, revealing where the agent struggled.</li>
<li><strong>Unresolved errors:</strong> Errors that were never resolved are surfaced so you know the run ended in a problematic state.</li>
<li><strong>Interventions:</strong> Human actions (permission denials, course corrections) are marked.</li>
<li><strong>Outliers:</strong> Unusually long or unusual nodes stand out.</li>
<li><strong>Course changes:</strong> Points where the agent changed direction are visible.</li>
</ul>
<p>On a clean run, the signal strip sits at zero height — nothing flashes, nothing vies for your attention. It only activates when there is something worth seeing. This is what the brief calls &ldquo;opinionated observability&rdquo;: rendering not everything equal, but the things that matter louder. For a developer reviewing whether a run went smoothly, this is the difference between staring at a wall of text and immediately knowing where to look.</p>
<h2 id="replay-minimap-and-sharing-runs-as-vendor-neutral-bundles">Replay, Minimap, and Sharing Runs as Vendor-Neutral Bundles</h2>
<p>Two more interactive features round out the dashboard: live growth and replay.</p>
<p>Because rungraph reads transcripts, a run that is happening <strong>right now</strong> grows live as the agent works. You can watch the graph extend in real time rather than only after the run completes. A replay bar then lets you scrub back and forth through the run, inspecting the state of the graph at any point in time.</p>
<p>These graphs are built to be shared. Rungraph exports run bundles in a <code>.rungraph</code> format — a vendor-neutral package you can hand to a teammate, attach to a PR, or archive. An example from the project&rsquo;s README shows a bundle containing 2 runs, 143 nodes, 12 prompts, and 24 files touched, serialized to a 412KB file. Because a graph plus focused node deep links is a far more reviewable artifact than a pasted log, rungraph positions agent runs as <em>collaborative</em> artifacts in code review and team debugging.</p>
<h2 id="security-by-design-secret-redaction-in-export">Security by Design: Secret Redaction in Export</h2>
<p>Transcripts are a security hazard that is easy to overlook. When an agent runs a tool like a file reader, it records the file contents verbatim — so an agent that merely opens <code>.env</code> to read its keys has captured those keys in its session log. Any tool that re-shares or exports a run must handle this responsibly.</p>
<p>Rungraph addresses it in two ways on export:</p>
<ul>
<li><strong>Secret redaction:</strong> Shared <code>.rungraph</code> bundles redact likely secrets automatically.</li>
<li><strong>High-confidence blocking:</strong> Export blocks on high-confidence secrets, including AWS keys and GitHub, Slack, and API tokens.</li>
</ul>
<p>This is a meaningful security posture for a tool whose whole value proposition is sharing runs with other people. Because the data stays local until you explicitly export, and because export is aggressively redacted, rungraph avoids becoming a channel for leaking credential material that transcripts routinely capture.</p>
<h2 id="rungraph-vs-the-agent-observability-landscape">Rungraph vs. the Agent-Observability Landscape</h2>
<p>Rungraph does not exist in a vacuum — the agent-observability space is crowded and moving fast in 2026. Here is how it stacks up against the main alternatives:</p>
<table>
  <thead>
      <tr>
          <th>Tool / Approach</th>
          <th>Model</th>
          <th>Instrumentation</th>
          <th>Data locality</th>
          <th>Best at</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td><strong>Rungraph</strong></td>
          <td>Graph-native visualization + MCP query</td>
          <td>None (reads on-disk transcripts)</td>
          <td>Fully local</td>
          <td>Retroactive, shareable, graph exploration</td>
      </tr>
      <tr>
          <td><strong>Agentlore</strong></td>
          <td>Searchable team log over agent sessions</td>
          <td>Reads session data</td>
          <td>Local per team</td>
          <td>Team/communal search &amp; indexing</td>
      </tr>
      <tr>
          <td><strong>LangChain SmithDB</strong></td>
          <td>Purpose-built observability data layer</td>
          <td>Central tracing</td>
          <td>Cloud / hosted</td>
          <td>Enterprise LLM trace analytics</td>
      </tr>
      <tr>
          <td><strong>Grafana agent observability</strong></td>
          <td>Standard-dashboard ingestion</td>
          <td>Telemetry to Grafana</td>
          <td>Aggregated / hosted</td>
          <td>Folding agent runs into existing dashboards</td>
      </tr>
      <tr>
          <td><strong>Log-file grepping</strong></td>
          <td>Manual</td>
          <td>None</td>
          <td>Local</td>
          <td>Ad-hoc inspection of a single run</td>
      </tr>
  </tbody>
</table>
<p>The key contrast is <strong>instrumentation philosophy</strong>. SmithDB and Grafana&rsquo;s approach assume you build tracing into the pipeline and funnel event data into a central system. Agentlore positions run visibility as a team-collaboration problem focused on searchability. Rungraph, by contrast, is zero-instrumentation and local-first: it performs retroactive visualization of transcripts you already have, with graph structure plus MCP-driven interrogation as the interface. Where SmithDB answers enterprise &ldquo;trace everything&rdquo; needs, rungraph answers the individual developer&rsquo;s &ldquo;what actually happened here?&rdquo; need.</p>
<p>The Grafana adapter for Hermes Agent is a useful data point: it shows the observability ecosystem racing to ingest agent-run data into standard dashboards. Rungraph&rsquo;s differentiator is that it needs none of that infrastructure — there is nothing to ingest because the data is already on your disk.</p>
<h2 id="who-rungraph-is-for-limitations-and-2026-context">Who Rungraph Is For, Limitations, and 2026 Context</h2>
<p>Rungraph is best suited to developers who already work with terminal and CLI coding agents and want to understand, audit, or share what their agents did. It is especially relevant in 2026, when terminal agents have gone fully mainstream — comparisons of Goose, Claude Code, opencode, and Pi are common — and where the coexistence of multiple agents fragments run and session formats. A vendor-neutral graph that reads them all is a practical answer to that fragmentation.</p>
<p>The tool&rsquo;s main limitations are honest ones:</p>
<ul>
<li><strong>Coverage is not 100%.</strong> Because transcript formats are undocumented, rungraph may fail to interpret some records. It reports a coverage percentage and names what it could not read.</li>
<li><strong>Node requirements.</strong> Hermes, opencode, and Cursor use rungraph&rsquo;s SQLite reader, which requires Node 22.13+.</li>
<li><strong>Local-first by design.</strong> If you need centralized, enterprise-wide tracing across a fleet of agents, rungraph is not that; SmithDB or Grafana ingestion is.</li>
<li><strong>Young project.</strong> At research time it had 24 stars and 2 forks — useful and capable, but not battle-tested across a large community yet.</li>
</ul>
<p>For the Hermes Agent user specifically, rungraph has native support — a nice alignment for anyone running Hermes-managed pipelines who also wants to see how individual runs unfolded.</p>
<h2 id="verdict--is-rungraph-worth-adding-to-your-agent-workflow">Verdict — Is Rungraph Worth Adding to Your Agent Workflow?</h2>
<p>Rungraph solves a real, growing problem in a clever way. If you use Claude Code, Codex, Hermes Agent, opencode, or Cursor, and you have ever wished you could actually <em>see</em> what your agent did — or explain it to a teammate without pasting a wall of log text — rungraph is worth a try. The zero-instrumentation, retroactive design means there is no cost to adopting it: you can point it at runs you already have and get value immediately.</p>
<p>The MCP &ldquo;ask your agent what happened&rdquo; workflow, the opinionated signal strip, and the shareable <code>.rungraph</code> bundles make it genuinely useful for debugging and code review, not merely a pretty visualization. Be mindful of its coverage limitations and the Node 22.13 requirement, and treat exported bundles with the same caution you would any transcript (rungraph redacts, but transcripts are sensitive by default).</p>
<p><strong>Bottom line:</strong> For individual developers and small teams who want to understand and share agent runs without rearchitecting their observability stack, rungraph is a strong, low-friction addition to the toolkit in 2026.</p>
<h2 id="faq">FAQ</h2>
<h3 id="what-is-a-rungraph-agent-run">What is a rungraph agent run?</h3>
<p>A rungraph agent run is a visualization of one coding-agent session loaded from the transcript already stored on disk. Rungraph renders the orchestrator, subagents, and tool calls as nodes with spawn/return edges, marks human interventions, and lets you replay and query the run. It supports Claude Code, Codex, Hermes Agent, opencode, and Cursor with one <code>npx rungraph</code> command and no hooks or telemetry.</p>
<h3 id="how-do-i-ask-my-agent-what-happened-in-a-run">How do I ask my agent what happened in a run?</h3>
<p>Rungraph ships an MCP server with tools like <code>list_runs</code>, <code>get_graph</code>, <code>find_nodes</code>, <code>get_detail</code>, and <code>focus_nodes</code>. You connect it to your AI assistant, ask a question about the run in plain language, and <code>focus_nodes</code> highlights the exact nodes the answer references in the open dashboard — turning a silent transcript into a question-answerable, visual artifact.</p>
<h3 id="is-rungraph-really-zero-instrumentation">Is rungraph really zero-instrumentation?</h3>
<p>Yes. Rungraph reads the session transcripts your agents already write to disk; it does not install hooks, wrap your commands, or send telemetry. It works retroactively on runs that happened before you installed it, and it makes no network requests — the tool is fully local.</p>
<h3 id="what-agent-observability-tools-compare-with-rungraph">What agent observability tools compare with rungraph?</h3>
<p>The main alternatives are Agentlore (a searchable team log over agent sessions), LangChain SmithDB (a purpose-built enterprise tracing data layer), and Grafana agent observability for Hermes (ingesting run data into standard dashboards). Rungraph differs by being zero-instrumentation, local-first, and graph-native, with MCP-driven interrogation rather than centralized tracing.</p>
<h3 id="how-does-rungraph-handle-secrets-when-exporting-runs">How does rungraph handle secrets when exporting runs?</h3>
<p>Agent transcripts capture file contents verbatim, so merely opening <code>.env</code> can log its keys. On export, rungraph redacts likely secrets and blocks on high-confidence ones such as AWS keys and GitHub, Slack, or API tokens, so shared <code>.rungraph</code> bundles do not leak credential material.</p>
]]></content:encoded></item><item><title>MCP as an Observability Interface: Connecting AI Agents to Kernel Tracepoints</title><link>https://baeseokjae.github.io/posts/mcp-as-an-observability-interface-connecting-ai-agents-to-kernel-tracepoints/</link><pubDate>Mon, 24 Aug 2026 07:01:52 +0000</pubDate><guid>https://baeseokjae.github.io/posts/mcp-as-an-observability-interface-connecting-ai-agents-to-kernel-tracepoints/</guid><description>MCP observability connects AI agents to kernel tracepoints, giving them ground-truth system state. Learn how eBPF, kprobes, and OTel GenAI conventions close the agent observability gap.</description><content:encoded><![CDATA[<p>MCP observability turns the Model Context Protocol into a two-way interface: AI agents don&rsquo;t just call tools, they receive ground-truth telemetry from kernel tracepoints, eBPF programs, and kprobes. By exposing low-level system instrumentation through MCP servers, agents get a real-world model of the live system instead of hallucinated state — closing the observability gap that traditional APM leaves wide open.</p>
<h2 id="what-is-mcp-and-why-it-needs-observability">What Is MCP and Why It Needs Observability</h2>
<p>The Model Context Protocol (MCP) is an open standard that standardizes how AI agents discover and invoke tools, resources, and prompts. Instead of every agent building bespoke integrations with every service, MCP defines a common protocol: a host (the agent runtime) connects to MCP servers, which expose tools the model can call and resources it can read.</p>
<p>The problem is that what happens <em>between</em> tool calls is a black box. When an agent invokes a tool, the request crosses the network, hits a server, executes, and returns — but none of that journey is visible by default. As SigNoz notes, &ldquo;MCP-based architectures enable AI agents to invoke tools, but what happens in between is a black box.&rdquo; Latency spikes in tool responses degrade agent performance, and silent failures occur when a tool invocation returns no valid data.</p>
<p>This is precisely why MCP needs observability. An agent that chains ten tools in a single run multiplies its failure and cost surface tenfold. Without telemetry on each step, you cannot tell whether a slow response is a network issue, a server overload, or a misbehaving tool — and you cannot trust the agent&rsquo;s output.</p>
<h2 id="the-observability-gap-why-traditional-apm-fails-for-ai-agents">The Observability Gap: Why Traditional APM Fails for AI Agents</h2>
<p>Infrastructure solved its observability problem a decade ago with Prometheus, Grafana, and Datadog. AI agents remain opaque. Traditional APM sees an HTTP 200 and 143ms latency and calls it success — while completely missing the failures that actually matter for agents: leaked PII, hallucinated citations, cost overruns, and prompt injection.</p>
<p>The gap is structural. Traditional APM instruments <em>requests</em>; agents produce <em>reasoning chains</em>. A single agent execution is not one call but a sequence of decisions, tool invocations, and intermediate results. As the March 2026 state-of-MCP-observability analysis points out, 89% of teams deploying software have production observability in place, yet AI agents remain largely unobserved.</p>
<p>What&rsquo;s missing is protocol-native observability. Most current approaches are SDK-based instrumentation bolted onto the agent runtime, not telemetry carried by the protocol itself. That is the core of the gap: you are observing the agent from the outside, not from within the conversation it is having with the world.</p>
<h2 id="mcp-as-a-two-way-observability-interface">MCP as a Two-Way Observability Interface</h2>
<p>The framing that unlocks this problem is to treat MCP not as a one-way tool-calling channel but as a two-way observability interface. In one direction, agents call tools to act on the system. In the other, MCP servers expose telemetry back to the agent — metrics, traces, logs, and kernel-level state — so the agent can reason about the actual condition of the infrastructure it is operating on.</p>
<p>This is the orchestration model ClickHouse describes: MCP for observability is about orchestration, not just investigation. Agents drive the full incident workflow, jumping between logs, traces, dashboards, deployments, and hypotheses the way a human engineer does. Every query, chart, reasoning step, and intermediate result should be visible and auditable.</p>
<p>The two-way model matters because it gives the agent a feedback loop. It can observe the effect of its own actions, correct course, and align its reasoning with ground truth at every step — rather than operating on a stale mental model.</p>
<h2 id="kernel-tracepoints-the-ground-truth-source-for-agent-state">Kernel Tracepoints: The Ground-Truth Source for Agent State</h2>
<p>Kernel tracepoints are static instrumentation points compiled into the Linux kernel. They fire at well-defined locations — syscall entry and exit, scheduler events, network packet processing, filesystem operations — and expose structured data about what the kernel is actually doing.</p>
<p>For AI agents, tracepoints are the ultimate ground-truth source. An LLM can give perfect directions on a map it was trained on, but it has no ground-truth model of the live system. Kernel tracepoints provide exactly that: real, current, verifiable state about CPU usage, I/O, network, memory, and process behavior.</p>
<p>The challenge, as Jonathan Corbet outlined at the 2016 Kernel Summit, is that tracepoints must be kept stable across kernel versions to avoid breaking tracing tools. Maintaining a stable tracepoint ABI is a known and ongoing challenge. But the payoff is enormous: a stable, low-overhead, kernel-blessed source of truth that agents can query through an MCP server.</p>
<h2 id="ebpf-kprobes-and-uprobes-instrumenting-the-kernel-for-agents">eBPF, kprobes, and uprobes: Instrumenting the Kernel for Agents</h2>
<p>While tracepoints are static, eBPF and the dynamic instrumentation points — kprobes and uprobes — let you attach programs to nearly any function in the kernel or user space without modifying source code.</p>
<ul>
<li><strong>Tracepoints</strong>: static, stable, low-overhead instrumentation points compiled into the kernel.</li>
<li><strong>kprobes</strong>: dynamic probes that attach to kernel function entry and exit points.</li>
<li><strong>uprobes</strong>: dynamic probes that attach to user-space function entry and exit points.</li>
<li><strong>eBPF</strong>: a safe, sandboxed virtual machine that runs small programs at these probe points with minimal overhead.</li>
</ul>
<p>eBPF enables low-overhead, safe kernel-level observability without modifying kernel source. An MCP server can wrap eBPF programs and expose their output as MCP resources and tools, giving agents a live, queryable view of kernel behavior.</p>
<table>
  <thead>
      <tr>
          <th>Instrumentation</th>
          <th>Type</th>
          <th>Stability</th>
          <th>Overhead</th>
          <th>Use Case for Agents</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>Tracepoints</td>
          <td>Static</td>
          <td>High (ABI-stable)</td>
          <td>Very low</td>
          <td>Ground-truth syscall/scheduler state</td>
      </tr>
      <tr>
          <td>kprobes</td>
          <td>Dynamic</td>
          <td>Low (may change)</td>
          <td>Low</td>
          <td>Kernel function-level debugging</td>
      </tr>
      <tr>
          <td>uprobes</td>
          <td>Dynamic</td>
          <td>Low</td>
          <td>Low</td>
          <td>User-space function tracing</td>
      </tr>
      <tr>
          <td>eBPF</td>
          <td>Program</td>
          <td>Depends on probe</td>
          <td>Low</td>
          <td>Safe, flexible kernel observability</td>
      </tr>
  </tbody>
</table>
<h2 id="protocol-native-vs-sdk-based-observability">Protocol-Native vs SDK-Based Observability</h2>
<p>The key architectural decision is whether observability lives in the protocol or in the SDK. SDK-based instrumentation requires every agent runtime to embed a tracing library, configure exporters, and agree on a common schema. It is fragile, fragmented, and observes the agent from outside.</p>
<p>Protocol-native observability carries telemetry inside MCP itself. The MCP server emits spans and metrics as part of its normal operation, so any host that speaks MCP automatically gets observability without extra instrumentation. This is the direction the ecosystem is moving: OpenTelemetry GenAI semantic conventions now include dedicated specifications for agent spans and MCP server telemetry.</p>
<p>Protocol-native is more robust because it observes the actual conversation between agent and tool — the exact thing that matters — rather than approximating it from HTTP-level signals.</p>
<h2 id="opentelemetry-genai-semantic-conventions-and-mcp-server-telemetry">OpenTelemetry GenAI Semantic Conventions and MCP Server Telemetry</h2>
<p>OpenTelemetry&rsquo;s GenAI semantic conventions are now published, and they are the backbone of modern agent observability. They define standard attribute names for agent spans, tool invocations, token usage, and MCP server telemetry, so that observability data from different vendors and runtimes is comparable.</p>
<p>Every major observability vendor is building on these conventions. Datadog ships an MCP bridge; Arize Phoenix builds on OTel GenAI conventions. The result is a common language for describing what an agent did, which tools it called, how long each took, and what it cost.</p>
<p>For MCP observability specifically, the conventions let you track <code>tool_token_usage_total</code> counters for cost, distributed traces across tool chains, and performance metrics per tool. This is the difference between &ldquo;the agent ran&rdquo; and &ldquo;the agent ran these ten tools, this one took 4 seconds, and it consumed 12,000 tokens.&rdquo;</p>
<h2 id="security-the-adoption-bottleneck-for-mcp-observability">Security: The Adoption Bottleneck for MCP Observability</h2>
<p>Security is the single biggest blocker to MCP adoption. The data is stark: 25% of MCP servers have no authentication whatsoever, and 38% of teams say security concerns are actively blocking their MCP adoption.</p>
<p>This is where kernel-level visibility becomes a trust mechanism. If an MCP server exposes kernel tracepoints, you can observe exactly what the agent is doing at the system level — which processes it spawns, which files it touches, which network connections it makes. That visibility is the foundation of trust: you can verify that the agent is not leaking PII, not being prompt-injected, and not exceeding its authority.</p>
<p>An unauthenticated MCP server that can trigger kernel instrumentation is a serious risk. Kernel-level observability must be paired with strict authentication, authorization, and audit logging — the same discipline you would apply to any privileged system access.</p>
<h2 id="cost-visibility-across-the-agent-tool-chain">Cost Visibility Across the Agent Tool Chain</h2>
<p>Cost is a blind spot in agent observability because agents make chains, not individual calls. A single $0.01 API call becomes $0.50 per execution when you trace the full chain. A typical agent execution can chain 10+ tools in a single run, multiplying the cost surface.</p>
<p>MCP observability solves this by attributing cost to the full chain rather than individual calls. With <code>tool_token_usage_total</code> counters and distributed tracing, you can see the cumulative cost of an entire agent execution — not just one API call. This is essential for budgeting, for detecting runaway agents, and for understanding which tools are the expensive ones.</p>
<table>
  <thead>
      <tr>
          <th>Observability Signal</th>
          <th>What It Tells You</th>
          <th>Why It Matters for Agents</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>Performance metrics</td>
          <td>Per-tool latency</td>
          <td>Detect slow tools degrading agent performance</td>
      </tr>
      <tr>
          <td>Distributed traces</td>
          <td>Full tool chain</td>
          <td>See the 10-tool execution, not one call</td>
      </tr>
      <tr>
          <td>Token/cost counters</td>
          <td>Cumulative spend</td>
          <td>Catch cost overruns and runaway agents</td>
      </tr>
      <tr>
          <td>Kernel telemetry</td>
          <td>System-level state</td>
          <td>Ground truth, not hallucinated state</td>
      </tr>
  </tbody>
</table>
<h2 id="human-in-the-loop-agents-as-sre-co-pilots">Human-in-the-Loop: Agents as SRE Co-Pilots</h2>
<p>The honest take is that LLMs are not yet ready to replace human SREs. Hallucinations, misclassification biases, and a lack of explainability mean agent suggestions must be confirmed against dashboards, logs, and traces. The combinatorial explosion of tool chains means an agent can go astray unless aligned with ground truth at every step.</p>
<p>Kernel tracepoints are the alignment mechanism. When an agent&rsquo;s claim about system state can be checked against live kernel telemetry, you have a human-in-the-loop loop that actually works: the agent proposes, the tracepoints verify, the human decides. This is the co-pilot model — agents augment SREs with speed and breadth, while humans retain judgment and accountability.</p>
<h2 id="building-an-mcp-observability-interface-a-practical-guide">Building an MCP Observability Interface: A Practical Guide</h2>
<p>To build an MCP observability interface that connects agents to kernel tracepoints, follow this practical path:</p>
<ol>
<li><strong>Expose kernel telemetry as MCP resources.</strong> Wrap eBPF programs and tracepoint readers in an MCP server that exposes live system state as queryable resources.</li>
<li><strong>Instrument the MCP server with OTel GenAI conventions.</strong> Emit agent spans, tool spans, and <code>tool_token_usage_total</code> counters so every invocation is traceable.</li>
<li><strong>Add authentication and authorization.</strong> Never expose kernel instrumentation through an unauthenticated MCP server. Enforce strict access control and audit logging.</li>
<li><strong>Track the full tool chain.</strong> Use distributed tracing to see the entire agent execution, not individual calls, so cost and failure surface are visible.</li>
<li><strong>Align agents with ground truth.</strong> Have the agent verify its claims against kernel telemetry at every step, and require human confirmation for consequential actions.</li>
<li><strong>Keep humans in the loop.</strong> Treat the agent as a co-pilot that proposes, with tracepoints as the source of truth and humans as the decision-makers.</li>
</ol>
<h2 id="conclusion-from-black-box-to-ground-truth">Conclusion: From Black Box to Ground Truth</h2>
<p>MCP observability is the bridge between high-level agent reasoning and low-level kernel reality. By treating MCP as a two-way interface — agents calling tools, and tools exposing kernel tracepoints back to agents — you replace the black box with ground truth. Agents get a real-world model of the live system, cost and failure surfaces become visible across the full tool chain, and security is enforced through kernel-level visibility.</p>
<p>The infrastructure world solved observability a decade ago. With MCP as an observability interface and kernel tracepoints as the source of truth, AI agents can finally be observed with the same rigor — and trusted with the same confidence.</p>
<h2 id="faq">FAQ</h2>
<p><strong>What is MCP observability?</strong>
MCP observability is the practice of instrumenting the Model Context Protocol so that AI agent tool calls, reasoning chains, costs, and system-level effects are visible and auditable, rather than operating as a black box.</p>
<p><strong>How do kernel tracepoints help AI agents?</strong>
Kernel tracepoints provide ground-truth, real-time system state — CPU, I/O, network, memory, and process behavior — that agents can query through an MCP server, replacing hallucinated state with verifiable facts.</p>
<p><strong>What is the difference between tracepoints, kprobes, and uprobes?</strong>
Tracepoints are static, ABI-stable instrumentation points compiled into the kernel; kprobes dynamically probe kernel functions; uprobes dynamically probe user-space functions. eBPF programs attach to all of them safely.</p>
<p><strong>Why does traditional APM fail for AI agents?</strong>
Traditional APM observes individual HTTP requests and calls them success on a 200 status, missing the failures that matter for agents: leaked PII, hallucinated citations, cost overruns, and prompt injection across multi-tool reasoning chains.</p>
<p><strong>Is MCP observability secure?</strong>
Only with strict controls. Since 25% of MCP servers have no authentication, kernel-level observability must be paired with authentication, authorization, and audit logging to prevent privileged instrumentation from being abused.</p>
]]></content:encoded></item><item><title>Multi-Agent Workflow Observability in 2026: How to Test, Trace, and Debug Delegation</title><link>https://baeseokjae.github.io/posts/multi-agent-workflow-lab-observability-2026/</link><pubDate>Thu, 13 Aug 2026 20:33:06 +0000</pubDate><guid>https://baeseokjae.github.io/posts/multi-agent-workflow-lab-observability-2026/</guid><description>Multi-agent workflow observability means tracing every delegation hop, tool call, and handoff. Here&amp;#39;s how to test and monitor multi-agent systems in 2026.</description><content:encoded><![CDATA[<p>Multi-agent workflow observability means capturing every delegation hop, tool call, and sub-agent handoff as first-class telemetry instead of relying on flat log lines. Because LLM agents fail silently, teams must trace intermediate reasoning, run offline evals against synthetic datasets, and add regression suites before shipping. This guide explains the observability gap, how to trace delegation hops, and how to build a practical observability and testing stack in 2026.</p>
<h2 id="why-multi-agent-delegation-demands-a-new-observability-mindset">Why Multi-Agent Delegation Demands a New Observability Mindset</h2>
<p>A multi-agent system distributes a complex goal across specialized agents that hand work to one another. The rationale is straightforward: multi-agent systems solve problems that are difficult or impossible for a single monolithic agent, which is the core justification for delegation patterns in the first place. When an agent delegates a subtask to a colleague agent, a supervisor, or a sub-process, the resulting behavior is emergent, non-deterministic, and often invisible to the humans who wrote the system.</p>
<p>The problem is that this power comes with a reliability tax. In a single-agent architecture, a failure usually happens in one place: the agent&rsquo;s final output. In a multi-agent architecture, a failure can originate anywhere in a chain that includes reasoning, tool invocation, and two or more handoffs. If you only observe the final result, you cannot tell whether the failure came from bad input, a broken tool, a hallucinated intermediate step, or a delegation decision that sent work to the wrong agent. That ambiguity is why observability is not a nice-to-have for multi-agent systems; it is the layer that makes production deployment possible.</p>
<p>Industry commentary reinforces this. Reliability engineers have repeatedly pointed out that LLM agent workflows fail silently and that the reliability layer is a major gap for production deployment. Silent failure is the dangerous case. A flaky test can fail loudly, but a delegation chain that quietly returns a plausible-but-wrong answer consumes trust, budget, and time before anyone notices. Multi-agent observability is fundamentally about making the silent failures visible.</p>
<h2 id="the-agent-observability-gap-what-standard-logs-miss">The Agent Observability Gap: What Standard Logs Miss</h2>
<p>Standard application logging was designed for deterministic request/response systems. It captures timestamps, levels, messages, and maybe structured fields. That model breaks down with LLM agents in several specific ways.</p>
<p>First, standard logs miss the intermediate reasoning that produced an action. When an agent decides to call a tool, the log typically records the fact that a tool was called and its result, but not the chain-of-thought that led to that call. If that reasoning was flawed, the log gives you no diagnostic path.</p>
<p>Second, logs miss tool invocations that happen inside sub-agent contexts. The analyst, writer, and SEO agents in a content pipeline each make their own tool calls. Unless those calls are correlated across agent boundaries, you see fragments of activity but cannot reconstruct the full path a task traveled.</p>
<p>Third, and most critically, logs miss the delegation handoff itself. This is what the observability literature calls the &ldquo;agent observability gap&rdquo;: tool calls, intermediate reasoning, and delegation hops are often invisible to standard logs. The handoff between a supervisor and a worker agent is an event, not a log line, and treating it as telemetry is a distinct design decision.</p>
<p>The fix is structured, correlated telemetry over flat log lines. Instead of writing &ldquo;agent A finished task X,&rdquo; you emit a span that records which parent span it belongs to, which agent executed it, which model was used, which tools were invoked, how many tokens were consumed, and what the outcome was. This correlated structure is what turns a pile of logs into a trace that you can actually reconstruct.</p>
<h2 id="tracing-the-delegation-hop-sub-agent-handoffs-as-telemetry">Tracing the Delegation Hop: Sub-Agent Handoffs as Telemetry</h2>
<p>The delegation hop is the unit of work that most distinguishes multi-agent observability from ordinary tracing. A handoff happens when one agent produces output that becomes the input to another agent, or when a supervisor spawns a sub-agent and awaits its result.</p>
<p>To trace this properly, every handoff should carry four things. First, a correlation ID that survives across agent boundaries. Second, the input context being passed, so you can see what the receiving agent actually saw. Third, the outcome classification, such as success, retry, or failure. Fourth, timing information so you can spot latency that balloons at a specific hop.</p>
<p>Treating handoffs as first-class telemetry changes how you debug. Consider a pipeline where a writer agent produces a draft and hands it to an SEO agent. If the SEO agent produces a poor result, a naive log tells you the SEO agent failed. A traced handoff tells you whether the writer&rsquo;s draft was malformed, whether the model context was truncated at the handoff, or whether the SEO agent simply received bad instructions. The handoff trace localizes the fault.</p>
<p>OpenTelemetry has become the dominant standard for this correlated tracing. Its span model maps cleanly onto agent execution: one span for the agent&rsquo;s reasoning, child spans for each tool call, and a child span for each delegation hop. Startups in the space have built on this standard to detect higher-order failures, including hallucinations, by analyzing patterns across OTel spans rather than single log lines. If you are building a multi-agent lab from scratch, OpenTelemetry is the natural backbone because it is vendor-neutral and maps onto the agent execution model almost one-to-one.</p>
<h2 id="testing-multi-agent-workflows-evals-synthetic-data-and-regression-suites">Testing Multi-Agent Workflows: Evals, Synthetic Data, and Regression Suites</h2>
<p>Observability tells you what happened; testing tells you whether it should have happened. The two are complements, and a mature multi-agent lab runs both.</p>
<p>The testing problem is compounded by non-determinism. Because LLM output is stochastic, a single test pass is nearly meaningless. The evaluator community has converged on a distinction between offline evals, which run against fixed datasets before deployment, and online evals, which monitor production behavior. The pitfall of a single-pass eval is that it misses the non-deterministic behavior that is inherent to agent systems. You need repeated runs and statistical thresholds, not one pass/fail.</p>
<p>Synthetic evaluation datasets have become increasingly important because real-world agent evals are scarce. You cannot easily collect thousands of realistic multi-agent delegation trajectories from production, so teams construct synthetic datasets that exercise the failure modes they care about: ambiguous instructions, missing context, tool errors, and multi-hop reasoning. These datasets give you a stable baseline against which to measure every change.</p>
<p>Regression suites close the loop. Whenever you change a prompt, swap a model, or add a tool, you rerun the synthetic suite and compare against the previous baseline. A statistically significant drop in task completion, tool-call correctness, or handoff quality blocks the change. This is the discipline that turns a multi-agent experiment into a reliable product.</p>
<h2 id="building-the-observability-stack-opentelemetry-ebpf-and-trace-pricing">Building the Observability Stack: OpenTelemetry, eBPF, and Trace Pricing</h2>
<p>There are two broad instrumentation philosophies in 2026: SDK-based tracing and zero-instrumentation observability.</p>
<p>SDK-based tracing is the mainstream choice. You add an OpenTelemetry SDK or a similar library to your agent framework, and it emits spans automatically for agent steps, tool calls, and handoffs. This gives you rich, structured, correlated telemetry with full control over what you capture. The cost is that you must modify agent source code, and you must do so correctly across every agent and every framework in your stack.</p>
<p>Zero-instrumentation observability is the emerging alternative. Using eBPF at the kernel level, tools can capture agent trajectories and tool calls without modifying agent source code at all. This is attractive for teams that cannot touch agent source, whether because it is a third-party dependency, a closed system, or simply too large to retrofit. The trade-off is a coarser view: eBPF observes system calls and network activity, so it captures what an agent does but not the reasoning that led to it. For many teams the two approaches are complementary: SDK tracing for deep in-process detail, eBPF for coverage across agents you do not control.</p>
<p>The economics are changing too. As commercial observability platforms matured in 2026, several began pricing per agent trace rather than per host or per log volume. One example prices around $10 per million agent traces, which signals a maturing commercial tooling wave. This pricing model matters for cost planning, because a busy delegation-heavy workflow can emit far more spans than a traditional microservice, and the per-trace cost can compound quickly across thousands of agents.</p>
<h2 id="open-source-vs-commercial-tooling-in-2026">Open-Source vs. Commercial Tooling in 2026</h2>
<p>The tooling landscape splits into open-source stacks and commercial platforms, and the right choice depends on your team&rsquo;s tolerance for assembly versus the value of a managed experience.</p>
<p>Open-source options such as Arize Phoenix and Laminar give you full control and no per-trace fees. They integrate with OpenTelemetry, so you can capture the same correlated spans you would with any vendor, then visualize and analyze them yourself. Local-first stacks such as Agent Super Spy offer a lightweight dev-time alternative that catches tool-call and token-level issues during the build loop without standing up a hosted platform. For a team new to agent telemetry, starting open-source is often the fastest path to understanding what you actually need.</p>
<p>Commercial platforms such as Oodle and Lucidic focus on trace cost, latency, and failure diagnosis for multi-agent systems, often with per-agent-trace pricing. They trade a subscription fee for lower operational burden, built-in dashboards, and alerting. The 2026 commercialization wave is driven by the reality that agent telemetry is genuinely hard to operate at scale, and many teams prefer to pay for a managed diagnosis surface.</p>
<p>The comparison in practice comes down to a few dimensions:</p>
<table>
  <thead>
      <tr>
          <th>Dimension</th>
          <th>Open-Source (Phoenix, Laminar, Agent Super Spy)</th>
          <th>Commercial (Oodle, Lucidic)</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>Cost model</td>
          <td>Free software, self-hosted infrastructure</td>
          <td>Subscription, often per agent trace</td>
      </tr>
      <tr>
          <td>Setup effort</td>
          <td>High; you assemble and operate the stack</td>
          <td>Low; managed dashboards and alerts</td>
      </tr>
      <tr>
          <td>Data control</td>
          <td>Full; data stays in your environment</td>
          <td>Data flows to vendor platform</td>
      </tr>
      <tr>
          <td>Depth</td>
          <td>Deep; you configure every span</td>
          <td>Curated; opinionated agent dashboards</td>
      </tr>
      <tr>
          <td>Best for</td>
          <td>Teams learning telemetry, high-data volumes</td>
          <td>Teams needing fast diagnosis at scale</td>
      </tr>
  </tbody>
</table>
<p>A reasonable default is to start open-source, learn the correlated-span model, and adopt a commercial platform only when operational overhead or trace volume makes it worthwhile.</p>
<h2 id="a-practical-playbook-for-your-multi-agent-lab">A Practical Playbook for Your Multi-Agent Lab</h2>
<p>If you are building a multi-agent workflow lab today, here is a concrete sequence to follow.</p>
<p>Start with the delegation graph. Before instrumenting anything, draw the agents and the handoffs between them. Every edge in that graph is a place where a failure can hide, and every edge should become a traced span.</p>
<p>Instrument with OpenTelemetry at the framework layer. Emit a span for each agent execution, child spans for each tool call, and a dedicated span for each handoff that carries the correlation ID and the input context. This single step closes most of the observability gap because it makes the delegation path reconstructable.</p>
<p>Build a synthetic eval suite that exercises your known failure modes. Include ambiguous instructions, missing context, tool errors, and multi-hop reasoning. Run it as a regression baseline before every prompt, model, or tool change.</p>
<p>Add online evaluation. In production, sample live agent runs, classify their outcomes, and feed failures back into the synthetic suite so the tests improve over time. This creates a feedback loop between the observability and testing halves of the system.</p>
<p>Decide your instrumentation philosophy deliberately. If you can modify agent source, prefer SDK tracing for depth. If you have agents you cannot touch, add eBPF-based tracing for coverage. Many mature labs use both.</p>
<p>Finally, set budgets and alerts on trace volume and cost. With per-trace pricing becoming common, a runaway delegation loop is not just a reliability problem; it is a cost problem. Alert on abnormal span volume as a proxy for runaway behavior.</p>
<h2 id="when-delegation-breaks-diagnosing-failures-and-adding-reliability-layers">When Delegation Breaks: Diagnosing Failures and Adding Reliability Layers</h2>
<p>Even with good observability, delegation fails. The value of telemetry is that it makes the failure localizable, and the value of testing is that it catches many failures before they reach production. Together they let you diagnose fast.</p>
<p>Common failure signatures include a handoff that passes truncated context, an agent that repeatedly delegates to itself in a loop, a sub-agent that returns a plausible answer without completing its assigned work, and a supervisor that assigns work to an agent lacking the right tools. Each of these has a distinct trace pattern. Truncated context shows up as a handoff span with an unusually small input payload. Self-delegation shows up as a deep chain of identical spans. Silent non-completion shows up as a handoff outcome marked success followed by a downstream agent that had nothing useful to work with.</p>
<p>The reliability layer is what turns diagnosis into prevention. Add timeouts on every delegated sub-task, retries with bounded backoff on tool failures, budget caps on token consumption, and validation gates that check handoff output against a schema or a lightweight classifier before it propagates. Because LLM agent workflows fail silently, you should assume failure and design the guardrails in from the start rather than bolting them on after an incident.</p>
<h2 id="the-road-ahead-agents-that-observe-themselves">The Road Ahead: Agents That Observe Themselves</h2>
<p>The trajectory in 2026 is clear. Observability is moving from a retrofit to a built-in property of agent frameworks, from flat logs to correlated spans, and from manual instrumentation to options that require no code changes at all. OpenTelemetry has won the standard battle, per-trace pricing has legitimized agent telemetry as a market, and kernel-level tracing has made observability possible even where source changes are impossible.</p>
<p>The next frontier is agents that observe themselves. Instead of a human reading a dashboard after a failure, agents will emit telemetry that another agent consumes to self-correct, re-plan, or escalate. The same delegation graph that today needs human monitoring will increasingly be monitored by supervisory agents that detect drift, run evals, and adjust prompts without a human in the loop. The teams that invest now in correlated telemetry, regression suites, and a reliability-first mindset will be the ones best positioned to let their multi-agent systems run safely and autonomously as that future arrives.</p>
<h2 id="faq">FAQ</h2>
<p><strong>Q: What is multi-agent workflow observability?</strong>
A: It is the practice of capturing every delegation hop, tool call, and sub-agent handoff as correlated telemetry so you can reconstruct, debug, and monitor how tasks flow across multiple AI agents. It goes beyond standard logs by tracing intermediate reasoning and agent-to-agent handoffs.</p>
<p><strong>Q: Why do standard logs fail for multi-agent systems?</strong>
A: Standard logs miss intermediate reasoning, tool invocations inside sub-agents, and delegation handoffs. This is called the &ldquo;agent observability gap.&rdquo; Without correlated spans, you see fragments of activity but cannot reconstruct the full path a task traveled, so silent failures stay invisible.</p>
<p><strong>Q: What is the difference between offline and online agent evaluation?</strong>
A: Offline evals run against fixed, often synthetic datasets before deployment to catch regressions, while online evals monitor live production behavior. Both are needed because single-pass tests miss the non-deterministic behavior inherent to LLM agents.</p>
<p><strong>Q: What is eBPF-based agent observability?</strong>
A: It is zero-instrumentation tracing that captures agent trajectories and tool calls at the kernel level using eBPF, without modifying agent source code. It is useful for agents you cannot change, but it observes what agents do rather than the reasoning behind it.</p>
<p><strong>Q: How much does agent observability cost in 2026?</strong>
A: Commercial platforms increasingly price per agent trace, with some around $10 per million agent traces. Open-source options like Arize Phoenix and Laminar carry no per-trace fee but require you to self-host and operate the infrastructure yourself.</p>
]]></content:encoded></item></channel></rss>