<?xml version="1.0" encoding="utf-8" standalone="yes"?><rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom" xmlns:content="http://purl.org/rss/1.0/modules/content/"><channel><title>Context Portability on RockB</title><link>https://baeseokjae.github.io/tags/context-portability/</link><description>Recent content in Context Portability on RockB</description><image><title>RockB</title><url>https://baeseokjae.github.io/images/og-default.png</url><link>https://baeseokjae.github.io/images/og-default.png</link></image><generator>Hugo</generator><language>en-us</language><lastBuildDate>Mon, 31 Aug 2026 10:01:58 +0000</lastBuildDate><atom:link href="https://baeseokjae.github.io/tags/context-portability/index.xml" rel="self" type="application/rss+xml"/><item><title>Portable Handoff: How to Keep Context Between AI Chats, Models, and Coding Agents</title><link>https://baeseokjae.github.io/posts/portable-handoff-agent-context/</link><pubDate>Mon, 31 Aug 2026 10:01:58 +0000</pubDate><guid>https://baeseokjae.github.io/posts/portable-handoff-agent-context/</guid><description>Keep agent context portable across AI chats, models, and coding agents with a single source-of-truth handoff file — symlinked and imported everywhere.</description><content:encoded><![CDATA[<h1 id="portable-handoff-how-to-keep-context-between-ai-chats-models-and-coding-agents">Portable Handoff: How to Keep Context Between AI Chats, Models, and Coding Agents</h1>
<p>AI coding agents start every session with a fresh context window, so anything you do not persist is re-learned from scratch. A portable handoff file solves this: you write your instructions, preferences, and project conventions once, then symlink or import that single source of truth into every agent you use — Claude, Gemini, Cursor, or Codex — so context follows you across chats, models, and tools without drifting.</p>
<h2 id="why-ai-agents-forget-everything-between-chats-the-amnesia-problem">Why AI agents forget everything between chats (the amnesia problem)</h2>
<p>Every coding agent you use opens a new session with an empty memory of your project. As Hendrycks and colleagues note in their 2025 definition of AGI, &ldquo;without continual learning, AI systems suffer amnesia, forcing re-learned context each interaction.&rdquo; A coding agent is no different: unless you give it persistent memory, it re-learns your build commands, code style, and conventions every time you start a chat.</p>
<p>In Claude Code specifically, every session begins with a fresh context window, and your remembered instructions are the only thing carried over by default. The cost of this amnesia is real. Developers lose minutes to hours re-explaining their setup, and small mistakes — the wrong package manager, the ignored test command, the forgotten lint rule — creep back in whenever context is not portable.</p>
<p>The root cause is not the model. It is that context has to be <em>transferred</em> rather than <em>recalled</em>. A portable handoff file is the mechanism for that transfer, and once it exists it becomes the single reference every agent loads on startup.</p>
<h2 id="the-two-memory-systems-in-claude-code--claudemd-vs-auto-memory">The two memory systems in Claude Code — CLAUDE.md vs auto memory</h2>
<p>Claude Code gives you two distinct ways to carry context across sessions, and it helps to keep them separate:</p>
<ol>
<li><strong>CLAUDE.md (user-and-project-written memory).</strong> This is the file you control. It is loaded at the start of every session and describes your conventions, preferences, and workflows. You write it once and update it deliberately.</li>
<li><strong>Auto memory (Claude-written notes).</strong> Claude Code automatically writes compact notes about things it learns while working, and it consults these notes when it needs to recall preferences or prior context.</li>
</ol>
<p>The two are managed differently. CLAUDE.md is your durable, human-edited source of truth, while auto memory is a living scratchpad the agent maintains. If context matters enough to be stable, it belongs in CLAUDE.md; if it is a transient detail the agent observed, it may live in auto memory.</p>
<p>Because CLAUDE.md files are concatenated into the context at the start of every session, they consume tokens alongside the conversation — which is why concise instructions produce more reliable agents than sprawling ones. Every directory you work in can contribute a CLAUDE.md, and the file is checked in the current working directory and every directory above it, with folders closer to where you launched loading last.</p>
<h2 id="choose-your-source-of-truth-agentsmd-claudemd-or-both">Choose your source of truth: AGENTS.md, CLAUDE.md, or both</h2>
<p>The single biggest source of agent amnesia drift is that different tools read different instruction files. Claude Code reads <code>CLAUDE.md</code>, while Cursor and the Codex CLI read <code>AGENTS.md</code>. Write your instructions only in <code>CLAUDE.md</code> and Cursor will never see them; write only in <code>AGENTS.md</code> and Claude Code may miss them unless configured.</p>
<p>The pragmatic answer is to pick <strong>one source of truth</strong> and route it everywhere. <code>AGENTS.md</code> is emerging as the cross-tool convention because it is read by multiple agents natively. Claude Code itself reads <code>AGENTS.md</code>, and its <code>/init</code> command can generate a <code>CLAUDE.md</code> that incorporates <code>AGENTS.md</code>, Copilot rules (<code>.github/copilot-instructions.md</code>), <code>.devin/rules/</code>, <code>.windsurf/rules/</code>, and <code>.clinerules</code>.</p>
<p>The decision matrix looks like this:</p>
<table>
  <thead>
      <tr>
          <th>Choice</th>
          <th>Best for</th>
          <th>Trade-off</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td><code>AGENTS.md</code> only</td>
          <td>Multi-tool teams using Cursor, Codex, Copilot</td>
          <td>Claude Code reads it but may need a compatibility note</td>
      </tr>
      <tr>
          <td><code>CLAUDE.md</code> only</td>
          <td>Claude Code heavy users</td>
          <td>Other tools will miss your instructions</td>
      </tr>
      <tr>
          <td><code>AGENTS.md</code> + symlinked <code>CLAUDE.md</code></td>
          <td>Everything reads one shared file</td>
          <td>Symlink keeps both names pointing to a single inode</td>
      </tr>
  </tbody>
</table>
<p>For most teams, the winner is a single shared <code>AGENTS.md</code> as the canonical file, symlinked to <code>CLAUDE.md</code> so Claude Code sees it automatically and there is exactly one place to edit.</p>
<h2 id="three-ways-to-keep-files-in-sync--symlink--import-or-pointer-file">Three ways to keep files in sync — symlink, @-import, or pointer file</h2>
<p>Once you have a shared source of truth, you need to make every tool read it without copying content. There are three proven patterns:</p>
<p><strong>1. Unix symlink.</strong> The cleanest solution. Because both names resolve to one file, the content can never drift:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-bash" data-lang="bash"><span style="display:flex;"><span>ln -s AGENTS.md CLAUDE.md
</span></span></code></pre></div><p>Since both names point to the same inode, editing either updates both instantly. This is the DRY principle applied to agent memory.</p>
<p><strong>2. @-reference / import.</strong> If you want a shared file plus tool-specific additions, use Claude Code&rsquo;s import syntax. A <code>CLAUDE.md</code> can pull in another file with <code>@path</code> syntax (up to four hops deep), letting you keep the shared content separate from Claude-specific instructions while still loading both.</p>
<p><strong>3. Pointer file.</strong> Create a small <code>CLAUDE.md</code> whose entire purpose is to redirect the agent to the real source:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-text" data-lang="text"><span style="display:flex;"><span>READ AGENTS.md FIRST
</span></span></code></pre></div><p>Placed in ALL CAPS at the top, this tells Claude Code to find and follow the shared instructions file without duplicating it. It is the oldest and most compatible approach when symlinks are unavailable.</p>
<p>The principle mirrors code: one source of truth, routed everywhere. When you update project conventions, you update one file and every tool picks it up next session.</p>
<h2 id="a-portable-handoff-workflow-you-can-copy-write-once-reuse-everywhere">A portable handoff workflow you can copy (write once, reuse everywhere)</h2>
<p>Here is a concrete, copyable workflow that keeps context portable without adding daily maintenance:</p>
<p><strong>Step 1 — Create one canonical file.</strong> Start with a single <code>AGENTS.md</code> at your project root. Include the essentials: how to build and test, your lint and style rules, the architecture map, and any commands you never want an agent to get wrong.</p>
<p><strong>Step 2 — Symlink it for Claude Code.</strong></p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-bash" data-lang="bash"><span style="display:flex;"><span>ln -sf AGENTS.md CLAUDE.md
</span></span></code></pre></div><p>Now Claude Code, Cursor, and Codex CLI all load the same canonical instructions.</p>
<p><strong>Step 3 — Keep per-tool specifics minimal.</strong> If a tool genuinely needs something different, add a thin layer on top rather than a second copy. For Claude-only notes, rely on <code>@</code>-references or a pointer file so you never maintain parallel duplicates.</p>
<p><strong>Step 4 — Export once, reuse everywhere.</strong> Because the file is plain Markdown, the same handoff works across Claude, Gemini CLI, Cursor, and Codex. Write the context once; every agent loads it on startup.</p>
<p><strong>Step 5 — Review on a schedule.</strong> Agent memory files rot just like documentation. Treat <code>AGENTS.md</code> as living code — update it when conventions change, and prune dead instructions to keep token usage low.</p>
<h2 id="keep-personal-rules-private-with-claudelocalmd">Keep personal rules private with CLAUDE.local.md</h2>
<p>Not everything belongs in a shared, committed file. Personal preferences — your editor keybindings, your preferred branch strategy, one-off formatting tastes — should stay out of <code>AGENTS.md</code> and live in <code>CLAUDE.local.md</code>, which is gitignored by default.</p>
<p>The hierarchy works by scope:</p>
<table>
  <thead>
      <tr>
          <th>Scope</th>
          <th>File</th>
          <th>Committed?</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>Organization</td>
          <td><code>/etc/claude-code/CLAUDE.md</code></td>
          <td>No</td>
      </tr>
      <tr>
          <td>User</td>
          <td><code>~/.claude/CLAUDE.md</code></td>
          <td>No</td>
      </tr>
      <tr>
          <td>Project (shared)</td>
          <td><code>./AGENTS.md</code> → <code>./CLAUDE.md</code></td>
          <td>Yes</td>
      </tr>
      <tr>
          <td>Local (personal)</td>
          <td><code>CLAUDE.local.md</code></td>
          <td>No (gitignored)</td>
      </tr>
  </tbody>
</table>
<p>The rule is simple: shared truth goes in the committed project file; personal truth goes in the gitignored local file. That keeps a portable, reviewable source of truth while still letting each engineer work the way they like.</p>
<h2 id="make-the-handoff-survive-a-long-session-compact-auto-memory">Make the handoff survive a long session (/compact, auto memory)</h2>
<p>Even with a perfect handoff file, a single conversation can outgrow its context window. Two Claude Code features keep context stable during long sessions:</p>
<ul>
<li><strong><code>/compact</code></strong> summarizes the conversation when context runs low, distilling what has been discussed into a tighter form so work can continue without losing the thread.</li>
<li><strong>Auto memory</strong> keeps Claude-written notes about your preferences and prior context, so even mid-session the agent can consult what it has learned rather than re-asking.</li>
</ul>
<p>Because <code>CLAUDE.md</code> is loaded at session start and consumes tokens alongside the conversation, keeping it concise directly improves reliability over long sessions — every unnecessary line competes with the actual conversation for the context window.</p>
<h2 id="portability-across-models-and-tools-gemini-cli-cursor-codex">Portability across models and tools (Gemini CLI, Cursor, Codex)</h2>
<p>The whole point of a portable handoff is that it is not locked to one vendor. Because your handoff file is plain Markdown, the same <code>AGENTS.md</code> loads in Claude Code, Cursor, the Codex CLI, and, with the right plugin or import step, Gemini CLI and other models.</p>
<p>This cross-model portability is becoming its own product category, which is a strong signal of demand. Tools exist specifically to sync and port agent memory files across agents, and plugins can export context once and reuse it in another agent. The pattern behind all of them is identical: define context in a neutral, portable form, then let each tool read that same form.</p>
<p>For a development team, the payoff is that an engineer can switch models — or a team can standardize on one — without anyone re-explaining the project. The handoff file travels with the code.</p>
<h2 id="pitfalls-to-avoid-drift-contradictions-oversized-memory-files">Pitfalls to avoid (drift, contradictions, oversized memory files)</h2>
<p>Portable handoff works only if the file stays trustworthy. Watch for these three failure modes:</p>
<ol>
<li><strong>Drift.</strong> The classic killer: instructions written to <code>CLAUDE.md</code> and <code>AGENTS.md</code> as separate copies, then updated in only one place. Fix it permanently with a symlink or <code>@</code>-reference so there is never a second copy to forget.</li>
<li><strong>Contradictions.</strong> Two files saying different things about the same workflow confuse agents and produce inconsistent behavior. A single source of truth eliminates the conflict at the root.</li>
<li><strong>Oversized memory files.</strong> Every line of <code>CLAUDE.md</code> consumes tokens at every session start. Bloated instruction files degrade reliability across all tools. Keep only what is durable and prune the rest.</li>
</ol>
<p>Treat your agent memory files like any other code: version them, review them, and keep them DRY.</p>
<h2 id="faq">FAQ</h2>
<p><strong>What is a portable handoff file for AI agents?</strong>
A portable handoff file — typically <code>AGENTS.md</code> or <code>CLAUDE.md</code> — is a plain-text document that carries your project instructions, preferences, and conventions from one AI session to the next, and across different AI tools and models, so context does not have to be re-explained every time.</p>
<p><strong>Why do AI coding agents forget context between chats?</strong>
Coding agents open each session with a fresh context window and, without persistent memory, must re-learn your setup from scratch every time — the &ldquo;amnesia&rdquo; problem. A handoff file loaded at session start is the fix.</p>
<p><strong>Should I use AGENTS.md or CLAUDE.md?</strong>
Use a single <code>AGENTS.md</code> as your canonical source of truth because it is the cross-tool convention read by Cursor and Codex, then symlink it to <code>CLAUDE.md</code> so Claude Code reads the same file. Pick one file and route it everywhere.</p>
<p><strong>How do I keep AGENTS.md and CLAUDE.md in sync?</strong>
The most reliable way is a symlink (<code>ln -s AGENTS.md CLAUDE.md</code>), so both names resolve to one file and can never drift. Alternatives are an <code>@</code>-reference/import or a pointer file that redirects the agent to the shared source.</p>
<p><strong>Does portable context work across different AI models?</strong>
Yes. Because a handoff file is plain Markdown, the same file loads in Claude Code, Gemini CLI, Cursor, and the Codex CLI, with the right import step. Cross-model context portability is an active product category precisely because developers want one handoff that follows them everywhere.</p>
]]></content:encoded></item></channel></rss>