<?xml version="1.0" encoding="utf-8" standalone="yes"?><rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom" xmlns:content="http://purl.org/rss/1.0/modules/content/"><channel><title>Portkey on RockB</title><link>https://baeseokjae.github.io/tags/portkey/</link><description>Recent content in Portkey on RockB</description><image><title>RockB</title><url>https://baeseokjae.github.io/images/og-default.png</url><link>https://baeseokjae.github.io/images/og-default.png</link></image><generator>Hugo</generator><language>en-us</language><lastBuildDate>Thu, 21 May 2026 09:34:26 +0000</lastBuildDate><atom:link href="https://baeseokjae.github.io/tags/portkey/index.xml" rel="self" type="application/rss+xml"/><item><title>LLM Gateway Comparison 2026: Portkey vs Helicone vs LiteLLM After the Shakeup</title><link>https://baeseokjae.github.io/posts/llm-gateway-comparison-2026/</link><pubDate>Thu, 21 May 2026 09:34:26 +0000</pubDate><guid>https://baeseokjae.github.io/posts/llm-gateway-comparison-2026/</guid><description>Helicone was acquired and LiteLLM was compromised in 2026. Here&amp;#39;s how Portkey, LiteLLM, Helicone, and emerging alternatives compare for routing, cost, and reliability.</description><content:encoded><![CDATA[<p>The short answer: <strong>Portkey</strong> is the best drop-in replacement if you&rsquo;re running Helicone or evaluating alternatives after the LiteLLM security scare. It covers 200+ providers, adds under 1ms of latency, and gives you routing, caching, and observability in a single package. LiteLLM is still viable for self-hosted open-source use if you pin a pre-compromise version and monitor CVEs actively.</p>
<h2 id="why-2026-is-the-year-of-llm-gateway-evaluation">Why 2026 Is the Year of LLM Gateway Evaluation</h2>
<p>The LLM gateway market hit a turning point in early 2026 with two simultaneous events that forced teams to re-evaluate their infrastructure. On March 3, 2026, Helicone was acquired by Mintlify — the documentation platform — and immediately entered maintenance mode, meaning no new features, only security patches and bug fixes. Within the same quarter, LiteLLM suffered a documented security compromise that raised concerns about the supply chain security of open-source proxy deployments. These two events hit simultaneously at a moment when enterprise LLM API spending had already grown from $3.5B in late 2024 to $8.4B by mid-2025 — a 2.4x increase in roughly six months. Teams that had quietly been running Helicone for observability or LiteLLM for routing suddenly had urgent migration decisions to make. Add to this that 37% of enterprises now run five or more LLMs in production, and the case for a robust, multi-provider gateway has never been stronger. This guide evaluates your real options with the current market in mind.</p>
<h3 id="helicone-acquisition-what-it-means-for-users">Helicone Acquisition: What It Means for Users</h3>
<p>Helicone joining Mintlify is good news for Mintlify&rsquo;s documentation product and bad news for teams relying on Helicone as an active LLM observability gateway. The official announcement confirmed the product is in maintenance mode: the team moved to San Francisco to work on Mintlify&rsquo;s core product. What this means practically is that any open issue you&rsquo;ve filed is likely to stay open. Feature requests are dead. If you&rsquo;ve built internal tooling around Helicone&rsquo;s API, you&rsquo;re on borrowed time. The proxy still works — it passes requests through and logs them — but you&rsquo;re now running a deprecated dependency with no clear sunset date. The sensible move is a planned migration rather than waiting for the surprise end-of-life notice.</p>
<h3 id="litellm-security-compromise-how-to-respond">LiteLLM Security Compromise: How to Respond</h3>
<p>The LiteLLM security incident in 2026 was a supply chain compromise — a compromised dependency in a specific version range. If you&rsquo;re running LiteLLM in a production environment and haven&rsquo;t audited your version and its transitive dependencies since early 2026, you need to. The community response was fast, and patched versions are available, but the incident highlighted a real risk with open-source proxies: you inherit the attack surface of every library they pull in. Enterprise teams running LiteLLM behind their API gateway got largely got away with shallow exposure, but teams with direct internet-facing LiteLLM deployments had a harder time. The takeaway isn&rsquo;t &ldquo;never use LiteLLM&rdquo; — it&rsquo;s &ldquo;apply the same supply chain hygiene to your AI infrastructure as you would to any other production service.&rdquo;</p>
<h2 id="llm-gateway-comparison-portkey-vs-helicone-vs-litellm">LLM Gateway Comparison: Portkey vs Helicone vs LiteLLM</h2>
<p>The three tools in this comparison serve overlapping but distinct purposes, and their architecture choices reflect different bets about what matters most. Portkey is a commercial cloud gateway built for multi-provider routing at enterprise scale, supporting over 200 LLM providers with advanced routing logic, semantic caching, and built-in observability. Helicone was a proxy-based observability layer optimized for logging and monitoring — it sat in front of your LLM calls and captured metadata — but it was never primarily a routing engine. LiteLLM is the open-source universal interface that lets you call any LLM with the OpenAI SDK format, typically deployed self-hosted. Each of these sits at a different point on the control vs. convenience spectrum. Portkey maximizes capabilities at the cost of vendor dependency. LiteLLM maximizes control at the cost of operational overhead. Helicone, in maintenance mode, is the tool you&rsquo;re probably migrating away from. Enterprise LLM spending growth and the 37% figure on multi-LLM production deployments make clear that the routing and cost-optimization layer — Portkey&rsquo;s core value prop — matters more than ever.</p>
<h3 id="portkey-multi-provider-enterprise-gateway">Portkey: Multi-Provider Enterprise Gateway</h3>
<p>Portkey is the most feature-complete option in this comparison. The gateway supports 200+ LLM providers with a unified API surface. You get routing rules that span load balancing, fallback chains, and model-specific cost thresholds. Semantic caching — where Portkey recognizes semantically similar requests and serves cached responses — can cut costs meaningfully on high-traffic patterns. The observability layer captures latency, cost per call, token usage, and error rates across all providers. Setup is a one-line base URL swap if you&rsquo;re already using OpenAI&rsquo;s SDK format.</p>
<p>For teams migrating from Helicone, the operational continuity is smooth. Portkey&rsquo;s API is compatible with the same request/response format you&rsquo;re already using. The paid tier starts at $49/month for production use. There&rsquo;s a free tier for low-volume testing.</p>
<h3 id="helicone-maintenance-mode-reality-check">Helicone: Maintenance Mode Reality Check</h3>
<p>Helicone&rsquo;s architecture was a lightweight proxy that sat between your application and any LLM API. It added minimal latency — roughly 1-2ms — and captured extensive request/response logs. The dashboard was genuinely useful for cost tracking and debugging prompt regressions. But the acquisition changed the calculus. If you&rsquo;re still running Helicone, you should audit your integration against Mintlify&rsquo;s official maintenance mode statement: security patches and new model support only. No new features. Your logs are fine, your existing integrations work, but you&rsquo;re accumulating technical debt with every month you stay on it.</p>
<p>The migration path is straightforward. Portkey and Langfuse both offer Helicone-compatible logging adapters. The observability use case maps cleanly to either, while Portkey additionally covers the routing and cost-optimization angles Helicone never touched.</p>
<h3 id="litellm-open-source-power-with-operational-cost">LiteLLM: Open-Source Power With Operational Cost</h3>
<p>LiteLLM&rsquo;s core promise — call 100+ LLMs with a single SDK interface — remains valid and useful. The proxy server mode gives you a self-hosted gateway that your team controls entirely. For teams with strong infrastructure preferences, data sovereignty requirements, or cost sensitivity at scale, LiteLLM&rsquo;s self-hosted deployment still makes sense.</p>
<p>The operational overhead is real, though. You own the deployment, the uptime, the version upgrades, and now, explicitly, the security audits. The 2026 compromise was a wakeup call for teams treating LiteLLM as a set-and-forget proxy. If you maintain LiteLLM, commit to a dependency audit cadence and subscribe to their security advisories.</p>
<p>The Python bottleneck is also worth acknowledging. Under heavy load — around 500+ requests per second — LiteLLM&rsquo;s Python-based routing adds measurable latency compared to alternatives like Bifrost (a Rust-based gateway that adds only 11 microseconds at 5,000 RPS). For most teams, this isn&rsquo;t a real-world constraint, but it matters for latency-sensitive applications.</p>
<h2 id="key-differentiators-feature-comparison-table">Key Differentiators: Feature Comparison Table</h2>
<p>Choosing between LLM gateways means understanding where each tool draws its architectural boundaries. Portkey is a commercial gateway with routing and caching as core capabilities — it&rsquo;s not just an observability layer. Helicone was built specifically as an observability proxy that logged requests without modifying them, which is why it never shipped routing features. LiteLLM started as a Python abstraction layer (call any model with OpenAI syntax) and grew a self-hostable proxy server on top. These origins matter because they determine what each tool does well under production conditions. Portkey&rsquo;s routing handles provider outages transparently — if Anthropic returns a 529, Portkey reraises to your configured fallback without your application seeing the error. LiteLLM&rsquo;s routing requires explicit fallback configuration and re-raises errors differently. Helicone passed everything through with no modification, which was its strength (no risk of altering requests) and its ceiling (no ability to add intelligence). Understanding these architectural commitments helps explain the feature gaps in the table below.</p>
<table>
  <thead>
      <tr>
          <th>Feature</th>
          <th>Portkey</th>
          <th>Helicone</th>
          <th>LiteLLM</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>Provider Support</td>
          <td>200+</td>
          <td>Any (proxy)</td>
          <td>100+</td>
      </tr>
      <tr>
          <td>Routing &amp; Fallbacks</td>
          <td>Yes (advanced)</td>
          <td>No</td>
          <td>Yes (basic)</td>
      </tr>
      <tr>
          <td>Semantic Caching</td>
          <td>Yes</td>
          <td>No</td>
          <td>Limited</td>
      </tr>
      <tr>
          <td>Observability</td>
          <td>Yes (built-in)</td>
          <td>Yes (core feature)</td>
          <td>Limited</td>
      </tr>
      <tr>
          <td>Self-Hosted</td>
          <td>Paid enterprise</td>
          <td>Open source</td>
          <td>Yes (free)</td>
      </tr>
      <tr>
          <td>Maintenance Status</td>
          <td>Active</td>
          <td>Maintenance mode</td>
          <td>Active (post-patch)</td>
      </tr>
      <tr>
          <td>Latency Overhead</td>
          <td>~1ms</td>
          <td>~1-2ms</td>
          <td>~2-5ms (Python)</td>
      </tr>
      <tr>
          <td>Security Compliance</td>
          <td>SOC 2 Type II</td>
          <td>N/A (maintenance)</td>
          <td>Self-managed</td>
      </tr>
      <tr>
          <td>Pricing</td>
          <td>Free + $49/mo</td>
          <td>Free (maintenance)</td>
          <td>Free (self-hosted)</td>
      </tr>
  </tbody>
</table>
<h3 id="provider-support-the-numbers-matter">Provider Support: The Numbers Matter</h3>
<p>Provider support at 200+ (Portkey) versus 100+ (LiteLLM) sounds like a marketing number until you need a specific model. In practice, the models you can&rsquo;t route through your gateway are the ones you end up calling directly — which creates a two-tier infrastructure and an observability gap. Portkey&rsquo;s broader coverage means you&rsquo;re less likely to hit the &ldquo;this model isn&rsquo;t supported yet&rdquo; wall when a new provider launches. LiteLLM&rsquo;s community-driven model additions are fast but depend on contributor bandwidth.</p>
<h3 id="routing-capabilities-where-portkey-pulls-ahead">Routing Capabilities: Where Portkey Pulls Ahead</h3>
<p>Load balancing across providers, automatic fallback chains, and cost-threshold routing are features Portkey was built around from day one. If your primary call to GPT-4o fails, Portkey can automatically retry with Claude 3.5 Sonnet or any other fallback you configure. You can set rules like &ldquo;use the cheapest model that meets this latency threshold&rdquo; or &ldquo;failover to self-hosted Llama if OpenAI returns a 429.&rdquo;</p>
<p>LiteLLM has basic fallback support, but the configuration is more manual and the routing logic less sophisticated. Helicone never offered routing at all — it was observation-only.</p>
<h3 id="security-and-compliance-portkey-vs-self-managed">Security and Compliance: Portkey vs Self-Managed</h3>
<p>Portkey holds SOC 2 Type II certification. For enterprise teams with compliance requirements, this matters more than feature lists. LiteLLM self-hosted means you&rsquo;re taking on the compliance posture yourself — which is fine if you have the infrastructure team to back it, but not fine if you&rsquo;re a startup trying to close an enterprise deal.</p>
<h2 id="deployment-options-cloud-vs-self-hosted-vs-hybrid">Deployment Options: Cloud vs Self-Hosted vs Hybrid</h2>
<p>Your deployment choice should be driven by data residency requirements, team operational capacity, and scale.</p>
<p><strong>Cloud (Portkey):</strong> Fastest path to production. No infrastructure to manage. Data goes through Portkey&rsquo;s cloud, which is a non-starter for some regulated industries. Appropriate for most teams that don&rsquo;t have explicit data sovereignty requirements.</p>
<p><strong>Self-Hosted (LiteLLM):</strong> Maximum control, maximum operational burden. Your data never leaves your infrastructure. Appropriate for financial services, healthcare, government, or any team with explicit data residency requirements. Requires a committed infrastructure owner.</p>
<p><strong>Hybrid:</strong> Some teams run Portkey for external providers while keeping a self-hosted LiteLLM instance for calls to internal or on-premises models. This adds architectural complexity but gives you the best of both worlds for specific use cases.</p>
<p>Helicone had a self-hosted Docker option, but maintenance mode makes it risky to build new deployments on it.</p>
<h2 id="real-world-migration-scenarios">Real-World Migration Scenarios</h2>
<p>The 2026 LLM gateway shakeup created three migration pressures simultaneously: Helicone users needing an actively developed replacement, LiteLLM users evaluating security posture, and teams still on bare-metal API calls realizing they need centralized cost management as their LLM spend scales. Each scenario has a different urgency and a different technical path. Helicone-to-Portkey is the cleanest migration: both use OpenAI-compatible API surfaces and the base URL swap is a one-line change in most codebases. LiteLLM-to-Portkey requires more careful alias mapping but eliminates the operational burden of running your own Python gateway process. Teams moving from direct API calls to a gateway for the first time need to decide between control (LiteLLM self-hosted) and convenience (Portkey cloud). What follows are concrete migration paths for each scenario, drawn from common deployment patterns rather than theoretical examples. Smart routing combined with caching — something you get from day one with Portkey — achieves 47–80% cost reduction in production AI systems, which typically pays for the migration effort within weeks.</p>
<h3 id="migrating-from-helicone-to-portkey">Migrating from Helicone to Portkey</h3>
<p>The migration is a two-line change for most applications. Portkey uses the same OpenAI-compatible API surface. You swap your base URL from <code>https://oai.helicone.ai/v1</code> to <code>https://api.portkey.ai/v1</code> and add a <code>x-portkey-api-key</code> header. Your existing observability logging continues. You gain routing, caching, and a broader provider surface on top of what Helicone provided.</p>
<p>The one gotcha: Helicone&rsquo;s custom request metadata fields don&rsquo;t map 1:1 to Portkey&rsquo;s tagging system. If you&rsquo;ve built reporting dashboards that depend on Helicone&rsquo;s specific log schema, you&rsquo;ll need to update those queries. The data is all there in Portkey — it just uses different field names.</p>
<h3 id="migrating-from-litellm-to-portkey">Migrating from LiteLLM to Portkey</h3>
<p>If you moved off LiteLLM due to the security concerns or operational complexity, Portkey covers the same routing use cases with less maintenance burden. The critical difference: LiteLLM in proxy mode let you configure arbitrary model aliases that your application code never needed to change. Portkey&rsquo;s virtual key system achieves the same result. Audit your <code>config.yaml</code> to identify which provider aliases your application uses, map them to Portkey virtual keys, and update your base URL.</p>
<h3 id="cost-optimization-achieving-47-80-savings-with-gateway-routing">Cost Optimization: Achieving 47-80% Savings With Gateway Routing</h3>
<p>The cost-optimization case for an LLM gateway is compelling at scale. Smart routing combined with semantic caching achieves 47-80% cost reduction in production AI systems, according to benchmarks from teams like Mavik Labs. The mechanism: route simple queries to cheaper models (simple queries cost ~$0.10/M tokens vs complex coding tasks at ~$30/M — a 300x gap), cache semantically similar requests to avoid redundant API calls, and set cost-per-call thresholds that trigger automatic model downgrades.</p>
<p>RouteLLM from UC Berkeley demonstrated 85% cost reduction on MT Bench benchmarks while maintaining 95% of GPT-4 performance quality. These numbers don&rsquo;t translate directly to every workload, but they establish the ceiling of what&rsquo;s achievable with intelligent routing.</p>
<h2 id="future-trends-ai-gateway-landscape-20262030">Future Trends: AI Gateway Landscape 2026–2030</h2>
<p>The AI gateway layer is actively converging with agent orchestration infrastructure, and the tools that survive the next three years will be those that understand requests as part of longer workflows rather than isolated API calls. Current gateways treat each LLM call independently — you pass in a prompt, you get back a completion, the gateway logs cost and latency. But 40% of enterprise applications will embed task-specific AI agents by end of 2026 (Gartner), and those agents make dozens or hundreds of interdependent calls per task. A gateway that optimizes each call independently may actually harm agent performance by switching models mid-workflow, breaking the conversational context that a more expensive model was maintaining. The next generation of LLM gateways will need workflow-aware routing: understanding that calls belong to a session, that consistency across a task may outweigh per-call cost savings, and that agent failures have different recovery paths than single-call failures. Portkey&rsquo;s virtual session concept is early movement in this direction, but no gateway has fully solved workflow-aware routing yet. Here&rsquo;s where the three dimensions of future development are heading.</p>
<p>The gateway layer is converging toward three capabilities that current tools don&rsquo;t fully address:</p>
<p><strong>Agent-aware routing:</strong> Current gateways treat every request as independent. The next generation will understand that a request is part of an agentic workflow and route accordingly — holding a model selection stable across a multi-step task rather than optimizing each call in isolation.</p>
<p><strong>MCP integration:</strong> The Model Context Protocol is becoming the standard interface for tool use and context management. Gateways that integrate natively with MCP will be able to route based on tool availability, context size, and agent capability requirements.</p>
<p><strong>Real-time cost arbitrage:</strong> As model pricing changes dynamically and new providers emerge monthly, gateways will need real-time pricing feeds and automated routing adjustments rather than static configuration.</p>
<h2 id="decision-framework-which-gateway-is-right-for-your-team">Decision Framework: Which Gateway Is Right for Your Team</h2>
<p><strong>Choose Portkey if:</strong></p>
<ul>
<li>You&rsquo;re migrating from Helicone and want operational continuity</li>
<li>You need SOC 2 compliance without building it yourself</li>
<li>You&rsquo;re running multiple LLM providers and want centralized routing</li>
<li>You want semantic caching without implementing it yourself</li>
</ul>
<p><strong>Choose LiteLLM (self-hosted, post-audit) if:</strong></p>
<ul>
<li>You have strict data residency requirements</li>
<li>You have the infrastructure team to maintain it</li>
<li>You need to support on-premises or private model deployments</li>
<li>Cost of commercial tools is prohibitive at your scale</li>
</ul>
<p><strong>Stay on Helicone only if:</strong></p>
<ul>
<li>You&rsquo;re in active migration to an alternative</li>
<li>Your use case is purely observability and you accept zero new features</li>
</ul>
<p><strong>Consider Bifrost if:</strong></p>
<ul>
<li>Latency is your primary constraint (11μs overhead at 5,000 RPS)</li>
<li>You&rsquo;re building latency-sensitive real-time applications</li>
<li>You&rsquo;re comfortable with a less mature but extremely fast Rust-based option</li>
</ul>
<h2 id="faq">FAQ</h2>
<p><strong>Is Helicone still safe to use in 2026?</strong>
Yes, but with caveats. Helicone is in maintenance mode — security patches and new model support only. The proxy works and your existing integrations are stable, but you&rsquo;re accumulating technical debt. Plan your migration to an actively developed alternative within the next 6-12 months.</p>
<p><strong>Is LiteLLM safe to use after the 2026 security compromise?</strong>
Yes, if you&rsquo;re running a patched version. The LiteLLM team released fixes quickly after the compromise was disclosed. Pin a verified version, subscribe to their security advisories (GitHub Dependabot or their mailing list), and run regular dependency audits. Self-hosted means you own the security posture.</p>
<p><strong>How much does Portkey cost for a production deployment?</strong>
Portkey&rsquo;s paid tier starts at $49/month. For high-volume teams, they offer enterprise pricing. There&rsquo;s a free tier for development and low-volume testing. Given that a properly configured Portkey routing setup typically reduces total LLM spend by 20-50%, the gateway cost is usually a fraction of the savings it generates.</p>
<p><strong>What&rsquo;s the latency overhead of adding an LLM gateway?</strong>
For Portkey and Helicone-style proxies, expect 1-3ms of added latency. Rust-based gateways like Bifrost add only 11 microseconds. LiteLLM&rsquo;s Python implementation adds roughly 2-5ms under normal load, with degradation at high concurrency. For most AI applications where LLM inference itself takes 500ms+, gateway overhead is effectively invisible.</p>
<p><strong>Can I run multiple gateways simultaneously?</strong>
Yes, and some teams do. A common pattern: Portkey for external cloud providers (OpenAI, Anthropic, Gemini) and a self-hosted LiteLLM instance for internal or on-premises models. The added complexity is usually worth it for organizations with hybrid cloud and on-premises AI deployments. Route to the right gateway based on model name in your application config.</p>
]]></content:encoded></item><item><title>Helicone Alternatives 2026: Best LLM Observability Tools After the Mintlify Acquisition</title><link>https://baeseokjae.github.io/posts/helicone-alternatives-2026/</link><pubDate>Sat, 16 May 2026 00:00:00 +0000</pubDate><guid>https://baeseokjae.github.io/posts/helicone-alternatives-2026/</guid><description>Helicone was acquired by Mintlify in March 2026 and is now in maintenance mode. Here are the best alternatives: Langfuse, LangSmith, Portkey, Braintrust, and Stockyard compared.</description><content:encoded><![CDATA[<p>Helicone was acquired by Mintlify on March 3, 2026, and the platform has been in maintenance mode ever since — receiving only security patches, bug fixes, and support for new model identifiers. If you depend on Helicone in production today, your migration window is open. The strongest replacements are <strong>Langfuse</strong> (open-source, SDK-based, 40,000+ active builders), <strong>LangSmith</strong> (deepest LangChain integration available), <strong>Portkey</strong> (200+ LLM provider gateway), <strong>Braintrust</strong> (eval-first with 1M free spans per month), and <strong>Stockyard</strong> (a single ~25MB Go binary requiring zero cloud dependency).</p>
<h2 id="what-happened-to-helicone-the-mintlify-acquisition-explained">What Happened to Helicone? The Mintlify Acquisition Explained</h2>
<p>Helicone was acquired by Mintlify on <strong>March 3, 2026</strong> — the entire founding team relocated to Mintlify&rsquo;s San Francisco office within weeks of the deal closing. That single event turned one of the most popular lightweight LLM observability proxies into a maintenance-mode product overnight. Helicone had grown on the strength of its zero-friction onboarding: you could start logging all OpenAI calls by changing one base URL and adding one header, with no SDK installation and no code changes required. That developer experience won it widespread adoption among solo developers and small teams shipping fast. After the acquisition, Mintlify announced that Helicone would continue operating under maintenance mode — meaning security patches, critical bug fixes, and new model name support would be applied, but no new features would ship. The March 2026 acquisition arrived during an already turbulent moment for LLM proxy tooling: LiteLLM experienced a security compromise around the same time, creating simultaneous migration pressure across two of the most widely used open-source LLM proxy tools. Teams that had spread risk across both products found themselves evaluating entirely new options at the same moment, accelerating the shift toward more stable, actively maintained observability platforms.</p>
<h2 id="should-you-migrate-now-understanding-maintenance-mode">Should You Migrate Now? Understanding Maintenance Mode</h2>
<p>Maintenance mode at an acquired startup creates a specific, measurable risk: your infrastructure stays functional today, but the gap between your product needs and the platform&rsquo;s capabilities widens every month. <strong>Helicone&rsquo;s maintenance mode is active as of March 3, 2026</strong> — the API endpoints are live, existing integrations continue to process traces, and the dashboard functions normally. What stops immediately is feature development. Teams shipping new agent architectures, new evaluation pipelines, or new model integrations will hit gaps within months: no new evaluation frameworks, no improved cost analytics, no deeper tracing for multi-step agent workflows, no collaboration features, no integrations beyond security-patch-level model name support. For startups changing their LLM stack frequently, the migration window is effectively now. For teams with stable, infrequently changing production pipelines, urgency is lower — but the 6-month planning horizon is still the right call. Setting a target of completing migration within 6 months of the acquisition (by September 2026) gives you time to evaluate alternatives without rushing, run parallel deployments to validate data quality, and avoid being caught off-guard by a formal deprecation announcement. The LiteLLM compromise that coincided with Helicone&rsquo;s acquisition is a useful reminder: LLM tooling vendor risk is real, and a migration is an opportunity to audit your entire observability stack — not just replace one proxy with another.</p>
<h2 id="top-helicone-alternatives-compared-langfuse-langsmith-portkey-braintrust">Top Helicone Alternatives Compared: Langfuse, LangSmith, Portkey, Braintrust</h2>
<p>The four strongest Helicone alternatives collectively cover more than 95% of real-world migration scenarios, and each targets a different primary use case. <strong>Langfuse</strong> is the most direct feature replacement: open-source (MIT license), used by 40,000+ builders, SDK-based rather than proxy-based — which means zero added latency on LLM calls since observability data ships asynchronously out-of-band. <strong>LangSmith</strong> from LangChain offers the deepest integration for teams already running LangChain or LangGraph pipelines; chain-level debuggability and native trace visualization are features no other tool matches for LangChain-specific architectures. <strong>Portkey</strong> operates as a production LLM gateway supporting 200+ providers, with routing, automatic fallback, load balancing, and semantic caching — it keeps the proxy architecture that Helicone users are familiar with while adding capabilities that justify the network hop. <strong>Braintrust</strong> shifts the focus to structured evaluation: 1M free spans per month, unlimited users on all pricing tiers (no per-seat charges), and a CI/CD-friendly eval framework that integrates directly into automated test pipelines. Each tool takes a different architectural stance from Helicone&rsquo;s pure-proxy model, and the right choice depends on what your team is optimizing for — raw observability, chain debugging, multi-provider reliability, or continuous evaluation.</p>
<table>
  <thead>
      <tr>
          <th>Tool</th>
          <th>Architecture</th>
          <th>Best For</th>
          <th>Free Tier</th>
          <th>Self-Host</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>Langfuse</td>
          <td>SDK-based</td>
          <td>Open-source teams, self-hosters</td>
          <td>50K units/mo</td>
          <td>Yes (MIT)</td>
      </tr>
      <tr>
          <td>LangSmith</td>
          <td>SDK-based</td>
          <td>LangChain / LangGraph shops</td>
          <td>5K traces/mo</td>
          <td>Enterprise only</td>
      </tr>
      <tr>
          <td>Portkey</td>
          <td>Proxy-based</td>
          <td>Multi-provider gateways</td>
          <td>Limited</td>
          <td>Yes (OSS)</td>
      </tr>
      <tr>
          <td>Braintrust</td>
          <td>SDK-based</td>
          <td>Eval-heavy CI/CD pipelines</td>
          <td>1M spans/mo</td>
          <td>Yes</td>
      </tr>
      <tr>
          <td>Arize Phoenix</td>
          <td>SDK-based</td>
          <td>Mixed ML + LLM workloads</td>
          <td>Open-source</td>
          <td>Yes (Apache 2)</td>
      </tr>
      <tr>
          <td>Stockyard</td>
          <td>Proxy-based</td>
          <td>Air-gapped / solo developers</td>
          <td>Self-hosted</td>
          <td>Yes (single binary)</td>
      </tr>
  </tbody>
</table>
<h2 id="pricing-comparison-what-each-alternative-costs-at-scale">Pricing Comparison: What Each Alternative Costs at Scale</h2>
<p>At 1 million traces per month — the threshold where pricing differences become business-material — <strong>Langfuse Cloud costs approximately $919/month while LangSmith costs $2,500+</strong>, making Langfuse 2.7x cheaper at equivalent cloud scale. LangSmith&rsquo;s pricing model charges $39 per seat per month plus $2.50–$5 per 1,000 traces; at 1M traces and even a small team of 5 seats, the bill exceeds $2,500 before accounting for seats above the base plan. Langfuse&rsquo;s pricing tiers are: Free (50K units/month), Core at $29/month, and Pro at $199/month with additional usage billed on top — reaching approximately $919/month at 1M trace units. The self-hosted Langfuse alternative drops that cost to roughly $150/month in infrastructure at the same 1M trace scale, a 6x improvement over Langfuse Cloud and a 16x improvement over LangSmith. Braintrust&rsquo;s free tier covers 1M spans per month with unlimited users on all tiers — eliminating per-seat pricing entirely, which is a meaningful cost structure difference for growing teams. Portkey pricing is usage-based and gateway-focused, with costs varying by routing complexity and provider count. Stockyard costs zero beyond your own server: it runs as a ~25MB Go binary on any Linux instance, with no external database or cloud service required.</p>
<table>
  <thead>
      <tr>
          <th>Tool</th>
          <th>Free Tier</th>
          <th>At 1M Traces/Month</th>
          <th>Self-Hosted Cost</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>Langfuse Cloud</td>
          <td>50K units/mo</td>
          <td>~$919/mo</td>
          <td>~$150/mo infra</td>
      </tr>
      <tr>
          <td>LangSmith</td>
          <td>5K traces/mo</td>
          <td>$2,500+/mo</td>
          <td>Enterprise only</td>
      </tr>
      <tr>
          <td>Braintrust</td>
          <td>1M spans/mo</td>
          <td>Free–$400/mo</td>
          <td>Available</td>
      </tr>
      <tr>
          <td>Portkey</td>
          <td>Limited</td>
          <td>Usage-based</td>
          <td>Available</td>
      </tr>
      <tr>
          <td>Stockyard</td>
          <td>Unlimited (self-hosted)</td>
          <td>$0 + infra</td>
          <td>~$10–50/mo</td>
      </tr>
  </tbody>
</table>
<h2 id="proxy-based-vs-sdk-based-observability-which-architecture-fits-your-stack">Proxy-Based vs SDK-Based Observability: Which Architecture Fits Your Stack</h2>
<p>The architectural choice between proxy-based and SDK-based LLM observability is the most consequential decision when migrating from Helicone — and it is one Helicone&rsquo;s design never forced you to make. <strong>Helicone&rsquo;s proxy model adds latency to every LLM call</strong> because every request routes through Helicone&rsquo;s servers before reaching OpenAI (or another provider) and back. That round-trip is invisible at low request volumes but measurable in user-facing latency at scale. SDK-based tools like Langfuse and LangSmith instrument your application code directly and ship trace data asynchronously to their backends — the LLM call itself is never blocked, which means zero added latency on the hot path. The tradeoff is that SDK-based tools require code changes rather than a single URL swap: you install an SDK, add decorators or callbacks, and configure credentials. For latency-sensitive production applications where every millisecond of LLM response time affects user experience, SDK-based observability is the architecturally correct choice. Proxy-based tools remain valid for prototypes, internal tools, and rapid experimentation where zero-code integration is worth more than the latency cost. Portkey specifically bridges this tradeoff: it keeps the proxy model but adds gateway-level features (failover routing, semantic caching, load balancing across 200+ providers) that justify the network hop in a way that Helicone&rsquo;s pure-observability proxy never did. Teams comfortable with Helicone&rsquo;s architecture will find Portkey the most familiar migration target; teams optimizing for production performance should move to SDK-based instrumentation.</p>
<h2 id="self-hosted-options-langfuse-oss-stockyard-and-arize-phoenix">Self-Hosted Options: Langfuse OSS, Stockyard, and Arize Phoenix</h2>
<p>Self-hosted LLM observability has become a priority for teams with data residency requirements, regulated industries with compliance constraints, or organizations that simply will not send LLM trace data — which may contain sensitive prompt content — to third-party cloud services. <strong>Langfuse OSS</strong> is the production-ready leader: it ships as a Docker Compose configuration (Kubernetes manifests available for larger deployments), includes the full feature set of traces, evaluations, prompt management, and cost tracking, and is MIT-licensed with no feature gating between OSS and cloud versions. At 1M traces per month, self-hosted Langfuse costs approximately <strong>$150/month in infrastructure</strong> — compared to $919/month for Langfuse Cloud and $2,500+ for LangSmith at equivalent scale, the cost advantage compounds quickly. <strong>Stockyard</strong> targets the opposite end of the complexity spectrum: a single ~25MB Go binary with an embedded SQLite database and no external dependencies whatsoever. No Docker, no Postgres, no Redis, no cloud component — you download one file and run it. This makes Stockyard the right tool for solo developers, small teams, and air-gapped environments where operating a full Docker stack is unnecessary overhead. The tradeoff is capability: Stockyard does not include evaluations, prompt management, or advanced analytics. <strong>Arize Phoenix</strong> fills the niche for organizations running both traditional ML models and LLM applications — it accepts OpenTelemetry traces from both contexts, making it a single pane of glass for teams that already monitor ML workloads and want to extend that infrastructure to cover LLM calls. All three tools are MIT or Apache 2.0 licensed, providing full IP clarity and no vendor lock-in risk.</p>
<h2 id="migration-guide-how-to-move-from-helicone-without-breaking-production">Migration Guide: How to Move from Helicone Without Breaking Production</h2>
<p>Migrating from Helicone without a production outage requires running your new platform in parallel before cutting over — a process that typically takes one to two weeks for a single-model application. <strong>For Langfuse</strong> (the most common migration destination): install the SDK (<code>pip install langfuse</code> or <code>npm install langfuse</code>), replace Helicone proxy configuration with Langfuse&rsquo;s <code>observe()</code> decorator for OpenAI calls or the <code>LangfuseCallbackHandler</code> for LangChain, and set <code>LANGFUSE_PUBLIC_KEY</code> and <code>LANGFUSE_SECRET_KEY</code> in your environment. Helicone&rsquo;s custom properties map directly to Langfuse metadata fields, so your existing trace enrichment carries over. <strong>For LangSmith</strong>: if you are on LangChain, the migration is three environment variables — <code>LANGCHAIN_TRACING_V2=true</code>, <code>LANGCHAIN_ENDPOINT</code>, and <code>LANGCHAIN_API_KEY</code>. No code changes required for LangChain users. <strong>For Portkey</strong> (the closest proxy-to-proxy migration): swap the base URL from Helicone&rsquo;s endpoint to Portkey&rsquo;s gateway URL, replace the <code>Helicone-Auth</code> header with <code>x-portkey-api-key</code>, and optionally add <code>x-portkey-provider</code> to enable routing features. The recommended rollout process for all alternatives follows the same pattern: stand up the target platform in parallel with Helicone running; route 10% of traffic to the new platform and validate for 48 hours; confirm trace completeness, latency impact, and cost data accuracy; ramp to 100% over one week; keep Helicone in passive-receive mode for 30 days before decommissioning. Teams using Helicone&rsquo;s custom metadata headers should audit which header fields feed active dashboards before migration — not all alternatives map these identically, and gaps in metadata coverage will silently break cost attribution reports.</p>
<h2 id="which-helicone-alternative-should-you-choose-decision-framework">Which Helicone Alternative Should You Choose? Decision Framework</h2>
<p>Three variables determine the right Helicone alternative for your team: your primary observability use case (raw logging vs evaluation vs multi-provider reliability), your existing toolchain, and your scale and data residency requirements. <strong>If your team runs LangChain or LangGraph</strong>: choose LangSmith. Native integration eliminates instrumentation overhead, and chain-level trace visualization is a capability no other tool matches for LangChain-specific agent architectures. The higher cost at scale ($2,500+/month at 1M traces) is justified if LangChain is core infrastructure. <strong>If you need self-hosting or have data residency constraints</strong>: choose Langfuse OSS. It is the most production-ready self-hosted option with the broadest feature set, MIT-licensed, and significantly cheaper than any cloud alternative at scale — roughly $150/month in infrastructure at 1M traces. <strong>If you route traffic through multiple LLM providers</strong> (OpenAI plus Anthropic plus Gemini plus fine-tuned endpoints): choose Portkey. The 200+ provider support, semantic caching, and automatic failover routing make it a production gateway that does more than just observe your traffic — it actively improves reliability. <strong>If evaluations and CI/CD integration are your primary need</strong>: choose Braintrust. The 1M free spans per month, unlimited users on all tiers, and structured eval framework make it the strongest choice for teams treating LLM quality as a continuous testing problem. <strong>If you are a solo developer or small team in an air-gapped environment</strong>: choose Stockyard. A single binary, embedded database, and zero cloud dependencies eliminate operational overhead entirely. <strong>For teams with no strong existing toolchain preference</strong>: Langfuse is the default recommendation — open-source, actively maintained with no acquisition risk, SDK-based for zero-latency overhead, aggressively priced, and the closest feature match to what Helicone offered. The 40,000+ active builders and MIT license mean the project has both community momentum and full IP portability if you later want to self-host.</p>
<hr>
<h2 id="faq">FAQ</h2>
<p>Helicone&rsquo;s acquisition by Mintlify on March 3, 2026 triggered one of the most active migration discussions in the LLM observability community that quarter, with developers across GitHub, Reddit, and Hacker News evaluating alternatives under real time pressure. The decision criteria differ meaningfully by team: proxy-based tools like Helicone add minimal code changes but introduce a latency hop, while SDK-based tools like Langfuse require more instrumentation work but eliminate that latency. Pricing at scale is the other forcing function — LangSmith&rsquo;s $2,500+/month at 1M traces versus Langfuse Cloud at $919 versus self-hosted Langfuse at ~$150 creates a cost difference that compounds fast as AI usage grows. The questions below address the most common decision points: whether Helicone is truly shutting down, which tool to choose based on team profile, what migration effort actually looks like, and how to avoid repeating the vendor risk that the Helicone situation exposed. Each answer reflects the state of these tools as of May 2026.</p>
<h3 id="is-helicone-shutting-down-after-the-mintlify-acquisition">Is Helicone shutting down after the Mintlify acquisition?</h3>
<p>Helicone is not shutting down immediately — it entered maintenance mode on March 3, 2026, when the founding team joined Mintlify in San Francisco. The platform continues to operate: API endpoints are live, the dashboard functions, and existing integrations process traces normally. What stops is feature development. Security patches, bug fixes, and new model identifier support will continue, but no new observability features, evaluation frameworks, or provider integrations will ship. Teams should plan migration within 6 months of the announcement to avoid being caught by a future formal deprecation.</p>
<h3 id="what-is-the-best-free-alternative-to-helicone">What is the best free alternative to Helicone?</h3>
<p>Braintrust offers the most generous cloud free tier: 1 million spans per month with unlimited users and no per-seat charges. Langfuse&rsquo;s cloud free tier covers 50,000 units per month, which is lower — but the self-hosted version (MIT-licensed) is completely free beyond your own infrastructure costs, making it effectively unlimited for teams willing to run their own deployment. For solo developers and small teams, Stockyard is also free in the self-hosted sense: a single binary with no cloud subscription required.</p>
<h3 id="does-migrating-from-helicone-to-langfuse-require-changing-my-application-code">Does migrating from Helicone to Langfuse require changing my application code?</h3>
<p>Yes — unlike Helicone&rsquo;s proxy model where migration is a URL swap and a header change, Langfuse uses SDK-based instrumentation that requires adding the Langfuse SDK to your application. For LangChain users, the change is adding a <code>LangfuseCallbackHandler</code> to your chain invocations. For direct OpenAI users, you wrap calls with Langfuse&rsquo;s <code>observe()</code> decorator. The migration typically takes one to four hours for a single-model application and a full day for complex multi-step agent pipelines. The latency benefit — zero added overhead vs Helicone&rsquo;s proxy round-trip — is the payoff for that code investment.</p>
<h3 id="is-langfuse-safe-from-acquisition-risk-given-what-happened-to-helicone">Is Langfuse safe from acquisition risk given what happened to Helicone?</h3>
<p>Langfuse is MIT-licensed and fully self-hostable with no cloud dependency. The self-hosted deployment gives you complete control over your data and zero vendor lock-in — if Langfuse Cloud were ever acquired or deprecated, you could continue running your existing self-hosted instance indefinitely with no service interruption. The MIT license also means you can fork the codebase if needed. For teams specifically concerned about acquisition risk after the Helicone situation, self-hosted Langfuse eliminates that risk category entirely, at a cost of roughly $150/month in infrastructure at 1M traces per month.</p>
<h3 id="how-does-portkey-differ-from-helicone-as-a-proxy-based-alternative">How does Portkey differ from Helicone as a proxy-based alternative?</h3>
<p>Both Helicone and Portkey route your LLM calls through their infrastructure, but Portkey adds production-grade gateway features that Helicone never offered. Portkey supports 200+ LLM providers with unified routing, which means you can send traffic to OpenAI, Anthropic, Google, and fine-tuned model endpoints through a single integration. It adds automatic failover when a provider returns errors, load balancing across multiple model endpoints, and semantic caching that deduplicates similar prompts before they hit the API — reducing both latency and cost on repeated or near-identical queries. Helicone&rsquo;s proxy provided observability logging with a network hop; Portkey&rsquo;s proxy provides observability logging plus active reliability and cost optimization features that make the network hop worthwhile for multi-provider production deployments.</p>
]]></content:encoded></item></channel></rss>