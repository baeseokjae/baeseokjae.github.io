<?xml version="1.0" encoding="utf-8" standalone="yes"?><rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom" xmlns:content="http://purl.org/rss/1.0/modules/content/"><channel><title>Plugins on RockB</title><link>https://baeseokjae.github.io/tags/plugins/</link><description>Recent content in Plugins on RockB</description><image><title>RockB</title><url>https://baeseokjae.github.io/images/og-default.png</url><link>https://baeseokjae.github.io/images/og-default.png</link></image><generator>Hugo</generator><language>en-us</language><lastBuildDate>Tue, 30 Jun 2026 00:21:33 +0000</lastBuildDate><atom:link href="https://baeseokjae.github.io/tags/plugins/index.xml" rel="self" type="application/rss+xml"/><item><title>Cursor 3.9 Customize Page Guide 2026: Unified Plugin, Skill, and MCP Management</title><link>https://baeseokjae.github.io/posts/cursor-3-9-customize-page-guide-2026/</link><pubDate>Tue, 30 Jun 2026 00:21:33 +0000</pubDate><guid>https://baeseokjae.github.io/posts/cursor-3-9-customize-page-guide-2026/</guid><description>Complete guide to Cursor 3.9&amp;#39;s Customize page for managing plugins, MCP servers, skills, subagents, rules, commands, and hooks in one unified UI.</description><content:encoded><![CDATA[<p>The Cursor 3.9 Customize page, shipped June 22, 2026, replaces scattered JSON config files and settings panels with one unified UI for managing plugins, MCP servers, skills, subagents, rules, commands, and hooks. Instead of editing <code>mcp.json</code> by hand or hunting through tabs for rule files, you now open a single sidebar panel and manage every extension category from one place. This guide walks through every feature — scope levels, the plugin marketplace, team leaderboards, MCP server management without JSON, and the new plugin format — so you can configure Cursor for yourself or your entire team in minutes.</p>
<h2 id="what-is-the-cursor-customize-page-cursor-39">What Is the Cursor Customize Page (Cursor 3.9)?</h2>
<p>The Customize page is a unified management interface in Cursor 3.9 — released June 22, 2026 — that centralizes every extension and configuration point into a single UI accessed from the Agents Window sidebar. Before 3.9, managing rules meant digging through <code>.cursor/rules</code> folders, setting up MCP servers required editing raw <code>mcp.json</code> and remembering the exact transport syntax, skills lived in a separate settings panel, and hooks had no visible management surface at all. The Customize page collapses all of that into seven chip-style tabs: Plugins, MCPs, Skills, Subagents, Rules, Commands, and Hooks. As of June 2026, Cursor reports 7.5 million monthly active developers, and the Customize page is its most significant quality-of-life improvement for those users. The core takeaway: one tab replaces six disparate workflows, and what previously required a terminal and a documentation search now takes a few clicks.</p>
<h2 id="how-to-open-and-navigate-the-customize-page">How to Open and Navigate the Customize Page</h2>
<p>Open the Customize page by clicking the gear icon in the Agents Window — the same panel where you interact with Cursor&rsquo;s AI agent. The page opens as a full-width sub-panel with a row of chip-style category tabs across the top. Each tab reveals a scrollable list of installed items for that category, plus buttons to add, configure, or remove entries. A search bar at the top filters across all categories, and the header displays the active scope (more on that below). The layout is deliberately flat: no nested menus, no modal chains. Click a tab, see what&rsquo;s installed, click + to add something new. The design mirrors what team platform administrators have been asking for since Cursor introduced custom MCPs in 2024.</p>
<h3 id="scope-levels-user-vs-team-vs-workspace">Scope Levels: User vs Team vs Workspace</h3>
<p>The Customize page supports three scope levels. <strong>User</strong> applies to your personal configuration across all projects — your preferred MCP servers, your personal skills, your rules. <strong>Team</strong> shares configuration across everyone in your Cursor organization, letting a platform team install plugins and MCPs once for all members. <strong>Workspace</strong> scopes to a single project, useful when different repos need different rule sets or MCP connections. A scope selector sits in the header bar so you can switch between views without losing your place. The same plugin or MCP can exist at all three scopes simultaneously; Cursor merges them at runtime with workspace overriding team overriding user.</p>
<h3 id="the-seven-component-tabs-plugins-mcps-skills-subagents-rules-commands-hooks">The Seven Component Tabs (Plugins, MCPs, Skills, Subagents, Rules, Commands, Hooks)</h3>
<p>Each tab manages a distinct type of Cursor extension. <strong>Plugins</strong> are bundles that combine multiple component types — a plugin can ship skills, MCP servers, subagents, rules, commands, AND hooks in a single installable unit. <strong>MCPs</strong> lists all connected MCP servers with protocol type, transport, status indicator, and auth state. <strong>Skills</strong> shows installed custom skills (reusable prompt templates). <strong>Subagents</strong> manages agent definitions you create for specialized tasks. <strong>Rules</strong> lists all active <code>.cursor/rules</code> with a preview toggle. <strong>Commands</strong> shows custom slash commands. <strong>Hooks</strong> lists lifecycle hooks (pre-save, post-agent, etc.). This flat organization means you never need to hunt across five settings screens to find what you installed last week.</p>
<h2 id="managing-plugins-from-the-customize-page">Managing Plugins from the Customize Page</h2>
<p>A plugin is a portable, versioned bundle that packages multiple Cursor components — skills, MCP servers, rules, commands, hooks, and subagents — into a single installable unit with a <code>plugin.json</code> manifest. Before Cursor 3.9, setting up a development environment meant manually copying rule files, pasting MCP configs, and training each teammate on the same setup steps individually. A Stripe plugin, for example, bundles the Stripe MCP server, a skill for writing payment-intent code, and a rule that flags hardcoded API keys — all installed with one click. The Plugins tab shows each installed plugin with its name, version, source scope, and a live status indicator for bundled MCP servers. From this view you can uninstall, update, or disable individual components without removing the entire plugin. This is a fundamental shift from the pre-3.9 world where each component had to be installed and maintained separately.</p>
<h3 id="installing-plugins-from-the-marketplace">Installing Plugins from the Marketplace</h3>
<p>Click the &ldquo;Browse Marketplace&rdquo; button at the top of the Plugins tab to open the marketplace view. The marketplace shows featured plugins, recently updated plugins, and a search bar. Launch partners include Linear, Figma, Stripe, AWS, Cloudflare, Vercel, Databricks, Snowflake, Amplitude, and Hex — each offering plugins that bundle relevant MCP servers, skills, and rules for their platform. Click &ldquo;Install&rdquo; on any plugin, confirm the scope, and Cursor downloads and activates it immediately. The installed plugin appears in your Plugins tab with all its sub-components populated across their respective tabs. The marketplace supports version pinning so teams don&rsquo;t get surprised by breaking changes on auto-update.</p>
<h3 id="creating-a-custom-plugin-with-pluginjson">Creating a Custom Plugin with plugin.json</h3>
<p>Create a custom plugin by dropping a <code>.cursor-plugin/plugin.json</code> manifest into your repo. The manifest requires only a <code>name</code> field; all component paths are optional with auto-discovery. A minimal example:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-json" data-lang="json"><span style="display:flex;"><span>{
</span></span><span style="display:flex;"><span>  <span style="color:#f92672">&#34;name&#34;</span>: <span style="color:#e6db74">&#34;my-rules&#34;</span>,
</span></span><span style="display:flex;"><span>  <span style="color:#f92672">&#34;description&#34;</span>: <span style="color:#e6db74">&#34;Team coding standards&#34;</span>,
</span></span><span style="display:flex;"><span>  <span style="color:#f92672">&#34;rules&#34;</span>: [<span style="color:#e6db74">&#34;.cursor/rules&#34;</span>],
</span></span><span style="display:flex;"><span>  <span style="color:#f92672">&#34;mcpServers&#34;</span>: {
</span></span><span style="display:flex;"><span>    <span style="color:#f92672">&#34;my-api&#34;</span>: {
</span></span><span style="display:flex;"><span>      <span style="color:#f92672">&#34;type&#34;</span>: <span style="color:#e6db74">&#34;streamable-http&#34;</span>,
</span></span><span style="display:flex;"><span>      <span style="color:#f92672">&#34;url&#34;</span>: <span style="color:#e6db74">&#34;https://api.example.com/mcp&#34;</span>
</span></span><span style="display:flex;"><span>    }
</span></span><span style="display:flex;"><span>  }
</span></span><span style="display:flex;"><span>}
</span></span></code></pre></div><p>Place corresponding files in <code>skills/</code>, <code>rules/</code>, <code>commands/</code>, and <code>hooks/</code> directories under <code>.cursor-plugin/</code>, and Cursor discovers them automatically. The plugin.json format mirrors how Claude Desktop configs specify MCP servers, so you can often port configurations between tools with minimal changes.</p>
<h3 id="plugin-canvases--hex-and-atlassian-examples">Plugin Canvases — Hex and Atlassian Examples</h3>
<p>Plugin canvases go beyond static configuration into interactive shared dashboards. The first two canvases launch with 3.9: <strong>Hex Canvas</strong> embeds a live data exploration pane inside Cursor, letting your agent query databases and visualize results without leaving the editor; <strong>Atlassian Canvas</strong> provides a real-time Jira and Confluence surface where the agent can create tickets, update sprint status, and fetch documentation context inline. Canvases are themselves plugins, installed from the marketplace and managed in the Plugins tab. They represent a new category of Cursor extension — not just a config bundle but a persistent workspace element your agent can interact with.</p>
<h2 id="adding-and-managing-mcp-servers-without-json">Adding and Managing MCP Servers Without JSON</h2>
<p>The MCPs tab eliminates the last major reason to touch a JSON config file by providing a form-based GUI for every MCP management operation. Before 3.9, adding an MCP server required editing <code>.cursor/mcp.json</code> with exact transport syntax — one wrong comma or bracket and the server silently failed, forcing a restart to test each change. A common scenario: adding the <code>@modelcontextprotocol/server-filesystem</code> package involved looking up the correct command string, pasting it into JSON, restarting Cursor, and checking the logs if it didn&rsquo;t work. The MCPs tab replaces that entire cycle with a dialog where you paste the command or URL, click a test button, and see live status. Every connected server appears in a list with its protocol, transport type (stdio, streamable-http, or SSE-deprecated), connection status, and authentication state. You can add, edit, remove, or toggle servers on and off — all without opening a single file.</p>
<h3 id="using-the-gui-to-add-an-mcp-server">Using the GUI to Add an MCP Server</h3>
<p>Click the + button in the MCPs tab and a dialog opens. For stdio (local subprocess), paste the command and arguments — for example, <code>npx -y @modelcontextprotocol/server-filesystem</code>. For streamable-http (remote), enter the URL and any headers. The form validates syntax immediately and shows a test button that runs a tools/list call to confirm the server is reachable. Once saved, the server activates in seconds with no restart required. The GUI also surfaces error logs inline: if a server fails to start, you see the stderr output right in the tab, saving the old cycle of checking terminal output or log files.</p>
<h3 id="adding-custom-internal-mcp-servers">Adding Custom Internal MCP Servers</h3>
<p>Internal API servers and proprietary tooling use the same interface as public MCP servers. Enter the URL, add authentication headers (API keys, bearer tokens), and save. The Customize page stores credentials securely. For teams, publish the custom MCP server definition as part of a plugin or add it directly to the team scope so every member gets it automatically. The workflow for internal tooling: build your MCP server, publish it to a GitLab or Azure DevOps repo, import that repo as a team marketplace source, and add a custom MCP pointing at your internal URL. Distribute via Required mode (see below) and every team member gets access with zero manual configuration.</p>
<h3 id="enabling-auto-run-and-permission-policies">Enabling Auto-Run and Permission Policies</h3>
<p>Each MCP server in the tab has a toggle for auto-run behavior and a permission policy selector. Auto-run lets the agent invoke MCP tools without asking for confirmation — useful for low-risk operations like file reads or search queries. For destructive operations (database writes, file deletion), set the permission policy to &ldquo;ask&rdquo; or &ldquo;deny.&rdquo; These settings mirror the auto-run and permission controls Cursor introduced for regular tools, now applied per MCP server. You can also set a permission timeout: if the agent requests access, the permission grant expires after a configurable interval.</p>
<h2 id="using-the-team-leaderboard-for-discovery">Using the Team Leaderboard for Discovery</h2>
<p>The team leaderboard is a ranked, real-time view in the Customize page that shows every plugin, skill, and MCP server by adoption across your organization, replacing ad-hoc Slack questions and shared Google Docs. Instead of messaging &ldquo;hey, does anyone have the AWS MCP setup working?&rdquo;, you glance at the leaderboard and see it&rsquo;s installed by 47 of 82 team members with a 4.2-star rating. The leaderboard displays installation count, last-updated timestamp, and Required or Optional status for each item. As of June 2026, Cursor teams using the leaderboard report 3x faster onboarding for new engineers because discovery happens in the tool rather than through documentation. The leaderboard updates in near-real-time as teammates add or remove items, so the ranking always reflects actual organizational usage patterns.</p>
<h3 id="how-the-leaderboard-ranks-plugins-skills-and-mcps">How the Leaderboard Ranks Plugins, Skills, and MCPs</h3>
<p>Ranking is based on a composite score: installation count (weighted highest), recent activity (installs in the last 7 days get a boost), and team ratings (thumbs up/down). Filters let you view by category, scope, or recency. The leaderboard also surfaces &ldquo;trending&rdquo; items — plugins or MCPs that saw a sudden spike in adoption in the last 48 hours. For platform teams, this is a discovery and governance tool in one: see what&rsquo;s actually useful to your engineers, then promote popular items to Required status.</p>
<h3 id="one-click-install-from-the-leaderboard">One-Click Install from the Leaderboard</h3>
<p>Every row in the leaderboard has an Install button. Click it, confirm the scope, and the plugin or MCP is installed. One-click install from the leaderboard drops the friction to near zero — no searching the marketplace, no reading documentation, no copy-pasting config snippets. Combined with the leaderboard&rsquo;s social proof (47 teammates already use this), it drives adoption through visibility and convenience rather than mandates.</p>
<h2 id="setting-up-a-team-marketplace">Setting Up a Team Marketplace</h2>
<p>A team marketplace is an organization-scoped plugin catalog in the Customize page that indexes plugins from your version control repositories and makes them available to every team member through a familiar Browse Marketplace view. Unlike the public marketplace — which features plugins from Linear, Figma, Stripe, AWS, and other launch partners — the team marketplace surfaces only the plugins your organization publishes: internal API MCP servers, proprietary rule sets for your codebase, custom skills tailored to your stack, and hooks that enforce deployment policies. Before 3.9, distributing an internal MCP server meant sharing a code snippet and hoping teammates configured it correctly. With the team marketplace, you add one or more repository connections — GitHub, GitLab, Bitbucket, or Azure DevOps — and Cursor indexes every <code>.cursor-plugin/plugin.json</code> it finds. Setting it up requires a Cursor Teams or Enterprise plan, and the configuration takes under five minutes.</p>
<h3 id="importing-plugin-repos-from-github-gitlab-bitbucket-or-azure-devops">Importing Plugin Repos from GitHub, GitLab, BitBucket, or Azure DevOps</h3>
<p>Before 3.9, Cursor&rsquo;s marketplace integration worked with GitHub repositories only. The Customize page adds GitLab, Bitbucket, and Azure DevOps support — a direct upgrade path for enterprise teams locked out of GitHub-only ecosystems. Navigate to the team marketplace settings, authenticate with your provider, and select the repos that contain <code>.cursor-plugin/plugin.json</code> files. Cursor scans each repo on import, indexes all plugins found, and presents them in the Browse Marketplace view with a &ldquo;Team&rdquo; badge. Each repo can host multiple plugins as long as each has its own <code>.cursor-plugin/</code> directory.</p>
<h3 id="auto-refresh-for-latest-versions">Auto-Refresh for Latest Versions</h3>
<p>Once a repo is connected, Cursor polls it for changes. Push an update to your plugin&rsquo;s <code>plugin.json</code> or component files, and within minutes the team marketplace reflects the new version. Team members are notified of available updates through a badge on the Plugins tab. They can update with a single click. Auto-refresh means no manual re-import, no CI step to redeploy a plugin catalog — just push to the repo and the team picks up the changes on their next sync.</p>
<h3 id="required-vs-optional-plugin-distribution">Required vs Optional Plugin Distribution</h3>
<p>Every plugin in the team marketplace can be marked as Required or Optional. Required plugins auto-install for every team member and cannot be removed individually — the user sees them as read-only entries in their Customize page. Use Required for security rules, compliance standards, and mandatory MCP connections (like a SSO gateway or centralized logging server). Optional plugins appear in the marketplace and leaderboard but require explicit installation. Platform teams should reserve Required for non-negotiable tooling: if an MCP server handles authentication or a rule enforces code review policy, mark it Required.</p>
<h2 id="migrating-from-legacy-mcpjson-to-the-customize-page">Migrating from Legacy mcp.json to the Customize Page</h2>
<p>The Customize page does NOT auto-migrate entries from existing <code>mcp.json</code> files — this is by design to avoid silently breaking workflows that depend on specific transport configurations. If you have a <code>.cursor/mcp.json</code> with MCP server definitions from a previous Cursor version, those servers continue to work as before; they simply won&rsquo;t appear in the Customize page UI until you re-add them through the MCPs tab. The migration path is straightforward: open the MCPs tab, click + for each server in your <code>mcp.json</code>, paste the command or URL and any arguments, and save. The GUI&rsquo;s built-in test button confirms the server is reachable before you finish. A tip for teams using the <code>@modelcontextprotocol/server-filesystem</code> server: its stdio command is <code>npx -y @modelcontextprotocol/server-filesystem /path/to/project</code> — the path argument must match exactly. Once added, the old <code>mcp.json</code> becomes redundant and you can delete it.</p>
<h2 id="best-practices-for-cursor-customization-at-scale">Best Practices for Cursor Customization at Scale</h2>
<p><strong>Standardize on plugins, not individual components.</strong> If your team needs a consistent set of rules, MCPs, and skills, bundle them into one plugin and mark it Required. That way a single marketplace entry replaces three separate instructions in your onboarding docs. <strong>Use workspace scope for project-specific config.</strong> Database schemas differ per repo; don&rsquo;t apply <code>SELECT</code>-only rules globally. <strong>Audit with the team leaderboard.</strong> A weekly glance at the leaderboard tells you which plugins are actually delivering value and which are collecting dust. <strong>Version your plugins.</strong> Push breaking changes to a new plugin name or use version-pinned marketplace entries so old agents don&rsquo;t break when you update a server protocol. <strong>Keep Required to a minimum.</strong> Every Required plugin is a constraint; only mandate what directly affects security or compliance.</p>
<h2 id="faq">FAQ</h2>
<p>This section answers the most common questions developers ask when adopting the Cursor 3.9 Customize page, based on patterns observed across engineering teams running Cursor at scale. The questions below cover scope management (user vs team vs workspace), interoperability with other AI coding tools like Claude Code and VS Code Copilot, migration strategies for teams transitioning from pre-3.9 configurations, the behavior of plugin deletion and component isolation, and per-repo MCP setups. As teams adopt the Customize page at scale — Cursor&rsquo;s 7.5 million monthly active developers as of June 2026 — these patterns emerge repeatedly in platform engineering discussions. Platform teams managing 50+ developers report that the three most common questions are migration paths, scope behavior, and whether existing rule files survive the upgrade. Most questions center on whether the new UI replaces existing files, how to handle the transition without breaking current workflows, and what happens when team members install or uninstall marketplace plugins.</p>
<h3 id="does-the-customize-page-replace-cursorrules-files">Does the Customize page replace <code>.cursor/rules</code> files?</h3>
<p>No. The Rules tab in the Customize page manages which rule sets are active, but the rule files themselves still live in your project&rsquo;s <code>.cursor/rules</code> directory or inside a plugin&rsquo;s rules folder. The tab provides visibility and toggling; it doesn&rsquo;t store rule content.</p>
<h3 id="can-i-use-mcp-servers-from-the-customize-page-with-claude-code">Can I use MCP servers from the Customize page with Claude Code?</h3>
<p>MCP server definitions are portable. A plugin.json that declares MCP servers uses the same shape as Claude Desktop&rsquo;s <code>claude_desktop_config.json</code>, so you can copy the MCP definition between tools with minimal edits. However, Cursor&rsquo;s Customize page is a UI for Cursor — Claude Code doesn&rsquo;t read it.</p>
<h3 id="what-happens-if-i-delete-a-plugin-i-created-from-the-marketplace">What happens if I delete a plugin I created from the marketplace?</h3>
<p>Deleting a marketplace plugin removes all its sub-components — MCPs, skills, rules, commands, and hooks — from your Cursor configuration. If you want to keep specific components, clone them to your user or workspace scope first, then delete the plugin.</p>
<h3 id="how-do-i-migrate-my-team-from-pre-39-configs-to-the-customize-page">How do I migrate my team from pre-3.9 configs to the Customize page?</h3>
<p>Create a plugin that bundles your existing rules, MCP servers, and skills. Publish it to your team marketplace as Required. Every team member who syncs gets the full configuration automatically. Then ask members to clean up their old <code>mcp.json</code> files at their convenience.</p>
<h3 id="does-the-customize-page-support-per-repo-mcp-configurations">Does the Customize page support per-repo MCP configurations?</h3>
<p>Yes. Use workspace scope to set MCP servers that activate only when that workspace is open. This is ideal for per-repo database connections or project-specific APIs.</p>
]]></content:encoded></item><item><title>OpenAI Codex Plugins Guide: 90+ Enterprise AI Workflow Integrations (2026)</title><link>https://baeseokjae.github.io/posts/openai-codex-plugins-guide-2026/</link><pubDate>Tue, 19 May 2026 00:04:41 +0000</pubDate><guid>https://baeseokjae.github.io/posts/openai-codex-plugins-guide-2026/</guid><description>Complete guide to OpenAI Codex&amp;#39;s 90+ plugins for enterprise teams — categories, governance, CLI setup, custom development, and competitor comparison.</description><content:encoded><![CDATA[<p>OpenAI Codex plugins are pre-built integrations that connect Codex&rsquo;s AI coding agent to external tools — from Slack and GitHub to Jira and CircleCI — letting developers trigger multi-step workflows across your entire software stack without switching contexts. As of April 2026, the marketplace offers 90+ plugins across seven categories, and enterprise teams at Cisco, Rakuten, and Ramp are using them to automate developer workflows that previously required custom tooling.</p>
<h2 id="what-are-openai-codex-plugins-the-2026-plugin-system-explained">What Are OpenAI Codex Plugins? (The 2026 Plugin System Explained)</h2>
<p>OpenAI Codex plugins are modular extensions that give the Codex AI agent access to external services, APIs, and tools beyond its default code-generation capabilities. Launched on March 26, 2026 with 20+ initial integrations, the plugin marketplace expanded to 90+ additional plugins in April 2026 alongside Codex&rsquo;s computer-use and in-app browser features. A plugin tells Codex <em>how</em> to interact with an external system — authenticating, reading data, writing back results — so the agent can operate on your behalf inside Slack, GitHub, Neon databases, or Figma without requiring custom API code. By April 21, 2026, Codex had reached 4 million+ weekly active developers (up from 3 million just two weeks prior), and ChatGPT Business and Enterprise Codex usage grew 6x since January 2026. Plugins are the engine driving that growth: they convert a standalone AI coding assistant into a cross-platform workflow orchestrator that operates where your teams already work.</p>
<p>The key distinction from simple API calls: Codex plugins are persistent connections with memory, governance controls, and admin policy enforcement. Enterprise IT admins can mandate specific plugins for all users, restrict others, and log all plugin-triggered actions to SIEM systems for compliance.</p>
<h2 id="plugin-architecture-skills-apps-and-mcp-servers">Plugin Architecture: Skills, Apps, and MCP Servers</h2>
<p>OpenAI Codex&rsquo;s plugin system has three distinct integration types, each designed for a different use case. Understanding which type to use determines how deeply Codex can act on your behalf, how much custom code you need to write, and what governance controls apply. Choosing the wrong type is the most common mistake enterprises make when first deploying Codex plugins — teams either under-invest with a shallow App integration when they need a full MCP server, or over-engineer a Skill for a straightforward service-to-service call.</p>
<p><strong>Skills</strong> are lightweight, function-level extensions that expose a specific action or capability to Codex. A Skill might wrap a REST endpoint (e.g., &ldquo;create a Linear ticket with these fields&rdquo;) or a local script (e.g., &ldquo;run the test suite on this branch&rdquo;). Skills are the fastest to author — you define a JSON schema describing the action, and Codex calls it like a function. Skills are stateless by design.</p>
<p><strong>Apps</strong> are deeper integrations with OAuth2 authentication and bidirectional data flow. An App integration connects Codex to a SaaS platform (Slack, Notion, Google Drive) and enables reading <em>and</em> writing: reading channel history to understand context, then posting a summary. Apps handle token refresh automatically and appear in the plugin marketplace as first-class connectors.</p>
<p><strong>MCP Servers</strong> (Model Context Protocol) are the most powerful integration type. An MCP server runs as a local or remote process that Codex communicates with over a standardized protocol. MCP servers enable arbitrary tool execution, long-running processes, database queries, and complex stateful workflows. For enterprise self-hosted tooling (internal CI systems, private Kubernetes clusters, custom data stores), MCP servers are the correct choice.</p>
<table>
	<thead>
			<tr>
					<th>Type</th>
					<th>Auth</th>
					<th>Stateful</th>
					<th>Custom Code</th>
					<th>Best For</th>
			</tr>
	</thead>
	<tbody>
			<tr>
					<td>Skill</td>
					<td>API Key / None</td>
					<td>No</td>
					<td>Minimal (JSON schema)</td>
					<td>Single-action wrappers</td>
			</tr>
			<tr>
					<td>App</td>
					<td>OAuth2</td>
					<td>Yes</td>
					<td>Low (marketplace connector)</td>
					<td>SaaS read/write integrations</td>
			</tr>
			<tr>
					<td>MCP Server</td>
					<td>Any</td>
					<td>Yes</td>
					<td>High (custom server)</td>
					<td>Internal tooling, complex workflows</td>
			</tr>
	</tbody>
</table>
<h2 id="the-complete-list-of-90-codex-plugins-by-category">The Complete List of 90+ Codex Plugins by Category</h2>
<p>The Codex plugin marketplace organizes 90+ integrations into seven primary categories: communication and collaboration, project management, CI/CD and DevOps, databases and data infrastructure, code review and security, design and documentation, and Microsoft enterprise productivity. OpenAI released 20 initial plugins on March 26, 2026, then expanded to 90+ additional plugins in April 2026 alongside the computer-use and in-app browser features — making Codex one of the largest enterprise AI plugin ecosystems available. Each category reflects a phase of the software development lifecycle where Codex can reduce context-switching: from ideation (project management, design) through building (databases, CI/CD) to shipping and monitoring (security, DevOps). Below is a representative breakdown with key plugins in each area. The full catalog is accessible via <code>codex plugin list --all</code> in the CLI or at platform.openai.com/codex/plugins. Plugin availability varies by region and subscription tier — Business and Enterprise plans have access to all 90+ plugins, while Plus users get the core marketplace subset.</p>
<h3 id="communication--collaboration-plugins-slack-gmail-notion-google-drive">Communication &amp; Collaboration Plugins (Slack, Gmail, Notion, Google Drive)</h3>
<p>Communication plugins give Codex read and write access to the platforms where teams coordinate work, enabling it to post status updates, retrieve meeting notes, summarize threads, and file information automatically. Slack is the most-used Codex plugin by volume: developers configure Codex to post PR review summaries, deployment alerts, and bug triage notes directly to team channels — eliminating the manual copy-paste cycle that burns 20-30 minutes per incident. Gmail integration lets Codex draft replies to client-reported bugs, extract action items from email threads, and file issues without leaving the terminal. Notion integration covers reading and writing project wikis, sprint docs, and runbook pages. Google Drive access enables Codex to retrieve architecture docs, ingest PRDs as context, and write generated documentation back to shared drives. Microsoft 365 integration (Teams, Word, SharePoint) launched in April 2026 as part of the enterprise productivity cluster, making Codex viable in Microsoft-first environments where Google Workspace isn&rsquo;t used.</p>
<p><strong>Key plugins:</strong> Slack, Gmail, Notion, Google Drive, Microsoft Teams, SharePoint, Outlook, Zoom, Box</p>
<h3 id="project-management-plugins-atlassian-rovojira-linear-gitlab-issues">Project Management Plugins (Atlassian Rovo/JIRA, Linear, GitLab Issues)</h3>
<p>Project management plugins connect Codex to ticketing and roadmap systems, letting the agent create, update, and query issues programmatically. Atlassian Rovo — launched as a dedicated Codex plugin in April 2026 — provides deep JIRA integration with semantic search across your backlog, enabling Codex to find duplicate tickets, link related issues, and estimate story points based on historical velocity. Linear&rsquo;s Codex plugin is popular with engineering-first teams: Codex can create Linear issues from TODO comments in code, auto-assign them based on code ownership, and close them when the corresponding PR merges. GitLab Issues integration covers the full GitLab workflow for teams that don&rsquo;t use GitHub, including milestones, labels, and merge request linking. For teams using multiple trackers simultaneously, Codex can sync state across systems — creating a Jira ticket and a corresponding Linear issue from a single natural-language command.</p>
<p><strong>Key plugins:</strong> Atlassian Rovo (JIRA), Linear, GitLab Issues, GitHub Issues, Asana, Monday.com, Shortcut (formerly Clubhouse)</p>
<h3 id="cicd--devops-plugins-circleci-github-actions-render-cloudflare">CI/CD &amp; DevOps Plugins (CircleCI, GitHub Actions, Render, Cloudflare)</h3>
<p>CI/CD plugins give Codex direct access to pipeline systems, enabling it to trigger builds, read failure logs, diagnose test failures, and propose fixes — all from within a single agentic session. CircleCI&rsquo;s Codex plugin (launched April 2026) lets the agent inspect failed pipeline steps, retrieve logs, and generate patch commits that re-run the pipeline to verify the fix. This closes the inner loop that typically requires switching between terminal, CI dashboard, and code editor. GitHub Actions integration enables Codex to read workflow files, suggest optimizations for cache hit rates, and create new workflow files from natural language descriptions. Render integration supports deployment management — Codex can query deployment status, roll back a release, and set environment variables. Cloudflare integration covers Workers, Pages, and R2 — Codex can deploy edge functions and manage configurations via Workers API. For Kubernetes-heavy environments, an MCP server pattern is recommended over the App-layer plugins to retain full kubectl access.</p>
<p><strong>Key plugins:</strong> CircleCI, GitHub Actions, Render, Cloudflare Workers, Vercel, Railway, Fly.io, Jenkins (via MCP), ArgoCD (via MCP)</p>
<h3 id="database--data-plugins-neon-by-databricks-hugging-face">Database &amp; Data Plugins (Neon by Databricks, Hugging Face)</h3>
<p>Database plugins give Codex the ability to query, migrate, and manage data stores within a governed session. Neon by Databricks — one of the April 2026 additions — provides serverless Postgres access with branch-based development: Codex can spin up a database branch, run migrations against it, validate results, and delete the branch, all without touching production. Hugging Face integration enables Codex to pull models, datasets, and inference endpoints into coding sessions — useful for ML engineers building fine-tuning pipelines or evaluation harnesses. PlanetScale, Supabase, and MongoDB Atlas integrations follow the same pattern: Codex gets read/write access with credentials managed by the plugin&rsquo;s OAuth layer, not stored in plaintext in prompts.</p>
<p><strong>Key plugins:</strong> Neon by Databricks, Hugging Face, Supabase, PlanetScale, MongoDB Atlas, Redis Cloud, Snowflake (via MCP), BigQuery (via MCP)</p>
<h3 id="code-review--security-plugins-coderabbit-sentry-datadog">Code Review &amp; Security Plugins (CodeRabbit, Sentry, Datadog)</h3>
<p>Code review and security plugins extend Codex into the quality and observability layer of the software lifecycle. CodeRabbit&rsquo;s Codex plugin (April 2026) integrates AI-powered code review directly: Codex can request a CodeRabbit review pass on its own output before committing, creating a self-review loop that catches issues before they reach human reviewers. Sentry integration gives Codex access to production error events — the agent can correlate a Sentry error fingerprint with the relevant code path and propose a targeted fix rather than guessing from stack traces. Datadog integration covers dashboards, monitors, and logs: Codex can read metric anomalies, correlate them with recent deploys, and generate runbook entries. Snyk and Semgrep integrations add SAST scanning to the loop — Codex can run a security scan against a proposed change and iterate until the finding is resolved.</p>
<p><strong>Key plugins:</strong> CodeRabbit, Sentry, Datadog, Snyk, Semgrep, PagerDuty, New Relic, Grafana (via MCP)</p>
<h3 id="design--documentation-plugins-figma-box">Design &amp; Documentation Plugins (Figma, Box)</h3>
<p>Design plugins connect Codex to the visual and document layers of product development. Figma integration lets Codex read component specifications, extract design tokens, and generate corresponding React/CSS code from a Figma file URL — reducing the designer-to-developer handoff gap. Box integration covers enterprise document storage: Codex can retrieve architecture decision records (ADRs), compliance documents, and technical specifications stored in Box, using them as context for code generation without requiring the developer to manually attach files. Confluence and Notion serve similar roles for teams that prefer those platforms.</p>
<p><strong>Key plugins:</strong> Figma, Box, Confluence, Miro (via MCP), Adobe XD (via MCP)</p>
<h3 id="microsoft-suite--enterprise-productivity-plugins">Microsoft Suite &amp; Enterprise Productivity Plugins</h3>
<p>Microsoft ecosystem integrations launched in April 2026, making Codex viable for the substantial portion of enterprise customers that standardize on Microsoft 365. Teams integration enables posting to channels and retrieving conversation history. SharePoint access covers document libraries and wiki pages. Azure DevOps integration adds JIRA-equivalent project management for Microsoft shops, including boards, pipelines (Azure Pipelines), and artifact management. Power BI integration is in preview, allowing Codex to query data models and generate DAX expressions. For enterprises on Microsoft&rsquo;s Copilot ecosystem, OpenAI positions Codex plugins as a complement rather than a replacement — Codex handles code-centric tasks while Copilot handles document and presentation tasks.</p>
<p><strong>Key plugins:</strong> Microsoft Teams, SharePoint, Outlook, Azure DevOps, Azure Pipelines, OneDrive, Power BI (preview)</p>
<h2 id="enterprise-governance-managing-plugins-at-scale-with-json-policies">Enterprise Governance: Managing Plugins at Scale with JSON Policies</h2>
<p>Enterprise IT admins control Codex plugin availability through JSON policy files pushed via the OpenAI admin API, enabling department-wide rollout or restriction without touching individual user accounts. The policy system launched with three states: <code>INSTALLED_BY_DEFAULT</code> (plugin is active for all users in the org), <code>AVAILABLE</code> (users can install it themselves), and <code>NOT_AVAILABLE</code> (plugin is blocked). Cisco&rsquo;s IT team, for example, sets internal-only MCP server plugins to <code>INSTALLED_BY_DEFAULT</code> while restricting consumer SaaS plugins that haven&rsquo;t passed security review to <code>NOT_AVAILABLE</code> — ensuring engineers can&rsquo;t inadvertently send proprietary code to unapproved third-party services. Governance policies apply at the organization level or can be scoped to specific groups, enabling different policies for the infra team (full DevOps plugin access) versus the compliance team (read-only integrations). All plugin actions are logged with actor identity, timestamp, and payload hash, which can be streamed to SIEM systems like Splunk or Microsoft Sentinel via the audit log API. eDiscovery support means legal teams can retrieve all Codex-plugin interactions for a given user within a date range.</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-json" data-lang="json"><span style="display:flex;"><span>{
</span></span><span style="display:flex;"><span>  <span style="color:#f92672">&#34;plugins&#34;</span>: {
</span></span><span style="display:flex;"><span>    <span style="color:#f92672">&#34;slack&#34;</span>: <span style="color:#e6db74">&#34;INSTALLED_BY_DEFAULT&#34;</span>,
</span></span><span style="display:flex;"><span>    <span style="color:#f92672">&#34;github&#34;</span>: <span style="color:#e6db74">&#34;INSTALLED_BY_DEFAULT&#34;</span>,
</span></span><span style="display:flex;"><span>    <span style="color:#f92672">&#34;circleci&#34;</span>: <span style="color:#e6db74">&#34;AVAILABLE&#34;</span>,
</span></span><span style="display:flex;"><span>    <span style="color:#f92672">&#34;figma&#34;</span>: <span style="color:#e6db74">&#34;AVAILABLE&#34;</span>,
</span></span><span style="display:flex;"><span>    <span style="color:#f92672">&#34;consumer-app-x&#34;</span>: <span style="color:#e6db74">&#34;NOT_AVAILABLE&#34;</span>
</span></span><span style="display:flex;"><span>  }
</span></span><span style="display:flex;"><span>}
</span></span></code></pre></div><h2 id="how-to-install-and-configure-codex-plugins-cli-guide">How to Install and Configure Codex Plugins (CLI Guide)</h2>
<p>Installing Codex plugins takes under two minutes for marketplace plugins and requires only the Codex CLI and appropriate service credentials. The CLI plugin management system uses <code>codex plugin</code> subcommands that mirror familiar package manager patterns, making adoption straightforward for developer teams already comfortable with npm or pip workflows. Enterprise admins can pre-install plugins via the API before users ever run a CLI command, ensuring day-one availability for onboarding developers.</p>
<p><strong>Install a plugin from the marketplace:</strong></p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-bash" data-lang="bash"><span style="display:flex;"><span>codex plugin install slack
</span></span><span style="display:flex;"><span>codex plugin install github
</span></span><span style="display:flex;"><span>codex plugin install circleci
</span></span></code></pre></div><p><strong>Authenticate the plugin (OAuth2 flow opens in browser):</strong></p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-bash" data-lang="bash"><span style="display:flex;"><span>codex plugin auth slack
</span></span><span style="display:flex;"><span><span style="color:#75715e"># Opens browser → authorize → token stored in ~/.codex/credentials</span>
</span></span></code></pre></div><p><strong>List installed plugins and their status:</strong></p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-bash" data-lang="bash"><span style="display:flex;"><span>codex plugin list
</span></span><span style="display:flex;"><span><span style="color:#75715e"># NAME       TYPE    STATUS    VERSION</span>
</span></span><span style="display:flex;"><span><span style="color:#75715e"># slack      app     active    1.4.2</span>
</span></span><span style="display:flex;"><span><span style="color:#75715e"># github     app     active    2.1.0</span>
</span></span><span style="display:flex;"><span><span style="color:#75715e"># circleci   app     active    1.0.5</span>
</span></span></code></pre></div><p><strong>Add a custom MCP server:</strong></p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-bash" data-lang="bash"><span style="display:flex;"><span>codex plugin add-mcp --name internal-ci <span style="color:#ae81ff">\
</span></span></span><span style="display:flex;"><span>  --command <span style="color:#e6db74">&#34;node /path/to/mcp-server.js&#34;</span> <span style="color:#ae81ff">\
</span></span></span><span style="display:flex;"><span>  --env CI_TOKEN<span style="color:#f92672">=</span>$CI_TOKEN
</span></span></code></pre></div><p><strong>Use a plugin within a Codex session:</strong></p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-bash" data-lang="bash"><span style="display:flex;"><span>codex <span style="color:#e6db74">&#34;check CircleCI for the last failed build on main, find the failing test, and fix it&#34;</span>
</span></span><span style="display:flex;"><span><span style="color:#75715e"># Codex automatically invokes the circleci plugin to retrieve logs</span>
</span></span></code></pre></div><p><strong>Remove a plugin:</strong></p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-bash" data-lang="bash"><span style="display:flex;"><span>codex plugin remove figma
</span></span></code></pre></div><p>Plugin credentials are stored in an encrypted local keychain (macOS Keychain, Linux Secret Service, or Windows Credential Manager) — Codex never transmits raw credentials to OpenAI servers. Enterprise deployments can instead use a central credential vault with the <code>--credential-provider</code> flag pointing to a HashiCorp Vault endpoint.</p>
<h2 id="building-custom-codex-plugins-skills-apps-and-mcp-servers">Building Custom Codex Plugins: Skills, Apps, and MCP Servers</h2>
<p>Custom Codex plugins follow a well-defined spec that any developer with API familiarity can implement in an afternoon for a Skill, or a few days for a full MCP server. The fastest path is a Skill wrapping an internal REST API — define the JSON schema, register it, and Codex can invoke it within minutes. For teams with proprietary internal tooling not covered by the marketplace, custom plugins are the primary way to bring Codex into existing workflows without rewriting those tools.</p>
<p><strong>Building a Skill (simplest):</strong></p>
<p>Create a <code>skill.json</code> describing your action:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-json" data-lang="json"><span style="display:flex;"><span>{
</span></span><span style="display:flex;"><span>  <span style="color:#f92672">&#34;name&#34;</span>: <span style="color:#e6db74">&#34;deploy_staging&#34;</span>,
</span></span><span style="display:flex;"><span>  <span style="color:#f92672">&#34;description&#34;</span>: <span style="color:#e6db74">&#34;Deploy the current branch to the staging environment&#34;</span>,
</span></span><span style="display:flex;"><span>  <span style="color:#f92672">&#34;parameters&#34;</span>: {
</span></span><span style="display:flex;"><span>    <span style="color:#f92672">&#34;branch&#34;</span>: { <span style="color:#f92672">&#34;type&#34;</span>: <span style="color:#e6db74">&#34;string&#34;</span>, <span style="color:#f92672">&#34;description&#34;</span>: <span style="color:#e6db74">&#34;Branch name to deploy&#34;</span> },
</span></span><span style="display:flex;"><span>    <span style="color:#f92672">&#34;environment&#34;</span>: { <span style="color:#f92672">&#34;type&#34;</span>: <span style="color:#e6db74">&#34;string&#34;</span>, <span style="color:#f92672">&#34;enum&#34;</span>: [<span style="color:#e6db74">&#34;staging&#34;</span>, <span style="color:#e6db74">&#34;preview&#34;</span>] }
</span></span><span style="display:flex;"><span>  },
</span></span><span style="display:flex;"><span>  <span style="color:#f92672">&#34;endpoint&#34;</span>: <span style="color:#e6db74">&#34;https://internal-deploy.example.com/api/deploy&#34;</span>,
</span></span><span style="display:flex;"><span>  <span style="color:#f92672">&#34;auth&#34;</span>: { <span style="color:#f92672">&#34;type&#34;</span>: <span style="color:#e6db74">&#34;bearer&#34;</span>, <span style="color:#f92672">&#34;token_env&#34;</span>: <span style="color:#e6db74">&#34;DEPLOY_TOKEN&#34;</span> }
</span></span><span style="display:flex;"><span>}
</span></span></code></pre></div><p>Register the Skill:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-bash" data-lang="bash"><span style="display:flex;"><span>codex plugin register-skill ./skill.json
</span></span></code></pre></div><p><strong>Building an MCP Server (maximum flexibility):</strong></p>
<p>An MCP server implements the Model Context Protocol, exposing tools as named functions with typed inputs and outputs. The OpenAI-provided MCP SDK handles the protocol framing:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-python" data-lang="python"><span style="display:flex;"><span><span style="color:#f92672">from</span> openai_mcp <span style="color:#f92672">import</span> MCPServer, tool
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>server <span style="color:#f92672">=</span> MCPServer(name<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;internal-ci&#34;</span>)
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#a6e22e">@tool</span>
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">def</span> <span style="color:#a6e22e">get_pipeline_status</span>(pipeline_id: str) <span style="color:#f92672">-&gt;</span> dict:
</span></span><span style="display:flex;"><span>    <span style="color:#e6db74">&#34;&#34;&#34;Retrieve CI pipeline status from internal system&#34;&#34;&#34;</span>
</span></span><span style="display:flex;"><span>    <span style="color:#66d9ef">return</span> internal_ci_client<span style="color:#f92672">.</span>get_status(pipeline_id)
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#a6e22e">@tool</span>
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">def</span> <span style="color:#a6e22e">trigger_deploy</span>(branch: str, target: str) <span style="color:#f92672">-&gt;</span> dict:
</span></span><span style="display:flex;"><span>    <span style="color:#e6db74">&#34;&#34;&#34;Trigger a deployment to the specified target environment&#34;&#34;&#34;</span>
</span></span><span style="display:flex;"><span>    <span style="color:#66d9ef">return</span> internal_deploy_client<span style="color:#f92672">.</span>deploy(branch<span style="color:#f92672">=</span>branch, target<span style="color:#f92672">=</span>target)
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>server<span style="color:#f92672">.</span>run()  <span style="color:#75715e"># Listens on stdio by default</span>
</span></span></code></pre></div><p>Connect the MCP server to Codex:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-bash" data-lang="bash"><span style="display:flex;"><span>codex plugin add-mcp --name internal-ci --command <span style="color:#e6db74">&#34;python3 /opt/mcp/ci_server.py&#34;</span>
</span></span></code></pre></div><p>For enterprise publishing (making your custom plugin available to your whole org), package the plugin and submit via the admin API. OpenAI reviews plugins for the public marketplace but allows private org-scoped plugins without review.</p>
<h2 id="enterprise-use-cases-cisco-rakuten-ramp-and-virgin-atlantic">Enterprise Use Cases: Cisco, Rakuten, Ramp, and Virgin Atlantic</h2>
<p>Real enterprise deployments illustrate how Codex plugins move from demo to production value. These are not hypothetical workflows — they represent reported deployment patterns from OpenAI&rsquo;s enterprise partner announcements and public case study material.</p>
<p><strong>Cisco</strong> uses Codex with GitHub, Sentry, and internal MCP server plugins to power an autonomous bug-triage workflow: when Sentry fires an alert, Codex retrieves the error context, finds the relevant code via GitHub, proposes a fix, and files a PR — all without engineer intervention. Cisco engineering leads reported 40% reduction in time-to-patch for P3/P4 bugs in the first deployment quarter.</p>
<p><strong>Rakuten</strong> (Japanese e-commerce and fintech) integrated Codex with Jira, CircleCI, and Slack to close the sprint-ceremony gap: Codex attends planning by reading the Jira backlog, estimates complexity based on codebase analysis, and posts summaries to the Slack planning channel before engineers join. Teams reported 30-minute reduction per sprint planning session.</p>
<p><strong>Ramp</strong> (B2B fintech) uses Codex plugins to automate compliance check workflows: Codex reads new code changes, runs Semgrep SAST scans via plugin, checks against internal policy rules stored in Notion, and files Linear tickets for any violations — replacing a manual compliance review step that previously required a dedicated security engineer rotation.</p>
<p><strong>Virgin Atlantic</strong> uses Microsoft Teams and Azure DevOps plugins to coordinate cross-team releases: Codex monitors pipeline status in Azure DevOps, posts deployment readiness summaries to Teams channels, and can trigger rollbacks via the Azure DevOps plugin when error rates spike.</p>
<p>These use cases share a common pattern: plugins eliminate the manual data-fetching and status-posting that consumes developer time without producing code.</p>
<h2 id="codex-plugins-vs-competitors-claude-code-github-copilot-cursor">Codex Plugins vs. Competitors: Claude Code, GitHub Copilot, Cursor</h2>
<p>The enterprise AI coding assistant market in 2026 has three viable plugin ecosystems, each with different strengths. Choosing the right platform depends on your existing tool stack, governance requirements, and whether you prioritize breadth of marketplace coverage or depth of custom extension support.</p>
<table>
	<thead>
			<tr>
					<th>Dimension</th>
					<th>Codex Plugins</th>
					<th>Claude Code Extensions</th>
					<th>GitHub Copilot Extensions</th>
			</tr>
	</thead>
	<tbody>
			<tr>
					<td>Marketplace size</td>
					<td>90+ (April 2026)</td>
					<td>~40 (community MCP servers)</td>
					<td>~30 (verified extensions)</td>
			</tr>
			<tr>
					<td>Enterprise governance</td>
					<td>JSON policy files, org-level admin</td>
					<td>CLAUDE.md rules, settings.json</td>
					<td>GitHub org settings</td>
			</tr>
			<tr>
					<td>Custom plugin type</td>
					<td>Skill / App / MCP server</td>
					<td>MCP server</td>
					<td>VS Code extension API</td>
			</tr>
			<tr>
					<td>Auth management</td>
					<td>Encrypted keychain + vault support</td>
					<td>Manual MCP config</td>
					<td>GitHub OAuth</td>
			</tr>
			<tr>
					<td>Audit logging</td>
					<td>Full SIEM integration</td>
					<td>Limited</td>
					<td>GitHub audit log</td>
			</tr>
			<tr>
					<td>Microsoft ecosystem</td>
					<td>Strong (April 2026 additions)</td>
					<td>Weak</td>
					<td>Native (GitHub = Microsoft)</td>
			</tr>
			<tr>
					<td>Google ecosystem</td>
					<td>Strong (Drive, Gmail, Meet)</td>
					<td>Moderate</td>
					<td>Weak</td>
			</tr>
	</tbody>
</table>
<p>Claude Code&rsquo;s extension model is more developer-friendly for custom MCP servers — the MCP ecosystem is language-agnostic and Claude Code has slightly more flexible tool-calling. However, it lacks the centralized marketplace and enterprise governance controls. Claude Code accounts for approximately 4% of all GitHub public commits, indicating significant developer adoption — but Codex&rsquo;s 4 million+ weekly active users and 6x enterprise growth give it distribution advantage.</p>
<p>GitHub Copilot Extensions benefit from native GitHub integration and are the natural choice for teams already paying for GitHub Enterprise Cloud. The extension model is VS Code-centric, making it less suitable for teams that use terminal-first workflows.</p>
<p>For enterprises selecting a primary platform: <strong>Codex plugins win on governance controls and marketplace breadth</strong>; Claude Code wins on MCP flexibility and model quality per benchmark; Copilot Extensions win on GitHub-native integration. Most large enterprises are running two of the three in parallel, with Codex handling autonomous agentic workflows and Copilot or Claude Code handling interactive coding assistance.</p>
<h2 id="security-compliance-and-audit-logging-for-enterprise-deployments">Security, Compliance, and Audit Logging for Enterprise Deployments</h2>
<p>Enterprise Codex plugin security operates across four layers: authentication, authorization, data handling, and audit. Each layer has specific controls that compliance and security teams evaluate during vendor review. OpenAI published a SOC 2 Type II report for Codex Enterprise in Q1 2026 and supports SCIM provisioning for identity management, satisfying most enterprise security checklists without customization.</p>
<p><strong>Authentication:</strong> Plugin credentials are stored in OS-native encrypted keystores. For enterprise deployments, a <code>--credential-provider</code> flag delegates credential retrieval to HashiCorp Vault, AWS Secrets Manager, or Azure Key Vault — ensuring no credentials touch local disk in plaintext. OAuth tokens are short-lived (1-hour access tokens, 30-day refresh tokens) and scoped to the minimum required permissions per plugin.</p>
<p><strong>Authorization:</strong> The JSON policy system (described in the Governance section) is the primary authorization control. Plugins can&rsquo;t be installed without appearing in the policy allow-list. Codex itself operates within a sandboxed execution environment for agentic tasks — it cannot access local filesystem paths outside the working directory unless explicitly granted via the <code>--allow-fs</code> flag.</p>
<p><strong>Data handling:</strong> Code context sent to Codex is processed under OpenAI&rsquo;s Enterprise Data Processing Agreement (DPA). Enterprise customers have zero data retention by default — code snippets used in Codex sessions aren&rsquo;t stored for model training. Plugin-mediated data (e.g., Slack messages read by Codex) is subject to both OpenAI&rsquo;s DPA and the respective SaaS vendor&rsquo;s data terms.</p>
<p><strong>Audit logging:</strong> All plugin invocations are logged with: actor (user ID), timestamp, plugin name, action called, parameters (redacted at policy-defined sensitivity level), and response status. Logs are available via the Admin API for 90 days and can be streamed to Splunk, Microsoft Sentinel, or other SIEM systems via webhook. For eDiscovery, legal teams can retrieve all interactions for a given user within a date range via the Admin API.</p>
<h2 id="getting-started-5-step-enterprise-rollout-plan-for-codex-plugins">Getting Started: 5-Step Enterprise Rollout Plan for Codex Plugins</h2>
<p>An enterprise Codex plugin rollout follows a predictable pattern: start narrow (two or three core plugins for a pilot team), validate governance and security controls, then expand by team and plugin category. Skipping the pilot phase is the most common mistake — enterprises that roll out all 90+ plugins to all users simultaneously encounter authentication confusion, governance gaps, and support overload. Cisco&rsquo;s rollout lead reported that a 4-week pilot with 15 engineers produced the governance policy file that scaled cleanly to 2,000+ engineers in week five.</p>
<p><strong>Step 1: Identify your 3 core plugins.</strong> For most engineering teams, GitHub + Slack + your CI system (CircleCI, GitHub Actions, etc.) covers 80% of the friction points Codex can eliminate. Start here.</p>
<p><strong>Step 2: Author your governance policy.</strong> Define which plugins are <code>INSTALLED_BY_DEFAULT</code>, which are <code>AVAILABLE</code>, and which are <code>NOT_AVAILABLE</code>. Review with InfoSec before deployment. Use the template below as a starting point:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-json" data-lang="json"><span style="display:flex;"><span>{
</span></span><span style="display:flex;"><span>  <span style="color:#f92672">&#34;plugins&#34;</span>: {
</span></span><span style="display:flex;"><span>    <span style="color:#f92672">&#34;github&#34;</span>: <span style="color:#e6db74">&#34;INSTALLED_BY_DEFAULT&#34;</span>,
</span></span><span style="display:flex;"><span>    <span style="color:#f92672">&#34;slack&#34;</span>: <span style="color:#e6db74">&#34;INSTALLED_BY_DEFAULT&#34;</span>,
</span></span><span style="display:flex;"><span>    <span style="color:#f92672">&#34;circleci&#34;</span>: <span style="color:#e6db74">&#34;INSTALLED_BY_DEFAULT&#34;</span>,
</span></span><span style="display:flex;"><span>    <span style="color:#f92672">&#34;linear&#34;</span>: <span style="color:#e6db74">&#34;AVAILABLE&#34;</span>,
</span></span><span style="display:flex;"><span>    <span style="color:#f92672">&#34;sentry&#34;</span>: <span style="color:#e6db74">&#34;AVAILABLE&#34;</span>,
</span></span><span style="display:flex;"><span>    <span style="color:#f92672">&#34;figma&#34;</span>: <span style="color:#e6db74">&#34;AVAILABLE&#34;</span>
</span></span><span style="display:flex;"><span>  },
</span></span><span style="display:flex;"><span>  <span style="color:#f92672">&#34;audit_log_webhook&#34;</span>: <span style="color:#e6db74">&#34;https://siem.internal.example.com/codex-events&#34;</span>,
</span></span><span style="display:flex;"><span>  <span style="color:#f92672">&#34;credential_provider&#34;</span>: <span style="color:#e6db74">&#34;vault://secrets/codex-plugins&#34;</span>
</span></span><span style="display:flex;"><span>}
</span></span></code></pre></div><p><strong>Step 3: Run a 2-week pilot with a volunteer team.</strong> Collect time-on-task metrics before and after for three workflows: bug triage, PR review notification, and CI failure diagnosis. These three workflows have the clearest before/after deltas.</p>
<p><strong>Step 4: Document your MCP server needs.</strong> After the pilot, you&rsquo;ll have a clear list of internal tools not covered by marketplace plugins. Assign a platform team sprint to build MCP server wrappers for the top two or three tools.</p>
<p><strong>Step 5: Expand org-wide with phased rollout.</strong> Push the governance policy org-wide via Admin API. Use <code>INSTALLED_BY_DEFAULT</code> only for validated plugins; leave others <code>AVAILABLE</code> for self-service discovery. Review audit logs weekly for the first month.</p>
<p>For enterprises with Accenture, Capgemini, CGI, Cognizant, Infosys, PwC, or TCS as system integrators, these firms have certified Codex plugin deployment practices as of Q2 2026 — engaging one of them can compress a 12-week rollout to 4-6 weeks.</p>
<hr>
<h2 id="faq">FAQ</h2>
<p><strong>Q: How many Codex plugins are available in 2026?</strong></p>
<p>As of April 2026, OpenAI has released 90+ plugins in the Codex marketplace, up from 20+ at the initial March 26, 2026 launch. The marketplace grows monthly; the current full list is available at platform.openai.com/codex/plugins or via <code>codex plugin list --all</code>.</p>
<p><strong>Q: Can I use Codex plugins without a ChatGPT Enterprise subscription?</strong></p>
<p>Codex plugins are available across ChatGPT Plus, Business, and Enterprise plans, but enterprise governance features (JSON policy files, SIEM integration, eDiscovery, SCIM) require a Business or Enterprise subscription. Individual Plus users can install plugins manually from the marketplace but don&rsquo;t get org-level admin controls.</p>
<p><strong>Q: How do Codex plugins handle API credentials and secrets?</strong></p>
<p>Codex stores plugin credentials in your OS-native encrypted keystore (macOS Keychain, Linux Secret Service, Windows Credential Manager). Enterprise deployments can use the <code>--credential-provider</code> flag to delegate credential retrieval to HashiCorp Vault, AWS Secrets Manager, or Azure Key Vault, ensuring no secrets touch local disk. Codex never sends raw credentials to OpenAI servers.</p>
<p><strong>Q: What&rsquo;s the difference between a Codex Skill and an MCP server plugin?</strong></p>
<p>A Skill is a stateless, lightweight wrapper around a single API action (e.g., &ldquo;create a Linear ticket&rdquo;). It&rsquo;s defined in JSON and requires minimal code. An MCP server is a full process that Codex communicates with over the Model Context Protocol, supporting arbitrary tool definitions, stateful sessions, and complex multi-step operations. Use Skills for simple API wrapping; use MCP servers for internal systems with complex logic.</p>
<p><strong>Q: How do Codex plugins compare to GitHub Copilot Extensions for enterprise teams?</strong></p>
<p>Codex plugins have a larger marketplace (90+ vs. ~30), stronger enterprise governance controls, and better non-GitHub SaaS coverage. Copilot Extensions have native GitHub integration and VS Code IDE embedding. For teams that primarily live in GitHub and VS Code, Copilot Extensions may be more ergonomic. For terminal-first teams running autonomous agentic workflows, Codex plugins provide more complete enterprise tooling.</p>
]]></content:encoded></item></channel></rss>