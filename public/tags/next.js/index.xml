<?xml version="1.0" encoding="utf-8" standalone="yes"?><rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom" xmlns:content="http://purl.org/rss/1.0/modules/content/"><channel><title>Next.js on RockB</title><link>https://baeseokjae.github.io/tags/next.js/</link><description>Recent content in Next.js on RockB</description><image><title>RockB</title><url>https://baeseokjae.github.io/images/og-default.png</url><link>https://baeseokjae.github.io/images/og-default.png</link></image><generator>Hugo</generator><language>en-us</language><lastBuildDate>Tue, 02 Jun 2026 06:04:31 +0000</lastBuildDate><atom:link href="https://baeseokjae.github.io/tags/next.js/index.xml" rel="self" type="application/rss+xml"/><item><title>TanStack Query v5: Data Fetching and Caching for AI-Powered React Apps</title><link>https://baeseokjae.github.io/posts/tanstack-query-v5-data-fetching-react-guide-2026/</link><pubDate>Tue, 02 Jun 2026 06:04:31 +0000</pubDate><guid>https://baeseokjae.github.io/posts/tanstack-query-v5-data-fetching-react-guide-2026/</guid><description>TanStack Query v5 guide for AI-powered React apps: streamedQuery, optimistic updates, cross-tab sync, SSR, and production patterns.</description><content:encoded><![CDATA[<p>TanStack Query v5 is the server state library for React that handles caching, background refetching, and stale-while-revalidate out of the box — with 12M+ weekly downloads in 2026, it&rsquo;s become the default choice for teams building AI-powered applications that need real-time data and LLM streaming.</p>
<h2 id="why-tanstack-query-v5-is-the-default-choice-for-ai-powered-react-apps-in-2026">Why TanStack Query v5 Is the Default Choice for AI-Powered React Apps in 2026</h2>
<p>TanStack Query v5 (formerly React Query) is a server state management library that handles all the complexity between your React components and your data sources — caching, deduplication, background synchronization, loading states, and error recovery — with minimal configuration. As of June 2026, the library ships at version 5.100.14, has 48K+ GitHub stars (overtaking SWR&rsquo;s 32K in 2024), and sits at 12.3M weekly npm downloads — a 2.5x lead over SWR&rsquo;s 4.9M. That adoption reflects a real shift: AI-powered React apps need capabilities beyond simple data fetching. LLM responses stream over seconds, not milliseconds. Dashboards pull from three or more data sources simultaneously. Users open the same AI tool across five browser tabs. TanStack Query v5&rsquo;s new <code>streamedQuery</code>, <code>broadcastQueryClient</code>, and deep Suspense integration address these patterns directly, which is why teams building AI chatbots, real-time dashboards, and LLM-augmented features in 2026 are reaching for it first.</p>
<h3 id="what-changed-from-v4-to-v5">What Changed from v4 to v5?</h3>
<p>V5 introduced several breaking changes that matter for AI apps. The <code>useQuery</code> callback options <code>onSuccess</code>, <code>onError</code>, and <code>onSettled</code> were removed from the query level (they now live only on mutations). The <code>cacheTime</code> option was renamed to <code>gcTime</code> (garbage collection time) for clarity. Suspense queries got their own dedicated hooks: <code>useSuspenseQuery</code>, <code>useSuspenseInfiniteQuery</code>, and <code>useSuspenseQueries</code>. Most significantly, the experimental <code>streamedQuery</code> helper was added — purpose-built for consuming AsyncIterables from LLM APIs. If you&rsquo;re migrating from v4, budget time for these API changes.</p>
<h2 id="core-concepts-every-ai-developer-needs-to-understand-queries-mutations-cache">Core Concepts Every AI Developer Needs to Understand (Queries, Mutations, Cache)</h2>
<p>TanStack Query separates server state into two primitives: queries (reads that can be cached, deduplicated, and refetched) and mutations (writes that trigger side effects). The cache sits between your components and your server, keyed by arrays you control. A query result with <code>staleTime: 30_000</code> stays fresh for 30 seconds — any component mounting during that window gets the cached value instantly without a network request. After stale, the cache serves the old data immediately while refetching in the background — the stale-while-revalidate pattern that makes AI dashboards feel fast. The <code>gcTime</code> (default: 5 minutes) controls how long inactive cache entries survive before garbage collection. For AI apps, these two numbers are your primary performance levers: high <code>staleTime</code> for expensive LLM-generated content that doesn&rsquo;t change often, low <code>staleTime</code> (or zero) for real-time data like chat histories. Understanding the lifecycle — fresh → stale → inactive → garbage collected — is the prerequisite to every other pattern in this guide.</p>
<h3 id="query-keys-the-foundation-of-everything">Query Keys: The Foundation of Everything</h3>
<p>Query keys are arrays that uniquely identify each piece of cached data. The key <code>['users', userId]</code> caches per-user data separately from <code>['users', 'list']</code>. For AI apps, keys like <code>['chat', sessionId, 'messages']</code> let you invalidate a specific conversation&rsquo;s cache when a new LLM response arrives. Every cache operation — <code>invalidateQueries</code>, <code>setQueryData</code>, <code>prefetchQuery</code> — targets keys. Get key design right early; it determines how cleanly you can invalidate cache when AI state changes.</p>
<h2 id="setting-up-tanstack-query-v5-in-a-nextjs-or-vite-react-project">Setting Up TanStack Query v5 in a Next.js or Vite React Project</h2>
<p>Setting up TanStack Query v5 requires installing <code>@tanstack/react-query</code> and wrapping your app in <code>QueryClientProvider</code>. The setup is nearly identical across Vite and Next.js App Router, with one difference: in Next.js App Router you must instantiate <code>QueryClient</code> inside a <code>'use client'</code> component to avoid sharing state between server requests. Install the package with <code>npm install @tanstack/react-query</code>, and optionally install <code>@tanstack/react-query-devtools</code> for the browser devtools overlay. The DevTools are indispensable for AI apps — they show cache state, query status, and stale timing in real time, which is exactly what you need when debugging why an LLM response isn&rsquo;t refetching when expected. Version 5.100.14 ships with full TypeScript support and no additional type packages needed.</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-tsx" data-lang="tsx"><span style="display:flex;"><span><span style="color:#75715e">// app/providers.tsx — Next.js App Router
</span></span></span><span style="display:flex;"><span><span style="color:#75715e"></span><span style="color:#e6db74">&#39;use client&#39;</span>
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">import</span> { <span style="color:#a6e22e">QueryClient</span>, <span style="color:#a6e22e">QueryClientProvider</span> } <span style="color:#66d9ef">from</span> <span style="color:#e6db74">&#39;@tanstack/react-query&#39;</span>
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">import</span> { <span style="color:#a6e22e">ReactQueryDevtools</span> } <span style="color:#66d9ef">from</span> <span style="color:#e6db74">&#39;@tanstack/react-query-devtools&#39;</span>
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">import</span> { <span style="color:#a6e22e">useState</span> } <span style="color:#66d9ef">from</span> <span style="color:#e6db74">&#39;react&#39;</span>
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">export</span> <span style="color:#66d9ef">function</span> <span style="color:#a6e22e">Providers</span>({ <span style="color:#a6e22e">children</span> }<span style="color:#f92672">:</span> { <span style="color:#a6e22e">children</span>: <span style="color:#66d9ef">React.ReactNode</span> }) {
</span></span><span style="display:flex;"><span>  <span style="color:#66d9ef">const</span> [<span style="color:#a6e22e">queryClient</span>] <span style="color:#f92672">=</span> <span style="color:#a6e22e">useState</span>(() <span style="color:#f92672">=&gt;</span> <span style="color:#66d9ef">new</span> <span style="color:#a6e22e">QueryClient</span>({
</span></span><span style="display:flex;"><span>    <span style="color:#a6e22e">defaultOptions</span><span style="color:#f92672">:</span> {
</span></span><span style="display:flex;"><span>      <span style="color:#a6e22e">queries</span><span style="color:#f92672">:</span> {
</span></span><span style="display:flex;"><span>        <span style="color:#a6e22e">staleTime</span>: <span style="color:#66d9ef">60</span> <span style="color:#f92672">*</span> <span style="color:#ae81ff">1000</span>, <span style="color:#75715e">// 1 minute default
</span></span></span><span style="display:flex;"><span><span style="color:#75715e"></span>        <span style="color:#a6e22e">gcTime</span>: <span style="color:#66d9ef">5</span> <span style="color:#f92672">*</span> <span style="color:#ae81ff">60</span> <span style="color:#f92672">*</span> <span style="color:#ae81ff">1000</span>, <span style="color:#75715e">// 5 minutes garbage collection
</span></span></span><span style="display:flex;"><span><span style="color:#75715e"></span>      },
</span></span><span style="display:flex;"><span>    },
</span></span><span style="display:flex;"><span>  }))
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>  <span style="color:#66d9ef">return</span> (
</span></span><span style="display:flex;"><span>    &lt;<span style="color:#f92672">QueryClientProvider</span> <span style="color:#a6e22e">client</span><span style="color:#f92672">=</span>{<span style="color:#a6e22e">queryClient</span>}&gt;
</span></span><span style="display:flex;"><span>      {<span style="color:#a6e22e">children</span>}
</span></span><span style="display:flex;"><span>      &lt;<span style="color:#f92672">ReactQueryDevtools</span> <span style="color:#a6e22e">initialIsOpen</span><span style="color:#f92672">=</span>{<span style="color:#66d9ef">false</span>} /&gt;
</span></span><span style="display:flex;"><span>    &lt;/<span style="color:#f92672">QueryClientProvider</span>&gt;
</span></span><span style="display:flex;"><span>  )
</span></span><span style="display:flex;"><span>}
</span></span></code></pre></div><p>Wrap <code>layout.tsx</code> with <code>&lt;Providers&gt;</code> and every Server Component route automatically gets access to the client-side cache in child Client Components.</p>
<h2 id="streaming-ai-responses-with-streamedquery--the-llm-native-approach">Streaming AI Responses with streamedQuery — The LLM-Native Approach</h2>
<p><code>streamedQuery</code> is TanStack Query v5&rsquo;s experimental answer to the LLM streaming problem. It wraps a function that returns an <code>AsyncIterable</code> and progressively updates the query cache as each chunk arrives, triggering React re-renders on every chunk without any manual state management. Before <code>streamedQuery</code>, teams wired up custom <code>useEffect</code> hooks, manually maintained partial-response state, and wrote cleanup logic for aborted streams — hundreds of lines of boilerplate per feature. With <code>streamedQuery</code>, the pattern collapses to a single hook call. The helper is stable enough for production use in 2026 (multiple large AI apps ship it), though the API carries the &ldquo;experimental&rdquo; label and could change between minor versions. For apps using the Vercel AI SDK, <code>streamedQuery</code> pairs naturally with <code>streamText</code> responses: the Vercel SDK yields an <code>AsyncIterable</code> that <code>streamedQuery</code> consumes directly, with automatic cache entry creation and incremental updates as tokens arrive.</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-tsx" data-lang="tsx"><span style="display:flex;"><span><span style="color:#66d9ef">import</span> { <span style="color:#a6e22e">streamedQuery</span>, <span style="color:#a6e22e">useQuery</span> } <span style="color:#66d9ef">from</span> <span style="color:#e6db74">&#39;@tanstack/react-query&#39;</span>
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">const</span> <span style="color:#a6e22e">aiQueryOptions</span> <span style="color:#f92672">=</span> (<span style="color:#a6e22e">prompt</span>: <span style="color:#66d9ef">string</span>) <span style="color:#f92672">=&gt;</span> ({
</span></span><span style="display:flex;"><span>  <span style="color:#a6e22e">queryKey</span><span style="color:#f92672">:</span> [<span style="color:#e6db74">&#39;ai-response&#39;</span>, <span style="color:#a6e22e">prompt</span>],
</span></span><span style="display:flex;"><span>  <span style="color:#a6e22e">queryFn</span>: <span style="color:#66d9ef">streamedQuery</span>({
</span></span><span style="display:flex;"><span>    <span style="color:#a6e22e">queryFn</span>: <span style="color:#66d9ef">async</span> ({ <span style="color:#a6e22e">signal</span> }) <span style="color:#f92672">=&gt;</span> {
</span></span><span style="display:flex;"><span>      <span style="color:#66d9ef">const</span> <span style="color:#a6e22e">response</span> <span style="color:#f92672">=</span> <span style="color:#66d9ef">await</span> <span style="color:#a6e22e">fetch</span>(<span style="color:#e6db74">&#39;/api/chat&#39;</span>, {
</span></span><span style="display:flex;"><span>        <span style="color:#a6e22e">method</span><span style="color:#f92672">:</span> <span style="color:#e6db74">&#39;POST&#39;</span>,
</span></span><span style="display:flex;"><span>        <span style="color:#a6e22e">body</span>: <span style="color:#66d9ef">JSON.stringify</span>({ <span style="color:#a6e22e">prompt</span> }),
</span></span><span style="display:flex;"><span>        <span style="color:#a6e22e">signal</span>,
</span></span><span style="display:flex;"><span>      })
</span></span><span style="display:flex;"><span>      <span style="color:#66d9ef">return</span> <span style="color:#a6e22e">response</span>.<span style="color:#a6e22e">body</span> <span style="color:#75715e">// ReadableStream → AsyncIterable via browser APIs
</span></span></span><span style="display:flex;"><span><span style="color:#75715e"></span>    },
</span></span><span style="display:flex;"><span>  }),
</span></span><span style="display:flex;"><span>  <span style="color:#a6e22e">staleTime</span>: <span style="color:#66d9ef">Infinity</span>, <span style="color:#75715e">// LLM responses don&#39;t go stale
</span></span></span><span style="display:flex;"><span><span style="color:#75715e"></span>})
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">function</span> <span style="color:#a6e22e">AIResponse</span>({ <span style="color:#a6e22e">prompt</span> }<span style="color:#f92672">:</span> { <span style="color:#a6e22e">prompt</span>: <span style="color:#66d9ef">string</span> }) {
</span></span><span style="display:flex;"><span>  <span style="color:#66d9ef">const</span> { <span style="color:#a6e22e">data</span>, <span style="color:#a6e22e">isLoading</span> } <span style="color:#f92672">=</span> <span style="color:#a6e22e">useQuery</span>(<span style="color:#a6e22e">aiQueryOptions</span>(<span style="color:#a6e22e">prompt</span>))
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>  <span style="color:#66d9ef">if</span> (<span style="color:#a6e22e">isLoading</span>) <span style="color:#66d9ef">return</span> &lt;<span style="color:#f92672">span</span>&gt;<span style="color:#a6e22e">Thinking</span>...&lt;/<span style="color:#f92672">span</span>&gt;
</span></span><span style="display:flex;"><span>  <span style="color:#66d9ef">return</span> &lt;<span style="color:#f92672">div</span>&gt;{<span style="color:#a6e22e">data</span><span style="color:#f92672">?</span>.<span style="color:#a6e22e">join</span>(<span style="color:#e6db74">&#39;&#39;</span>)}&lt;/<span style="color:#f92672">div</span>&gt; <span style="color:#75715e">// data is string[] of chunks
</span></span></span><span style="display:flex;"><span><span style="color:#75715e"></span>}
</span></span></code></pre></div><p>The <code>signal</code> parameter wires abort automatically — navigating away cancels the in-flight LLM request. Set <code>staleTime: Infinity</code> for LLM responses: they don&rsquo;t change after generation, and you want them cached for the session.</p>
<h2 id="optimistic-updates-for-ai-chatbot-ux-show-messages-before-server-confirms">Optimistic Updates for AI Chatbot UX: Show Messages Before Server Confirms</h2>
<p>Optimistic updates are the technique of immediately updating the UI as if a mutation succeeded, then rolling back if the server returns an error. For AI chatbots, this is essential: users expect to see their message appear in the chat instantly when they press Send, not after the LLM API round-trip completes. TanStack Query&rsquo;s mutation lifecycle — <code>onMutate</code>, <code>onError</code>, <code>onSettled</code> — gives you exact hooks for this pattern. In <code>onMutate</code>, you snapshot the current cache and inject the optimistic message. In <code>onError</code>, you restore the snapshot. In <code>onSettled</code>, you invalidate the query to fetch the real server state. This three-step pattern handles the edge case of out-of-order responses (two messages sent fast) and ensures the UI never gets permanently stuck in an optimistic state. Teams building production AI chatbots like internal Slack-style tools report 40–60% improvement in perceived responsiveness with this pattern, purely from eliminating the submit-to-appear delay.</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-tsx" data-lang="tsx"><span style="display:flex;"><span><span style="color:#66d9ef">const</span> <span style="color:#a6e22e">sendMessage</span> <span style="color:#f92672">=</span> <span style="color:#a6e22e">useMutation</span>({
</span></span><span style="display:flex;"><span>  <span style="color:#a6e22e">mutationFn</span><span style="color:#f92672">:</span> (<span style="color:#a6e22e">message</span>: <span style="color:#66d9ef">string</span>) <span style="color:#f92672">=&gt;</span> 
</span></span><span style="display:flex;"><span>    <span style="color:#a6e22e">fetch</span>(<span style="color:#e6db74">&#39;/api/chat&#39;</span>, { <span style="color:#a6e22e">method</span><span style="color:#f92672">:</span> <span style="color:#e6db74">&#39;POST&#39;</span>, <span style="color:#a6e22e">body</span>: <span style="color:#66d9ef">JSON.stringify</span>({ <span style="color:#a6e22e">message</span> }) }),
</span></span><span style="display:flex;"><span>  
</span></span><span style="display:flex;"><span>  <span style="color:#a6e22e">onMutate</span>: <span style="color:#66d9ef">async</span> (<span style="color:#a6e22e">newMessage</span>) <span style="color:#f92672">=&gt;</span> {
</span></span><span style="display:flex;"><span>    <span style="color:#66d9ef">await</span> <span style="color:#a6e22e">queryClient</span>.<span style="color:#a6e22e">cancelQueries</span>({ <span style="color:#a6e22e">queryKey</span><span style="color:#f92672">:</span> [<span style="color:#e6db74">&#39;messages&#39;</span>, <span style="color:#a6e22e">sessionId</span>] })
</span></span><span style="display:flex;"><span>    <span style="color:#66d9ef">const</span> <span style="color:#a6e22e">previous</span> <span style="color:#f92672">=</span> <span style="color:#a6e22e">queryClient</span>.<span style="color:#a6e22e">getQueryData</span>([<span style="color:#e6db74">&#39;messages&#39;</span>, <span style="color:#a6e22e">sessionId</span>])
</span></span><span style="display:flex;"><span>    
</span></span><span style="display:flex;"><span>    <span style="color:#a6e22e">queryClient</span>.<span style="color:#a6e22e">setQueryData</span>([<span style="color:#e6db74">&#39;messages&#39;</span>, <span style="color:#a6e22e">sessionId</span>], (<span style="color:#a6e22e">old</span>: <span style="color:#66d9ef">Message</span>[]) <span style="color:#f92672">=&gt;</span> [
</span></span><span style="display:flex;"><span>      ...<span style="color:#a6e22e">old</span>,
</span></span><span style="display:flex;"><span>      { <span style="color:#a6e22e">id</span><span style="color:#f92672">:</span> <span style="color:#e6db74">&#39;temp&#39;</span>, <span style="color:#a6e22e">content</span>: <span style="color:#66d9ef">newMessage</span>, <span style="color:#a6e22e">role</span><span style="color:#f92672">:</span> <span style="color:#e6db74">&#39;user&#39;</span>, <span style="color:#a6e22e">pending</span>: <span style="color:#66d9ef">true</span> }
</span></span><span style="display:flex;"><span>    ])
</span></span><span style="display:flex;"><span>    
</span></span><span style="display:flex;"><span>    <span style="color:#66d9ef">return</span> { <span style="color:#a6e22e">previous</span> }
</span></span><span style="display:flex;"><span>  },
</span></span><span style="display:flex;"><span>  
</span></span><span style="display:flex;"><span>  <span style="color:#a6e22e">onError</span><span style="color:#f92672">:</span> (<span style="color:#a6e22e">err</span>, <span style="color:#a6e22e">vars</span>, <span style="color:#a6e22e">context</span>) <span style="color:#f92672">=&gt;</span> {
</span></span><span style="display:flex;"><span>    <span style="color:#a6e22e">queryClient</span>.<span style="color:#a6e22e">setQueryData</span>([<span style="color:#e6db74">&#39;messages&#39;</span>, <span style="color:#a6e22e">sessionId</span>], <span style="color:#a6e22e">context</span><span style="color:#f92672">?</span>.<span style="color:#a6e22e">previous</span>)
</span></span><span style="display:flex;"><span>  },
</span></span><span style="display:flex;"><span>  
</span></span><span style="display:flex;"><span>  <span style="color:#a6e22e">onSettled</span><span style="color:#f92672">:</span> () <span style="color:#f92672">=&gt;</span> {
</span></span><span style="display:flex;"><span>    <span style="color:#a6e22e">queryClient</span>.<span style="color:#a6e22e">invalidateQueries</span>({ <span style="color:#a6e22e">queryKey</span><span style="color:#f92672">:</span> [<span style="color:#e6db74">&#39;messages&#39;</span>, <span style="color:#a6e22e">sessionId</span>] })
</span></span><span style="display:flex;"><span>  },
</span></span><span style="display:flex;"><span>})
</span></span></code></pre></div><h2 id="cross-tab-ai-state-sync-with-broadcastqueryclient-experimental">Cross-Tab AI State Sync with broadcastQueryClient (Experimental)</h2>
<p><code>broadcastQueryClient</code> is an experimental TanStack Query plugin that synchronizes cache state across browser tabs using the native Broadcast Channel API. When a user has your AI app open in three tabs and completes an action in one — uploading a document, finishing a chat session, updating AI settings — <code>broadcastQueryClient</code> propagates the cache invalidation to the other tabs automatically. Without it, the other tabs show stale data until the user manually refreshes. The plugin is plug-and-play: install <code>@tanstack/query-broadcast-client-experimental</code>, wrap your <code>QueryClient</code>, and cross-tab sync works automatically for all queries. The Broadcast Channel API is supported in all modern browsers and requires no server-side infrastructure — it operates entirely client-side through shared memory channels. For AI apps where users frequently compare outputs across tabs (a common pattern with LLM tools), this prevents the confusing UX of seeing different AI responses for the same prompt across windows.</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-tsx" data-lang="tsx"><span style="display:flex;"><span><span style="color:#66d9ef">import</span> { <span style="color:#a6e22e">broadcastQueryClient</span> } <span style="color:#66d9ef">from</span> <span style="color:#e6db74">&#39;@tanstack/query-broadcast-client-experimental&#39;</span>
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">const</span> <span style="color:#a6e22e">queryClient</span> <span style="color:#f92672">=</span> <span style="color:#66d9ef">new</span> <span style="color:#a6e22e">QueryClient</span>()
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#a6e22e">broadcastQueryClient</span>({
</span></span><span style="display:flex;"><span>  <span style="color:#a6e22e">queryClient</span>,
</span></span><span style="display:flex;"><span>  <span style="color:#a6e22e">broadcastChannel</span><span style="color:#f92672">:</span> <span style="color:#e6db74">&#39;my-ai-app&#39;</span>, <span style="color:#75715e">// unique channel name per app
</span></span></span><span style="display:flex;"><span><span style="color:#75715e"></span>})
</span></span></code></pre></div><p>Install the experimental package separately: <code>npm install @tanstack/query-broadcast-client-experimental</code>. The API is stable enough for production but may have breaking changes between minor v5 releases.</p>
<h2 id="server-side-rendering-and-suspense-streaming-for-ai-applications">Server-Side Rendering and Suspense Streaming for AI Applications</h2>
<p>TanStack Query v5&rsquo;s SSR story uses <code>HydrationBoundary</code> to transfer prefetched server data to the client without a second network round-trip. In Next.js App Router, you prefetch queries in Server Components using <code>queryClient.prefetchQuery</code>, serialize the cache state with <code>dehydrate</code>, and pass it to <code>HydrationBoundary</code> in the client component tree. The client hydrates instantly from the dehydrated state — no loading flash on first render. For AI apps, this means an AI-generated summary, recommendation, or search result rendered on the server shows up immediately on the client without re-fetching. Combine this with <code>useSuspenseQuery</code> (the v5 hook that integrates with React Suspense) and Next.js streaming to progressively render AI-heavy pages: the server streams the shell immediately, then streams in AI-dependent sections as they complete. The v5 <code>useSuspenseQuery</code> hook is the replacement for the <code>suspense: true</code> option from v4 — it makes the component suspend while data loads, works with React&rsquo;s built-in <code>&lt;Suspense&gt;</code> boundaries, and enables streaming SSR without any additional configuration.</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-tsx" data-lang="tsx"><span style="display:flex;"><span><span style="color:#75715e">// app/dashboard/page.tsx — Server Component
</span></span></span><span style="display:flex;"><span><span style="color:#75715e"></span><span style="color:#66d9ef">import</span> { <span style="color:#a6e22e">dehydrate</span>, <span style="color:#a6e22e">HydrationBoundary</span>, <span style="color:#a6e22e">QueryClient</span> } <span style="color:#66d9ef">from</span> <span style="color:#e6db74">&#39;@tanstack/react-query&#39;</span>
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">export</span> <span style="color:#66d9ef">default</span> <span style="color:#66d9ef">async</span> <span style="color:#66d9ef">function</span> <span style="color:#a6e22e">DashboardPage() {</span>
</span></span><span style="display:flex;"><span>  <span style="color:#66d9ef">const</span> <span style="color:#a6e22e">queryClient</span> <span style="color:#f92672">=</span> <span style="color:#66d9ef">new</span> <span style="color:#a6e22e">QueryClient</span>()
</span></span><span style="display:flex;"><span>  
</span></span><span style="display:flex;"><span>  <span style="color:#66d9ef">await</span> <span style="color:#a6e22e">queryClient</span>.<span style="color:#a6e22e">prefetchQuery</span>({
</span></span><span style="display:flex;"><span>    <span style="color:#a6e22e">queryKey</span><span style="color:#f92672">:</span> [<span style="color:#e6db74">&#39;ai-insights&#39;</span>, <span style="color:#e6db74">&#39;dashboard&#39;</span>],
</span></span><span style="display:flex;"><span>    <span style="color:#a6e22e">queryFn</span>: <span style="color:#66d9ef">fetchAIInsights</span>,
</span></span><span style="display:flex;"><span>  })
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>  <span style="color:#66d9ef">return</span> (
</span></span><span style="display:flex;"><span>    &lt;<span style="color:#f92672">HydrationBoundary</span> <span style="color:#a6e22e">state</span><span style="color:#f92672">=</span>{<span style="color:#a6e22e">dehydrate</span>(<span style="color:#a6e22e">queryClient</span>)}&gt;
</span></span><span style="display:flex;"><span>      &lt;<span style="color:#f92672">Suspense</span> <span style="color:#a6e22e">fallback</span><span style="color:#f92672">=</span>{&lt;<span style="color:#f92672">InsightsSkeleton</span> /&gt;}&gt;
</span></span><span style="display:flex;"><span>        &lt;<span style="color:#f92672">AIInsights</span> /&gt;
</span></span><span style="display:flex;"><span>      &lt;/<span style="color:#f92672">Suspense</span>&gt;
</span></span><span style="display:flex;"><span>    &lt;/<span style="color:#f92672">HydrationBoundary</span>&gt;
</span></span><span style="display:flex;"><span>  )
</span></span><span style="display:flex;"><span>}
</span></span></code></pre></div><h2 id="tanstack-query-vs-swr-vs-apollo-for-ai-apps--which-should-you-pick">TanStack Query vs SWR vs Apollo for AI Apps — Which Should You Pick?</h2>
<p>The choice between TanStack Query, SWR, and Apollo comes down to feature requirements and bundle size tolerance. TanStack Query leads on features — <code>streamedQuery</code>, <code>broadcastQueryClient</code>, infinite queries, offline support, DevTools, and the most complete mutation lifecycle — at 13 KB gzipped. SWR is lighter at 4 KB but lacks streaming support and the mutation patterns AI chatbots need. Apollo is the choice only if you&rsquo;re already on GraphQL; for REST or custom AI APIs it adds unnecessary GraphQL infrastructure.</p>
<table>
  <thead>
      <tr>
          <th>Library</th>
          <th>Bundle (gzipped)</th>
          <th>Weekly Downloads</th>
          <th>Streaming</th>
          <th>Optimistic Updates</th>
          <th>DevTools</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>TanStack Query v5</td>
          <td>~13 KB</td>
          <td>12.3M</td>
          <td><code>streamedQuery</code> ✓</td>
          <td>Full lifecycle ✓</td>
          <td>Built-in ✓</td>
      </tr>
      <tr>
          <td>SWR 2.x</td>
          <td>~4 KB</td>
          <td>4.9M</td>
          <td>Manual only</td>
          <td><code>mutate()</code> only</td>
          <td>Third-party</td>
      </tr>
      <tr>
          <td>Apollo Client 3.x</td>
          <td>~47 KB</td>
          <td>3.2M</td>
          <td>Subscriptions</td>
          <td>✓</td>
          <td>Built-in ✓</td>
      </tr>
  </tbody>
</table>
<p>For AI apps specifically: if you&rsquo;re building an LLM chatbot, document processor, or real-time AI dashboard, TanStack Query&rsquo;s streaming and mutation features justify the 9 KB over SWR. If you&rsquo;re building a simple data display app with no streaming requirements and bundle size is critical (mobile web), SWR is a reasonable choice. Apollo is rarely the right call for AI apps unless your entire data layer is already GraphQL.</p>
<h2 id="query-key-factories-and-cache-invalidation-patterns-for-ai-features">Query Key Factories and Cache Invalidation Patterns for AI Features</h2>
<p>Query key factories are a pattern for centralizing query key definitions to prevent typos and make cache invalidation predictable at scale. The pattern is a plain object of functions that return key arrays, defined once and imported wherever you need to interact with the cache. For AI apps with multiple features — chat sessions, document analysis, AI recommendations — scattered string keys like <code>['chat', sessionId]</code> become unmaintainable. A factory like <code>chatKeys.messages(sessionId)</code> is refactorable, type-safe, and self-documenting. The invalidation payoff is significant: <code>queryClient.invalidateQueries({ queryKey: chatKeys.all })</code> clears all chat-related cache in one call, while <code>queryClient.invalidateQueries({ queryKey: chatKeys.messages(sessionId) })</code> targets a specific conversation. Selective invalidation is what separates a snappy AI app from one that re-fetches everything on every user action.</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-tsx" data-lang="tsx"><span style="display:flex;"><span><span style="color:#66d9ef">export</span> <span style="color:#66d9ef">const</span> <span style="color:#a6e22e">chatKeys</span> <span style="color:#f92672">=</span> {
</span></span><span style="display:flex;"><span>  <span style="color:#a6e22e">all</span><span style="color:#f92672">:</span> [<span style="color:#e6db74">&#39;chat&#39;</span>] <span style="color:#66d9ef">as</span> <span style="color:#66d9ef">const</span>,
</span></span><span style="display:flex;"><span>  <span style="color:#a6e22e">sessions</span><span style="color:#f92672">:</span> () <span style="color:#f92672">=&gt;</span> [...<span style="color:#a6e22e">chatKeys</span>.<span style="color:#a6e22e">all</span>, <span style="color:#e6db74">&#39;sessions&#39;</span>] <span style="color:#66d9ef">as</span> <span style="color:#66d9ef">const</span>,
</span></span><span style="display:flex;"><span>  <span style="color:#a6e22e">session</span><span style="color:#f92672">:</span> (<span style="color:#a6e22e">id</span>: <span style="color:#66d9ef">string</span>) <span style="color:#f92672">=&gt;</span> [...<span style="color:#a6e22e">chatKeys</span>.<span style="color:#a6e22e">sessions</span>(), <span style="color:#a6e22e">id</span>] <span style="color:#66d9ef">as</span> <span style="color:#66d9ef">const</span>,
</span></span><span style="display:flex;"><span>  <span style="color:#a6e22e">messages</span><span style="color:#f92672">:</span> (<span style="color:#a6e22e">sessionId</span>: <span style="color:#66d9ef">string</span>) <span style="color:#f92672">=&gt;</span> [...<span style="color:#a6e22e">chatKeys</span>.<span style="color:#a6e22e">session</span>(<span style="color:#a6e22e">sessionId</span>), <span style="color:#e6db74">&#39;messages&#39;</span>] <span style="color:#66d9ef">as</span> <span style="color:#66d9ef">const</span>,
</span></span><span style="display:flex;"><span>  <span style="color:#a6e22e">aiResponse</span><span style="color:#f92672">:</span> (<span style="color:#a6e22e">sessionId</span>: <span style="color:#66d9ef">string</span>, <span style="color:#a6e22e">messageId</span>: <span style="color:#66d9ef">string</span>) <span style="color:#f92672">=&gt;</span> 
</span></span><span style="display:flex;"><span>    [...<span style="color:#a6e22e">chatKeys</span>.<span style="color:#a6e22e">messages</span>(<span style="color:#a6e22e">sessionId</span>), <span style="color:#a6e22e">messageId</span>, <span style="color:#e6db74">&#39;ai&#39;</span>] <span style="color:#66d9ef">as</span> <span style="color:#66d9ef">const</span>,
</span></span><span style="display:flex;"><span>}
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#75715e">// Invalidate all messages for a session when AI responds
</span></span></span><span style="display:flex;"><span><span style="color:#75715e"></span><span style="color:#a6e22e">queryClient</span>.<span style="color:#a6e22e">invalidateQueries</span>({ <span style="color:#a6e22e">queryKey</span>: <span style="color:#66d9ef">chatKeys.messages</span>(<span style="color:#a6e22e">sessionId</span>) })
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#75715e">// Invalidate everything when user deletes their account
</span></span></span><span style="display:flex;"><span><span style="color:#75715e"></span><span style="color:#a6e22e">queryClient</span>.<span style="color:#a6e22e">invalidateQueries</span>({ <span style="color:#a6e22e">queryKey</span>: <span style="color:#66d9ef">chatKeys.all</span> })
</span></span></code></pre></div><h2 id="performance-tuning-staletime-gctime-and-background-refetch-for-ai-workloads">Performance Tuning: staleTime, gcTime, and Background Refetch for AI Workloads</h2>
<p>Tuning <code>staleTime</code> and <code>gcTime</code> is how you balance freshness against network cost for AI workloads. The defaults — <code>staleTime: 0</code>, <code>gcTime: 5 minutes</code> — mean every component mount triggers a background refetch. For AI apps where fetches are expensive (LLM API calls can cost $0.01–$0.10+ each), defaulting to aggressive refetching is wasteful. The right strategy segments data by update frequency. AI-generated content that doesn&rsquo;t change (a processed document summary, a one-time recommendation) should have <code>staleTime: Infinity</code> — fetch once, cache forever for the session. Real-time chat messages need <code>staleTime: 0</code> with <code>refetchInterval</code> for polling, or better, use <code>streamedQuery</code> for push-based updates. User preferences and settings can use <code>staleTime: 5 * 60 * 1000</code> (5 minutes) — fresh enough to catch updates, conservative enough to avoid unnecessary requests. Configure these at the <code>QueryClient</code> level as defaults, then override per-query where the data&rsquo;s freshness requirements differ.</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-tsx" data-lang="tsx"><span style="display:flex;"><span><span style="color:#66d9ef">const</span> <span style="color:#a6e22e">queryClient</span> <span style="color:#f92672">=</span> <span style="color:#66d9ef">new</span> <span style="color:#a6e22e">QueryClient</span>({
</span></span><span style="display:flex;"><span>  <span style="color:#a6e22e">defaultOptions</span><span style="color:#f92672">:</span> {
</span></span><span style="display:flex;"><span>    <span style="color:#a6e22e">queries</span><span style="color:#f92672">:</span> {
</span></span><span style="display:flex;"><span>      <span style="color:#a6e22e">staleTime</span>: <span style="color:#66d9ef">60_000</span>,        <span style="color:#75715e">// 1 minute: most app data
</span></span></span><span style="display:flex;"><span><span style="color:#75715e"></span>      <span style="color:#a6e22e">gcTime</span>: <span style="color:#66d9ef">10</span> <span style="color:#f92672">*</span> <span style="color:#ae81ff">60</span><span style="color:#a6e22e">_000</span>,      <span style="color:#75715e">// 10 minutes: keep inactive cache longer
</span></span></span><span style="display:flex;"><span><span style="color:#75715e"></span>      <span style="color:#a6e22e">retry</span>: <span style="color:#66d9ef">2</span>,                 <span style="color:#75715e">// retry failed AI API calls twice
</span></span></span><span style="display:flex;"><span><span style="color:#75715e"></span>      <span style="color:#a6e22e">retryDelay</span>: <span style="color:#66d9ef">attemptIndex</span> <span style="color:#f92672">=&gt;</span> Math.<span style="color:#a6e22e">min</span>(<span style="color:#ae81ff">1000</span> <span style="color:#f92672">*</span> <span style="color:#ae81ff">2</span> <span style="color:#f92672">**</span> <span style="color:#a6e22e">attemptIndex</span>, <span style="color:#ae81ff">30</span><span style="color:#a6e22e">_000</span>),
</span></span><span style="display:flex;"><span>    },
</span></span><span style="display:flex;"><span>  },
</span></span><span style="display:flex;"><span>})
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#75715e">// Per-query override for LLM-generated content
</span></span></span><span style="display:flex;"><span><span style="color:#75715e"></span><span style="color:#66d9ef">const</span> { <span style="color:#a6e22e">data</span> } <span style="color:#f92672">=</span> <span style="color:#a6e22e">useQuery</span>({
</span></span><span style="display:flex;"><span>  <span style="color:#a6e22e">queryKey</span><span style="color:#f92672">:</span> [<span style="color:#e6db74">&#39;summary&#39;</span>, <span style="color:#a6e22e">documentId</span>],
</span></span><span style="display:flex;"><span>  <span style="color:#a6e22e">queryFn</span><span style="color:#f92672">:</span> () <span style="color:#f92672">=&gt;</span> <span style="color:#a6e22e">generateSummary</span>(<span style="color:#a6e22e">documentId</span>),
</span></span><span style="display:flex;"><span>  <span style="color:#a6e22e">staleTime</span>: <span style="color:#66d9ef">Infinity</span>,  <span style="color:#75715e">// Never re-fetch the same document summary
</span></span></span><span style="display:flex;"><span><span style="color:#75715e"></span>  <span style="color:#a6e22e">gcTime</span>: <span style="color:#66d9ef">30</span> <span style="color:#f92672">*</span> <span style="color:#ae81ff">60</span><span style="color:#a6e22e">_000</span>,  <span style="color:#75715e">// Keep in cache for 30 minutes
</span></span></span><span style="display:flex;"><span><span style="color:#75715e"></span>})
</span></span></code></pre></div><h2 id="production-checklist-error-handling-retry-logic-and-devtools-for-ai-apps">Production Checklist: Error Handling, Retry Logic, and DevTools for AI Apps</h2>
<p>A production TanStack Query setup for AI apps needs error boundaries, retry logic calibrated to LLM API rate limits, and DevTools enabled in development. The library&rsquo;s default retry behavior (3 retries with exponential backoff) is usually too aggressive for LLM APIs — a 429 rate limit error from OpenAI or Anthropic means waiting seconds or minutes, not milliseconds. Set <code>retry: 1</code> or <code>retry: false</code> for LLM queries and handle rate limit errors explicitly. For error UI, pair <code>useSuspenseQuery</code> with React&rsquo;s <code>&lt;ErrorBoundary&gt;</code> so network failures surface cleanly instead of crashing the component tree. The TanStack Query DevTools browser panel shows every cache entry, its status (fresh/stale/fetching/error), and remaining stale time — run it in development to verify your <code>staleTime</code> configuration actually produces the caching behavior you expect. Before deploying AI features that call expensive LLM APIs, use the DevTools to confirm that parallel component mounts deduplicate to a single request and that cached responses serve without network hits.</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-tsx" data-lang="tsx"><span style="display:flex;"><span><span style="color:#75715e">// Calibrated retry for LLM APIs
</span></span></span><span style="display:flex;"><span><span style="color:#75715e"></span><span style="color:#66d9ef">const</span> { <span style="color:#a6e22e">data</span>, <span style="color:#a6e22e">error</span> } <span style="color:#f92672">=</span> <span style="color:#a6e22e">useQuery</span>({
</span></span><span style="display:flex;"><span>  <span style="color:#a6e22e">queryKey</span><span style="color:#f92672">:</span> [<span style="color:#e6db74">&#39;llm-response&#39;</span>, <span style="color:#a6e22e">prompt</span>],
</span></span><span style="display:flex;"><span>  <span style="color:#a6e22e">queryFn</span><span style="color:#f92672">:</span> () <span style="color:#f92672">=&gt;</span> <span style="color:#a6e22e">callLLM</span>(<span style="color:#a6e22e">prompt</span>),
</span></span><span style="display:flex;"><span>  <span style="color:#a6e22e">retry</span><span style="color:#f92672">:</span> (<span style="color:#a6e22e">failureCount</span>, <span style="color:#a6e22e">error</span>) <span style="color:#f92672">=&gt;</span> {
</span></span><span style="display:flex;"><span>    <span style="color:#75715e">// Don&#39;t retry 4xx errors (bad request, rate limit exceeded aggressively)
</span></span></span><span style="display:flex;"><span><span style="color:#75715e"></span>    <span style="color:#66d9ef">if</span> (<span style="color:#a6e22e">error</span>.<span style="color:#a6e22e">status</span> <span style="color:#f92672">&gt;=</span> <span style="color:#ae81ff">400</span> <span style="color:#f92672">&amp;&amp;</span> <span style="color:#a6e22e">error</span>.<span style="color:#a6e22e">status</span> <span style="color:#f92672">&lt;</span> <span style="color:#ae81ff">500</span>) <span style="color:#66d9ef">return</span> <span style="color:#66d9ef">false</span>
</span></span><span style="display:flex;"><span>    <span style="color:#66d9ef">return</span> <span style="color:#a6e22e">failureCount</span> <span style="color:#f92672">&lt;</span> <span style="color:#ae81ff">2</span>
</span></span><span style="display:flex;"><span>  },
</span></span><span style="display:flex;"><span>  <span style="color:#a6e22e">retryDelay</span><span style="color:#f92672">:</span> (<span style="color:#a6e22e">attemptIndex</span>) <span style="color:#f92672">=&gt;</span> Math.<span style="color:#a6e22e">min</span>(<span style="color:#ae81ff">2000</span> <span style="color:#f92672">*</span> <span style="color:#ae81ff">2</span> <span style="color:#f92672">**</span> <span style="color:#a6e22e">attemptIndex</span>, <span style="color:#ae81ff">60</span><span style="color:#a6e22e">_000</span>),
</span></span><span style="display:flex;"><span>})
</span></span></code></pre></div><p>For observability, consider adding <code>queryClient.getQueryCache().subscribe()</code> to log cache events to your monitoring system. You get real-time visibility into how often queries miss cache and hit the LLM API — critical for cost control.</p>
<hr>
<h2 id="faq">FAQ</h2>
<p><strong>Q: Do I need TanStack Query if I&rsquo;m already using the Vercel AI SDK for streaming?</strong></p>
<p>The Vercel AI SDK (<code>useChat</code>, <code>useCompletion</code>) handles the streaming transport layer for LLM responses and works standalone. TanStack Query adds value on top when you need to cache AI responses across components, sync state across browser tabs with <code>broadcastQueryClient</code>, combine AI data with non-AI server data in the same cache, or manage optimistic updates for chat messages. For simple chatbot UIs, the AI SDK alone is sufficient. For complex AI dashboards or apps with multiple data sources, TanStack Query complements the AI SDK by managing the broader data layer.</p>
<p><strong>Q: What&rsquo;s the difference between staleTime and gcTime in v5?</strong></p>
<p><code>staleTime</code> controls when data is considered stale (and eligible for background refetch on next mount/focus). <code>gcTime</code> controls when inactive cache entries are garbage collected from memory. Data can be stale but still in cache — components get the stale value instantly while a background refetch runs. When <code>gcTime</code> expires for an inactive entry, it&rsquo;s removed entirely and the next access triggers a fresh fetch with a loading state. In v4 this was called <code>cacheTime</code>; v5 renamed it to <code>gcTime</code> to clarify the distinction.</p>
<p><strong>Q: How does streamedQuery work with abort signals when users navigate away?</strong></p>
<p><code>streamedQuery</code> automatically passes an <code>AbortSignal</code> to your query function via the <code>signal</code> parameter in the function context. When the component unmounts (user navigates away) or the query is cancelled, TanStack Query aborts the signal. If you pass this signal to <code>fetch()</code>, the in-flight HTTP request is cancelled at the browser level — stopping the LLM stream and preventing unnecessary token consumption. Always wire <code>signal</code> through to your fetch calls when using <code>streamedQuery</code>.</p>
<p><strong>Q: Can I use TanStack Query with React Server Components in Next.js App Router?</strong></p>
<p>Yes, with a specific pattern: you prefetch queries in Server Components using <code>queryClient.prefetchQuery</code> and serialize the result with <code>dehydrate</code>. This dehydrated state passes to <code>HydrationBoundary</code> in a Client Component, which hydrates the client cache. Client Components then use <code>useQuery</code> or <code>useSuspenseQuery</code> and get the prefetched data immediately without a loading state. The QueryClient itself must be instantiated in a <code>'use client'</code> boundary — never in Server Components — to avoid sharing state between concurrent requests.</p>
<p><strong>Q: Is TanStack Query suitable for apps with real-time WebSocket data alongside AI responses?</strong></p>
<p>Yes. TanStack Query doesn&rsquo;t manage the WebSocket connection itself, but you can use <code>queryClient.setQueryData</code> to push WebSocket messages directly into the cache from an effect or a WebSocket event handler. This pattern — external event source writes into the query cache, components read from the cache — works cleanly for real-time AI streaming dashboards. You get TanStack Query&rsquo;s cache management, DevTools, and component integration for free, with WebSocket handling only where it belongs: the connection layer.</p>
]]></content:encoded></item><item><title>React Server Components in Next.js App Router: Complete Developer Guide</title><link>https://baeseokjae.github.io/posts/react-server-components-nextjs-app-router-guide-2026/</link><pubDate>Tue, 02 Jun 2026 03:48:36 +0000</pubDate><guid>https://baeseokjae.github.io/posts/react-server-components-nextjs-app-router-guide-2026/</guid><description>Master React Server Components in Next.js App Router — data fetching, streaming, Server Actions, PPR, and migration from Pages Router.</description><content:encoded><![CDATA[<p>React Server Components (RSC) are components that run exclusively on the server, never ship JavaScript to the browser, and can access databases and file systems directly. In Next.js 15 App Router, every component in the <code>app/</code> directory is a Server Component by default — you opt into client-side interactivity with <code>'use client'</code>, not out of it. This guide covers the complete RSC mental model, data fetching patterns, streaming, Server Actions, caching, Partial Prerendering, and the 7 mistakes that silently wreck bundle size.</p>
<h2 id="what-are-react-server-components-the-mental-model-that-changes-everything">What Are React Server Components? (The Mental Model That Changes Everything)</h2>
<p>React Server Components are a fundamentally new rendering primitive introduced in React 18 and productionized in Next.js 13+ App Router. An RSC renders entirely on the server — once — and sends its output as a serialized React tree (not HTML, not JSON) over the wire. The browser never receives the component&rsquo;s JavaScript. It never re-renders. It never hydrates. This is categorically different from Server-Side Rendering (SSR), which sends HTML but still ships the full component JavaScript bundle for hydration. With RSC, non-interactive UI — product descriptions, blog posts, nav trees, data tables — never appears in the client bundle at all. Applications that fully adopt RSC patterns consistently report <strong>50–70% reductions in First Load JS</strong> and significant Largest Contentful Paint (LCP) improvements (vladimirsiedykh.com, 2025). A component that fetches from a database and renders read-only HTML simply disappears from the client bundle entirely. That is the mental model shift: the default changed from &ldquo;client unless proven server&rdquo; to &ldquo;server unless you need the browser.&rdquo;</p>
<p>The key distinction from SSR: SSR pre-renders HTML on the server and then hydrates the same component on the client (downloading its JS). RSC components run on the server only — no hydration, no client JS. The two can coexist on the same page: a Server Component wraps a Client Component that hydrates normally.</p>
<h3 id="why-most-developers-havent-made-the-shift">Why Most Developers Haven&rsquo;t Made the Shift</h3>
<p>Despite more than 50% of developers expressing positive sentiment about RSC, only <strong>29% have actually used them</strong> — a massive awareness-adoption gap (State of React 2025). The friction is real: the mental model requires unlearning 10 years of SPA instincts, the Context API doesn&rsquo;t work across the server-client boundary, and the error messages when you mix them incorrectly can be cryptic. This guide exists to close that gap.</p>
<h2 id="server-components-vs-client-components-the-decision-framework">Server Components vs Client Components: The Decision Framework</h2>
<p>Server Components and Client Components are not better or worse — they answer different questions. Use a Server Component when you need to fetch data from a database, read environment secrets, access the file system, reduce client bundle size, or render static/read-only UI. Use <code>'use client'</code> when you need <code>useState</code>, <code>useEffect</code>, event handlers (<code>onClick</code>, <code>onChange</code>), browser APIs (<code>localStorage</code>, <code>window</code>, geolocation), or React Query / SWR for client-side caching. In Next.js App Router, all components in <code>app/</code> are Server Components by default — you only add <code>'use client'</code> when you genuinely need the browser. The most expensive mistake developers make is adding <code>'use client'</code> at the top of a large component tree because one leaf needs interactivity. That pushes the entire subtree into the client bundle. The correct pattern: push <code>'use client'</code> as deep as the tree as possible, to the single interactive leaf, and keep all parent containers as Server Components.</p>
<p><strong>Decision table:</strong></p>
<table>
  <thead>
      <tr>
          <th>Need</th>
          <th>Server Component</th>
          <th>Client Component</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>Database / ORM queries</td>
          <td>✅</td>
          <td>❌</td>
      </tr>
      <tr>
          <td>Secrets / API keys</td>
          <td>✅</td>
          <td>❌</td>
      </tr>
      <tr>
          <td><code>useState</code> / <code>useReducer</code></td>
          <td>❌</td>
          <td>✅</td>
      </tr>
      <tr>
          <td><code>useEffect</code></td>
          <td>❌</td>
          <td>✅</td>
      </tr>
      <tr>
          <td>Event handlers</td>
          <td>❌</td>
          <td>✅</td>
      </tr>
      <tr>
          <td>Browser APIs</td>
          <td>❌</td>
          <td>✅</td>
      </tr>
      <tr>
          <td><code>async/await</code> in component body</td>
          <td>✅</td>
          <td>❌</td>
      </tr>
      <tr>
          <td>Can render other Server Components</td>
          <td>✅</td>
          <td>❌ (import only Client or pass as <code>children</code>)</td>
      </tr>
  </tbody>
</table>
<h3 id="the-donut-pattern-server-inside-client">The &ldquo;Donut&rdquo; Pattern: Server Inside Client</h3>
<p>You cannot import a Server Component inside a <code>'use client'</code> module — the import graph seals it into the client bundle. But you <strong>can</strong> pass a Server Component as <code>children</code> or a prop to a Client Component. This is the &ldquo;donut&rdquo; pattern: the Client Component is the donut (with a hole), and a Server Component fills the hole.</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-tsx" data-lang="tsx"><span style="display:flex;"><span><span style="color:#75715e">// ❌ Wrong: imports RSC inside client module
</span></span></span><span style="display:flex;"><span><span style="color:#75715e"></span><span style="color:#e6db74">&#39;use client&#39;</span>
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">import</span> <span style="color:#a6e22e">ServerSidebar</span> <span style="color:#66d9ef">from</span> <span style="color:#e6db74">&#39;./ServerSidebar&#39;</span> <span style="color:#75715e">// now bundled for client
</span></span></span><span style="display:flex;"><span><span style="color:#75715e"></span>
</span></span><span style="display:flex;"><span><span style="color:#75715e">// ✅ Correct: pass as children
</span></span></span><span style="display:flex;"><span><span style="color:#75715e">// layout.tsx (Server Component)
</span></span></span><span style="display:flex;"><span><span style="color:#75715e"></span><span style="color:#66d9ef">import</span> <span style="color:#a6e22e">ClientShell</span> <span style="color:#66d9ef">from</span> <span style="color:#e6db74">&#39;./ClientShell&#39;</span>
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">import</span> <span style="color:#a6e22e">ServerSidebar</span> <span style="color:#66d9ef">from</span> <span style="color:#e6db74">&#39;./ServerSidebar&#39;</span>
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">export</span> <span style="color:#66d9ef">default</span> <span style="color:#66d9ef">function</span> <span style="color:#a6e22e">Layout() {</span>
</span></span><span style="display:flex;"><span>  <span style="color:#66d9ef">return</span> (
</span></span><span style="display:flex;"><span>    &lt;<span style="color:#f92672">ClientShell</span>&gt;
</span></span><span style="display:flex;"><span>      &lt;<span style="color:#f92672">ServerSidebar</span> /&gt; {<span style="color:#75715e">/* stays server-only */</span>}
</span></span><span style="display:flex;"><span>    &lt;/<span style="color:#f92672">ClientShell</span>&gt;
</span></span><span style="display:flex;"><span>  )
</span></span><span style="display:flex;"><span>}
</span></span></code></pre></div><h2 id="setting-up-your-nextjs-app-router-project">Setting Up Your Next.js App Router Project</h2>
<p>Getting started with the App Router requires Next.js 13.4+ (stable), though 15.x is recommended for the full RSC, Server Actions, and PPR feature set. Create a new project with <code>npx create-next-app@latest --app</code> and choose TypeScript. The resulting structure places all routes under <code>app/</code>, with <code>page.tsx</code> as the route entry, <code>layout.tsx</code> for shared shells, <code>loading.tsx</code> for Suspense fallbacks, and <code>error.tsx</code> for error boundaries. Every file in <code>app/</code> is a Server Component by default — no annotation needed. The <code>public/</code> directory, <code>next.config.ts</code>, and <code>tailwind.config.ts</code> live at the root. TypeScript strict mode is enabled by default in new projects. Turbopack is now the default bundler in Next.js 15, delivering <strong>10x faster HMR and 4x faster production builds</strong> compared to webpack — you get this for free on new projects.</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-bash" data-lang="bash"><span style="display:flex;"><span>npx create-next-app@latest my-app <span style="color:#ae81ff">\
</span></span></span><span style="display:flex;"><span><span style="color:#ae81ff"></span>  --typescript --tailwind --eslint --app <span style="color:#ae81ff">\
</span></span></span><span style="display:flex;"><span><span style="color:#ae81ff"></span>  --src-dir --import-alias <span style="color:#e6db74">&#34;@/*&#34;</span>
</span></span><span style="display:flex;"><span>cd my-app <span style="color:#f92672">&amp;&amp;</span> npm run dev
</span></span></code></pre></div><h3 id="file-conventions-you-must-know">File Conventions You Must Know</h3>
<table>
  <thead>
      <tr>
          <th>File</th>
          <th>Purpose</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td><code>layout.tsx</code></td>
          <td>Shared UI wrapper (not remounted on navigation)</td>
      </tr>
      <tr>
          <td><code>page.tsx</code></td>
          <td>Route leaf, publicly addressable</td>
      </tr>
      <tr>
          <td><code>loading.tsx</code></td>
          <td>Auto-wrapped Suspense fallback for the route segment</td>
      </tr>
      <tr>
          <td><code>error.tsx</code></td>
          <td>Error boundary for the route segment (must be Client Component)</td>
      </tr>
      <tr>
          <td><code>not-found.tsx</code></td>
          <td>404 UI for the segment</td>
      </tr>
      <tr>
          <td><code>route.ts</code></td>
          <td>API endpoint (replaces <code>pages/api/</code>)</td>
      </tr>
      <tr>
          <td><code>template.tsx</code></td>
          <td>Like layout but remounts on navigation</td>
      </tr>
  </tbody>
</table>
<h2 id="data-fetching-patterns-sequential-vs-parallel-vs-streaming">Data Fetching Patterns: Sequential vs Parallel vs Streaming</h2>
<p>Data fetching in the App Router happens directly in <code>async</code> Server Components — no <code>getServerSideProps</code>, no custom hooks, no API layer needed for internal data. The <code>fetch()</code> function is extended by Next.js to support request deduplication and caching. The three patterns every developer needs: <strong>sequential</strong> (when request B depends on response A — unavoidable but keep it shallow), <strong>parallel</strong> (when requests are independent — always prefer), and <strong>streaming</strong> (when some data is slow and you want to unblock the rest of the UI). Sequential fetching is the default trap: <code>const user = await getUser(); const posts = await getPostsByUser(user.id)</code> serializes the requests, adding latency for every hop. For independent data, use <code>Promise.all()</code> to parallelize.</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-tsx" data-lang="tsx"><span style="display:flex;"><span><span style="color:#75715e">// ❌ Sequential — slow
</span></span></span><span style="display:flex;"><span><span style="color:#75715e"></span><span style="color:#66d9ef">async</span> <span style="color:#66d9ef">function</span> <span style="color:#a6e22e">UserDashboard</span>({ <span style="color:#a6e22e">userId</span> }<span style="color:#f92672">:</span> { <span style="color:#a6e22e">userId</span>: <span style="color:#66d9ef">string</span> }) {
</span></span><span style="display:flex;"><span>  <span style="color:#66d9ef">const</span> <span style="color:#a6e22e">user</span> <span style="color:#f92672">=</span> <span style="color:#66d9ef">await</span> <span style="color:#a6e22e">getUser</span>(<span style="color:#a6e22e">userId</span>)         <span style="color:#75715e">// 120ms
</span></span></span><span style="display:flex;"><span><span style="color:#75715e"></span>  <span style="color:#66d9ef">const</span> <span style="color:#a6e22e">posts</span> <span style="color:#f92672">=</span> <span style="color:#66d9ef">await</span> <span style="color:#a6e22e">getPostsByUser</span>(<span style="color:#a6e22e">userId</span>) <span style="color:#75715e">// +85ms = 205ms total
</span></span></span><span style="display:flex;"><span><span style="color:#75715e"></span>  <span style="color:#66d9ef">return</span> &lt;<span style="color:#f92672">div</span>&gt;...&lt;/<span style="color:#f92672">div</span>&gt;
</span></span><span style="display:flex;"><span>}
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#75715e">// ✅ Parallel — fast
</span></span></span><span style="display:flex;"><span><span style="color:#75715e"></span><span style="color:#66d9ef">async</span> <span style="color:#66d9ef">function</span> <span style="color:#a6e22e">UserDashboard</span>({ <span style="color:#a6e22e">userId</span> }<span style="color:#f92672">:</span> { <span style="color:#a6e22e">userId</span>: <span style="color:#66d9ef">string</span> }) {
</span></span><span style="display:flex;"><span>  <span style="color:#66d9ef">const</span> [<span style="color:#a6e22e">user</span>, <span style="color:#a6e22e">posts</span>] <span style="color:#f92672">=</span> <span style="color:#66d9ef">await</span> <span style="color:#a6e22e">Promise</span>.<span style="color:#a6e22e">all</span>([
</span></span><span style="display:flex;"><span>    <span style="color:#a6e22e">getUser</span>(<span style="color:#a6e22e">userId</span>),       <span style="color:#75715e">// 120ms
</span></span></span><span style="display:flex;"><span><span style="color:#75715e"></span>    <span style="color:#a6e22e">getPostsByUser</span>(<span style="color:#a6e22e">userId</span>) <span style="color:#75715e">// 85ms (runs concurrently)
</span></span></span><span style="display:flex;"><span><span style="color:#75715e"></span>  ])                       <span style="color:#75715e">// = 120ms total
</span></span></span><span style="display:flex;"><span><span style="color:#75715e"></span>  <span style="color:#66d9ef">return</span> &lt;<span style="color:#f92672">div</span>&gt;...&lt;/<span style="color:#f92672">div</span>&gt;
</span></span><span style="display:flex;"><span>}
</span></span></code></pre></div><h3 id="sibling-server-components-for-parallelism">Sibling Server Components for Parallelism</h3>
<p>The cleanest way to parallelize in React is <strong>sibling Server Components in separate Suspense boundaries</strong>. Each component fetches its own data independently, and React streams them as they resolve — no <code>Promise.all</code> needed at the parent level.</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-tsx" data-lang="tsx"><span style="display:flex;"><span><span style="color:#75715e">// Each section fetches independently and streams as ready
</span></span></span><span style="display:flex;"><span><span style="color:#75715e"></span><span style="color:#66d9ef">export</span> <span style="color:#66d9ef">default</span> <span style="color:#66d9ef">function</span> <span style="color:#a6e22e">Dashboard() {</span>
</span></span><span style="display:flex;"><span>  <span style="color:#66d9ef">return</span> (
</span></span><span style="display:flex;"><span>    &lt;&gt;
</span></span><span style="display:flex;"><span>      &lt;<span style="color:#f92672">Suspense</span> <span style="color:#a6e22e">fallback</span><span style="color:#f92672">=</span>{&lt;<span style="color:#f92672">MetricsSkeleton</span> /&gt;}&gt;
</span></span><span style="display:flex;"><span>        &lt;<span style="color:#f92672">MetricsPanel</span> /&gt;   {<span style="color:#75715e">/* fetches /api/metrics */</span>}
</span></span><span style="display:flex;"><span>      &lt;/<span style="color:#f92672">Suspense</span>&gt;
</span></span><span style="display:flex;"><span>      &lt;<span style="color:#f92672">Suspense</span> <span style="color:#a6e22e">fallback</span><span style="color:#f92672">=</span>{&lt;<span style="color:#f92672">ActivitySkeleton</span> /&gt;}&gt;
</span></span><span style="display:flex;"><span>        &lt;<span style="color:#f92672">ActivityFeed</span> /&gt;   {<span style="color:#75715e">/* fetches /api/activity */</span>}
</span></span><span style="display:flex;"><span>      &lt;/<span style="color:#f92672">Suspense</span>&gt;
</span></span><span style="display:flex;"><span>    &lt;/&gt;
</span></span><span style="display:flex;"><span>  )
</span></span><span style="display:flex;"><span>}
</span></span></code></pre></div><h2 id="streaming-with-suspense-making-slow-data-invisible-to-users">Streaming with Suspense: Making Slow Data Invisible to Users</h2>
<p>Streaming is the RSC feature that changes how users experience slow pages. Instead of waiting for every data fetch before sending any HTML, Next.js sends the static shell immediately and streams in dynamic sections as their data resolves. The result: users see meaningful content within milliseconds even if some API calls take 500ms+. Streaming uses the standard React <code>&lt;Suspense&gt;</code> boundary — wrap any slow Server Component in <code>&lt;Suspense fallback={&lt;Skeleton /&gt;}&gt;</code> and Next.js handles the rest. The <code>loading.tsx</code> file in any route segment automatically creates a top-level Suspense boundary for that entire segment. For granular control, add Suspense boundaries at the component level. Server Components can yield their output as a stream; Client Components hydrate after their corresponding HTML chunk arrives. In practice, streaming transforms a 1.2s white screen into a 100ms skeleton-to-content progression — a measurable conversion improvement for e-commerce and SaaS dashboards.</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-tsx" data-lang="tsx"><span style="display:flex;"><span><span style="color:#75715e">// app/dashboard/page.tsx
</span></span></span><span style="display:flex;"><span><span style="color:#75715e"></span><span style="color:#66d9ef">import</span> { <span style="color:#a6e22e">Suspense</span> } <span style="color:#66d9ef">from</span> <span style="color:#e6db74">&#39;react&#39;</span>
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">import</span> { <span style="color:#a6e22e">RevenueChart</span> } <span style="color:#66d9ef">from</span> <span style="color:#e6db74">&#39;@/components/RevenueChart&#39;</span>
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">import</span> { <span style="color:#a6e22e">RecentSales</span> } <span style="color:#66d9ef">from</span> <span style="color:#e6db74">&#39;@/components/RecentSales&#39;</span>
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">export</span> <span style="color:#66d9ef">default</span> <span style="color:#66d9ef">function</span> <span style="color:#a6e22e">DashboardPage() {</span>
</span></span><span style="display:flex;"><span>  <span style="color:#66d9ef">return</span> (
</span></span><span style="display:flex;"><span>    &lt;<span style="color:#f92672">main</span>&gt;
</span></span><span style="display:flex;"><span>      &lt;<span style="color:#f92672">h1</span>&gt;<span style="color:#a6e22e">Dashboard</span>&lt;/<span style="color:#f92672">h1</span>&gt;
</span></span><span style="display:flex;"><span>      {<span style="color:#75715e">/* Streams in as revenue data resolves */</span>}
</span></span><span style="display:flex;"><span>      &lt;<span style="color:#f92672">Suspense</span> <span style="color:#a6e22e">fallback</span><span style="color:#f92672">=</span>{&lt;<span style="color:#f92672">ChartSkeleton</span> /&gt;}&gt;
</span></span><span style="display:flex;"><span>        &lt;<span style="color:#f92672">RevenueChart</span> /&gt;
</span></span><span style="display:flex;"><span>      &lt;/<span style="color:#f92672">Suspense</span>&gt;
</span></span><span style="display:flex;"><span>      {<span style="color:#75715e">/* Streams in independently — doesn&#39;t wait for chart */</span>}
</span></span><span style="display:flex;"><span>      &lt;<span style="color:#f92672">Suspense</span> <span style="color:#a6e22e">fallback</span><span style="color:#f92672">=</span>{&lt;<span style="color:#f92672">SalesSkeleton</span> /&gt;}&gt;
</span></span><span style="display:flex;"><span>        &lt;<span style="color:#f92672">RecentSales</span> /&gt;
</span></span><span style="display:flex;"><span>      &lt;/<span style="color:#f92672">Suspense</span>&gt;
</span></span><span style="display:flex;"><span>    &lt;/<span style="color:#f92672">main</span>&gt;
</span></span><span style="display:flex;"><span>  )
</span></span><span style="display:flex;"><span>}
</span></span></code></pre></div><h3 id="loadingtsx-vs-manual-suspense">Loading.tsx vs Manual Suspense</h3>
<p><code>loading.tsx</code> wraps the entire page segment in Suspense automatically. Use it for page-level skeletons. Use manual <code>&lt;Suspense&gt;</code> at the component level when different sections of a page have different data speeds — the fast sections shouldn&rsquo;t wait for the slow ones.</p>
<h2 id="server-actions-replacing-api-routes-for-mutations">Server Actions: Replacing API Routes for Mutations</h2>
<p>Server Actions are async functions that run on the server but can be called directly from Client Components — including from <code>&lt;form action={...}&gt;</code> and from <code>onClick</code> handlers. They replace the traditional pattern of writing a <code>POST /api/...</code> route handler, calling it with <code>fetch()</code> from the client, and managing loading/error state manually. A Server Action is marked with <code>'use server'</code> at the top of the function body (or at the top of a file to mark all exports as server actions). Next.js wires the RPC call automatically. Critically, <code>'use server'</code> is <strong>not the same as &ldquo;Server Component&rdquo;</strong> — it&rsquo;s a directive that tells the bundler to extract this function into a server endpoint callable from the client. Server Actions integrate natively with form&rsquo;s <code>action</code> prop, <code>useActionState</code> hook (React 19), and Next.js <code>revalidatePath</code>/<code>revalidateTag</code> for cache invalidation — making full-stack mutations a first-class pattern without a separate API layer.</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-tsx" data-lang="tsx"><span style="display:flex;"><span><span style="color:#75715e">// app/actions.ts
</span></span></span><span style="display:flex;"><span><span style="color:#75715e"></span><span style="color:#e6db74">&#39;use server&#39;</span>
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">import</span> { <span style="color:#a6e22e">revalidatePath</span> } <span style="color:#66d9ef">from</span> <span style="color:#e6db74">&#39;next/cache&#39;</span>
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">import</span> { <span style="color:#a6e22e">db</span> } <span style="color:#66d9ef">from</span> <span style="color:#e6db74">&#39;@/lib/db&#39;</span>
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">export</span> <span style="color:#66d9ef">async</span> <span style="color:#66d9ef">function</span> <span style="color:#a6e22e">createPost</span>(<span style="color:#a6e22e">formData</span>: <span style="color:#66d9ef">FormData</span>) {
</span></span><span style="display:flex;"><span>  <span style="color:#66d9ef">const</span> <span style="color:#a6e22e">title</span> <span style="color:#f92672">=</span> <span style="color:#a6e22e">formData</span>.<span style="color:#66d9ef">get</span>(<span style="color:#e6db74">&#39;title&#39;</span>) <span style="color:#66d9ef">as</span> <span style="color:#66d9ef">string</span>
</span></span><span style="display:flex;"><span>  <span style="color:#66d9ef">await</span> <span style="color:#a6e22e">db</span>.<span style="color:#a6e22e">post</span>.<span style="color:#a6e22e">create</span>({ <span style="color:#a6e22e">data</span><span style="color:#f92672">:</span> { <span style="color:#a6e22e">title</span> } })
</span></span><span style="display:flex;"><span>  <span style="color:#a6e22e">revalidatePath</span>(<span style="color:#e6db74">&#39;/posts&#39;</span>)
</span></span><span style="display:flex;"><span>}
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#75715e">// app/posts/new/page.tsx (Server Component)
</span></span></span><span style="display:flex;"><span><span style="color:#75715e"></span><span style="color:#66d9ef">import</span> { <span style="color:#a6e22e">createPost</span> } <span style="color:#66d9ef">from</span> <span style="color:#e6db74">&#39;@/app/actions&#39;</span>
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">export</span> <span style="color:#66d9ef">default</span> <span style="color:#66d9ef">function</span> <span style="color:#a6e22e">NewPostPage() {</span>
</span></span><span style="display:flex;"><span>  <span style="color:#66d9ef">return</span> (
</span></span><span style="display:flex;"><span>    &lt;<span style="color:#f92672">form</span> <span style="color:#a6e22e">action</span><span style="color:#f92672">=</span>{<span style="color:#a6e22e">createPost</span>}&gt;
</span></span><span style="display:flex;"><span>      &lt;<span style="color:#f92672">input</span> <span style="color:#a6e22e">name</span><span style="color:#f92672">=</span><span style="color:#e6db74">&#34;title&#34;</span> <span style="color:#a6e22e">placeholder</span><span style="color:#f92672">=</span><span style="color:#e6db74">&#34;Post title&#34;</span> /&gt;
</span></span><span style="display:flex;"><span>      &lt;<span style="color:#f92672">button</span> <span style="color:#a6e22e">type</span><span style="color:#f92672">=</span><span style="color:#e6db74">&#34;submit&#34;</span>&gt;<span style="color:#a6e22e">Create</span>&lt;/<span style="color:#f92672">button</span>&gt;
</span></span><span style="display:flex;"><span>    &lt;/<span style="color:#f92672">form</span>&gt;
</span></span><span style="display:flex;"><span>  )
</span></span><span style="display:flex;"><span>}
</span></span></code></pre></div><h3 id="server-actions-vs-api-routes-when-to-use-each">Server Actions vs API Routes: When to Use Each</h3>
<p>Use Server Actions for form submissions, CRUD mutations from Client Components, and any mutation that should revalidate cached data in Next.js. Use <code>route.ts</code> API routes when you need to expose an endpoint to external consumers (mobile apps, third-party services, webhooks), when you need fine-grained HTTP control (status codes, headers, streaming responses), or when building a public REST API. For internal Next.js mutations, Server Actions eliminate the client/server boilerplate entirely.</p>
<h2 id="caching-in-the-app-router-fetch-unstable_cache-and-use-cache">Caching in the App Router: fetch(), unstable_cache, and use cache</h2>
<p>Next.js App Router has one of the most powerful and one of the most confusing caching systems in frontend frameworks. There are four layers: <strong>Request Memoization</strong> (deduplicates identical <code>fetch()</code> calls within a single render), <strong>Data Cache</strong> (persists <code>fetch()</code> responses across requests — opt out with <code>{ cache: 'no-store' }</code>), <strong>Full Route Cache</strong> (caches rendered HTML at build time for static routes), and <strong>Router Cache</strong> (client-side cache of visited route segments). By default, <code>fetch()</code> in Server Components is cached indefinitely — which surprised many developers upgrading from Pages Router. Next.js 15 changed the default to <code>no-store</code> for <code>fetch()</code> to reduce confusion. For database queries and ORM calls that don&rsquo;t go through <code>fetch()</code>, use <code>unstable_cache</code> (stable in Next.js 15 despite the name) or the new <code>use cache</code> directive (experimental in 15.x). Tag-based invalidation with <code>revalidateTag()</code> is the most surgical caching approach: tag your cached data, then invalidate exactly those tags from a Server Action when data changes.</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-tsx" data-lang="tsx"><span style="display:flex;"><span><span style="color:#66d9ef">import</span> { <span style="color:#a6e22e">unstable_cache</span> } <span style="color:#66d9ef">from</span> <span style="color:#e6db74">&#39;next/cache&#39;</span>
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#75715e">// Cached for 1 hour, tagged for invalidation
</span></span></span><span style="display:flex;"><span><span style="color:#75715e"></span><span style="color:#66d9ef">const</span> <span style="color:#a6e22e">getProductById</span> <span style="color:#f92672">=</span> <span style="color:#a6e22e">unstable_cache</span>(
</span></span><span style="display:flex;"><span>  <span style="color:#66d9ef">async</span> (<span style="color:#a6e22e">id</span>: <span style="color:#66d9ef">string</span>) <span style="color:#f92672">=&gt;</span> <span style="color:#a6e22e">db</span>.<span style="color:#a6e22e">product</span>.<span style="color:#a6e22e">findUnique</span>({ <span style="color:#a6e22e">where</span><span style="color:#f92672">:</span> { <span style="color:#a6e22e">id</span> } }),
</span></span><span style="display:flex;"><span>  [<span style="color:#e6db74">&#39;product&#39;</span>],
</span></span><span style="display:flex;"><span>  { <span style="color:#a6e22e">revalidate</span>: <span style="color:#66d9ef">3600</span>, <span style="color:#a6e22e">tags</span><span style="color:#f92672">:</span> [<span style="color:#e6db74">&#39;products&#39;</span>] }
</span></span><span style="display:flex;"><span>)
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#75715e">// Server Action to invalidate
</span></span></span><span style="display:flex;"><span><span style="color:#75715e"></span><span style="color:#e6db74">&#39;use server&#39;</span>
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">import</span> { <span style="color:#a6e22e">revalidateTag</span> } <span style="color:#66d9ef">from</span> <span style="color:#e6db74">&#39;next/cache&#39;</span>
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">export</span> <span style="color:#66d9ef">async</span> <span style="color:#66d9ef">function</span> <span style="color:#a6e22e">updateProduct</span>(<span style="color:#a6e22e">id</span>: <span style="color:#66d9ef">string</span>, <span style="color:#a6e22e">data</span>: <span style="color:#66d9ef">Partial</span>&lt;<span style="color:#f92672">Product</span>&gt;) {
</span></span><span style="display:flex;"><span>  <span style="color:#66d9ef">await</span> <span style="color:#a6e22e">db</span>.<span style="color:#a6e22e">product</span>.<span style="color:#a6e22e">update</span>({ <span style="color:#a6e22e">where</span><span style="color:#f92672">:</span> { <span style="color:#a6e22e">id</span> }, <span style="color:#a6e22e">data</span> })
</span></span><span style="display:flex;"><span>  <span style="color:#a6e22e">revalidateTag</span>(<span style="color:#e6db74">&#39;products&#39;</span>) <span style="color:#75715e">// clears all cached queries tagged &#39;products&#39;
</span></span></span><span style="display:flex;"><span><span style="color:#75715e"></span>}
</span></span></code></pre></div><h3 id="the-four-caching-layers-at-a-glance">The Four Caching Layers at a Glance</h3>
<table>
  <thead>
      <tr>
          <th>Layer</th>
          <th>What It Caches</th>
          <th>Duration</th>
          <th>Opt Out</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>Request Memoization</td>
          <td><code>fetch()</code> within single render</td>
          <td>Single request</td>
          <td>Automatic</td>
      </tr>
      <tr>
          <td>Data Cache</td>
          <td><code>fetch()</code> responses</td>
          <td>Persistent</td>
          <td><code>cache: 'no-store'</code></td>
      </tr>
      <tr>
          <td>Full Route Cache</td>
          <td>Rendered HTML</td>
          <td>Until redeploy / revalidate</td>
          <td><code>export const dynamic = 'force-dynamic'</code></td>
      </tr>
      <tr>
          <td>Router Cache</td>
          <td>Client-side route segments</td>
          <td>Session / 30s (dynamic)</td>
          <td><code>router.refresh()</code></td>
      </tr>
  </tbody>
</table>
<h2 id="partial-prerendering-ppr-the-best-of-static-and-dynamic">Partial Prerendering (PPR): The Best of Static and Dynamic</h2>
<p>Partial Prerendering (PPR) is Next.js&rsquo;s answer to the long-standing tradeoff between static (fast, stale) and dynamic (slow, fresh) rendering. With PPR, a single route can serve a <strong>static shell from the CDN edge</strong> (TTFB under 50ms) while <strong>streaming dynamic sections</strong> on request as they resolve — on the same page, with no layout shift. The static shell is pre-rendered at build time and cached at the edge; each Suspense boundary marks a &ldquo;hole&rdquo; that fills dynamically. This is fundamentally different from ISR (Incremental Static Regeneration), which regenerates the whole page. PPR regenerates nothing — the shell is always static, the holes are always fresh. Enable PPR in <code>next.config.ts</code> with <code>experimental: { ppr: true }</code> (stable in Next.js 15 for opt-in routes with <code>export const experimental_ppr = true</code>). With PPR, static shell TTFB from CDN edge can be <strong>under 50ms</strong>, while dynamic sections stream in separately on request (samcheek.com, 2026).</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-ts" data-lang="ts"><span style="display:flex;"><span><span style="color:#75715e">// next.config.ts
</span></span></span><span style="display:flex;"><span><span style="color:#75715e"></span><span style="color:#66d9ef">import</span> <span style="color:#66d9ef">type</span> { <span style="color:#a6e22e">NextConfig</span> } <span style="color:#66d9ef">from</span> <span style="color:#e6db74">&#39;next&#39;</span>
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">const</span> <span style="color:#a6e22e">config</span>: <span style="color:#66d9ef">NextConfig</span> <span style="color:#f92672">=</span> {
</span></span><span style="display:flex;"><span>  <span style="color:#a6e22e">experimental</span><span style="color:#f92672">:</span> {
</span></span><span style="display:flex;"><span>    <span style="color:#a6e22e">ppr</span><span style="color:#f92672">:</span> <span style="color:#e6db74">&#39;incremental&#39;</span>, <span style="color:#75715e">// opt-in per route in Next.js 15
</span></span></span><span style="display:flex;"><span><span style="color:#75715e"></span>  },
</span></span><span style="display:flex;"><span>}
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">export</span> <span style="color:#66d9ef">default</span> <span style="color:#a6e22e">config</span>
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#75715e">// app/product/[id]/page.tsx
</span></span></span><span style="display:flex;"><span><span style="color:#75715e"></span><span style="color:#66d9ef">export</span> <span style="color:#66d9ef">const</span> <span style="color:#a6e22e">experimental_ppr</span> <span style="color:#f92672">=</span> <span style="color:#66d9ef">true</span> <span style="color:#75715e">// enable PPR for this route
</span></span></span><span style="display:flex;"><span><span style="color:#75715e"></span>
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">import</span> { <span style="color:#a6e22e">Suspense</span> } <span style="color:#66d9ef">from</span> <span style="color:#e6db74">&#39;react&#39;</span>
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">import</span> { <span style="color:#a6e22e">StaticProductInfo</span> } <span style="color:#66d9ef">from</span> <span style="color:#e6db74">&#39;@/components/StaticProductInfo&#39;</span>
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">import</span> { <span style="color:#a6e22e">DynamicInventory</span> } <span style="color:#66d9ef">from</span> <span style="color:#e6db74">&#39;@/components/DynamicInventory&#39;</span>
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">export</span> <span style="color:#66d9ef">default</span> <span style="color:#66d9ef">function</span> <span style="color:#a6e22e">ProductPage</span>({ <span style="color:#a6e22e">params</span> }<span style="color:#f92672">:</span> { <span style="color:#a6e22e">params</span><span style="color:#f92672">:</span> { <span style="color:#a6e22e">id</span>: <span style="color:#66d9ef">string</span> } }) {
</span></span><span style="display:flex;"><span>  <span style="color:#66d9ef">return</span> (
</span></span><span style="display:flex;"><span>    &lt;&gt;
</span></span><span style="display:flex;"><span>      {<span style="color:#75715e">/* Served from edge cache immediately */</span>}
</span></span><span style="display:flex;"><span>      &lt;<span style="color:#f92672">StaticProductInfo</span> <span style="color:#a6e22e">productId</span><span style="color:#f92672">=</span>{<span style="color:#a6e22e">params</span>.<span style="color:#a6e22e">id</span>} /&gt;
</span></span><span style="display:flex;"><span>      {<span style="color:#75715e">/* Streams in fresh on every request */</span>}
</span></span><span style="display:flex;"><span>      &lt;<span style="color:#f92672">Suspense</span> <span style="color:#a6e22e">fallback</span><span style="color:#f92672">=</span>{&lt;<span style="color:#f92672">InventorySkeleton</span> /&gt;}&gt;
</span></span><span style="display:flex;"><span>        &lt;<span style="color:#f92672">DynamicInventory</span> <span style="color:#a6e22e">productId</span><span style="color:#f92672">=</span>{<span style="color:#a6e22e">params</span>.<span style="color:#a6e22e">id</span>} /&gt;
</span></span><span style="display:flex;"><span>      &lt;/<span style="color:#f92672">Suspense</span>&gt;
</span></span><span style="display:flex;"><span>    &lt;/&gt;
</span></span><span style="display:flex;"><span>  )
</span></span><span style="display:flex;"><span>}
</span></span></code></pre></div><h3 id="ppr-vs-isr-vs-static-vs-dynamic">PPR vs ISR vs Static vs Dynamic</h3>
<table>
  <thead>
      <tr>
          <th>Strategy</th>
          <th>TTFB</th>
          <th>Freshness</th>
          <th>When to Use</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>Static (<code>force-static</code>)</td>
          <td>~20ms CDN</td>
          <td>Build time</td>
          <td>Marketing pages, docs</td>
      </tr>
      <tr>
          <td>ISR</td>
          <td>~20ms CDN</td>
          <td>Stale up to revalidate period</td>
          <td>Blog posts, product pages</td>
      </tr>
      <tr>
          <td>PPR</td>
          <td>~50ms CDN</td>
          <td>Shell static, holes fresh</td>
          <td>Mixed static/dynamic per page</td>
      </tr>
      <tr>
          <td>Dynamic (<code>force-dynamic</code>)</td>
          <td>~200ms+ origin</td>
          <td>Always fresh</td>
          <td>Dashboards, user-specific pages</td>
      </tr>
  </tbody>
</table>
<h2 id="7-common-react-server-component-mistakes-and-how-to-fix-them">7 Common React Server Component Mistakes (and How to Fix Them)</h2>
<p>These are the seven mistakes that senior Next.js developers catch in code review most often. Each silently increases bundle size, breaks builds at runtime, or creates subtle correctness issues that only appear in production. The RSC model is strict about the server/client boundary — the bundler enforces it, not the developer. Crossing the boundary incorrectly produces some of the least actionable error messages in the React ecosystem. Understanding these patterns in advance prevents hours of debugging.</p>
<p><strong>Mistake 1: Putting <code>'use client'</code> on a parent that contains only one interactive leaf.</strong></p>
<p>The fix: extract the single interactive element into its own Client Component file. The parent stays server-side and keeps all its data fetching out of the bundle.</p>
<p><strong>Mistake 2: Importing a Server Component inside a <code>'use client'</code> module.</strong></p>
<p>This silently promotes the Server Component to a Client Component, bundling its code and losing its data fetching benefits. Fix: pass as <code>children</code> prop (the donut pattern).</p>
<p><strong>Mistake 3: Using the Context API across the server-client boundary.</strong></p>
<p>Context is a client-side mechanism. Providers must live in Client Components. The most common pattern is a <code>Providers</code> Client Component wrapping the app in <code>layout.tsx</code> with all client providers.</p>
<p><strong>Mistake 4: Accessing <code>cookies()</code>, <code>headers()</code>, or <code>searchParams</code> inside a cached component.</strong></p>
<p>These are dynamic APIs. Calling them inside a statically cached route will throw at build time. Either mark the route <code>force-dynamic</code> or move the dynamic read to the nearest Suspense-bounded Server Component.</p>
<p><strong>Mistake 5: Passing non-serializable props (functions, class instances) from Server to Client Components.</strong></p>
<p>Props cross the wire as JSON. Functions, Dates (use ISO strings instead), class instances, and <code>undefined</code> values throw serialization errors. Use <code>Date.toISOString()</code>, plain objects, and primitives only.</p>
<p><strong>Mistake 6: Creating waterfall fetches inside a single async Server Component.</strong></p>
<p>Sequential <code>await</code> calls are fine when the second depends on the first, but avoid them when requests are independent. Use <code>Promise.all()</code> or sibling Suspense components for parallel execution.</p>
<p><strong>Mistake 7: Ignoring cache semantics and fetching fresh data that should be cached.</strong></p>
<p>The inverse problem: marking everything <code>no-store</code> when most data could be cached and revalidated on demand. Use <code>unstable_cache</code> with <code>revalidateTag()</code> for database queries and set explicit <code>revalidate</code> values.</p>
<h2 id="testing-server-components-and-server-actions">Testing Server Components and Server Actions</h2>
<p>Testing React Server Components requires a different approach from standard component testing because they&rsquo;re async and run in a Node.js environment, not a browser. As of 2026, the testing ecosystem is catching up but is not fully mature — testing gaps remain a significant adoption barrier for some teams. For Server Components, use <strong>Vitest with React Testing Library</strong> (RTL) version 15+, which added async component rendering support, or <strong>Playwright</strong> for end-to-end tests that test the full rendering pipeline including streaming. For Server Actions, you can unit test them directly in Vitest by importing and calling them with a mocked database — they&rsquo;re just async functions. Jest with <code>@testing-library/react</code> requires the <code>experimental-vm-modules</code> flag for ESM support. The recommended setup in 2026 is Vitest + RTL for unit/integration and Playwright for end-to-end.</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-tsx" data-lang="tsx"><span style="display:flex;"><span><span style="color:#75715e">// __tests__/ProductCard.test.tsx
</span></span></span><span style="display:flex;"><span><span style="color:#75715e"></span><span style="color:#66d9ef">import</span> { <span style="color:#a6e22e">render</span>, <span style="color:#a6e22e">screen</span> } <span style="color:#66d9ef">from</span> <span style="color:#e6db74">&#39;@testing-library/react&#39;</span>
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">import</span> { <span style="color:#a6e22e">describe</span>, <span style="color:#a6e22e">it</span>, <span style="color:#a6e22e">expect</span>, <span style="color:#a6e22e">vi</span> } <span style="color:#66d9ef">from</span> <span style="color:#e6db74">&#39;vitest&#39;</span>
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">import</span> { <span style="color:#a6e22e">ProductCard</span> } <span style="color:#66d9ef">from</span> <span style="color:#e6db74">&#39;@/components/ProductCard&#39;</span>
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#75715e">// Mock the database call
</span></span></span><span style="display:flex;"><span><span style="color:#75715e"></span><span style="color:#a6e22e">vi</span>.<span style="color:#a6e22e">mock</span>(<span style="color:#e6db74">&#39;@/lib/db&#39;</span>, () <span style="color:#f92672">=&gt;</span> ({
</span></span><span style="display:flex;"><span>  <span style="color:#a6e22e">db</span><span style="color:#f92672">:</span> { <span style="color:#a6e22e">product</span><span style="color:#f92672">:</span> { <span style="color:#a6e22e">findUnique</span>: <span style="color:#66d9ef">vi.fn</span>().<span style="color:#a6e22e">mockResolvedValue</span>({ <span style="color:#a6e22e">id</span><span style="color:#f92672">:</span> <span style="color:#e6db74">&#39;1&#39;</span>, <span style="color:#a6e22e">name</span><span style="color:#f92672">:</span> <span style="color:#e6db74">&#39;Widget&#39;</span>, <span style="color:#a6e22e">price</span>: <span style="color:#66d9ef">29.99</span> }) } }
</span></span><span style="display:flex;"><span>}))
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#a6e22e">describe</span>(<span style="color:#e6db74">&#39;ProductCard&#39;</span>, () <span style="color:#f92672">=&gt;</span> {
</span></span><span style="display:flex;"><span>  <span style="color:#a6e22e">it</span>(<span style="color:#e6db74">&#39;renders product details&#39;</span>, <span style="color:#66d9ef">async</span> () <span style="color:#f92672">=&gt;</span> {
</span></span><span style="display:flex;"><span>    <span style="color:#66d9ef">const</span> <span style="color:#a6e22e">jsx</span> <span style="color:#f92672">=</span> <span style="color:#66d9ef">await</span> <span style="color:#a6e22e">ProductCard</span>({ <span style="color:#a6e22e">productId</span><span style="color:#f92672">:</span> <span style="color:#e6db74">&#39;1&#39;</span> })
</span></span><span style="display:flex;"><span>    <span style="color:#a6e22e">render</span>(<span style="color:#a6e22e">jsx</span>)
</span></span><span style="display:flex;"><span>    <span style="color:#a6e22e">expect</span>(<span style="color:#a6e22e">screen</span>.<span style="color:#a6e22e">getByText</span>(<span style="color:#e6db74">&#39;Widget&#39;</span>)).<span style="color:#a6e22e">toBeInTheDocument</span>()
</span></span><span style="display:flex;"><span>    <span style="color:#a6e22e">expect</span>(<span style="color:#a6e22e">screen</span>.<span style="color:#a6e22e">getByText</span>(<span style="color:#e6db74">&#39;$29.99&#39;</span>)).<span style="color:#a6e22e">toBeInTheDocument</span>()
</span></span><span style="display:flex;"><span>  })
</span></span><span style="display:flex;"><span>})
</span></span></code></pre></div><h3 id="testing-server-actions-directly">Testing Server Actions Directly</h3>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-tsx" data-lang="tsx"><span style="display:flex;"><span><span style="color:#75715e">// __tests__/actions.test.ts
</span></span></span><span style="display:flex;"><span><span style="color:#75715e"></span><span style="color:#66d9ef">import</span> { <span style="color:#a6e22e">describe</span>, <span style="color:#a6e22e">it</span>, <span style="color:#a6e22e">expect</span>, <span style="color:#a6e22e">vi</span>, <span style="color:#a6e22e">beforeEach</span> } <span style="color:#66d9ef">from</span> <span style="color:#e6db74">&#39;vitest&#39;</span>
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">import</span> { <span style="color:#a6e22e">createPost</span> } <span style="color:#66d9ef">from</span> <span style="color:#e6db74">&#39;@/app/actions&#39;</span>
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">import</span> { <span style="color:#a6e22e">revalidatePath</span> } <span style="color:#66d9ef">from</span> <span style="color:#e6db74">&#39;next/cache&#39;</span>
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#a6e22e">vi</span>.<span style="color:#a6e22e">mock</span>(<span style="color:#e6db74">&#39;next/cache&#39;</span>)
</span></span><span style="display:flex;"><span><span style="color:#a6e22e">vi</span>.<span style="color:#a6e22e">mock</span>(<span style="color:#e6db74">&#39;@/lib/db&#39;</span>, () <span style="color:#f92672">=&gt;</span> ({ <span style="color:#a6e22e">db</span><span style="color:#f92672">:</span> { <span style="color:#a6e22e">post</span><span style="color:#f92672">:</span> { <span style="color:#a6e22e">create</span>: <span style="color:#66d9ef">vi.fn</span>() } } }))
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#a6e22e">describe</span>(<span style="color:#e6db74">&#39;createPost&#39;</span>, () <span style="color:#f92672">=&gt;</span> {
</span></span><span style="display:flex;"><span>  <span style="color:#a6e22e">it</span>(<span style="color:#e6db74">&#39;creates post and revalidates path&#39;</span>, <span style="color:#66d9ef">async</span> () <span style="color:#f92672">=&gt;</span> {
</span></span><span style="display:flex;"><span>    <span style="color:#66d9ef">const</span> <span style="color:#a6e22e">formData</span> <span style="color:#f92672">=</span> <span style="color:#66d9ef">new</span> <span style="color:#a6e22e">FormData</span>()
</span></span><span style="display:flex;"><span>    <span style="color:#a6e22e">formData</span>.<span style="color:#a6e22e">append</span>(<span style="color:#e6db74">&#39;title&#39;</span>, <span style="color:#e6db74">&#39;Test Post&#39;</span>)
</span></span><span style="display:flex;"><span>    <span style="color:#66d9ef">await</span> <span style="color:#a6e22e">createPost</span>(<span style="color:#a6e22e">formData</span>)
</span></span><span style="display:flex;"><span>    <span style="color:#a6e22e">expect</span>(<span style="color:#a6e22e">revalidatePath</span>).<span style="color:#a6e22e">toHaveBeenCalledWith</span>(<span style="color:#e6db74">&#39;/posts&#39;</span>)
</span></span><span style="display:flex;"><span>  })
</span></span><span style="display:flex;"><span>})
</span></span></code></pre></div><h2 id="migrating-from-pages-router-to-app-router">Migrating from Pages Router to App Router</h2>
<p>Migrating a production Next.js application from Pages Router to App Router is incremental — Next.js explicitly supports running both routers in the same application. The Pages Router at <code>pages/</code> and the App Router at <code>app/</code> coexist. Start by moving layouts: the <code>_app.tsx</code> global wrapper becomes <code>app/layout.tsx</code>, and <code>_document.tsx</code> disappears (its functionality is built into the root layout). Route files move from <code>pages/route.tsx</code> to <code>app/route/page.tsx</code>. <code>getServerSideProps</code> and <code>getStaticProps</code> are replaced by <code>async</code> Server Components that fetch directly. <code>getStaticPaths</code> becomes <code>generateStaticParams</code>. API routes move from <code>pages/api/</code> to <code>app/api/route.ts</code> with named HTTP method exports. The biggest mental model shift is middleware and <code>useRouter</code> — import <code>useRouter</code> from <code>next/navigation</code> instead of <code>next/router</code>, and <code>router.push</code> behavior differences around shallow routing require careful testing.</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-ts" data-lang="ts"><span style="display:flex;"><span><span style="color:#75715e">// Before: pages/products/[id].tsx
</span></span></span><span style="display:flex;"><span><span style="color:#75715e"></span><span style="color:#66d9ef">export</span> <span style="color:#66d9ef">async</span> <span style="color:#66d9ef">function</span> <span style="color:#a6e22e">getServerSideProps</span>({ <span style="color:#a6e22e">params</span> }) {
</span></span><span style="display:flex;"><span>  <span style="color:#66d9ef">const</span> <span style="color:#a6e22e">product</span> <span style="color:#f92672">=</span> <span style="color:#66d9ef">await</span> <span style="color:#a6e22e">getProduct</span>(<span style="color:#a6e22e">params</span>.<span style="color:#a6e22e">id</span>)
</span></span><span style="display:flex;"><span>  <span style="color:#66d9ef">return</span> { <span style="color:#a6e22e">props</span><span style="color:#f92672">:</span> { <span style="color:#a6e22e">product</span> } }
</span></span><span style="display:flex;"><span>}
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">export</span> <span style="color:#66d9ef">default</span> <span style="color:#66d9ef">function</span> <span style="color:#a6e22e">ProductPage</span>({ <span style="color:#a6e22e">product</span> }) { ... }
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#75715e">// After: app/products/[id]/page.tsx
</span></span></span><span style="display:flex;"><span><span style="color:#75715e"></span><span style="color:#66d9ef">export</span> <span style="color:#66d9ef">default</span> <span style="color:#66d9ef">async</span> <span style="color:#66d9ef">function</span> <span style="color:#a6e22e">ProductPage</span>({ <span style="color:#a6e22e">params</span> }<span style="color:#f92672">:</span> { <span style="color:#a6e22e">params</span><span style="color:#f92672">:</span> { <span style="color:#a6e22e">id</span>: <span style="color:#66d9ef">string</span> } }) {
</span></span><span style="display:flex;"><span>  <span style="color:#66d9ef">const</span> <span style="color:#a6e22e">product</span> <span style="color:#f92672">=</span> <span style="color:#66d9ef">await</span> <span style="color:#a6e22e">getProduct</span>(<span style="color:#a6e22e">params</span>.<span style="color:#a6e22e">id</span>) <span style="color:#75715e">// fetch directly
</span></span></span><span style="display:flex;"><span><span style="color:#75715e"></span>  <span style="color:#66d9ef">return</span> &lt;<span style="color:#f92672">ProductView</span> <span style="color:#a6e22e">product</span><span style="color:#f92672">=</span>{<span style="color:#a6e22e">product</span>} /&gt;
</span></span><span style="display:flex;"><span>}
</span></span></code></pre></div><h3 id="migration-checklist">Migration Checklist</h3>
<ul>
<li><input disabled="" type="checkbox"> Add <code>app/</code> directory alongside <code>pages/</code> — both coexist</li>
<li><input disabled="" type="checkbox"> Create <code>app/layout.tsx</code> with <code>&lt;html&gt;</code> and <code>&lt;body&gt;</code> tags</li>
<li><input disabled="" type="checkbox"> Migrate routes one by one, starting with the simplest static pages</li>
<li><input disabled="" type="checkbox"> Replace <code>getServerSideProps</code> with <code>async</code> Server Component data fetching</li>
<li><input disabled="" type="checkbox"> Replace <code>getStaticProps</code> + <code>getStaticPaths</code> with Server Component + <code>generateStaticParams</code></li>
<li><input disabled="" type="checkbox"> Move client-side interactivity to leaf <code>'use client'</code> components</li>
<li><input disabled="" type="checkbox"> Update <code>useRouter</code> imports to <code>next/navigation</code></li>
<li><input disabled="" type="checkbox"> Replace <code>pages/api/</code> routes with <code>app/api/route.ts</code> for public endpoints</li>
<li><input disabled="" type="checkbox"> Convert internal mutations to Server Actions</li>
<li><input disabled="" type="checkbox"> Update middleware if you rely on <code>experimental.appDir</code> routing behavior</li>
</ul>
<hr>
<h2 id="frequently-asked-questions">Frequently Asked Questions</h2>
<p>These are the questions developers ask most frequently when adopting React Server Components in Next.js App Router. RSC introduces a new set of invariants — the server/client boundary, serialization rules, caching defaults, and state manager placement — that break intuitions built on the Pages Router and traditional React SPAs. The confusion is compounded by overlapping terminology: &ldquo;Server Component,&rdquo; &ldquo;Server Action,&rdquo; <code>'use server'</code>, and SSR all sound related but mean distinct things. Getting these fundamentals clear eliminates the majority of runtime errors and cryptic build failures teams encounter in their first month with App Router. Each question below addresses a real adoption blocker reported by developers in the State of React 2025 survey and community discussions. Context API incompatibility alone accounts for 59 developer mentions as the single biggest pain point — so understanding the boundary rules is not optional for production teams.</p>
<h3 id="do-i-have-to-use-the-app-router-if-im-on-nextjs-15">Do I have to use the App Router if I&rsquo;m on Next.js 15?</h3>
<p>No. Next.js 15 ships both the App Router and Pages Router, and both receive updates. The Pages Router is not deprecated. The App Router is the recommended direction for new applications, but migration is strictly opt-in and incremental — you can run both simultaneously in the same project during migration.</p>
<h3 id="can-i-use-redux-zustand-or-other-state-managers-with-server-components">Can I use Redux, Zustand, or other state managers with Server Components?</h3>
<p>Yes, but only in Client Components. State managers that rely on <code>useState</code>, context, or browser APIs must live inside <code>'use client'</code> modules. The standard pattern is to keep your state provider in a Client Component wrapper at the root of your layout and access the store only from other Client Components. Server Components have no state — data flows in through props, fetch calls, or direct database reads.</p>
<h3 id="whats-the-difference-between-use-server-and-a-server-component">What&rsquo;s the difference between <code>'use server'</code> and a Server Component?</h3>
<p>A Server Component is any component in the <code>app/</code> directory that does not have <code>'use client'</code> at the top. It renders on the server. <code>'use server'</code> is a directive you add to an <code>async</code> function (or a file of functions) to create a <strong>Server Action</strong> — a server-callable RPC endpoint that Client Components can invoke for mutations. They are different concepts: Server Component = rendering primitive; <code>'use server'</code> = callable server function.</p>
<h3 id="why-did-my-fetch-stop-caching-in-nextjs-15">Why did my <code>fetch()</code> stop caching in Next.js 15?</h3>
<p>Next.js 15 changed the default <code>cache</code> behavior of <code>fetch()</code> from <code>force-cache</code> (cached indefinitely) to <code>no-store</code> (always fresh). This was a breaking change from Next.js 13/14 behavior. To restore caching, explicitly pass <code>{ cache: 'force-cache' }</code> or <code>{ next: { revalidate: 3600 } }</code> to your <code>fetch()</code> calls.</p>
<h3 id="how-do-i-share-data-between-a-server-component-and-a-client-component-without-prop-drilling">How do I share data between a Server Component and a Client Component without prop drilling?</h3>
<p>Use React&rsquo;s <code>cache()</code> function to memoize a data-fetching function, then call it in both the Server Component (for the initial render) and the Client Component (via Server Action). For UI state that only lives client-side, use Zustand or Jotai in Client Components. For server-to-client data that doesn&rsquo;t need reactivity, pass it as serializable props from the parent Server Component to the child Client Component.</p>
]]></content:encoded></item><item><title>Vercel AI SDK Guide 2026: Build Streaming AI Apps in TypeScript With One SDK</title><link>https://baeseokjae.github.io/posts/vercel-ai-sdk-guide-2026/</link><pubDate>Wed, 22 Apr 2026 05:37:21 +0000</pubDate><guid>https://baeseokjae.github.io/posts/vercel-ai-sdk-guide-2026/</guid><description>Complete guide to Vercel AI SDK in 2026 — streaming, tool calling, structured output, agents, and production deployment with code examples.</description><content:encoded><![CDATA[<p>The Vercel AI SDK is a unified TypeScript library that lets you build streaming AI applications across OpenAI, Anthropic, Google, and 13+ other providers without rewriting your core logic when you switch models. Install it once, pick your provider, and ship production-ready AI features in hours instead of days.</p>
<h2 id="what-is-the-vercel-ai-sdk-and-why-it-matters-in-2026">What Is the Vercel AI SDK and Why It Matters in 2026</h2>
<p>The Vercel AI SDK is an open-source TypeScript toolkit for building AI-powered web applications with a provider-agnostic API, first-class streaming support, and framework-native UI hooks. As of April 2026, it has 11.5 million weekly npm downloads, 23.7K GitHub stars, and 614+ contributors — making it the most widely adopted TypeScript AI library for web developers. The SDK is organized into three layers: AI SDK Core handles server-side text generation, object generation, and tool calling; AI SDK UI provides React/Vue/Svelte hooks like <code>useChat</code> and <code>useCompletion</code> for building chat interfaces without managing stream state; and AI SDK RSC integrates with React Server Components for edge-compatible generative UI. The SDK supports 100+ LLM models across 16+ providers via the Vercel AI Gateway, including OpenAI GPT-4o, Anthropic Claude, Google Gemini, and open models on Together/Groq. In 2026 Vercel added three major features on top: Workflows (long-running durable agents), Sandbox (secure agent code execution), and AI Elements (prebuilt UI components). OpenCode — one of the most popular open-source coding agents — is built entirely on AI SDK, which validates its production-grade viability.</p>
<h3 id="the-three-layer-architecture">The Three-Layer Architecture</h3>
<p>The SDK cleanly separates concerns: Core runs on the server or edge, UI runs on the client, and RSC bridges the two with streaming server components. This separation means you can adopt incrementally — start with Core for a simple API route, add UI hooks when you need chat state management, and layer in RSC if you need server-driven generative UI.</p>
<h3 id="how-it-fits-the-vercel-ecosystem">How It Fits the Vercel Ecosystem</h3>
<p>AI Gateway gives you one API key to access 100+ models with automatic fallbacks and rate limit management. Sandbox provides a secure Node.js environment for agents that need to execute code. Workflows lets agents suspend and resume across function invocations, solving the serverless timeout problem for long-running tasks.</p>
<h2 id="getting-started-installing-and-configuring-ai-sdk">Getting Started: Installing and Configuring AI SDK</h2>
<p>Getting started with the Vercel AI SDK requires installing the <code>ai</code> core package plus one or more provider adapters. The setup takes under five minutes for a Next.js project and works equally well in any Node.js or edge runtime environment. The provider adapter pattern is the key architectural decision: you import a model from its provider package and pass it to AI SDK functions, meaning you can swap from OpenAI to Anthropic by changing a single import and model string — your business logic stays untouched. This design was explicitly chosen to prevent vendor lock-in, and in practice it means you can A/B test models in production, build fallback chains, or migrate providers without refactoring your entire codebase. The package size is small — <code>ai</code> is under 200KB minified — and it is designed to run on Vercel Edge Functions, Cloudflare Workers, and standard Node.js without adaptation. For new projects, the recommended starting point is a Next.js App Router app with the <code>edge</code> runtime on API routes, which gives you global distribution and sub-100ms cold starts.</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-bash" data-lang="bash"><span style="display:flex;"><span>npm install ai @ai-sdk/openai @ai-sdk/anthropic @ai-sdk/google
</span></span></code></pre></div><div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-typescript" data-lang="typescript"><span style="display:flex;"><span><span style="color:#75715e">// .env.local
</span></span></span><span style="display:flex;"><span><span style="color:#75715e"></span><span style="color:#a6e22e">OPENAI_API_KEY</span><span style="color:#f92672">=</span><span style="color:#a6e22e">sk</span><span style="color:#f92672">-</span>...
</span></span><span style="display:flex;"><span><span style="color:#a6e22e">ANTHROPIC_API_KEY</span><span style="color:#f92672">=</span><span style="color:#a6e22e">sk</span><span style="color:#f92672">-</span><span style="color:#a6e22e">ant</span><span style="color:#f92672">-</span>...
</span></span><span style="display:flex;"><span><span style="color:#a6e22e">GOOGLE_GENERATIVE_AI_API_KEY</span><span style="color:#f92672">=</span>...
</span></span></code></pre></div><div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-typescript" data-lang="typescript"><span style="display:flex;"><span><span style="color:#75715e">// app/api/chat/route.ts
</span></span></span><span style="display:flex;"><span><span style="color:#75715e"></span><span style="color:#66d9ef">import</span> { <span style="color:#a6e22e">streamText</span> } <span style="color:#66d9ef">from</span> <span style="color:#e6db74">&#39;ai&#39;</span>
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">import</span> { <span style="color:#a6e22e">openai</span> } <span style="color:#66d9ef">from</span> <span style="color:#e6db74">&#39;@ai-sdk/openai&#39;</span>
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">export</span> <span style="color:#66d9ef">const</span> <span style="color:#a6e22e">runtime</span> <span style="color:#f92672">=</span> <span style="color:#e6db74">&#39;edge&#39;</span>
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">export</span> <span style="color:#66d9ef">async</span> <span style="color:#66d9ef">function</span> <span style="color:#a6e22e">POST</span>(<span style="color:#a6e22e">req</span>: <span style="color:#66d9ef">Request</span>) {
</span></span><span style="display:flex;"><span>  <span style="color:#66d9ef">const</span> { <span style="color:#a6e22e">messages</span> } <span style="color:#f92672">=</span> <span style="color:#66d9ef">await</span> <span style="color:#a6e22e">req</span>.<span style="color:#a6e22e">json</span>()
</span></span><span style="display:flex;"><span>  
</span></span><span style="display:flex;"><span>  <span style="color:#66d9ef">const</span> <span style="color:#a6e22e">result</span> <span style="color:#f92672">=</span> <span style="color:#a6e22e">streamText</span>({
</span></span><span style="display:flex;"><span>    <span style="color:#a6e22e">model</span>: <span style="color:#66d9ef">openai</span>(<span style="color:#e6db74">&#39;gpt-4o&#39;</span>),
</span></span><span style="display:flex;"><span>    <span style="color:#a6e22e">messages</span>,
</span></span><span style="display:flex;"><span>  })
</span></span><span style="display:flex;"><span>  
</span></span><span style="display:flex;"><span>  <span style="color:#66d9ef">return</span> <span style="color:#a6e22e">result</span>.<span style="color:#a6e22e">toDataStreamResponse</span>()
</span></span><span style="display:flex;"><span>}
</span></span></code></pre></div><h3 id="ai-gateway-one-key-for-100-models">AI Gateway: One Key for 100+ Models</h3>
<p>Vercel AI Gateway lets you use a single <code>VERCEL_API_KEY</code> to access models from OpenAI, Anthropic, Google, Mistral, and more. It handles rate limit rotation, cost tracking, and automatic retry logic. For teams that need to experiment with multiple providers without managing individual API key billing, Gateway is the fastest path to a multi-model setup.</p>
<h2 id="ai-sdk-core-text-generation-and-streaming">AI SDK Core: Text Generation and Streaming</h2>
<p>AI SDK Core is the server-side engine that converts provider-specific APIs into a consistent interface for generating text, streaming responses, and calling tools. The two primary functions are <code>generateText</code> and <code>streamText</code>. <code>generateText</code> is for synchronous operations — you send a prompt and wait for the full response, which is ideal for batch jobs, summarization pipelines, and any context where the user is not watching a UI render in real time. <code>streamText</code> is the streaming counterpart: it returns a <code>ReadableStream</code> that you can pipe directly to a <code>Response</code> object, and it integrates with UI hooks via the <code>toDataStreamResponse()</code> method. Both functions accept the same options object — <code>model</code>, <code>messages</code>, <code>system</code>, <code>tools</code>, <code>maxSteps</code>, <code>temperature</code>, and more — so switching between them is a one-word change. Provider switching is similarly simple: swapping <code>openai('gpt-4o')</code> for <code>anthropic('claude-opus-4-7')</code> is the only change needed. Retry logic and fallbacks are handled with the <code>wrapLanguageModel</code> utility and the <code>fallback</code> provider, which tries a list of models in order if the primary returns an error. The consistency across providers is the single biggest productivity gain AI SDK offers compared to using provider SDKs directly.</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-typescript" data-lang="typescript"><span style="display:flex;"><span><span style="color:#66d9ef">import</span> { <span style="color:#a6e22e">generateText</span>, <span style="color:#a6e22e">streamText</span> } <span style="color:#66d9ef">from</span> <span style="color:#e6db74">&#39;ai&#39;</span>
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">import</span> { <span style="color:#a6e22e">anthropic</span> } <span style="color:#66d9ef">from</span> <span style="color:#e6db74">&#39;@ai-sdk/anthropic&#39;</span>
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#75715e">// One-shot generation
</span></span></span><span style="display:flex;"><span><span style="color:#75715e"></span><span style="color:#66d9ef">const</span> { <span style="color:#a6e22e">text</span> } <span style="color:#f92672">=</span> <span style="color:#66d9ef">await</span> <span style="color:#a6e22e">generateText</span>({
</span></span><span style="display:flex;"><span>  <span style="color:#a6e22e">model</span>: <span style="color:#66d9ef">anthropic</span>(<span style="color:#e6db74">&#39;claude-sonnet-4-6&#39;</span>),
</span></span><span style="display:flex;"><span>  <span style="color:#a6e22e">prompt</span><span style="color:#f92672">:</span> <span style="color:#e6db74">&#39;Summarize the key features of React 19 in 3 bullet points.&#39;</span>,
</span></span><span style="display:flex;"><span>})
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#75715e">// Streaming
</span></span></span><span style="display:flex;"><span><span style="color:#75715e"></span><span style="color:#66d9ef">const</span> <span style="color:#a6e22e">result</span> <span style="color:#f92672">=</span> <span style="color:#a6e22e">streamText</span>({
</span></span><span style="display:flex;"><span>  <span style="color:#a6e22e">model</span>: <span style="color:#66d9ef">anthropic</span>(<span style="color:#e6db74">&#39;claude-sonnet-4-6&#39;</span>),
</span></span><span style="display:flex;"><span>  <span style="color:#a6e22e">prompt</span><span style="color:#f92672">:</span> <span style="color:#e6db74">&#39;Write a guide on async/await in TypeScript.&#39;</span>,
</span></span><span style="display:flex;"><span>})
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">for</span> <span style="color:#66d9ef">await</span> (<span style="color:#66d9ef">const</span> <span style="color:#a6e22e">chunk</span> <span style="color:#66d9ef">of</span> <span style="color:#a6e22e">result</span>.<span style="color:#a6e22e">textStream</span>) {
</span></span><span style="display:flex;"><span>  <span style="color:#a6e22e">process</span>.<span style="color:#a6e22e">stdout</span>.<span style="color:#a6e22e">write</span>(<span style="color:#a6e22e">chunk</span>)
</span></span><span style="display:flex;"><span>}
</span></span></code></pre></div><h3 id="built-in-fallbacks-and-retry-logic">Built-In Fallbacks and Retry Logic</h3>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-typescript" data-lang="typescript"><span style="display:flex;"><span><span style="color:#66d9ef">import</span> { <span style="color:#a6e22e">createFallback</span> } <span style="color:#66d9ef">from</span> <span style="color:#e6db74">&#39;@ai-sdk/provider-utils&#39;</span>
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">import</span> { <span style="color:#a6e22e">openai</span> } <span style="color:#66d9ef">from</span> <span style="color:#e6db74">&#39;@ai-sdk/openai&#39;</span>
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">import</span> { <span style="color:#a6e22e">anthropic</span> } <span style="color:#66d9ef">from</span> <span style="color:#e6db74">&#39;@ai-sdk/anthropic&#39;</span>
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">const</span> <span style="color:#a6e22e">resilientModel</span> <span style="color:#f92672">=</span> <span style="color:#a6e22e">createFallback</span>([
</span></span><span style="display:flex;"><span>  <span style="color:#a6e22e">openai</span>(<span style="color:#e6db74">&#39;gpt-4o&#39;</span>),
</span></span><span style="display:flex;"><span>  <span style="color:#a6e22e">anthropic</span>(<span style="color:#e6db74">&#39;claude-sonnet-4-6&#39;</span>),
</span></span><span style="display:flex;"><span>])
</span></span></code></pre></div><h2 id="structured-output-with-zod-schemas">Structured Output with Zod Schemas</h2>
<p><code>generateObject</code> and <code>streamObject</code> are AI SDK&rsquo;s solution to one of the biggest pain points in production AI: getting reliable, type-safe structured data from LLMs instead of freeform text that you then parse with fragile regexes. These functions accept a Zod schema and use the model&rsquo;s native structured output mode — JSON mode for OpenAI, tool-use-based extraction for Anthropic — to guarantee the response matches the schema shape. If the model returns malformed output, AI SDK retries automatically. This is not just a developer convenience: structured output is essential for any AI pipeline where the response feeds into downstream logic, databases, or APIs. Teams using <code>generateObject</code> in production report near-zero JSON parsing errors compared to prompt-based extraction, and the Zod types flow through the entire TypeScript type system so you get autocomplete on the AI response object. The <code>streamObject</code> variant lets you stream partial structured objects, enabling progressive UI rendering as the AI fills in fields — useful for forms, dashboards, or any interface where showing partial data is better than a blank loading state. For data extraction tasks — pulling product specs from HTML, extracting entities from documents, or parsing unstructured API responses — structured output with Zod is the recommended production approach.</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-typescript" data-lang="typescript"><span style="display:flex;"><span><span style="color:#66d9ef">import</span> { <span style="color:#a6e22e">generateObject</span>, <span style="color:#a6e22e">streamObject</span> } <span style="color:#66d9ef">from</span> <span style="color:#e6db74">&#39;ai&#39;</span>
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">import</span> { <span style="color:#a6e22e">openai</span> } <span style="color:#66d9ef">from</span> <span style="color:#e6db74">&#39;@ai-sdk/openai&#39;</span>
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">import</span> { <span style="color:#a6e22e">z</span> } <span style="color:#66d9ef">from</span> <span style="color:#e6db74">&#39;zod&#39;</span>
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">const</span> <span style="color:#a6e22e">BlogPostSchema</span> <span style="color:#f92672">=</span> <span style="color:#a6e22e">z</span>.<span style="color:#66d9ef">object</span>({
</span></span><span style="display:flex;"><span>  <span style="color:#a6e22e">title</span>: <span style="color:#66d9ef">z.string</span>(),
</span></span><span style="display:flex;"><span>  <span style="color:#a6e22e">summary</span>: <span style="color:#66d9ef">z.string</span>().<span style="color:#a6e22e">max</span>(<span style="color:#ae81ff">200</span>),
</span></span><span style="display:flex;"><span>  <span style="color:#a6e22e">tags</span>: <span style="color:#66d9ef">z.array</span>(<span style="color:#a6e22e">z</span>.<span style="color:#66d9ef">string</span>()).<span style="color:#a6e22e">max</span>(<span style="color:#ae81ff">5</span>),
</span></span><span style="display:flex;"><span>  <span style="color:#a6e22e">seoScore</span>: <span style="color:#66d9ef">z.number</span>().<span style="color:#a6e22e">min</span>(<span style="color:#ae81ff">0</span>).<span style="color:#a6e22e">max</span>(<span style="color:#ae81ff">100</span>),
</span></span><span style="display:flex;"><span>  <span style="color:#a6e22e">sections</span>: <span style="color:#66d9ef">z.array</span>(<span style="color:#a6e22e">z</span>.<span style="color:#66d9ef">object</span>({
</span></span><span style="display:flex;"><span>    <span style="color:#a6e22e">heading</span>: <span style="color:#66d9ef">z.string</span>(),
</span></span><span style="display:flex;"><span>    <span style="color:#a6e22e">keyPoints</span>: <span style="color:#66d9ef">z.array</span>(<span style="color:#a6e22e">z</span>.<span style="color:#66d9ef">string</span>()),
</span></span><span style="display:flex;"><span>  })),
</span></span><span style="display:flex;"><span>})
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">const</span> { <span style="color:#66d9ef">object</span> } <span style="color:#f92672">=</span> <span style="color:#66d9ef">await</span> <span style="color:#a6e22e">generateObject</span>({
</span></span><span style="display:flex;"><span>  <span style="color:#a6e22e">model</span>: <span style="color:#66d9ef">openai</span>(<span style="color:#e6db74">&#39;gpt-4o&#39;</span>),
</span></span><span style="display:flex;"><span>  <span style="color:#a6e22e">schema</span>: <span style="color:#66d9ef">BlogPostSchema</span>,
</span></span><span style="display:flex;"><span>  <span style="color:#a6e22e">prompt</span><span style="color:#f92672">:</span> <span style="color:#e6db74">&#39;Analyze this article and return structured metadata: ...&#39;</span>,
</span></span><span style="display:flex;"><span>})
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#a6e22e">console</span>.<span style="color:#a6e22e">log</span>(<span style="color:#66d9ef">object</span>.<span style="color:#a6e22e">title</span>) <span style="color:#75715e">// TypeScript knows the full type
</span></span></span></code></pre></div><h3 id="streaming-structured-objects">Streaming Structured Objects</h3>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-typescript" data-lang="typescript"><span style="display:flex;"><span><span style="color:#66d9ef">const</span> { <span style="color:#a6e22e">partialObjectStream</span> } <span style="color:#f92672">=</span> <span style="color:#a6e22e">streamObject</span>({
</span></span><span style="display:flex;"><span>  <span style="color:#a6e22e">model</span>: <span style="color:#66d9ef">openai</span>(<span style="color:#e6db74">&#39;gpt-4o&#39;</span>),
</span></span><span style="display:flex;"><span>  <span style="color:#a6e22e">schema</span>: <span style="color:#66d9ef">BlogPostSchema</span>,
</span></span><span style="display:flex;"><span>  <span style="color:#a6e22e">prompt</span><span style="color:#f92672">:</span> <span style="color:#e6db74">&#39;Generate a blog post outline for: &#34;AI agents in 2026&#34;&#39;</span>,
</span></span><span style="display:flex;"><span>})
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">for</span> <span style="color:#66d9ef">await</span> (<span style="color:#66d9ef">const</span> <span style="color:#a6e22e">partial</span> <span style="color:#66d9ef">of</span> <span style="color:#a6e22e">partialObjectStream</span>) {
</span></span><span style="display:flex;"><span>  <span style="color:#75715e">// partial.title appears as soon as the model generates it
</span></span></span><span style="display:flex;"><span><span style="color:#75715e"></span>  <span style="color:#a6e22e">updateUI</span>(<span style="color:#a6e22e">partial</span>)
</span></span><span style="display:flex;"><span>}
</span></span></code></pre></div><h2 id="building-chat-uis-with-ai-sdk-ui-hooks">Building Chat UIs with AI SDK UI Hooks</h2>
<p>AI SDK UI is the client-side complement to Core, providing React hooks that manage chat state, streaming responses, and optimistic updates without requiring a single <code>useState</code> or <code>useEffect</code> for stream handling. The primary hook is <code>useChat</code>, which gives you <code>messages</code>, <code>input</code>, <code>handleInputChange</code>, <code>handleSubmit</code>, and <code>isLoading</code> — everything needed to build a ChatGPT-like interface in under 50 lines of React. Under the hood it connects to your AI route, handles stream parsing, and appends message chunks to state as they arrive. The <code>useCompletion</code> hook handles text completion use cases — autocomplete, writing suggestions, or any single-prompt UX. <code>useObject</code> streams structured objects from a <code>streamObject</code> route and exposes the partial object as it builds, enabling progressive form filling or AI-driven dashboard updates. All three hooks work with React, Vue, Svelte, and SolidJS — the framework-agnostic design means you can share backend patterns between projects built on different frontend stacks. The hooks integrate with React Suspense and Error Boundaries for graceful loading and error states without extra wiring.</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-typescript" data-lang="typescript"><span style="display:flex;"><span><span style="color:#75715e">// app/components/Chat.tsx
</span></span></span><span style="display:flex;"><span><span style="color:#75715e"></span><span style="color:#e6db74">&#39;use client&#39;</span>
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">import</span> { <span style="color:#a6e22e">useChat</span> } <span style="color:#66d9ef">from</span> <span style="color:#e6db74">&#39;ai/react&#39;</span>
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">export</span> <span style="color:#66d9ef">function</span> <span style="color:#a6e22e">Chat() {</span>
</span></span><span style="display:flex;"><span>  <span style="color:#66d9ef">const</span> { <span style="color:#a6e22e">messages</span>, <span style="color:#a6e22e">input</span>, <span style="color:#a6e22e">handleInputChange</span>, <span style="color:#a6e22e">handleSubmit</span>, <span style="color:#a6e22e">isLoading</span> } <span style="color:#f92672">=</span> <span style="color:#a6e22e">useChat</span>({
</span></span><span style="display:flex;"><span>    <span style="color:#a6e22e">api</span><span style="color:#f92672">:</span> <span style="color:#e6db74">&#39;/api/chat&#39;</span>,
</span></span><span style="display:flex;"><span>  })
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>  <span style="color:#66d9ef">return</span> (
</span></span><span style="display:flex;"><span>    &lt;<span style="color:#f92672">div</span>&gt;
</span></span><span style="display:flex;"><span>      {<span style="color:#a6e22e">messages</span>.<span style="color:#a6e22e">map</span>(<span style="color:#a6e22e">m</span> <span style="color:#f92672">=&gt;</span> (
</span></span><span style="display:flex;"><span>        &lt;<span style="color:#f92672">div</span> <span style="color:#a6e22e">key</span><span style="color:#f92672">=</span>{<span style="color:#a6e22e">m</span>.<span style="color:#a6e22e">id</span>} <span style="color:#a6e22e">className</span><span style="color:#f92672">=</span>{<span style="color:#a6e22e">m</span>.<span style="color:#a6e22e">role</span> <span style="color:#f92672">===</span> <span style="color:#e6db74">&#39;user&#39;</span> <span style="color:#f92672">?</span> <span style="color:#e6db74">&#39;user&#39;</span> <span style="color:#f92672">:</span> <span style="color:#e6db74">&#39;assistant&#39;</span>}&gt;
</span></span><span style="display:flex;"><span>          {<span style="color:#a6e22e">m</span>.<span style="color:#a6e22e">content</span>}
</span></span><span style="display:flex;"><span>        &lt;/<span style="color:#f92672">div</span>&gt;
</span></span><span style="display:flex;"><span>      ))}
</span></span><span style="display:flex;"><span>      &lt;<span style="color:#f92672">form</span> <span style="color:#a6e22e">onSubmit</span><span style="color:#f92672">=</span>{<span style="color:#a6e22e">handleSubmit</span>}&gt;
</span></span><span style="display:flex;"><span>        &lt;<span style="color:#f92672">input</span> <span style="color:#a6e22e">value</span><span style="color:#f92672">=</span>{<span style="color:#a6e22e">input</span>} <span style="color:#a6e22e">onChange</span><span style="color:#f92672">=</span>{<span style="color:#a6e22e">handleInputChange</span>} <span style="color:#a6e22e">disabled</span><span style="color:#f92672">=</span>{<span style="color:#a6e22e">isLoading</span>} /&gt;
</span></span><span style="display:flex;"><span>        &lt;<span style="color:#f92672">button</span> <span style="color:#a6e22e">type</span><span style="color:#f92672">=</span><span style="color:#e6db74">&#34;submit&#34;</span> <span style="color:#a6e22e">disabled</span><span style="color:#f92672">=</span>{<span style="color:#a6e22e">isLoading</span>}&gt;<span style="color:#a6e22e">Send</span>&lt;/<span style="color:#f92672">button</span>&gt;
</span></span><span style="display:flex;"><span>      &lt;/<span style="color:#f92672">form</span>&gt;
</span></span><span style="display:flex;"><span>    &lt;/<span style="color:#f92672">div</span>&gt;
</span></span><span style="display:flex;"><span>  )
</span></span><span style="display:flex;"><span>}
</span></span></code></pre></div><h3 id="framework-support-comparison">Framework Support Comparison</h3>
<table>
  <thead>
      <tr>
          <th>Hook</th>
          <th>React</th>
          <th>Vue</th>
          <th>Svelte</th>
          <th>SolidJS</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td><code>useChat</code></td>
          <td>✅</td>
          <td>✅</td>
          <td>✅</td>
          <td>✅</td>
      </tr>
      <tr>
          <td><code>useCompletion</code></td>
          <td>✅</td>
          <td>✅</td>
          <td>✅</td>
          <td>✅</td>
      </tr>
      <tr>
          <td><code>useObject</code></td>
          <td>✅</td>
          <td>✅</td>
          <td>✅</td>
          <td>✅</td>
      </tr>
      <tr>
          <td><code>useAssistant</code></td>
          <td>✅</td>
          <td>❌</td>
          <td>❌</td>
          <td>❌</td>
      </tr>
  </tbody>
</table>
<h2 id="tool-calling-giving-your-ai-agent-superpowers">Tool Calling: Giving Your AI Agent Superpowers</h2>
<p>Tool calling in AI SDK is the mechanism that transforms a passive text generator into an active agent — the model describes which tools it wants to invoke, AI SDK executes them server-side, and the results feed back into the next model turn automatically. Tools are defined with the <code>tool</code> helper, which takes a <code>description</code> (natural language explanation for the model), <code>parameters</code> (a Zod schema for typed inputs), and an <code>execute</code> function (the actual implementation). The SDK handles the full tool-call cycle: formatting the tool description for the provider, parsing the model&rsquo;s structured tool-call output, executing the function with validated arguments, and appending the result to the conversation context. <code>maxSteps</code> controls how many tool-call cycles the agent can run before stopping, preventing infinite loops while allowing multi-step reasoning chains of 5–10 steps. Tool results stream to the client via <code>toDataStreamResponse()</code>, so users see intermediate tool outputs in real time rather than waiting for the final answer. In production applications, tool sets commonly include database query tools, web search, calculator functions, external API calls, and file operations — anything your server-side code can do, the agent can orchestrate. The Zod parameter schemas provide input validation at zero extra cost, catching malformed tool calls before they reach your database or external services.</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-typescript" data-lang="typescript"><span style="display:flex;"><span><span style="color:#66d9ef">import</span> { <span style="color:#a6e22e">streamText</span>, <span style="color:#a6e22e">tool</span> } <span style="color:#66d9ef">from</span> <span style="color:#e6db74">&#39;ai&#39;</span>
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">import</span> { <span style="color:#a6e22e">openai</span> } <span style="color:#66d9ef">from</span> <span style="color:#e6db74">&#39;@ai-sdk/openai&#39;</span>
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">import</span> { <span style="color:#a6e22e">z</span> } <span style="color:#66d9ef">from</span> <span style="color:#e6db74">&#39;zod&#39;</span>
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">const</span> <span style="color:#a6e22e">result</span> <span style="color:#f92672">=</span> <span style="color:#a6e22e">streamText</span>({
</span></span><span style="display:flex;"><span>  <span style="color:#a6e22e">model</span>: <span style="color:#66d9ef">openai</span>(<span style="color:#e6db74">&#39;gpt-4o&#39;</span>),
</span></span><span style="display:flex;"><span>  <span style="color:#a6e22e">maxSteps</span>: <span style="color:#66d9ef">5</span>,
</span></span><span style="display:flex;"><span>  <span style="color:#a6e22e">tools</span><span style="color:#f92672">:</span> {
</span></span><span style="display:flex;"><span>    <span style="color:#a6e22e">getWeather</span>: <span style="color:#66d9ef">tool</span>({
</span></span><span style="display:flex;"><span>      <span style="color:#a6e22e">description</span><span style="color:#f92672">:</span> <span style="color:#e6db74">&#39;Get current weather for a city&#39;</span>,
</span></span><span style="display:flex;"><span>      <span style="color:#a6e22e">parameters</span>: <span style="color:#66d9ef">z.object</span>({ <span style="color:#a6e22e">city</span>: <span style="color:#66d9ef">z.string</span>() }),
</span></span><span style="display:flex;"><span>      <span style="color:#a6e22e">execute</span>: <span style="color:#66d9ef">async</span> ({ <span style="color:#a6e22e">city</span> }) <span style="color:#f92672">=&gt;</span> {
</span></span><span style="display:flex;"><span>        <span style="color:#66d9ef">const</span> <span style="color:#a6e22e">res</span> <span style="color:#f92672">=</span> <span style="color:#66d9ef">await</span> <span style="color:#a6e22e">fetch</span>(<span style="color:#e6db74">`https://api.weather.com/</span><span style="color:#e6db74">${</span><span style="color:#a6e22e">city</span><span style="color:#e6db74">}</span><span style="color:#e6db74">`</span>)
</span></span><span style="display:flex;"><span>        <span style="color:#66d9ef">return</span> <span style="color:#a6e22e">res</span>.<span style="color:#a6e22e">json</span>()
</span></span><span style="display:flex;"><span>      },
</span></span><span style="display:flex;"><span>    }),
</span></span><span style="display:flex;"><span>    <span style="color:#a6e22e">searchDatabase</span>: <span style="color:#66d9ef">tool</span>({
</span></span><span style="display:flex;"><span>      <span style="color:#a6e22e">description</span><span style="color:#f92672">:</span> <span style="color:#e6db74">&#39;Search the product database&#39;</span>,
</span></span><span style="display:flex;"><span>      <span style="color:#a6e22e">parameters</span>: <span style="color:#66d9ef">z.object</span>({ <span style="color:#a6e22e">query</span>: <span style="color:#66d9ef">z.string</span>(), <span style="color:#a6e22e">limit</span>: <span style="color:#66d9ef">z.number</span>().<span style="color:#66d9ef">default</span>(<span style="color:#ae81ff">5</span>) }),
</span></span><span style="display:flex;"><span>      <span style="color:#a6e22e">execute</span>: <span style="color:#66d9ef">async</span> ({ <span style="color:#a6e22e">query</span>, <span style="color:#a6e22e">limit</span> }) <span style="color:#f92672">=&gt;</span> {
</span></span><span style="display:flex;"><span>        <span style="color:#66d9ef">return</span> <span style="color:#a6e22e">db</span>.<span style="color:#a6e22e">products</span>.<span style="color:#a6e22e">search</span>(<span style="color:#a6e22e">query</span>, { <span style="color:#a6e22e">limit</span> })
</span></span><span style="display:flex;"><span>      },
</span></span><span style="display:flex;"><span>    }),
</span></span><span style="display:flex;"><span>  },
</span></span><span style="display:flex;"><span>  <span style="color:#a6e22e">messages</span><span style="color:#f92672">:</span> [{ <span style="color:#a6e22e">role</span><span style="color:#f92672">:</span> <span style="color:#e6db74">&#39;user&#39;</span>, <span style="color:#a6e22e">content</span><span style="color:#f92672">:</span> <span style="color:#e6db74">&#39;What is the weather in Tokyo and do we sell umbrellas?&#39;</span> }],
</span></span><span style="display:flex;"><span>})
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">return</span> <span style="color:#a6e22e">result</span>.<span style="color:#a6e22e">toDataStreamResponse</span>()
</span></span></code></pre></div><h3 id="multi-step-agent-loop">Multi-Step Agent Loop</h3>
<p>With <code>maxSteps: 5</code>, the model can: call <code>getWeather</code> → get Tokyo weather → call <code>searchDatabase</code> for umbrellas → combine results → return final answer. Each step is visible to the user via streaming tool call indicators rendered automatically by <code>useChat</code>.</p>
<h2 id="building-ai-agents-with-multi-step-reasoning">Building AI Agents with Multi-Step Reasoning</h2>
<p>An AI agent in the Vercel AI SDK context is a <code>streamText</code> or <code>generateText</code> call with tools enabled and <code>maxSteps</code> set above 1 — the model reasons, calls tools, observes results, and reasons again until it reaches a conclusion or exhausts its step budget. This loop pattern is what separates agents from chatbots: rather than answering from static training knowledge, the agent actively queries databases, fetches URLs, or calls APIs to gather real-time information before formulating a response. The key to a production-grade agent is memory and context management: you control what goes in <code>messages</code>, so you can implement sliding window context, summarization, or retrieval-augmented generation by fetching relevant documents before calling the model. For RAG integration, the standard pattern is to embed the user query, retrieve top-k chunks from a vector store (Pinecone, Supabase pgvector, or Upstash), and prepend them as a system message. The agent then has access to both retrieved context and its tool-calling ability, so it can fetch additional information if the retrieved chunks are insufficient. OpenCode&rsquo;s architecture demonstrates this at scale: it uses AI SDK with file system tools, runs multi-step reasoning loops to understand a codebase, and streams results back to a terminal UI — all without custom streaming infrastructure because AI SDK handles it.</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-typescript" data-lang="typescript"><span style="display:flex;"><span><span style="color:#75715e">// Research agent with RAG
</span></span></span><span style="display:flex;"><span><span style="color:#75715e"></span><span style="color:#66d9ef">async</span> <span style="color:#66d9ef">function</span> <span style="color:#a6e22e">researchAgent</span>(<span style="color:#a6e22e">query</span>: <span style="color:#66d9ef">string</span>) {
</span></span><span style="display:flex;"><span>  <span style="color:#66d9ef">const</span> <span style="color:#a6e22e">relevantDocs</span> <span style="color:#f92672">=</span> <span style="color:#66d9ef">await</span> <span style="color:#a6e22e">vectorStore</span>.<span style="color:#a6e22e">search</span>(<span style="color:#a6e22e">query</span>, { <span style="color:#a6e22e">topK</span>: <span style="color:#66d9ef">5</span> })
</span></span><span style="display:flex;"><span>  
</span></span><span style="display:flex;"><span>  <span style="color:#66d9ef">const</span> <span style="color:#a6e22e">result</span> <span style="color:#f92672">=</span> <span style="color:#a6e22e">streamText</span>({
</span></span><span style="display:flex;"><span>    <span style="color:#a6e22e">model</span>: <span style="color:#66d9ef">openai</span>(<span style="color:#e6db74">&#39;gpt-4o&#39;</span>),
</span></span><span style="display:flex;"><span>    <span style="color:#a6e22e">maxSteps</span>: <span style="color:#66d9ef">8</span>,
</span></span><span style="display:flex;"><span>    <span style="color:#a6e22e">system</span><span style="color:#f92672">:</span> <span style="color:#e6db74">`You are a research agent. Use context and tools to answer thoroughly.
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">    
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">Context from knowledge base:
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74"></span><span style="color:#e6db74">${</span><span style="color:#a6e22e">relevantDocs</span>.<span style="color:#a6e22e">map</span>(<span style="color:#a6e22e">d</span> <span style="color:#f92672">=&gt;</span> <span style="color:#a6e22e">d</span>.<span style="color:#a6e22e">content</span>).<span style="color:#a6e22e">join</span>(<span style="color:#e6db74">&#39;\n\n&#39;</span>)<span style="color:#e6db74">}</span><span style="color:#e6db74">`</span>,
</span></span><span style="display:flex;"><span>    <span style="color:#a6e22e">messages</span><span style="color:#f92672">:</span> [{ <span style="color:#a6e22e">role</span><span style="color:#f92672">:</span> <span style="color:#e6db74">&#39;user&#39;</span>, <span style="color:#a6e22e">content</span>: <span style="color:#66d9ef">query</span> }],
</span></span><span style="display:flex;"><span>    <span style="color:#a6e22e">tools</span><span style="color:#f92672">:</span> {
</span></span><span style="display:flex;"><span>      <span style="color:#a6e22e">searchWeb</span>: <span style="color:#66d9ef">tool</span>({ <span style="color:#75715e">/* web search implementation */</span> }),
</span></span><span style="display:flex;"><span>      <span style="color:#a6e22e">fetchUrl</span>: <span style="color:#66d9ef">tool</span>({ <span style="color:#75715e">/* URL fetching implementation */</span> }),
</span></span><span style="display:flex;"><span>      <span style="color:#a6e22e">saveNote</span>: <span style="color:#66d9ef">tool</span>({ <span style="color:#75715e">/* note saving implementation */</span> }),
</span></span><span style="display:flex;"><span>    },
</span></span><span style="display:flex;"><span>  })
</span></span><span style="display:flex;"><span>  
</span></span><span style="display:flex;"><span>  <span style="color:#66d9ef">return</span> <span style="color:#a6e22e">result</span>.<span style="color:#a6e22e">toDataStreamResponse</span>()
</span></span><span style="display:flex;"><span>}
</span></span></code></pre></div><h2 id="vercel-workflows-long-running-agents-that-survive">Vercel Workflows: Long-Running Agents That Survive</h2>
<p>Vercel Workflows is a 2026 addition to the AI SDK ecosystem that solves the most critical limitation of serverless AI agents: function timeout. Standard serverless functions on Vercel time out after 30 seconds (Pro plan) or 5 minutes (Enterprise), which is insufficient for agents that need to search the web, process large documents, run multi-stage pipelines, or wait for human approval. Workflows introduces durable execution — agent tasks are broken into named steps that can suspend (persist state to managed storage), wait for external events, and resume exactly where they left off across multiple function invocations without losing context. This makes genuinely complex agentic pipelines feasible on serverless infrastructure: a content generation pipeline can run for 20+ minutes as it researches, drafts, and revises content, with the agent suspending between phases. The <code>@vercel/workflows</code> package integrates directly with AI SDK&rsquo;s <code>generateText</code> and <code>streamText</code> — you wrap agent logic in a <code>workflow</code> function and use <code>step.run()</code> to define resumable checkpoints. Human-in-the-loop approval is supported via <code>step.waitForEvent()</code>, which suspends the workflow until a webhook fires. In 2026, Workflows is the recommended architecture for any AI task that may exceed 30 seconds or requires coordination between multiple agents.</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-typescript" data-lang="typescript"><span style="display:flex;"><span><span style="color:#66d9ef">import</span> { <span style="color:#a6e22e">workflow</span>, <span style="color:#a6e22e">step</span> } <span style="color:#66d9ef">from</span> <span style="color:#e6db74">&#39;@vercel/workflows&#39;</span>
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">import</span> { <span style="color:#a6e22e">generateText</span> } <span style="color:#66d9ef">from</span> <span style="color:#e6db74">&#39;ai&#39;</span>
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">import</span> { <span style="color:#a6e22e">anthropic</span> } <span style="color:#66d9ef">from</span> <span style="color:#e6db74">&#39;@ai-sdk/anthropic&#39;</span>
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">export</span> <span style="color:#66d9ef">const</span> <span style="color:#a6e22e">contentPipeline</span> <span style="color:#f92672">=</span> <span style="color:#a6e22e">workflow</span>(<span style="color:#66d9ef">async</span> ({ <span style="color:#a6e22e">input</span> }<span style="color:#f92672">:</span> { <span style="color:#a6e22e">input</span><span style="color:#f92672">:</span> { <span style="color:#a6e22e">topic</span>: <span style="color:#66d9ef">string</span> } }) <span style="color:#f92672">=&gt;</span> {
</span></span><span style="display:flex;"><span>  <span style="color:#75715e">// Each step is resumable — survives function timeout
</span></span></span><span style="display:flex;"><span><span style="color:#75715e"></span>  <span style="color:#66d9ef">const</span> <span style="color:#a6e22e">research</span> <span style="color:#f92672">=</span> <span style="color:#66d9ef">await</span> <span style="color:#a6e22e">step</span>.<span style="color:#a6e22e">run</span>(<span style="color:#e6db74">&#39;research&#39;</span>, <span style="color:#66d9ef">async</span> () <span style="color:#f92672">=&gt;</span> {
</span></span><span style="display:flex;"><span>    <span style="color:#66d9ef">const</span> { <span style="color:#a6e22e">text</span> } <span style="color:#f92672">=</span> <span style="color:#66d9ef">await</span> <span style="color:#a6e22e">generateText</span>({
</span></span><span style="display:flex;"><span>      <span style="color:#a6e22e">model</span>: <span style="color:#66d9ef">anthropic</span>(<span style="color:#e6db74">&#39;claude-opus-4-7&#39;</span>),
</span></span><span style="display:flex;"><span>      <span style="color:#a6e22e">prompt</span><span style="color:#f92672">:</span> <span style="color:#e6db74">`Research: </span><span style="color:#e6db74">${</span><span style="color:#a6e22e">input</span>.<span style="color:#a6e22e">topic</span><span style="color:#e6db74">}</span><span style="color:#e6db74">. Return key facts and sources.`</span>,
</span></span><span style="display:flex;"><span>    })
</span></span><span style="display:flex;"><span>    <span style="color:#66d9ef">return</span> <span style="color:#a6e22e">text</span>
</span></span><span style="display:flex;"><span>  })
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>  <span style="color:#66d9ef">const</span> <span style="color:#a6e22e">draft</span> <span style="color:#f92672">=</span> <span style="color:#66d9ef">await</span> <span style="color:#a6e22e">step</span>.<span style="color:#a6e22e">run</span>(<span style="color:#e6db74">&#39;draft&#39;</span>, <span style="color:#66d9ef">async</span> () <span style="color:#f92672">=&gt;</span> {
</span></span><span style="display:flex;"><span>    <span style="color:#66d9ef">const</span> { <span style="color:#a6e22e">text</span> } <span style="color:#f92672">=</span> <span style="color:#66d9ef">await</span> <span style="color:#a6e22e">generateText</span>({
</span></span><span style="display:flex;"><span>      <span style="color:#a6e22e">model</span>: <span style="color:#66d9ef">anthropic</span>(<span style="color:#e6db74">&#39;claude-sonnet-4-6&#39;</span>),
</span></span><span style="display:flex;"><span>      <span style="color:#a6e22e">prompt</span><span style="color:#f92672">:</span> <span style="color:#e6db74">`Using this research: </span><span style="color:#e6db74">${</span><span style="color:#a6e22e">research</span><span style="color:#e6db74">}</span><span style="color:#960050;background-color:#1e0010">\</span><span style="color:#e6db74">n</span><span style="color:#960050;background-color:#1e0010">\</span><span style="color:#e6db74">nWrite a 1000-word article about </span><span style="color:#e6db74">${</span><span style="color:#a6e22e">input</span>.<span style="color:#a6e22e">topic</span><span style="color:#e6db74">}</span><span style="color:#e6db74">.`</span>,
</span></span><span style="display:flex;"><span>    })
</span></span><span style="display:flex;"><span>    <span style="color:#66d9ef">return</span> <span style="color:#a6e22e">text</span>
</span></span><span style="display:flex;"><span>  })
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>  <span style="color:#66d9ef">return</span> { <span style="color:#a6e22e">research</span>, <span style="color:#a6e22e">draft</span> }
</span></span><span style="display:flex;"><span>})
</span></span></code></pre></div><h2 id="production-deployment-and-scaling">Production Deployment and Scaling</h2>
<p>Deploying a Vercel AI SDK application to production requires careful attention to runtime selection, cost management, and observability. For runtime selection, Edge Functions are the right choice for streaming chat routes because they have lower cold-start latency and are globally distributed across 30+ regions — users in Tokyo get a fast response without routing to a US datacenter. Node.js runtime is better for heavy tool execution, large file processing, or anything requiring Node-specific APIs. Cost management starts with the <code>maxTokens</code> parameter to cap spending per request, and AI Gateway adds team-level spend limits and per-model cost tracking with dashboards. For rate limiting on API routes, <code>@vercel/kv</code> with a sliding window counter is the standard pattern: each user or IP gets N requests per minute, excess requests return 429 with a <code>retry-after</code> header. Observability is critical for catching silent model failures: the <code>onFinish</code> callback in <code>streamText</code> and <code>generateText</code> lets you log token usage, model name, latency, and finish reason to your analytics pipeline, enabling cost attribution per feature and alerting on abnormal token consumption. Vercel&rsquo;s built-in function logs surface AI SDK error events automatically for debugging.</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-typescript" data-lang="typescript"><span style="display:flex;"><span><span style="color:#66d9ef">export</span> <span style="color:#66d9ef">async</span> <span style="color:#66d9ef">function</span> <span style="color:#a6e22e">POST</span>(<span style="color:#a6e22e">req</span>: <span style="color:#66d9ef">Request</span>) {
</span></span><span style="display:flex;"><span>  <span style="color:#66d9ef">const</span> { <span style="color:#a6e22e">messages</span> } <span style="color:#f92672">=</span> <span style="color:#66d9ef">await</span> <span style="color:#a6e22e">req</span>.<span style="color:#a6e22e">json</span>()
</span></span><span style="display:flex;"><span>  
</span></span><span style="display:flex;"><span>  <span style="color:#75715e">// Rate limiting check
</span></span></span><span style="display:flex;"><span><span style="color:#75715e"></span>  <span style="color:#66d9ef">const</span> <span style="color:#a6e22e">ip</span> <span style="color:#f92672">=</span> <span style="color:#a6e22e">req</span>.<span style="color:#a6e22e">headers</span>.<span style="color:#66d9ef">get</span>(<span style="color:#e6db74">&#39;x-forwarded-for&#39;</span>) <span style="color:#f92672">??</span> <span style="color:#e6db74">&#39;anonymous&#39;</span>
</span></span><span style="display:flex;"><span>  <span style="color:#66d9ef">const</span> { <span style="color:#a6e22e">success</span> } <span style="color:#f92672">=</span> <span style="color:#66d9ef">await</span> <span style="color:#a6e22e">ratelimit</span>.<span style="color:#a6e22e">limit</span>(<span style="color:#a6e22e">ip</span>)
</span></span><span style="display:flex;"><span>  <span style="color:#66d9ef">if</span> (<span style="color:#f92672">!</span><span style="color:#a6e22e">success</span>) <span style="color:#66d9ef">return</span> <span style="color:#66d9ef">new</span> <span style="color:#a6e22e">Response</span>(<span style="color:#e6db74">&#39;Rate limit exceeded&#39;</span>, { <span style="color:#a6e22e">status</span>: <span style="color:#66d9ef">429</span> })
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>  <span style="color:#66d9ef">const</span> <span style="color:#a6e22e">result</span> <span style="color:#f92672">=</span> <span style="color:#a6e22e">streamText</span>({
</span></span><span style="display:flex;"><span>    <span style="color:#a6e22e">model</span>: <span style="color:#66d9ef">openai</span>(<span style="color:#e6db74">&#39;gpt-4o&#39;</span>),
</span></span><span style="display:flex;"><span>    <span style="color:#a6e22e">messages</span>,
</span></span><span style="display:flex;"><span>    <span style="color:#a6e22e">maxTokens</span>: <span style="color:#66d9ef">2000</span>,
</span></span><span style="display:flex;"><span>    <span style="color:#a6e22e">temperature</span>: <span style="color:#66d9ef">0.7</span>,
</span></span><span style="display:flex;"><span>    <span style="color:#a6e22e">onFinish</span><span style="color:#f92672">:</span> ({ <span style="color:#a6e22e">usage</span>, <span style="color:#a6e22e">finishReason</span> }) <span style="color:#f92672">=&gt;</span> {
</span></span><span style="display:flex;"><span>      <span style="color:#a6e22e">analytics</span>.<span style="color:#a6e22e">track</span>(<span style="color:#e6db74">&#39;ai_completion&#39;</span>, {
</span></span><span style="display:flex;"><span>        <span style="color:#a6e22e">tokens</span>: <span style="color:#66d9ef">usage.totalTokens</span>,
</span></span><span style="display:flex;"><span>        <span style="color:#a6e22e">finishReason</span>,
</span></span><span style="display:flex;"><span>        <span style="color:#a6e22e">model</span><span style="color:#f92672">:</span> <span style="color:#e6db74">&#39;gpt-4o&#39;</span>,
</span></span><span style="display:flex;"><span>      })
</span></span><span style="display:flex;"><span>    },
</span></span><span style="display:flex;"><span>  })
</span></span><span style="display:flex;"><span>  
</span></span><span style="display:flex;"><span>  <span style="color:#66d9ef">return</span> <span style="color:#a6e22e">result</span>.<span style="color:#a6e22e">toDataStreamResponse</span>()
</span></span><span style="display:flex;"><span>}
</span></span></code></pre></div><h2 id="ai-sdk-vs-langchain-vs-mastra-framework-comparison">AI SDK vs LangChain vs Mastra: Framework Comparison</h2>
<p>Choosing between Vercel AI SDK, LangChain.js, and Mastra in 2026 depends primarily on your stack, agent complexity, and how important streaming and bundle size are to your application. Vercel AI SDK is the right choice for TypeScript web developers building streaming-first applications — it is the lightest of the three (under 200KB), has the best Next.js and Edge Function integration, and provides the most seamless streaming API with minimal boilerplate. LangChain.js has the broadest ecosystem: pre-built chains, 50+ vector store integrations, document loaders, memory modules, and a large community cookbook — making it better for teams needing to quickly assemble complex RAG pipelines from components rather than writing integration code themselves. Mastra, which emerged in late 2025, sits between the two: TypeScript-native like AI SDK but with an opinionated agent framework including built-in memory, durable workflow primitives, and multi-agent coordination, targeting developers who need more structure than AI SDK provides without LangChain&rsquo;s abstraction overhead. The bundle size difference is meaningful for edge and browser deployments where LangChain.js&rsquo;s 2MB+ footprint can impact cold start times. For most Next.js applications in 2026, AI SDK is the practical default and LangChain or Mastra are reached for only when specific missing features justify the additional complexity.</p>
<table>
  <thead>
      <tr>
          <th>Feature</th>
          <th>Vercel AI SDK</th>
          <th>LangChain.js</th>
          <th>Mastra</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>Bundle size</td>
          <td>~200KB</td>
          <td>~2MB+</td>
          <td>~500KB</td>
      </tr>
      <tr>
          <td>Streaming</td>
          <td>First-class</td>
          <td>Good</td>
          <td>Good</td>
      </tr>
      <tr>
          <td>Tool calling</td>
          <td>Native</td>
          <td>Via chains</td>
          <td>Native</td>
      </tr>
      <tr>
          <td>Structured output</td>
          <td>Zod-native</td>
          <td>Manual</td>
          <td>Zod-native</td>
      </tr>
      <tr>
          <td>Long-running agents</td>
          <td>Via Workflows</td>
          <td>Partial</td>
          <td>Built-in</td>
      </tr>
      <tr>
          <td>Next.js/Edge</td>
          <td>Excellent</td>
          <td>Moderate</td>
          <td>Good</td>
      </tr>
      <tr>
          <td>Pre-built integrations</td>
          <td>16+ providers</td>
          <td>50+</td>
          <td>20+</td>
      </tr>
      <tr>
          <td>TypeScript types</td>
          <td>Excellent</td>
          <td>Good</td>
          <td>Excellent</td>
      </tr>
      <tr>
          <td>Learning curve</td>
          <td>Low</td>
          <td>High</td>
          <td>Medium</td>
      </tr>
  </tbody>
</table>
<h2 id="faq">FAQ</h2>
<p><strong>What is the difference between AI SDK Core and AI SDK UI?</strong>
AI SDK Core (<code>generateText</code>, <code>streamText</code>, <code>generateObject</code>) runs on the server and handles model calls. AI SDK UI (<code>useChat</code>, <code>useCompletion</code>, <code>useObject</code>) runs on the client and manages stream state, message history, and UI updates. In a Next.js app, Core lives in <code>app/api/</code> routes and UI hooks live in client components. You can use Core without UI (for backend pipelines) but UI requires a Core-powered API endpoint.</p>
<p><strong>Can I use Vercel AI SDK without Vercel hosting?</strong>
Yes. The AI SDK is a pure npm package with no dependency on Vercel&rsquo;s infrastructure. You can use it in any Node.js server, AWS Lambda, Cloudflare Workers, or on-premise environment. Vercel-specific features like Workflows and AI Gateway require Vercel hosting, but AI SDK Core and UI work on any JavaScript runtime.</p>
<p><strong>How do I switch between AI providers in Vercel AI SDK?</strong>
Change one line: swap the model import and the model string. Replace <code>openai('gpt-4o')</code> with <code>anthropic('claude-sonnet-4-6')</code>. The rest of your code — messages, tools, streaming — stays identical. This is the main design goal: provider portability without refactoring business logic.</p>
<p><strong>What is the recommended way to add memory to an AI SDK agent?</strong>
The SDK does not manage memory itself — you control <code>messages</code>. Store conversation history in a database (KV, Postgres, or Upstash), retrieve the last N turns before each request, and pass them as <code>messages</code>. For long-term memory across sessions, embed user facts and retrieve them via vector search, prepending relevant memories to the system prompt before each request.</p>
<p><strong>Does Vercel AI SDK support multi-modal inputs like images and PDFs?</strong>
Yes. Models that support vision (GPT-4o, Claude Opus 4, Gemini Pro Vision) accept <code>content</code> arrays with <code>{ type: 'image', image: url }</code> or <code>{ type: 'file', data: base64, mimeType: 'application/pdf' }</code> parts alongside text. AI SDK normalizes these into the provider&rsquo;s expected format automatically, so you write the same code regardless of which vision model you use.</p>
]]></content:encoded></item></channel></rss>