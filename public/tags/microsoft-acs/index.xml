<?xml version="1.0" encoding="utf-8" standalone="yes"?><rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom" xmlns:content="http://purl.org/rss/1.0/modules/content/"><channel><title>Microsoft ACS on RockB</title><link>https://baeseokjae.github.io/tags/microsoft-acs/</link><description>Recent content in Microsoft ACS on RockB</description><image><title>RockB</title><url>https://baeseokjae.github.io/images/og-default.png</url><link>https://baeseokjae.github.io/images/og-default.png</link></image><generator>Hugo</generator><language>en-us</language><lastBuildDate>Sat, 13 Jun 2026 10:05:06 +0000</lastBuildDate><atom:link href="https://baeseokjae.github.io/tags/microsoft-acs/index.xml" rel="self" type="application/rss+xml"/><item><title>Agent Control Specification ACS AI Agent Governance Guide</title><link>https://baeseokjae.github.io/posts/microsoft-acs-agent-control-specification-2026/</link><pubDate>Sat, 13 Jun 2026 10:05:06 +0000</pubDate><guid>https://baeseokjae.github.io/posts/microsoft-acs-agent-control-specification-2026/</guid><description>A developer guide to Agent Control Specification ACS AI agent governance, runtime policy checks, and enterprise adoption.</description><content:encoded><![CDATA[<p>Agent Control Specification ACS AI agent governance is a portable way to apply policy checks while an agent runs, not just before it starts. ACS defines standard intervention points, policy manifests, evidence inputs, and auditable verdicts so teams can govern tool use, approvals, data handling, and shutdown behavior across agent frameworks.</p>
<h2 id="what-is-the-agent-control-specification-acs">What Is the Agent Control Specification (ACS)?</h2>
<p>Agent Control Specification is an open, vendor-neutral runtime governance standard for AI agents that defines where policy decisions happen and what evidence those decisions receive. Microsoft describes ACS as framework-independent, and its published model names eight intervention points, including <code>pre_model_call</code>, <code>pre_tool_call</code>, <code>post_tool_call</code>, and <code>output</code>. The practical idea is simple: instead of hiding safety rules inside prompts, SDK callbacks, or one-off middleware, ACS makes agent governance a portable contract. A host runtime supplies a snapshot of the agent state, tool metadata, annotations from evidence providers, and the policy target. A policy engine returns a verdict such as allow, warn, deny, or escalate. For developers, ACS is closest to policy-as-code for autonomous systems. The takeaway: ACS standardizes runtime control so security teams can review one governance model across many agent implementations.</p>
<h3 id="how-is-acs-different-from-ordinary-guardrails">How is ACS different from ordinary guardrails?</h3>
<p>ACS is different from ordinary guardrails because it governs the agent while work is happening, not only when a prompt enters or an answer leaves the model. A prompt guardrail might say &ldquo;do not send secrets,&rdquo; but ACS can check the exact tool call, destination, data classification, user identity, and approval state before the tool executes. That makes it better suited to agents that browse repositories, query databases, call payment APIs, or send email.</p>
<h2 id="why-do-ai-agents-need-runtime-governance-now">Why Do AI Agents Need Runtime Governance Now?</h2>
<p>AI agents need runtime governance now because deployment is outrunning control systems in real enterprises. IBM&rsquo;s June 2026 study says technology leaders expect a 38% increase in deployed AI agents by 2027, while only 11% feel completely prepared and 77% say AI adoption is moving faster than governance. That matches what I see in engineering reviews: the first agent pilot often has a narrow prompt and a demo-safe tool, but the second version gets write access, credentials, retrieval, schedulers, or customer data. Gartner has also warned that governance gaps will lead many enterprises to demote or decommission autonomous agents by 2027. Static reviews cannot keep up with agents that branch, retry, call tools, and recover from failures. The takeaway: runtime governance is necessary because agent behavior is dynamic, stateful, and operationally risky.</p>
<h3 id="where-do-agent-failures-usually-start">Where do agent failures usually start?</h3>
<p>Agent failures usually start at the boundary between reasoning and action. The model may summarize a request correctly, then choose the wrong account, overbroad query, unsafe API, or unapproved recipient. Traditional application controls help, but agents add ambiguity: intent is inferred, plans are generated, and tool arguments are composed at runtime. ACS targets that boundary by giving policy engines a standard place to inspect state before and after each sensitive step.</p>
<h2 id="how-does-acs-work-with-policy-manifests-snapshots-evidence-and-verdicts">How Does ACS Work With Policy Manifests, Snapshots, Evidence, and Verdicts?</h2>
<p>ACS works by combining a policy manifest with runtime snapshots, evidence annotations, and policy verdicts at standardized checkpoints. In Microsoft&rsquo;s Agent Governance Toolkit documentation, the manifest shape includes version metadata, inheritance through <code>extends</code>, named policies, intervention points, tool metadata, annotators, and approval configuration. The agent host calls the policy engine at a defined checkpoint and passes a structured input: intervention point, policy target, runtime snapshot, available annotations, and tool context. Evidence providers can attach classifications such as sensitive data, destination risk, user role, or provenance labels. The policy engine evaluates those inputs and returns a decision the host must enforce. This is intentionally stateless from the policy engine&rsquo;s point of view: the host owns the live runtime context and provides it on each check. The takeaway: ACS separates policy decision logic from agent framework code without ignoring runtime context.</p>
<h3 id="what-does-a-policy-manifest-usually-contain">What does a policy manifest usually contain?</h3>
<p>A policy manifest usually contains metadata, policy names, enforcement points, tool descriptions, evidence providers, and approval rules. In practice, I would expect a security team to own the shared baseline manifest while product teams extend it for local tools. For example, a finance agent might inherit the company DLP policy, then add a stricter <code>pre_tool_call</code> rule for wire-transfer APIs and customer export tools.</p>
<h2 id="what-are-the-eight-acs-intervention-points">What Are the Eight ACS Intervention Points?</h2>
<p>The eight ACS intervention points are the lifecycle checkpoints where an agent host can ask a policy engine for a governance decision: <code>agent_startup</code>, <code>input</code>, <code>pre_model_call</code>, <code>post_model_call</code>, <code>pre_tool_call</code>, <code>post_tool_call</code>, <code>output</code>, and <code>agent_shutdown</code>. The most important number here is eight because it makes ACS more precise than a generic &ldquo;before and after&rdquo; guardrail. <code>agent_startup</code> can verify identity, configuration, and policy version before work begins. <code>input</code> can classify the user&rsquo;s request. <code>pre_model_call</code> and <code>post_model_call</code> can control model routing and inspect generated plans. <code>pre_tool_call</code> is the critical gate before real-world side effects. <code>post_tool_call</code> can inspect tool results and update evidence. <code>output</code> governs what leaves the system, and <code>agent_shutdown</code> records final state. The takeaway: ACS maps governance to the actual lifecycle of an autonomous task.</p>
<table>
	<thead>
			<tr>
					<th>Intervention point</th>
					<th>What it controls</th>
					<th>Example policy</th>
			</tr>
	</thead>
	<tbody>
			<tr>
					<td><code>agent_startup</code></td>
					<td>Agent identity, configuration, policy version</td>
					<td>Deny startup if policy bundle is missing</td>
			</tr>
			<tr>
					<td><code>input</code></td>
					<td>User request and task scope</td>
					<td>Warn on regulated-data requests</td>
			</tr>
			<tr>
					<td><code>pre_model_call</code></td>
					<td>Model selection and prompt context</td>
					<td>Deny sending secrets to external models</td>
			</tr>
			<tr>
					<td><code>post_model_call</code></td>
					<td>Plans and generated arguments</td>
					<td>Escalate if the plan includes deletion</td>
			</tr>
			<tr>
					<td><code>pre_tool_call</code></td>
					<td>Tool execution before side effects</td>
					<td>Require approval for payment API calls</td>
			</tr>
			<tr>
					<td><code>post_tool_call</code></td>
					<td>Tool results and returned data</td>
					<td>Redact sensitive fields from retrieved rows</td>
			</tr>
			<tr>
					<td><code>output</code></td>
					<td>Final user-visible response</td>
					<td>Block customer data leakage</td>
			</tr>
			<tr>
					<td><code>agent_shutdown</code></td>
					<td>Final audit and cleanup</td>
					<td>Persist decision trace without raw content</td>
			</tr>
	</tbody>
</table>
<h3 id="which-intervention-point-matters-most">Which intervention point matters most?</h3>
<p><code>pre_tool_call</code> matters most for production risk because it is the last reliable checkpoint before an agent changes an external system. I treat it like an authorization hook with richer context: who asked, what tool is being called, what arguments were generated, what data was used, and whether approval exists. <code>post_tool_call</code> is the second checkpoint I would prioritize because it catches sensitive results before they propagate.</p>
<h2 id="what-can-acs-policies-decide">What Can ACS Policies Decide?</h2>
<p>ACS policies can decide whether an agent action should be allowed, warned, denied, escalated, or transformed depending on host and policy-engine support. Microsoft describes core verdicts such as allow, warn, deny, and escalate, while practical governance systems often add transformations like redaction, argument rewriting, or destination narrowing. The value is not the vocabulary itself; it is the consistent enforcement contract. A <code>pre_tool_call</code> rule might allow a read-only CRM lookup, warn on a broad export, deny a request that includes social security numbers, and escalate a refund over $500 to a human approver. A <code>post_tool_call</code> rule might allow a database result but transform it by redacting fields before the model sees them. The takeaway: ACS turns governance from advice into enforceable runtime decisions tied to specific agent actions.</p>
<h3 id="should-policies-fail-open-or-fail-closed">Should policies fail open or fail closed?</h3>
<p>Policies should fail closed for high-impact actions and explicitly declare any fail-open exceptions for low-risk reads. If an approval service, annotator, or policy engine is unavailable, a payment, deletion, email, or production deployment should stop. For a low-risk documentation lookup, warning and telemetry may be acceptable. The decision should be encoded in the manifest, not left to whatever behavior the SDK adapter happens to implement.</p>
<h2 id="how-does-acs-compare-with-prompt-guardrails-framework-callbacks-mcp-and-observability">How Does ACS Compare With Prompt Guardrails, Framework Callbacks, MCP, and Observability?</h2>
<p>ACS refers to runtime policy governance, while prompt guardrails, framework callbacks, MCP, and observability each solve adjacent but different problems. Prompt guardrails shape model behavior but are weak against tool misuse and jailbreak pressure. Framework callbacks in LangChain, AutoGen, CrewAI, Semantic Kernel, or custom orchestration code can enforce controls, but they are usually framework-specific. MCP standardizes tool access, not enterprise policy decisions across every agent lifecycle checkpoint. Observability tools record traces, spans, state transitions, approvals, and failures, but recording an unsafe action is not the same as stopping it. ACS sits between these layers as the decision contract: it tells the host where to ask, what evidence to provide, and what verdict to enforce. The takeaway: ACS complements existing agent infrastructure rather than replacing prompts, tool protocols, or telemetry.</p>
<table>
	<thead>
			<tr>
					<th>Layer</th>
					<th>Primary job</th>
					<th>ACS relationship</th>
			</tr>
	</thead>
	<tbody>
			<tr>
					<td>Prompt guardrails</td>
					<td>Influence model behavior</td>
					<td>Useful signal, not enough enforcement</td>
			</tr>
			<tr>
					<td>Framework callbacks</td>
					<td>Hook into one runtime</td>
					<td>Possible ACS adapter surface</td>
			</tr>
			<tr>
					<td>MCP</td>
					<td>Standardize tool connections</td>
					<td>Tool metadata can feed ACS policies</td>
			</tr>
			<tr>
					<td>Observability</td>
					<td>Trace what happened</td>
					<td>ACS decisions should be logged there</td>
			</tr>
			<tr>
					<td>ACS</td>
					<td>Decide and enforce policy</td>
					<td>Governance contract across runtimes</td>
			</tr>
	</tbody>
</table>
<h3 id="why-not-just-use-framework-callbacks">Why not just use framework callbacks?</h3>
<p>Framework callbacks are useful when one team owns one stack, but they do not scale cleanly across mixed agent environments. A company may have LangChain prototypes, OpenAI Agents SDK services, Semantic Kernel workflows, and vendor agents running at the same time. Without a shared contract, each team invents slightly different policy hooks, logs, and approval behavior. ACS gives security and platform teams a common target even when application teams choose different frameworks.</p>
<h2 id="where-does-acs-fit-in-microsofts-agent-governance-toolkit-and-foundry-stack">Where Does ACS Fit in Microsoft&rsquo;s Agent Governance Toolkit and Foundry Stack?</h2>
<p>ACS fits into Microsoft&rsquo;s broader agent governance stack as the runtime control layer, alongside evaluation and validation tools such as ASSERT and platform services in Microsoft Foundry. Microsoft Foundry&rsquo;s 2026 framing describes a loop: use ASSERT to identify policy failures, apply ACS controls at runtime checkpoints, then rerun evaluation to validate that the agent behaves better. That loop matters because governance is not a single review before launch. A team can test a procurement agent, find that it attempts unapproved supplier changes, add a <code>pre_tool_call</code> escalation policy, and rerun the evaluation suite. The Agent Governance Toolkit positions ACS as the decision-runtime core behind policy evaluation, with SDK surfaces documented for Python, Node.js, .NET, and Rust. The takeaway: ACS is most useful when paired with repeatable evaluation and production telemetry.</p>
<h3 id="what-role-does-telemetry-play">What role does telemetry play?</h3>
<p>Telemetry turns ACS from a blocking mechanism into an audit system. The useful records are not raw prompts or private outputs; they are policy IDs, reason codes, verdicts, intervention points, annotator results, durations, approval identifiers, and error classes. Microsoft&rsquo;s ACS documentation emphasizes content-redacted telemetry by default. That is the right default because security teams need evidence of control without creating a new sensitive-data warehouse in the observability backend.</p>
<h2 id="which-framework-and-sdk-support-should-developers-watch">Which Framework and SDK Support Should Developers Watch?</h2>
<p>Developers should watch ACS support across agent frameworks, SDKs, tool protocols, policy engines, and observability vendors because the specification only becomes valuable when hosts enforce it consistently. TechCrunch reported SDK or plugin support around the ACS launch for LangChain, OpenAI Agents SDK, Anthropic Agents SDK, AutoGen, CrewAI, Semantic Kernel, Microsoft.Extensions.AI, MCP tools, and related ecosystems. Microsoft also named partners across governance, security, observability, and frameworks, including Arize AI, IBM, CrewAI, KPMG, and Zscaler. The specific vendor list will change, but the evaluation criteria should not. Ask which intervention points are implemented, whether tool metadata is complete, whether policy failures are fail-closed, and whether decision telemetry is exported in a usable format. The takeaway: ACS compatibility is only meaningful when enforcement depth is documented.</p>
<h3 id="what-should-an-sdk-adapter-prove">What should an SDK adapter prove?</h3>
<p>An SDK adapter should prove that it calls the policy engine at the promised intervention points and enforces the returned verdict. For <code>pre_tool_call</code>, that means the tool does not run after a deny verdict. For escalation, it means the agent blocks until the approval workflow returns a valid decision. For telemetry, it means every decision has a durable trace with a policy ID and reason code.</p>
<h2 id="what-enterprise-use-cases-benefit-most-from-acs">What Enterprise Use Cases Benefit Most From ACS?</h2>
<p>Enterprise ACS use cases benefit most when agents have tools, data access, side effects, or regulatory obligations. PwC&rsquo;s 2025 survey found 79% of senior executives said AI agents were already being adopted in their companies, and 88% planned to increase AI-related budgets because of agentic AI. Those agents are moving into workflows where &ldquo;just trust the prompt&rdquo; is not a serious control. Tool permissions are the first use case: restrict which APIs an agent can call for a user, tenant, or task. Data loss prevention is second: classify inputs, retrieved documents, and outputs before information leaves a boundary. Human approval is third: route high-risk actions to named approvers. Audit trails are fourth: preserve redacted decision evidence for compliance reviews. The takeaway: ACS is strongest where autonomous action meets enterprise accountability.</p>
<h3 id="what-would-this-look-like-in-a-finance-workflow">What would this look like in a finance workflow?</h3>
<p>In a finance workflow, ACS could allow an agent to read invoices, warn when a supplier record is incomplete, require human approval for a payment above $500, and deny any transfer to an unverified bank account. The manifest would define the tool sinks, approval rules, and evidence required for each decision. The audit log would show the policy version, approver, verdict, and reason code without storing the full invoice text.</p>
<h3 id="what-would-this-look-like-in-a-developer-workflow">What would this look like in a developer workflow?</h3>
<p>In a developer workflow, ACS could let an agent read repository files, require approval before opening a pull request, deny production secret access, and block destructive database commands. A <code>post_tool_call</code> rule could redact credentials returned by a shell tool before the model sees them. This is especially relevant for coding agents because their power comes from tool access, not from chat alone.</p>
<h2 id="what-are-the-limitations-and-implementation-risks">What Are the Limitations and Implementation Risks?</h2>
<p>ACS has limitations because a specification does not enforce itself; the host runtime, SDK adapter, approval service, evidence providers, telemetry pipeline, and surrounding infrastructure must all behave correctly. Rubrik&rsquo;s 2026 survey found that 86% of IT and security leaders expect AI agents to outpace their organization&rsquo;s security guardrails within a year, and only 23% report full visibility into agents operating in their environments. ACS can reduce that gap, but it cannot fix invisible agents, missing identity, weak tool authorization, or network paths that bypass the governed host. A compromised runtime could fail to call the policy engine. An incomplete adapter could skip <code>post_tool_call</code>. A noisy annotator could create false confidence. The takeaway: ACS is a governance contract, not a substitute for defense in depth.</p>
<h3 id="why-might-network-enforcement-still-matter">Why might network enforcement still matter?</h3>
<p>Network enforcement still matters because an agent runtime may not be the only control point between generated intent and external systems. Aviatrix argues that ACS-style policy can be compiled into multicloud network controls across AWS, Azure, Google Cloud, and Kubernetes. I would not treat network controls as a replacement for ACS, but they are valuable backstops for destination controls, protocol restrictions, and blast-radius reduction when runtime enforcement fails.</p>
<h2 id="how-should-teams-evaluate-an-acs-compatible-vendor-or-agent-platform">How Should Teams Evaluate an ACS-Compatible Vendor or Agent Platform?</h2>
<p>Teams should evaluate an ACS-compatible vendor by testing enforcement behavior, not by accepting a badge or launch announcement. Grant Thornton&rsquo;s 2026 AI Impact Survey found 78% of C-suite and senior business leaders lack strong confidence they could pass an independent AI governance audit within 90 days, which means evidence quality matters as much as feature claims. Start with a matrix of the eight intervention points and ask the vendor to show which are supported today. Then test fail-closed behavior, approval workflows, redacted telemetry, policy versioning, reason codes, annotator quality, and integration with your identity provider. Run adversarial cases: overbroad exports, unapproved recipients, sensitive tool arguments, malformed tool metadata, and unavailable policy services. The takeaway: ACS procurement should be based on observable enforcement and audit evidence.</p>
<table>
	<thead>
			<tr>
					<th>Evaluation question</th>
					<th>Strong answer</th>
					<th>Weak answer</th>
			</tr>
	</thead>
	<tbody>
			<tr>
					<td>Which intervention points are enforced?</td>
					<td>Documented per SDK and tested</td>
					<td>&ldquo;We support guardrails&rdquo;</td>
			</tr>
			<tr>
					<td>What happens on policy engine failure?</td>
					<td>Fail-closed for high-risk actions</td>
					<td>Runtime-specific default</td>
			</tr>
			<tr>
					<td>Is telemetry content-redacted?</td>
					<td>Decision metadata without raw content</td>
					<td>Full prompts in logs</td>
			</tr>
			<tr>
					<td>How are approvals recorded?</td>
					<td>Named approver, reason, timestamp</td>
					<td>Chat message or manual note</td>
			</tr>
			<tr>
					<td>Can policies be versioned?</td>
					<td>Immutable versions in traces</td>
					<td>Mutable config only</td>
			</tr>
	</tbody>
</table>
<h3 id="what-is-the-fastest-proof-of-control-test">What is the fastest proof-of-control test?</h3>
<p>The fastest proof-of-control test is to create a policy that denies one harmless but observable tool call, then verify that the tool does not execute, the agent receives a clear denial, and telemetry records the intervention point, policy ID, reason code, and verdict. Repeat the same test for an approval path and a policy-engine outage. This catches shallow integrations quickly.</p>
<h2 id="how-can-developers-and-security-teams-adopt-acs-practically">How Can Developers and Security Teams Adopt ACS Practically?</h2>
<p>Developers and security teams can adopt ACS practically by starting with high-risk tool calls, not by trying to model every possible agent behavior on day one. McKinsey&rsquo;s 2026 AI trust research reports that 74% of respondents identify inaccuracy and 72% cite cybersecurity as highly relevant AI risks, while active mitigation still trails awareness. A realistic first rollout should cover <code>pre_tool_call</code> and <code>output</code> for one production agent. Define tool metadata, classify data boundaries, choose fail-closed actions, and log redacted decision metadata. Add human approval for irreversible operations. Then expand to <code>input</code>, <code>post_tool_call</code>, and model-routing policies. Pair ACS policies with evaluation suites, incident reviews, and regular policy version audits. The takeaway: ACS adoption works best as an incremental control program tied to real agent risk.</p>
<ol>
<li>Inventory production and pilot agents with tool access.</li>
<li>Classify tools by read-only, write, financial, destructive, external communication, and regulated-data impact.</li>
<li>Add <code>pre_tool_call</code> policies for the highest-risk tools first.</li>
<li>Add <code>output</code> controls for sensitive data leaving the system.</li>
<li>Configure fail-closed behavior for irreversible actions.</li>
<li>Route escalations to named human approval systems.</li>
<li>Export redacted decision telemetry to the existing observability stack.</li>
<li>Rerun agent evaluations after every material policy change.</li>
</ol>
<h3 id="what-should-the-first-acs-policy-cover">What should the first ACS policy cover?</h3>
<p>The first ACS policy should cover a tool whose misuse would be obvious and costly but easy to test safely. Good candidates are outbound email, customer export, payment initiation, production deployment, and database mutation tools. Define who can call it, under what task scope, with which argument constraints, and when approval is required. Avoid starting with vague behavior rules; start with concrete tool boundaries.</p>
<h2 id="faq-agent-control-specification-and-ai-agent-governance">FAQ: Agent Control Specification and AI Agent Governance</h2>
<p>Agent Control Specification FAQ answers should focus on what ACS does at runtime, how it relates to existing agent stacks, and what teams should verify before trusting it in production. The most important practical fact is that ACS defines eight governance checkpoints and structured policy verdicts, but each implementation still depends on host enforcement. That distinction answers most buyer and developer questions. ACS is not a new model, not a replacement for MCP, and not a complete compliance program by itself. It is a portable decision contract that can make policies reusable across frameworks, approvals auditable, and tool actions controllable. Teams adopting ACS should start by testing whether deny and escalation decisions actually stop risky behavior in their agent runtime. The takeaway: ACS is useful when it produces enforceable decisions and durable evidence.</p>
<h3 id="is-acs-only-for-microsoft-agent-platforms">Is ACS only for Microsoft agent platforms?</h3>
<p>ACS is not only for Microsoft agent platforms. Microsoft introduced and documents much of the current ecosystem, but the specification is framed as open and vendor-neutral. Its value depends on support from non-Microsoft frameworks, SDKs, observability systems, policy engines, and security vendors. A team should still verify whether a given integration supports the intervention points it needs.</p>
<h3 id="does-acs-replace-mcp">Does ACS replace MCP?</h3>
<p>ACS does not replace MCP. MCP standardizes how agents connect to tools and external resources, while ACS standardizes how runtime policy decisions can be made around agent behavior. In a mature architecture, MCP tool metadata can become evidence for ACS decisions, and ACS can decide whether a specific MCP tool call should proceed.</p>
<h3 id="can-acs-stop-prompt-injection">Can ACS stop prompt injection?</h3>
<p>ACS can reduce the blast radius of prompt injection, but it cannot eliminate prompt injection by itself. If a malicious document convinces an agent to send data to an external endpoint, a <code>pre_tool_call</code> or <code>output</code> policy can deny the action. The prompt injection still occurred; ACS helps stop or contain the harmful side effect.</p>
<h3 id="what-teams-should-own-acs-policies">What teams should own ACS policies?</h3>
<p>ACS policies should be jointly owned by platform engineering, security, compliance, and the application team that understands the workflow. Security can define baseline controls, but application owners know which tools are normal for the task. The best operating model is shared policy-as-code review with versioned manifests and production telemetry.</p>
<h3 id="is-acs-ready-for-regulated-industries">Is ACS ready for regulated industries?</h3>
<p>ACS is relevant to regulated industries, but readiness depends on the implementation. Finance, healthcare, HR, legal, and public-sector teams need identity integration, approval records, policy versioning, evidence retention, redacted telemetry, and independent auditability. ACS can help structure those controls, but auditors will evaluate the full system, not the specification alone.</p>
]]></content:encoded></item></channel></rss>